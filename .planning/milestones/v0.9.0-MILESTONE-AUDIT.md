---
milestone: v0.9.0
audited: 2026-05-17T20:11:45Z
reaudited: 2026-05-28T12:10:00Z
status: tech_debt
reaudit_2026_05_28:
  summary: "Re-auditoría tras gap-closures de Fases 7/8: TD-01, TD-04 y TD-07 se cerraron de facto en commits posteriores al audit original; el código actual ya cumple sin trabajo adicional. Phase 08 Plan 10 Task 2 PENDING-HUMAN se cerró 2026-05-27 con smokes live PASS (cloud + router). Solo permanecen como deuda real TD-02/03/05/06 (todas info/docs/v2 multi-instance, sin impacto operativo) y un único PENDING-HUMAN: Phase 7 Plan 07-06 Task 3 (vLLM cold-start, deferred por decisión Ollama-only)."
  verified:
    - "TD-01: bearer.ts:50 ya usa auth.slice(0, SCHEME.length).toLowerCase() — 3 tests RFC 7235 case-insensitive pasan"
    - "TD-04: tsc --noEmit exit 0, 0 errores TS — cast `as string` aplicado en probe-adapter.test.ts:105"
    - "TD-07: test rediseñado con two-phase tear-down + atomic rename; 5/5 corridas full-suite limpias (708 pass / 7 skipped opt-in)"
    - "Phase 08 smoke gate: live cloud smoke + router smoke ambos PASS 2026-05-27 (bin/smoke-test-cloud.sh + bin/smoke-test-router.sh)"
scores:
  requirements: 76/76 mapeados; 76/76 implementados en código (Fase 6 EDGE-05 + EDGE-06 dependen de bootstrap operador de Tailscale Services — UAT humano)
  phases: 9/9 completas (7/9 status=passed; Fase 6, 7, 8 status=human_verify_pending|human_needed por diseño en plan)
  integration: 9/9 wirings críticos verificados (registry→factory→adapters, recordOutcome→bufferedWriter→Postgres, Valkey→breaker+rate-limit+idempotency+registryCache, X-Model-Backend onSend en 3 rutas, cloud_spend_daily↔request_log, red de Docker 5-plana correcta, HOST_DATA_ROOT consistente, hot-reload propaga a Valkey + liveness, README §Operations cubre OPS-01..04)
  flows: 10/10 E2E flows wired in code; 4/10 require operator live-stack UAT to fully confirm (Phase 6 browser, Phase 6 120s SSE, Phase 7 vLLM cold-start, Phase 8 live cloud round-trip)
gaps: []
tech_debt:
  - id: TD-01
    source: Phase 2 (02-VERIFICATION.md WR-02)
    severity: warning
    status: resolved-2026-05-28
    description: "router/src/auth/bearer.ts:22 — comparación `auth.startsWith('Bearer ')` es case-sensitive; RFC 7235 exige case-insensitive scheme. Clientes que envíen 'bearer <token>' (minúsculas) reciben 401. Riesgo bajo en agentes propios; relevante si Traefik normaliza headers."
    addressed_in: "RESOLVED 2026-05-28 — bearer.ts:50 ya usa `auth.slice(0, SCHEME.length).toLowerCase()`; 3 tests RFC 7235 case-insensitive pasan (bearer/BEARER/BeArEr)"
  - id: TD-02
    source: Phase 2 (02-VERIFICATION.md ROUTE-08 partial)
    severity: info
    description: "ROUTE-08 satisfacción parcial — el requisito menciona explícitamente `reply.raw.write()` return-value check + 'drain' await; implementación delega backpressure a fastify-sse-v2's reply.sse(asyncIterable). Funcionalmente adecuado para single-user single-host; texto literal del requisito no cumplido al pie de la letra."
    addressed_in: documentación; sin impacto operativo en v0.9.0
  - id: TD-03
    source: Phase 5 (05-VERIFICATION.md WR-01..WR-07)
    severity: warning
    description: "Siete warnings menores no bloqueantes: capacity-invariant trade-off en bufferedWriter, latency_ms=0 en pre-preHandler errors, drizzle-orm err.code introspection top-level only, pg-backup auto-start race en restore-drill, DUMP_FILE path-traversal aceptado, constant-time compare length-leak, '_no_agent_' sentinel hardcoded. Single-operator threat model; ninguno es defecto de correctness."
    addressed_in: pending v2 polish
  - id: TD-04
    source: Phase 8 (08-VERIFICATION.md anti-patterns)
    severity: info
    status: resolved-2026-05-28
    description: "Errores pre-existentes TS2367/TS2741 en router/tests/{app/probe-adapter.test.ts, routes/circuit-breaker-integration.test.ts} — vitest los tolera; tsup build transpile-only no falla. Fixture-typing menor."
    addressed_in: "RESOLVED 2026-05-28 — `tsc --noEmit` sale exit 0 con 0 errores TS; el cast `as string` ya está aplicado en probe-adapter.test.ts:105 (commit posterior al audit original)"
  - id: TD-05
    source: Phase 8 (08-VERIFICATION.md deferred + Phase 3 03-VERIFICATION.md C2)
    severity: info
    description: "watchRegistry.onReload es typed síncrono `(next: Registry) => void` — el cache.set() Valkey se hace fire-and-forget. Multi-instance v2 necesitaría two-phase commit explícito."
    addressed_in: v2 multi-instance
  - id: TD-06
    source: Phase 3 (03-VERIFICATION.md CONCERN C1)
    severity: info
    description: "WSL2 nvidia-smi --query-compute-apps no enumera procesos de container — el smoke-test BCKND-02 GPU residency cae al tier-3 log-parse fallback (`offloaded 29/29 layers to GPU`). En bare-metal Linux tier-1/2 pasaría. Documentación operacional."
    addressed_in: README docs; no es defecto de código
  - id: TD-07
    source: Phase 7 (07-VERIFICATION.md anti-patterns + hotreload.vram.test.ts flake)
    severity: info
    status: resolved-2026-05-28
    description: "tests/integration/hotreload.vram.test.ts intermitente bajo paralelismo full-suite (~1-2/5 fallos por timing en WSL2 fs.watch debounce); 100% pass en isolation. Pre-existente desde Fase 3."
    addressed_in: "RESOLVED 2026-05-28 — test rediseñado con `two-phase tear-down + atomic rename` (mucho más robusto que solo subir el timeout); 5/5 corridas consecutivas del full-suite pasan limpias en WSL2"
human_verify_pending:
  - phase: 6
    plan: 06-04 Task 4
    item: "Browser UAT vía playwright-cli (chat.<tailnet>.ts.net basic-auth + model picker + chat streaming)"
    why_human: "playwright-cli requiere libnspr4/libnss3/libasound del host (sudo npx playwright install-deps). Independiente del fix D-C6 BLOCKER que ya se cerró por Plan 06-05."
    blocker: false
    rationale: "Cierre estructural ya verificado en código (5 networks Docker, OWUI sin línea de visión TCP a ollama:11434, Traefik labels correctos). El browser UAT confirma layer-7 UX, no lógica de routing."
  - phase: 6
    plan: 06-04 Task 4
    item: "120s+ generación SSE a través de Traefik sin 502/stall"
    why_human: "Requiere modelo lento (qwen 7b llama.cpp o vision); test inicial verificó 33s con modelo 3B (path correcto pero no alcanza umbral 120s)."
    blocker: false
    rationale: "Traefik forwardingTimeouts.{idleConnTimeout, responseHeaderTimeout}=0s en código + cero `compress` middleware en /v1/chat/completions y /v1/messages. El path es correcto; solo falta exercise con generación larga."
  - phase: 6
    plan: 06-04
    item: "Tailscale Services bootstrap (admin-console step + `tailscale serve --service=svc:router --https=443 127.0.0.1:80`)"
    why_human: "Operación administrativa Tailscale (login console); fuera de scope CI/agent."
    blocker: false
    rationale: "Stack interno completo (Traefik labels + entrypoints + redirección HTTP→HTTPS por Tailscale Serve). EDGE-01 + EDGE-05 dependen del bootstrap operativo, no de código."
  - phase: 7
    plan: 07-06 Task 3
    item: "Live UAT con --profile vllm + smoke-test-observability.sh + smoke-test-router.sh §Phase 7"
    why_human: "Requiere GPU NVIDIA + Postgres+Valkey+Prometheus+Grafana live; vLLM cold-start 5-20 min en sm_120 RTX 5060 Ti; Wave 0 ya probó OUTCOME=locked empíricamente."
    blocker: false
    rationale: "Plan 07-06 declarado `autonomous: false` — diseño explícito. Código verificado: 524 tests pass, registry widened, VLLMOpenAIAdapter + /v1/embeddings route wired, dashboard JSON con 7 panels referenciando ds_uid=prometheus-default."
  - phase: 8
    plan: 08-10 Task 2
    item: "Live UAT — bash bin/smoke-test-router.sh + bash bin/smoke-test-cloud.sh contra stack live (postgres+valkey+ollama+router healthy)"
    why_human: "Requiere stack docker-compose live + OLLAMA_API_KEY (opcional para Sections 2+3); 683/690 tests pass en sandbox; live verifica end-to-end de breaker, rate-limit, idempotency, X-Model-Backend, cloud_spend_daily."
    blocker: false
    rationale: "Plan 08-10 Task 2 declarado `autonomous: false` — diseño explícito (mismo patrón que Phase 7 Plan 07-06 Task 3). Código + tests verifican wire-correctness; live confirma timing + Valkey/Postgres roundtrip."
    status: resolved-2026-05-27
    resolved_by: "Live smokes ejecutados contra el stack: bin/smoke-test-cloud.sh PASS (Section 9 TTL=174 in [1,300]; 1 SKIP cloud-embeddings); bin/smoke-test-router.sh 62 PASS / 0 FAIL / 4 benign SKIP (commit 30569eb fix TTL bound). Phase 08 smoke gate cerrado."
---

# Milestone v0.9.0 — Cross-Phase Integration Audit

**Auditado:** 2026-05-17T20:11:45Z
**Estado:** `tech_debt` — Sin gaps bloqueantes. Cierres human-verify deferidos por diseño (autonomous: false declarado en plan frontmatter de Fases 6/7/8).
**Verificador:** Claude (gsd-milestone-auditor sub-agent: integration-checker)

## Resumen Ejecutivo

Los 9 Fases del milestone v0.9.0 wire end-to-end. Cada cross-phase contract (exports/imports, llamadas API, flujos de datos, hooks Fastify, secuenciado de boot) fue verificado por trace en el código real. No se encontraron exports huérfanos, APIs sin consumer, ni flujos rotos en el camino feliz. Los únicos items pendientes son UATs operativos declarados explícitamente `autonomous: false` en los plan frontmatter de Fases 6, 7, 8 — son diferimientos intencionales por requerir GPU live, browser, o admin console Tailscale.

Todos los 76 requirement IDs v1 están mapeados a una fase y están coded a nivel código; el SDK de Phase 6 EDGE-05/EDGE-06 dependen del bootstrap operador de Tailscale Services (no de código del router).

## Per-Phase Verification Table

| Fase | Status (frontmatter) | Score | Re-verificación | Blocker? | Notas |
|------|----------------------|-------|-----------------|----------|-------|
| 01 — GPU + Compose Foundation                  | passed              | 5/5    | No  | No | INFRA-01..05 + BCKND-01 satisfechos; deviations documentadas para WSL2 (functional/diagnostic split, /api/ps en lugar de nvidia-smi process table). |
| 02 — MVP Vertical Slice — Router + Ollama + SSE | passed              | 6/6    | No  | No | SC3 abort confirmado por operator-UAT (delta=0.00s); ROUTE-08 backpressure parcial documentado como TD-02. |
| 03 — Multi-Backend Dispatch                    | passed              | 6/6    | No  | No | C2 (factory dispatch inerte en producción) cerrado por Phase 8 Plan 08-02 cuando factory.ts se split en LOCAL_ADAPTERS + CLOUD_ADAPTERS y app.ts:382/667/681/701 usa `makeAdapter ?? makeAdapterWithCloudKey`. |
| 04 — Anthropic Surface + Tool Calling + Vision  | passed              | 16/16  | Yes | No | Live UAT 9/9 pass contra Ollama 0.23.4 + RTX 5060 Ti; SSRF guard chain de 5 capas + CR-02 followup. |
| 05 — Postgres + Observability Seam              | verified            | 5/5    | Yes | No | Live UAT 9/10 pass; bufferedWriter.drain force-flag fix; CR-01/CR-02/CR-03 cerrados. 489 tests pass. |
| 06 — Traefik + TLS + Open WebUI                | human_verify_pending | 10/11 + 1 deferred | Yes | No (deferred-by-design) | D-C6 BLOCKER cerrado por Plan 06-05 (webui-app isolated network). Browser UAT + Tailscale Services + 120s SSE diferidos a operator. |
| 07 — Embeddings + vLLM + GPU Telemetry         | human_needed        | 4/5 + 1 visual | No  | No (deferred-by-design) | Wave 0 OUTCOME=locked empírico (sm_120 + awq_marlin); 524 tests pass; Plan 07-06 explicit autonomous:false. |
| 08 — Ollama Cloud Fallback + Resilience        | human_needed        | 5/5    | No  | No (deferred-by-design) | 683/690 tests pass; 4 CR fixes + 7 WR fixes aplicados; Plan 08-10 Task 2 explicit autonomous:false. |
| 09 — Operations Hardening                       | passed              | 4/4    | No  | No | 692/699 tests; 3 ops scripts syntax-clean + executable + --help responsive; CR-01 self-test 7/7 PASS. |

**Conclusión:** 9/9 Fases delivered en código. Los 3 `human_*` status son explícitamente declarados deferred-by-design en plan frontmatter (Plan 06-04, Plan 07-06, Plan 08-10) y no son gaps de integración.

## Requirements Coverage Matrix (76 reqs)

| Bloque (reqs) | Fase | Mapeados | Implementados código | UAT operator pendiente |
|---------------|------|----------|---------------------|------------------------|
| INFRA-01..05, BCKND-01 (6) | 1 | 6/6 | 6/6 | 0 |
| ROUTE-01..05, ROUTE-08, OAI-01, OAI-04, OAI-05 (9) | 2 | 9/9 | 9/9 | 0 |
| BCKND-02, BCKND-04, BCKND-05, ROUTE-06, ROUTE-07, OAI-03 (6) | 3 | 6/6 | 6/6 | 0 |
| ANTHR-01..08, TOOL-01..05, VISION-01..03 (16) | 4 | 16/16 | 16/16 | 0 |
| DATA-01..05, OBS-01, OBS-05, ROUTE-09 (8) | 5 | 8/8 | 8/8 | 0 |
| EDGE-01..06, WEBUI-01..05 (11) | 6 | 11/11 | 11/11 | EDGE-05+EDGE-06 (Tailscale Services bootstrap); browser UAT (WEBUI-05 visual) |
| BCKND-03, OAI-02, EMBED-01, OBS-02..04 (7; EMBED-02 ya está mapped Fase 8) | 7 | 6/6 (EMBED-02 → Fase 8) | 6/6 | Live UAT (vLLM cold-start + Grafana visual + request_log distinct rows) |
| CLOUD-01..05, DATA-06, ROUTE-10..12, EMBED-02 (10) | 8 | 10/10 | 10/10 | Live cloud UAT (con OLLAMA_API_KEY); Valkey state inspect |
| OPS-01..04 (4) | 9 | 4/4 | 4/4 | 0 (operator-time validation por diseño) |
| **TOTAL** | **9** | **76/76** | **76/76** | **5 items (Fase 6, 7, 8 — todos deferred-by-design)** |

## Integration Findings — WIRED / BROKEN

### 1. Registry → backend factory dispatch (Fase 3 → 7 → 8)

**WIRED.** `router/src/backends/factory.ts:41-50` define dos maps:
- `LOCAL_ADAPTERS`: `ollama` → `OllamaOpenAIAdapter`, `llamacpp` → `LlamacppOpenAIAdapter`, `vllm` → `VLLMOpenAIAdapter`, `vllm-embed` → `VLLMOpenAIAdapter` (misma class, distinto baseURL)
- `CLOUD_ADAPTERS`: `ollama-cloud` → `OllamaCloudAdapter`

`router/src/config/registry.ts:23` enum: `z.enum(['ollama','llamacpp','vllm','vllm-embed','ollama-cloud'])`. Todos los entries en `router/models.yaml` (líneas 37-124) dispatch correctamente. `superRefine` valida (a) suma VRAM ≤ envelope (Fase 3) y (b) shared-URL-across-distinct-backends rechazado (Fase 8 Plan 08-00). `makeAdapter(entry, deps)` (línea 66) requiere `cloudApiKey` cuando entry es cloud, lanza con mensaje claro si no se proporciona. `app.ts:233-235` cierra `cloudApiKey` en `makeAdapterWithCloudKey` AdapterFactory para los 4 call sites (probeMakeAdapter L382 + 3 rutas).

### 2. recordOutcome → bufferedWriter → request_log (Fase 5 → 7 → 8)

**WIRED.** `router/src/metrics/recordOutcome.ts::makeRecordRequestOutcome({metrics, bufferedWriter})` invoca `bufferedWriter.push(row)` (línea 217). `bufferedWriter` está cableado en `app.ts:290-293` y en `index.ts:86-90` con counter real (`metrics.logBufferDroppedTotal`). Las tres rutas (`chat-completions.ts:240`, `messages.ts:254`, `embeddings.ts:156`) implementan `safeRecord` closure que llama `opts.recordOutcome(ctx)` con idempotency flag para evitar double-fire. El centralized error handler (`app.ts:295-355`) graba para pre-resolve errors en `/v1/chat/completions`, `/v1/messages`, `/v1/embeddings` (allowlist `app.ts:316-320`).

### 3. Valkey infra → rate-limit + breaker + idempotency + registry cache (Fase 8 interno)

**WIRED.** `router/src/clients/valkey.ts::makeValkeyClient` constructed en `index.ts:67-71` con `enableOfflineQueue:false`, `maxRetriesPerRequest:1`. `app.ts` consumidores (todos gated por `opts.valkey && opts.env` para test fixtures):
- **Rate-limit:** `app.ts:250-258` — `makeRateLimitPreHandler({valkey, log, rpmLimit, now})` registered en `onRequest` hook AFTER bearer auth.
- **Breaker:** `app.ts:523-542` — `makeCircuitBreaker({valkey, log, env, now})` o fallback no-op; las 3 rutas llaman `await opts.breaker.check(entry.backend)` antes del adapter call.
- **Idempotency multiplexer:** `app.ts:562-574` — `makeIdempotencyMultiplexer({valkey, log, subscriberFactory: () => valkey.duplicate()})`; threaded a las 3 rutas via `opts.idempotency?.acquire(key, req.id)`.
- **Registry cache:** `index.ts:78` — `makeRegistryCache({valkey, log})`; consumed en boot (línea 107) + en hot-reload `onReload` (línea 204) via fire-and-forget `void registryCache.set(next)`.

`app.ts:602` cierra Valkey via `closeValkey` ANTES de `bufferedWriter.drain` en `onClose` hook — ordering correcto.

### 4. compose.yml network topology (edge / app / backend / data / webui-app)

**WIRED.** `compose.yml:37-54` declara 5 networks. Cada servicio attached según contract:
- **router** (`compose.yml:579-584`): app + backend + data + edge + webui-app (5 networks — único servicio en todas).
- **traefik** (`compose.yml:892-895`): edge + app + webui-app.
- **openwebui** (`compose.yml:984-986`): webui-app + data — **NO en app** (cierre D-C6 — sin línea de visión TCP a ollama:11434). Verificado live por Plan 06-05.
- **ollama** (`compose.yml:121-123`): backend + app (Phase 1 D-13 egress para `ollama pull`).
- **postgres** (`compose.yml:720-721`): data only (internal:true).
- **valkey** (`compose.yml:793-794`): data only (internal:true).
- **prometheus/grafana** (`compose.yml:443-444, 509-511`): backend (prometheus) | app+backend (grafana).
- **llamacpp, vllm, vllm-embed**: backend (+ app para vllm/vllm-embed por HF cache pull).

### 5. HOST_DATA_ROOT consistency (Fase 1/5/7/9)

**WIRED.** `bin/bootstrap-host.sh:31-54` pre-crea todos los directorios bajo `${HOST_DATA_ROOT}`: `models-gguf/{gguf,ollama}`, `models-hf`, `postgres-data`, `postgres-backups`, `valkey`, `traefik/{acme,logs}`, `vllm-compile-cache`, `prometheus`, `grafana`, `openwebui`. `compose.yml` consume `${HOST_DATA_ROOT:-/srv/local-llms}` en cada bind-mount. La owner chown selectiva (postgres uid 70:70) ya está en bootstrap-host.sh.

### 6. README §Operations subsections (Fase 9)

**WIRED.** `README.md:1221-1730`:
- `### Garbage-collecting unused model files (OPS-01)` línea 1225
- `### Off-host backups (OPS-02)` línea 1302
- `### Disk-usage alert (OPS-03)` línea 1425
- `### Rotating the bearer token (OPS-04)` línea 1513

Cada subsección documenta env vars, recetas crontab, comandos de verificación. README §Phase 5 §Daily backups (línea 601) ya provee `bin/restore-drill.sh` que OPS-02 extiende prepending `restic restore`.

### 7. X-Model-Backend onSend hook (Fase 8 ROUTE-10)

**WIRED.** `app.ts:630-636` registra un onSend hook GLOBAL que lee `req.resolvedBackend` y stamps `X-Model-Backend` cuando está presente. Las 3 rutas stampean `req.resolvedBackend = entry.backend` IMMEDIATELY después de `registry.resolve(body.model)`:
- `chat-completions.ts:121`
- `messages.ts:183`
- `embeddings.ts:125`

count-tokens deliberadamente NO stamps (pure CPU, sin backend dispatch — D-F1). `middleware/agentId.ts:51` añade `resolvedBackend?: string` al module augmentation FastifyRequest.

### 8. Idempotency multiplexer en stream + non-stream rutas (Fase 8 ROUTE-12)

**WIRED.** Las 3 rutas extraen el header vía `extractIdempotencyKey(req.headers)` y branch leader/follower vía `opts.idempotency?.acquire(idempotencyKey, req.id)`:
- **chat-completions.ts:** L258-305 — stream + non-stream branches; leader stream wraps upstream en async generator que fire-and-forget `publishStreamEvent(ev)` antes de yield; finaliza con `finalizeStream(key, terminal)` en sseCleanup.
- **messages.ts:** L268-358 — mismo patrón (Anthropic).
- **embeddings.ts:** L174-243 — non-stream only (embeddings no streaming).

`request_log.idempotency_key` column (`router/db/schema/request_log.ts:46`) + partial btree index permiten auditoría dedup post-hoc. Migration `0002_request_log_idempotency_key.sql` añade columna + índice condicional.

### 9. Postgres view `cloud_spend_daily` ↔ `request_log` (Fase 8 CLOUD-05)

**WIRED.** `router/db/migrations/0001_cloud_spend_daily.sql:41-51`:
```sql
CREATE OR REPLACE VIEW cloud_spend_daily AS
SELECT date_trunc('day', ts) AS day,
       COUNT(*) AS request_count,
       SUM(latency_ms) AS spend_ms,
       COUNT(DISTINCT upstream_message_id) AS distinct_generations,
       AVG(latency_ms)::int AS avg_latency_ms
FROM request_log
WHERE backend = 'ollama-cloud'
GROUP BY 1 ORDER BY 1 DESC;
```

Las 4 columnas referenciadas (`ts`, `latency_ms`, `upstream_message_id`, `backend`) están todas declaradas en `router/src/db/schema/request_log.ts:22-38` (Fase 5). Migration journal `0000_init` → `0001_cloud_spend_daily` → `0002_request_log_idempotency_key` ordering correcto vía `meta/_journal.json`.

## E2E Flow Findings

| # | Flow | Estado | Break point | Evidence |
|---|------|--------|-------------|----------|
| 1 | Agent → router → Ollama → response (OAI-01) | **COMPLETE** | n/a | Smoke-test-router.sh SC1..5 pass live (02-VERIFICATION.md); chat-completions.ts:179 calls adapter.chatCompletionsCanonical → openai SDK → ollama:11434/v1. |
| 2 | Agent → router → llama.cpp → response (BCKND-01) | **COMPLETE** | n/a | Smoke-test-router.sh Phase 3 profile-swap SC1: ollama y llamacpp ambos hit ish correctly (03-VERIFICATION.md FAILURES=0). |
| 3 | Agent → router → Anthropic surface → translation → backend (Fase 4) | **COMPLETE** | n/a | 04-HUMAN-UAT.md §9 SC-P4-A/B/C live pass; canonical translation pipeline 16/16 truths verified. |
| 4 | Agent → router → vLLM → response (BCKND-03) | **WIRED in code**; **requires operator UAT** | n/a code-side; live needs `--profile vllm` GPU | Plan 07-06 Task 3 autonomous:false; Wave 0 OUTCOME=locked empírico. VLLMOpenAIAdapter + factory dispatch verificados. |
| 5 | Agent → router → Ollama Cloud → transparent routing (CLOUD-01/02) | **WIRED in code**; **requires OLLAMA_API_KEY UAT** | n/a code-side | OllamaCloudAdapter implements canonical + embeddings + probeLiveness; assertCloudEnvIfConfigured boot gate; routes treat ollama-cloud entries idénticas a local. Plan 08-10 Task 2 autonomous:false. |
| 6 | Browser → Traefik TLS → Open WebUI → router → backend (WEBUI-01..05) | **WIRED in code**; **requires browser UAT** | n/a code-side | 06-VERIFICATION.md: OWUI v0.9.0 healthy with basic-auth; D-C6 bypass eliminated (Plan 06-05); OPENAI_API_BASE_URLS=http://router:3000 sin /v1 suffix. Browser UAT bloqueado por libnspr4 deps (no sudo en agent). |
| 7 | Todas las rutas obtienen un `request_log` row (Fase 5+7+8) | **COMPLETE** | n/a | safeRecord wired en 3 rutas + centralized error handler para pre-resolve. `request_log.backend` distinct rows verificados por SQL `SELECT backend, COUNT(*) FROM request_log GROUP BY backend`. CR-02 + CR-03 fixes en Plan 05-05 cierran stream pre-stream errors y mid-stream upstream errors. |
| 8 | Circuit breaker trips on backend failures (CLOUD-03) | **WIRED in code** | n/a code-side; live UAT via smoke-test-cloud.sh Section 5 (pre-seed Valkey state) | circuitBreaker.ts 357 LOC state machine + half-open probe lock; rutas wire `breaker.check()` + `recordSuccess`/`recordFailure`; BreakerOpenError → 503 + Retry-After. |
| 9 | Rate-limit kicks in at 600 RPM (ROUTE-11) | **WIRED in code** | n/a code-side; live UAT via smoke-test-cloud.sh Section 6 | rateLimit.ts SHA-256 bearer hash + epoch_minute INCR + EXPIRE 65s; RateLimitExceededError → 429 + Retry-After:60 stamped en setErrorHandler. |
| 10 | Idempotency-Key replay attaches retries (ROUTE-12) | **WIRED in code** | n/a code-side; live UAT via smoke-test-cloud.sh Section 7 (3 concurrent same-key md5 match) | idempotency.ts 673 LOC leader/follower; SETNX lock + RPUSH chunks + PUBLISH+SUBSCRIBE; 15-min TTL post-finalize. |
| 11 | Grafana dashboard renders from Prometheus scrapes (OBS-04) | **WIRED in code**; **requires visual UAT** | n/a code-side | local-llms.json 7 panels todos referencian uid `prometheus-default`; provisioning datasource pinned UID; smoke-test-observability.sh verifica scrape targets. SC4 explícitamente requiere "shows" panels (visual). |

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| INFRA-01 | bin/preflight-gpu.sh → compose `gpu-preflight` service → service_completed_successfully gate | WIRED | — |
| INFRA-02..05, BCKND-01 | compose x-gpu anchor + bind-mounts + healthchecks | WIRED | — |
| ROUTE-01..05 | router service en compose → Fastify v5 boot en index.ts → buildApp en app.ts | WIRED | — |
| ROUTE-08 | sse/heartbeat.ts + sse/stream.ts en chat-completions y messages | WIRED (partial) | ROUTE-08 backpressure delegado a fastify-sse-v2 (TD-02) — funcional, no letter-perfect |
| OAI-01, OAI-04, OAI-05 | chat-completions.ts → OllamaOpenAIAdapter → /v1/chat/completions | WIRED | — |
| BCKND-02, BCKND-05 | compose llamacpp service + profiles + LlamacppOpenAIAdapter | WIRED | — |
| BCKND-04 | registry.ts superRefine VRAM envelope | WIRED | — |
| ROUTE-06 | liveness.ts scheduler + readyz.ts route + hot-reload re-register (CR-01 closed) | WIRED | — |
| ROUTE-07 | semaphore.ts + BackendSaturatedError → 429 | WIRED | — |
| OAI-03 | models.ts route con capabilities | WIRED | — |
| ANTHR-01..08, TOOL-01..05, VISION-01..03 | canonical.ts + anthropic-in/out + openai-in/out + ollama-native-out (vision /api/chat) | WIRED | — |
| DATA-01..05 | postgres + initdb + drizzle migrations + bufferedWriter + pg-backup + restore-drill | WIRED | — |
| OBS-01, OBS-05 | metrics/registry.ts + /metrics route + compose healthchecks | WIRED | — |
| ROUTE-09 | middleware/agentId.ts → request_log.agent_id | WIRED | — |
| EDGE-01..04 | traefik service + traefik.yml + dynamic/middlewares.yml | WIRED | — |
| EDGE-05, EDGE-06 | traefik HTTP→HTTPS via Tailscale Serve + SSE-friendly forwardingTimeouts | WIRED in code; **operator UAT pendiente** | Tailscale Services bootstrap (admin console) + 120s SSE smoke con modelo lento |
| WEBUI-01..05 | openwebui service + webui-app network + basic-auth middleware + DATABASE_URL → openwebui DB | WIRED | Browser UAT (model picker visual) operator-only |
| BCKND-03 | vllm + vllm-embed services + VLLMOpenAIAdapter | WIRED | — |
| OAI-02, EMBED-01 | embeddings.ts route + adapter.embeddings() en 3 adapters | WIRED | — |
| OBS-02, OBS-03 | prometheus.yml 5 scrape jobs + nvidia_gpu_exporter service | WIRED | — |
| OBS-04 | grafana provisioning + local-llms.json (7 panels referencing prometheus-default ds_uid) | WIRED | Visual confirmation operator-only |
| CLOUD-01..05 | OllamaCloudAdapter + circuit breaker + max_tokens cap + cloud_spend_daily view | WIRED | — |
| DATA-06 | Valkey service + ioredis client + registry cache + rate-limit Valkey backend | WIRED | — |
| ROUTE-10 | onSend hook global stamps X-Model-Backend from req.resolvedBackend | WIRED | — |
| ROUTE-11 | rateLimit.ts onRequest hook + Valkey INCR + 429 + Retry-After | WIRED | — |
| ROUTE-12 | idempotency.ts multiplexer + Valkey SETNX/RPUSH/PUBSUB | WIRED | — |
| EMBED-02 | OllamaCloudAdapter.embeddings() forwards to https://ollama.com/v1/embeddings | WIRED | — |
| OPS-01..04 | bin/{gc-models,backup-postgres,disk-alert}.sh + README §Operations | WIRED | — |

**Requisitos sin cross-phase wiring (self-contained en su fase):** Ninguno — todos los 76 requisitos tienen al menos una conexión con código de otra fase o con la plataforma de Compose/scripts.

## Tech Debt Aggregated by Phase

| Fase | Tech Debt Items |
|------|------------------|
| 1 | SC1 functional/diagnostic split + SC4 /api/ps fallback documentados como deviations WSL2 (no defectos). Phase-01 script cleanup follow-ups en `.planning/todos/pending/phase-01-script-cleanup.md`. |
| 2 | TD-01 (Bearer case-sensitive), TD-02 (ROUTE-08 backpressure delegado a fastify-sse-v2). 7 warnings menores documentados en 02-VERIFICATION.md (WR-01..WR-07). |
| 3 | TD-06 (WSL2 nvidia-smi compute-apps fidelity gap — tier-3 log-parse fallback funciona). CONCERN C2 cerrado por Phase 8 Plan 08-02. |
| 4 | Hot-reload VRAM flaky test (heredado en 04 deferred-items.md — pre-existente en Fase 3 watchRegistry). |
| 5 | TD-03 (7 warnings menores: bufferedWriter capacity-invariant, latency_ms=0 en pre-preHandler, drizzle err.code top-level, pg-backup auto-start race, DUMP_FILE path-traversal, constant-time length-leak, '_no_agent_' sentinel). |
| 6 | Browser UAT + Tailscale Services bootstrap + 120s SSE (deferred-by-design, operador). |
| 7 | TD-07 (hotreload.vram.test.ts flake intermitente bajo paralelismo full-suite). EMBED-02 explícitamente deferred a Fase 8 (no es defecto). |
| 8 | TD-04 (TS2367/TS2741 errors pre-existentes en 2 test files — vitest tolera, build pasa). TD-05 (watchRegistry onReload sync — v2 multi-instance). |
| 9 | Ninguno — todos los CR + WR fixes aplicados (CR-01 + CR-02 + WR-01..WR-06). |

## Human-Verify Queue

| # | Fase | Plan | Item | Bloqueador para milestone v0.9.0? | Razón deferral |
|---|------|------|------|----------------------------------|----------------|
| 1 | 6 | 06-04 Task 4 | Browser UAT vía playwright-cli | No | Requiere libnspr4/libnss3/libasound sudo install — operator-only |
| 2 | 6 | 06-04 Task 4 | 120s+ SSE smoke (necesita modelo lento) | No | Requiere modelo grande live; path correcto verificado a 33s |
| 3 | 6 | 06-04 | Tailscale Services admin-console bootstrap | No | Operación administrativa Tailscale — fuera CI scope |
| 4 | 7 | 07-06 Task 3 | Live UAT con --profile vllm | No | Plan declarado autonomous:false; vLLM cold-start 5-20 min |
| 5 | 8 | 08-10 Task 2 | Live UAT — bin/smoke-test-router.sh + bin/smoke-test-cloud.sh | No | Plan declarado autonomous:false; mismo patrón que Phase 7 |

**Total human-verify pending:** 5 items, todos `deferred-by-design` con plan frontmatter `autonomous: false`. Ninguno es gap o regresión.

## Recommendation

**Status final:** `tech_debt`

- 9/9 fases delivered en código
- 76/76 requirement IDs coded
- 0 broken integrations detectadas
- 5 items pendientes son explicit deferred-by-design UAT operativo

El milestone v0.9.0 cierra estructuralmente. Los items human_verify_pending pueden cerrarse en una sesión de operador unificada (mismo recipe ya documentado en `08-10-SUMMARY.md §User Setup Required`, `07-06-SUMMARY.md §User Setup Required`, y `README §Phase 6 §Smoke test` + `§Manual evidence — EDGE-05/EDGE-06`).

---

*Audited by Claude (gsd-milestone-auditor / integration-checker sub-agent) — 2026-05-17T20:11:45Z*
