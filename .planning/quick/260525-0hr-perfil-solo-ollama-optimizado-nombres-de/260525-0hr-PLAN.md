---
quick_id: 260525-0hr
status: in-progress
---

# Quick Task 260525-0hr: Perfil "solo Ollama" optimizado + nombres de modelo por rol

## Objetivo

Simplificar el stack a un perfil Ollama-céntrico que entre holgado en la GPU de 16 GB
(piso ~5.7 GB de Windows/WSL → ~10.6 GB usables), y exponer nombres de modelo lógicos
por rol para que agentes/n8n apunten a roles estables en vez de tags de modelo.

## Tareas

1. **compose.yml (servicio `ollama`)**: `OLLAMA_MAX_LOADED_MODELS` 1→2 (chat-7B ~5.5 GB +
   bge-m3 ~1.2 GB calientes a la vez); agregar `OLLAMA_KEEP_ALIVE=-1` (modelo caliente,
   sin cold-load para agentes). No tocar `OLLAMA_HOST` ni `OLLAMA_NUM_PARALLEL`.
2. **Apagar backends GPU redundantes**: `docker compose stop vllm vllm-embed llamacpp`
   (Qwen/bge-m3 duplicados de lo que sirve Ollama). Definiciones quedan para on-demand.
3. **Pull** `qwen2.5:7b-instruct-q4_K_M` en Ollama (chat local real con tools).
4. **router/models.yaml**: 4 alias por rol — `chat-local`, `vision-local`, `embed-local`
   (→ ollama), `big-cloud` (→ ollama-cloud). `vram_budget_gb: 0` (punteros; evita
   doble-conteo en el superRefine per-backend de registry.ts).
5. **Verificar**: reload del router, `/v1/models` lista los roles, `chat-local` y
   `embed-local` responden 200; medir VRAM antes/después.

## Restricciones

- Tope VRAM 16 GB; backend `ollama` ya suma 14.5 GB en el envelope → alias en 0.
- No tocar Traefik ni el puerto 3210 (commit reciente).
- El router valida models.yaml con zod (per-backend VRAM ≤ 16) — no romper el reload.
