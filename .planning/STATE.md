---
gsd_state_version: 1.0
milestone: v0.9.0
milestone_name: milestone
status: completed
stopped_at: "Phase 08 gap-closure (08-11) COMPLETE + verified 5/5 + threat-secured 48/48. All 9 phases complete; Milestone v0.9.0 closing. Phase 08 smoke gate RESOLVED (live-stack smokes PASS). Only remaining gate: Phase 07 Plan 07-06 Task 3 vLLM cold-start (PENDING-HUMAN)."
last_updated: 2026-05-27T13:16:39.332Z
progress:
  total_phases: 9
  completed_phases: 9
  total_plans: 55
  completed_plans: 55
  percent: 100
---

# Project State: local-llms

**Last Updated:** 2026-05-27 — Phase 08 Plan 11 COMPLETE (DATA-06 gap-closure: waitUntilReady helper + TTL=300; UAT Tests 1+7 gaps closed; smoke Section 9 expected PASS); Phase 09 Plan 04 previously COMPLETE; Milestone v0.9.0 closes; Phase 08 Plan 10 Task 2 still PENDING-HUMAN
**Status:** Phase 09 COMPLETE — Milestone v0.9.0 closes (Phase 08 incl. 08-11 gap-closure done & verified 5/5; Phase 08 Plan 10 Task 2 + Phase 07 07-06 Task 3 PENDING-HUMAN)

## Project Reference

**Core Value:** Un endpoint único, estable y multi-protocolo para que los agentes del usuario consuman cualquier modelo disponible — local cuando cabe, Ollama Cloud cuando no — sin que el cliente se entere de quién está respondiendo detrás.

**Current Focus:** Milestone v0.9.0 closing — only PENDING-HUMAN smoke verification remains (no phases left to plan)

## Current Position

Phase: 09 (Operations Hardening) — COMPLETE
Plan: 4 of 4 (all complete)

- **Milestone:** v0.9.0
- **Phase:** 08 — gap-closure 08-11 COMPLETE + verified 5/5 (all 9 phases complete)
- **Plan:** 08-11 (Wave 5, DATA-06 gap-closure — waitUntilReady boot guard + WR-01 fail-open fix + TTL 300s) — COMPLETE
- **Plan:** 08-10 (Wave 4 — Phase 8 smoke + human-verify) — TASK 1 COMPLETE; Task 2 PENDING-HUMAN. New bin/smoke-test-cloud.sh (540 LOC, executable bit set; bash -n clean) — 9 sections exercising all 10 Phase 8 requirement IDs (CLOUD-01..05, DATA-06, ROUTE-10..12, EMBED-02): Section 1 X-Model-Backend response header on chat/messages/embeddings (ROUTE-10); Sections 2+3 live cloud chat + embeddings (CLOUD-01/CLOUD-02/EMBED-02), SKIP-clean when OLLAMA_API_KEY empty; Section 4 max_tokens=16385 → 400 + code='cloud_max_tokens_exceeded' with boundary check on max_tokens=16384 (CLOUD-04); Section 5 direct-Valkey write opens breaker → 503 + code='backend_circuit_open' + Retry-After (CLOUD-03); Section 6 pre-seed ratelimit:<hash>:<minute>=RPM+1 → 429 + code='rate_limit_exceeded' + Retry-After (ROUTE-11); Section 7 3 concurrent Idempotency-Key requests return byte-identical bodies (md5 match) + request_log projection (3 rows, 1 distinct upstream_message_id) (ROUTE-12); Section 8 cloud_spend_daily Postgres view exists + queryable (CLOUD-05); Section 9 registry:models-yaml:cache:v1 populated + TTL ∈ [1,30] (DATA-06). Direct-Valkey state pre-seeding for sections 5+6 instead of burst loops keeps the smoke deterministic + fast (1 curl per assertion) regardless of CIRCUIT_FAILURE_THRESHOLD / ROUTER_RATE_LIMIT_RPM tuning. bin/smoke-test-router.sh extended with === Phase 8 — Resilience + Cloud + Telemetry === block (mirrors sections 1, 4-9 of cloud smoke; skips OLLAMA_API_KEY-gated 2+3); gated on Valkey running + VALKEY_PASSWORD set so older Phase 2/5/7 callers still PASS. .env.example documents CIRCUIT_FAILURE_THRESHOLD=5 / CIRCUIT_WINDOW_MS=30000 / CIRCUIT_COOLDOWN_MS=60000 / ROUTER_RATE_LIMIT_RPM=600 with per-var rationale. README.md gains §Phase 8 (~175 LOC) — Valkey bring-up, cloud bring-up, X-Model-Backend, breaker inspect/reset, max_tokens cap, rate-limit inspect, idempotency recipe, cloud_spend_daily query, registry cache inspect, smoke recipes. 1 atomic commit: 1732b2c (feat). 0 deviations — Task 1 executed exactly as plan's <interfaces> template specified (chose plan's documented direct-Valkey path for sections 5+6). Threat mitigations applied: all curl uses -fsS (silent on success), never -v, so bearer token never leaks on stdout (T-08-S-04). Task 2 PENDING-HUMAN — operator runs both smokes against live stack (full recipe in .planning/phases/08-ollama-cloud-fallback-resilience-hardening/08-10-SUMMARY.md §User Setup Required) and replies 'approved' for Phase 8 closure. Phase 7 carry-over (Plan 07-06 task 3) may close in the same operator session.
- **Plan:** 08-09 (Wave 3, DATA-06 — Valkey-backed registry cache) — COMPLETE. New `router/src/config/registryCache.ts` exports `makeRegistryCache({ valkey, log })` factory with `get/set/clear` over a single versioned Valkey key (`registry:models-yaml:cache:v1`) and a 30 s TTL via `valkey.set(..., 'EX', 30)` (D-D4). `get` parses JSON and re-validates through `RegistrySchema.safeParse` — defense in depth against an attacker with Valkey write access planting a tampered blob (T-08-T-08 mitigation). All three methods are non-throwing: Valkey down → warn-log + `null`/no-op so the file load is always the fallback path. Boot wiring in `router/src/index.ts`: after `makeValkeyClient`, construct `registryCache = makeRegistryCache(...)`; replace the unconditional `loadRegistryFromFile(...)` with cache-first read + file fallback + cache populate-on-miss. `watchRegistry`'s `onReload` callback now fires-and-forgets `registryCache.set(next)` so fs.watch-triggered hot-reloads propagate to Valkey (matches the synchronous `(next: Registry) => void` callback signature via `void cache.set(next).catch(warnLog)`). 9 unit tests (`tests/config/registryCache.test.ts`) cover set/get round-trip, get on empty, malformed JSON → null + warn, schema mismatch → null + warn, set TTL is `EX 30`, Valkey-down on get/set/clear → no throw. 2 atomic commits: `3124b8b` (RED test) + `6888d4e` (GREEN module + boot/onReload wiring). Full suite 674→683 pass / 7 skipped (+9 new). Build clean. 1 Rule-3 deviation: `watchRegistry.onReload` is typed `(next: Registry) => void` synchronous, not `async` as the plan's `<interfaces>` block sketched — used the plan's documented fallback pattern `void registryCache.set(next).catch((err) => app.log.warn(...))`. Same observable behavior; matches the existing type. 1 pre-existing flake: `tests/integration/hotreload.vram.test.ts → recovery: after failed VRAM reload...` failed once under full-suite CPU contention; passes isolated AND on full-suite re-run; reproduces on HEAD~2 (not caused by this plan). DATA-06 closes structurally — rate-limit counters (Plan 08-06) + registry cache (this plan) are both live Valkey consumers. Plan 08-10's smoke confirms the cache by `docker compose exec valkey valkey-cli GET registry:models-yaml:cache:v1` returns the JSON blob after a router restart within 30 s.
- **Plan:** 08-08 (Wave 3, CLOUD-05 — cloud_spend_daily Postgres view) — COMPLETE. New Drizzle migration `router/db/migrations/0001_cloud_spend_daily.sql` defines `CREATE OR REPLACE VIEW cloud_spend_daily AS SELECT date_trunc('day', ts) AS day, COUNT(*) AS request_count, SUM(latency_ms) AS spend_ms, COUNT(DISTINCT upstream_message_id) AS distinct_generations, AVG(latency_ms)::int AS avg_latency_ms FROM request_log WHERE backend = 'ollama-cloud' GROUP BY 1 ORDER BY 1 DESC` + `COMMENT ON VIEW` with CLOUD-05 lineage. `meta/_journal.json` registers the `0001_cloud_spend_daily` tag at idx 1 so `runMigrations()` picks it up at boot (Phase 5 D-B5). Operator-facing surface: `docker compose exec postgres psql -U app -d router -c 'SELECT * FROM cloud_spend_daily;'` (D-C4 — no HTTP admin endpoint). `spend_ms = SUM(latency_ms)` is the D-C3 cost-proxy metric (generation_duration_ms proxy); `distinct_generations = COUNT(DISTINCT upstream_message_id)` collapses Plan 08-07 follower retries to billable units (raw COUNT(*) still includes followers via `request_count`). No router code change — view is read-only DB-side projection over already-populated columns. 3 atomic commits: bf2e76b (RED test) + 097e054 (GREEN migration + journal) + 7a28268 (docs deferred-items). 12 SQL-shape + journal-integrity tests pass always-on; 5 PG_TESTS=1-gated real-DB cases skip (smoke gate Plan 08-10 anchors the real-DB exercise). Full suite: 661→674 pass / 2→7 skipped (+13). Build clean. 1 Rule-3 deviation (filename `0001_*.sql` not plan's `008_*.sql` — Drizzle's migrator reads `meta/_journal.json` by `tag` field; next slot after `0000_init.sql` is `0001_`). 1 Rule-3 no-op (plan listed `router/src/db/migrate.ts` defensively; inspection shows migrator auto-discovers via journal). CLOUD-05 closes. Observability surface ready for Plan 08-10 live verification.
- **Plan:** 08-07 (Wave 3, ROUTE-12 — Idempotency-Key multiplexer) — COMPLETE. router/src/resilience/idempotency.ts exports `makeIdempotencyMultiplexer({ valkey, log, subscriberFactory, timeoutMs? })` factory with full surface: `acquire(key, requestId)` SETNX EX 1800 → leader|follower role; `publishNonStream(key, body, msgId)` SET EX 900 result + PUBLISH 'done' marker; `publishStreamEvent(key, event)` RPUSH chunks list + PUBLISH event JSON; `finalizeStream(key, terminal, msgId)` SET EX 900 result + EXPIRE 900 chunks + PUBLISH terminal; `awaitNonStreamResult(key, requestId)` SUBSCRIBE-first + GET result race-free + 30s timeout → IdempotencyTimeoutError; `awaitStreamResult(key, requestId)` async iterable yielding cached LRANGE chunks then future PUBLISH events until terminal marker. Key namespace `idempotency:${key}:{lock|result|chunks|channel}`. Terminal-discriminant via `$terminal` field on payloads (canonical events never use this field). InvalidIdempotencyKeyError class added to envelope.ts with HTTP 400 + OpenAI envelope (invalid_request_error / invalid_idempotency_key / param 'Idempotency-Key') + Anthropic envelope (invalid_request_error). extractIdempotencyKey middleware helper validates regex /^[A-Za-z0-9._:-]{1,256}$/, rejects array-form (duplicate-header) per RFC 9110 single-value convention. app.ts constructs the multiplexer after Plan 08-01's Valkey decoration (gated on opts.valkey, subscriberFactory = () => opts.valkey.duplicate()) and threads idempotency? into chat/messages/embeddings route registration. Chat-completions + messages routes wire both branches: NON-STREAM follower returns awaitNonStreamResult body verbatim; STREAM follower pipes awaitStreamResult iterator through the SAME canonicalToOpenAISse/canonicalToAnthropicSse translator with the SAME displayModel (byte-identical wire output guaranteed by translator-determinism over canonical events); leader's stream-branch wraps the upstream iterable in an async generator that fire-and-forget publishStreamEvent(ev) BEFORE yielding to the SSE translator + captures upstream_message_id from message_start + finalizeStream('done'|'error'|'aborted') in sseCleanup. Embeddings route wires non-stream only (no stream branch). Followers NEVER acquire the per-backend semaphore — the explicit cost-saving the multiplexer provides. upstream_message_id captured from message_start AND threaded into both leader + follower request_log rows for Plan 08-08 GROUP BY upstream_message_id cost-attribution. 3 atomic commits: 4ac2908+7e5b06c+ac31b36 across 3 tasks (15 unit + 9 integration tests). 662/664 tests pass (+24 new; 2 skipped pre-existing). Build clean. 2 auto-fix deviations: Rule 3 — timeoutMs test seam on multiplexer factory parallels breakerNow/rateLimitNow from Plans 08-04/08-06 (vi.useFakeTimers freezes Fastify internals); Rule 1 — fake adapter must read pauseBetweenEvents flag LIVE inside the async generator (initial capture-at-start approach left followers deadlocked). ROUTE-12 closes. PITFALLS Pitfall 14 (agent retry-storms DoS the local GPU) closes for SDK-driven retries supplying the header; storms without the header are still bounded by Plans 08-04 (breaker) + 08-06 (rate-limit) + Phase 3 (semaphore). Cloud-cost-protection layer 4/4 COMPLETE: rate-limit (08-06) → breaker (08-04) → max_tokens cap (08-05) → idempotency mux (08-07).
- **Plan:** 09-01 (Wave 1, OPS-01 — bin/gc-models.sh) — COMPLETE. New bin/gc-models.sh (340 LOC) scans ${HOST_DATA_ROOT}/models-gguf/gguf/ + ${HOST_DATA_ROOT}/models-hf/ against router/models.yaml; dry-run by default; --apply gated by 'GC' confirmation phrase + --yes + TTY check (Pattern B from restore-drill.sh). Move-to-trash via mv into ${HOST_DATA_ROOT}/.gc-trash/<UTC-ISO-ts>/ — never `rm` (T-09-D). Allowlist via readlink -f re-asserted before mv (T-09-E). New router/src/ops/gcModels.ts exports collectReferencedTokens + classifyCandidate (pure helpers); router/scripts/gc-classify.ts is the stdin/stdout tsx bridge from bash → parser (single-source-of-truth). 9 vitest unit tests cover 6-rule decision tree. README §Operations top-level + ### Garbage-collecting unused model files (OPS-01) subsection with HTML-comment anchors for OPS-02/03/04. 3 atomic commits: e663217 (RED) + 8353a23 (GREEN) + 03ab048 (docs). 2 Rule-1 deviations: tsx helper as file (not --eval — CWD-relative import drift); classifier reason='-' placeholder (bash read IFS=$'\\t' collapses tabs). OPS-01 closes.
- **Plan:** 09-02 (Wave 1, OPS-02 — off-host backup destination via restic) — COMPLETE. New bin/backup-postgres.sh (325 LOC, executable, bash -n clean) — host-side wrapper: picks newest router-*.dump under ${HOST_DATA_ROOT}/postgres-backups/ via `ls -t | head -1`, publishes to operator-provided restic repo with `--tag local-llms --tag postgres --tag <hostname>`, then enforces retention via `restic forget --prune` (default --keep-daily 7 --keep-weekly 4 --keep-monthly 6; operator override via BACKUP_KEEP_POLICY env). Password flows via RESTIC_PASSWORD env var, NEVER argv (T-09-I-02 — no ps exposure); no `set -x`; password never echoed. Caller-env-wins + per-variable grep|cut|sed extraction from .env mirrors bin/restore-drill.sh:120-138 (no `source .env` — avoids leaking unrelated secrets to subprocess). Backup-vs-retention exit code split: backup failure exits 1; retention-only failures WARN but exit 0 (partial success > crontab spam on transient prune lock contention). .env.example gains Phase 9 section: BACKUP_RESTIC_REPO + BACKUP_RESTIC_PASSWORD + optional BACKUP_KEEP_POLICY with full operator docs (sample URIs, openssl rand -hex 32 recipe, LOSING-THIS-MEANS-LOSING-THE-BACKUPS warning). README.md gains ### Off-host backups (OPS-02) under ## Operations (~120 LOC): prereqs, host crontab recipe (30 4 * * * cd /path/to/local-llms && bash bin/backup-postgres.sh >> /var/log/local-llms-backup.log 2>&1), retention policy, four-step off-host restore drill that PREPENDS a `restic restore` step before the unchanged bin/restore-drill.sh recipe (CONTEXT §Specifics line 75 — EXTEND not replace), NOT-backed-up scope (models-*, openwebui filesystem, Valkey), security note on password storage. Phase 5 §Known limitations 'Backups are on-host.' bullet UPDATED to point at the new subsection — limitation closes when operator wires env + crontab. 3 atomic commits: b3715dd (feat — script) + bd5978b (docs — .env.example) + 5f6bd4d (docs — README). 0 deviations — plan executed exactly as written. OPS-02 closes.
- **Plan:** 09-04 (Wave 1, OPS-04 — bearer-token rotation runbook) — COMPLETE. README §Operations §### Rotating the bearer token (OPS-04) subsection (~196 LOC) inserted between OPS-03 anchor and ## Anti-patterns. 10-step procedure mirroring Phase 5 §Restore drill template, each step with copy-pasteable shell snippet + expected outcome. Pre-flight inventory of 5 token consumers (router process, OWUI persisted openwebui DB, smoke scripts, agent clients, shell history). OWUI PersistentConfig pivot (Phase 6 CONTEXT line 16 — OPENAI_API_KEYS persists to DB on first boot; subsequent restarts read DB not env) documented in TWO variants: Path A admin UI (safe default for human operators) + Path B direct SQL (opt-in, with explicit OPERATOR-RESPONSIBILITY caveat for OWUI 0.9 JSONB schema verification — schema evolves between releases). Verification grep uses OLD_PREFIX (first 8 hex chars only, NEVER full token — T-09-I-07 mitigation: full token in shell history would defeat rotation) across router/openwebui/traefik logs; pino redaction (ROUTE-05) makes router half always-zero by design — grep is regression check. Rollback note: rotation REVERSIBLE until step 10 (discard old token). Cross-reference matrix for other-secret rotation: VALKEY_PASSWORD (~30s downtime) / POSTGRES_PASSWORD (~1-2min, ALTER USER + connection-string update + 3 service restarts) / GRAFANA_ADMIN_PASSWORD (Grafana UI; env seeds only first boot) / BACKUP_RESTIC_PASSWORD (CANNOT rotate in place — restic encryption key derived from password; re-init repo + re-publish required) / OWUI_SECRET_KEY (IRREVERSIBLE per Phase 6 CONTEXT — rotation breaks at-rest-encrypted OWUI fields; explicit NON-goal of OPS-04) / TRAEFIK_BASIC_AUTH (regenerate htpasswd + restart Traefik — straightforward). Pin assumptions: pino redaction (ROUTE-05), constant-time compare (ROUTE-03), PersistentConfig (Phase 6 line 16), .env mode 600 (bootstrap-host.sh:138). Anti-patterns reminder: never paste bearer to git; never expose router publicly without Tailscale; never skip step 9 verification grep; never grep with full token. 1 atomic commit: 1a95edc (docs). 0 deviations — plan executed exactly as written; all 12 verify gates pass on first run. OPS-04 closes. Phase 9 closes (4/4 requirements — OPS-01 + OPS-02 + OPS-03 + OPS-04). Milestone v0.9.0 closes.

- **Plan:** 09-03 (Wave 1, OPS-03 — disk-usage alert on ${HOST_DATA_ROOT}) — COMPLETE. New bin/disk-alert.sh (307 LOC, executable, bash -n clean) — single-shot df -P threshold check invoked from host crontab. Emits exactly ONE structured single-line log per invocation: `[disk-alert] LEVEL={INFO|WARN} target=... used_pct=... threshold_pct=... fs=... ts=... hostname=...` (key=value, grep/journalctl/Loki-friendly contract for future log consumers). DISK_ALERT_THRESHOLD_PCT default 80, range 1..99 asserted. Optional HTTP push hook on WARN: `curl --fail -sS --max-time 10` POST to NTFY_URL with plain-text body — best-effort, curl failure does NOT fail the script. On curl failure a SECONDARY log line emits only `url_host=<sed-extracted host only>`; FULL NTFY_URL NEVER LOGGED (T-09-I-05 — URL-IS-credential for ntfy.sh + Discord webhooks). HARD-FAILS (exit 1) when HOST_DATA_ROOT is missing/not-a-dir — NEVER falls back to / (T-09-D — wrong-target alert worse than no alert). NO auto-remediation (script ALERTS, operator REMEDIATES — destructive-op contract). T-09-D-04 mitigated via --max-time 10 (hung receiver bounded to 10s). CLI: --threshold N override (does NOT mutate .env); --help. Caller-env-wins + per-variable grep|cut|sed extraction from .env mirrors bin/restore-drill.sh:120-138 (no `source .env`). .env.example gains ## Phase 9 — Disk-usage alert (OPS-03) section: DISK_ALERT_THRESHOLD_PCT=80 + optional NTFY_URL= with full URL-IS-credential security note + sample URIs (hosted ntfy.sh, self-hosted via Tailscale Serve, Discord webhook) + openssl rand recipe for unguessable topic. README §Operations §### Disk-usage alert (OPS-03) subsection (~90 LOC): crontab recipe (15-min cadence + /var/log perms setup), three alert sinks (stdout structured / NTFY_URL HTTP / MAILTO-only-on-breach via grep pipe), sample INFO + WARN log lines, remediation pointers (a-e: gc-models.sh / BACKUP_KEEP_POLICY / ollama rm / valkey FLUSHDB / manual du), explicit v2 non-goals (Alertmanager, Grafana panel, auto-remediation, hysteresis, inode-usage, trend alerts). 3 atomic commits: 9ba36b2 (feat — script) + 8a6f5e2 (docs — .env.example) + 9840daf (docs — README). 0 deviations — plan executed exactly as written. OPS-03 closes.
- **Next plan:** None — Phase 9 COMPLETE. Milestone v0.9.0 closes structurally; only the Phase 7 vLLM carry-over remains before milestone v1 closure.
- **Phase 8 closure gate:** ✅ RESOLVED 2026-05-27 — live-stack smoke verification PASS on rebuilt router (includes 08-11 + WR-01 fix). `bin/smoke-test-cloud.sh` PASS (Section 9 TTL=174 in [1,300]; 1 SKIP cloud-embeddings). `bin/smoke-test-router.sh` 62 PASS / 0 FAIL / 4 benign SKIP (vision VRAM, 2× vLLM-embed not in default profile, Section 9 cache-empty boot-warm design). Smoke Section 9 TTL bound corrected 30→300 (commit 30569eb) to match 08-11.
- **Phase 7 carry-over:** Plan 07-06 task 3 still PENDING-HUMAN — vLLM cold-start verification on RTX 5060 Ti host (not exercised; vLLM profile not running — `--profile vllm` conflicts with one-backend-hot VRAM budget). Recipe in 07-06-SUMMARY.md §User Setup Required.

### Progress

```
Phase 1: ██████████ 100% (6/6 requirements) — Complete 2026-05-10
Phase 2: ██████████ 100% (9/9 requirements) — Complete 2026-05-12
Phase 3: ██████████ 100% (6/6 requirements) — Complete 2026-05-13
Phase 4: ░░░░░░░░░░ 0% (0/16 requirements)
Phase 5: ░░░░░░░░░░ 0% (0/8 requirements)
Phase 6: ░░░░░░░░░░ 0% (0/11 requirements)
Phase 7: █████████░ 86% (6/7 requirements coded; smoke scripts in tree; OBS-05 already complete from Phase 5; awaiting operator human-verify on 07-06 task 3)
Phase 8: ██████████ 100% (10/10 requirements coded; 11/11 plans structurally complete — CLOUD-01 precondition closed via Plan 08-00; DATA-06 foundation closed via Plan 08-01; CLOUD-01 + CLOUD-02 + EMBED-02 vertical slice closed via Plan 08-02; ROUTE-10 closed via Plan 08-03; CLOUD-03 closed via Plan 08-04; CLOUD-04 closed via Plan 08-05; ROUTE-11 closed via Plan 08-06; ROUTE-12 closed via Plan 08-07; CLOUD-05 closed via Plan 08-08; DATA-06 registry-cache portion closed via Plan 08-09; Plan 08-10 Task 1 — smoke + runbook in tree, Task 2 PENDING-HUMAN gating phase closure)
Phase 9: ██████████ 100% (4/4 requirements — OPS-01 closed via Plan 09-01; OPS-02 closed via Plan 09-02; OPS-03 closed via Plan 09-03; OPS-04 closed via Plan 09-04)

Overall: ████░░░░░░ 38% (29/76 v1 requirements)
```

## Performance Metrics

- **Phases planned:** 9
- **Phases completed:** 0
- **Requirements mapped:** 76/76 (100% coverage)
- **Research artifacts:** PROJECT.md, REQUIREMENTS.md, research/SUMMARY.md, research/STACK.md, research/FEATURES.md, research/ARCHITECTURE.md, research/PITFALLS.md
- **Research-flagged phases:** 4 (Anthropic translation), 6 (Traefik + Open WebUI), 7 (vLLM + embeddings), 8 (Ollama Cloud + resilience)

## Accumulated Context

### Key Decisions (carried from PROJECT.md)

- **Router stack:** Node 22 LTS + Fastify v5 + TypeScript + pino + zod + `fastify-sse-v2` + `@bram-dc/fastify-type-provider-zod`. `node:22-bookworm-slim` (not alpine).
- **Backends:** `ollama/ollama:0.5.7` (catalog), `ghcr.io/ggml-org/llama.cpp:server-cuda` pinned to a build tag (GGUF), `vllm/vllm-openai:v0.20.2-cu129-ubuntu2404` (HF AWQ); Ollama Cloud as a declared `backend: ollama-cloud` entry, not a magic spillover.
- **GPU reservation:** `deploy.resources.reservations.devices` (modern form), via `x-gpu` YAML anchor reused by every backend service. Never `runtime: nvidia`.
- **Storage:** two volumes — `models-gguf/` (Ollama + llama.cpp can read the same `.gguf` via symlink for dedup) and `models-hf/` (HuggingFace snapshot dir for vLLM). Never one shared `/models` tree.
- **Networks:** four — `edge`, `app`, `backend: internal: true`, `data: internal: true`. Router is the only service on all four.
- **Auth:** single bearer token from `.env`, constant-time compare. No multi-key, no OAuth, no spend caps.
- **Streaming:** SSE obligatorio; pino redaction, abort propagation, and 15s heartbeat baked in from Phase 2.
- **Anthropic translation:** normalize internally to canonical Anthropic-shape (strict superset of OpenAI). Translate inbound + outbound separately. Round-trip golden tests.
- **VRAM partitioning:** static, encoded in `models.yaml`. Default policy: one backend hot at a time via Compose `profiles:`. vLLM always with explicit `--max-model-len` and `--gpu-memory-utilization 0.45`.

### Standing Anti-Patterns to Reject (carried from research)

- `:latest` Docker tags on any inference runtime.
- `node:22-alpine` for the router.
- `runtime: nvidia` (legacy form) anywhere in Compose.
- Linux NVIDIA driver installed inside the WSL distro.
- `compress` middleware on `/v1/chat/completions` or `/v1/messages`.
- `redis:latest`; use `valkey/valkey:8-alpine` instead.
- `traefik:v2.x`.
- Single shared `/models` volume across all three runtimes.
- Open WebUI bypass connections (OWUI → backend directly).
- `WEBUI_AUTH=True` "just for testing first" — first boot is permanent.
- Public-internet exposure of the router (Tailscale recommended for remote).

### Active Todos

(empty — no plans exist yet)

### Blockers

()

- Phase 8 Plan 10 Task 2 PENDING-HUMAN — operator must run bin/smoke-test-router.sh + bin/smoke-test-cloud.sh on the live stack (Valkey + Postgres + router up; optionally OLLAMA_API_KEY set for cloud sections 2+3) and reply 'approved' for Phase 8 closure. Full recipe in .planning/phases/08-ollama-cloud-fallback-resilience-hardening/08-10-SUMMARY.md §User Setup Required.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260510-v8z | Phase 01 script cleanup — non-blocking warnings + info items from 01-REVIEW.md | 2026-05-10 | 20d57d2 | [260510-v8z-phase-01-script-cleanup-non-blocking-war](./quick/260510-v8z-phase-01-script-cleanup-non-blocking-war/) |
| 260525-0hr | Perfil Ollama-only optimizado (MAX_LOADED=2, KEEP_ALIVE=-1) + alias de modelo por rol (chat/vision/embed-local, big-cloud) | 2026-05-25 | c7b0e82 | [260525-0hr-perfil-solo-ollama-optimizado-nombres-de](./quick/260525-0hr-perfil-solo-ollama-optimizado-nombres-de/) |
| Phase 08 P08 | 15min | 1 tasks | 3 files |
| Phase 08 P09 | 4min | 1 tasks | 3 files |
| Phase 08 P10 | 30min | 1 tasks | 4 files |
| Phase 09 P03 | 22min | 3 tasks | 3 files |
| Phase 09 P04 | 12min | 1 tasks | 1 files |

## Session Continuity

Last session: 2026-05-27T13:00:21.349Z
Stopped at: Phase 9 Plan 03 (OPS-03 — disk-usage alert) COMPLETE. New bin/disk-alert.sh (307 LOC, executable, bash -n clean) — host-cron-driven df -P threshold check emitting one structured single-line log per invocation: `[disk-alert] LEVEL={INFO|WARN} target=... used_pct=... threshold_pct=... fs=... ts=... hostname=...` (key=value, grep/journalctl/Loki-friendly). Default threshold 80, range 1..99 asserted. Optional HTTP push hook on WARN (curl --fail -sS --max-time 10 POST to NTFY_URL); best-effort — curl failure does NOT fail the script + emits SECONDARY log line with url_host=<sed-extracted host only>. FULL NTFY_URL NEVER LOGGED (T-09-I-05). HARD-FAILS exit 1 when HOST_DATA_ROOT missing — NEVER falls back to / (T-09-D). NO auto-remediation (operator decides via OPS-01 GC / OPS-02 retention / ollama rm). CLI: --threshold N override + --help. .env.example gains Phase 9 section: DISK_ALERT_THRESHOLD_PCT=80 + optional NTFY_URL= with URL-IS-credential security note. README §Operations §### Disk-usage alert (OPS-03) — crontab recipe (15-min cadence), three alert sinks (stdout / NTFY / MAILTO-on-breach), sample INFO + WARN log lines, remediation pointers, v2 non-goals. 3 atomic commits: 9ba36b2 (feat) + 8a6f5e2 (docs env) + 9840daf (docs README). 0 deviations. OPS-03 closes. Phase 9 progress: 3/4.

**Next action:** Plan 09-04 (Wave 1, OPS-04 — bearer-token rotation runbook). Last plan of Phase 9. Anchor preserved in README + .env.example. After 09-04 closes: Phase 9 100%, milestone v0.9.0 closes. Phase 8 carry-over: Plan 08-10 Task 2 still PENDING-HUMAN. Phase 7 carry-over: Plan 07-06 task 3 still PENDING-HUMAN.

**Open questions for the user (none blocking):**

- Phase 4 is research-flagged: Anthropic translation is the hardest piece. Decide between `/gsd-discuss-phase 4` (human-in-loop) vs `/gsd-plan-phase --research-phase 4` (autonomous research).
- Phase 6 will need to choose Let's Encrypt (public DNS) vs mkcert (LAN-only). Decide before Phase 6 planning.
- Phase 7 needs the host NVIDIA driver version recorded by the Phase 1 preflight to pick the right vLLM image tag (`cu129` ≥ 555.x, otherwise `cu126`/`cu124`).
- Phase 8 needs current Ollama Cloud quotas/naming validated empirically (research flag).

---
*State initialized: 2026-05-10 after roadmap creation*
*Last activity: 2026-05-17 — Phase 7 Plan 06 tasks 1-2 auto-complete (smoke scripts in tree); task 3 awaits operator human-verify*
| 2026-05-23 | fast | publish router loopback host port 3210 | ✅ |
