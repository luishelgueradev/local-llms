---
phase: 07-embeddings-vllm-gpu-telemetry
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - compose.yml
  - prometheus/prometheus.yml
  - grafana/provisioning/datasources/datasource.yml
autonomous: true
requirements:
  - OBS-02
  - OBS-03
user_setup: []

must_haves:
  truths:
    - "compose.yml declares nvidia_gpu_exporter service on backend network with EXPLICIT bind-mounts for /usr/bin/nvidia-smi and libnvidia-ml.so{,.1} per 07-RESEARCH R-6 (x-gpu anchor alone is insufficient for the exporter)"
    - "compose.yml declares prometheus service on backend network with file-mounted scrape config targeting router:3000, vllm:8000, vllm-embed:8000, llamacpp:8080, nvidia_gpu_exporter:9835, and prometheus itself"
    - "compose.yml declares grafana service on app+backend networks with GF_SECURITY_ADMIN_PASSWORD from .env, anonymous auth disabled, provisioning bind-mount read-only, Traefik labels for grafana.<tailnet>.ts.net via webui-basic-auth@docker"
    - "prometheus/prometheus.yml exists with 5 scrape jobs per 07-RESEARCH Validated Code Snippets"
    - "grafana/provisioning/datasources/datasource.yml declares Prometheus with uid:prometheus-default per Pitfall P-1"
    - "docker compose config (default profile) validates clean AND lists the three new services"
  artifacts:
    - path: "compose.yml"
      provides: "Three new always-on observability services"
      contains: "nvidia_gpu_exporter"
    - path: "prometheus/prometheus.yml"
      provides: "Declarative scrape config — 5 jobs"
      contains: "scrape_configs:"
    - path: "grafana/provisioning/datasources/datasource.yml"
      provides: "Pre-provisioned Prometheus datasource with pinned UID"
      contains: "uid: prometheus-default"
  key_links:
    - from: "compose.yml services.prometheus"
      to: "prometheus/prometheus.yml"
      via: "bind-mount /etc/prometheus/prometheus.yml:ro"
      pattern: "prometheus.yml:/etc/prometheus/prometheus.yml"
    - from: "compose.yml services.grafana"
      to: "grafana/provisioning/datasources/datasource.yml"
      via: "bind-mount /etc/grafana/provisioning:ro"
      pattern: "grafana/provisioning:/etc/grafana/provisioning"
    - from: "prometheus/prometheus.yml scrape_configs"
      to: "Docker DNS targets on the backend network"
      via: "static_configs"
      pattern: "static_configs:"
    - from: "compose.yml services.nvidia_gpu_exporter"
      to: "host /usr/bin/nvidia-smi + libnvidia-ml.so"
      via: "explicit volume bind-mounts per Pitfall G-3 / R-6"
      pattern: "nvidia-smi:/usr/bin/nvidia-smi"
---

<objective>
Add the three always-on observability services (nvidia_gpu_exporter, prometheus, grafana) per 07-CONTEXT D-C1..D-D8 and 07-RESEARCH Validated Code Snippets, with the research-updated pins (gpu-exporter 1.4.1, grafana 12.4.3, prometheus v3.10.0 — NOT the older CONTEXT.md pins). Create the two declarative provisioning files Prometheus and Grafana read at boot. None of these services join the [vllm] profile — they are always-on so the observability surface stays alive when vLLM is down.

Purpose: Land OBS-02 + OBS-03 in a single focused edit. Prometheus scrapes the router (Phase 5 /metrics surface), llamacpp (Phase 3 --metrics flag), the gpu exporter, and the vLLM services from Plan 07-01 (those targets show `up == 0` until `--profile vllm` is active; that is expected and documented). Grafana auto-provisions the Prometheus datasource so Plan 07-05 only needs to drop the dashboard JSON in place.

Output: compose.yml gains three new service blocks (~120 lines). Two new files under `prometheus/` and `grafana/provisioning/datasources/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-01-SUMMARY.md
@CLAUDE.md
@compose.yml

<interfaces>
<!-- Image pins this plan installs (the executor MUST use these — NOT the older CONTEXT.md pins; research updated them per Pitfalls G-1 and G-2): -->

prom/prometheus:v3.10.0                    — released 2026-02-24; v3 schema unchanged from v2 (CLOSED R-4)
grafana/grafana-oss:12.4.3                 — Pitfall G-1 — 11.x is 14 months stale
utkuozdemir/nvidia_gpu_exporter:1.4.1      — Pitfall G-2 SIGTERM fix

<!-- Network attachment policy (D-D6 + R-6): -->

nvidia_gpu_exporter: networks=[backend]
prometheus:          networks=[backend]
grafana:             networks=[app, backend]   (app for Traefik edge per Pitfall 12; backend to query Prometheus by Docker DNS)

<!-- Pre-existing infrastructure this plan REUSES (do NOT redefine): -->

x-gpu anchor (compose.yml line 21): Used by GPU services. nvidia_gpu_exporter does NOT use it — uses explicit /dev + volume binds per R-6 / Pitfall G-3.
Networks (compose.yml lines 37-54): edge, app, backend (internal:true), data (internal:true), webui-app (internal:true).
TRAEFIK_BASIC_AUTH (compose.yml line 510): declared on the traefik service as `webui-basic-auth` Docker-provider middleware. Grafana attaches `webui-basic-auth@docker` per D-D8 (single operator credential reused).
${HOST_DATA_ROOT}: Phase 1 D-02 default /srv/local-llms; .../grafana/ and .../prometheus/ paths pre-created by bin/bootstrap-host.sh.
${TAILNET_HOSTNAME}: pre-existing env from Phase 6.
${COMPOSE_PROJECT_NAME}: default `local-llms`. Used in `traefik.docker.network` label (Pitfall 12).

<!-- nvidia_gpu_exporter bind-mount paths (R-6 Option B + Pitfall G-3): -->

devices:
  - /dev/nvidiactl:/dev/nvidiactl
  - /dev/nvidia0:/dev/nvidia0
volumes:
  - /usr/bin/nvidia-smi:/usr/bin/nvidia-smi:ro
  - /usr/lib/x86_64-linux-gnu/libnvidia-ml.so:/usr/lib/x86_64-linux-gnu/libnvidia-ml.so:ro
  - /usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1:/usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1:ro

WSL2 note: On Docker Desktop, NCT projects these into the container's standard Linux paths. If the bind sources do not exist on the host, the container fails at startup — Plan 07-06 smoke catches this; Plan 07-05 README documents the `/usr/lib/wsl/lib/libnvidia-ml.so.1` fallback.

<!-- Prometheus scrape targets (D-D2): -->

router:3000             /metrics  (Phase 5 surface; bearer skipped for /metrics per auth/bearer.ts PUBLIC_PATHS)
vllm:8000               /metrics  (vLLM native; `up` becomes 1 only when --profile vllm active)
vllm-embed:8000         /metrics  (same; profile-gated)
llamacpp:8080           /metrics  (Phase 3 --metrics flag on the llamacpp command line — compose.yml line 195)
nvidia_gpu_exporter:9835 /metrics
localhost:9090           (Prometheus self-scrape for its own internal metrics)

<!-- Grafana provisioning files (declarative, apiVersion: 1 per CLOSED R-5): -->

datasource.yml: declares Prometheus datasource with uid `prometheus-default` (Pitfall P-1 — dashboard JSON in Plan 07-05 references this UID).
The dashboards provider yml + the dashboard JSON itself land in Plan 07-05 (they don't exist yet; the bind-mount in this plan's grafana service block references the path so the file just needs to land later — Grafana boots without dashboards but with the datasource alive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nvidia_gpu_exporter, prometheus, grafana services to compose.yml</name>
  <files>compose.yml</files>
  <read_first>
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 697-834 (Validated Code Snippets — service blocks verbatim with updated pins)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 232-256 (R-6 closure — WSL2 binds)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 488-507 (Pitfalls G-1, G-2, G-3)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md lines 69-93 (D-C1..D-D8 + D-D7 Traefik label format)
    - compose.yml lines 454-524 (existing traefik service — analog for label patterns and basic-auth declaration line 510)
    - compose.yml lines 549-621 (existing openwebui service — closest analog for Grafana: app network + Traefik labels + Pitfall 12 disambiguation label)
    - compose.yml lines 360-399 (existing postgres service — INSERT POINT REFERENCE: new services go AFTER the vllm-embed block Plan 07-01 added, but BEFORE postgres)
  </read_first>
  <action>
    Append three new top-level service blocks to compose.yml. Insert order: nvidia_gpu_exporter, prometheus, grafana. Insertion point: after the vllm-embed block (added by Plan 07-01) and before the existing postgres block (compose.yml line 360 in the pre-Plan-07-01 file). Each block carries the file's `# ── Section header ────` style with a multi-line docstring explaining decisions and pitfalls (match compose.yml lines 168-173 voice).

    **nvidia_gpu_exporter service (per D-C1..D-C4 + R-6):**
    - image: `utkuozdemir/nvidia_gpu_exporter:1.4.1` (Pitfall G-2 — NOT 1.3.0; in the docstring, note the pin update over CONTEXT.md D-C1)
    - container_name: `${COMPOSE_PROJECT_NAME:-local-llms}-gpu-exporter`
    - restart: unless-stopped
    - devices block: list `/dev/nvidiactl:/dev/nvidiactl` and `/dev/nvidia0:/dev/nvidia0` (R-6 INSTALL.md canonical path)
    - volumes block — three explicit bind-mounts per R-6 Option B (do NOT use `<<: *gpu`; the exporter's documented path is explicit binds):
        - `/usr/bin/nvidia-smi:/usr/bin/nvidia-smi:ro`
        - `/usr/lib/x86_64-linux-gnu/libnvidia-ml.so:/usr/lib/x86_64-linux-gnu/libnvidia-ml.so:ro`
        - `/usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1:/usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1:ro`
        Add a YAML COMMENT immediately after these three entries: `# WSL2 fallback path (Pitfall G-3) — uncomment if standard paths fail on the host:` followed by a commented-out line `# - /usr/lib/wsl/lib/libnvidia-ml.so.1:/usr/lib/wsl/lib/libnvidia-ml.so.1:ro`. Plan 07-05 README explains when to use it.
    - networks: `[backend]` only (D-C2)
    - NO host port published (internal-scrape only)
    - healthcheck: test `["CMD-SHELL", "wget -qO- http://localhost:9835/metrics >/dev/null 2>&1 || exit 1"]`; interval 30s; timeout 5s; start_period 30s; retries 5 (D-C4 — port 9835 is the exporter default)
    - NO depends_on (the exporter is a thin nvidia-smi wrapper; its healthcheck catches any failure)

    **prometheus service (per D-D1, D-D2):**
    - image: `prom/prometheus:v3.10.0` (CLOSED R-4 — v3 schema unchanged from v2)
    - container_name: `${COMPOSE_PROJECT_NAME:-local-llms}-prometheus`
    - restart: unless-stopped
    - command (YAML list of args — match the file's `command:` style on llamacpp line 181):
        - `"--config.file=/etc/prometheus/prometheus.yml"`
        - `"--storage.tsdb.path=/prometheus"`
        - `"--storage.tsdb.retention.time=15d"`
        - `"--web.listen-address=:9090"`
        - `"--web.enable-lifecycle"`
    - volumes:
        - `./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro`  (the file Task 2 creates)
        - `${HOST_DATA_ROOT:-/srv/local-llms}/prometheus:/prometheus`
    - networks: `[backend]` only
    - NO host port (Grafana queries Prometheus by Docker DNS on backend)
    - healthcheck: `["CMD-SHELL", "wget -qO- http://localhost:9090/-/ready >/dev/null 2>&1 || exit 1"]`; interval 15s; timeout 3s; start_period 30s; retries 5
    - YAML COMMENT linking to Pitfall P-2: `# Pitfall P-2 — prom runs as UID 65534. README documents: sudo chown -R 65534:65534 ${HOST_DATA_ROOT}/prometheus`

    **grafana service (per D-D3..D-D8):**
    - image: `grafana/grafana-oss:12.4.3` (Pitfall G-1 — NOT 11.x; in docstring, note the pin update over CONTEXT.md D-D3)
    - container_name: `${COMPOSE_PROJECT_NAME:-local-llms}-grafana`
    - restart: unless-stopped
    - environment:
        - `GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}`   (D-D5; matches the .env.example var Plan 07-01 added)
        - `GF_AUTH_ANONYMOUS_ENABLED=false`   (D-D5)
        - `GF_AUTH_BASIC_ENABLED=true`        (D-D5)
        - `GF_USERS_ALLOW_SIGN_UP=false`
        - `GF_INSTALL_PLUGINS=`               (empty — no third-party plugins)
        - `GF_PATHS_PROVISIONING=/etc/grafana/provisioning`
    - volumes:
        - `${HOST_DATA_ROOT:-/srv/local-llms}/grafana:/var/lib/grafana`
        - `./grafana/provisioning:/etc/grafana/provisioning:ro`   (read-only — Pitfall on dashboards: operator UI edits clone to internal DB; reset = `docker compose down -v`)
        - `./grafana/provisioning/dashboards/local-llms.json:/etc/grafana/dashboards/local-llms.json:ro`   (file Plan 07-05 creates; declaring the bind-mount NOW means Plan 07-05 only commits the JSON; until then Grafana boots with the datasource alive but no dashboard — that's expected)
    - networks: `[app, backend]`   (D-D6)
    - healthcheck: `["CMD-SHELL", "wget -qO- http://localhost:3000/api/health >/dev/null 2>&1 || exit 1"]`; interval 15s; timeout 3s; start_period 30s; retries 5
    - labels (D-D7 + D-D8 — match openwebui pattern compose.yml lines 600-621):
        - `"traefik.enable=true"`
        - `"traefik.docker.network=${COMPOSE_PROJECT_NAME:-local-llms}_app"`   (Pitfall 12)
        - `"traefik.http.routers.grafana-edge.rule=Host(\`grafana.${TAILNET_HOSTNAME}.ts.net\`)"`
        - `"traefik.http.routers.grafana-edge.entrypoints=web"`
        - `"traefik.http.routers.grafana-edge.middlewares=webui-basic-auth@docker"`   (D-D8 — REUSE Phase 6 credential)
        - `"traefik.http.services.grafana-edge.loadbalancer.server.port=3000"`
    - depends_on:
        - prometheus: { condition: service_healthy, required: false }
    - YAML COMMENT pointing to D-D7: `# Tailscale Service svc:grafana is a deferred operator step; until then Grafana reachable via 127.0.0.1:80 + Host: grafana.<tn>.ts.net header (Phase 6 LAN bypass pattern).`

    DO NOT modify the existing traefik `depends_on:` (lines 511-524) — Grafana is reached via Docker provider label discovery; no explicit upstream-dep needed for Traefik to find it.

    DO NOT touch any prior service block (gpu-preflight, ollama, llamacpp, vllm, vllm-embed, router, router-dev, postgres, pg-backup, traefik, openwebui).

    Confirm with `docker compose config -q` (default profile) at the end — silent stderr = success.
  </action>
  <verify>
    <automated>docker compose config -q 2>&1 | grep -v '^$' | wc -l | grep -q '^0$' && grep -q 'utkuozdemir/nvidia_gpu_exporter:1.4.1' compose.yml && grep -q 'prom/prometheus:v3.10.0' compose.yml && grep -q 'grafana/grafana-oss:12.4.3' compose.yml && grep -q 'nvidia-smi:/usr/bin/nvidia-smi' compose.yml && grep -q 'libnvidia-ml.so.1:/usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1' compose.yml && grep -q 'GF_AUTH_ANONYMOUS_ENABLED=false' compose.yml && grep -q 'grafana-edge.rule' compose.yml && grep -q 'webui-basic-auth@docker' compose.yml</automated>
  </verify>
  <acceptance_criteria>
    `docker compose config -q` (default profile) renders silently (zero stderr). All three pin strings present. Both nvidia-smi and libnvidia-ml.so.1 bind-mount sources present. Grafana environment includes GF_AUTH_ANONYMOUS_ENABLED=false. Grafana Traefik labels include grafana-edge router rule and webui-basic-auth@docker middleware reference.
  </acceptance_criteria>
  <done>
    Default-profile `docker compose config` lists the three new services; pin strings (v0.21.0 from Plan 07-01 + v3.10.0 + 12.4.3 + 1.4.1 from this plan) all present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create prometheus/prometheus.yml scrape config</name>
  <files>prometheus/prometheus.yml</files>
  <read_first>
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 755-790 (the verbatim scrape config — copy with no edits; targets are Docker DNS names on the backend network)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md lines 79-84 (D-D2 — scrape targets enumerated)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 213-222 (CLOSED R-4 — v3 schema unchanged from v2; static_configs.targets the canonical form)
  </read_first>
  <action>
    Create `prometheus/prometheus.yml` (a NEW directory + file). Use the verbatim block from 07-RESEARCH Validated Code Snippets §"Prometheus Compose service block + scrape config":

    ```yaml
    # prometheus/prometheus.yml — Phase 7 (OBS-02 + D-D2)
    # CLOSED R-4: Prometheus v3.10 schema unchanged from v2.
    # Targets are Docker DNS names on the `backend` network. up{job="vllm"} == 0
    # until `docker compose --profile vllm up -d` is active — that is EXPECTED behavior.

    global:
      scrape_interval: 15s
      evaluation_interval: 15s

    scrape_configs:
      - job_name: router
        metrics_path: /metrics
        static_configs:
          - targets: ['router:3000']
            labels: {service: 'router', component: 'edge-router'}

      - job_name: vllm
        metrics_path: /metrics
        static_configs:
          - targets: ['vllm:8000', 'vllm-embed:8000']
            labels: {service: 'vllm', component: 'inference'}

      - job_name: llamacpp
        metrics_path: /metrics
        static_configs:
          - targets: ['llamacpp:8080']
            labels: {service: 'llamacpp', component: 'inference'}

      - job_name: gpu
        metrics_path: /metrics
        static_configs:
          - targets: ['nvidia_gpu_exporter:9835']
            labels: {service: 'nvidia_gpu_exporter', component: 'gpu'}

      - job_name: prometheus
        static_configs:
          - targets: ['localhost:9090']
            labels: {service: 'prometheus', component: 'observability'}
    ```

    DO NOT alter scrape intervals, target hostnames, or labels — these are exactly what the smoke script in Plan 07-06 asserts on (`up{job="router"} == 1`, etc.). The `prometheus` self-scrape job (last) targets `localhost:9090` because that resolves to the prometheus container's own listener — Prometheus self-scrape pattern is canonical.

    Create the `prometheus/` directory if it doesn't exist.
  </action>
  <verify>
    <automated>test -f prometheus/prometheus.yml && grep -q 'scrape_interval: 15s' prometheus/prometheus.yml && grep -q "targets: \['router:3000'\]" prometheus/prometheus.yml && grep -q "targets: \['vllm:8000', 'vllm-embed:8000'\]" prometheus/prometheus.yml && grep -q "targets: \['llamacpp:8080'\]" prometheus/prometheus.yml && grep -q "targets: \['nvidia_gpu_exporter:9835'\]" prometheus/prometheus.yml && grep -q "job_name: prometheus" prometheus/prometheus.yml</automated>
  </verify>
  <acceptance_criteria>
    File exists at prometheus/prometheus.yml. global.scrape_interval is 15s. All 5 scrape jobs (router, vllm, llamacpp, gpu, prometheus self-scrape) present with their exact target strings.
  </acceptance_criteria>
  <done>
    prometheus/prometheus.yml renders the 5 jobs with the canonical target hostnames Plan 07-06 asserts on.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create grafana/provisioning/datasources/datasource.yml</name>
  <files>grafana/provisioning/datasources/datasource.yml</files>
  <read_first>
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 836-851 (verbatim datasource.yml snippet — CLOSED R-5; apiVersion: 1)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 509-518 (Pitfall P-1 — pin the datasource UID so dashboard JSON in Plan 07-05 can reference it)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md D-D4 (Grafana auto-provisioning policy)
  </read_first>
  <action>
    Create `grafana/provisioning/datasources/datasource.yml` (NEW directory tree). Use the verbatim CLOSED R-5 snippet:

    ```yaml
    # grafana/provisioning/datasources/datasource.yml — Phase 7 (OBS-04 + D-D4)
    # CLOSED R-5: apiVersion: 1 stable across Grafana 10.x → 13.x.
    # uid `prometheus-default` is referenced from the dashboard JSON in Plan 07-05 (Pitfall P-1).

    apiVersion: 1

    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus:9090       # Docker DNS on the `backend` network (Grafana joins backend per D-D6)
        isDefault: true
        uid: prometheus-default            # pin UID — dashboard JSON references this (Pitfall P-1)
        editable: false                    # provisioned datasources should be read-only in UI
        jsonData:
          timeInterval: 15s                # matches prometheus.yml global scrape_interval
          httpMethod: POST
    ```

    Create the `grafana/provisioning/datasources/` directory tree if it doesn't exist. DO NOT create the `dashboards/` subdirectory in this plan — Plan 07-05 owns it. The compose.yml bind-mount in Task 1 maps the whole `grafana/provisioning/` tree into the container; Grafana boots fine with only the datasources subdir present.
  </action>
  <verify>
    <automated>test -f grafana/provisioning/datasources/datasource.yml && grep -q 'apiVersion: 1' grafana/provisioning/datasources/datasource.yml && grep -q 'uid: prometheus-default' grafana/provisioning/datasources/datasource.yml && grep -q 'url: http://prometheus:9090' grafana/provisioning/datasources/datasource.yml && grep -q 'isDefault: true' grafana/provisioning/datasources/datasource.yml</automated>
  </verify>
  <acceptance_criteria>
    File exists at the canonical Grafana provisioning path. apiVersion is 1. Datasource URL points at the Docker DNS for prometheus. UID is pinned to `prometheus-default` so Plan 07-05's dashboard JSON can reference it.
  </acceptance_criteria>
  <done>
    Grafana datasource provisioning file in place; when grafana boots in Plan 07-06 smoke, GET /api/datasources/uid/prometheus-default returns 200.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→grafana | Operator authenticates via Traefik basic-auth (Phase 6 credential) before reaching Grafana login; Grafana admin password is the second factor. |
| internal scrape | Prometheus reads /metrics from all backend services without auth — only `backend` network is internal:true so external reach is impossible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | S (Spoofing) | grafana provisioned dashboards | mitigate | datasource provisioning has `editable: false`; UID is pinned; Plan 07-05 dashboard JSON references `prometheus-default` UID, so a malicious user creating a same-name datasource cannot redirect queries. |
| T-07-05 | I (Info disclosure) | nvidia_gpu_exporter on backend | accept | Exporter reveals per-process VRAM usage; backend network is internal:true and accessible only to other compose services; no external exposure. |
| T-07-06 | E (Elevation) | grafana admin password | mitigate | GF_USERS_ALLOW_SIGN_UP=false; GF_AUTH_ANONYMOUS_ENABLED=false; admin user only; password from .env via GRAFANA_ADMIN_PASSWORD; not embedded in compose.yml or provisioning files. |
</threat_model>

<verification>
- `docker compose config -q` (default profile) renders silently
- All three new images pinned: `prom/prometheus:v3.10.0`, `grafana/grafana-oss:12.4.3`, `utkuozdemir/nvidia_gpu_exporter:1.4.1`
- nvidia_gpu_exporter has BOTH the devices block (/dev/nvidiactl + /dev/nvidia0) AND all three explicit volume bind-mounts for nvidia-smi + libnvidia-ml.so{,.1}
- prometheus/prometheus.yml has all 5 scrape jobs with the correct Docker DNS targets
- grafana/provisioning/datasources/datasource.yml has apiVersion: 1 + uid: prometheus-default + url: http://prometheus:9090
- Grafana Traefik labels use webui-basic-auth@docker (reuse Phase 6 credential per D-D8)
</verification>

<success_criteria>
After this plan, `docker compose up -d` (default profile) brings up the existing Phase 1-6 services AND prometheus + grafana + nvidia_gpu_exporter. Prometheus targets page (queried in Plan 07-06 smoke) shows `up{job="router"}=1`, `up{job="llamacpp"}=1` (if --profile llamacpp), `up{job="gpu"}=1`, `up{job="prometheus"}=1`. The vllm targets show `up=0` until `--profile vllm` is added — by design. Grafana login page (via `127.0.0.1:80` with `Host: grafana.<tn>.ts.net`) renders; admin/${GRAFANA_ADMIN_PASSWORD} grants access; the Prometheus datasource is pre-provisioned.
</success_criteria>

<output>
After completion, write `.planning/phases/07-embeddings-vllm-gpu-telemetry/07-02-SUMMARY.md` summarizing the three new services + the two provisioning files. Note any pin deviation from CONTEXT.md (research updated all three pins).
</output>
