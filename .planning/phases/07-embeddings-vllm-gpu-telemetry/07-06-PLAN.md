---
phase: 07-embeddings-vllm-gpu-telemetry
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - bin/smoke-test-observability.sh
  - bin/smoke-test-router.sh
autonomous: false
requirements:
  - BCKND-03
  - OAI-02
  - EMBED-01
  - OBS-02
  - OBS-03
  - OBS-04
user_setup: []

must_haves:
  truths:
    - "bin/smoke-test-observability.sh exists, asserts Prometheus has ≥4 healthy scrape targets (router, llamacpp/conditional-on-profile, gpu, prometheus), `up{job=router}==1`, `up{job=gpu}==1`, `up{job=vllm}==1` (when --profile vllm active), Grafana datasource `prometheus-default` is provisioned, dashboard `local-llms` is provisioned, GPU exporter returns at least one nvidia_smi_memory_used_bytes sample with a numeric value"
    - "bin/smoke-test-router.sh is extended with /v1/embeddings checks: bge-m3-ollama returns dimensions == 1024, bge-m3-vllm (when --profile vllm) returns dimensions == 1024, capability gate on qwen2.5-7b-instruct-awq returns 400, request_log has rows where route='/v1/embeddings' grouped by backend showing ollama AND vllm-embed"
    - "Human-verify checkpoint: operator brings up the stack with --profile vllm, runs both smoke scripts end-to-end, watches the Grafana dashboard render real metrics, and approves the phase"
    - "All six phase requirements (BCKND-03, OAI-02, EMBED-01, OBS-02, OBS-03, OBS-04) are covered by automated assertions in the two scripts before the human-verify checkpoint"
  artifacts:
    - path: "bin/smoke-test-observability.sh"
      provides: "Phase 7 observability smoke covering OBS-02, OBS-03, OBS-04"
      min_lines: 60
    - path: "bin/smoke-test-router.sh"
      provides: "Phase 2/3/4/5/6 smoke extended with Phase 7 /v1/embeddings + request_log distinct-rows assertion"
      contains: "/v1/embeddings"
  key_links:
    - from: "bin/smoke-test-observability.sh"
      to: "Prometheus /api/v1/targets + /api/v1/query?query=up"
      via: "docker compose exec -T prometheus wget"
      pattern: "/api/v1/targets"
    - from: "bin/smoke-test-observability.sh"
      to: "Grafana /api/datasources/uid/prometheus-default + /api/dashboards/uid/local-llms"
      via: "docker compose exec -T grafana wget --user admin:$GRAFANA_ADMIN_PASSWORD"
      pattern: "/api/dashboards/uid/local-llms"
    - from: "bin/smoke-test-router.sh"
      to: "POST /v1/embeddings via router edge"
      via: "curl -fsS -H Authorization: Bearer ..."
      pattern: "/v1/embeddings"
---

<objective>
Add the Phase 7 smoke surface and run a final human-verify checkpoint against the live stack with `--profile vllm` active. The two scripts cover all six phase requirements: BCKND-03 (vLLM serves chat) + OAI-02/EMBED-01 (/v1/embeddings against both backends) + OBS-02 (Prometheus scrapes vLLM + llama.cpp) + OBS-03 (GPU exporter alive + scraped) + OBS-04 (Grafana dashboard provisioned). The Wave 0 cold-start gate already proved the vLLM kernel — this plan exercises the FULL stack end-to-end.

Purpose: Phase gate. After this plan, the phase is verifiable (`bash bin/smoke-test-observability.sh && bash bin/smoke-test-router.sh && docker compose ps` is the recipe a verifier runs to assert phase completion). Plan 07-04's recordRequestOutcome wiring is empirically proven via the request_log distinct-rows SQL assertion.

Output: One new script + one existing script extended + a human-verify checkpoint. autonomous: false because Task 3 is the human-verify gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-01-SUMMARY.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-02-SUMMARY.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-03-SUMMARY.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-04-SUMMARY.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-05-SUMMARY.md
@CLAUDE.md
@bin/smoke-test-router.sh

<interfaces>
<!-- Existing bin/smoke-test-router.sh contract (Phase 2-5 sections; extended by this plan): -->

The script's existing pattern (per Phase 3 + Phase 5 conventions):
  - Reads ROUTER_BEARER_TOKEN from .env (or env)
  - Uses `set -euo pipefail`
  - Prints `=== <section> ===` headers
  - Each assertion prints `✓ <description>` on success; exits non-zero with `FAIL: <reason>` on failure
  - Section-gated by --profile-aware checks (existing pattern from Phase 5 has SC-P5-* etc.)

Plan 07-06 ADDS a new `=== Phase 7 — /v1/embeddings + request_log ===` section. Existing sections are unmodified.

<!-- New script bin/smoke-test-observability.sh shape: -->

Standalone bash script (NOT extending the router smoke — these are infra-level assertions). Sections:
  1. Prometheus targets — assert `up{job="router"}==1`, `up{job="gpu"}==1`, `up{job="prometheus"}==1` always; `up{job="llamacpp"}==1` when --profile llamacpp; `up{job="vllm"}==1` when --profile vllm
  2. GPU exporter — assert `nvidia_smi_memory_used_bytes` is queryable AND returns a numeric value > 0 (proves the binds in Plan 07-02 work — Pitfall G-3 detection)
  3. Grafana datasource — assert GET /api/datasources/uid/prometheus-default returns 200 with name "Prometheus"
  4. Grafana dashboard — assert GET /api/dashboards/uid/local-llms returns 200 with dashboard.title non-empty
  5. Optional: assert Prometheus `vllm:generation_tokens_total` exists as a metric IF --profile vllm and a chat has been issued (otherwise skip)

The script reads GRAFANA_ADMIN_PASSWORD from .env or env. If unset, prints a clear FAIL with the recipe (`openssl rand -hex 24`).

<!-- Smoke commands the script invokes (per 07-RESEARCH lines 1100-1127): -->

`docker compose exec -T prometheus wget -qO- 'http://localhost:9090/api/v1/targets' | jq ...`
`docker compose exec -T prometheus wget -qO- 'http://localhost:9090/api/v1/query?query=up{job="..."}'  | jq ...`
`docker compose exec -T prometheus wget -qO- 'http://localhost:9090/api/v1/query?query=nvidia_smi_memory_used_bytes' | jq ...`
`docker compose exec -T grafana wget -qO- --user "admin:${GRAFANA_ADMIN_PASSWORD}" 'http://localhost:3000/api/datasources/uid/prometheus-default' | jq ...`
`docker compose exec -T grafana wget -qO- --user "admin:${GRAFANA_ADMIN_PASSWORD}" 'http://localhost:3000/api/dashboards/uid/local-llms' | jq ...`

The script assumes the stack is up (`docker compose ps` shows healthy). It does NOT bring up the stack — that's the operator's setup step before invoking the smoke (documented in the human-verify task).

<!-- /v1/embeddings smoke (added to bin/smoke-test-router.sh per 07-RESEARCH lines 1071-1097): -->

`curl -fsS -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"bge-m3-ollama","input":"Hola mundo desde local-llms"}' | jq '.data[0].embedding | length'`
  Expected: 1024

`curl -fsS -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"bge-m3-vllm","input":"Hola mundo desde local-llms"}' | jq '.data[0].embedding | length'`
  Expected: 1024 (gated on --profile vllm being active; section detects this via `docker compose ps vllm-embed` and skips if not running)

Capability-gate negative test:
`curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"qwen2.5-7b-instruct-awq","input":"x"}'`
  Expected: 400

request_log distinct-rows assertion (Phase SC5):
`docker compose exec -T postgres psql -U app -d router -tA -c "SELECT backend, COUNT(*) FROM request_log WHERE route='/v1/embeddings' GROUP BY backend ORDER BY backend;"`
  Expected: at least one row for 'ollama'; one row for 'vllm-embed' (after --profile vllm exercise). The smoke gates the second assertion on --profile vllm being active.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/smoke-test-observability.sh</name>
  <files>bin/smoke-test-observability.sh</files>
  <read_first>
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 1099-1127 (verbatim observability smoke template)
    - bin/smoke-test-router.sh (existing — match the file's bash style: `set -euo pipefail`, `=== Section ===` headers, `✓` success markers, env-var loading from .env if present)
    - bin/preflight-gpu.sh (env discovery patterns — HOST_DATA_ROOT default, set -euo pipefail, exit-code conventions)
  </read_first>
  <action>
    Create executable bash script `bin/smoke-test-observability.sh`:

    Header:
      `#!/usr/bin/env bash`
      `set -euo pipefail`
      Source .env if present: `[[ -f .env ]] && set -o allexport && . .env && set +o allexport`
      Fail fast if GRAFANA_ADMIN_PASSWORD is unset (print the openssl recipe).

    Section 1 — Prometheus targets active:
      `docker compose exec -T prometheus wget -qO- 'http://localhost:9090/api/v1/targets' | jq -e '.data.activeTargets | map(select(.health == "up")) | length >= 3' >/dev/null`
      Print `✓ Prometheus has ≥3 healthy scrape targets` on pass (3 because: router + gpu + prometheus self are minimum without any --profile flag; up to 5+ when vllm + llamacpp profiles active).

    Section 2 — Mandatory up{job=X}==1 for always-on targets:
      for JOB in router gpu prometheus:
        RESULT=$(docker compose exec -T prometheus wget -qO- "http://localhost:9090/api/v1/query?query=up%7Bjob%3D%22${JOB}%22%7D" | jq -r '.data.result[0].value[1] // "0"')
        [[ "$RESULT" == "1" ]] || { echo "FAIL: up{job=\"$JOB\"} != 1 (got $RESULT)"; exit 1; }
        echo "✓ up{job=\"$JOB\"} = 1"

    Section 3 — Conditional up{job=X}==1 (only if the profile-gated service is running):
      `docker compose ps vllm --format '{{.State}}' 2>/dev/null | grep -q running && check up{job="vllm"}==1 || echo "ℹ vllm service not running — skipping vllm scrape check"`
      Same pattern for llamacpp.

    Section 4 — GPU exporter has data:
      `RESULT=$(docker compose exec -T prometheus wget -qO- 'http://localhost:9090/api/v1/query?query=nvidia_smi_memory_used_bytes' | jq -r '.data.result[0].value[1] // ""')`
      Assert non-empty AND `[[ "$RESULT" =~ ^[0-9]+\.?[0-9]*([eE][+-]?[0-9]+)?$ ]]` (numeric, possibly scientific).
      On pass: `✓ nvidia_smi_memory_used_bytes returns numeric sample: $RESULT`. On fail: print the WSL2 fallback hint (Pitfall G-3 — uncomment the /usr/lib/wsl/lib bind in compose.yml).

    Section 5 — Grafana datasource provisioned:
      `docker compose exec -T grafana wget -qO- --user "admin:${GRAFANA_ADMIN_PASSWORD}" 'http://localhost:3000/api/datasources/uid/prometheus-default' | jq -e '.name == "Prometheus" and .uid == "prometheus-default"' >/dev/null`
      On pass: `✓ Grafana datasource provisioned (uid=prometheus-default)`.

    Section 6 — Grafana dashboard provisioned:
      `docker compose exec -T grafana wget -qO- --user "admin:${GRAFANA_ADMIN_PASSWORD}" 'http://localhost:3000/api/dashboards/uid/local-llms' | jq -e '.dashboard.uid == "local-llms" and (.dashboard.title | length > 0) and (.dashboard.panels | length >= 6)' >/dev/null`
      On pass: `✓ Grafana dashboard provisioned (uid=local-llms, panels≥6)`.

    Final line: `echo "✓ Phase 7 observability smoke PASS"` and exit 0.

    Make the script executable (chmod +x).
  </action>
  <verify>
    <automated>test -x bin/smoke-test-observability.sh && bash -n bin/smoke-test-observability.sh && grep -q 'set -euo pipefail' bin/smoke-test-observability.sh && grep -q '/api/v1/targets' bin/smoke-test-observability.sh && grep -q '/api/datasources/uid/prometheus-default' bin/smoke-test-observability.sh && grep -q '/api/dashboards/uid/local-llms' bin/smoke-test-observability.sh && grep -q 'nvidia_smi_memory_used_bytes' bin/smoke-test-observability.sh && grep -q 'GRAFANA_ADMIN_PASSWORD' bin/smoke-test-observability.sh</automated>
  </verify>
  <acceptance_criteria>
    Script is executable, syntax-clean, references all the canonical endpoints (/api/v1/targets, datasource UID, dashboard UID, nvidia_smi metric name). GRAFANA_ADMIN_PASSWORD env var is read.
  </acceptance_criteria>
  <done>
    Script runs end-to-end against a live stack with all sections passing (or surfacing actionable hints on fail).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend bin/smoke-test-router.sh with /v1/embeddings section</name>
  <files>bin/smoke-test-router.sh</files>
  <read_first>
    - bin/smoke-test-router.sh (full file — locate the existing Phase 5 SC-P5-* section as the most-recent precedent; append the Phase 7 section AFTER it)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 1071-1097 (verbatim /v1/embeddings smoke + request_log SQL)
  </read_first>
  <action>
    Append a new section `=== Phase 7 — /v1/embeddings + request_log ===` to bin/smoke-test-router.sh. Match the existing file's section-header convention. The section runs:

    1. Pull bge-m3 into Ollama if not present (idempotent):
       `docker compose exec -T ollama ollama list | grep -q bge-m3 || docker compose exec -T ollama ollama pull bge-m3`
       Print `✓ bge-m3 present in Ollama`.

    2. Ollama /v1/embeddings happy path:
       `DIM=$(curl -fsS -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"bge-m3-ollama","input":"Hola mundo desde local-llms"}' | jq '.data[0].embedding | length')`
       `[[ "$DIM" == "1024" ]] || { echo "FAIL: bge-m3-ollama dimensions=$DIM (expected 1024)"; exit 1; }`
       `echo "✓ bge-m3-ollama → 1024-dim"`

    3. Capability-gate negative test (always runs; doesn't require --profile vllm):
       `STATUS=$(curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"qwen2.5-7b-instruct-awq","input":"x"}')`
       `[[ "$STATUS" == "400" ]] || { echo "FAIL: capability-gate returned $STATUS (expected 400)"; exit 1; }`
       `echo "✓ capability gate: chat-only model returns 400"`

    4. Empty input zod-gate negative test (Pitfall E-1):
       `STATUS=$(curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"bge-m3-ollama","input":""}')`
       `[[ "$STATUS" == "400" ]] || { echo "FAIL: empty input returned $STATUS (expected 400)"; exit 1; }`
       `echo "✓ empty input rejected at zod boundary"`

    5. vLLM-embed /v1/embeddings (gated on --profile vllm active):
       `if docker compose ps vllm-embed --format '{{.State}}' 2>/dev/null | grep -q running; then`
         `DIM=$(curl -fsS -X POST http://localhost:3000/v1/embeddings -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" -H "Content-Type: application/json" -d '{"model":"bge-m3-vllm","input":"Hola mundo desde local-llms"}' | jq '.data[0].embedding | length')`
         `[[ "$DIM" == "1024" ]] || { echo "FAIL: bge-m3-vllm dimensions=$DIM (expected 1024)"; exit 1; }`
         `echo "✓ bge-m3-vllm → 1024-dim"`
       `else`
         `echo "ℹ vllm-embed not running — skipping bge-m3-vllm check (run with --profile vllm to include it)"`
       `fi`

    6. request_log distinct-rows assertion (Phase SC5):
       Wait 3s for the buffered writer to flush (Phase 5 D-B4 buffer is 1–2s + slack).
       `ROWS=$(docker compose exec -T postgres psql -U app -d router -tA -c "SELECT backend, COUNT(*) FROM request_log WHERE route='/v1/embeddings' GROUP BY backend ORDER BY backend;")`
       Assert the output includes `ollama|<n>` row with n >= 1. If vllm-embed was exercised, also assert `vllm-embed|<m>` row with m >= 1. The smoke gates the second assertion on --profile vllm.
       `echo "✓ request_log distinct rows for embedding dispatch: $ROWS"`

    DO NOT modify the existing sections of smoke-test-router.sh. DO NOT remove any pre-existing assertion.

    Make sure the script still parses cleanly and the new section runs only after the existing Phase 2-6 sections complete.
  </action>
  <verify>
    <automated>bash -n bin/smoke-test-router.sh && grep -q 'Phase 7 — /v1/embeddings' bin/smoke-test-router.sh && grep -q "bge-m3-ollama" bin/smoke-test-router.sh && grep -q "bge-m3-vllm" bin/smoke-test-router.sh && grep -q "qwen2.5-7b-instruct-awq" bin/smoke-test-router.sh && grep -q "1024" bin/smoke-test-router.sh && grep -q "request_log" bin/smoke-test-router.sh && grep -q "route='/v1/embeddings'" bin/smoke-test-router.sh</automated>
  </verify>
  <acceptance_criteria>
    Script syntax-checks. New Phase 7 section added with all five assertions (Ollama happy path, capability gate, empty input, vLLM happy path conditional, request_log SQL).
  </acceptance_criteria>
  <done>
    The script runs end-to-end against the live stack with Phase 2-6 sections passing unchanged and the new Phase 7 section also passing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 7 final human-verify — full stack with --profile vllm</name>
  <what-built>
    The complete Phase 7 surface: vLLM (chat) + vLLM-embed (BAAI/bge-m3 pooling) Compose services; observability stack (Prometheus + Grafana + nvidia_gpu_exporter); router /v1/embeddings route dispatching to Ollama or vLLM-embed via the registry; pre-provisioned Grafana dashboard with VRAM + request rate + TTFT + latency + error rate + backend selection panels; README §Phase 7 with operator commands; two smoke scripts (observability + router) covering all six phase requirements.
  </what-built>
  <how-to-verify>
    Run these steps in order on the live host (the only host with the RTX 5060 Ti):

    1. **Bring up the stack with the vLLM profile:**
       ```
       docker compose --profile vllm up -d
       ```
       Expected: All services come up. vllm + vllm-embed enter the "starting" state. The vllm container's healthcheck is `start_period: 600s` — expect 5–15 minutes for `docker compose ps vllm` to show `healthy` on first cold-start. Watch progress: `docker compose logs -f vllm | grep -i "capturing CUDA graphs"`. The vllm-embed model is smaller (300s start_period) and should be healthy in ~3–5 minutes.

    2. **First-time host setup (one-time, if smoke hits the Pitfall messages):**
       - `sudo chown -R 65534:65534 ${HOST_DATA_ROOT:-/srv/local-llms}/prometheus` (Pitfall P-2; only if Prometheus fails to start)
       - If `docker compose logs nvidia_gpu_exporter` shows `libnvidia-ml.so: cannot open shared object file`: uncomment the WSL2 fallback bind-mount in compose.yml (the YAML comment in the nvidia_gpu_exporter service block marks it; Pitfall G-3) and `docker compose up -d nvidia_gpu_exporter`.

    3. **Pull bge-m3 into Ollama (one-time):**
       ```
       docker compose exec ollama ollama pull bge-m3
       ```

    4. **Run the observability smoke:**
       ```
       bash bin/smoke-test-observability.sh
       ```
       Expected: all sections print `✓`; final line `✓ Phase 7 observability smoke PASS`. Exit code 0.

    5. **Run the router smoke (includes Phase 7 /v1/embeddings section):**
       ```
       bash bin/smoke-test-router.sh
       ```
       Expected: all Phase 2–6 sections pass; Phase 7 section prints all five `✓` markers; exit 0.

    6. **Visual Grafana check (LAN bypass — Tailscale Service may not be registered yet):**
       Open `http://127.0.0.1/d/local-llms/` in a browser, sending the `Host: grafana.<TAILNET_HOSTNAME>.ts.net` header (use a browser extension like ModHeader, or `curl` with `-H`). Authenticate with Traefik basic-auth user + the credential from `.env` `TRAEFIK_BASIC_AUTH`. Then log in to Grafana with `admin` + `${GRAFANA_ADMIN_PASSWORD}`. The dashboard should render with live data: VRAM panel shows non-zero usage (vLLM + Ollama loaded); Request Rate panel shows the recent smoke-test traffic; Backend Selection panel shows both `ollama` and `vllm-embed` rows.

    7. **Sanity-check request_log distinct rows manually:**
       ```
       docker compose exec postgres psql -U app -d router -c \
         "SELECT backend, route, COUNT(*) FROM request_log WHERE route='/v1/embeddings' GROUP BY backend, route;"
       ```
       Expected: ≥ 2 rows — one for backend='ollama', one for backend='vllm-embed' (Phase SC5).

    8. **Verify VRAM is realistic when both backends are hot** (this is the manual SC3 check that the static-budget arithmetic plays out in practice):
       ```
       docker compose exec vllm nvidia-smi
       ```
       Expected: vllm process shown with ~7-8 GB VRAM (matches D-E1 budget of 7.2 GB for the Qwen 7B AWQ chat model + KV cache). vllm-embed and ollama processes may also be visible.

    Approve when:
    - All smoke scripts pass
    - Grafana dashboard renders with live data showing all six panels
    - request_log has distinct rows per backend
    - nvidia-smi shows realistic VRAM (no silent CPU fallback)

    Issues to surface if found:
    - Specific panel showing "No data" (likely Pitfall P-1 — metric name mismatch; surface the actual metric name from `docker compose exec prometheus wget -qO- http://localhost:9090/api/v1/label/__name__/values | jq .` and update the dashboard JSON)
    - vLLM startup failure (would have been caught by Wave 0; if it surfaces here, escalate)
    - Embedding dimensions != 1024 from vLLM-embed (Pitfall E-2 — bge-m3 with --hf-overrides should serve dense; if vLLM is returning sparse/colbert, the dashboard panel for embedding latency may still work but the smoke fails)
  </how-to-verify>
  <resume-signal>Reply `approved` if all checks passed, OR a list of failing assertions and observed behavior. The phase is NOT considered complete until this checkpoint signs off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→stack | Operator runs smoke scripts directly on the host; same trust as docker socket access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | I (Info disclosure) | smoke scripts log bearer tokens | mitigate | Scripts use `curl -H "Authorization: Bearer $ROUTER_BEARER_TOKEN"` — token is in process env, not echoed. set +x discipline preserved (no debug-trace echoes). |
| T-07-15 | T (Tampering) | smoke success criteria | accept | Smoke scripts can pass with broken behavior IF the assertions are too loose. Acceptance criteria above (≥6 panels, dim==1024, both backends in request_log) close the most-likely gaps. |
</threat_model>

<verification>
- bin/smoke-test-observability.sh exists, is executable, syntax-clean, and asserts on prometheus targets + grafana datasource + grafana dashboard + GPU metric
- bin/smoke-test-router.sh has a Phase 7 section asserting embedding dimensions + capability gate + request_log distinct rows
- Human-verify checkpoint signed off — operator has seen the live Grafana dashboard render real data
</verification>

<success_criteria>
The phase is complete. All six requirements (BCKND-03, OAI-02, EMBED-01, OBS-02, OBS-03, OBS-04) are covered by automated assertions PLUS a human visual check of the Grafana dashboard. The Wave 0 sm_120 risk is resolved (smoke gate passed). The single VRAM envelope (16 GB) is empirically honored (nvidia-smi shows the static budget arithmetic plays out at runtime).
</success_criteria>

<output>
After completion, write `.planning/phases/07-embeddings-vllm-gpu-telemetry/07-06-SUMMARY.md` summarizing the smoke scripts + the human-verify outcome (operator's reply quoted verbatim).
</output>
