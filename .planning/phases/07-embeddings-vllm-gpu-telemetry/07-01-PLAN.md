---
phase: 07-embeddings-vllm-gpu-telemetry
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - compose.yml
  - .env.example
autonomous: true
requirements:
  - BCKND-03
user_setup: []

must_haves:
  truths:
    - "compose.yml declares vllm service with locked image vllm/vllm-openai:v0.21.0-cu129, the verbatim D-A3 command flags (subject to Wave 0 fallback overrides recorded in 07-00-SUMMARY.md), ipc:host, shm_size:16gb, profile:[vllm], healthcheck start_period:600s, networks [backend, app], HF cache bind-mount, x-gpu anchor, depends_on gpu-preflight"
    - "compose.yml declares vllm-embed service with the same image, --runner pooling + --hf-overrides for BAAI/bge-m3, lower --gpu-memory-utilization 0.25, profile:[vllm], healthcheck start_period:300s, networks [backend, app], same HF cache mount, x-gpu anchor"
    - "Both vLLM services set VLLM_FLASH_ATTN_VERSION=2 defensively per 07-RESEARCH Pitfall V-1 fallback B; additional fallback vars are present iff 07-00-SUMMARY.md OUTCOME=fallback-env"
    - "--quantization on vllm reflects 07-00-SUMMARY.md outcome: awq_marlin (locked/fallback-env) OR awq (fallback-quant)"
    - ".env.example declares GRAFANA_ADMIN_PASSWORD (no default; with `openssl rand -hex 24` generation recipe) and HUGGINGFACE_HUB_TOKEN (empty default; documented as optional)"
    - "docker compose --profile vllm config (dry-render) validates with no errors AND lists both vllm and vllm-embed in services"
  artifacts:
    - path: "compose.yml"
      provides: "vllm + vllm-embed services with all locked flags and the chosen Pitfall V-1 fallback (if any)"
      contains: "vllm/vllm-openai:v0.21.0-cu129"
    - path: ".env.example"
      provides: "GRAFANA_ADMIN_PASSWORD + HUGGINGFACE_HUB_TOKEN env contract for Wave 2/3 plans"
      contains: "GRAFANA_ADMIN_PASSWORD="
  key_links:
    - from: "compose.yml services.vllm"
      to: "${HOST_DATA_ROOT}/models-hf/"
      via: "HF cache bind-mount at /root/.cache/huggingface"
      pattern: "models-hf:/root/\\.cache/huggingface"
    - from: "compose.yml services.vllm-embed"
      to: "BAAI/bge-m3 with --runner pooling + --hf-overrides architectures override"
      via: "vLLM pooling runner per CLOSED R-1"
      pattern: "BgeM3EmbeddingModel"
    - from: "compose.yml services.vllm + services.vllm-embed"
      to: "gpu-preflight one-shot service"
      via: "depends_on condition:service_completed_successfully"
      pattern: "gpu-preflight"
---

<objective>
Add the vllm (chat) and vllm-embed (embeddings) Compose services per the 07-RESEARCH §"Validated Code Snippets" reference blocks, applying the Wave 0 Pitfall V-1 fallback decision recorded in 07-00-SUMMARY.md. Add the two new env vars (GRAFANA_ADMIN_PASSWORD, HUGGINGFACE_HUB_TOKEN) to .env.example so Wave 2/3 plans inherit a stable env contract.

Purpose: Land the BCKND-03 compose surface in a single focused edit. No router source touches in this plan — that lands in Plan 07-03 in parallel with Plan 07-02 (observability). Keeping vllm-service-only here keeps file ownership clean: 07-01 owns the vllm/vllm-embed blocks; 07-02 owns the prometheus/grafana/gpu-exporter blocks; both edits land in serialized waves on compose.yml.

Output: compose.yml gains ~80 lines (two new service blocks + the depends_on links). .env.example gains 8–12 lines (two new env entries with comments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md
@CLAUDE.md
@compose.yml
@.env.example

<interfaces>
<!-- The Wave 0 outcome dictates the variant — read it BEFORE making edits. -->

Decision lookup (executor MUST run this grep at the start of Task 1):
```
grep -oE 'OUTCOME: (locked|fallback-env|fallback-quant)' .planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md
```
The grep result is exactly one of three strings — apply the matching variant per the table below.

Per-outcome compose.yml differences (only these three knobs change between variants — everything else from 07-RESEARCH §"Validated Code Snippets" is verbatim):

| Outcome | vllm `--quantization` | vllm environment.VLLM_USE_TRITON_AWQ | vllm-embed environment.VLLM_USE_TRITON_AWQ |
|---|---|---|---|
| locked         | awq_marlin | (not set) | (not set) |
| fallback-env   | awq_marlin | 1         | 1         |
| fallback-quant | awq        | (not set) | (not set) |

VLLM_FLASH_ATTN_VERSION=2 is set in ALL THREE variants (defensive; doesn't hurt the locked path; 07-RESEARCH explicitly recommends it as the always-on default for Blackwell).

x-gpu anchor (DO NOT redefine — reuse the existing anchor on compose.yml line 21):
  Already declares driver:nvidia + count:all + capabilities:[gpu,utility]. The new vllm + vllm-embed services use `<<: *gpu` to inherit.

Phase 1 D-13 networks (DO NOT redefine — they exist at compose.yml lines 37-54):
  edge / app / backend (internal:true) / data (internal:true) / webui-app (internal:true). The vllm services join `backend` + `app` ONLY (D-A6).

Phase 1 D-02 paths (already exist on host):
  ${HOST_DATA_ROOT}/models-hf/ — pre-created by bin/bootstrap-host.sh; vllm and vllm-embed mount it at /root/.cache/huggingface.

Phase 3 pattern (Compose `profiles:` per-backend — compose.yml lines 97, 177):
  `profiles: [vllm]` on BOTH new services. Default `docker compose up -d` does NOT start them; `docker compose --profile vllm up -d` brings them up as a pair.

D-A4 healthcheck (NON-NEGOTIABLE):
  vllm.healthcheck.start_period: 600s — JIT compile + CUDA graphs per Pitfall V-2; DO NOT reduce.
  vllm-embed.healthcheck.start_period: 300s — smaller model, 5min sufficient.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vllm and vllm-embed services to compose.yml</name>
  <files>compose.yml</files>
  <read_first>
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md (extract OUTCOME via grep -oE 'OUTCOME: (locked|fallback-env|fallback-quant)' — apply the matching variant)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 601-693 (Validated Code Snippets — vllm + vllm-embed service blocks verbatim)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md lines 44-67 (D-A1..D-A8 + D-B1..D-B5 locked decisions)
    - compose.yml lines 17-54 (existing x-gpu anchor + networks — REUSE, do not redefine)
    - compose.yml lines 168-212 (llamacpp service — closest existing analog: GGUF backend with profile:[llamacpp], x-gpu anchor, backend-only network, gpu-preflight dep, gpu-init-libcuda.sh entrypoint volume mount pattern)
    - compose.yml lines 60-91 (gpu-preflight service — the depends_on target with `condition: service_completed_successfully`)
  </read_first>
  <action>
    Per D-A1..D-A8 and D-B5(a) and the OUTCOME read from 07-00-SUMMARY.md, append two new top-level service blocks to compose.yml AFTER the existing llamacpp service (lines 168-212) and BEFORE the router service block (line 217). Maintain compose.yml's existing pattern: a `# ── Section header ─────` comment line above each service block, and a multi-line docstring explaining decisions and pitfalls.

    Both services use:
    - image: `vllm/vllm-openai:v0.21.0-cu129` (the locked pin per 07-CONTEXT D-A1 and 07-RESEARCH §Standard Stack — DO NOT pin the older v0.20.2)
    - container_name template matching existing pattern: `${COMPOSE_PROJECT_NAME:-local-llms}-vllm` and `${COMPOSE_PROJECT_NAME:-local-llms}-vllm-embed`
    - profiles: [vllm] (D-A5)
    - `<<: *gpu` (inherits the existing x-gpu anchor — DO NOT add a separate `deploy.resources.reservations.devices` block; the anchor is the canonical form per Phase 1 D-13)
    - restart: unless-stopped
    - ipc: host + shm_size: 16gb (vllm) / shm_size: 8gb (vllm-embed) — D-A4
    - HF cache volume mount: `${HOST_DATA_ROOT:-/srv/local-llms}/models-hf:/root/.cache/huggingface` (D-A7)
    - ALSO mount `./bin/gpu-init-libcuda.sh:/usr/local/bin/gpu-init-libcuda.sh:ro` (same WSL2/Docker Desktop libcuda pattern Ollama + llamacpp use — compose.yml lines 132, 201; preserves WSL2 portability)
    - networks: [backend, app] (D-A6 — backend for router data plane, app for HF egress to huggingface.co; explicitly NOT webui-app, NOT edge, NOT data)
    - depends_on: gpu-preflight with `condition: service_completed_successfully` (D-04 pattern from Phase 1)

    vllm service `command:` (D-A3 + Wave 0 outcome):
    - --model=Qwen/Qwen2.5-7B-Instruct-AWQ
    - --quantization=awq_marlin  (OR --quantization=awq if OUTCOME=fallback-quant)
    - --max-model-len=8192
    - --gpu-memory-utilization=0.45
    - --enable-auto-tool-choice
    - --tool-call-parser=hermes  (CLOSED R-2 — NOT qwen2_5; hermes is the canonical for Qwen2.5-Instruct per docs.vllm.ai)
    - --served-model-name=Qwen/Qwen2.5-7B-Instruct-AWQ
    Inline-comment the Wave 0 decision next to --quantization with the exact OUTCOME string from 07-00-SUMMARY.md and a reference to Pitfall V-1 fallback path.

    vllm `environment:`:
    - VLLM_FLASH_ATTN_VERSION=2  (always — defensive per Pitfall V-1 fallback B)
    - VLLM_USE_TRITON_AWQ=1  (ONLY if OUTCOME=fallback-env)
    - HF_HOME=/root/.cache/huggingface
    - HF_HUB_CACHE=/root/.cache/huggingface
    - HUGGINGFACE_HUB_TOKEN=${HUGGINGFACE_HUB_TOKEN:-}  (optional; empty default — wired here so users with private mirrors can set it later)

    vllm `healthcheck:`:
    - test: curl -fsS http://localhost:8000/health || exit 1
    - interval: 30s, timeout: 5s, retries: 10, start_period: 600s  (D-A4 — JIT compile + CUDA graphs; DO NOT reduce)

    vllm-embed service `command:` (D-B5(a) — CLOSED R-1):
    - --model=BAAI/bge-m3
    - --runner=pooling  (the canonical flag in vLLM 0.21.x per CLOSED R-1 — NOT --task)
    - `--hf-overrides={"architectures":["BgeM3EmbeddingModel"]}` (required for sparse/colbert weight loading — per 07-RESEARCH State of the Art row 2)
    - --gpu-memory-utilization=0.25
    - --max-model-len=8192
    - --served-model-name=BAAI/bge-m3
    - --port=8000

    vllm-embed `environment:`:
    - VLLM_FLASH_ATTN_VERSION=2  (always defensive)
    - VLLM_USE_TRITON_AWQ=1  (ONLY if OUTCOME=fallback-env — bge-m3 is non-AWQ but env var is harmless and keeps both vllm services consistent)
    - HF_HOME=/root/.cache/huggingface
    - HF_HUB_CACHE=/root/.cache/huggingface
    - HUGGINGFACE_HUB_TOKEN=${HUGGINGFACE_HUB_TOKEN:-}

    vllm-embed `healthcheck:`:
    - test: curl -fsS http://localhost:8000/health || exit 1
    - interval: 30s, timeout: 5s, retries: 10, start_period: 300s  (smaller model — 5min suffices per 07-RESEARCH)

    Docstring above each service explains: locked pin and why; Wave 0 OUTCOME and which fallback (if any) is applied; CLOSED R-1 single-process limitation as the rationale for vllm-embed being a separate service.

    DO NOT publish any host ports on either service (D-A6 spirit — backend internal:true blocks egress; vllm + vllm-embed are router-only).
    DO NOT add a `compress` middleware reference (n/a — these are not behind Traefik for now; just noting the project's standing rule).
    DO NOT modify the existing x-gpu anchor or networks declarations.
    DO NOT touch the gpu-preflight, ollama, llamacpp, router, router-dev, postgres, pg-backup, traefik, or openwebui service blocks.
  </action>
  <verify>
    <automated>docker compose --profile vllm config -q 2>&1 | grep -v '^$' | wc -l | grep -q '^0$' && grep -c 'vllm/vllm-openai:v0.21.0-cu129' compose.yml | grep -E '^[2-9]$|^[1-9][0-9]+$' && grep -A 30 'services:$' compose.yml | grep -q ' vllm:' && grep -c 'profiles: \[vllm\]' compose.yml | grep -E '^[2-9]$' && grep -q 'start_period: 600s' compose.yml && grep -q 'start_period: 300s' compose.yml && grep -q 'VLLM_FLASH_ATTN_VERSION=2' compose.yml && grep -q -- '--runner=pooling' compose.yml && grep -q 'BgeM3EmbeddingModel' compose.yml && grep -q -- '--tool-call-parser=hermes' compose.yml</automated>
  </verify>
  <acceptance_criteria>
    `docker compose --profile vllm config` renders without errors (silent stderr). Image pin v0.21.0-cu129 appears ≥ 2 times (vllm + vllm-embed). Both `profiles: [vllm]` instances are present. The 600s and 300s start_periods are both present. VLLM_FLASH_ATTN_VERSION=2 appears (defensive baseline). `--runner=pooling` appears (CLOSED R-1). `BgeM3EmbeddingModel` appears in --hf-overrides (vLLM sparse/colbert loading). `--tool-call-parser=hermes` appears (CLOSED R-2).
  </acceptance_criteria>
  <done>
    `docker compose --profile vllm config` renders cleanly; both vllm and vllm-embed are listed under `services:`; all locked flags and the Wave 0 outcome-specific knobs are reflected verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GRAFANA_ADMIN_PASSWORD and HUGGINGFACE_HUB_TOKEN to .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (full file — find the appropriate section to append after; the Phase 6 TAILNET_HOSTNAME + TRAEFIK_BASIC_AUTH section is the most recent precedent for comment-heavy env var documentation)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 1129-1143 (the .env.example additions snippet — verbatim is fine)
  </read_first>
  <action>
    Append a new top-level commented section to .env.example titled `# ── Phase 7 — Embeddings + vLLM + GPU Telemetry ──`. Within the section, declare two env vars with comment blocks matching the existing file's style:

    1. `GRAFANA_ADMIN_PASSWORD=` (no default value)
       Comments above explain:
       - This is Grafana's internal admin password (separate from Traefik basic-auth on the edge per D-D8)
       - Required by Plan 07-02 (grafana service GF_SECURITY_ADMIN_PASSWORD env)
       - Generation recipe: `openssl rand -hex 24` (per 07-CONTEXT D-D5 and Pitfall P-2 spirit)
       - Phase 6 Traefik basic-auth (TRAEFIK_BASIC_AUTH) is the EDGE gate; Grafana's admin password is the FALLBACK gate when reached via 127.0.0.1:80 LAN bypass (matches the dual-layer auth pattern OWUI uses)

    2. `HUGGINGFACE_HUB_TOKEN=` (empty default — declared but unset is fine)
       Comments above explain:
       - Optional. Empty default works for Phase 7 because Qwen2.5-7B-Instruct-AWQ and BAAI/bge-m3 are both PUBLIC HuggingFace models (per 07-RESEARCH §Environment Availability)
       - Set ONLY if pinning a gated HF model in a later phase or running behind a private HF mirror
       - Wired through to vllm + vllm-embed service environment in compose.yml as `${HUGGINGFACE_HUB_TOKEN:-}` (empty interpolates to empty string — no broken auth header)
       - Token generation: https://huggingface.co/settings/tokens (READ scope sufficient)

    Both comment blocks should match the existing 60-80-char line width of the file. Do NOT modify any existing env var, do NOT reorder, do NOT delete the TODO comments left by Phase 6.
  </action>
  <verify>
    <automated>grep -q '^GRAFANA_ADMIN_PASSWORD=' .env.example && grep -q '^HUGGINGFACE_HUB_TOKEN=' .env.example && grep -q 'openssl rand -hex 24' .env.example && grep -q 'Phase 7' .env.example</automated>
  </verify>
  <acceptance_criteria>
    .env.example has the two new env-var declarations as exact `GRAFANA_ADMIN_PASSWORD=` and `HUGGINGFACE_HUB_TOKEN=` lines; the generation recipe `openssl rand -hex 24` is documented in comments; a `Phase 7` section header is present.
  </acceptance_criteria>
  <done>
    .env.example renders cleanly with the new section; the two env vars are documented with their generation recipes and a Phase 7 marker.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→vllm | Operator can pull a malicious image if the pin is replaced post-merge. Pin discipline is the only mitigation. |
| huggingface.co→vllm | First-cold-start downloads model weights. CDN compromise could in theory serve malicious tensors; HF Hub uses signed downloads, mitigated upstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | compose.yml image pin | mitigate | Pin v0.21.0-cu129 exact tag (NEVER :latest, NEVER :v0.21 floating per CLAUDE.md "What NOT to Use"). Wave 4 smoke records the pulled digest. |
| T-07-02 | I (Info disclosure) | HUGGINGFACE_HUB_TOKEN env | accept | Empty default; token only set if user opts in. Pino redaction on the router side does not apply here — token never flows through the router. |
| T-07-03 | D (DoS) | vllm uncapped first cold-start | mitigate | start_period: 600s + retries: 10 prevents Compose from declaring the container unhealthy during JIT compile (Pitfall V-2). |
</threat_model>

<verification>
- `docker compose --profile vllm config` validates with zero errors
- `grep -c 'vllm/vllm-openai:v0.21.0-cu129' compose.yml` returns ≥ 2 (vllm + vllm-embed both pinned to the same image)
- `grep -c 'profiles: \[vllm\]' compose.yml` returns ≥ 2 (both new services profile-gated)
- `start_period: 600s` (vllm chat) AND `start_period: 300s` (vllm embed) both present
- VLLM_FLASH_ATTN_VERSION=2 present on both services (defensive baseline)
- Wave 0 outcome reflected: if fallback-env → VLLM_USE_TRITON_AWQ=1 also present; if fallback-quant → --quantization=awq (not awq_marlin)
- .env.example has GRAFANA_ADMIN_PASSWORD and HUGGINGFACE_HUB_TOKEN declared with comments + generation recipe
</verification>

<success_criteria>
After this plan, `docker compose --profile vllm up -d vllm vllm-embed` SHOULD succeed (subject to Wave 0 having confirmed the kernel works). The router still sees no vllm models (that lands in Plan 07-03). The compose file is now complete enough for Plan 07-02 to add the observability stack without merge conflicts on this plan's blocks.
</success_criteria>

<output>
After completion, write `.planning/phases/07-embeddings-vllm-gpu-telemetry/07-01-SUMMARY.md` capturing the exact OUTCOME variant applied and any operator-visible deviations (e.g. if Pitfall V-1 fallback-env required an extra defensive env var the planner didn't anticipate).
</output>
