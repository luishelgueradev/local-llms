---
phase: 07-embeddings-vllm-gpu-telemetry
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - bin/smoke-test-vllm-coldstart.sh
autonomous: false
requirements:
  - BCKND-03
user_setup: []

must_haves:
  truths:
    - "Operator (or Claude) has run a 5-minute vLLM cold-start smoke against the locked image vllm/vllm-openai:v0.21.0-cu129"
    - "Smoke outcome is recorded: PASS (proceed with locked image + flags) OR FAIL (switch downstream plans to a documented fallback)"
    - "If smoke FAILS, the operator picks one of the four fallback paths (Pitfall V-1 ladder) and Plan 07-01 implements it"
    - "Wave 1+ plans never run before this gate resolves — the locked vLLM image and quantization flag are CONFIRMED or REPLACED before any compose.yml edits land"
  artifacts:
    - path: "bin/smoke-test-vllm-coldstart.sh"
      provides: "Standalone smoke script that pulls the locked vLLM image, runs a 1-token chat dry-run against Qwen2.5-7B-AWQ, and asserts nvidia-smi shows the vllm process — exit 0 = locked image works, exit non-zero = fallback ladder triggered"
      min_lines: 40
    - path: ".planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md"
      provides: "Records PASS/FAIL outcome + which fallback path (if any) downstream plans must adopt"
      contains: "OUTCOME:"
  key_links:
    - from: "bin/smoke-test-vllm-coldstart.sh"
      to: "Pitfall V-1 fallback decision tree in 07-RESEARCH.md"
      via: "human-verify checkpoint that surfaces the outcome and the chosen fallback (if any) before downstream plans execute"
      pattern: "checkpoint:decision"
---

<objective>
Run the mandatory Pitfall V-1 smoke gate BEFORE any compose.yml edits or router widening land. The locked vLLM image `vllm/vllm-openai:v0.21.0-cu129` MAY or MAY NOT include sm_120 (Blackwell, RTX 5060 Ti) kernels in its bundled PyTorch wheels — the only deterministic way to know is to pull the image and try one chat request against the locked AWQ Marlin path. If it works, downstream plans proceed with the locked stack. If it fails, the human operator picks one of the four documented fallback paths (per 07-RESEARCH.md Pitfall V-1 ladder), and that decision is recorded in this plan's SUMMARY so Plan 07-01 can implement the chosen variant.

Purpose: Convert the MEDIUM-confidence locked-image risk into a binary fact before sinking ~5 plans of work into the assumption. The Wave 0 gate is the single mandatory smoke task referenced in 07-RESEARCH.md Pitfall V-1 mitigation.

Output: A new smoke script `bin/smoke-test-vllm-coldstart.sh`, a human-verify checkpoint that surfaces the smoke outcome, and a SUMMARY.md row recording the chosen path (locked / image-fallback / env-var-fallback / quant-fallback) for downstream consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-CONTEXT.md
@.planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md
@CLAUDE.md
@compose.yml
@bin/preflight-gpu.sh

<interfaces>
<!-- The smoke script does NOT add to or rely on router source. It exercises the vLLM image directly via `docker run --rm`. The only contract is the documented fallback ladder. -->

Fallback ladder (from 07-RESEARCH.md Pitfall V-1, lines 196-211 — MUST be followed in order if locked image fails):
  Locked: vllm/vllm-openai:v0.21.0-cu129 + --quantization=awq_marlin
  Fallback A (image bump — UNLIKELY needed since v0.21.0 IS the post-Blackwell-fix release; documented for completeness in case a future v0.21.x patch ships):
      Same image OR newer v0.21.x patch
  Fallback B (env-var defensive — try first if Marlin kernel error surfaces):
      VLLM_FLASH_ATTN_VERSION=2 AND VLLM_USE_TRITON_AWQ=1
  Fallback C (quant kernel swap — last resort, slower):
      --quantization=awq (Triton path, kernel-agnostic; non-Marlin)

Outcomes the operator records in 07-00-SUMMARY.md:
  OUTCOME=locked          → Plan 07-01 uses the verbatim 07-RESEARCH §"Validated Code Snippets" vllm Compose block
  OUTCOME=fallback-env    → Plan 07-01 ALSO sets VLLM_FLASH_ATTN_VERSION=2 + VLLM_USE_TRITON_AWQ=1 in vllm + vllm-embed environment blocks
  OUTCOME=fallback-quant  → Plan 07-01 ALSO swaps --quantization=awq_marlin to --quantization=awq on the vllm service (vllm-embed is unaffected — embedding model doesn't use AWQ)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bin/smoke-test-vllm-coldstart.sh — Pitfall V-1 gate</name>
  <files>bin/smoke-test-vllm-coldstart.sh</files>
  <read_first>
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 442-471 (Pitfall V-1 mitigation strategy + warning signs)
    - .planning/phases/07-embeddings-vllm-gpu-telemetry/07-RESEARCH.md lines 1037-1067 (smoke-test-vllm.sh template — adapt the cold-start portion)
    - bin/preflight-gpu.sh (existing script for env discovery patterns: HOST_DATA_ROOT resolution, set -euo pipefail discipline, exit-code conventions)
    - compose.yml lines 17-30 (x-gpu anchor — the script does NOT use compose; it invokes `docker run --rm --gpus all` directly to bypass any compose-yml-pending-edits state)
  </read_first>
  <action>
    Create bin/smoke-test-vllm-coldstart.sh as an executable bash script (chmod +x as final step). Script behavior:

    1. `set -euo pipefail`. Resolve HOST_DATA_ROOT the same way preflight-gpu.sh does (env var with /srv/local-llms default). Ensure `${HOST_DATA_ROOT}/models-hf/` exists (mkdir -p with same UID:GID dance the script already does for other dirs).
    2. Image pin (single source of truth at top of file): `VLLM_IMAGE="vllm/vllm-openai:v0.21.0-cu129"` (the locked image per 07-CONTEXT D-A1 + 07-RESEARCH).
    3. `docker pull "$VLLM_IMAGE"` — fail fast if the pin is invalid or registry is unreachable. Print the digest after pull.
    4. Run `docker run --rm --gpus all --ipc host --shm-size 16gb -v "${HOST_DATA_ROOT}/models-hf:/root/.cache/huggingface" "$VLLM_IMAGE" --model Qwen/Qwen2.5-7B-Instruct-AWQ --quantization awq_marlin --max-model-len 1024 --gpu-memory-utilization 0.45 --tool-call-parser hermes --enable-auto-tool-choice --port 8000 &` IN BACKGROUND, capture the PID/container name; wait up to 900s (15-min budget per Pitfall V-2 JIT compile note) for `curl -fsS http://localhost:8000/health` (via `docker exec`) to return 200. Use a timed loop with 10s interval.
    5. If the container exits BEFORE health passes, capture `docker logs` and grep for the exact strings from Pitfall V-1 warning signs: `no kernel image is available`, `sm_120 is not compatible`, `CUDA error`. If any match: exit 2 with a printed message instructing the operator to run with `VLLM_FLASH_ATTN_VERSION=2 VLLM_USE_TRITON_AWQ=1 bin/smoke-test-vllm-coldstart.sh` (the env vars are read by this script and passed via `-e` to docker run when set — implement this re-run path).
    6. Once /health is 200: send one chat request (`docker exec` curl with `max_tokens: 4`) to http://localhost:8000/v1/chat/completions for `Qwen/Qwen2.5-7B-Instruct-AWQ` with body `{"messages":[{"role":"user","content":"hi"}],"max_tokens":4}`. Assert HTTP 200 AND `.choices[0].message.content` is a non-empty string (use `jq -e '.choices[0].message.content | length > 0'`).
    7. Run `docker exec <container> nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv,noheader` and assert at least one row contains `python` or `vllm` (per Pitfall V-1 warning sign 2 inverted — process MUST appear). If empty: exit 3.
    8. Tear down: `docker kill <container>` and `docker rm -f <container>` (the `--rm` covers the rm in normal cases; defensive on early kill).
    9. On success: print `OUTCOME: locked` and exit 0. Surface env-var-fallback success as `OUTCOME: fallback-env` (when re-run with VLLM_FLASH_ATTN_VERSION=2 set). Surface quant-fallback success as `OUTCOME: fallback-quant` (when re-run with VLLM_QUANT_OVERRIDE=awq env var the script reads — substitute into the command).
    10. Exit codes: 0 = locked passed, 1 = pull/setup failure, 2 = kernel-incompatibility detected (env-var fallback recommended), 3 = no GPU process detected (likely CPU fallback, env-var fallback recommended), 4 = chat-request semantic failure (response was empty / non-200, escalate to quant fallback). Print the exit code's human-readable meaning on the last line in all cases.

    Script MUST NOT touch compose.yml, .env, the router source, or .planning/. It is a pure docker-run probe. It is intentionally standalone so a failed Wave 0 doesn't corrupt the repo state.
  </action>
  <verify>
    <automated>bash -n bin/smoke-test-vllm-coldstart.sh && grep -q 'OUTCOME: locked' bin/smoke-test-vllm-coldstart.sh && grep -q 'OUTCOME: fallback-env' bin/smoke-test-vllm-coldstart.sh && grep -q 'OUTCOME: fallback-quant' bin/smoke-test-vllm-coldstart.sh && grep -q 'VLLM_FLASH_ATTN_VERSION=2' bin/smoke-test-vllm-coldstart.sh && grep -q 'no kernel image is available' bin/smoke-test-vllm-coldstart.sh && test -x bin/smoke-test-vllm-coldstart.sh</automated>
  </verify>
  <acceptance_criteria>
    Script syntax-checks clean; ALL three outcome labels (locked / fallback-env / fallback-quant) appear as exact strings the script can print; the env-var fallback variables (VLLM_FLASH_ATTN_VERSION=2 + VLLM_USE_TRITON_AWQ=1) appear in the source; the Pitfall V-1 detection string ("no kernel image is available") appears as a grep pattern in the failure-classification branch; file is chmod +x.
  </acceptance_criteria>
  <done>
    Script exists, is executable, parses cleanly, and contains all three branch-outcome labels plus the documented fallback variable names.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the smoke gate against live GPU + record the outcome</name>
  <what-built>
    Standalone smoke script that pulls the locked vLLM image, runs a real cold-start against the GPU, sends one tiny chat request, and verifies the model produced output via a CUDA kernel that actually ran on the sm_120 device.
  </what-built>
  <how-to-verify>
    1. From the repo root, run: `bash bin/smoke-test-vllm-coldstart.sh` (no compose, no router — just the script).
    2. Watch the output. Be patient — first cold-start can take 10–15 minutes (JIT compile + CUDA graphs per Pitfall V-2). The script's healthcheck loop will print progress every 10s.
    3. Note the final `OUTCOME:` line. Exactly one of: `OUTCOME: locked` (exit 0), `OUTCOME: fallback-env` (only printed if re-running with `VLLM_FLASH_ATTN_VERSION=2 VLLM_USE_TRITON_AWQ=1` env vars set), `OUTCOME: fallback-quant` (only printed if re-running with `VLLM_QUANT_OVERRIDE=awq` set).
    4. If the first invocation prints exit code 2 or 3 with a kernel-incompatibility message: re-run with `VLLM_FLASH_ATTN_VERSION=2 VLLM_USE_TRITON_AWQ=1 bash bin/smoke-test-vllm-coldstart.sh`. If THAT passes, the outcome is `fallback-env`.
    5. If the env-var fallback also fails: re-run with `VLLM_QUANT_OVERRIDE=awq bash bin/smoke-test-vllm-coldstart.sh`. If THAT passes, the outcome is `fallback-quant`.
    6. If ALL THREE fail: this is a hard blocker. Stop the phase and surface the error message + `docker logs` excerpt in the resume signal. Do NOT proceed to Plan 07-01.

    Expected normal outcome: `OUTCOME: locked` (the v0.21.0 image was released specifically with Blackwell scope in its release notes — per 07-RESEARCH §"Closure of ⚠ Research Items" R-3 evidence — and the chance of it working clean is HIGH).
  </how-to-verify>
  <resume-signal>Reply with exactly one of: `OUTCOME=locked`, `OUTCOME=fallback-env`, `OUTCOME=fallback-quant`, or `BLOCKED: <error>`. This decision is read by Plan 07-01.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record outcome in 07-00-SUMMARY.md for downstream consumption</name>
  <files>.planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY structure)
    - The resume-signal value from Task 2 (the OUTCOME=X string)
  </read_first>
  <action>
    Create 07-00-SUMMARY.md using the canonical SUMMARY template. The summary's `## Outcome` section MUST contain a single bold line `**OUTCOME: <locked|fallback-env|fallback-quant>**` so that Plan 07-01's `<read_first>` grep can extract it deterministically. The `## Implications for downstream plans` section enumerates the exact changes Plan 07-01 makes per outcome:

    - `OUTCOME: locked` → vllm service uses the verbatim 07-RESEARCH §"Validated Code Snippets" command + env block; no fallback env vars beyond the defensive `VLLM_FLASH_ATTN_VERSION=2` already documented.
    - `OUTCOME: fallback-env` → vllm AND vllm-embed services add `VLLM_USE_TRITON_AWQ=1` to their `environment:` block alongside the already-defensive `VLLM_FLASH_ATTN_VERSION=2`.
    - `OUTCOME: fallback-quant` → vllm service's `--quantization=awq_marlin` becomes `--quantization=awq` (slower Triton path); vllm-embed unaffected (no AWQ on bge-m3).

    Include a brief verbatim copy of the relevant `docker logs` excerpt OR the chat-response excerpt that justifies the outcome (proof of a real GPU kernel firing).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md && grep -E '^\*\*OUTCOME: (locked|fallback-env|fallback-quant)\*\*$' .planning/phases/07-embeddings-vllm-gpu-telemetry/07-00-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    07-00-SUMMARY.md exists; contains exactly one `**OUTCOME: ...**` line with one of the three valid values; downstream-implications section enumerates the per-outcome compose.yml changes Plan 07-01 must implement.
  </acceptance_criteria>
  <done>
    SUMMARY.md is committed; the next plan can `grep -oE 'OUTCOME: (locked|fallback-env|fallback-quant)' 07-00-SUMMARY.md` to read the gate result.
  </done>
</task>

</tasks>

<verification>
- bin/smoke-test-vllm-coldstart.sh is executable, syntax-clean, and contains all three OUTCOME labels and the kernel-error detection string
- Operator has run the smoke against the live GPU and surfaced a definitive OUTCOME (or escalated BLOCKED)
- 07-00-SUMMARY.md records the OUTCOME in a grep-able format
</verification>

<success_criteria>
The vLLM Blackwell sm_120 prebuilt-wheel risk (Pitfall V-1) is RESOLVED to a binary fact (works / fallback X / fallback Y) before any compose.yml edits begin. Plan 07-01 can read 07-00-SUMMARY.md and pick the right vllm-service shape on the first try.
</success_criteria>

<output>
After completion, the 07-00-SUMMARY.md file is the single artifact Plan 07-01 reads to decide which fallback path (if any) to encode in the compose.yml additions.
</output>
