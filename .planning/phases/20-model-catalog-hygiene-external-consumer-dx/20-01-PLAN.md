---
phase: 20-model-catalog-hygiene-external-consumer-dx
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - router/models.yaml
  - router/src/config/registry.ts
  - router/src/config/__tests__/registry-disabled.test.ts
autonomous: true
gap_closure: false
requirements: [CAT-01]
commit_strategy: one_atomic_commit
must_haves:
  truths:
    - "router/models.yaml entries for qwen2.5-7b-instruct-q4km, qwen2.5-7b-instruct-awq, bge-m3-vllm each declare `disabled: true` plus a one-line explanatory comment referencing project_vram_budget"
    - "After Valkey DEL + router force-recreate, GET /v1/models returns exactly 10 entries (was 13) — the 3 disabled entries do NOT appear"
    - "GET /v1/models/<disabled-name> returns 404 model_not_found for each of the 3 disabled aliases (same envelope as a fully-unknown model)"
    - "Phase 19 RESS-WITH-TOOLS smoke gate still PASSes — Wave 0 makes ZERO source changes that affect the streaming translator or cloud dispatch"
    - "All 3 dead entries remain physically present in models.yaml (operator can flip disabled to re-enable in a 1-line edit; schema unchanged) — closes CAT-01 per D-01"
    - "Unit test asserts: a registry parsed from a YAML with `disabled: true` filters that entry out of `registry.get().models` (or the entry is retained but flagged AND the route filter excludes it — see action for D-01 implementation choice)"
  artifacts:
    - path: "router/models.yaml"
      provides: "3 disabled-flagged entries + explanatory comments"
      contains: "disabled: true"
    - path: "router/src/config/registry.ts"
      provides: "ModelEntrySchema extended with optional disabled boolean (default false); filter helper to expose only enabled entries to the public surface"
      exports: ["ModelEntrySchema"]
    - path: "router/src/config/__tests__/registry-disabled.test.ts"
      provides: "Unit test covering disabled-filter behavior + backward compat (entries without the field load as enabled)"
  key_links:
    - from: "router/models.yaml"
      to: "router/src/config/registry.ts ModelEntrySchema"
      via: "Zod parse — RegistrySchema.parse(yaml.load(file))"
      pattern: "disabled: z.boolean().default(false)"
    - from: "router/src/routes/v1/models.ts filterAndProject"
      to: "ModelEntry.disabled"
      via: "filter callback excludes disabled entries from the response array"
      pattern: "filter\\(.*disabled"
    - from: "router/src/dispatch/preflight.ts applyPreflight"
      to: "ModelEntry.disabled"
      via: "registry.resolve() throws RegistryUnknownModelError if the resolved entry is disabled (so /v1/chat/completions and friends reject the alias with the same envelope as a non-existent model)"
      pattern: "throw.*RegistryUnknownModelError"
---

<objective>
Wave 0 (hardest bug, fastest, fully reversible) — close CAT-01 by flagging the 3 dead-backend entries as `disabled: true` instead of routing requests to non-existent containers. This eliminates the artiscrapper 15s-ENOTFOUND failure class immediately and is safe to ship overnight.

Per D-01 (LOCKED): the entries STAY in `router/models.yaml` with an explanatory comment so an operator can re-enable a backend in v0.13.0 via a 1-line flip. Removal would change the `backends:` schema; flagging keeps it intact.

Per the strategic frame "Catalog says X, router serves X — siempre": a flagged entry tells consumers the alias is intentionally out of service rather than silently routing to a dead backend.

Purpose:
- Stop dispatching `qwen2.5-7b-instruct-q4km` (→ llamacpp), `qwen2.5-7b-instruct-awq` (→ vllm), `bge-m3-vllm` (→ vllm-embed) to backends that do not run on this host (per project_vram_budget).
- Preserve project_models_yaml_hot_edit atomicity — the edit + Valkey DEL + force-recreate is ONE shippable unit.
- Preserve project_n8n_integration backward-compat: cloud aliases (`big-cloud`, `chat-local`, `embed-local`, `vision-local`, etc.) remain unaffected; n8n at objetiva.com.ar continues to work.

Output: 3 atomic-commit-ready file changes + 1 unit test + live router serving 10 entries on `/v1/models`. All reversible via `git revert <commit>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/seeds/SEED-001-model-catalog-hygiene-consumer-dx.md
@.planning/phases/20-model-catalog-hygiene-external-consumer-dx/20-CONTEXT.md
@.planning/phases/20-model-catalog-hygiene-external-consumer-dx/20-DISCUSSION-LOG.md
@router/models.yaml
@router/src/config/registry.ts
@router/src/routes/v1/models.ts
@router/src/dispatch/preflight.ts
@bin/smoke-test-router.sh

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From router/src/config/registry.ts (current state — pre-Wave-0):
- `LocalBackendEnum`: 'ollama' | 'llamacpp' | 'vllm' | 'vllm-embed' | 'ollama-cloud'
- `ModelEntrySchema`: zod object with required `name`, `backend`, `backend_url`, `backend_model`, `capabilities`, `vram_budget_gb`; optional `concurrency`, `max_model_len`, `profile`, `dims`, `pricing`, `policy`, `ctx_size` (default 8192), `context_strategy` (default 'sliding-window'), `mcp_servers_enabled`, `pre_completion_hooks`
- `RegistrySchema`: top-level `models: ModelEntrySchema[].min(1)`, `backends?`, `policies?`, `mcp_servers?` with cross-field superRefine for VRAM envelope + URL-uniqueness + dims-required-for-embeddings + mcp_servers_enabled cross-validation
- `loadRegistryFromFile(path)`: throws zod error on invalid YAML/schema
- `makeRegistryStore(initial)`: `.get()`, `.resolve(name)` (throws `RegistryUnknownModelError`), `._swap(next)`, `.getCreatedAtSec()`

From router/src/routes/v1/models.ts:
- `registerModelsRoute(app, registry)`: registers GET /v1/models + GET /v1/models/:id
- `filterAndProject(reg, created)`: filters by `policies.default.model_allowlist` (empty = allow-all), projects to `{id, object, created, owned_by, capabilities, policy: {cloud_allowed}}`
- `GET /v1/models/:id`: returns 404 `model_not_found` if `!entry || (allow.length > 0 && !allow.includes(entry.name))`

From router/src/dispatch/preflight.ts:
- `applyPreflight(requested_model, opts)`: calls `opts.registry.resolve(requested_model)` first; `RegistryUnknownModelError` propagates verbatim → 404 via centralized error handler

From router/src/errors/envelope.ts (referenced by registry.ts):
- `RegistryUnknownModelError(requested, available)`: maps to 404 with `model_not_found` envelope
</interfaces>

<dead_entries>
<!-- The exact 3 entries getting `disabled: true` flags. Source of truth: SEED-001 §"Reproduced Facts (snapshot 2026-06-03)". -->

1. `qwen2.5-7b-instruct-q4km` (line 186 in router/models.yaml) — backend `llamacpp` (container does not run; would never run on this host per VRAM budget)
2. `qwen2.5-7b-instruct-awq` (line 207 in router/models.yaml) — backend `vllm` (same)
3. `bge-m3-vllm` (line 232 in router/models.yaml) — backend `vllm-embed` (same)

Aliases NOT touched (must continue to work — live consumer impact):
- `chat-local` (→ qwen2.5:7b via ollama) — workhorse; n8n + Unsloth + artiscrapper depend on it
- `big-cloud`, `gpt-oss:120b-cloud`, `gpt-oss:20b-cloud` — Phase 19 RESS-WITH-TOOLS literal grep depends on `gpt-oss:20b-cloud`
- `vision-local`, `bge-reranker-local`, `embed-local`, `bge-m3-ollama`, `llama3.2:3b-instruct-q4_K_M`, `llama3.2-vision:11b-instruct-q4_K_M`
</dead_entries>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ModelEntrySchema with optional `disabled` field + filter helpers</name>
  <files>router/src/config/registry.ts</files>
  <action>
Add the `disabled` field to `ModelEntrySchema` (per D-01 LOCKED):

- Insert a new optional field `disabled: z.boolean().default(false)` on `ModelEntrySchema`, placed adjacent to `policy` for logical grouping. Add a JSDoc comment explaining: "Phase 20 / CAT-01 (v0.12.0) — operator flag for entries whose backend is intentionally offline on this host. When true, the entry is invisible to /v1/models and /v1/chat/completions resolution (treated as a 404 model_not_found at dispatch time). Defaults to false so existing YAML files require no migration."

- Add a NEW exported helper `enabledModels(reg: Registry): ModelEntry[]` immediately after `makeRegistryStore`. It returns `reg.models.filter((m) => !m.disabled)`. This is the canonical filter — both the route handler AND the resolve() path consume it.

- Modify `makeRegistryStore` so `resolve(name)` throws `RegistryUnknownModelError` if the looked-up entry has `disabled: true`. The error message MUST be identical to the "not in registry" path (the consumer should not learn whether an alias is unknown vs disabled — keeps the surface uniform per the strategic frame "consumer picks programmatically, not by reading docs"). Implementation: after `find`, if `!found || found.disabled` then throw. Pass `enabledModels(snapshot).map(m => m.name)` as the `available` list so the error envelope does not leak disabled names.

- Update `RegistrySchema.superRefine` VRAM-envelope summation to SKIP disabled entries (`if (m.disabled) continue` before the `sums.set` line). Disabled entries should not count against the VRAM budget — otherwise a re-enable-later flip would suddenly fail validation. Same skip pattern for the URL-uniqueness check and the dims-required-for-embeddings check (a disabled embeddings entry without dims should not block boot).

Constraints honored:
- Do NOT touch `/v1/embeddings` (P7-01 BLOCK — byte-identical wire shape from v0.11.0).
- Do NOT add any `_id$` labels (POL-06 — N/A here, no new metrics).
- Do NOT add any new external dependencies.

Per D-01 (LOCKED): the `disabled` field is the ONLY schema change. No `aliases:` block, no taxonomy renames, no removal of entries.
  </action>
  <verify>
    <automated>cd router &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "error TS" | wc -l | grep -q "^0$" &amp;&amp; echo "TYPECHECK_OK"</automated>
  </verify>
  <done>ModelEntrySchema has `disabled: z.boolean().default(false)` field; `enabledModels()` helper exported; `resolve()` treats disabled as not-found; superRefine skips disabled entries; tsc --noEmit reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Flag the 3 dead entries in router/models.yaml + wire route filter</name>
  <files>router/models.yaml, router/src/routes/v1/models.ts</files>
  <action>
**Part A — router/models.yaml:**

Add `disabled: true` plus a one-line comment to each of the 3 dead entries. Place the `disabled:` line as the LAST field in each entry so the diff is minimal. Comment format: `# Phase 20 / CAT-01 / D-01 — backend not running on this host (project_vram_budget: vllm/llamacpp redundantes). Flip to false + start backend to re-enable.`

The 3 entries (per `<dead_entries>` block above):
1. `qwen2.5-7b-instruct-q4km` (backend: llamacpp)
2. `qwen2.5-7b-instruct-awq` (backend: vllm)
3. `bge-m3-vllm` (backend: vllm-embed)

Do NOT touch any other entry. Do NOT touch the `backends:` map (per D-01 the backend declarations stay — re-enabling later is a 1-line flip without re-deriving structure).

**Part B — router/src/routes/v1/models.ts:**

Modify `filterAndProject` to apply the disabled filter BEFORE the allowlist filter:
- Import `enabledModels` from `'../../config/registry.js'`
- Change `reg.models.filter(...)` to `enabledModels(reg).filter(...)` — single-character spirit change

Modify the `GET /v1/models/:id` handler similarly:
- After `const entry = reg.models.find(...)`, add `if (entry?.disabled) return 404 model_not_found` (use the existing 404 envelope verbatim — copy it; do NOT extract a helper this plan)

Add an inline comment block above `filterAndProject` documenting Phase 20 / CAT-01:
```
// Phase 20 (v0.12.0 — CAT-01 / D-01): disabled entries are invisible to the
// public surface. enabledModels() applies the filter once; the allowlist filter
// stacks on top. /v1/models/:id returns 404 model_not_found for a disabled id
// (uniform surface — consumer does not learn whether an alias is unknown vs
// intentionally offline).
```

Per the strategic frame "consumer picks programmatically, not by reading docs": filtering at the surface boundary means consumers polling `/v1/models` see exactly the alias set that will resolve successfully on `/v1/chat/completions`.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/local-llms &amp;&amp; node -e "const yaml = require('./router/node_modules/js-yaml'); const fs = require('fs'); const reg = yaml.load(fs.readFileSync('router/models.yaml','utf8')); const disabled = reg.models.filter(m => m.disabled === true); const enabled = reg.models.filter(m => !m.disabled); console.log('disabled:', disabled.map(m => m.name).sort().join(',')); console.log('enabled_count:', enabled.length); process.exit(disabled.length === 3 &amp;&amp; enabled.length === 10 ? 0 : 1);"</automated>
  </verify>
  <done>models.yaml has exactly 3 entries with `disabled: true` (qwen2.5-7b-instruct-q4km, qwen2.5-7b-instruct-awq, bge-m3-vllm) sorted alphabetically; enabled count is exactly 10; /v1/models filter and /v1/models/:id 404 logic both gate on `disabled`; tsc --noEmit clean.</done>
</task>

<task type="auto">
  <name>Task 3: Unit test for disabled-filter behavior + backward compat</name>
  <files>router/src/config/__tests__/registry-disabled.test.ts</files>
  <action>
Create a new vitest unit test file at `router/src/config/__tests__/registry-disabled.test.ts`. The file must:

- Import `loadRegistryFromString`, `enabledModels`, `makeRegistryStore`, `RegistryUnknownModelError` from `../registry.js`
- Cover these cases (one `it()` per case):

  1. **Backward compat**: a YAML with NO `disabled` field on any entry loads cleanly, all entries default to `disabled: false`, `enabledModels()` returns ALL of them. (Anchors that pre-Phase-20 YAML files do not break.)
  2. **Filter excludes flagged entries**: a YAML with 3 entries, 1 flagged `disabled: true`, `enabledModels()` returns the 2 non-flagged.
  3. **resolve() throws on disabled**: `makeRegistryStore(reg).resolve('disabled-name')` throws `RegistryUnknownModelError` whose message is identical to the message for a fully-unknown name (anti-leak — consumer cannot distinguish unknown vs disabled).
  4. **resolve() returns enabled entries normally**: `resolve('enabled-name')` returns the entry.
  5. **VRAM envelope skips disabled**: a YAML where the SUM of vram_budget_gb across all entries (including disabled) exceeds VRAM_ENVELOPE_GB=16, but the sum across ENABLED-only entries is ≤16, loads without error. (Anchors that re-enabling later via 1-line flip is the right model.)
  6. **URL-uniqueness skips disabled**: a YAML where a disabled entry shares its backend_url with two distinct enabled backends would currently trigger the URL-collision superRefine — assert that with `disabled: true` on the offending entry, the registry loads cleanly.

- Use `loadRegistryFromString(yaml)` to keep the tests pure (no filesystem). Use minimal YAML fixtures inline as template-literal strings.
- Mirror the existing test convention in `router/src/config/__tests__/` (look at any sibling for style — `vi.describe`/`it`/`expect` pattern).
- Do NOT mock anything. Pure parse + filter assertions.
- Each `it()` must use real `expect()` calls — no `it.todo()` placeholders.
  </action>
  <verify>
    <automated>cd router &amp;&amp; npx vitest run src/config/__tests__/registry-disabled.test.ts 2>&amp;1 | tee /tmp/wave0-test.log | grep -E "Test Files.*passed|Tests.*passed" | tail -5</automated>
  </verify>
  <done>All 6 it() cases pass in the new test file; no skips, no todos; assertions verify CAT-01 / D-01 behavior end-to-end at the registry layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → models.yaml | Operator edits config; Zod schema is the trust gate at parse time. |
| router → backend (dispatch) | When a disabled entry is requested, the router rejects at preflight — never reaches the backend network call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | RegistryUnknownModelError message | mitigate | `available` list in the error envelope excludes disabled entries (uses `enabledModels()`) — consumer cannot enumerate disabled aliases via error messages. |
| T-20-02 | Tampering | models.yaml `disabled: true` flag | accept | Single-operator config; physical-host trust; same trust model as the rest of models.yaml. |
| T-20-03 | Denial of Service | Operator accidentally flips `chat-local` to disabled | accept | Strategic frame allows this — consumer gets a clean 404 not a 30s ENOTFOUND, which is strictly better. CAT-02 (Wave 1) adds the `health` field that makes this visible. |

No package-install tasks → no Package Legitimacy Audit needed.
</threat_model>

<verification>
After Tasks 1–3 land in source:

1. `cd router && npx tsc --noEmit` → exit 0
2. `cd router && npx vitest run src/config/__tests__/registry-disabled.test.ts` → all 6 cases pass
3. `cd router && npx vitest run src/config/__tests__/` → no regression on any pre-existing config test
4. `cd router && npx vitest run src/routes/__tests__/models.test.ts` (if exists) → no regression on /v1/models route tests
5. Operator-side smoke (executor performs after commit):
   - `git status` clean (working tree matches the planned 3 file changes)
   - `docker compose exec -T valkey valkey-cli -a "$VALKEY_PASSWORD" --no-auth-warning DEL 'model-registry:*'`
   - `docker compose up -d --force-recreate router`
   - `sleep 6 && curl -s -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" http://127.0.0.1:3210/v1/models | jq '.data | length'` → 10
   - `curl -s -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" http://127.0.0.1:3210/v1/models/qwen2.5-7b-instruct-q4km | jq '.error.code'` → `"model_not_found"`
   - `bash bin/smoke-test-router.sh --profile prod 2>&1 | grep -E "PASS.*RESS-WITH-TOOLS|FAIL.*RESS-WITH-TOOLS"` → contains PASS (or SKIP if OLLAMA_API_KEY unset — both are acceptable; FAIL is a regression)

If step 5 finds RESS-WITH-TOOLS regression → `git revert HEAD && docker compose up -d --force-recreate router` (Wave 0 is fully reversible by design).
</verification>

<success_criteria>
- [ ] router/models.yaml has exactly 3 entries with `disabled: true`, each accompanied by the comment per Task 2
- [ ] enabled model count is exactly 10 (down from 13)
- [ ] `cd router && npx tsc --noEmit` exits 0
- [ ] `cd router && npx vitest run src/config/__tests__/registry-disabled.test.ts` passes all 6 cases
- [ ] After Valkey DEL + force-recreate, `GET /v1/models` returns 10 entries
- [ ] `GET /v1/models/qwen2.5-7b-instruct-q4km` returns 404 `model_not_found` (same envelope as a fully-unknown alias)
- [ ] Phase 19 RESS-WITH-TOOLS smoke gate still PASSes (or SKIPs cleanly on missing OLLAMA_API_KEY) — NO regression
- [ ] One atomic commit `feat(20-01): flag dead-backend aliases as disabled — close CAT-01 [D-01]` containing all 3 file changes
- [ ] CAT-01 closed in the SUMMARY.md
</success_criteria>

<output>
Create `.planning/phases/20-model-catalog-hygiene-external-consumer-dx/20-01-SUMMARY.md` summarizing:
- What shipped (the 3 yaml flags + the schema field + the route filter + the unit test)
- Decisions honored (D-01 LOCKED — disabled:true not removal)
- Live verification transcript (10 entries on /v1/models, 404 on disabled-id, RESS-WITH-TOOLS still green)
- CAT-01 status: CLOSED
- Reversibility note: `git revert HEAD` + force-recreate restores prior 13-entry surface
</output>
