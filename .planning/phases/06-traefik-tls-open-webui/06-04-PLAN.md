---
phase: 06-traefik-tls-open-webui
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - bin/smoke-test-traefik.sh                # NEW — Phase 6 smoke (5+ assertions covering EDGE + WEBUI + D-B1/D-B2)
  - bin/smoke-test-router.sh                 # MUTATE — fix Pitfall 11 (loopback assumption removed from prod path) + reuse for dev profile
  - README.md                                # MUTATE — new Phase 6 section: Tailscale Services prereq, htpasswd recipe, smoke invocation, EDGE-05/EDGE-06 evidence commands
autonomous: false
requirements: [EDGE-01, EDGE-02, EDGE-03, EDGE-04, EDGE-05, EDGE-06, WEBUI-01, WEBUI-02, WEBUI-03, WEBUI-04, WEBUI-05]
tags: [smoke-test, readme, human-verify, edge-validation, sse-120s, metrics-blackhole, basic-auth, auto-discovery]

must_haves:
  truths:
    - "bin/smoke-test-traefik.sh exists, is executable (`chmod +x`), and runs from any cwd (uses `cd \"$(dirname \"$0\")/..\"` pattern matching bin/smoke-test-router.sh)"
    - "Smoke script asserts EDGE-03 statically: `docker compose config` returns zero `0.0.0.0:*` mappings across ALL services in the prod profile; only Traefik publishes (and only on 127.0.0.1:80 + 127.0.0.1:443)"
    - "Smoke script asserts EDGE-04 statically: traefik/traefik.yml contains `idleConnTimeout: 0s` and `responseHeaderTimeout: 0s` (load-bearing for SC3); `grep -ri compress traefik/` returns zero matches; `grep -ri buffering traefik/` returns zero matches"
    - "Smoke script asserts Tailscale Services advertisement: `tailscale serve status` shows both `svc:router` and `svc:chat` advertised; if not, prints a clear remediation message pointing at the README §Phase 6 admin-console prereq"
    - "Smoke script asserts EDGE-01 + EDGE-02 live: `curl -fsS https://router.${TAILNET_HOSTNAME}.ts.net/healthz` returns 200 with a Let's Encrypt cert (not self-signed); router service joins all four networks per `docker compose config | yq '.services.router.networks'`"
    - "Smoke script asserts D-B1 live: `curl -s -o /dev/null -w '%{http_code}' https://router.${TAILNET_HOSTNAME}.ts.net/metrics` returns exactly `404`"
    - "Smoke script asserts D-B2 live: `docker compose exec -T traefik wget -qO- http://router:3000/metrics | head -1 | grep -q '^# HELP'` succeeds (internal scrape bypasses Traefik, returns Prometheus exposition format)"
    - "Smoke script asserts EDGE-05 live: `curl -s -o /dev/null -w '%{http_code}' http://router.${TAILNET_HOSTNAME}.ts.net/healthz` returns 308 (or 301/302/307) — Tailscale Serve refuses plain HTTP and redirects to HTTPS"
    - "Smoke script asserts EDGE-06 live (the big one): a 120s+ generation streams through Tailscale → Traefik → router → Ollama without 502 or stall; deltas arrive < 1s apart throughout; terminates with `data: [DONE]`; total wall-clock time > 120s; can be skipped with `--quick` flag (which runs all other assertions in ~15s)"
    - "Smoke script asserts WEBUI-03 live: `curl -s -o /dev/null -w '%{http_code}' https://chat.${TAILNET_HOSTNAME}.ts.net/` returns 401 WITHOUT basic-auth creds; with `-u $TRAEFIK_BASIC_AUTH_USER:$TRAEFIK_BASIC_AUTH_PASS_PLAIN` returns 200; the response body does NOT contain a login form (proving WEBUI_AUTH=False posture)"
    - "Smoke script asserts WEBUI-04 live: `docker compose exec -T postgres psql -U app -d openwebui -c '\\dt'` shows tables AFTER first OWUI boot (e.g. `user`, `chat`, `auth`)"
    - "Smoke script asserts WEBUI-05 live: `docker compose logs --since=2m openwebui | grep -qE 'router:3000/v1/models|router:3000.*models'` — OWUI has made at least one auto-discovery hit to the router's /v1/models endpoint"
    - "Smoke script handles missing env vars cleanly: TAILNET_HOSTNAME, ROUTER_BEARER_TOKEN, TRAEFIK_BASIC_AUTH_USER, TRAEFIK_BASIC_AUTH_PASS_PLAIN, POSTGRES_PASSWORD are checked at top of script; missing → exit 2 with a clear message"
    - "bin/smoke-test-router.sh is updated (Pitfall 11 fix) — its `ROUTER_URL` default is unchanged for dev mode, but the prod-path assertions now route via `docker compose exec router ...` for internal checks; the script gains a `--profile {prod|dev}` flag (default dev for backward compat) so the existing Phase 2-5 smoke continues working under `--profile dev` with the router-dev:3000 host-port bypass"
    - "README §Phase 6 documents in order: (1) Tailscale prereq — admin console step to define svc:router + svc:chat, (2) host-side `tailscale serve --service=svc:router/--service=svc:chat` bootstrap commands, (3) htpasswd recipe for TRAEFIK_BASIC_AUTH (`htpasswd -nB admin | sed -e 's/\\$/\\$\\$/g'`), (4) OWUI first-boot warning (WEBUI_AUTH=False irreversible if DB has users), (5) smoke command (`bash bin/smoke-test-traefik.sh`), (6) EDGE-05 + EDGE-06 manual evidence commands for human-verify"
    - "Human-verify checkpoint at end of plan: operator runs `bash bin/smoke-test-traefik.sh` against the live stack; all 5 ROADMAP SC1-SC5 pass; operator signs off"
  artifacts:
    - path: "bin/smoke-test-traefik.sh"
      provides: "Phase 6 smoke — 12+ assertions (static + Tailscale + live HTTP) gating all 11 phase requirements. --quick flag skips the 120s SSE for fast iteration. Exit 0 on full pass, 1 on any failure, 2 on missing env."
      contains: "smoke-test-traefik"
    - path: "bin/smoke-test-router.sh"
      provides: "Pitfall 11 fix — gains `--profile {prod|dev}` flag (default `dev` for backward compat); in `--profile prod` mode, all router probes route via `docker compose exec router ...` instead of `curl http://localhost:3000`; in `--profile dev` mode, unchanged behavior."
      contains: "--profile"
    - path: "README.md"
      provides: "New Phase 6 section after Phase 5 section. Tailscale Services prereq (admin console + CLI), htpasswd recipe with $$-escaping explanation, OWUI first-boot warning, smoke command, EDGE-05/EDGE-06 manual evidence."
      contains: "## Phase 6"
  key_links:
    - from: "bin/smoke-test-traefik.sh ASSERTION 'no public 0.0.0.0 bindings'"
      to: "docker compose config (consumes the post-Plan-01/02/03 compose.yml)"
      via: "awk + grep pattern matching prod-profile services"
      pattern: "0\\.0\\.0\\.0:"
    - from: "bin/smoke-test-traefik.sh ASSERTION '/metrics 404 external'"
      to: "traefik/dynamic/middlewares.yml metrics-blackhole + compose.yml router service label `middlewares=metrics-blackhole@file`"
      via: "live HTTP curl through Tailscale → Traefik → middleware-rewrite → Fastify 404"
      pattern: "curl -s.*metrics"
    - from: "bin/smoke-test-traefik.sh ASSERTION 'WEBUI auto-discovery'"
      to: "docker compose logs openwebui (OWUI logs the /v1/models GET when it auto-discovers)"
      via: "since=2m time-bounded log grep"
      pattern: "router:3000.*models"
    - from: "README §Phase 6 Tailscale prereq"
      to: ".planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §Validated Code Snippets #1 (Tailscale Services bootstrap)"
      via: "operator follows the README which mirrors 06-RESEARCH's verbatim CLI shape"
      pattern: "tailscale serve --service=svc:"
---

<objective>
Close Phase 6 by producing the end-to-end smoke test, the README operational section, and a human-verify checkpoint against the live stack. This is the only plan with a `checkpoint:human-verify` task; all prior plans in this phase are autonomous.

Deliverables:
1. `bin/smoke-test-traefik.sh` (NEW) — 12+ assertions covering all 11 phase requirements (EDGE-01..06 + WEBUI-01..05) plus D-B1/D-B2 evidence. Includes the load-bearing 120s+ E2E SSE proof for EDGE-06 / Pitfall 3. Includes a `--quick` flag that skips the 120s SSE for fast iteration in tight CI loops.
2. `bin/smoke-test-router.sh` (MUTATE) — Pitfall 11 fix. The Phase 2-5 smoke assumed `127.0.0.1:3000:3000` host port on the prod router service, which Plan 06-02 removed. Add a `--profile {prod|dev}` flag (default `dev` for backward compat with the existing CI invocation), and in `--profile prod` mode route all router probes via `docker compose exec router ...`.
3. `README.md` §Phase 6 (NEW SECTION) — operational runbook documenting: Tailscale Services prereq (admin console + CLI), htpasswd recipe with the `$$`-escaping rule, OWUI first-boot warning (WEBUI_AUTH=False irreversibility), smoke invocation, EDGE-05 + EDGE-06 manual evidence commands.
4. Human-verify checkpoint task — operator brings the full stack up against live Ollama, runs `bash bin/smoke-test-traefik.sh`, and signs off that all 5 ROADMAP Phase 6 success criteria pass.

Purpose:
- All 11 Phase 6 requirements (EDGE-01..06 + WEBUI-01..05) are verified end-to-end.
- The 120s+ SSE through Traefik proof (EDGE-06 / Pitfall 3) is the load-bearing assertion that the `idleConnTimeout: 0s` knob in Plan 06-01 actually works.
- Pitfall 11 carry-forward (Plan 02 removed the host port; the existing Phase 2-5 smoke must be made to continue working) is closed.
- README runbook teaches the operator the prereq + recovery paths.
- The human-verify checkpoint is the final gating step before Phase 6 is declared complete.

Output:
- `bin/smoke-test-traefik.sh` committed and passes against a live stack.
- `bin/smoke-test-router.sh` updated; `bash bin/smoke-test-router.sh --profile dev` continues to pass (no regression to Phase 2-5).
- `README.md` updated with Phase 6 section.
- Human-verify checkpoint: operator signs off that the full smoke is green and ROADMAP SC1-SC5 are satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-traefik-tls-open-webui/06-CONTEXT.md
@.planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md
@.planning/phases/06-traefik-tls-open-webui/06-01-PLAN.md
@.planning/phases/06-traefik-tls-open-webui/06-02-PLAN.md
@.planning/phases/06-traefik-tls-open-webui/06-03-PLAN.md
@CLAUDE.md
@compose.yml
@bin/smoke-test-router.sh
@README.md

<interfaces>
<!-- Existing smoke patterns to mirror. -->

From bin/smoke-test-router.sh (current state — pre-Plan 06-04):
- Uses `set -uo pipefail` (NOT `-e` — accumulates failures).
- Reads ROUTER_BEARER_TOKEN from caller env first, then .env (preflight-gpu.sh lines 107-125 pattern).
- Uses a FAILURES counter; exits 0 on all-pass, 1 on any-fail.
- Section headers via `echo "▶ <name>"`.
- Uses `docker compose exec -T ollama curl ...` for internal probes (Phase 2 D-A4 pattern — same approach Plan 06-04 must use for the router prod path).

From 06-RESEARCH §"Validated Code Snippets" #9 (`bin/smoke-test-traefik.sh` skeleton — verbatim shape — lines 1120-1218). The skeleton covers static + Tailscale + live HTTP assertions. The full 120s SSE block continues past line 1218.

From .env (operator's actual file — NOT committed):
- TAILNET_HOSTNAME (Plan 06-01 added to .env.example)
- ROUTER_BEARER_TOKEN (Phase 2)
- TRAEFIK_BASIC_AUTH (Plan 06-01 refreshed — the $$-doubled htpasswd hash)
- POSTGRES_PASSWORD (Phase 5)
- OWUI_SECRET_KEY (Plan 06-01 added)

For smoke purposes: the script needs the PLAIN basic-auth password to run `curl -u user:password`. Add TWO new .env vars in this plan via .env.example (this is the ONLY .env.example mutation Plan 06-04 makes — Plan 06-01 owned all the others):
- `TRAEFIK_BASIC_AUTH_USER` — plain username (e.g. `admin`)
- `TRAEFIK_BASIC_AUTH_PASS_PLAIN` — plain password used ONLY for smoke; NEVER pass this to Traefik; document that production rotates this OUT after smoke gates pass

NOTE: Adding two more .env.example vars in Plan 06-04 is a small additive mutation; it does NOT regress Plan 06-01's .env work. The smoke-only vars are append-only at the end of the Phase 6 .env section.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/smoke-test-traefik.sh — 12+ assertions covering all 11 phase requirements</name>
  <files>
    bin/smoke-test-traefik.sh (NEW),
    .env.example (MUTATE — APPEND TRAEFIK_BASIC_AUTH_USER + TRAEFIK_BASIC_AUTH_PASS_PLAIN at end of Phase 6 section)
  </files>
  <read_first>
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Validated Code Snippets" #9 (smoke skeleton — verbatim, complete it through the 120s SSE),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Validation Architecture" Phase Requirements → Test Map (each row is one assertion),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Common Pitfalls" Pitfall 3 (120s+ idle-conn 90s default → 502 — the regression gate this smoke proves the fix for),
    .planning/phases/06-traefik-tls-open-webui/06-CONTEXT.md §"Specifics" (120s+ smoke recipe + /metrics block assertion verbatim),
    bin/smoke-test-router.sh (existing patterns to mirror — set -uo pipefail, FAILURES counter, section headers, env-loading priority)
  </read_first>
  <action>
    Create `bin/smoke-test-traefik.sh` per 06-RESEARCH §"Validated Code Snippets" #9. The script MUST cover, in this order:

    **Header + bootstrap:**
    - Shebang `#!/usr/bin/env bash`.
    - Header comment: purpose, requirements covered, usage with `--quick` flag, exit codes.
    - `set -uo pipefail` (NOT `-e` — accumulate failures).
    - `cd "$(dirname "$0")/.."` so script works from any cwd.
    - Source `.env` (use `set -a; source .env; set +a` or `source <(grep -v '^#' .env | grep '=')` per the existing smoke-test-router.sh pattern).
    - Parse `--quick` flag.
    - PASS/FAIL counters + `ok()` / `fail()` helpers (mirror smoke-test-router.sh; do NOT invent new shapes).
    - `require()` helper that exits 2 with a clear message if a required env var is unset.
    - Required env vars (all must be set; missing → exit 2): TAILNET_HOSTNAME, ROUTER_BEARER_TOKEN, POSTGRES_PASSWORD, TRAEFIK_BASIC_AUTH_USER, TRAEFIK_BASIC_AUTH_PASS_PLAIN.
    - Compute ROUTER_FQDN=`router.${TAILNET_HOSTNAME}.ts.net` and CHAT_FQDN=`chat.${TAILNET_HOSTNAME}.ts.net`.

    **Section 1 — Static (no live containers needed) — runs in `--quick` mode:**
    1. Assert no `0.0.0.0:*` mappings in `docker compose config` (EDGE-03).
    2. Assert only traefik publishes host ports (use awk to walk service blocks; non-traefik AND non-router-dev services must have zero `127.0.0.1:` mappings).
    3. Assert `idleConnTimeout: 0s` and `responseHeaderTimeout: 0s` present in `traefik/traefik.yml` (EDGE-04).
    4. Assert no `compress` middleware anywhere in `traefik/` (`grep -ri compress traefik/` returns nothing; if found, FAIL — Pitfall 4).
    5. Assert no `buffering` middleware anywhere in `traefik/`.
    6. Assert four-networks topology unchanged: `docker compose config | yq '.networks | keys | sort'` equals `[app, backend, data, edge]` (EDGE-02).

    **Section 2 — Tailscale advertisement (no live HTTP yet) — runs in `--quick` mode:**
    7. Assert `tailscale serve status` lists both `svc:router` and `svc:chat`. If `tailscale` binary missing OR command fails, FAIL with message: "Run admin-console prereq + `sudo tailscale serve --service=svc:router --https=443 127.0.0.1:80` per README §Phase 6".

    **Section 3 — Live HTTP through Tailscale → Traefik → router/owui — runs in `--quick` mode (except #11):**
    8. Assert EDGE-01: `curl -fsS --max-time 10 https://${ROUTER_FQDN}/healthz` returns 200 (router reachable via Tailscale-provisioned TLS).
    9. Assert D-B1: `curl -s -o /dev/null -w '%{http_code}' --max-time 10 https://${ROUTER_FQDN}/metrics` returns exactly `404`.
    10. Assert D-B2: `docker compose exec -T traefik wget -qO- http://router:3000/metrics | head -1 | grep -q '^# HELP'` succeeds.
    11. Assert EDGE-05: `curl -s -o /dev/null -w '%{http_code}' --max-time 10 http://${ROUTER_FQDN}/healthz` returns 308 (or 301/302/307) — Tailscale Serve refuses plain HTTP.
    12. Assert WEBUI-03: `curl -s -o /dev/null -w '%{http_code}' --max-time 10 https://${CHAT_FQDN}/` returns 401 WITHOUT creds; with `-u $TRAEFIK_BASIC_AUTH_USER:$TRAEFIK_BASIC_AUTH_PASS_PLAIN` returns 200 (or 302 to /auth — accept either; the key is "passes basic-auth").
    13. Assert WEBUI-03 posture: GET https://${CHAT_FQDN}/ (with basic-auth creds) — response body MUST NOT contain `<form` patterns suggesting an OWUI login form (proves WEBUI_AUTH=False is active).
    14. Assert WEBUI-04: `docker compose exec -T postgres psql -U app -d openwebui -c '\\dt' | grep -qE 'user|chat'` (after OWUI has booted and run its first DB migration).
    15. Assert WEBUI-05: `docker compose logs --since=5m openwebui | grep -qE 'router:3000.*models|router:3000/v1/models'`. If no match in 5m, INFO message: "open https://${CHAT_FQDN} once and re-run" (this is OK on a cold smoke — auto-discovery is on a cadence).

    **Section 4 — Long generation (EDGE-06 / Pitfall 3) — SKIPPED if --quick:**
    16. The big one. Run a 120s+ generation through Tailscale + Traefik + router + Ollama. Use the recipe from 06-CONTEXT §Specifics verbatim:
        ```
        curl -N --max-time 180 \
          -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" \
          -d '{"model":"llama3.2:3b-instruct-q4_K_M","messages":[{"role":"user","content":"count to 200 very slowly"}],"stream":true,"max_tokens":1200}' \
          https://${ROUTER_FQDN}/v1/chat/completions
        ```
        Capture timing markers: stream start, first delta, last delta, terminator `data: [DONE]`. Assert: deltas arrive < 1s apart (use awk on stream timestamps), total wall-clock > 120s, no `HTTP/2 502` or `HTTP/1.1 502`, terminator present.
    - Document model name via `SMOKE_MODEL` env var override (default `llama3.2:3b-instruct-q4_K_M` per 06-CONTEXT §Specifics).
    - Skip the entire section if --quick is set.

    **Footer:**
    - Print summary: `PASS=$PASS / FAIL=$FAIL`.
    - Exit 0 if FAIL==0, else exit 1.

    **Also mutate `.env.example`** (small additive change — append to the Phase 6 section after TRAEFIK_BASIC_AUTH or OWUI_SECRET_KEY):
    ```
    # ── Phase 6 smoke (NOT used by Traefik or OWUI; only by bin/smoke-test-traefik.sh) ──
    # Plain basic-auth credentials used by the smoke test to exercise the
    # chat.<TAILNET_HOSTNAME>.ts.net basic-auth gate. MUST match the credentials
    # that hashed to TRAEFIK_BASIC_AUTH above. Smoke-only — does not affect Traefik.
    # If you rotate TRAEFIK_BASIC_AUTH, rotate these in lockstep.
    TRAEFIK_BASIC_AUTH_USER=
    TRAEFIK_BASIC_AUTH_PASS_PLAIN=
    ```

    Make the script executable: `chmod +x bin/smoke-test-traefik.sh`.
  </action>
  <verify>
    <automated>test -f bin/smoke-test-traefik.sh \
      && test -x bin/smoke-test-traefik.sh \
      && bash -n bin/smoke-test-traefik.sh \
      && grep -q "TAILNET_HOSTNAME" bin/smoke-test-traefik.sh \
      && grep -q "svc:router" bin/smoke-test-traefik.sh \
      && grep -q "svc:chat" bin/smoke-test-traefik.sh \
      && grep -q "/metrics" bin/smoke-test-traefik.sh \
      && grep -q "120s\|--max-time 180\|count to 200" bin/smoke-test-traefik.sh \
      && grep -q -- "--quick" bin/smoke-test-traefik.sh \
      && grep -q "TRAEFIK_BASIC_AUTH_USER" .env.example \
      && grep -q "TRAEFIK_BASIC_AUTH_PASS_PLAIN" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `bin/smoke-test-traefik.sh` exists, has `+x` permission, passes `bash -n` syntax check.
    - The script references each of: TAILNET_HOSTNAME, ROUTER_BEARER_TOKEN, POSTGRES_PASSWORD, TRAEFIK_BASIC_AUTH_USER, TRAEFIK_BASIC_AUTH_PASS_PLAIN — and exits with code 2 (with a specific message) if any is unset.
    - The script implements at least 15 of the 16 assertions enumerated above (assertion 16 is the 120s SSE — present but gated by --quick).
    - The script's PASS/FAIL discipline matches smoke-test-router.sh (counter increment, summary line, exit 0/1).
    - `.env.example` has TRAEFIK_BASIC_AUTH_USER and TRAEFIK_BASIC_AUTH_PASS_PLAIN appended (both empty defaults; both commented to explain they are smoke-only).
    - Running `bash bin/smoke-test-traefik.sh --quick` against a live stack (Plans 01-03 applied + Tailscale Services bootstrapped + .env populated) returns exit 0 with FAIL=0.
  </acceptance_criteria>
  <done>
    Smoke script is in place, executable, passes syntax check. Covers all 11 phase requirements + D-B1 + D-B2 + Pitfall 11 dev/prod profile split. Ready for Task 2 (smoke-test-router.sh fix) and Task 3 (README).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update bin/smoke-test-router.sh — add --profile {prod|dev} flag for Pitfall 11</name>
  <files>
    bin/smoke-test-router.sh (MUTATE — add CLI flag + branch internal-vs-host probes by profile)
  </files>
  <read_first>
    bin/smoke-test-router.sh (entire file — to understand the existing structure and the spots that reference http://127.0.0.1:3000 or http://localhost:3000),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Common Pitfalls" Pitfall 11 (Removing 127.0.0.1:3000:3000 breaks Plan-5 smoke scripts),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Pattern 6: router-dev discretion" (dev keeps host port; smoke continues working in dev mode),
    .planning/phases/06-traefik-tls-open-webui/06-02-PLAN.md (confirms router-dev: keeps 127.0.0.1:3000:3000)
  </read_first>
  <action>
    Mutate `bin/smoke-test-router.sh` to handle the Pitfall 11 scenario. Plan 06-02 removed the `127.0.0.1:3000:3000` host port from the prod `router:` service; the existing smoke-test-router.sh hits `http://127.0.0.1:3000/...` directly which no longer works for prod. The router-dev: service (under `--profile dev`) keeps the host port.

    **Add CLI flag** `--profile {prod|dev}` to the argument parser. Default is `dev` for backward compatibility (existing CI invocations don't pass --profile and should continue working — router-dev: is up via the dev profile).

    **Branch internal probes** based on profile:
    - In `--profile dev` mode (default): keep existing behavior. ROUTER_URL defaults to `http://127.0.0.1:3000` (router-dev host port).
    - In `--profile prod` mode: ROUTER_URL becomes a sentinel like `internal://router:3000`. All curl invocations against ROUTER_URL must be replaced by `docker compose exec -T router curl ...` calls. Use a helper function `router_curl()` that wraps the two cases.

    **Minimal-change approach** (recommended to avoid a rewrite):
    1. Add `--profile prod|dev` CLI flag parsing.
    2. Set `ROUTER_PROBE_MODE` based on flag: `host` (default — dev) or `exec` (prod).
    3. Define a wrapper:
       ```
       router_curl() {
         if [ "$ROUTER_PROBE_MODE" = "exec" ]; then
           docker compose exec -T router curl "$@"
         else
           curl "$@"
         fi
       }
       ```
    4. Find every `curl ... "$ROUTER_URL"...` invocation in the existing script and replace with `router_curl ... "http://localhost:3000"...` (when in exec mode, the in-container URL is `http://localhost:3000`; when in host mode, ROUTER_URL is `http://127.0.0.1:3000`).
    5. Update the usage block to document the new flag with a clear example: `bash bin/smoke-test-router.sh --profile prod` to exercise the prod router via `docker compose exec`.

    Verify with `bash -n bin/smoke-test-router.sh` and a live `bash bin/smoke-test-router.sh --profile dev` (existing Phase 2-5 smoke must continue to pass with no regression).
  </action>
  <verify>
    <automated>bash -n bin/smoke-test-router.sh \
      && grep -q -- "--profile" bin/smoke-test-router.sh \
      && grep -qE "ROUTER_PROBE_MODE|router_curl|docker compose exec.*router" bin/smoke-test-router.sh \
      && grep -qE "prod\|dev" bin/smoke-test-router.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/smoke-test-router.sh` exits 0.
    - The script accepts `--profile prod` and `--profile dev` arguments.
    - Default profile is `dev` (verified: running the script with no args and the dev profile up produces the same output as pre-Plan 06-04).
    - In `--profile prod`, all router-direct probes route via `docker compose exec -T router ...` (no host loopback assumptions).
    - The script's usage/help block documents the new flag.
    - Running `bash bin/smoke-test-router.sh --profile dev` against the dev-profile-up stack returns exit 0 with FAILURES=0 (no regression to Phase 2-5 smoke).
  </acceptance_criteria>
  <done>
    Pitfall 11 closed. Phase 2-5 smoke continues working under `--profile dev`; Phase 6 prod stack is exercisable under `--profile prod`. Plan 06-04 smoke (bin/smoke-test-traefik.sh) covers the external-via-Tailscale path orthogonally.
  </done>
</task>

<task type="auto">
  <name>Task 3: README §Phase 6 — Tailscale prereq + htpasswd recipe + smoke command + EDGE-05/EDGE-06 evidence</name>
  <files>
    README.md (MUTATE — APPEND new section after Phase 5 section)
  </files>
  <read_first>
    README.md (entire file — find where Phase 5 section ends; the new Phase 6 section goes immediately after),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Validated Code Snippets" #1 (Tailscale Services bootstrap — verbatim CLI shape),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Validated Code Snippets" #4 (htpasswd recipe with $$-escaping — verbatim),
    .planning/phases/06-traefik-tls-open-webui/06-RESEARCH.md §"Common Pitfalls" Pitfall 1 (admin-console prereq), Pitfall 2 (WEBUI_AUTH=False fresh-DB requirement) — fold both warnings into the README,
    .planning/phases/06-traefik-tls-open-webui/06-CONTEXT.md (D-A6 host-side Tailscale daemon prereq; D-C3 WEBUI_AUTH irreversibility)
  </read_first>
  <action>
    Append a new `## Phase 6 — Traefik + TLS + Open WebUI` section to README.md. The section MUST appear AFTER the existing Phase 5 section and BEFORE any later content (if Phase 7+ sections exist as placeholders, this goes before them). Structure:

    **Section 1 — Prereq: Tailscale Services.** Document in order:
    1. The admin-console step: navigate to https://login.tailscale.com/admin → Services → Advertise → Define a Service. Define `router` (endpoint `tcp:443`) and `chat` (endpoint `tcp:443`).
    2. The host-side CLI advertisement commands (06-RESEARCH §Snippet #1 verbatim):
       ```
       sudo tailscale serve --service=svc:router --https=443 127.0.0.1:80
       sudo tailscale serve --service=svc:chat   --https=443 127.0.0.1:80
       tailscale serve status
       ```
    3. The approval step (if auto-approval is off): admin must approve the host in the Services console.
    4. Reference Pitfall 1 ("services NOT auto-created by CLI" — admin-console step is non-skippable).

    **Section 2 — Prereq: .env values.** List the new Phase 6 vars in .env that the operator must populate:
    - TAILNET_HOSTNAME — get via `tailscale status --json | jq -r '.MagicDNSSuffix' | sed 's/\\.$//' | sed 's/\\.ts\\.net$//'`.
    - OWUI_SECRET_KEY — generate with `openssl rand -hex 32`. NEVER rotate without recreating the openwebui DB (Pitfall 10).
    - TRAEFIK_BASIC_AUTH — generate with the htpasswd recipe (next section).
    - TRAEFIK_BASIC_AUTH_USER + TRAEFIK_BASIC_AUTH_PASS_PLAIN — smoke-only; same credentials in plaintext.

    **Section 3 — htpasswd recipe** (06-RESEARCH §Snippet #4 verbatim):
    ```
    # Option A — htpasswd installed on host:
    htpasswd -nB admin | sed -e 's/\\$/\\$\\$/g'

    # Option B — dockerized:
    docker run --rm httpd:2.4-alpine htpasswd -nbB admin "your-strong-password" | sed -e 's/\\$/\\$\\$/g'

    # Paste output verbatim (with all $$ signs preserved) into .env:
    #   TRAEFIK_BASIC_AUTH=admin:$$2y$$05$$...

    # Verify Compose interpolation collapses to single $ as expected:
    docker compose config | grep webui-basic-auth.basicauth.users
    # Expected single $ in rendered output.
    ```
    Explain the `$$` escaping rule: Compose interpolation eats single `$`, so .env stores `$$` and Compose renders `$` in the label.

    **Section 4 — OWUI first-boot warning.** Document the WEBUI_AUTH=False / Pitfall 2 trap:
    > "WEBUI_AUTH=False is IRREVERSIBLE once OWUI has any user in its DB. Phase 5's postgres/initdb/01-init.sql creates the openwebui DB empty, so the precondition holds at first boot. If you ever boot OWUI with WEBUI_AUTH=True 'just to test', you cannot flip it back. Recovery requires: `docker compose down openwebui`; `docker compose exec postgres psql -U app -c 'DROP DATABASE openwebui; CREATE DATABASE openwebui OWNER app;'`; `rm -rf ${HOST_DATA_ROOT}/openwebui/`; redeploy with WEBUI_AUTH=False."

    **Section 5 — Bring it up.**
    ```
    docker compose up -d
    # Wait for healthy:
    docker compose ps --format '{{.Name}} {{.Health}}'
    ```

    **Section 6 — Smoke command.**
    ```
    # Full suite (~3 min including 120s+ SSE):
    bash bin/smoke-test-traefik.sh

    # Quick mode (~15s, skips the 120s SSE):
    bash bin/smoke-test-traefik.sh --quick
    ```
    Document expected output (all assertions [OK], exit 0).

    **Section 7 — Manual evidence commands (EDGE-05 + EDGE-06).**
    EDGE-05 manual proof:
    ```
    # HTTP → HTTPS redirect at Tailscale Serve
    curl -i http://router.${TAILNET_HOSTNAME}.ts.net/healthz
    # Expected: HTTP/1.1 308 (or 301/302/307) + Location: https://...
    ```
    EDGE-06 manual proof (120s+ generation through proxy — exact recipe from 06-CONTEXT §Specifics):
    ```
    curl -N --max-time 180 \
      -H "Authorization: Bearer $ROUTER_BEARER_TOKEN" \
      -d '{"model":"llama3.2:3b-instruct-q4_K_M","messages":[{"role":"user","content":"count to 200 very slowly"}],"stream":true,"max_tokens":1200}' \
      https://router.${TAILNET_HOSTNAME}.ts.net/v1/chat/completions
    # Expected: deltas < 1s apart throughout; total > 120s; terminates with data: [DONE]; no 502.
    ```

    **Section 8 — Dev mode bypasses Tailscale.** Document the `--profile dev` shortcut:
    ```
    # Iterate on router code without Tailscale roundtrip:
    docker compose --profile dev up router-dev
    # Direct host loopback:
    curl http://127.0.0.1:3000/healthz
    # Phase 2-5 smoke continues to work in dev mode:
    bash bin/smoke-test-router.sh --profile dev
    ```

    **Section 9 — /metrics is internal-only.** Note that `/metrics` now returns 404 externally (D-B1 / metrics-blackhole middleware) but is still reachable inside Docker for Phase 7's Prometheus:
    ```
    # External: 404
    curl -i https://router.${TAILNET_HOSTNAME}.ts.net/metrics
    # Internal (Phase 7 Prometheus path):
    docker compose exec -T traefik wget -qO- http://router:3000/metrics | head
    ```

    Preserve all existing README content unchanged. The new section is purely additive.
  </action>
  <verify>
    <automated>grep -q "## Phase 6" README.md \
      && grep -q "tailscale serve --service=svc:router" README.md \
      && grep -q "tailscale serve --service=svc:chat" README.md \
      && grep -q "htpasswd -nB" README.md \
      && grep -q "WEBUI_AUTH=False is IRREVERSIBLE\|IRREVERSIBLE\|irreversible" README.md \
      && grep -q "bin/smoke-test-traefik.sh" README.md \
      && grep -q "EDGE-05\|HTTP → HTTPS\|HTTP -> HTTPS\|HTTP→HTTPS\|308" README.md \
      && grep -q "count to 200\|120s\|--max-time 180" README.md \
      && grep -qE "--profile dev|router-dev" README.md \
      && grep -qE "/metrics" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md has a new `## Phase 6 — Traefik + TLS + Open WebUI` section (or similar heading).
    - The section documents Tailscale Services prereq with both the admin-console step AND the CLI commands.
    - The htpasswd recipe with `$$`-escaping rule is documented with the explanation.
    - The WEBUI_AUTH=False irreversibility warning is present with recovery procedure.
    - The smoke command (`bash bin/smoke-test-traefik.sh` and `--quick` variant) is documented.
    - EDGE-05 + EDGE-06 manual evidence commands are present (the exact curl recipes).
    - Dev-mode bypass (`--profile dev` + `127.0.0.1:3000`) is documented.
    - The /metrics external-vs-internal split is documented.
    - All pre-Plan-04 README content is preserved unchanged.
  </acceptance_criteria>
  <done>
    README §Phase 6 section is complete and self-contained. An operator reading it cold can: provision the Tailscale Services, populate .env, bring up the stack, run the smoke, and verify EDGE-05/EDGE-06 manually.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify checkpoint — run full smoke against live stack + sign-off on ROADMAP SC1-SC5</name>
  <what-built>
    Plans 06-01 through 06-04 collectively delivered:
    - `traefik:v3.7.1` service running on `127.0.0.1:80` + `127.0.0.1:443` (loopback-only) with SSE-friendly knobs (idleConnTimeout: 0s, responseHeaderTimeout: 0s) and dashboard fully disabled (`api=false`).
    - `traefik/dynamic/middlewares.yml` with `metrics-blackhole` middleware closing the Phase 5 D-C5 `/metrics` external-block carry-forward.
    - Router service mutated: no host port (EDGE-03), attached to all four networks (EDGE-02), Traefik labels routing `router.<tailnet>.ts.net` with `metrics-blackhole` middleware.
    - Open WebUI v0.9.0 service with `WEBUI_AUTH=False` from boot zero, `ENABLE_OLLAMA_API=False` defense-in-depth, `OPENAI_API_BASE_URLS=http://router:3000` (no /v1 suffix), Traefik basic-auth gate on `chat.<tailnet>.ts.net`.
    - `bin/smoke-test-traefik.sh` covering 12+ assertions across all 11 phase requirements + D-B1/D-B2.
    - `bin/smoke-test-router.sh` updated with `--profile {prod|dev}` flag (Pitfall 11 fix).
    - README §Phase 6 operational runbook.
  </what-built>
  <how-to-verify>
    Prereq check (operator runs ONCE before bringing stack up):
    1. Confirm Tailscale admin console has `svc:router` and `svc:chat` defined (Services → Advertise → Define a Service).
    2. Confirm `.env` has populated values for: TAILNET_HOSTNAME, ROUTER_BEARER_TOKEN, POSTGRES_PASSWORD, OWUI_SECRET_KEY, TRAEFIK_BASIC_AUTH, TRAEFIK_BASIC_AUTH_USER, TRAEFIK_BASIC_AUTH_PASS_PLAIN.
    3. Confirm host has run `sudo tailscale serve --service=svc:router --https=443 127.0.0.1:80` AND `sudo tailscale serve --service=svc:chat --https=443 127.0.0.1:80`. Verify with `tailscale serve status`.

    Then bring the full stack up:
    ```
    docker compose up -d
    # Wait ~60s for all services healthy:
    docker compose ps --format 'table {{.Name}}\t{{.Health}}'
    # Expected: every service except gpu-preflight (one-shot) shows "healthy".
    ```

    Then run the full smoke (takes ~3 minutes including the 120s SSE):
    ```
    bash bin/smoke-test-traefik.sh
    ```
    Expected output: every `▶` section line prints `[OK]` for each assertion; final summary shows `PASS=N / FAIL=0`; script exits 0.

    Then manually verify each of the 5 ROADMAP Phase 6 success criteria:
    - **SC1** (TLS-fronted; HTTP→HTTPS; zero 0.0.0.0:* on backends): `docker compose ps` shows `127.0.0.1:80->80/tcp, 127.0.0.1:443->443/tcp` ONLY on traefik (and `127.0.0.1:3000->3000/tcp` on router-dev IF the dev profile is up). `curl -i http://router.${TAILNET_HOSTNAME}.ts.net/healthz` returns 308 to HTTPS.
    - **SC2** (four-network topology + router on all four): `docker compose config | yq '.services.router.networks | sort'` returns `[app, backend, data, edge]`. `docker compose config | yq '.networks | keys | sort'` returns `[app, backend, data, edge]`.
    - **SC3** (120s+ SSE through Traefik): the smoke's `EDGE-06` section passes [OK]. Optionally re-run the manual recipe from README §Phase 6 Section 7.
    - **SC4** (OWUI on chat subdomain with WEBUI_AUTH=False + basic-auth gate + shared Postgres): the smoke's `WEBUI-03` and `WEBUI-04` assertions pass [OK]. Optionally open `https://chat.${TAILNET_HOSTNAME}.ts.net` in a browser — get basic-auth challenge, enter creds, land directly in OWUI chat (NO OWUI login page — proving WEBUI_AUTH=False).
    - **SC5** (single connection to router, no /v1 suffix, auto-discovery): the smoke's `WEBUI-02` and `WEBUI-05` assertions pass [OK]. Optionally inspect OWUI admin Connections panel (top-right gear) — should show exactly ONE connection pointing at `http://router:3000` (no /v1 suffix).
  </how-to-verify>
  <resume-signal>Type "approved" (smoke green + all 5 SCs verified) or describe issues. If issues, the planner will work with you to create a gap-closure plan via `/gsd-plan-phase 6 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Already enumerated in Plans 06-01, 06-02, 06-03. Plan 06-04 introduces no new trust boundaries — it consumes and verifies the boundaries set up by the prior plans.

## STRIDE Threat Register (Plan 06-04-specific)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information Disclosure | TRAEFIK_BASIC_AUTH_PASS_PLAIN in .env | accept | Smoke-only credential lives in the operator's .env (gitignored). Same trust boundary as POSTGRES_PASSWORD. Document in README that rotation should happen in lockstep with TRAEFIK_BASIC_AUTH if creds are ever changed. |
| T-06-16 | Tampering | Smoke script accidentally edits stack config | mitigate | bin/smoke-test-traefik.sh is read-only (curl + grep + docker compose exec — no write paths). Reviewed in Task 1 acceptance criteria. |
</threat_model>

<verification>
End-of-plan checks (closes Phase 6):

1. **Smoke exists and is executable:** `test -x bin/smoke-test-traefik.sh`.
2. **Smoke syntax valid:** `bash -n bin/smoke-test-traefik.sh`.
3. **Smoke covers all 11 requirements:** grep the script for explicit references to each of EDGE-01..06 + WEBUI-01..05 OR equivalent assertion patterns. (Grep gates already in Task 1.)
4. **Updated smoke-test-router.sh:** `bash -n bin/smoke-test-router.sh && bash bin/smoke-test-router.sh --profile dev` returns 0 (no regression).
5. **README Phase 6 section present:** `grep -q "## Phase 6" README.md`.
6. **Live smoke green** (gates the human-verify checkpoint): `bash bin/smoke-test-traefik.sh` exits 0 with all assertions [OK].
7. **Human approval received** via `<resume-signal>`.
</verification>

<success_criteria>
- All 11 Phase 6 requirements (EDGE-01..06 + WEBUI-01..05) verified live.
- All 5 ROADMAP Phase 6 SCs satisfied with smoke evidence + human sign-off.
- Pitfall 11 (Plan-5 smoke breakage from host port removal) closed via the `--profile` flag.
- README §Phase 6 runbook is complete and self-contained.
- Phase 6 is gated as complete; State can advance to "Phase 6 complete" for the orchestrator.
</success_criteria>

<output>
After completion, create `.planning/phases/06-traefik-tls-open-webui/06-04-SUMMARY.md` per the standard template:
- Files created: `bin/smoke-test-traefik.sh`.
- Files mutated: `bin/smoke-test-router.sh` (Pitfall 11 fix — `--profile` flag), `README.md` (Phase 6 section), `.env.example` (TRAEFIK_BASIC_AUTH_USER + TRAEFIK_BASIC_AUTH_PASS_PLAIN).
- Verification: complete smoke transcript (every [OK]/[FAIL] line). Manual SC1-SC5 evidence (commands run + observed output).
- Human-verify outcome: operator sign-off recorded.
- Decisions implemented end-to-end: D-A1..D-A6, D-B1..D-B4, D-C1..D-C8, D-D1, D-D2.
- Phase 6 status update: all 11 requirements PENDING → COMPLETE.
- Carry-forward to Phase 7: `/metrics` is internal-only — Phase 7's Prometheus must scrape via `http://router:3000/metrics` on the `app` network (NOT through Traefik). The smoke proves this path works. ENABLE_OLLAMA_API=False stays set — Phase 7's vLLM-metrics scraper goes via separate paths.
</output>
