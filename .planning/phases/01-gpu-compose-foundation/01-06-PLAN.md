---
phase: 01-gpu-compose-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yml
  - .planning/phases/01-gpu-compose-foundation/01-03-PLAN.md
  - .planning/phases/01-gpu-compose-foundation/01-04-PLAN.md
  - .planning/phases/01-gpu-compose-foundation/01-UAT.md
autonomous: true
gap_closure: true
closes_gaps:
  - "SC2 acceptance grep returns 0 against compose.yml (UAT test 3, minor)"
requirements:
  - INFRA-02
  - INFRA-04
must_haves:
  truths:
    - "The SC2 acceptance command `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` returns 0 against the rephrased compose.yml file"
    - "The canonical written-down form of the SC2 grep in 01-03-PLAN.md (lines 354, 382) and 01-04-PLAN.md (line 292) is the comment-aware form: `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'`"
    - "compose.yml continues to document the standing anti-pattern list in its header — the pedagogical intent is preserved while the literal banned substrings no longer appear in any line, comment or otherwise"
    - "`docker compose config --quiet` still exits 0 against the rephrased compose.yml (YAML structure unchanged)"
    - "The substantive Compose configuration is unchanged: pinned image tags (`nvidia/cuda:12.6.0-base-ubuntu24.04`, `ollama/ollama:0.5.7`), modern `deploy.resources.reservations.devices` syntax, the `x-gpu` anchor merged into both `gpu-preflight` and `ollama`"
  artifacts:
    - path: "compose.yml"
      provides: "Header comments rephrased to preserve pedagogy without containing the literal banned substrings"
      contains: "x-gpu"
    - path: ".planning/phases/01-gpu-compose-foundation/01-03-PLAN.md"
      provides: "SC2 grep hardened to the comment-aware form at lines 354 and 382"
    - path: ".planning/phases/01-gpu-compose-foundation/01-04-PLAN.md"
      provides: "SC2 grep hardened to the comment-aware form at line 292"
    - path: ".planning/phases/01-gpu-compose-foundation/01-UAT.md"
      provides: "UAT test 3 expected/reported updated to reference the hardened grep form (consistency only)"
  key_links:
    - from: "compose.yml header comments (lines 9, 18, 19 — to be rephrased)"
      to: "Phase 1 SC2 acceptance grep"
      via: "Source-rephrase pattern (matches the Plan 02 and Plan 04 precedent for this exact bug class)"
      pattern: "no .pinned image tags"
    - from: "01-03-PLAN.md lines 354, 382 and 01-04-PLAN.md line 292 (the canonical SC2 grep)"
      to: "Future UAT runs"
      via: "Comment-aware grep form: `grep -vE '^[[:space:]]*#' compose.yml | grep -cE ...`"
      pattern: "grep -vE.*compose.yml.*grep -cE"
---

<objective>
Close the **minor** UAT gap from Phase 1 test 3 by applying the combined fix the precedent of Plans 02 and 04 already established for this exact false-positive class: (a) rephrase the three header comments in `compose.yml` so the literal banned substrings (`:latest`, `runtime: nvidia`, `gpus: all`) no longer appear on any line — comment or otherwise; and (b) harden the canonical SC2 grep in the plan and UAT documents to the comment-aware form `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` so the same trap can never snare anyone again.

Purpose: SC2's literal regex was not comment-aware. Three header-comment lines in `compose.yml` document the standing anti-pattern list ("no `:latest`", "DO NOT use `runtime: nvidia`", "DO NOT use `gpus: all`") and contain the exact literals the grep hunts for. The substantive config has been clean since Plan 03 — `docker compose config` shows pinned tags, modern `deploy.resources.reservations.devices`, and the `x-gpu` anchor merged correctly. This gap is a documentation/test-instrumentation defect, not a config defect.

This is the same false-positive class Phase 1 Plan 02 and Plan 04 hit on `bin/preflight-gpu.sh` and `bin/smoke-test-gpu.sh`. Both chose source-rephrase. Plan 02 also hardened its own grep to the comment-aware form. The cumulative precedent points to the combined approach used here.

Output: a rephrased `compose.yml` header that preserves the pedagogical intent (any reader of the file still learns the anti-pattern rules) and four updated planning documents with the hardened grep form.

Closes UAT gap (verbatim from `.planning/phases/01-gpu-compose-foundation/01-UAT.md` lines 137-167):

- "SC2 acceptance grep returns 0 against compose.yml" (severity: minor, test 3)

No scope expansion. Substantive `compose.yml` config is NOT touched (the `x-gpu` anchor, the network block, the two service definitions are all untouched). The `01-03-SUMMARY.md` and `01-04-SUMMARY.md` historical execution records are NOT touched (per the UAT gap's `missing:` list — they are historical records, not specifications).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-gpu-compose-foundation/01-UAT.md
@.planning/phases/01-gpu-compose-foundation/01-03-PLAN.md
@.planning/phases/01-gpu-compose-foundation/01-04-PLAN.md
@.planning/phases/01-gpu-compose-foundation/01-02-PLAN.md
@.planning/debug/sc2-grep-false-positive.md
@compose.yml
@CLAUDE.md

<diagnosis-summary>
Verbatim from `.planning/debug/sc2-grep-false-positive.md` (the resolution section):

- The Phase 1 SC2 acceptance regex `(:latest|runtime: nvidia|gpus: all)` is not comment-aware.
- `grep -nE '(:latest|runtime: nvidia|gpus: all)' compose.yml` matches three lines, all comments:
  - Line 9:  `#   - pinned image tags (no :latest — INFRA-04)`
  - Line 18: `` # DO NOT use `runtime: nvidia` (legacy form, on the standing rejection list). ``
  - Line 19: `` # DO NOT use `gpus: all` shorthand (Compose-version-fragile). ``
- Comment-excluded grep already returns 0: `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` == 0.
- `docker compose config` shows the substantive config is clean: `nvidia/cuda:12.6.0-base-ubuntu24.04`, `ollama/ollama:0.5.7`, `deploy.resources.reservations.devices` with `driver: nvidia`, `count: all`, `capabilities: [gpu, utility]`. No `runtime:` directive anywhere. No `gpus:` directive anywhere.
- The SC2 grep is documentation-only. No script under `bin/` invokes it, no package.json (yet), no CI workflow, no Makefile. It is manually run during UAT.
- The grep is restated in: `01-03-PLAN.md` line 354 (Task 1 acceptance criterion) and line 382 (Task 1 final "done when" block) — THE canonical source. Also `01-04-PLAN.md` line 292 (with a slight line-anchored variant) and `01-UAT.md` lines 39 and 101.
- ROADMAP.md SC2 (line 33) is prose-only with NO grep attached and is therefore unaffected by either fix option.

**The combined fix** (matches the cumulative Plan 02 + Plan 04 precedent):

1. **Source-rephrase compose.yml** — replace the three offending header-comment lines with equivalent text that preserves the pedagogy without containing the literal banned substrings. The UAT gap's `missing:` list provides the suggested rephrasings verbatim; this plan codifies them.

2. **Harden the grep in the plan/UAT documents** — propagate the comment-aware form `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` to 01-03-PLAN.md lines 354 + 382, 01-04-PLAN.md line 292, and update 01-UAT.md lines 39 + 101 for consistency. Plan 02's `01-02-PLAN.md` already uses this form on `bin/preflight-gpu.sh` (lines 235, 246) — propagating to SC2 finishes the pattern.
</diagnosis-summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rephrase the three offending header comments in compose.yml</name>
  <files>compose.yml</files>
  <read_first>
    - compose.yml in full (162 lines). Confirm before editing:
      - line 9 reads `#   - pinned image tags (no :latest — INFRA-04)`
      - line 18 reads `` # DO NOT use `runtime: nvidia` (legacy form, on the standing rejection list). ``
      - line 19 reads `` # DO NOT use `gpus: all` shorthand (Compose-version-fragile). ``
      If line numbers have drifted (compose.yml is small and stable), locate by content with `grep -n`.
    - .planning/phases/01-gpu-compose-foundation/01-UAT.md SC2 gap (lines 137-167) — the `missing:` list contains the EXACT rephrasing strings; reproduce them verbatim.
    - .planning/debug/sc2-grep-false-positive.md the "Evidence" section about Plan 02 and Plan 04 precedent.
  </read_first>
  <action>
Edit `compose.yml` to rephrase three header-comment lines. All other lines (1-8, 10-17, 20-162) are untouched.

### Replace line 9

Current:
```
#   - pinned image tags (no :latest — INFRA-04)
```
Replace with (verbatim from UAT `missing:` line 161):
```
#   - pinned image tags (never use the floating "latest" form — INFRA-04)
```

Rationale: replaces the literal `:latest` substring with the prose phrase "the floating \"latest\" form". The pedagogical content is intact — any reader still learns that `:latest` is forbidden.

### Replace line 18

Current:
```
# DO NOT use `runtime: nvidia` (legacy form, on the standing rejection list).
```
Replace with (verbatim from UAT `missing:` line 162):
```
# DO NOT use the legacy "runtime nvidia" Compose directive (on the standing rejection list).
```

Rationale: replaces the literal `` `runtime: nvidia` `` (with backticks and colon) with the prose form `"runtime nvidia"` (no colon → no match against the grep pattern `runtime: nvidia`). The reader still learns the rule.

### Replace line 19

Current:
```
# DO NOT use `gpus: all` shorthand (Compose-version-fragile).
```
Replace with (verbatim from UAT `missing:` line 163):
```
# DO NOT use the "gpus all" shorthand (Compose-version-fragile).
```

Rationale: replaces `` `gpus: all` `` with `"gpus all"` (no colon → no match against the grep pattern `gpus: all`). Pedagogical content preserved.

### Optional: refresh line 20

Line 20 currently reads:
```
# This block is the canonical form per Docker Compose v2 docs and PITFALLS Pitfall 2.
```
Leave this line untouched — it has no banned substrings.

### Verification of the edit (sanity, before committing)

Run mentally:
- `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` should now return 0.
- `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` continues to return 0 (was already 0; should not regress).
- `docker compose config --quiet` exits 0 (YAML structure unchanged; only comment text changed).
- The substantive lines that reference image tags still pin them: line 58 reads `image: nvidia/cuda:12.6.0-base-ubuntu24.04` and line 89 reads `image: ollama/ollama:0.5.7`. (These tags do NOT match the grep — `12.6.0-base-ubuntu24.04` is not `:latest`, `0.5.7` is not `:latest`. Confirmed.)

**Forbidden:**
- Do NOT touch the `x-gpu` anchor block (lines 21-30). It contains `count: all` (note: this is `count: all`, not `gpus: all` — the grep pattern is `gpus: all` with the colon between `gpus` and `all`, so this is NOT a match. Re-verify with `grep -nE 'gpus: all' compose.yml` after editing; should return zero hits).
- Do NOT touch the network definitions (lines 37-48).
- Do NOT touch the `gpu-preflight` service (lines 57-85) or the `ollama` service (lines 88-161). Adding a `user:` directive is OPTIONAL per the gap and is DEFERRED (it's a defense-in-depth for Plan 01-05, not an SC2 fix).
- Do NOT change `nvidia/cuda:12.6.0-base-ubuntu24.04` or `ollama/ollama:0.5.7` — these are pinned image tags, exactly what SC2 wants to see.
- Do NOT introduce new comments that contain `:latest`, `runtime: nvidia`, or `gpus: all` substrings.
  </action>
  <verify>
    <automated>grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml | grep -q '^0$' && grep -cE 'never use the floating "latest" form' compose.yml | grep -q '^1$' && grep -cE 'DO NOT use the legacy "runtime nvidia" Compose directive' compose.yml | grep -q '^1$' && grep -cE 'DO NOT use the "gpus all" shorthand' compose.yml | grep -q '^1$' && grep -cE 'image: nvidia/cuda:12\.6\.0-base-ubuntu24\.04' compose.yml | grep -q '^1$' && grep -cE 'image: ollama/ollama:0\.5\.7' compose.yml | grep -q '^1$' && docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - SC2 grep now returns 0: `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` == 0. **This is the load-bearing assertion for the gap closure.**
    - The rephrased line 9 is present: `grep -cE 'never use the floating "latest" form' compose.yml` == 1.
    - The rephrased line 18 is present: `grep -cE 'DO NOT use the legacy "runtime nvidia" Compose directive' compose.yml` == 1.
    - The rephrased line 19 is present: `grep -cE 'DO NOT use the "gpus all" shorthand' compose.yml` == 1.
    - Pinned tags preserved: `grep -cE 'image: nvidia/cuda:12\.6\.0-base-ubuntu24\.04' compose.yml` == 1; `grep -cE 'image: ollama/ollama:0\.5\.7' compose.yml` == 1.
    - YAML structure unchanged: `docker compose config --quiet` exits 0 (provided `.env` is present with `HOST_DATA_ROOT` and `COMPOSE_PROJECT_NAME`, as is the case post-Phase-1).
    - The original three banned substrings are gone from comments: `grep -cE '\`runtime: nvidia\`' compose.yml` == 0; `grep -cE '\`gpus: all\`' compose.yml` == 0; `grep -cE 'no :latest' compose.yml` == 0.
    - The `x-gpu` anchor block is untouched: `grep -cE '^x-gpu: &gpu$' compose.yml` == 1, `grep -cE 'driver: nvidia$' compose.yml` ≥ 1, `grep -cE 'count: all$' compose.yml` == 1, `grep -cE 'capabilities: \[gpu, utility\]' compose.yml` == 1.
    - Total line count change is +0 (three replacements, line-for-line — no insertions, no deletions).
  </acceptance_criteria>
  <done>
    `compose.yml` header lines 9, 18, and 19 are rephrased to preserve the pedagogical anti-pattern documentation without containing the literal `:latest`, `runtime: nvidia`, or `gpus: all` substrings. The substantive Compose configuration (anchor, networks, services, image tags, bind mounts, healthchecks, depends_on) is byte-for-byte unchanged. `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` now returns 0. `docker compose config --quiet` still exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden the SC2 grep in the canonical plan and UAT documents to the comment-aware form</name>
  <files>.planning/phases/01-gpu-compose-foundation/01-03-PLAN.md, .planning/phases/01-gpu-compose-foundation/01-04-PLAN.md, .planning/phases/01-gpu-compose-foundation/01-UAT.md</files>
  <read_first>
    - .planning/phases/01-gpu-compose-foundation/01-03-PLAN.md lines 350-385 (the verification block + final SC2 mention). Confirm:
      - Line 354 reads `4. **No anti-patterns:** \`grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml\` returns 0.`
      - Line 382 (inside the `<output>` block) reads `- Confirmation that \`grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml\` returns 0`
    - .planning/phases/01-gpu-compose-foundation/01-04-PLAN.md lines 285-300 (the ROADMAP success-criteria checklist). Confirm:
      - Line 292 reads `- [ ] SC2: \`docker compose config\` shows the \`x-gpu\` anchor expanded into the \`ollama\` service; \`grep -cE '(:latest|^[[:space:]]*runtime:[[:space:]]*nvidia|^[[:space:]]*gpus:[[:space:]]*all)' compose.yml\` returns 0.`
    - .planning/phases/01-gpu-compose-foundation/01-UAT.md lines 35-45 and 95-105. Confirm:
      - Line 39 (inside test 3 "expected") references the grep.
      - Line 101 (inside the SC2 gap "reason") references the grep.
    - .planning/phases/01-gpu-compose-foundation/01-02-PLAN.md lines 235 and 246 — Plan 02's existing comment-aware form is the reference pattern. Re-read so the new SC2 form mirrors it exactly.

    If line numbers drift between writing this plan and execution, locate by literal content using `grep -n` on the exact string. The edits are content-anchored, not line-anchored.
  </read_first>
  <action>
Update four documents to use the hardened comment-aware grep form. All edits are surgical — replace one regex string with another, do not rewrite surrounding prose.

### Edit A — 01-03-PLAN.md line 354

Located in the `<verification>` block, the line currently reads:

```
4. **No anti-patterns:** `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` returns 0.
```

Replace with:

```
4. **No anti-patterns:** `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` returns 0. (Comment-aware form — mirrors the Plan 02 pattern at 01-02-PLAN.md lines 235, 246. The grep ignores `#`-prefixed lines, which is why the header documentation block at compose.yml lines 8-20 can describe the standing anti-patterns without triggering false positives. The substantive Compose configuration must remain free of these literal substrings on non-comment lines.)
```

### Edit B — 01-03-PLAN.md line 382

Located inside the `<output>` block (the SUMMARY.md template), the line currently reads:

```
- Confirmation that `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` returns 0
```

Replace with:

```
- Confirmation that `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` returns 0 (comment-aware form; see Plan 02 precedent)
```

### Edit C — 01-04-PLAN.md line 292

Located inside the `<how-to-verify>` block of the SC checklist task, the line currently reads:

```
- [ ] SC2: `docker compose config` shows the `x-gpu` anchor expanded into the `ollama` service; `grep -cE '(:latest|^[[:space:]]*runtime:[[:space:]]*nvidia|^[[:space:]]*gpus:[[:space:]]*all)' compose.yml` returns 0.
```

Replace with:

```
- [ ] SC2: `docker compose config` shows the `x-gpu` anchor expanded into the `ollama` service; `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` returns 0. (Comment-aware form per the Plan 02 / Plan 06 precedent — the leading `grep -v` skips comment lines so the header documentation block does not trigger false positives.)
```

Note: this replaces both the line-anchored variant AND the simple variant with the single comment-aware form. The line-anchored variant (`^[[:space:]]*runtime:[[:space:]]*nvidia|^[[:space:]]*gpus:[[:space:]]*all`) was a partial defense — it would not catch indented forms — and is superseded by the comment-aware form.

### Edit D — 01-UAT.md lines 39 and 101 (consistency only, marked optional in the UAT but applied for full consistency)

**Line 39** (inside test 3 "expected"), currently reads:

```
expected: `docker compose config` shows every GPU service references the same `x-gpu` YAML anchor (driver: nvidia, count: all, capabilities include `gpu`). No service uses the legacy `runtime: nvidia` form. No service uses `:latest`. Verify with: `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` returns 0.
```

Replace the final clause only — keep everything before "Verify with:" identical:

```
expected: `docker compose config` shows every GPU service references the same `x-gpu` YAML anchor (driver: nvidia, count: all, capabilities include `gpu`). No service uses the legacy `runtime: nvidia` form. No service uses `:latest`. Verify with: `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` returns 0.
```

**Line 101** (inside the SC2 gap `reason:` field — the part that DESCRIBES the false positive). This line is INTENTIONALLY left untouched because it is the historical narrative of what was observed during UAT. Editing it would rewrite history. **DO NOT EDIT LINE 101.** (This is a clarification on the UAT gap's optional `missing:` item — the gap text itself says "Update 01-UAT.md lines 39 and 101 to match the hardened expected form (optional but consistent)". Line 39 is the FORWARD-LOOKING `expected:` field — updating it is appropriate. Line 101 is the BACKWARD-LOOKING `reason:` field — leaving it untouched preserves the audit trail.)

**Forbidden:**
- Do NOT edit 01-03-SUMMARY.md or 01-04-SUMMARY.md. The UAT gap's `missing:` line 167 explicitly says: "Leave 01-03-SUMMARY.md and 01-04-SUMMARY.md untouched — they are historical execution records." Even though they contain the un-hardened grep, those records describe what was actually run during execution, not what should be run in the future.
- Do NOT edit `.planning/ROADMAP.md`. The Phase 1 SC2 entry (line 33) is prose-only with no executable grep attached — it is unaffected by the grep-hardening fix per the debug session's evidence section.
- Do NOT touch surrounding prose. Each edit is a single-line replacement in-place. Do not "improve" wording, do not reflow paragraphs, do not change indentation.
- Do NOT introduce a third variant of the grep. The hardened form is exactly `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` — keep it byte-for-byte consistent across the four edits.
- Do NOT touch 01-UAT.md line 101 (the gap `reason:` narrative).
  </action>
  <verify>
    <automated>grep -cE "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE '\(:latest\|runtime: nvidia\|gpus: all\)'" .planning/phases/01-gpu-compose-foundation/01-03-PLAN.md | grep -q '^2$' && grep -cE "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE '\(:latest\|runtime: nvidia\|gpus: all\)'" .planning/phases/01-gpu-compose-foundation/01-04-PLAN.md | grep -q '^1$' && grep -cE "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE '\(:latest\|runtime: nvidia\|gpus: all\)'" .planning/phases/01-gpu-compose-foundation/01-UAT.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - 01-03-PLAN.md contains the hardened form exactly TWICE (line 354 + line 382): `grep -cE "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE '\(:latest\|runtime: nvidia\|gpus: all\)'" .planning/phases/01-gpu-compose-foundation/01-03-PLAN.md` == 2.
    - 01-04-PLAN.md contains the hardened form exactly ONCE (line 292): same grep, count == 1.
    - 01-UAT.md contains the hardened form exactly ONCE (line 39, NOT line 101): same grep, count == 1.
    - 01-03-PLAN.md no longer contains the un-hardened literal grep on non-history lines. Allow at most 0 standalone instances (history-free): `grep -cE "^[^|]*grep -cE '\(:latest\|runtime: nvidia\|gpus: all\)' compose\.yml" .planning/phases/01-gpu-compose-foundation/01-03-PLAN.md` should be 0. (The pattern excludes the comment-aware form by requiring no preceding `|`.)
    - 01-04-PLAN.md no longer contains the line-anchored variant `^[[:space:]]*runtime:[[:space:]]*nvidia`: `grep -c '\^\[\[:space:\]\]\*runtime:' .planning/phases/01-gpu-compose-foundation/01-04-PLAN.md` == 0.
    - 01-UAT.md line 101 (or whichever line contains "All 3 matches are header comments documenting the anti-patterns") is UNCHANGED. Verify by checking the line still contains the original phrasing: `grep -c 'All 3 matches are header comments documenting the anti-patterns' .planning/phases/01-gpu-compose-foundation/01-UAT.md` == 1.
    - 01-03-SUMMARY.md is untouched: `git diff .planning/phases/01-gpu-compose-foundation/01-03-SUMMARY.md` returns no output before commit.
    - 01-04-SUMMARY.md is untouched: `git diff .planning/phases/01-gpu-compose-foundation/01-04-SUMMARY.md` returns no output before commit.
    - .planning/ROADMAP.md is untouched: `git diff .planning/ROADMAP.md` returns no output before commit.
  </acceptance_criteria>
  <done>
    The canonical SC2 grep across 01-03-PLAN.md (×2 instances), 01-04-PLAN.md (×1), and 01-UAT.md (×1, the forward-looking `expected:` field) is the comment-aware form `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'`. The historical `01-UAT.md` `reason:` narrative (line 101), 01-03-SUMMARY.md, 01-04-SUMMARY.md, and .planning/ROADMAP.md are all untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Targeted re-verification of SC2 — the gap-closure self-check</name>
  <files></files>
  <read_first>
    - .planning/phases/01-gpu-compose-foundation/01-UAT.md test 3 (lines 38-43) — the original failing UAT assertion.
  </read_first>
  <action>
This is a no-files-modified verification task. It re-runs the targeted SC2 assertion to prove the gap is closed, end-to-end.

Run, in order, these commands and capture their output:

```bash
# 1) The original SC2 assertion — must now return 0.
grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml

# 2) The hardened comment-aware form — must also return 0 (was already 0, must not regress).
grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'

# 3) docker compose config — must exit 0 (proves YAML didn't break).
docker compose config --quiet
echo "compose config exit: $?"

# 4) Substantive config still references the pinned tags
docker compose config | grep -cE 'image: nvidia/cuda:12\.6\.0-base-ubuntu24\.04'
docker compose config | grep -cE 'image: ollama/ollama:0\.5\.7'

# 5) The x-gpu anchor still merges into both services
docker compose config | grep -cE 'driver: nvidia'

# 6) Confirm the three rephrased comments are present in compose.yml
grep -c 'never use the floating "latest" form' compose.yml
grep -c 'DO NOT use the legacy "runtime nvidia" Compose directive' compose.yml
grep -c 'DO NOT use the "gpus all" shorthand' compose.yml

# 7) Confirm the hardened grep is propagated to the three documents
grep -c "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE" .planning/phases/01-gpu-compose-foundation/01-03-PLAN.md
grep -c "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE" .planning/phases/01-gpu-compose-foundation/01-04-PLAN.md
grep -c "grep -vE '\^\[\[:space:\]\]\*#' compose\.yml \| grep -cE" .planning/phases/01-gpu-compose-foundation/01-UAT.md
```

Expected results:
- (1) returns `0` — the gap is closed.
- (2) returns `0` — substantive config remains clean (no regression).
- (3) `docker compose config --quiet` exits 0; the echoed line shows `compose config exit: 0`.
- (4) Both return `1` — pinned tags preserved.
- (5) returns `2` — the anchor expands into both `gpu-preflight` and `ollama`.
- (6) Three counts of `1` — rephrased comments present.
- (7) Three counts: `2` (01-03-PLAN.md), `1` (01-04-PLAN.md), `1` (01-UAT.md).

If any expectation fails, return to Task 1 (compose.yml) or Task 2 (planning docs) and re-edit.

This task produces no file changes — it is a verification gate. The executor should record the actual command output in the per-plan SUMMARY (see `<output>` block) so the audit trail is complete.

**Forbidden:**
- Do NOT modify any file in this task. It is verification only.
- Do NOT use `docker compose up` — that's a Phase 1 SC4/SC5 concern, not SC2. The point is to verify SC2 alone is closed without re-running the full UAT.
- Do NOT skip step (3) (`docker compose config --quiet`) — it is the load-bearing assertion that Task 1's comment-only edit did not break the YAML parser. A typo in the rephrasing (e.g., breaking quoting inside a comment) is unlikely to break YAML but the check is cheap.
  </action>
  <verify>
    <automated>grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml | grep -q '^0$' && grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)' | grep -q '^0$' && docker compose config --quiet && docker compose config | grep -cE 'driver: nvidia' | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - Step (1) returns 0.
    - Step (2) returns 0.
    - Step (3) `docker compose config --quiet` exits 0.
    - Step (4) Both pinned-tag greps return 1.
    - Step (5) `driver: nvidia` count from resolved config is 2.
    - Step (6) All three rephrased-comment greps return 1.
    - Step (7) Hardened-grep propagation counts are 2, 1, 1 across the three documents.
  </acceptance_criteria>
  <done>
    SC2 is verifiably closed: the original failing UAT command (`grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml`) now returns 0 against the rephrased file, the hardened comment-aware grep ALSO returns 0 (proving the substantive config remains clean), `docker compose config --quiet` exits 0, and the canonical SC2 grep across the four documents matches the comment-aware form per the Plan 02 precedent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → compose.yml | Only three comment lines change. YAML structure is unaffected because comments are not parsed. Pinned image tags, anchor, networks, and service definitions are byte-for-byte unchanged. |
| Editor → planning documents (01-03-PLAN.md, 01-04-PLAN.md, 01-UAT.md) | These are markdown documentation files. They have no runtime effect — no script invokes them. Edits are purely textual. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | A future contributor could re-introduce a `:latest`, `runtime: nvidia`, or `gpus: all` literal anywhere in compose.yml (substantive or comment) | mitigate | The hardened grep `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` catches substantive recurrences. Comment-level recurrences are visible in review but no longer trigger SC2 false positives. STATE.md's "Standing Anti-Patterns" list reinforces the rule outside the file. |
| T-06-02 | Information disclosure | The rephrased comments mention WSL2 / NVIDIA Container Toolkit specifics | accept | The comments are pedagogical; they expose no secrets. The same content was already in the file before the rephrase. |
| T-06-03 | Denial of service | A typo in comment quoting could break `docker compose config` | mitigate | Task 3 Step (3) runs `docker compose config --quiet` as part of the gap-closure verification. A YAML parse failure would be caught immediately. |

## Threat Model Delta (vs Phase 1 baseline)

**Strictly textual change.** No new trust boundary, no new auth path, no new external integration. The change is purely documentation/test-instrumentation. The threat surface is unchanged. The only meaningful effect is that the SC2 grep now reliably returns 0 when the substantive config is clean, instead of returning false positives when the file's own pedagogy mentions the rules it enforces.
</threat_model>

<verification>
After this plan completes, run the seven assertions in Task 3 (the targeted self-check). Pass criteria:

1. `grep -cE '(:latest|runtime: nvidia|gpus: all)' compose.yml` returns 0. **This is the load-bearing closure of UAT gap 3.**
2. `grep -vE '^[[:space:]]*#' compose.yml | grep -cE '(:latest|runtime: nvidia|gpus: all)'` returns 0 (no regression in substantive config).
3. `docker compose config --quiet` exits 0 (YAML still parses).
4. Pinned image tags (`nvidia/cuda:12.6.0-base-ubuntu24.04`, `ollama/ollama:0.5.7`) still appear exactly once each in the resolved compose config.
5. `x-gpu` anchor still expands into both `gpu-preflight` and `ollama` (2 occurrences of `driver: nvidia` in resolved config).
6. The three rephrased comments are present.
7. The hardened grep is propagated to the three documents (2 + 1 + 1 occurrences).

No full UAT re-run is needed — the other Phase 1 SCs (1, 3, 4, 5) are unaffected by this plan.
</verification>

<success_criteria>
- UAT gap "SC2 acceptance grep returns 0 against compose.yml" → CLOSED. The original failing command now returns 0.
- Plan 02 + Plan 04 precedent (source-rephrase + hardened grep) is now fully propagated to SC2. Future readers of the canonical SC2 grep in 01-03-PLAN.md and 01-04-PLAN.md will see the comment-aware form by default.
- Substantive Compose configuration is unchanged: pinned tags, anchor, networks, services, healthchecks, depends_on — all byte-for-byte identical.
- Historical artefacts (01-03-SUMMARY.md, 01-04-SUMMARY.md, UAT gap `reason:` narrative on 01-UAT.md line 101, ROADMAP.md SC2 prose) are NOT touched.
- No threat-model changes.
</success_criteria>

<deviations>
**Deferred / explicitly out of scope** (justified):

1. **01-UAT.md line 101 — the SC2 gap `reason:` narrative.** The UAT gap's `missing:` list marks line-101 update as "optional but consistent". Editing it would rewrite a historical observation ("returns 3, not 0") as if it had never been observed. The audit trail is more valuable than the consistency, so line 101 is intentionally LEFT UNTOUCHED. Line 39 (the forward-looking `expected:` field) IS updated.

2. **01-03-SUMMARY.md and 01-04-SUMMARY.md — historical execution records.** The UAT gap's `missing:` line 167 explicitly excludes these from the change set ("Leave 01-03-SUMMARY.md and 01-04-SUMMARY.md untouched — they are historical execution records"). They describe what was actually run, not what should be run. No deviation; just respecting the gap's explicit scope boundary.

3. **.planning/ROADMAP.md SC2 prose (line 33).** Prose-only, no executable grep attached. Unaffected by the grep-hardening fix per the debug session's evidence section. No edit needed, no edit performed.

4. **Adding a CI check (e.g., a Makefile target, a pre-commit hook, or a GitHub Actions workflow) that runs the hardened SC2 grep automatically.** Out of scope for this gap closure — the grep is documentation-only by design at this stage of the project. Phase 2 introduces the router with a `package.json` and may add `npm run check:sc` as part of its test plan. Defer to whoever wires up CI.
</deviations>

<output>
After completion, create `.planning/phases/01-gpu-compose-foundation/01-06-SUMMARY.md` recording:
- The diff summary of `compose.yml` (three line-for-line replacements; line count change +0)
- The diff summary of 01-03-PLAN.md (two replacements at lines 354 + 382), 01-04-PLAN.md (one replacement at line 292), 01-UAT.md (one replacement at line 39 — NOT line 101)
- The actual command output from Task 3 Steps (1) through (7) — paste verbatim, line-by-line, to establish the audit trail
- Confirmation that 01-03-SUMMARY.md, 01-04-SUMMARY.md, and ROADMAP.md were NOT touched
- Atomic commit hashes for Task 1 (compose.yml) and Task 2 (planning docs) — one commit per task in `fix(01-gap):` style
</output>
</content>
</invoke>