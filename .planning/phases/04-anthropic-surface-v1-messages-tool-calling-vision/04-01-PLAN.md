---
phase: 04-anthropic-surface-v1-messages-tool-calling-vision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - router/src/translation/canonical.ts
  - router/src/translation/openai-in.ts
  - router/src/translation/openai-out.ts
  - router/src/translation/anthropic-in.ts
  - router/src/translation/anthropic-out.ts
  - router/src/translation/ollama-native-out.ts
  - router/src/backends/adapter.ts
  - router/src/backends/ollama-openai.ts
  - router/src/backends/llamacpp-openai.ts
  - router/src/routes/v1/chat-completions.ts
  - router/tests/translation/canonical.test.ts
  - router/tests/translation/openai-in.test.ts
  - router/tests/translation/openai-out.test.ts
  - router/tests/translation/anthropic-in.test.ts
  - router/tests/translation/anthropic-out.test.ts
  - router/tests/translation/ollama-native-out.test.ts
  - router/tests/translation/golden.test.ts
  - router/tests/translation/golden/.gitkeep
autonomous: true
requirements: []
tags:
  - canonical-shape
  - adapter-widening
  - phase-4-foundation
objective: "Land the canonical Anthropic-shape translation foundation + widen BackendAdapter to chatCompletionsCanonical{,Stream}; refactor /v1/chat/completions to flow through canonical so the Phase 2/3 OpenAI surface still passes its full test suite end-to-end."

must_haves:
  truths:
    - "router/src/translation/canonical.ts exports CanonicalRequest, CanonicalResponse, CanonicalStreamEvent, CanonicalMessage, ContentBlock, StopReason types plus the CanonicalRequestSchema zod validator (per D-A1)."
    - "BackendAdapter interface declares chatCompletionsCanonical(canonical, signal) AND chatCompletionsCanonicalStream(canonical, signal) AND keeps probeLiveness; the old chatCompletions / chatCompletionsStream methods are removed from the interface (per D-B1, D-B2)."
    - "OllamaOpenAIAdapter and LlamacppOpenAIAdapter both implement the new canonical methods; their constructors and probeLiveness bodies are unchanged byte-for-byte from Phase 3."
    - "router/src/routes/v1/chat-completions.ts flows zod → openAIRequestToCanonical → adapter.chatCompletionsCanonical{,Stream} → canonicalToOpenAIResponse|canonicalToOpenAISse; the AbortController + req.raw.socket.once('close') + safeRelease + semaphore acquire + heartbeat + sseCleanup plumbing is preserved byte-identical (per D-F3, PATTERNS.md S3)."
    - "Existing Phase 2/3 integration suite (`cd router && pnpm exec vitest run tests/integration/chat-completions`) is fully green after refactor — same wire output for /v1/chat/completions in both non-stream and stream modes including [DONE] terminator, midStreamErrorFrame, abort propagation."
    - "Wave-0 test scaffold files exist under router/tests/translation/ with at least one passing it() per file so vitest discovers and runs them (no skip/todo)."
    - "Plan 01 wave-1 adapter implementations emit `usage.input_tokens` = `opts.inputTokensHint ?? 0` and `usage.output_tokens` = 1 on the synthetic `message_start`; Plan 03 wires the route-supplied `inputTokensHint` so the final value at the route boundary is correct. Until Plan 03 lands, the `/v1/chat/completions` SSE surface internally synthesizes `input_tokens: 0` but never exposes `message_start` to OpenAI clients (it only reassembles `delta` chunks) — the rolling-completion intermediate state is invisible to clients and corrected before Plan 03 wires `/v1/messages`."
  artifacts:
    - path: router/src/translation/canonical.ts
      provides: "Canonical types + zod schemas (CanonicalRequest, ContentBlock discriminated union, CanonicalStreamEvent union, StopReason enum, newMessageId/newToolUseId ULID helpers)"
      contains: "export type CanonicalRequest"
    - path: router/src/translation/openai-in.ts
      provides: "openAIRequestToCanonical(body): CanonicalRequest"
      exports: ["openAIRequestToCanonical"]
    - path: router/src/translation/openai-out.ts
      provides: "canonicalToOpenAIResponse(canonical): ChatCompletion + canonicalToOpenAISse(events, opts): AsyncGenerator<{event?, data}>"
      exports: ["canonicalToOpenAIResponse", "canonicalToOpenAISse"]
    - path: router/src/translation/anthropic-in.ts
      provides: "anthropicRequestToCanonical(body): CanonicalRequest (signature + minimal text-only impl; full role-alternation + tools land in Plans 02+04)"
      exports: ["anthropicRequestToCanonical"]
    - path: router/src/translation/anthropic-out.ts
      provides: "canonicalToAnthropicResponse(canonical): Message + canonicalToAnthropicSse(events, opts) (signature + text-only impl; stream landing in Plan 03)"
      exports: ["canonicalToAnthropicResponse", "canonicalToAnthropicSse"]
    - path: router/src/translation/ollama-native-out.ts
      provides: "OllamaNativeChatRequest type + canonicalToOllamaNativeChat() signature stub (full impl in Plan 05)"
      exports: ["OllamaNativeChatRequest", "canonicalToOllamaNativeChat"]
    - path: router/src/backends/adapter.ts
      provides: "BackendAdapter interface widened with chatCompletionsCanonical{,Stream}; old methods removed"
      contains: "chatCompletionsCanonical("
    - path: router/src/backends/ollama-openai.ts
      provides: "chatCompletionsCanonical + chatCompletionsCanonicalStream wired through openai-in/openai-out (OpenAI-compat path only in this plan; native /api/chat branch lands in Plan 05)"
      contains: "chatCompletionsCanonical"
    - path: router/src/backends/llamacpp-openai.ts
      provides: "chatCompletionsCanonical + chatCompletionsCanonicalStream (OpenAI-compat path only; D-B4)"
      contains: "chatCompletionsCanonical"
    - path: router/src/routes/v1/chat-completions.ts
      provides: "Refactored route flows through canonical; semaphore/heartbeat/abort plumbing preserved"
      contains: "openAIRequestToCanonical"
    - path: router/tests/translation/canonical.test.ts
      provides: "Wave 0 scaffold + role-alternation refinement coverage (placeholder until Plan 02 expands)"
    - path: router/tests/translation/openai-in.test.ts
      provides: "Wave 0 scaffold + 1 happy-path text-only assertion"
    - path: router/tests/translation/openai-out.test.ts
      provides: "Wave 0 scaffold + 1 happy-path text-only assertion"
    - path: router/tests/translation/anthropic-in.test.ts
      provides: "Wave 0 scaffold + 1 happy-path text-only assertion"
    - path: router/tests/translation/anthropic-out.test.ts
      provides: "Wave 0 scaffold + 1 happy-path text-only assertion"
    - path: router/tests/translation/ollama-native-out.test.ts
      provides: "Wave 0 scaffold (full coverage in Plan 05)"
    - path: router/tests/translation/golden.test.ts
      provides: "Wave 0 scaffold runner — iterates router/tests/translation/golden/ dirs (empty until Plan 04)"
  key_links:
    - from: router/src/routes/v1/chat-completions.ts
      to: router/src/translation/openai-in.ts
      via: openAIRequestToCanonical import
      pattern: "from ['\"]\\.\\./\\.\\./translation/openai-in"
    - from: router/src/routes/v1/chat-completions.ts
      to: router/src/translation/openai-out.ts
      via: canonicalToOpenAIResponse + canonicalToOpenAISse imports
      pattern: "canonicalToOpenAI(Response|Sse)"
    - from: router/src/backends/ollama-openai.ts
      to: router/src/translation/canonical.ts
      via: CanonicalRequest type import
      pattern: "from ['\"]\\.\\./translation/canonical"
    - from: router/src/backends/adapter.ts
      to: router/src/translation/canonical.ts
      via: CanonicalRequest / CanonicalResponse / CanonicalStreamEvent type imports
      pattern: "from ['\"]\\.\\./translation/canonical"
---

<objective>
Land the canonical-shape translation foundation that every subsequent Phase 4 plan builds on, and prove the refactor preserves the existing OpenAI surface end-to-end.

Purpose: Phase 2 shipped `chatCompletions` / `chatCompletionsStream` on the `BackendAdapter` interface — those methods are OpenAI-typed (`ChatCompletionCreateParams` → `ChatCompletion`). Phase 4 needs ONE canonical entry point used by BOTH `/v1/chat/completions` and `/v1/messages` (per D-A3 — "no single-hop OpenAI↔Anthropic translation exists anywhere"). This plan adds `chatCompletionsCanonical{,Stream}`, removes the old methods, plumbs the route refactor, and scaffolds the test surface so Plans 02–05 can build vertical slices on top.

Output: Canonical type + zod schemas, both translators (openai-in/out) with full text-only behavior, both adapter implementations widened, the chat-completions route refactored to use canonical end-to-end. anthropic-in / anthropic-out / ollama-native-out get full signatures + minimal text-only bodies (the bulk of their logic lands in Plans 02/03/05). Wave-0 test scaffolds for every new translator file.

Coverage discipline: This plan covers ZERO requirement IDs directly — it ships pure infrastructure. Plans 02–05 attach the requirement IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-CONTEXT.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-VALIDATION.md
@.planning/phases/03-multi-backend-dispatch-llama-cpp-registry-hardening/03-CONTEXT.md
@.planning/phases/02-mvp-vertical-slice-router-ollama-sse/02-CONTEXT.md
@.planning/research/PITFALLS.md
@router/src/backends/adapter.ts
@router/src/backends/ollama-openai.ts
@router/src/backends/llamacpp-openai.ts
@router/src/routes/v1/chat-completions.ts
@router/src/sse/stream.ts
@router/src/sse/heartbeat.ts
@router/src/errors/envelope.ts
@router/src/config/registry.ts
@router/src/app.ts
@router/tests/integration/chat-completions.nonstream.test.ts
@router/tests/integration/chat-completions.stream.test.ts
@router/tests/msw/handlers.ts
@router/tests/unit/envelope.test.ts
@router/package.json
@CLAUDE.md

<interfaces>
<!-- Authoritative contracts the executor must respect verbatim. -->

From router/src/backends/adapter.ts (BEFORE this plan — to be REPLACED):
```typescript
export interface BackendAdapter {
  chatCompletions(req: ChatCompletionCreateParams, signal: AbortSignal): Promise<ChatCompletion>;
  chatCompletionsStream(req: ChatCompletionCreateParams, signal: AbortSignal): Promise<AsyncIterable<ChatCompletionChunk>>;
  probeLiveness(signal: AbortSignal): Promise<{ ok: boolean; latencyMs: number; error?: string }>;
}
```

From router/src/backends/adapter.ts (AFTER this plan — TARGET shape per D-B1 + D-B2 + PATTERNS.md lines 470–486):
```typescript
import type { CanonicalRequest, CanonicalResponse, CanonicalStreamEvent } from '../translation/canonical.js';

export interface BackendAdapter {
  chatCompletionsCanonical(canonical: CanonicalRequest, signal: AbortSignal): Promise<CanonicalResponse>;
  chatCompletionsCanonicalStream(canonical: CanonicalRequest, signal: AbortSignal): Promise<AsyncIterable<CanonicalStreamEvent>>;
  probeLiveness(signal: AbortSignal): Promise<{ ok: boolean; latencyMs: number; error?: string }>;
}
```

CanonicalStreamEvent union shape (RESEARCH.md Pattern 1, lines 287–308) — final authoritative shape this plan exports:
```typescript
export type CanonicalStreamEvent =
  | { type: 'message_start'; message: CanonicalMessage }
  | { type: 'content_block_start'; index: number; content_block: ContentBlock }
  | { type: 'content_block_delta'; index: number; delta: ContentBlockDelta }
  | { type: 'content_block_stop'; index: number }
  | { type: 'message_delta'; delta: { stop_reason: StopReason | null; stop_sequence: string | null }; usage: { output_tokens: number } }
  | { type: 'message_stop' }
  | { type: 'ping' };

export type ContentBlockDelta =
  | { type: 'text_delta'; text: string }
  | { type: 'input_json_delta'; partial_json: string };

export type StopReason =
  | 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'
  | 'pause_turn' | 'refusal' | 'model_context_window_exceeded';
```

ContentBlock discriminated union (from D-A2 + RESEARCH.md FINDING 4.1):
- TextBlock: `{type: 'text', text: string}`
- ImageBlock: `{type: 'image', source: {type: 'base64', media_type: string, data: string} | {type: 'url', url: string}}`
- ToolUseBlock: `{type: 'tool_use', id: string /* toolu_<ulid> */, name: string, input: object}`
- ToolResultBlock: `{type: 'tool_result', tool_use_id: string, content: string | ContentBlock[], is_error?: boolean}`

CanonicalRequest shape:
```typescript
export type CanonicalRequest = {
  model: string;              // canonical model name (the registry entry name; adapter remaps to backend_model)
  system?: string;            // top-level — Anthropic semantics
  messages: CanonicalMessage[];
  max_tokens?: number;
  temperature?: number;
  top_p?: number;
  top_k?: number;
  stop_sequences?: string[];  // capped at 5 (Anthropic limit) — enforced in openai-in.ts in Plan 04
  stream?: boolean;
  tools?: CanonicalTool[];    // (full shape lands in Plan 04 — keep the optional field declared now)
  tool_choice?: CanonicalToolChoice;  // (full shape lands in Plan 04)
  // No PDF / extended-thinking / cache_control in Phase 4 (v2 backlog).
};

export type CanonicalMessage = {
  role: 'user' | 'assistant';   // NO 'system' — system is top-level
  content: ContentBlock[];
};
```

CanonicalResponse shape (Anthropic Message — D-E3 ULID id format):
```typescript
export type CanonicalResponse = {
  id: string;                   // 'msg_' + ulid()
  type: 'message';
  role: 'assistant';
  content: ContentBlock[];      // assistant content blocks (text + tool_use)
  model: string;                // registry name (NOT backend_model)
  stop_reason: StopReason | null;
  stop_sequence: string | null;
  usage: { input_tokens: number; output_tokens: number };
};
```

From router/src/sse/stream.ts (full file 1–67) — analog for both openai-out.ts stream variant AND anthropic-out.ts stream variant (PATTERNS.md S1). Reuse `try { for await ... } catch (err) { if (opts.signal?.aborted) return; ... } finally { opts.onCleanup?.(); }` discipline byte-for-byte.

From router/src/routes/v1/chat-completions.ts (lines 76–242) — the EXACT shape to preserve in the refactor; only lines 152–220 (the body of the stream + non-stream branches) change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical types + zod schemas + ULID id helpers</name>
  <files>router/src/translation/canonical.ts, router/src/backends/adapter.ts, router/tests/translation/canonical.test.ts, router/package.json</files>
  <read_first>
    - router/src/config/registry.ts (lines 1–95 — zod superRefine + discriminated unions pattern; PATTERNS.md "canonical.ts analog" rows)
    - router/src/backends/adapter.ts (current interface; PATTERNS.md adapter.ts MODIFIED row, lines 449–488)
    - router/src/errors/envelope.ts (NO_ENVELOPE sentinel + error-class style — reused for translator errors)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md FINDING 1.1, 1.4, 3.9, 4.1 (wire shapes for content blocks + stop_reason union)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §"canonical.ts" rows (lines 47–99) and §S8 (ULID id generation, lines 1257–1271)
  </read_first>
  <behavior>
    - canonical.ts compiles standalone (no runtime import from `@anthropic-ai/sdk`; only `import type` of `Message` / `MessageStreamEvent` permitted, and only if needed for cross-check helper tests — adapters NEVER import the SDK per D-A4).
    - `CanonicalRequestSchema.parse({ model:'x', messages:[{role:'user', content:[{type:'text', text:'hi'}]}] })` succeeds.
    - `CanonicalRequestSchema.parse({ model:'x', messages:[{role:'user', content:'hi'}] })` succeeds (string content normalized to `[{type:'text', text:'hi'}]` via zod `.transform()` — accept both wire forms because OpenAI inputs land as strings).
    - `CanonicalRequestSchema.parse({ model:'x', messages:[] })` fails with a ZodError (min(1)).
    - `CanonicalRequestSchema.parse({ model:'x', messages:[{role:'system', content:'be helpful'}] })` fails with a ZodError ('system' is NOT in the role enum — top-level only).
    - `newMessageId()` returns a string matching `/^msg_[0-9A-HJKMNP-TV-Z]{26}$/` (ULID Crockford base32 — 26 chars after the `msg_` prefix).
    - `newToolUseId()` returns a string matching `/^toolu_[0-9A-HJKMNP-TV-Z]{26}$/`.
    - `BackendAdapter` interface compiles with the new methods AND does NOT export `ChatCompletion` / `ChatCompletionChunk` / `ChatCompletionCreateParams` imports anymore (those imports are removed from adapter.ts entirely).
  </behavior>
  <action>
    Create `router/src/translation/canonical.ts`. Imports: `import { z } from 'zod/v4';` and `import { ulid, monotonicFactory } from 'ulid';` and `import type { Message } from '@anthropic-ai/sdk/resources/messages.js';` (the SDK type-only import is allowed in tests only — keep this file SDK-free in runtime; the SDK type-check helper lives in tests). Define and EXPORT:

    1. `TextBlockSchema = z.object({type: z.literal('text'), text: z.string()})`.
    2. `ImageSourceSchema = z.discriminatedUnion('type', [z.object({type: z.literal('base64'), media_type: z.string(), data: z.string()}), z.object({type: z.literal('url'), url: z.string().url()})])`.
    3. `ImageBlockSchema = z.object({type: z.literal('image'), source: ImageSourceSchema})`.
    4. `ToolUseBlockSchema = z.object({type: z.literal('tool_use'), id: z.string(), name: z.string(), input: z.record(z.string(), z.unknown())})`.
    5. `ToolResultBlockSchema = z.object({type: z.literal('tool_result'), tool_use_id: z.string(), content: z.union([z.string(), z.array(z.lazy(() => ContentBlockSchema))]), is_error: z.boolean().optional()})`.
    6. `ContentBlockSchema = z.discriminatedUnion('type', [TextBlockSchema, ImageBlockSchema, ToolUseBlockSchema, ToolResultBlockSchema])`.
    7. `CanonicalMessageSchema = z.object({role: z.enum(['user', 'assistant']), content: z.union([z.string().transform((s) => [{type: 'text' as const, text: s}]), z.array(ContentBlockSchema).min(1)])})`. Note the string→text-block transform — accept both wire forms because the openai-in translator emits string content for simple messages.
    8. `CanonicalToolSchema = z.object({name: z.string(), description: z.string().optional(), input_schema: z.record(z.string(), z.unknown())})` — placeholder shape used by Plan 04; declare now.
    9. `CanonicalToolChoiceSchema = z.discriminatedUnion('type', [z.object({type: z.literal('auto'), disable_parallel_tool_use: z.boolean().optional()}), z.object({type: z.literal('any'), disable_parallel_tool_use: z.boolean().optional()}), z.object({type: z.literal('tool'), name: z.string(), disable_parallel_tool_use: z.boolean().optional()}), z.object({type: z.literal('none')})])` — full FINDING 3.4 mapping including `{type:'none'}` native + `disable_parallel_tool_use` modifier on the `tool_choice` object itself (Plan 04 uses this).
    10. `CanonicalRequestSchema = z.object({model: z.string().min(1), system: z.string().optional(), messages: z.array(CanonicalMessageSchema).min(1), max_tokens: z.number().int().positive().optional(), temperature: z.number().optional(), top_p: z.number().optional(), top_k: z.number().int().positive().optional(), stop_sequences: z.array(z.string()).max(5).optional(), stream: z.boolean().optional(), tools: z.array(CanonicalToolSchema).optional(), tool_choice: CanonicalToolChoiceSchema.optional()})`. The `max(5)` on `stop_sequences` enforces the Anthropic limit (Pitfall 6 / D-D5).
    11. `StopReasonSchema = z.enum(['end_turn', 'max_tokens', 'stop_sequence', 'tool_use', 'pause_turn', 'refusal', 'model_context_window_exceeded'])` per FINDING 3.9.
    12. `CanonicalResponseSchema = z.object({id: z.string().startsWith('msg_'), type: z.literal('message'), role: z.literal('assistant'), content: z.array(ContentBlockSchema), model: z.string(), stop_reason: StopReasonSchema.nullable(), stop_sequence: z.string().nullable(), usage: z.object({input_tokens: z.number().int().nonnegative(), output_tokens: z.number().int().nonnegative()})})`.
    13. The `CanonicalStreamEvent` TS union and `ContentBlockDelta` TS union (RESEARCH.md Pattern 1 — copy verbatim shape, but as TS types NOT zod schemas; runtime correctness for stream events is enforced by translators, not zod, because they're internal not external-facing).
    14. `z.infer<>` type aliases for every schema: `CanonicalRequest`, `CanonicalResponse`, `CanonicalMessage`, `ContentBlock`, `ImageBlock`, `TextBlock`, `ToolUseBlock`, `ToolResultBlock`, `CanonicalTool`, `CanonicalToolChoice`, `StopReason`.
    15. ULID id helpers per PATTERNS.md S8. Use a module-level `monotonicFactory()` instance so IDs within the same millisecond are still monotonic. Export `export function newMessageId(): string { return 'msg_' + factory(); }` and `export function newToolUseId(): string { return 'toolu_' + factory(); }`.

    Edit `router/src/backends/adapter.ts`: REMOVE the three `import type {...}` lines for `ChatCompletion` / `ChatCompletionChunk` / `ChatCompletionCreateParams` and the `chatCompletions` / `chatCompletionsStream` methods (lines 1–5 + 14–32). Add `import type { CanonicalRequest, CanonicalResponse, CanonicalStreamEvent } from '../translation/canonical.js';`. Replace the interface body with the TARGET shape from `<interfaces>` above. Keep `probeLiveness` byte-identical. Keep the `AdapterFactory` export at the bottom (lines 47–49) byte-identical.

    Edit `router/package.json` "dependencies": add `"ulid": "^3.0.2"`. (gpt-tokenizer + @anthropic-ai/sdk land in Plan 02; ulid is used here for newMessageId/newToolUseId.) Run `cd router && npm install` to refresh `package-lock.json` (or `pnpm install` — match existing tooling).

    Create `router/tests/translation/canonical.test.ts`. Mirror the table-driven style of `router/tests/unit/envelope.test.ts`. Coverage:
    - `it('CanonicalRequestSchema accepts text-only user message')` — happy path.
    - `it('CanonicalRequestSchema normalizes string content to [{type:text, text}]')` — verifies the `.transform()`.
    - `it('CanonicalRequestSchema rejects role: system')` — toThrow ZodError.
    - `it('CanonicalRequestSchema rejects stop_sequences with >5 entries')` — toThrow ZodError.
    - `it('newMessageId returns msg_<ULID>')` — regex assertion.
    - `it('newToolUseId returns toolu_<ULID>')` — regex assertion.
    - `it('newMessageId is monotonic within the same ms')` — call twice in tight loop; second value > first lexicographically (monotonicFactory guarantees this).

    NO fenced code blocks belong in this `<action>`. Type signatures and identifier names above are the contract; copy them into the file directly.
  </action>
  <verify>
    <automated>cd router && npm install && npx tsc --noEmit && npx vitest run tests/translation/canonical.test.ts --reporter=basic</automated>
  </verify>
  <done>canonical.ts compiles, exports all named types listed above, canonical.test.ts has ≥6 passing tests, adapter.ts no longer references openai/resources types, ulid@^3.0.2 appears in router/package.json dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement openai-in / openai-out (text-only + delta stream); add Wave 0 scaffolds for anthropic-in / anthropic-out / ollama-native-out</name>
  <files>router/src/translation/openai-in.ts, router/src/translation/openai-out.ts, router/src/translation/anthropic-in.ts, router/src/translation/anthropic-out.ts, router/src/translation/ollama-native-out.ts, router/tests/translation/openai-in.test.ts, router/tests/translation/openai-out.test.ts, router/tests/translation/anthropic-in.test.ts, router/tests/translation/anthropic-out.test.ts, router/tests/translation/ollama-native-out.test.ts, router/tests/translation/golden.test.ts, router/tests/translation/golden/.gitkeep</files>
  <read_first>
    - router/src/translation/canonical.ts (just-created — types + schemas Task 1)
    - router/src/sse/stream.ts (full file — chunkToSseEvents pattern; PATTERNS.md openai-out.ts analog at lines 143–186)
    - router/src/routes/v1/chat-completions.ts (lines 21–35 — ChatMessageSchema; lines 67–74 — body remap pattern)
    - router/tests/integration/chat-completions.stream.test.ts (lines 47–62 — parseSse helper to copy)
    - router/tests/msw/handlers.ts (lines 7–96 — ollamaNonStreamHandler + ollamaStreamHandler shapes that the OpenAI surface must continue to match wire-for-wire)
    - router/tests/unit/envelope.test.ts (lines 1–60 — table-driven describe/it pattern)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §"openai-in.ts" + §"openai-out.ts" + §S1
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md FINDING 1.1, 1.2, 1.3 (stream order + cumulative output_tokens)
  </read_first>
  <behavior>
    openai-in.ts:
    - `openAIRequestToCanonical({model, messages: [{role:'user', content:'hi'}]})` returns `{model, messages: [{role:'user', content:[{type:'text', text:'hi'}]}]}`.
    - First `{role:'system', content:'be brief'}` message in `messages[]` is lifted to top-level `canonical.system: 'be brief'`.
    - Multiple `role:'system'` messages — concatenate with `\n` separator into top-level `system`.
    - `messages[].content` as array of OpenAI content blocks `{type:'text', text}` and `{type:'image_url', image_url:{url}}` is normalized to canonical `ContentBlock[]` (image_url with `data:image/...;base64,` data URL → `{type:'image', source:{type:'base64', media_type, data}}`; bare http(s) URL → `{type:'image', source:{type:'url', url}}`).
    - `tool_calls` + `tool_choice` + `parallel_tool_calls` + `stop` mapping are STUBBED in this plan (Plan 04 fills them); but stub MUST forward the fields untouched on the canonical request so downstream consumers don't crash on undefined.
    - Errors: `openAIRequestToCanonical` throws a `z.ZodError` (already mapped to 400 by `mapToHttpStatus` envelope.ts:38) when input violates `CanonicalRequestSchema` after normalization. Synchronous (not async).

    openai-out.ts:
    - `canonicalToOpenAIResponse({id:'msg_...', content:[{type:'text', text:'Hello'}], model:'x', usage:{input_tokens:5, output_tokens:2}, stop_reason:'end_turn', ...})` returns OpenAI ChatCompletion `{id, object:'chat.completion', created, model:'x', choices:[{index:0, message:{role:'assistant', content:'Hello'}, finish_reason:'stop'}], usage:{prompt_tokens:5, completion_tokens:2, total_tokens:7}}`. The `stop_reason → finish_reason` mapping: `end_turn → stop`, `max_tokens → length`, `tool_use → tool_calls`, `stop_sequence → stop`, others → `stop`.
    - `canonicalToOpenAISse(events, {signal, onCleanup})` is an async generator that yields `{event?, data}` pairs matching the EXACT Phase 2/3 OpenAI wire format (so tests/integration/chat-completions.stream.test.ts still passes byte-for-byte). For each `content_block_delta {text_delta}` → yield one `{data: JSON.stringify({id, object:'chat.completion.chunk', created, model, choices:[{index:0, delta:{content:text}, finish_reason:null}]})}` chunk. On `message_delta` → emit final chunk `choices:[], usage:{prompt_tokens, completion_tokens, total_tokens}`. On `message_stop` → yield `{data: '[DONE]'}`. The cleanup discipline (try/catch with `if (opts.signal?.aborted) return;` + finally cleanup) mirrors `chunkToSseEvents` BYTE-FOR-BYTE (PATTERNS.md S1).
    - On any thrown upstream error during iteration AND signal not aborted: yield the `midStreamErrorFrameLines(envelope)` frames + `{data:'[DONE]'}` terminator (envelope.ts already exports `midStreamErrorFrameLines`).

    anthropic-in.ts / anthropic-out.ts / ollama-native-out.ts (SKELETONS in this plan, full impls in Plans 02/03/05):
    - Each file exports the named functions in the must_haves block above with a working text-only implementation. anthropic-in normalizes Anthropic-shape body (`{model, messages, system, max_tokens, ...}`) into `CanonicalRequest`. anthropic-out builds a CanonicalResponse-shape into the Anthropic `Message` wire format (id, type, role, content, model, stop_reason, stop_sequence, usage). ollama-native-out has a text-only implementation that builds `OllamaNativeChatRequest` with `content` as concatenated text (no `images` field yet — that's Plan 05).
    - Each test scaffold runs ≥1 passing assertion so vitest discovers it (no `.skip` / `.todo`).
  </behavior>
  <action>
    Create `router/src/translation/openai-in.ts`. Exports `export function openAIRequestToCanonical(body: unknown): CanonicalRequest`. Implementation:
    1. zod-parse the input with a permissive OpenAI body schema that mirrors `ChatCompletionRequestSchema` at `chat-completions.ts:29` (model + messages required, everything else passthrough).
    2. Extract `system` from messages[] — find every `role:'system'` entry, concatenate their `content` (string only — if content is array of blocks, JSON.stringify the array; an edge case) with `\n` separators, drop the entries from `messages`.
    3. Walk remaining messages: for `role:'user'|'assistant'` map content. If string → `[{type:'text', text}]`. If array, walk each block: `{type:'text', text}` → canonical text block; `{type:'image_url', image_url:{url}}` → if url starts with `data:image/`, parse out the media_type + base64 data via regex `/^data:(image\/[\w+-]+);base64,(.+)$/` → canonical `{type:'image', source:{type:'base64', media_type, data}}`; else → `{type:'image', source:{type:'url', url}}`. Throw a ZodError-shaped issue on unrecognized block types.
    4. For now (full mapping in Plan 04), pass through `temperature`, `top_p`, `max_tokens`, `stream` directly. Leave `tools`, `tool_choice`, `parallel_tool_calls`, `stop` undefined on the canonical (Plan 04 fills them).
    5. Return `CanonicalRequestSchema.parse(built)` so the result is validated end-to-end (will throw `ZodError` on shape violations — envelope.ts maps it to 400).

    Create `router/src/translation/openai-out.ts`. Exports two functions:
    1. `export function canonicalToOpenAIResponse(canonical: CanonicalResponse): ChatCompletion` — `import type { ChatCompletion } from 'openai/resources/chat/completions';`. Map content blocks: collect all `text` blocks into a single string for `choices[0].message.content`; tool_use blocks (Plan 04) get mapped to `choices[0].message.tool_calls`. Use `Math.floor(Date.now()/1000)` for `created`. Map `stop_reason → finish_reason` per the table in <behavior>. Compose `usage.{prompt_tokens, completion_tokens, total_tokens}` from canonical.usage.
    2. `export async function* canonicalToOpenAISse(events: AsyncIterable<CanonicalStreamEvent>, opts: { signal?: AbortSignal; onCleanup?: () => void } = {}): AsyncGenerator<{event?: string; data: string}, void, void>` — copy the try/catch/finally shape from `router/src/sse/stream.ts` lines 24–66 byte-for-byte. State machine inside the loop:
       - On `message_start` → capture `id`, `model`, `created = Math.floor(Date.now()/1000)`; do NOT yield anything (OpenAI doesn't have a per-stream start frame other than the first chunk).
       - On `content_block_start {type:'text'}` → no-op.
       - On `content_block_delta {type:'text_delta', text}` → yield one `{data: JSON.stringify({id, object:'chat.completion.chunk', created, model, choices:[{index:0, delta:{content:text}, finish_reason:null}]})}`.
       - On `content_block_start {type:'tool_use', id, name}` → buffer the tool_use index and emit `{data:...delta.tool_calls:[{index, id, type:'function', function:{name, arguments:''}}]}` (Plan 04 finalizes this; in this plan keep the no-op + a clearly marked TODO comment "// Plan 04: tool_use stream emission" — the stream test fixtures used in this plan don't exercise tool_use, so behavior is undefined for that case in Plan 01).
       - On `content_block_delta {type:'input_json_delta', partial_json}` → likewise Plan 04 TODO.
       - On `message_delta {usage:{output_tokens}}` → use the cumulative output_tokens AND the canonical `delta.stop_reason` to emit a final chunk `{data: JSON.stringify({id, object:'chat.completion.chunk', created, model, choices:[], usage:{prompt_tokens: captured_input_tokens, completion_tokens: output_tokens, total_tokens: captured_input_tokens + output_tokens}})}`. Capture `input_tokens` from `message_start.message.usage.input_tokens`.
       - On `message_stop` → yield `{data: '[DONE]'}`.
       - On error within the loop AND `!opts.signal?.aborted` → call `toOpenAIErrorEnvelope(err)` and yield `midStreamErrorFrameLines(env)` (when env !== NO_ENVELOPE).
       - In `finally`: call `opts.onCleanup?.()`.

    Create `router/src/translation/anthropic-in.ts`. Exports `export function anthropicRequestToCanonical(body: unknown): CanonicalRequest`. Use a permissive zod schema (`AnthropicMessagesRequestSchema`): `{model: z.string().min(1), system: z.string().optional(), messages: z.array(z.object({role: z.enum(['user', 'assistant']), content: z.union([z.string(), z.array(z.unknown())])})).min(1), max_tokens: z.number().int().positive(), stream: z.boolean().optional(), temperature: z.number().optional(), top_p: z.number().optional(), top_k: z.number().int().positive().optional(), stop_sequences: z.array(z.string()).max(5).optional(), tools: z.array(z.unknown()).optional(), tool_choice: z.unknown().optional()}).passthrough()`. Map directly to canonical: system passes through to top-level; messages map block-by-block (Anthropic content blocks ARE canonical content blocks — `text`, `image`, `tool_use`, `tool_result`). Role-alternation + tool_result-before-text refinements are STUBBED in this plan; Plan 02 adds them. Return `CanonicalRequestSchema.parse(built)`.

    Create `router/src/translation/anthropic-out.ts`. Exports two functions:
    1. `export function canonicalToAnthropicResponse(canonical: CanonicalResponse): AnthropicMessage` where `AnthropicMessage` is a local TS type matching Anthropic's wire shape (id, type:'message', role:'assistant', content: ContentBlock[], model, stop_reason, stop_sequence, usage:{input_tokens, output_tokens}). The mapping is essentially identity (canonical IS Anthropic-shape by design — D-A1).
    2. `export async function* canonicalToAnthropicSse(events: AsyncIterable<CanonicalStreamEvent>, opts: { signal?: AbortSignal; onCleanup?: () => void } = {}): AsyncIterable<{event: string; data: string}>` — STUB IN PLAN 01: emit one `{event:'message_stop', data:'{"type":"message_stop"}'}` event for every input event (placeholder). Plan 03 implements the full event-by-event switch from RESEARCH.md Pattern 2.

    Create `router/src/translation/ollama-native-out.ts`. Export `export interface OllamaNativeChatRequest { ... }` (full shape from RESEARCH.md Pattern 3 / FINDING 4.3, lines 355–366) AND `export async function canonicalToOllamaNativeChat(canonical: CanonicalRequest): Promise<OllamaNativeChatRequest>`. STUB: walks messages, joins text content into a string `content`, returns `{model: canonical.model, messages: [...], stream: false, options: {temperature: canonical.temperature, top_p: canonical.top_p}}` with NO `images` field. Plan 05 adds URL fetching + base64 normalization + image collection.

    Create Wave-0 test scaffold files. Each one has the following minimum:
    - `openai-in.test.ts`: `it('returns canonical for text-only OpenAI body')` — assert `openAIRequestToCanonical({model:'x', messages:[{role:'user', content:'hi'}]}).messages[0].content[0]).toEqual({type:'text', text:'hi'})`; also `it('lifts system message to top-level')` and `it('throws ZodError on empty messages')`.
    - `openai-out.test.ts`: `it('canonicalToOpenAIResponse maps text content + usage')` and `it('canonicalToOpenAISse emits chunk + [DONE] for text_delta event sequence')` — feed a canned AsyncIterable of `[message_start, content_block_start{text}, content_block_delta{text_delta, text:'hi'}, content_block_stop, message_delta{usage:{output_tokens:2}}, message_stop]`, collect outputs into array, assert `outputs.at(-1).data === '[DONE]'` and one chunk contains `"content":"hi"`.
    - `anthropic-in.test.ts`: `it('passes through Anthropic-shape body')` — assert `anthropicRequestToCanonical({model:'x', max_tokens:100, messages:[{role:'user', content:'hi'}]}).system).toBeUndefined()` and `.messages[0].content[0]).toEqual({type:'text', text:'hi'})`.
    - `anthropic-out.test.ts`: `it('canonicalToAnthropicResponse mirrors canonical shape')` — round-trip a canonical response object; assert deep equal.
    - `ollama-native-out.test.ts`: `it('emits content string for text-only canonical')` — assert `messages[0].content === 'hi'` and `messages[0].images` is undefined.
    - `golden.test.ts`: `describe('golden round-trip fixtures (TOOL-05)')` with `it.skip('plan-04-fills-fixtures-and-runs-them')` placeholder OR an `it('golden directory exists')` that does `expect(existsSync('router/tests/translation/golden')).toBe(true)` — at least one assertion so vitest doesn't error on empty `describe`.

    Create `router/tests/translation/golden/.gitkeep` (empty file) so the directory is tracked by git for Plan 04.
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && npx vitest run tests/translation/ --reporter=basic</automated>
  </verify>
  <done>All 7 translator files exist; openai-in.ts + openai-out.ts have full text-only behavior; other 3 have skeleton signatures + minimal text-only bodies; 7 test files pass with ≥1 it() each; tsc --noEmit succeeds; `grep -RIn "from '\\.\\./translation/canonical" router/src/translation/ router/src/backends/adapter.ts | wc -l` is ≥ 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire both adapters to chatCompletionsCanonical{,Stream}; refactor /v1/chat/completions to flow through canonical; verify Phase 2/3 OpenAI tests stay green</name>
  <files>router/src/backends/ollama-openai.ts, router/src/backends/llamacpp-openai.ts, router/src/routes/v1/chat-completions.ts</files>
  <read_first>
    - router/src/backends/ollama-openai.ts (current full file — Phase 2 pattern + Phase 3 probeLiveness; PATTERNS.md ollama-openai.ts MODIFIED row, lines 492–542)
    - router/src/backends/llamacpp-openai.ts (current full file; PATTERNS.md llamacpp-openai.ts MODIFIED row, lines 544–574)
    - router/src/routes/v1/chat-completions.ts (current full file; PATTERNS.md chat-completions.ts MODIFIED row, lines 577–605)
    - router/src/translation/openai-in.ts + openai-out.ts (just created Task 2)
    - router/src/translation/canonical.ts (just created Task 1)
    - router/tests/integration/chat-completions.nonstream.test.ts + chat-completions.stream.test.ts (existing tests — MUST stay green; they're the regression gate for this refactor)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §S3 (route handler shape rules — five-bullet discipline)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md FINDING 1.2 (input_json_delta chunking) — confirms passthrough strategy
  </read_first>
  <behavior>
    OllamaOpenAIAdapter:
    - `probeLiveness` unchanged byte-for-byte (lines 52–61 of current file).
    - Constructor unchanged (lines 14–19): `new OpenAI({ baseURL, apiKey: 'ollama', timeout: 60_000 })`.
    - NEW `chatCompletionsCanonical(canonical, signal)`: internally translates canonical → OpenAI ChatCompletionCreateParams via an internal helper `canonicalToOpenAIChatCompletionParams(canonical)` (placed in openai-in.ts as a sibling export `canonicalToOpenAIChatCompletionParams` — same module as `openAIRequestToCanonical`, since it's the inverse direction in non-stream non-tool case for now; full version with tools in Plan 04). Call `this.client.chat.completions.create({...openaiReq, stream:false, stream_options:{include_usage:true}}, {signal})`. Wrap the result with `openAIChatCompletionToCanonical(result)` from openai-out.ts (a new sibling export — translates ChatCompletion → CanonicalResponse). Use `newMessageId()` for the canonical response id; the OpenAI response id is discarded (Anthropic-shape requires `msg_` prefix per D-E3). Return the CanonicalResponse.
    - NEW `chatCompletionsCanonicalStream(canonical, signal)`: similar — translate to OpenAI params, call `chat.completions.create({stream:true, stream_options:{include_usage:true}}, {signal})`, get back `AsyncIterable<ChatCompletionChunk>`. Wrap with `openAIChunksToCanonicalEvents(chunks, {model, msgId})` (new helper in openai-out.ts) that translates the upstream chunk stream into a canonical stream event sequence: first chunk → `message_start` (with the registry's canonical model name + `msg_<ulid>` id, `usage.input_tokens` captured when the final-usage chunk arrives — pre-allocate with 1 then overwrite, OR capture and emit `message_start` after the first chunk; per Pitfall 3 emit `message_start.usage = {input_tokens: 0, output_tokens: 1}` initially because OpenAI doesn't expose `prompt_tokens` until the final usage chunk — Plan 02 corrects this if it's a problem; in Plan 01 use `0` then correct in `message_delta` via the final chunk's `prompt_tokens`); each `delta.content` → `content_block_delta {text_delta}`; final chunk with `choices:[]` + `usage` → `message_delta {usage:{output_tokens: usage.completion_tokens}}` (cumulative) and capture `input_tokens` for the message_start backfill (since OpenAI emits it ONLY at the end, the canonical stream is a SECOND pass — buffer in memory until first content delta, emit message_start at that point with input_tokens=0, then overwrite at message_delta time is the realistic approach). Wrap up with `message_stop`. The async generator must NOT swallow signal-aborted; mirror `chunkToSseEvents` discipline.

    LlamacppOpenAIAdapter:
    - Same shape as OllamaOpenAIAdapter for `chatCompletionsCanonical{,Stream}`. NO vision branch (D-B4). Constructor + probeLiveness unchanged.

    routes/v1/chat-completions.ts refactor (D-F3):
    - Imports change: add `import { openAIRequestToCanonical } from '../../translation/openai-in.js';` and `import { canonicalToOpenAIResponse, canonicalToOpenAISse } from '../../translation/openai-out.js';`. Remove `import type { ChatCompletionChunk, ChatCompletionCreateParams } from 'openai/resources/chat/completions';` and `import { chunkToSseEvents } from '../../sse/stream.js';` — they're no longer needed at the route level.
    - Lines 74 (`upstreamParams = ... as ChatCompletionCreateParams`) is REPLACED by `const canonical = openAIRequestToCanonical({...body, model: entry.backend_model});` — note the model is remapped to `entry.backend_model` BEFORE translation so the adapter receives the canonical with the BACKEND model id (the canonical's `model` field is what reaches the upstream SDK call).
    - Stream branch (lines 152–215): replace `adapter.chatCompletionsStream(upstreamParams, ...)` with `adapter.chatCompletionsCanonicalStream(canonical, controller.signal)` and replace `chunkToSseEvents(upstream, {signal, onCleanup: sseCleanup})` with `canonicalToOpenAISse(upstream, {signal: controller.signal, onCleanup: sseCleanup})`. Everything else (the AbortController setup at 76–114, the safeRelease at 136–142, the semaphore acquire at 144–151, the heartbeat + sseCleanup pattern at 174–205, the BackendSaturatedError catch at 229–231, the finally at 234–239) is BYTE-IDENTICAL.
    - Non-stream branch (lines 217–220): replace `adapter.chatCompletions(upstreamParams, ...)` with `const canonicalResult = await adapter.chatCompletionsCanonical(canonical, controller.signal); return reply.send(canonicalToOpenAIResponse(canonicalResult));`.

    Regression gate:
    - `cd router && npx vitest run tests/integration/chat-completions` MUST be fully green after refactor.
    - The wire output of `/v1/chat/completions` (both stream and non-stream) MUST be byte-equivalent to Phase 2/3 except for the `id` field, which now starts with `chatcmpl-` derived from `msg_<ulid>` — i.e., the existing tests at chat-completions.nonstream.test.ts:69 (`expect(body.usage).toEqual({prompt_tokens:12, completion_tokens:4, total_tokens:16})`) MUST still pass exactly. If a test breaks because it asserts `body.id === 'chatcmpl-msw'` (the msw fixture id), KEEP the original msw chatcmpl id by deriving the OpenAI ChatCompletion `id` from the upstream's response id (i.e., `canonicalToOpenAIResponse` either accepts a passed-in `originalId` from the adapter, OR `openAIChatCompletionToCanonical` stashes the upstream id on the canonical and `canonicalToOpenAIResponse` reads it back). Pick the cleanest path; document in commit message. The simplest path: have the canonical response carry an `_upstreamId?: string` private field (not in the schema, attached via Object.assign in the adapter) and have `canonicalToOpenAIResponse` use `canonical._upstreamId ?? canonical.id.replace(/^msg_/, 'chatcmpl-')` for the OpenAI `id`. This preserves the existing test expectations.
  </behavior>
  <action>
    Edit `router/src/backends/ollama-openai.ts`:
    1. Replace the `import type { ... }` block at the top with `import type { CanonicalRequest, CanonicalResponse, CanonicalStreamEvent } from '../translation/canonical.js';` + keep `import OpenAI from 'openai';` + add `import { canonicalToOpenAIChatCompletionParams } from '../translation/openai-in.js';` + `import { openAIChatCompletionToCanonical, openAIChunksToCanonicalEvents } from '../translation/openai-out.js';`.
    2. Delete the `chatCompletions` and `chatCompletionsStream` methods.
    3. Add `async chatCompletionsCanonical(canonical: CanonicalRequest, signal: AbortSignal): Promise<CanonicalResponse>` with the implementation in <behavior>.
    4. Add `async chatCompletionsCanonicalStream(canonical: CanonicalRequest, signal: AbortSignal): Promise<AsyncIterable<CanonicalStreamEvent>>` with the implementation in <behavior>.
    5. Keep constructor + probeLiveness BYTE-IDENTICAL.

    Repeat the same edits for `router/src/backends/llamacpp-openai.ts` — identical adapter shape; no vision branch (D-B4); apiKey placeholder is `'llamacpp'` per line 29 of current file.

    Add the missing helpers to openai-in.ts and openai-out.ts:
    - `openai-in.ts` adds `export function canonicalToOpenAIChatCompletionParams(canonical: CanonicalRequest): ChatCompletionCreateParams` — inverse of `openAIRequestToCanonical`. For text-only canonical: build OpenAI messages array with `{role:'user'|'assistant'|'system', content: <string from text blocks>}` plus put top-level `system` as the first `{role:'system'}` message. Plan 04 adds tool_calls / tool_choice / stop / parallel_tool_calls. Use the BACKEND model id (`canonical.model` already points to backend_model because the route remapped it before translation).
    - `openai-out.ts` adds `export function openAIChatCompletionToCanonical(result: ChatCompletion): CanonicalResponse` — extract `result.choices[0].message.content` → canonical `[{type:'text', text}]`; map `finish_reason` to canonical `stop_reason` (per FINDING 3.9 + Open Question 2 mapping: `stop → end_turn`, `length → max_tokens`, `tool_calls → tool_use`, `content_filter → refusal`, `function_call → tool_use`, else → `end_turn`); compose `usage = {input_tokens: result.usage?.prompt_tokens ?? 0, output_tokens: result.usage?.completion_tokens ?? 0}`. `id` = `newMessageId()`. Attach `_upstreamId = result.id` non-enumerable property via `Object.defineProperty(out, '_upstreamId', {value: result.id, enumerable: false, writable: false})` so `canonicalToOpenAIResponse` can recover it without polluting the canonical type / schema.
    - `openai-out.ts` adds `export async function* openAIChunksToCanonicalEvents(chunks: AsyncIterable<ChatCompletionChunk>, opts: {model: string}): AsyncIterable<CanonicalStreamEvent>` — buffer the first content delta, emit `message_start` (id=newMessageId(), model=opts.model, usage:{input_tokens:0, output_tokens:1}) on the FIRST chunk encountered; emit `content_block_start {index:0, content_block:{type:'text', text:''}}` immediately after; emit `content_block_delta {index:0, delta:{type:'text_delta', text: chunk.choices[0].delta.content}}` for each subsequent content chunk; on the final usage chunk (`choices:[]` + `usage`) capture `output_tokens = usage.completion_tokens`, emit `content_block_stop {index:0}` then `message_delta {delta:{stop_reason:'end_turn', stop_sequence:null}, usage:{output_tokens}}` then `message_stop`. Do NOT propagate the upstream `prompt_tokens` back into `message_start` after the fact — the canonical stream event union doesn't allow mutation, so document this as a Plan 02 fix-up (when full ANTHR-07 lands the route + translator are revisited to pre-allocate input_tokens correctly via tokenizer pre-count).
    - `canonicalToOpenAIResponse` is updated to read `canonical._upstreamId` when present and use it as the OpenAI `id` (with fallback `canonical.id.replace(/^msg_/, 'chatcmpl-')`).

    Edit `router/src/routes/v1/chat-completions.ts`:
    1. Update imports per <behavior>.
    2. Replace line 74's body remap with the canonical translation: `const canonical = openAIRequestToCanonical({ ...body, model: entry.backend_model });`. Wrap in try/catch — `openAIRequestToCanonical` throws ZodError on invalid input, which the centralized handler maps to 400. The catch is for cleanup only (off the socket close listener) before re-throwing.
    3. Replace the stream branch's `adapter.chatCompletionsStream(upstreamParams, ...)` + `chunkToSseEvents(upstream, ...)` per <behavior>.
    4. Replace the non-stream branch's `adapter.chatCompletions(upstreamParams, ...)` per <behavior>.
    5. Keep ALL the AbortController/onClose/safeRelease/semaphore/heartbeat/sseCleanup plumbing BYTE-IDENTICAL. Use `git diff router/src/routes/v1/chat-completions.ts` to confirm only the middle 3 lines (+ imports) changed.

    Note: the auth + bearer + `/healthz` + `/readyz` + `/v1/models` paths are NOT touched by this plan. Their tests continue unchanged.
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && npx vitest run tests/integration/chat-completions tests/translation/ --reporter=basic</automated>
  </verify>
  <done>Both adapters implement chatCompletionsCanonical + chatCompletionsCanonicalStream; chat-completions.ts route flows through canonical; existing Phase 2/3 chat-completions integration tests are fully green (zero regressions); all Wave-0 translator tests still pass; tsc --noEmit succeeds. `grep -RIn "chatCompletionsStream\\|chatCompletions(" router/src router/src/backends/ | grep -v "Canonical\\|test" | wc -l` is 0 (old method names gone from production code).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1/chat/completions (refactored) | Bearer-gated; body is zod-validated against `ChatCompletionRequestSchema` (passthrough) at the route, then re-validated against `CanonicalRequestSchema` after `openAIRequestToCanonical`. Two-pass validation is intentional — the second pass catches normalization bugs in the translator itself. |
| route → adapter | In-process; signal forwarded to undici via openai SDK (Phase 2 contract preserved). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-A1 | Tampering | openai-in.ts message normalization | mitigate | Two-pass zod validation (first at route, second in `CanonicalRequestSchema.parse`); any mutation that violates the canonical contract throws ZodError → 400 envelope. Refusal is observable in pino logs (route catches ZodError and centralized handler emits envelope). |
| T-04-A2 | Information Disclosure | _upstreamId attached non-enumerably | accept | The `Object.defineProperty(out, '_upstreamId', {enumerable: false})` ensures the field does not appear in JSON.stringify of canonical objects (e.g., when Phase 5 logs them). Reviewer must grep for `_upstreamId` and confirm it's only read at the openai-out.ts boundary. |
| T-04-A3 | Denial of Service | newMessageId() called in tight loop on stream events | accept | ULID generation is sub-microsecond; monotonicFactory call is amortized. No DoS surface. |
</threat_model>

<verification>
## Refactor Regression Suite

```bash
cd router
npx tsc --noEmit
npx vitest run --reporter=basic
```

All of the following MUST be green:
- `tests/translation/*.test.ts` — new Wave-0 suites pass with ≥1 it() each.
- `tests/integration/chat-completions.nonstream.test.ts` — Phase 2 contract preserved (200 + usage shape, 404 on unknown model, 502 on upstream unreachable).
- `tests/integration/chat-completions.stream.test.ts` — Phase 2 contract preserved (SSE wire format, [DONE] terminator, abort propagation, mid-stream error frame).
- `tests/integration/chat-completions.llamacpp.test.ts` — Phase 3 llamacpp dispatch preserved.
- `tests/integration/concurrency.*.test.ts` — semaphore + Retry-After preserved.
- `tests/integration/auth.test.ts` + `readyz.test.ts` + `models.test.ts` + `hotreload*.test.ts` + `shutdown.test.ts` — unrelated to Phase 4 but they cover the refactor's surroundings; must stay green.
- `tests/unit/*` — envelope, bearer, factory, liveness, registry, semaphore — all untouched but must stay green.

## Grep gates

```bash
# Old methods removed from production code:
test "$(grep -RIn 'chatCompletionsStream\|chatCompletions(' router/src/ | grep -v 'Canonical\|//' | wc -l)" = "0"

# Adapter no longer references openai/resources types:
test "$(grep -c "from 'openai/resources/chat/completions'" router/src/backends/adapter.ts)" = "0"

# Route file flows through canonical:
grep -q "openAIRequestToCanonical" router/src/routes/v1/chat-completions.ts
grep -q "canonicalToOpenAISse" router/src/routes/v1/chat-completions.ts

# Canonical types are imported in adapters:
test "$(grep -c "from '../translation/canonical" router/src/backends/ollama-openai.ts)" -ge "1"
test "$(grep -c "from '../translation/canonical" router/src/backends/llamacpp-openai.ts)" -ge "1"
```
</verification>

<success_criteria>
- [ ] `router/src/translation/canonical.ts` exports CanonicalRequest, CanonicalResponse, CanonicalMessage, ContentBlock, StopReason types + CanonicalRequestSchema validator + newMessageId/newToolUseId helpers.
- [ ] `BackendAdapter` interface has chatCompletionsCanonical + chatCompletionsCanonicalStream + probeLiveness; old methods are absent (grep `chatCompletionsStream` in router/src/backends/adapter.ts returns 0).
- [ ] OllamaOpenAIAdapter + LlamacppOpenAIAdapter implement both new canonical methods; constructors + probeLiveness untouched.
- [ ] `router/src/routes/v1/chat-completions.ts` flows through canonical; semaphore/heartbeat/abort plumbing is byte-identical to Phase 3.
- [ ] Wave-0 test scaffolds exist for canonical / openai-in / openai-out / anthropic-in / anthropic-out / ollama-native-out / golden; each has ≥1 passing assertion.
- [ ] `npx vitest run` is fully green; Phase 2/3 OpenAI integration tests show zero regressions.
- [ ] `npx tsc --noEmit` succeeds.
- [ ] `router/package.json` lists `ulid: ^3.0.2` in dependencies (gpt-tokenizer + @anthropic-ai/sdk land in Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-01-SUMMARY.md` recording:
- Canonical type surface area (list of exports from canonical.ts).
- Adapter widening verification (grep results above).
- Test count delta (before/after vitest counts).
- Any deviations from the plan (e.g., chosen `_upstreamId` strategy, ULID monotonic factory behavior under tests).
- Forward-handoff notes for Plan 02 (where in `openAIChunksToCanonicalEvents` does Plan 02 need to revisit the input_tokens pre-allocation? what tokenizer module is loaded? etc.).
</output>
