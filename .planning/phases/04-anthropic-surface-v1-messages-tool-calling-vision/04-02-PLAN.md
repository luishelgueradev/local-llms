---
phase: 04-anthropic-surface-v1-messages-tool-calling-vision
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - router/src/translation/anthropic-in.ts
  - router/src/translation/anthropic-out.ts
  - router/src/routes/v1/messages.ts
  - router/src/routes/v1/count-tokens.ts
  - router/src/translation/count-tokens.ts
  - router/src/errors/envelope.ts
  - router/src/app.ts
  - router/package.json
  - router/tests/translation/anthropic-in.test.ts
  - router/tests/translation/anthropic-out.test.ts
  - router/tests/translation/count-tokens.test.ts
  - router/tests/integration/messages.nonstream.test.ts
  - router/tests/integration/messages.count-tokens.test.ts
autonomous: true
requirements: ["ANTHR-02", "ANTHR-03", "ANTHR-04", "ANTHR-05"]
tags:
  - anthropic-non-stream
  - count-tokens
  - role-alternation
objective: "Deliver POST /v1/messages (stream:false) + POST /v1/messages/count_tokens end-to-end through canonical, with role-alternation refinement, top-level system honored, anthropic-version header echo, gpt-tokenizer-backed count_tokens, X-Token-Count-Method response header, and CapabilityNotSupportedError mapped to 400 Anthropic-shape envelope."

must_haves:
  truths:
    - "POST /v1/messages with {model, max_tokens, messages:[{role:'user', content:'hi'}], stream:false} returns 200 + Anthropic-shape Message body {id:/^msg_/, type:'message', role:'assistant', content:[{type:'text', text:...}], model, stop_reason, stop_sequence, usage:{input_tokens, output_tokens}} against an msw-stubbed Ollama upstream."
    - "POST /v1/messages with consecutive same-role messages (e.g., [user, user]) returns 400 with Anthropic-shape body {type:'error', error:{type:'invalid_request_error', message:/roles must alternate/}} (per ANTHR-04 + RESEARCH.md FINDING 1.5)."
    - "POST /v1/messages with role:'system' inside messages[] returns 400 (Anthropic forbids system in messages[]; top-level system is the only legal place per FINDING 1.4)."
    - "POST /v1/messages echoes the anthropic-version request header verbatim in the response (length-capped to 64 chars, CRLF stripped — per T-04-05 mitigation)."
    - "POST /v1/messages/count_tokens with {model, messages:[{role:'user', content:'hi there'}]} returns {input_tokens: N} where N > 0 using gpt-tokenizer cl100k_base, with X-Token-Count-Method: gpt-tokenizer/cl100k_base response header (D-E2)."
    - "POST /v1/messages/count_tokens with tools present adds ~340 tokens to input_tokens (FINDING 2.3 / 3.8 tool-system-prompt overhead)."
    - "POST /v1/messages and POST /v1/messages/count_tokens are bearer-gated; unauthenticated requests return 401 OpenAI-shape envelope (existing bearer hook semantics preserved — bearer pre-dates Anthropic-shape envelope; D-F2)."
    - "/v1/messages/count_tokens does NOT acquire a semaphore slot (pure CPU; D-F1)."
    - "The same upstream call shape continues to satisfy /v1/chat/completions tests — no regression on Phase 2/3 OpenAI surface (Plan 01's canonical pipeline is unchanged)."
    - "Routes do NOT mutate canonical response objects post-hoc; the registry name appears on the wire via translator-option seam introduced by Plan 04 (`canonicalToAnthropicResponse(..., { displayModel: entry.name })`). In Plan 02's intermediate state, the wire shape correctness is verified by integration tests; the cleanup of the temporary `canonicalResult.model = entry.name` mutation lands in Plan 04 Task 2."
  artifacts:
    - path: router/src/translation/anthropic-in.ts
      provides: "anthropicRequestToCanonical with role-alternation superRefine + tool_result-before-text refinement + system top-level handling; uses Anthropic-shape error envelope on rejection"
      contains: "ctx.addIssue"
    - path: router/src/translation/anthropic-out.ts
      provides: "canonicalToAnthropicResponse builds wire-correct Message with msg_<ulid> id, usage.{input_tokens, output_tokens}, stop_reason mapping"
      contains: "canonicalToAnthropicResponse"
    - path: router/src/translation/count-tokens.ts
      provides: "countTokens(canonical): number using gpt-tokenizer/encoding/cl100k_base with per-image overhead formula + tool-prompt overhead"
      exports: ["countTokens", "imageTokens"]
    - path: router/src/routes/v1/messages.ts
      provides: "registerMessagesRoute (POST /v1/messages) — non-stream branch (stream branch lands in Plan 03), capability gating before adapter call, anthropic-version header echo"
      exports: ["registerMessagesRoute"]
    - path: router/src/routes/v1/count-tokens.ts
      provides: "registerCountTokensRoute (POST /v1/messages/count_tokens) — pure CPU, no semaphore, no abort wiring"
      exports: ["registerCountTokensRoute"]
    - path: router/src/errors/envelope.ts
      provides: "CapabilityNotSupportedError class + toAnthropicErrorEnvelope helper + mapToHttpStatus row for CapabilityNotSupportedError (400)"
      contains: "CapabilityNotSupportedError"
    - path: router/src/app.ts
      provides: "registerMessagesRoute + registerCountTokensRoute wired into buildApp; centralized error handler picks Anthropic vs OpenAI envelope based on request URL"
      contains: "registerMessagesRoute"
    - path: router/tests/integration/messages.nonstream.test.ts
      provides: "End-to-end coverage for ANTHR-01 (non-stream half), ANTHR-03, ANTHR-04, ANTHR-05; capability mismatch placeholder (full vision test in Plan 05)"
    - path: router/tests/integration/messages.count-tokens.test.ts
      provides: "End-to-end coverage for ANTHR-02 — response shape, X-Token-Count-Method header, tool overhead, unknown-model 404"
  key_links:
    - from: router/src/routes/v1/messages.ts
      to: router/src/translation/anthropic-in.ts
      via: anthropicRequestToCanonical import
      pattern: "from ['\"]\\.\\./\\.\\./translation/anthropic-in"
    - from: router/src/routes/v1/messages.ts
      to: router/src/translation/anthropic-out.ts
      via: canonicalToAnthropicResponse import
      pattern: "canonicalToAnthropicResponse"
    - from: router/src/routes/v1/count-tokens.ts
      to: router/src/translation/count-tokens.ts
      via: countTokens import
      pattern: "from ['\"]\\.\\./\\.\\./translation/count-tokens"
    - from: router/src/app.ts
      to: router/src/routes/v1/messages.ts
      via: registerMessagesRoute call
      pattern: "registerMessagesRoute"
    - from: router/src/app.ts
      to: router/src/routes/v1/count-tokens.ts
      via: registerCountTokensRoute call
      pattern: "registerCountTokensRoute"
---

<objective>
Land the Anthropic non-stream surface end-to-end: `POST /v1/messages` (stream:false) and `POST /v1/messages/count_tokens`. After this plan, an agent can curl an Anthropic-shape request to the router against the existing Ollama text model and receive a wire-correct Anthropic `Message` back.

Purpose: Plan 01 built the canonical foundation but `/v1/messages` doesn't exist yet. This is the first user-visible Anthropic surface. Streaming (`event: message_start` → ... → `event: message_stop`) lands in Plan 03; tool calling lands in Plan 04; vision lands in Plan 05. This plan covers the text-only vertical slice plus count_tokens (which is text-content-only by nature — it never makes a backend call).

Output: Working `/v1/messages` non-stream branch, working `/v1/messages/count_tokens`, role-alternation enforcement, system top-level handling, `anthropic-version` echo, `CapabilityNotSupportedError` + `toAnthropicErrorEnvelope` infrastructure (used by Plan 03 stream branch and Plan 05 vision gating), and integration tests for ANTHR-02..05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-CONTEXT.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-01-SUMMARY.md
@router/src/translation/canonical.ts
@router/src/translation/anthropic-in.ts
@router/src/translation/anthropic-out.ts
@router/src/routes/v1/chat-completions.ts
@router/src/errors/envelope.ts
@router/src/app.ts
@router/src/auth/bearer.ts
@router/src/config/registry.ts
@router/tests/integration/chat-completions.nonstream.test.ts
@router/tests/msw/handlers.ts
@router/tests/unit/envelope.test.ts
@router/package.json
@CLAUDE.md

<interfaces>
<!-- Wire-correct Anthropic shapes — verified against RESEARCH.md FINDING 1, 2, 7. -->

POST /v1/messages REQUEST body (RESEARCH.md FINDING 1.4):
```typescript
type AnthropicMessagesRequest = {
  model: string;                            // canonical/registry model name
  max_tokens: number;                       // REQUIRED in Anthropic
  messages: Array<{
    role: 'user' | 'assistant';             // NO 'system' — top-level only
    content: string | ContentBlock[];       // string or array of {type:'text'|'image'|'tool_use'|'tool_result', ...}
  }>;
  system?: string;                          // top-level system prompt
  stream?: boolean;                         // default false
  temperature?: number;
  top_p?: number;
  top_k?: number;
  stop_sequences?: string[];                // max 5 per Anthropic
  tools?: Array<{name, description?, input_schema}>;
  tool_choice?: {type:'auto'|'any'|'tool'|'none', name?, disable_parallel_tool_use?};
  // metadata?, system as array, cache_control — NOT supported in Phase 4
};
```

POST /v1/messages RESPONSE body (non-stream — FINDING 1.1 sample at lines 495–509):
```typescript
type AnthropicMessage = {
  id: string;                               // 'msg_' + ulid()
  type: 'message';
  role: 'assistant';
  content: Array<{type:'text', text:string} | {type:'tool_use', id, name, input}>;
  model: string;                            // registry canonical name (NOT backend_model)
  stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use' | null;
  stop_sequence: string | null;
  usage: { input_tokens: number; output_tokens: number };
};
```

POST /v1/messages/count_tokens REQUEST/RESPONSE (FINDING 2.1):
```typescript
// Request: same shape as /v1/messages MINUS stream — backend never called.
// Response:
type CountTokensResponse = { input_tokens: number };
// Response header: X-Token-Count-Method: gpt-tokenizer/cl100k_base
```

Anthropic error envelope (FINDING 1.5 + RESEARCH.md Example C, lines 558–565):
```typescript
type AnthropicErrorEnvelope = {
  type: 'error';
  error: { type: string; message: string };  // type ∈ {invalid_request_error, authentication_error, permission_error, not_found_error, rate_limit_error, api_error, overloaded_error}
};
```

ANTHR-05 echo rule (D-E5 + T-04-05 mitigation):
- Read `req.headers['anthropic-version']`; if present, cap length to 64 chars, strip any `\r`/`\n`, echo as response header `anthropic-version`.
- If absent, do not inject a default (some agents test for it — RESEARCH.md FINDING 7).
- If the value is non-string (Fastify may give array on multi-value), pick the first.

CapabilityNotSupportedError + Anthropic envelope mapping (D-C2 + PATTERNS.md S2):
```typescript
export class CapabilityNotSupportedError extends Error {
  readonly code = 'model_capability_mismatch';
  constructor(public readonly modelName: string, public readonly missingCapability: 'vision' | 'tools') {
    super(`Model "${modelName}" does not support capability "${missingCapability}". Pick a model with "${missingCapability}" in its capabilities list.`);
    this.name = 'CapabilityNotSupportedError';
  }
}
```

count_tokens algorithm (RESEARCH.md Example E, lines 596–626; D-E1):
- Encoder: `import { encode } from 'gpt-tokenizer/encoding/cl100k_base';` (sub-package import — only loads cl100k_base BPE table).
- Module-level singleton — encoder is constructed ONCE at module load, reused per request.
- Per-image overhead: probe PNG IHDR (`\x89PNG\r\n\x1a\n...IHDR<4-byte width><4-byte height>`) or JPEG SOFn (`\xff\xc0..\xc3`) markers from the base64-decoded prefix; compute `Math.ceil((width × height) / 750)`; fallback to `1568` if dims unmeasurable OR source is URL (count_tokens NEVER fetches URLs — CONTEXT.md `<specifics>` line 258).
- Tool overhead: `total += 340` if `canonical.tools && canonical.tools.length > 0` (FINDING 2.3).
- Counts include `system` prompt tokens + each text block + each image overhead + tool block JSON-stringified.

Centralized error handler — split per route prefix (D-F5):
- `app.setErrorHandler` (app.ts:84) currently calls `toOpenAIErrorEnvelope` unconditionally. Phase 4 needs `/v1/messages*` errors to return the Anthropic envelope. Pick based on `req.url.startsWith('/v1/messages')`.
- Pre-stream errors (e.g., capability mismatch, role-alternation 400, unknown model 404) emit the Anthropic envelope via the body. Mid-stream errors (Plan 03) emit `event: error\ndata: ...` SSE frames.

NOTE on route-level model overwrite (temporary in Plan 02, cleaned in Plan 04 Task 2):
- Plan 02 step 6 of Task 2 sets `canonicalResult.model = entry.name;` immediately after the adapter call so the wire response carries the registry name (not backend_model). This is the simplest path that keeps Plan 02 self-contained. Plan 04 Task 2 introduces a translator-option seam (`canonicalToAnthropicResponse(canonical, { displayModel: entry.name })`) that REMOVES this mutation; until then, the integration tests verify the wire shape directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement anthropic-in role-alternation + tool_result-pairing refinements; add CapabilityNotSupportedError + toAnthropicErrorEnvelope; install gpt-tokenizer + @anthropic-ai/sdk; build count-tokens helper</name>
  <files>router/src/translation/anthropic-in.ts, router/src/translation/anthropic-out.ts, router/src/translation/count-tokens.ts, router/src/errors/envelope.ts, router/package.json, router/tests/translation/anthropic-in.test.ts, router/tests/translation/anthropic-out.test.ts, router/tests/translation/count-tokens.test.ts</files>
  <read_first>
    - router/src/translation/anthropic-in.ts (Plan 01 skeleton — replace stub with full impl)
    - router/src/translation/anthropic-out.ts (Plan 01 skeleton — strengthen non-stream side)
    - router/src/translation/canonical.ts (full file — CanonicalRequestSchema + CanonicalResponseSchema + helpers)
    - router/src/config/registry.ts (lines 43–64 — superRefine pattern; PATTERNS.md anthropic-in.ts analog rows lines 189–222)
    - router/src/errors/envelope.ts (existing typed errors; PATTERNS.md envelope.ts MODIFIED section lines 609–697)
    - router/tests/unit/envelope.test.ts (table-driven describe/it pattern)
    - router/tests/unit/registry.test.ts (schema-rejection pattern)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md FINDING 1.4, 1.5, 2.1, 2.2, 2.3, 3.8, 8.1 (count_tokens algorithm + tokenizer pin)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §"anthropic-in.ts" + §"count_tokens helper" rows
  </read_first>
  <behavior>
    Dependencies pinned (per RESEARCH.md FINDING 8.1, 8.2 + Standard Stack table; CONTEXT.md D-A4 + D-E1):
    - `router/package.json` "dependencies": add `"gpt-tokenizer": "^3.0.0"`.
    - `router/package.json` "devDependencies": add `"@anthropic-ai/sdk": "^0.95.1"` (TYPES ONLY per D-A4 — used in this task for cross-check tests).
    - `router/package.json` already has `ulid` from Plan 01.

    anthropic-in.ts (REPLACE Plan 01 stub with full impl):
    - Define `AnthropicMessagesRequestSchema` per <interfaces> shape, using `.passthrough()` so unknown fields don't reject.
    - Add `.superRefine((body, ctx) => {...})` enforcing:
      1. Role alternation: walk `body.messages`. The first message MUST be `role:'user'`. Subsequent must alternate (no two consecutive same-role). On violation → `ctx.addIssue({code:'custom', path:['messages', i, 'role'], message: 'messages: roles must strictly alternate user/assistant (first message must be user)'})`.
      2. No `role:'system'` in messages[] — enforced by the role enum at the schema level (`z.enum(['user', 'assistant'])`); a `role:'system'` body simply fails the enum check with a clear message.
      3. For each `role:'user'` message with content array containing both `tool_result` and `text` blocks: `tool_result` blocks MUST come BEFORE text/image blocks. On violation → `ctx.addIssue({code:'custom', path:['messages', i, 'content'], message: 'user content: tool_result blocks must precede text/image blocks (Anthropic ordering rule)'})`. (RESEARCH.md FINDING 1.5 + Pitfall 2.)
    - Body of `anthropicRequestToCanonical`: parse with the schema (throws ZodError on refinement failure), then walk messages converting string content → `[{type:'text', text}]`, then build `CanonicalRequest` using `CanonicalRequestSchema.parse(built)` as the final validation gate.

    anthropic-out.ts (strengthen `canonicalToAnthropicResponse`):
    - Map `canonical.content` blocks 1:1 to Anthropic content (already Anthropic-shaped — identity transform).
    - `id` from `canonical.id` (already `msg_<ulid>`).
    - `model` from `canonical.model` (registry name; route's post-adapter `canonicalResult.model = entry.name` step ensures this is correct until Plan 04 introduces the cleaner option seam).
    - `stop_reason` passthrough; map nulls correctly.
    - `usage` passthrough.
    - In the test suite use `@anthropic-ai/sdk` types to cross-check: `import type { Message } from '@anthropic-ai/sdk/resources/messages.js'; const m: Message = canonicalToAnthropicResponse(canon);` — if our shape diverges, the TS compiler in the test file errors out.

    count-tokens.ts (NEW file under `router/src/translation/`):
    - Singleton encoder loaded at module init: `import { encode } from 'gpt-tokenizer/encoding/cl100k_base';`.
    - `export function imageTokens(block: ImageBlock): number` — branches:
      - If `block.source.type === 'url'` → return `1568` (no fetch; CONTEXT.md `<specifics>` line 258).
      - If `block.source.type === 'base64'` → try to parse PNG IHDR (offset 16 → 4 bytes width BE, then 4 bytes height BE) for `image/png`, or JPEG SOFn marker for `image/jpeg`. If width × height succeeds: return `Math.ceil((width × height) / 750)`. Else fallback 1568.
    - `export function countTokens(canonical: CanonicalRequest): number`:
      1. `let total = 0;`
      2. If `canonical.system` → `total += encode(canonical.system).length`.
      3. For each message, for each content block:
         - `text` → `total += encode(block.text).length`.
         - `image` → `total += imageTokens(block)`.
         - `tool_use` → `total += encode(JSON.stringify(block.input)).length`.
         - `tool_result` → `total += encode(typeof block.content === 'string' ? block.content : JSON.stringify(block.content)).length`.
      4. If `canonical.tools && canonical.tools.length > 0` → `total += 340` (FINDING 2.3).
      5. Return `total`.

    envelope.ts (additions per PATTERNS.md S2 + D-C2):
    - Add `CapabilityNotSupportedError` class per <interfaces>.
    - Add row in `mapToHttpStatus`: `if (err instanceof CapabilityNotSupportedError) return 400;` placed BEFORE the generic 500 catch-all.
    - Add row in `toOpenAIErrorEnvelope`:
      ```
      if (err instanceof CapabilityNotSupportedError) {
        return { error: { message: err.message, type: 'invalid_request_error', code: 'model_capability_mismatch', param: 'model' } };
      }
      ```
    - Add `export type AnthropicErrorEnvelope = { type: 'error'; error: { type: string; message: string } };`.
    - Add `export const ANTHROPIC_NO_ENVELOPE = Symbol('ANTHROPIC_NO_ENVELOPE');`.
    - Add `mapOpenAITypeToAnthropic(type: string): string` helper per <interfaces>.
    - Add `export function toAnthropicErrorEnvelope(err: unknown): AnthropicErrorEnvelope | typeof ANTHROPIC_NO_ENVELOPE`.

    Test scaffolds expand:
    - `anthropic-in.test.ts`: add it() cases per <behavior> for role-alternation, tool_result ordering, top-level system, unknown-field passthrough, stop_sequences>5 rejection, role:'system' in messages[] rejection.
    - `anthropic-out.test.ts`: add it() cases for the `@anthropic-ai/sdk` type cross-check.
    - `count-tokens.test.ts` (NEW): cases for text-only, system prompt, base64 PNG (use a hand-crafted 1×1 PNG), URL image (assert 1568 fallback), tools array (assert +340 overhead), empty messages (throws ZodError via CanonicalRequestSchema).
    - envelope.test.ts: add cases for `CapabilityNotSupportedError → 400 + envelope` and `toAnthropicErrorEnvelope` mapping table.
  </behavior>
  <action>
    1. Edit `router/package.json`:
       - Dependencies: add `"gpt-tokenizer": "^3.0.0"`.
       - DevDependencies: add `"@anthropic-ai/sdk": "^0.95.1"`.
       - Run `npm install` (or pnpm — match existing).
    2. Replace `router/src/translation/anthropic-in.ts` with the full implementation per <behavior>.
    3. Strengthen `router/src/translation/anthropic-out.ts`.
    4. Create `router/src/translation/count-tokens.ts` with `imageTokens` + `countTokens` exports per <behavior>.
    5. Edit `router/src/errors/envelope.ts`:
       - Add `CapabilityNotSupportedError` class right after `RegistryUnknownModelError` (line ~33).
       - Add `mapToHttpStatus` row right after the `RegistryUnknownModelError → 404` line.
       - Add `toOpenAIErrorEnvelope` row right after `RegistryUnknownModelError` row (line ~86).
       - Add `AnthropicErrorEnvelope` type, `ANTHROPIC_NO_ENVELOPE` sentinel, `mapOpenAITypeToAnthropic`, `toAnthropicErrorEnvelope` exports at the end of the file (after `midStreamErrorFrameLines`).
    6. Expand test files per <behavior>.
  </action>
  <verify>
    <automated>cd router && npm install && npx tsc --noEmit && npx vitest run tests/translation/anthropic-in.test.ts tests/translation/anthropic-out.test.ts tests/translation/count-tokens.test.ts tests/unit/envelope.test.ts --reporter=basic</automated>
  </verify>
  <done>All four test files green; gpt-tokenizer + @anthropic-ai/sdk in router/package.json; envelope.ts exports CapabilityNotSupportedError + toAnthropicErrorEnvelope + ANTHROPIC_NO_ENVELOPE; anthropic-in.ts rejects [user, user] AND [user, system, user] AND tool_result-after-text; count-tokens.ts uses gpt-tokenizer/encoding/cl100k_base sub-package import (verified via grep).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register POST /v1/messages (non-stream branch) + POST /v1/messages/count_tokens; wire centralized error handler to pick Anthropic vs OpenAI envelope; integrate into app.ts</name>
  <files>router/src/routes/v1/messages.ts, router/src/routes/v1/count-tokens.ts, router/src/app.ts</files>
  <read_first>
    - router/src/routes/v1/chat-completions.ts (full file — PATTERNS.md S3 + messages.ts route analog at lines 311–445)
    - router/src/routes/v1/models.ts (small route shape — analog for count-tokens.ts)
    - router/src/app.ts (current full file — route registration pattern at lines 184–203; error handler at 84–93)
    - router/src/translation/anthropic-in.ts + anthropic-out.ts + count-tokens.ts (just-finished Task 1)
    - router/src/translation/canonical.ts (CapabilityNotSupportedError import path)
    - router/src/errors/envelope.ts (new toAnthropicErrorEnvelope + CapabilityNotSupportedError)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §S3 (route handler shape) + §"app.ts" MODIFIED section
  </read_first>
  <behavior>
    `registerMessagesRoute(app, opts: { registry, makeAdapter, semaphores })`:
    - Same signature as `registerChatCompletionsRoute` (chat-completions.ts:54).
    - Route: `typed.post('/v1/messages', { schema: { body: AnthropicMessagesRequestSchema } }, handler)`.
    - Handler structure mirrors `chat-completions.ts` byte-for-byte for the AbortController/onClose/safeRelease/semaphore/heartbeat plumbing. THIS PLAN: the stream branch returns `501 Not Implemented` body `{type:'error', error:{type:'invalid_request_error', message:'streaming not yet implemented; lands in Plan 03 — POST with stream:false'}}`. ONLY the non-stream branch is wired here.
    - Non-stream branch:
      1. Resolve model: `const entry = opts.registry.resolve(body.model);`.
      2. Translate: `const canonical = anthropicRequestToCanonical({...body, model: entry.backend_model});`.
      3. Capability gating (D-C2): walk `canonical.messages` for image blocks; if any AND `!entry.capabilities.includes('vision')` → throw `new CapabilityNotSupportedError(entry.name, 'vision')`. Tools gating is soft per D-C3.
      4. Echo `anthropic-version` request header (D-E5).
      5. Acquire semaphore: `const semaphore = opts.semaphores.get(entry.backend); release = await semaphore.acquire(controller.signal);`.
      6. Call adapter: `const canonicalResult = await adapter.chatCompletionsCanonical(canonical, controller.signal);`. Apply the temporary post-adapter overwrite: `canonicalResult.model = entry.name;` (cleaned up in Plan 04 Task 2 via translator-option seam). Add an inline comment: `// TEMPORARY: Plan 04 introduces canonicalToAnthropicResponse(..., { displayModel: entry.name }) which removes this mutation`.
      7. Serialize: `return reply.send(canonicalToAnthropicResponse(canonicalResult));`.
    - Error handling: re-throw to centralized handler. BackendSaturatedError: set `Retry-After` header before re-throw.

    `registerCountTokensRoute(app, opts: { registry })`:
    - Route: `typed.post('/v1/messages/count_tokens', { schema: { body: AnthropicCountTokensRequestSchema } }, handler)`.
    - Body: `const body = req.body;`.
    - Resolve model (404 on unknown).
    - Translate: `const canonical = anthropicRequestToCanonical({...body, model: entry.backend_model});`.
    - Compute: `const input_tokens = countTokens(canonical);`.
    - Set response header: `void reply.header('X-Token-Count-Method', 'gpt-tokenizer/cl100k_base');`.
    - Return: `return { input_tokens };`.
    - NO semaphore, NO abort wiring, NO backend call (D-F1).

    app.ts changes:
    - Imports: `import { registerMessagesRoute } from './routes/v1/messages.js'; import { registerCountTokensRoute } from './routes/v1/count-tokens.js'; import { toAnthropicErrorEnvelope, ANTHROPIC_NO_ENVELOPE } from './errors/envelope.js';`.
    - Inside `setErrorHandler`: check `req.url`. If it starts with `/v1/messages`: call `toAnthropicErrorEnvelope(err)`; if ANTHROPIC_NO_ENVELOPE → return; else `reply.code(mapToHttpStatus(err)).send(envelope)`. Otherwise the existing OpenAI path.
    - After `registerChatCompletionsRoute(...)` add `registerMessagesRoute(app, { registry: opts.registry, makeAdapter: opts.makeAdapter ?? defaultMakeAdapter, semaphores });` and `registerCountTokensRoute(app, { registry: opts.registry });`.
  </behavior>
  <action>
    1. Create `router/src/routes/v1/messages.ts`. Copy the structure of `router/src/routes/v1/chat-completions.ts` lines 54–242; replace zod schema + canonical translation + add capability gating + anthropic-version echo + the temporary `canonicalResult.model = entry.name;` per <behavior>.
    2. Create `router/src/routes/v1/count-tokens.ts` per <behavior>.
    3. Edit `router/src/app.ts` per <behavior>.
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && npx vitest run tests/integration/chat-completions tests/translation/ --reporter=basic && grep -c "registerMessagesRoute\|registerCountTokensRoute" router/src/app.ts</automated>
  </verify>
  <done>Both routes file exist; app.ts registers them; centralized error handler branches on /v1/messages prefix; tsc --noEmit succeeds; existing chat-completions integration suite still green; grep returns ≥2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: Integration tests for /v1/messages non-stream — ANTHR-03..05 + capability gate placeholder (12 it() cases in messages.nonstream.test.ts)</name>
  <files>router/tests/integration/messages.nonstream.test.ts</files>
  <read_first>
    - router/tests/integration/chat-completions.nonstream.test.ts (full file — analog to copy for setup/auth/error cases)
    - router/tests/msw/handlers.ts (full file — ollamaNonStreamHandler factory shape)
    - router/src/routes/v1/messages.ts (Task 2 just-finished)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §"messages.nonstream.test.ts" MODIFIED row
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-RESEARCH.md FINDING 1.1, 1.4, 1.5, 7
  </read_first>
  <behavior>
    `router/tests/integration/messages.nonstream.test.ts`:
    - Setup: clone the structure of `chat-completions.nonstream.test.ts` lines 1–44 byte-for-byte (TOKEN, MODEL_NAME, UPSTREAM_BASE, YAML, buildApp, fake semaphores, makeAdapter pointing to msw upstream).
    - Use the EXISTING `ollamaNonStreamHandler` from `tests/msw/handlers.ts`.

    Coverage (each is a separate it()):
    1. `happy path text-only` — POST `/v1/messages` with `{model: MODEL_NAME, max_tokens: 100, messages: [{role:'user', content:'Say hi'}]}`. Assert: status 200, body.id matches `/^msg_/`, body.type === 'message', body.role === 'assistant', body.content === `[{type:'text', text:'Hi from msw'}]`, body.model === MODEL_NAME, body.stop_reason === 'end_turn', body.usage === `{input_tokens: 12, output_tokens: 4}`.
    2. `ANTHR-03: top-level system honored` — POST with `{model, max_tokens, messages:[{role:'user', content:'hi'}], system:'be brief'}`. Intercept upstream POST body via msw and assert `messages[0].role === 'system'` AND `messages[0].content === 'be brief'`.
    3. `ANTHR-04: role-alternation [user, user] rejected` — Assert 400 with Anthropic envelope containing "alternate".
    4. `ANTHR-04: tool_result after text rejected` — Assert 400 with Anthropic envelope containing "tool_result".
    5. `ANTHR-04: role:'system' in messages[] rejected` — Assert 400 (zod enum rejection).
    6. `ANTHR-05: anthropic-version header echoed verbatim` — POST with header `anthropic-version: 2023-06-01`. Assert response header equals `2023-06-01`.
    7. `ANTHR-05: anthropic-version with CRLF stripped + length-capped` — Assert response header contains NO `\r` or `\n` AND length ≤ 64.
    8. `ANTHR-05: anthropic-version absent — no default injected` — POST without the header. Assert response does NOT contain `anthropic-version` header.
    9. `Unknown model returns 404 with Anthropic envelope`.
    10. `Upstream unreachable returns 502 with Anthropic envelope` — register HttpResponse.error() handler; assert mapped to `api_error`.
    11. `Missing max_tokens returns 400`.
    12. `stream: true returns 501 placeholder (Plan 03 finishes)` — assert 501 with Plan-03-deleted assertion. Plan 03 deletes this assertion.
  </behavior>
  <action>
    Create `router/tests/integration/messages.nonstream.test.ts`. Copy lines 1–44 of `chat-completions.nonstream.test.ts` byte-for-byte. Change the route URL to `/v1/messages`. Change the payload shape to Anthropic. Write all 12 it() cases per <behavior>.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/integration/messages.nonstream.test.ts --reporter=basic</automated>
  </verify>
  <done>messages.nonstream.test.ts has all 12 listed it() cases green; tsc --noEmit succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: Integration tests for /v1/messages/count_tokens — ANTHR-02 (9 it() cases in messages.count-tokens.test.ts)</name>
  <files>router/tests/integration/messages.count-tokens.test.ts</files>
  <read_first>
    - router/tests/integration/messages.nonstream.test.ts (Task 3a — just-authored; reuse setup pattern but drop the msw upstream because count_tokens never calls upstream)
    - router/src/routes/v1/count-tokens.ts (Task 2 — verify the route shape)
    - router/src/translation/count-tokens.ts (Task 1 — countTokens contract: per-image overhead formula + tools +340)
    - .planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-PATTERNS.md §"messages.count-tokens.test.ts" row
  </read_first>
  <behavior>
    `router/tests/integration/messages.count-tokens.test.ts`:
    - Setup: strip the msw upstream from messages.nonstream.test.ts's setup (count_tokens never calls upstream). Keep buildApp + fake semaphores + YAML.

    Coverage:
    1. `happy path text-only returns input_tokens > 0` — POST `/v1/messages/count_tokens` with `{model, messages:[{role:'user', content:'hello world'}], max_tokens: 100}`. Assert status 200, body matches `{input_tokens: number}`, body.input_tokens > 0.
    2. `D-E2: X-Token-Count-Method header set` — same request, assert response header `x-token-count-method` equals `gpt-tokenizer/cl100k_base`.
    3. `tools array adds 340 token overhead` — POST with tools defined. Assert `(input_tokens_with_tools - input_tokens_without_tools) >= 300`.
    4. `system prompt adds tokens` — POST with `system:'this is a system prompt'`. Compare to no-system baseline; difference > 0.
    5. `URL image falls back to 1568 constant` — POST with `content:[{type:'image', source:{type:'url', url:'https://example.com/x.png'}}, {type:'text', text:'.'}]`. Assert input_tokens >= 1568.
    6. `base64 PNG with measurable dims computes (w*h)/750` — POST with a hand-crafted 1×1 PNG base64 string. Assert input_tokens for the image block ≈ Math.ceil((1*1)/750) = 1.
    7. `unknown model returns 404 Anthropic envelope` — POST with bad model. Assert 404 + Anthropic envelope.
    8. `empty messages returns 400` — POST with `messages:[]`. Assert 400 + Anthropic envelope.
    9. `count_tokens does NOT acquire a semaphore` — wire YAML with `concurrency: 1`; fire 10 concurrent count_tokens requests; assert all 10 return 200 (no 429). Proves D-F1.
  </behavior>
  <action>
    Create `router/tests/integration/messages.count-tokens.test.ts`. Setup like nonstream.test.ts but DROP the msw handler imports + server.use() calls. Write all 9 it() cases per <behavior>.
    - For the 1×1 PNG fixture, declare at the top of the file: `const PNG_1x1 = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==';`.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/integration/messages.count-tokens.test.ts --reporter=basic</automated>
  </verify>
  <done>messages.count-tokens.test.ts has all 9 listed it() cases green; tsc --noEmit succeeds; full vitest suite still green with zero regressions on Phase 2/3 OpenAI tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1/messages | Bearer-gated; body zod-validated against AnthropicMessagesRequestSchema (with role-alternation superRefine); model resolved against registry; capability gating fires BEFORE adapter call. |
| client → /v1/messages/count_tokens | Bearer-gated; body zod-validated; NO backend call — pure CPU. |
| anthropic-version header → response header | Length-capped to 64 chars + CRLF stripped (T-04-05 mitigation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | /v1/messages/count_tokens unauthenticated probe of registry | mitigate | Route is bearer-gated via the existing `onRequest` bearer hook. Test #1 in `messages.count-tokens.test.ts` exercises this (omit Authorization → 401). The route never calls the backend so abuse is bounded to CPU; combined with bearer auth this is acceptable. |
| T-04-05 | Header Injection | anthropic-version response header echo | mitigate | Sanitize: cap to 64 chars (`.slice(0,64)`), strip `\r` and `\n` via `.replace(/[\r\n]/g, '')`. Test #6 + #7 in messages.nonstream.test.ts verify. |
| T-04-04b | DoS via count_tokens loop | gpt-tokenizer encode on very long input | accept | gpt-tokenizer encode is O(n) on input length and runs in-process — the existing 8 MB bodyLimit caps the worst case. |
| T-04-MV-1 | Tampering | model-name remap from registry.name to backend_model before adapter call | mitigate | `messages.ts` step 6 (<behavior>) overwrites `canonicalResult.model = entry.name;` AFTER the adapter call. Otherwise the response would leak the backend_model id. Verified by integration test #1 (`body.model === MODEL_NAME`). Plan 04 Task 2 replaces this with the translator-option seam. |
</threat_model>

<verification>
## Acceptance Smoke

```bash
cd router && npx vitest run --reporter=basic
```
All suites green. New integration test counts:
- `messages.nonstream.test.ts`: ≥12 it() cases (Task 3a).
- `messages.count-tokens.test.ts`: ≥9 it() cases (Task 3b).

## Grep gates

```bash
# Routes registered
grep -q "registerMessagesRoute" router/src/app.ts
grep -q "registerCountTokensRoute" router/src/app.ts

# Anthropic envelope branches in error handler
grep -q "toAnthropicErrorEnvelope" router/src/app.ts
grep -q "/v1/messages" router/src/app.ts

# Bearer skip-list unchanged
grep -E "PUBLIC_PATHS|/healthz|/readyz" router/src/auth/bearer.ts | grep -v "test"

# CapabilityNotSupportedError reachable
grep -q "CapabilityNotSupportedError" router/src/translation/anthropic-in.ts router/src/routes/v1/messages.ts router/src/errors/envelope.ts
test "$(grep -c CapabilityNotSupportedError router/src/errors/envelope.ts)" -ge 2

# Tokenizer pin
grep -q "gpt-tokenizer" router/package.json
grep -q "gpt-tokenizer/encoding/cl100k_base" router/src/translation/count-tokens.ts
```

## Manual

```bash
# After plan deploys to running stack:
curl -sS -H "Authorization: Bearer $LOCAL_LLMS_BEARER" \
     -H "Content-Type: application/json" \
     -H "anthropic-version: 2023-06-01" \
     -d '{"model":"llama3.2:3b-instruct-q4_K_M","max_tokens":100,"messages":[{"role":"user","content":"say hi"}]}' \
     http://router:3000/v1/messages | jq .
# Expect: {id:"msg_...", type:"message", role:"assistant", content:[...], usage:{input_tokens, output_tokens}}.

curl -sS -H "Authorization: Bearer $LOCAL_LLMS_BEARER" \
     -H "Content-Type: application/json" \
     -d '{"model":"llama3.2:3b-instruct-q4_K_M","messages":[{"role":"user","content":"hello world"}]}' \
     http://router:3000/v1/messages/count_tokens
# Expect: {"input_tokens":<N>}, response header X-Token-Count-Method: gpt-tokenizer/cl100k_base.
```
</verification>

<success_criteria>
- [ ] POST /v1/messages (stream:false) returns Anthropic-shape Message body against an Ollama text model end-to-end.
- [ ] POST /v1/messages/count_tokens returns `{input_tokens: N}` + X-Token-Count-Method header; uses gpt-tokenizer/cl100k_base; no backend call.
- [ ] Role-alternation violations + tool_result-after-text + role:'system' in messages[] → 400 Anthropic envelope.
- [ ] anthropic-version request header echoed in response (sanitized: ≤64 chars, no CRLF).
- [ ] All errors on /v1/messages* return Anthropic-shape envelope `{type:'error', error:{type, message}}`.
- [ ] CapabilityNotSupportedError + toAnthropicErrorEnvelope + ANTHROPIC_NO_ENVELOPE exported from envelope.ts.
- [ ] gpt-tokenizer + @anthropic-ai/sdk pinned in router/package.json.
- [ ] Full vitest suite green; Phase 2/3 OpenAI tests show zero regressions.
- [ ] Stream branch of /v1/messages returns 501 placeholder (replaced in Plan 03).
- [ ] Requirements ANTHR-02, ANTHR-03, ANTHR-04, ANTHR-05 verified via integration test cases.
</success_criteria>

<output>
After completion, create `.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-02-SUMMARY.md` recording:
- The anthropic-version sanitization implementation choice (raw CRLF rejection by Node parser vs server-side strip).
- Token-count delta numbers for the integration tests (so Plan 04 can verify the +340 tool overhead).
- The behavior of the centralized error handler when `req.url` is missing (e.g., on a raw 400 before routing). Document the fallback.
- Forward-handoff notes for Plan 03 (which file/lines of messages.ts contain the 501 placeholder that Plan 03 must replace; whether the heartbeat/sseCleanup machinery from chat-completions.ts is fully copied or stubbed) AND Plan 04 (the temporary `canonicalResult.model = entry.name;` mutation in Task 2 step 6 — Plan 04 Task 2 removes it via translator-option seam).
</output>
</content>
</invoke>