---
phase: 14-policy-primitives-tenant-project-id-foundation
plan: 07
type: execute
wave: 4
depends_on:
  - 14-05
  - 14-06
files_modified:
  - router/src/metrics/recordOutcome.ts
  - router/src/app.ts
  - router/src/routes/v1/chat-completions.ts
  - router/src/routes/v1/messages.ts
  - router/src/routes/v1/embeddings.ts
  - router/src/routes/v1/rerank.ts
  - router/src/routes/v1/responses.ts
  - router/src/routes/__tests__/scopedIds-request-log.test.ts
autonomous: true
requirements:
  - POL-04
tags:
  - postgres
  - recordOutcome
  - request_log
must_haves:
  truths:
    - "OutcomeContext widens with `tenantId?`, `projectId?`, `workloadClass?` mirroring the existing `agentId?` field (D-19)"
    - "row builder in recordOutcome.ts stamps `tenant_id`, `project_id`, `workload_class` into `RequestLogInsert` (`?? null` mirror of agent_id pattern)"
    - "All 13 safeRecord/recordOutcome call sites across the 5 routes pass `req.tenantId / req.projectId / req.workloadClass` next to existing `req.agentId`"
    - "Caller sending X-Tenant-ID + X-Project-ID + X-Workload-Class sees those values in the `request_log` Postgres row for that request (POL-04 success criterion 3)"
    - "setErrorHandler `recordOutcome` call in app.ts also threads the scoped IDs (pre-resolve errors get tenant context when scopedIdsPreHandler ran before the error)"
  artifacts:
    - path: "router/src/metrics/recordOutcome.ts"
      provides: "OutcomeContext widened + row builder stamps three new columns"
      contains: "tenant_id: ctx.tenantId ?? null"
    - path: "router/src/app.ts (modification of setErrorHandler recordOutcome call)"
      provides: "Pre-resolve error path now includes scoped IDs"
      contains: "tenantId: req.tenantId"
    - path: "router/src/routes/v1/{chat-completions,messages,embeddings,rerank,responses}.ts"
      provides: "13 safeRecord/recordOutcome call sites widened (3 lines added next to existing agentId)"
      contains: "tenantId: req.tenantId"
    - path: "router/src/routes/__tests__/scopedIds-request-log.test.ts"
      provides: "Integration test — full request lifecycle stamps tenant_id/project_id/workload_class into request_log row (bufferedWriter spy)"
  key_links:
    - from: "router/src/middleware/scopedIds.ts (Plan 06 — stamps req.* fields)"
      to: "router/src/metrics/recordOutcome.ts (row builder reads ctx.tenantId etc.)"
      via: "Per-route safeRecord closure passes req.* to OutcomeContext"
      pattern: "tenantId: req.tenantId"
    - from: "router/src/metrics/recordOutcome.ts"
      to: "router/db/migrations/0005_request_log_scoped_ids.sql (live columns)"
      via: "RequestLogInsert type widening (Plan 01) flows to bufferedWriter to Postgres"
      pattern: "RequestLogInsert"
---

<objective>
Plumb scoped IDs end-to-end from `req.tenantId`/`req.projectId`/`req.workloadClass` (stamped by Plan 06's scopedIdsPreHandler) through the per-route `safeRecord` closures, into `OutcomeContext`, into the `RequestLogInsert` row, and onto live Postgres. Integration test asserts a full-stack round trip: caller sends the three headers; the `request_log` row reflects them.

Purpose: POL-04 success criterion 3 — "A caller sending `X-Tenant-ID: acme` and `X-Project-ID: agents` sees both values appear in the Postgres `request_log` row for that request (verified by integration test querying the DB row)." Plan 01 added the columns; Plan 05 applied them live; Plan 06 stamped them on `req`; this plan threads them into the bufferedWriter row.

Output: One `recordOutcome.ts` patch (widen OutcomeContext + row builder), one `app.ts` patch (setErrorHandler recordOutcome call), 5 route-file patches (13 grep-driven sites), one integration test exercising the full stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-01-SUMMARY.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-05-SUMMARY.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-06-SUMMARY.md

<interfaces>
<!-- The recordOutcome plumbing pattern — verbatim shapes from RESEARCH.md -->

From router/src/metrics/recordOutcome.ts (existing OutcomeContext — line 60+):
  export interface OutcomeContext {
    // ...
    agentId?: string;       // <-- insert tenantId?, projectId?, workloadClass? next to this
    requestId: string;
    // ...
  }

From router/src/metrics/recordOutcome.ts (existing row builder — line ~255):
  const row: RequestLogInsert = {
    // ...
    agent_id: ctx.agentId ?? null,    // <-- insert tenant_id/project_id/workload_class lines after this
    request_id: ctx.requestId,
    // ...
  };

From router/src/app.ts setErrorHandler recordOutcome call (line ~340-355):
  recordOutcome({
    // ...existing fields...
    agentId: req.agentId,         // <-- insert tenantId/projectId/workloadClass next to this
    requestId: req.id,
    // ...
  });

<!-- Per-route safeRecord pattern — 13 sites identified by RESEARCH.md grep:
     chat-completions.ts: 461, 542, 561, 701, 933 (5 sites)
     messages.ts: 364, 452, 471, 601, 754 (5 sites)
     embeddings.ts: 526 (1 site)
     rerank.ts: 246 (1 site)
     responses.ts: 498 (1 site)
   At each site: existing line is `agentId: req.agentId,`
   Insertion: add three sibling lines immediately after:
     tenantId: req.tenantId,
     projectId: req.projectId,
     workloadClass: req.workloadClass,
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen OutcomeContext + row builder + setErrorHandler recordOutcome call</name>
  <files>router/src/metrics/recordOutcome.ts, router/src/app.ts</files>
  <read_first>
    - router/src/metrics/recordOutcome.ts (entire file — capture OutcomeContext shape around line 60+, the `row: RequestLogInsert = {...}` builder around line 240-269, the `truncateAndRedact` helper, and the bufferedWriter.push call)
    - router/src/db/schema/request_log.ts (confirm Plan 01 widened it with `tenant_id`, `project_id`, `workload_class`)
    - router/src/app.ts (lines ~340-355 — setErrorHandler recordOutcome invocation)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"recordOutcome Plumbing" Patches 1-3 (verbatim shapes)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md (lines 463-503)
  </read_first>
  <behavior>
    No behavior block — structural patch. Behavior tested end-to-end in Task 3.
  </behavior>
  <action>
    (1) `router/src/metrics/recordOutcome.ts`:
        - Widen `OutcomeContext` (after the existing `agentId?: string` line ~73): add three sibling optional fields `tenantId?: string`, `projectId?: string`, `workloadClass?: string`. Inline comment: `// Phase 14 (v0.11.0 — POL-03/POL-04)`.
        - Extend the `row: RequestLogInsert = {...}` builder (around line 255, immediately after `agent_id: ctx.agentId ?? null,`): add three sibling lines `tenant_id: ctx.tenantId ?? null,`, `project_id: ctx.projectId ?? null,`, `workload_class: ctx.workloadClass ?? null,`. Same `?? null` pattern as `agent_id` — the Drizzle `RequestLogInsert` type widened automatically when Plan 01 patched the schema.

    (2) `router/src/app.ts`:
        - Find the `setErrorHandler(...)` block that calls `recordOutcome({...})` (around line 340-355). Add three sibling lines next to the existing `agentId: req.agentId,`:
          - `tenantId: req.tenantId,`
          - `projectId: req.projectId,`
          - `workloadClass: req.workloadClass,`
        - Inline comment: `// Phase 14: pre-resolve errors still get scoped-ID context if scopedIdsPreHandler ran before the error.`

    DO NOT touch route files in this task — Task 2 fans the change across the 13 safeRecord sites.
  </action>
  <verify>
    <automated>cd router && pnpm typecheck && pnpm vitest run src/metrics --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantId?: string" router/src/metrics/recordOutcome.ts` returns ≥ 1.
    - `grep -c "projectId?: string" router/src/metrics/recordOutcome.ts` returns ≥ 1.
    - `grep -c "workloadClass?: string" router/src/metrics/recordOutcome.ts` returns ≥ 1.
    - `grep -c "tenant_id: ctx.tenantId ?? null" router/src/metrics/recordOutcome.ts` outputs `1`.
    - `grep -c "project_id: ctx.projectId ?? null" router/src/metrics/recordOutcome.ts` outputs `1`.
    - `grep -c "workload_class: ctx.workloadClass ?? null" router/src/metrics/recordOutcome.ts` outputs `1`.
    - `grep -c "tenantId: req.tenantId" router/src/app.ts` returns ≥ 1.
    - `grep -c "projectId: req.projectId" router/src/app.ts` returns ≥ 1.
    - `grep -c "workloadClass: req.workloadClass" router/src/app.ts` returns ≥ 1.
    - `cd router && pnpm typecheck` exits 0 (the RequestLogInsert type widening from Plan 01 flows through; ctx fields fit).
    - Existing `src/metrics` tests still pass: `cd router && pnpm vitest run src/metrics --reporter=dot` exits 0.
  </acceptance_criteria>
  <done>OutcomeContext + row builder + setErrorHandler all carry scoped IDs end-to-end through the data plane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fan the safeRecord widening across all 13 per-route call sites</name>
  <files>router/src/routes/v1/chat-completions.ts, router/src/routes/v1/messages.ts, router/src/routes/v1/embeddings.ts, router/src/routes/v1/rerank.ts, router/src/routes/v1/responses.ts</files>
  <read_first>
    - router/src/routes/v1/chat-completions.ts (find the 5 sites listed in RESEARCH.md: lines 461, 542, 561, 701, 933 — actual line numbers may have shifted post-Plan 05; use `grep -n 'agentId: req.agentId' router/src/routes/v1/chat-completions.ts` to locate)
    - router/src/routes/v1/messages.ts (find the 5 sites: 364, 452, 471, 601, 754 area; same grep)
    - router/src/routes/v1/embeddings.ts (find the 1 site near line 526)
    - router/src/routes/v1/rerank.ts (find the 1 site near line 246)
    - router/src/routes/v1/responses.ts (find the 1 site near line 498)
    - router/src/metrics/recordOutcome.ts (confirm Task 1 widened OutcomeContext — needed before TypeScript will accept the new fields at these call sites)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md (lines 559-577 — the grep-driven 13 sites map)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"recordOutcome Plumbing" Patch 4
  </read_first>
  <behavior>
    No behavior block — pure structural fan-out driven by grep gates.
  </behavior>
  <action>
    Each of the 5 route files contains one or more sites where a `safeRecord` closure or direct `recordOutcome(...)` call passes `agentId: req.agentId,`. At EVERY such site (13 total per RESEARCH.md grep — re-confirm with `grep -nE 'agentId:\\s*req\\.agentId' router/src/routes/v1/*.ts`):

    Insert three sibling lines IMMEDIATELY AFTER the existing `agentId: req.agentId,` line:
    ```
    tenantId: req.tenantId,
    projectId: req.projectId,
    workloadClass: req.workloadClass,
    ```

    Each insertion is purely additive — no other lines in these files change in this task. Do NOT touch the applyPolicyGate insertion (that landed in Plan 05). Do NOT touch error handling or anything else.

    Run TypeScript + the full vitest suite after the fan-out to confirm no regressions.
  </action>
  <verify>
    <automated>cd router && pnpm typecheck && pnpm vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tenantId: req.tenantId" router/src/routes/v1/chat-completions.ts` outputs `5` (one per existing agentId site).
    - `grep -c "tenantId: req.tenantId" router/src/routes/v1/messages.ts` outputs `5`.
    - `grep -c "tenantId: req.tenantId" router/src/routes/v1/embeddings.ts` outputs `1`.
    - `grep -c "tenantId: req.tenantId" router/src/routes/v1/rerank.ts` outputs `1`.
    - `grep -c "tenantId: req.tenantId" router/src/routes/v1/responses.ts` outputs `1`.
    - Total: `grep -rcE 'tenantId:\\s*req\\.tenantId' router/src/routes/v1/ | awk -F: '{s+=$2}END{print s}'` outputs `13`.
    - Same arithmetic holds for `projectId: req.projectId` (total 13) and `workloadClass: req.workloadClass` (total 13).
    - At every site where `agentId: req.agentId,` exists, the THREE new sibling lines appear IMMEDIATELY after (verified by sliding-window grep: every `tenantId: req.tenantId,` is preceded within 5 lines by `agentId: req.agentId,`).
    - `cd router && pnpm typecheck` exits 0.
    - `cd router && pnpm vitest run --reporter=dot` exits 0 (full suite green).
  </acceptance_criteria>
  <done>All 13 sites pass the new fields into OutcomeContext; TypeScript clean; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — full request lifecycle stamps tenant_id/project_id/workload_class into request_log row</name>
  <files>router/src/routes/__tests__/scopedIds-request-log.test.ts</files>
  <read_first>
    - router/tests/integration/recordOutcome.test.ts (lines 149-151 — bufferedWriter spy pattern; capture entire setup)
    - router/tests/integration/chat-completions.nonstream.test.ts (buildApp helper; MSW handler stubs for ollama upstream)
    - router/src/app.ts (buildApp shape — confirm bufferedWriter is injectable via opts; if not, the test injects via the opts.recordOutcome shape that already exists)
    - router/src/middleware/scopedIds.ts (confirm Plan 06 stamps the three req.* fields)
    - router/src/metrics/recordOutcome.ts (confirm Task 1 widened the row builder)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"Validation Architecture" (POL-03/POL-04 row-population pattern)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-VALIDATION.md row 14-07-01
  </read_first>
  <behavior>
    - Test 1 (happy path — all 3 headers populated): POST `/v1/chat/completions` with headers `X-Tenant-ID: acme`, `X-Project-ID: agents`, `X-Workload-Class: SENSITIVE` + valid body + bufferedWriter spy. MSW returns a canned 200. Assert pushed row has `tenant_id === 'acme'`, `project_id === 'agents'`, `workload_class === 'sensitive'` (lowercased per D-11).
    - Test 2 (no headers → all NULL): Same body, no scoped-ID headers. Assert pushed row has `tenant_id === null`, `project_id === null`, `workload_class === null`.
    - Test 3 (invalid X-Workload-Class silent-NULL row): POST with `X-Workload-Class: "in valid"` (contains space). Assert status 200 (silent-NULL behavior); pushed row has `workload_class === null`.
    - Test 4 (invalid X-Tenant-ID → 400 → still recorded with NULL): POST with `X-Tenant-ID: "../../etc"` (invalid). Assert status 400; if the error path records via setErrorHandler, the row has `tenant_id === null` (the value was rejected before stamping); otherwise the test asserts no row was pushed AND the response is the InvalidScopedIdError envelope.
    - Test 5 (cross-route — /v1/messages): Same happy-path body for `/v1/messages` with the three headers. Assert pushed row reflects them (validates the safeRecord widening hit messages.ts).
    - Test 6 (cross-route — /v1/embeddings): Same for embeddings (one route's safeRecord widening site).
  </behavior>
  <action>
    Create `router/src/routes/__tests__/scopedIds-request-log.test.ts` — vitest integration test.

    (1) Mirror `router/tests/integration/recordOutcome.test.ts` line 149-151 bufferedWriter spy:
        ```ts
        const pushed: RequestLogInsert[] = [];
        const bufferedWriter = {
          push: (row: RequestLogInsert) => pushed.push(row),
          drain: async () => {},
          get size() { return 0; },
        };
        ```
        Pass via `buildApp({ bufferedWriter, ... })` (or equivalent injection — confirm exact opts field via Task 1 reads).

    (2) MSW setup: register `http.post('http://ollama:11434/v1/chat/completions', () => HttpResponse.json(<canned-completion>))` for happy-path completion. Same for `/v1/embeddings` upstream.

    (3) Encode the 6 behavior cases above. Each uses `app.inject({ method: 'POST', url, headers, payload })` then asserts on `pushed[0]?.tenant_id` etc.

    (4) Each test reset `pushed.length = 0` in `beforeEach`.

    (5) For Test 4 (400 path): if the route records a row at the error path, capture it; otherwise assert `pushed.length === 0` and the response is the InvalidScopedIdError envelope. Both behaviors are valid; the test should match whichever Plan 06's preHandler-throw flow + app.ts setErrorHandler ultimately produce — confirm by reading recordOutcome.ts and app.ts at execution time.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run src/routes/__tests__/scopedIds-request-log.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && pnpm vitest run src/routes/__tests__/scopedIds-request-log.test.ts --reporter=dot` exits 0 with 6 passing tests.
    - Test 1 explicitly asserts `pushed[0]?.tenant_id === 'acme'`, `pushed[0]?.project_id === 'agents'`, `pushed[0]?.workload_class === 'sensitive'`.
    - Test 2 asserts all three columns are null when headers are absent.
    - Test 3 asserts workload_class is null when value violates WC_RE (silent-NULL path).
    - Test 5 + Test 6 confirm the cross-route fan-out from Task 2 actually populates rows on the messages and embeddings surfaces.
    - `cd router && pnpm vitest run --reporter=dot` (full suite) exits 0 — regression check.
    - `cd router && pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>POL-04 success criterion 3 proven end-to-end: caller-sent scoped IDs land in the `request_log` row via the bufferedWriter shim, on the canonical chat-completions surface plus at least two other routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| req.* fields (Plan 06 stamped) → OutcomeContext → bufferedWriter → Postgres INSERT | Plumbing trusted; values already regex-validated upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Information Disclosure | Cross-tenant leakage via row mis-attribution | accept | Each request has its own req object; safeRecord closure reads req.tenantId etc. at the moment of recording — no shared state. |
| T-14-07-02 | Tampering | Missed safeRecord site → row missing tenant_id | mitigate | Task 2 grep gates assert exactly 13 sites widened (matches existing agentId.req.agentId site count). |
| T-14-07-03 | Information Disclosure | Drizzle type inference mismatch → silent column drop on INSERT | accept | Plan 01 widened RequestLogInsert via Drizzle schema; TypeScript catches missing column writes; pnpm typecheck in acceptance criteria validates this. |
</threat_model>

<verification>
- vitest scoped IDs row: `cd router && pnpm vitest run src/routes/__tests__/scopedIds-request-log.test.ts --reporter=dot`
- full suite: `cd router && pnpm vitest run --reporter=dot`
- typecheck: `cd router && pnpm typecheck`
- 13-site grep gate per `<acceptance_criteria>`
</verification>

<success_criteria>
- OutcomeContext widened; row builder stamps three new columns
- setErrorHandler in app.ts threads scoped IDs into pre-resolve error path
- 13 safeRecord/recordOutcome sites fan-out across 5 routes
- Integration test proves end-to-end row population (POL-04 SC3)
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-07-SUMMARY.md` when done. Include: actual line numbers found for the 13 safeRecord sites (post-Plan 05 the numbers may have shifted), the integration-test pass list, and a one-line `pushed[0]` JSON example from Test 1.
</output>
