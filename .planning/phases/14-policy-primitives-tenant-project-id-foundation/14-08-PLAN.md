---
phase: 14-policy-primitives-tenant-project-id-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - router/scripts/check-prometheus-cardinality.ts
  - router/scripts/__tests__/check-prometheus-cardinality.test.ts
autonomous: true
requirements:
  - POL-06
tags:
  - prometheus
  - ci
  - cardinality
must_haves:
  truths:
    - "`checkCardinality(source)` scans labelNames arrays in `src/metrics/registry.ts` and asserts no element matches `/_id$/` (D-25/D-26)"
    - "Production `src/metrics/registry.ts` is clean — checkCardinality returns empty array"
    - "Synthetic `tenant_id` label injection is detected (regression test)"
    - "Legitimate `model` and `dims` labels are NOT flagged"
    - "Script registers as a vitest test under `scripts/__tests__/` — runs as part of `pnpm vitest run` (no separate test:cardinality script needed per Claude's discretion)"
  artifacts:
    - path: "router/scripts/check-prometheus-cardinality.ts"
      provides: "Exported `checkCardinality(source: string): CardinalityViolation[]` + CLI entry point"
      contains: "export function checkCardinality"
    - path: "router/scripts/__tests__/check-prometheus-cardinality.test.ts"
      provides: "3 vitest cases per RESEARCH.md"
  key_links:
    - from: "router/scripts/check-prometheus-cardinality.ts"
      to: "router/src/metrics/registry.ts (the target file to scan)"
      via: "fs.readFileSync at CLI entry; vitest test imports checkCardinality and passes source string"
      pattern: "src/metrics/registry.ts"
---

<objective>
Land the Prometheus cardinality CI guard locked by D-25/D-26: a TypeScript script `router/scripts/check-prometheus-cardinality.ts` that exports `checkCardinality(source)` AND a vitest test that scans the live `src/metrics/registry.ts`, asserting no `labelNames:` array contains an element matching `/_id$/`. Failure message references CONTEXT.md D-25 and Pitfall P8-03.

Purpose: POL-06 — durable defense against the tenant_id-as-Prometheus-label cardinality explosion (10 tenants × 5 projects × 800 existing series = 40K+ series; quantified BLOCK-severity pitfall). Phase 14 ships zero new metric labels (D-28); the CI guard ensures Phase 15+ don't regress.

Output: One CLI/module script + one vitest test (3 cases). No changes to `src/metrics/registry.ts` itself — its current shape is verified clean by the test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md

<interfaces>
<!-- Script signature locked in RESEARCH.md §"CI Cardinality Check — Implementation Outline" -->

export interface CardinalityViolation {
  location: string;          // "registry.ts:36"
  arrayText: string;         // "['protocol', 'tenant_id']"
  forbiddenLabel: string;    // "tenant_id"
  metricNameHint: string;    // "router_requests_total"
}

export function checkCardinality(source: string): CardinalityViolation[];

<!-- Target shape — existing labelNames discipline in registry.ts:33-38 -->
const requestsTotal = new Counter({
  name: 'router_requests_total',
  help: '...',
  labelNames: ['protocol', 'backend', 'model', 'status_class'] as const,
  registers: [register],
});

<!-- Regex bank -->
LABEL_NAMES_RE = /labelNames\s*:\s*\[([^\]]*)\]/g
METRIC_NAME_RE = /name\s*:\s*['"]([a-z0-9_]+)['"]/g
FORBIDDEN_LABEL_RE = /_id$/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement checkCardinality script + vitest test (3 cases)</name>
  <files>router/scripts/check-prometheus-cardinality.ts, router/scripts/__tests__/check-prometheus-cardinality.test.ts</files>
  <read_first>
    - router/src/metrics/registry.ts (entire file — confirm all `labelNames:` arrays are clean of `/_id$/` elements; capture the exact metric-name + labelNames pairs to assert against in the test)
    - router/scripts/gc-classify.ts (if present — analog for the script-as-module pattern with CLI entry; mirror its structure)
    - router/tsconfig.json (confirm `scripts/` is in the compile path or covered by vitest's tsx loader)
    - router/package.json scripts (confirm `vitest run` discovery includes `scripts/__tests__/`; capture `test` script wording)
    - router/vitest.config.ts (confirm test file glob includes `scripts/__tests__/**/*.test.ts`)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"CI Cardinality Check — Implementation Outline" (lines 773-871 — verbatim TypeScript implementation)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md (lines 581-596)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-VALIDATION.md row 14-08-01
  </read_first>
  <behavior>
    Script unit tests (vitest cases — all run via `pnpm vitest run scripts/__tests__/`):
    - Test 1 (production clean): Read `router/src/metrics/registry.ts` content via `fs.readFileSync`; call `checkCardinality(source)`; assert empty array (`[]`).
    - Test 2 (regression — synthetic tenant_id): Pass a hand-crafted source string that declares `new Counter({ name: 'router_fake_total', labelNames: ['protocol', 'tenant_id'] as const });`; call `checkCardinality(...)`; assert one violation with `forbiddenLabel === 'tenant_id'` and `metricNameHint === 'router_fake_total'`.
    - Test 3 (legitimate labels not flagged): Pass a source string `new Counter({ name: 'router_embeddings_dims_total', labelNames: ['model', 'dims'] as const });`; assert empty array.
    - Test 4 (defense — multiple violations counted): Pass source with `labelNames: ['protocol', 'tenant_id', 'project_id']`; assert TWO violations (`tenant_id` AND `project_id`).
    - Test 5 (defense — agent_id detected): Pass source with `labelNames: ['agent_id']`; assert one violation with `forbiddenLabel === 'agent_id'`.
  </behavior>
  <action>
    (1) Create `router/scripts/check-prometheus-cardinality.ts` per RESEARCH.md §"CI Cardinality Check — Implementation Outline" (verbatim shape):
        - Export interface `CardinalityViolation { location, arrayText, forbiddenLabel, metricNameHint }`.
        - Export function `checkCardinality(source: string): CardinalityViolation[]`:
          - Build an offset-indexed list of `{ offset, name }` pairs where `name` is each `router_*` metric `name:` value.
          - Iterate `LABEL_NAMES_RE` matches over the source.
          - For each match, extract individual labels via `/['"]([a-z0-9_]+)['"]/g`.
          - For each label ending in `_id` (regex `/_id$/`), build a `CardinalityViolation` with the nearest preceding metric name as `metricNameHint` and the line number computed from the offset.
        - CLI entry: if `import.meta.url === \`file://${process.argv[1]}\``, read `path.resolve(process.cwd(), 'src/metrics/registry.ts')`, run `checkCardinality`, write each violation to stderr with the message `"cardinality-check: FORBIDDEN _id label <label> found in <metricName> (<location>). Labels matching /_id$/ are forbidden — see CONTEXT.md D-25 / Pitfall P8-03."`, and `process.exit(1)` on violations.

    (2) Create `router/scripts/__tests__/check-prometheus-cardinality.test.ts` covering the 5 behaviors above. Use `fs.readFileSync(path.resolve(import.meta.dirname, '../../src/metrics/registry.ts'), 'utf8')` to load the production source for Test 1. For tests 2-5, pass hand-crafted source strings.

    (3) Confirm `pnpm vitest run scripts/__tests__/check-prometheus-cardinality.test.ts` works — if vitest's `include` glob in `vitest.config.ts` does not cover `scripts/__tests__/`, extend it; otherwise the test won't run by default. Run a quick check at execution: if the file glob excludes the path, add it; do NOT introduce a separate test script (Claude's discretion per D-26 — single `pnpm vitest run` is enough).

    (4) Add a top-of-file comment to `router/src/metrics/registry.ts` (one line pointer):
        `// Phase 14 (POL-06): labelNames arrays MUST NOT contain elements ending in '_id' — guarded by scripts/check-prometheus-cardinality.ts (see CONTEXT.md D-25).`
        This is a 1-line addition that surfaces the constraint at the source — RESEARCH.md Pitfall 6 recommends it.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run scripts/__tests__/check-prometheus-cardinality.test.ts --reporter=dot && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `router/scripts/check-prometheus-cardinality.ts` exists.
    - `grep -c "export function checkCardinality" router/scripts/check-prometheus-cardinality.ts` outputs `1`.
    - `grep -c "export interface CardinalityViolation" router/scripts/check-prometheus-cardinality.ts` outputs `1`.
    - `grep -c "_id\$" router/scripts/check-prometheus-cardinality.ts` returns ≥ 1 (the forbidden-label regex).
    - `grep -c "CONTEXT.md D-25" router/scripts/check-prometheus-cardinality.ts` returns ≥ 1 (failure message references the decision).
    - `cd router && pnpm vitest run scripts/__tests__/check-prometheus-cardinality.test.ts --reporter=dot` exits 0 with 5 passing tests.
    - Test 1 (production-clean assertion) actually scans `router/src/metrics/registry.ts` AND finds zero violations (proves Phase 14 didn't introduce any forbidden labels — D-28).
    - `cd router && pnpm vitest run --reporter=dot` (full suite) exits 0 — the new script test is discoverable.
    - `cd router && pnpm typecheck` exits 0.
    - `grep -c "POL-06" router/src/metrics/registry.ts` returns ≥ 1 (one-line pointer to the guard).
  </acceptance_criteria>
  <done>Cardinality CI guard exists; production source is verified clean; regression cases (tenant_id / agent_id / project_id) are detected; legitimate labels (model / dims) are NOT flagged. Phase 15+ adding a forbidden label will fail this test pre-merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future PR adds a `*_id` label to `src/metrics/registry.ts` → Prometheus cardinality blowup | CI script catches it pre-merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Denial of Service | Prometheus high-cardinality explosion (P8-03 BLOCK) | mitigate | Static-grep script + vitest test fails build on `/_id$/` labels; failure message names the offending label and references CONTEXT.md D-25; one-line pointer at top of registry.ts surfaces the constraint at source |
| T-14-08-01 | Tampering | Script bypass via inline-declared metric outside registry.ts | accept | RESEARCH.md Assumption A6 — Phase 14 commits to scanning `src/metrics/registry.ts` only; Phase 19 lifts the scope (live `/metrics` parse). Documented in script header. |
</threat_model>

<verification>
- vitest: `cd router && pnpm vitest run scripts/__tests__/check-prometheus-cardinality.test.ts --reporter=dot`
- full suite: `cd router && pnpm vitest run --reporter=dot`
- typecheck: `cd router && pnpm typecheck`
- 5 grep gates per `<acceptance_criteria>`
</verification>

<success_criteria>
- `checkCardinality(source)` exported and CLI-runnable
- Production `src/metrics/registry.ts` is provably clean (0 violations)
- Synthetic tenant_id/agent_id/project_id labels all detected
- Legitimate labels (model, dims, etc.) NOT flagged
- Single-line pointer at top of registry.ts surfaces the constraint
</success_criteria>

<output>
Create `.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-08-SUMMARY.md` when done. Include: a list of every metric+labelNames pair currently in `src/metrics/registry.ts` (proves the live scan passed against real source), and the 5 vitest case names.
</output>
