---
phase: 14-policy-primitives-tenant-project-id-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - router/src/errors/envelope.ts
  - router/src/errors/__tests__/policy-envelopes.test.ts
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-05
tags:
  - errors
  - envelope
  - openai
  - anthropic
must_haves:
  truths:
    - "AllowlistViolationError maps to 403 + OpenAI envelope `{type:'policy_violation', code:'model_not_in_allowlist'}` AND Anthropic envelope `{type:'permission_error'}` (D-10)"
    - "CloudNotAllowedError maps to 403 + OpenAI envelope `{type:'policy_violation', code:'cloud_not_allowed'}` AND Anthropic envelope `{type:'permission_error'}` (D-10)"
    - "InvalidScopedIdError maps to 400 + OpenAI envelope `{type:'invalid_request_error', code:'invalid_scoped_id', param:<headerLabel>}` AND Anthropic envelope `{type:'invalid_request_error'}` (mirrors InvalidAgentIdError)"
    - "InvalidScopedIdError truncates `suppliedValue` to 32 chars in the message (log-injection defense, mirrors InvalidAgentIdError)"
    - "Decisions covered: D-10 (new error types: AllowlistViolationError, CloudNotAllowedError + Anthropic envelope mirror), D-16 (invalid tenant/project IDs → InvalidScopedIdError → 400)"
  artifacts:
    - path: "router/src/errors/envelope.ts"
      provides: "Three new error classes + mapToHttpStatus + toOpenAIErrorEnvelope + toAnthropicErrorEnvelope branches"
      contains: "class AllowlistViolationError"
    - path: "router/src/errors/__tests__/policy-envelopes.test.ts"
      provides: "Unit tests asserting all 3 errors → all 3 mappings (9 assertion paths)"
  key_links:
    - from: "router/src/errors/envelope.ts"
      to: "router/src/app.ts setErrorHandler"
      via: "Throw-from-route → centralized envelope dispatch (existing seam, no patch needed)"
      pattern: "mapToHttpStatus|toOpenAIErrorEnvelope|toAnthropicErrorEnvelope"
---

<objective>
Add three new typed error classes (`AllowlistViolationError`, `CloudNotAllowedError`, `InvalidScopedIdError`) and wire them into the centralized error → wire envelope dispatch (mapToHttpStatus + OpenAI envelope + Anthropic envelope). Plans 04 (gate helper) and 06 (scopedIds preHandler) THROW these errors; the centralized setErrorHandler routes them to 403/400 with the correct envelope on both wire surfaces.

Purpose: Establish the typed-error → 403/400 envelope mapping so policy and scoped-ID violations surface consistently across OpenAI (`/v1/chat/completions`, `/v1/embeddings`, `/v1/rerank`, `/v1/responses`) and Anthropic (`/v1/messages`) routes. Dual mapping is the single source of truth — adding switch-statements in route files is the anti-pattern v0.9.0 explicitly rejects.

Output: Three error classes + 9 mapping branches (3 errors × 3 mappers) + unit tests proving each path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md

<interfaces>
<!-- Verbatim error-class template — CapabilityNotSupportedError, envelope.ts:57-69 -->
export class CapabilityNotSupportedError extends Error {
  readonly code = 'model_capability_mismatch';
  constructor(public readonly modelName: string, public readonly missingCapability: '...') {
    super(`Model "${modelName}" does not support capability "${missingCapability}"...`);
    this.name = 'CapabilityNotSupportedError';
  }
}

<!-- Verbatim truncation defense — InvalidAgentIdError, envelope.ts:278-290 -->
export class InvalidAgentIdError extends Error {
  readonly code = 'invalid_agent_id';
  constructor(public readonly suppliedValue: string) {
    const display = typeof suppliedValue === 'string' && suppliedValue.length > 32
      ? `${suppliedValue.slice(0, 32)}...` : String(suppliedValue ?? '');
    super(`X-Agent-Id "${display}" violates regex /^[A-Za-z0-9._:-]{1,128}$/`);
    this.name = 'InvalidAgentIdError';
  }
}

<!-- mapToHttpStatus existing branches — envelope.ts:327, 333 -->
if (err instanceof CapabilityNotSupportedError) return 400;
if (err instanceof InvalidAgentIdError) return 400;

<!-- OpenAI envelope branch — envelope.ts:395-403 -->
if (err instanceof CapabilityNotSupportedError) {
  return { error: { message: err.message, type: 'invalid_request_error', code: 'model_capability_mismatch', param: 'model' } };
}

<!-- Anthropic envelope branch — envelope.ts:628-630 -->
if (err instanceof CapabilityNotSupportedError) {
  return { type: 'error', error: { type: 'invalid_request_error', message: err.message } };
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 error classes + 9 mapping branches + unit tests</name>
  <files>router/src/errors/envelope.ts, router/src/errors/__tests__/policy-envelopes.test.ts</files>
  <read_first>
    - router/src/errors/envelope.ts (entire file — capture exact line numbers of: CapabilityNotSupportedError class definition; InvalidAgentIdError class definition; mapToHttpStatus existing branches; toOpenAIErrorEnvelope existing branches; toAnthropicErrorEnvelope existing branches; the InvalidIdempotencyKeyError block after which the new classes insert)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"Error Envelope Dual-Mapping — New Error Classes" (Patches 1–4, verbatim)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md (lines 149-220 for error-class + envelope analogs and the truncation-defense reuse)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md D-10 (locked envelope shapes)
  </read_first>
  <behavior>
    - Test 1: `new AllowlistViolationError("foo").code === "model_not_in_allowlist"`; `name === "AllowlistViolationError"`; message includes `"foo"`.
    - Test 2: `new CloudNotAllowedError("bar").code === "cloud_not_allowed"`; `name === "CloudNotAllowedError"`; message includes `"bar"`.
    - Test 3: `new InvalidScopedIdError("X-Tenant-ID", "abc!def").code === "invalid_scoped_id"`; message contains the regex `/^[A-Za-z0-9._:-]{1,128}$/`; message contains the label `X-Tenant-ID`.
    - Test 4 (truncation defense): `new InvalidScopedIdError("X-Tenant-ID", "a".repeat(40)).message` contains `"a".repeat(32) + "..."` AND does NOT contain the full 40-char string.
    - Test 5: `mapToHttpStatus(new AllowlistViolationError("m"))` returns 403.
    - Test 6: `mapToHttpStatus(new CloudNotAllowedError("m"))` returns 403.
    - Test 7: `mapToHttpStatus(new InvalidScopedIdError("X-Tenant-ID", "x"))` returns 400.
    - Test 8: `toOpenAIErrorEnvelope(new AllowlistViolationError("m"))` returns `{ error: { message: <any>, type: "policy_violation", code: "model_not_in_allowlist", param: "model" } }`.
    - Test 9: `toOpenAIErrorEnvelope(new CloudNotAllowedError("m"))` returns `{ error: { message: <any>, type: "policy_violation", code: "cloud_not_allowed", param: "model" } }`.
    - Test 10: `toOpenAIErrorEnvelope(new InvalidScopedIdError("X-Project-ID", "x"))` returns `{ error: { message: <any>, type: "invalid_request_error", code: "invalid_scoped_id", param: "X-Project-ID" } }`.
    - Test 11: `toAnthropicErrorEnvelope(new AllowlistViolationError("m"))` returns `{ type: "error", error: { type: "permission_error", message: <any> } }`.
    - Test 12: `toAnthropicErrorEnvelope(new CloudNotAllowedError("m"))` returns `{ type: "error", error: { type: "permission_error", message: <any> } }`.
    - Test 13: `toAnthropicErrorEnvelope(new InvalidScopedIdError("X-Tenant-ID", "x"))` returns `{ type: "error", error: { type: "invalid_request_error", message: <any> } }`.
  </behavior>
  <action>
    Extend `router/src/errors/envelope.ts` with three error classes and nine mapping branches. Mirror existing pattern precedents exactly.

    (1) Add three error classes after `InvalidIdempotencyKeyError` (around line 266) — mirror `CapabilityNotSupportedError` for the 403s and `InvalidAgentIdError` (including its 32-char truncation defense) for the 400:
        - `AllowlistViolationError(modelName)` — `readonly code = 'model_not_in_allowlist'`; message names the model and the file (`policies.default.model_allowlist` in `models.yaml`) where to fix.
        - `CloudNotAllowedError(modelName)` — `readonly code = 'cloud_not_allowed'`; message names the model and explains the resolved entry has `policy.cloud_allowed=false`.
        - `InvalidScopedIdError(headerLabel, suppliedValue)` — `readonly code: 'invalid_scoped_id'`; constructor truncates `suppliedValue` to 32 chars + `"..."` (mirrors `InvalidAgentIdError`); message format `${headerLabel} "<display>" violates regex /^[A-Za-z0-9._:-]{1,128}$/`.

    (2) Extend `mapToHttpStatus` (insert after the existing `InvalidAgentIdError` branch around line 333):
        - `AllowlistViolationError` → 403; `CloudNotAllowedError` → 403; `InvalidScopedIdError` → 400.

    (3) Extend `toOpenAIErrorEnvelope` (insert after the existing `InvalidAgentIdError` branch around line 440):
        - `AllowlistViolationError` → `{ error: { message, type: 'policy_violation', code: 'model_not_in_allowlist', param: 'model' } }`.
        - `CloudNotAllowedError` → `{ error: { message, type: 'policy_violation', code: 'cloud_not_allowed', param: 'model' } }`.
        - `InvalidScopedIdError` → `{ error: { message, type: 'invalid_request_error', code: 'invalid_scoped_id', param: err.headerLabel } }`.

    (4) Extend `toAnthropicErrorEnvelope` (insert after the existing `InvalidAgentIdError` branch around line 645):
        - `AllowlistViolationError` → `{ type: 'error', error: { type: 'permission_error', message } }`.
        - `CloudNotAllowedError` → `{ type: 'error', error: { type: 'permission_error', message } }`.
        - `InvalidScopedIdError` → `{ type: 'error', error: { type: 'invalid_request_error', message } }`.

    (5) Create `router/src/errors/__tests__/policy-envelopes.test.ts` — vitest unit tests covering the 13 behaviors above. Each `describe` block names the D-10 sub-decision it covers; each `it` block runs one assertion.

    Each new error class MUST be exported. Do NOT touch route files or app.ts — the centralized setErrorHandler in `app.ts:311` already dispatches by error type (the new types pick up automatically because they extend `Error`).
  </action>
  <verify>
    <automated>cd router && pnpm vitest run src/errors/__tests__/policy-envelopes.test.ts --reporter=dot && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export class AllowlistViolationError" router/src/errors/envelope.ts` outputs `1`.
    - `grep -c "export class CloudNotAllowedError" router/src/errors/envelope.ts` outputs `1`.
    - `grep -c "export class InvalidScopedIdError" router/src/errors/envelope.ts` outputs `1`.
    - `grep -c "readonly code = 'model_not_in_allowlist'" router/src/errors/envelope.ts` outputs `1`.
    - `grep -c "readonly code = 'cloud_not_allowed'" router/src/errors/envelope.ts` outputs `1`.
    - `grep -c "invalid_scoped_id" router/src/errors/envelope.ts` returns ≥ 2 (class code + OpenAI mapping).
    - `grep -c "instanceof AllowlistViolationError" router/src/errors/envelope.ts` outputs `3` (mapToHttpStatus + OpenAI mapper + Anthropic mapper).
    - `grep -c "instanceof CloudNotAllowedError" router/src/errors/envelope.ts` outputs `3`.
    - `grep -c "instanceof InvalidScopedIdError" router/src/errors/envelope.ts` outputs `3`.
    - `grep -c "type: 'policy_violation'" router/src/errors/envelope.ts` returns ≥ 2 (allowlist + cloud OpenAI envelopes).
    - `grep -c "type: 'permission_error'" router/src/errors/envelope.ts` returns ≥ 2 (allowlist + cloud Anthropic envelopes).
    - `cd router && pnpm vitest run src/errors/__tests__/policy-envelopes.test.ts --reporter=dot` exits 0.
    - `cd router && pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Three error classes + nine mapping branches + 13 unit tests. Downstream (Plan 04 gate, Plan 06 scopedIds preHandler) can throw these errors and they reach the wire correctly on both OpenAI and Anthropic routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Attacker-controlled header → log/envelope message | `InvalidScopedIdError` embeds `suppliedValue` after truncation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering / DoS | InvalidScopedIdError message construction | mitigate | 32-char truncation mirrors InvalidAgentIdError (envelope.ts:278-290); regex character class excludes control chars by construction (preHandler validates first when shape OK; the error fires only when shape FAILS so untrusted-content goes through truncation) |
| T-14-ENV-01 | Information Disclosure | OpenAI envelope `param` field | accept | `param: err.headerLabel` exposes the header name only (e.g. `"X-Tenant-ID"`); no value leaked; mirrors existing `param: 'model'` discipline |
| T-14-ENV-02 | Tampering | Wire-protocol drift between OpenAI/Anthropic envelopes | mitigate | Dual-mapping discipline preserved — single source of truth in envelope.ts; centralized setErrorHandler in app.ts dispatches by URL prefix |
</threat_model>

<verification>
- vitest: `cd router && pnpm vitest run src/errors/__tests__/policy-envelopes.test.ts --reporter=dot`
- typecheck: `cd router && pnpm typecheck`
- 13 grep gates listed in `<acceptance_criteria>`
</verification>

<success_criteria>
- 3 new error classes exported from envelope.ts
- mapToHttpStatus + toOpenAIErrorEnvelope + toAnthropicErrorEnvelope all dispatch the new types
- Truncation defense applied to InvalidScopedIdError
- 13 unit tests covering the dispatch matrix all green
</success_criteria>

<output>
Create `.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-03-SUMMARY.md` when done. Include: error-class line numbers (where added), the 9 mapping insertions (with line context), and the vitest pass count.
</output>
