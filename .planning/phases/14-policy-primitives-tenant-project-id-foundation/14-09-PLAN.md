---
phase: 14-policy-primitives-tenant-project-id-foundation
plan: 09
type: execute
wave: 5
depends_on:
  - 14-05
  - 14-07
files_modified:
  - router/models.yaml
  - DEPLOY.md
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - POL-01
  - POL-02
  - POL-03
tags:
  - docs
  - operator-affordances
  - smoke
user_setup:
  - service: docker-compose
    why: "Smoke test gate requires the live stack running locally to reach `/v1/chat/completions` etc."
    env_vars: []
    dashboard_config:
      - task: "Ensure `docker compose up -d` has the router, postgres, valkey services healthy before running smoke"
        location: "Local docker host"
must_haves:
  truths:
    - "`router/models.yaml` has a commented `# policies:` example block at the top showing both global allowlist and per-entry `policy.cloud_allowed: false` (operator affordance, default behavior unchanged)"
    - "DEPLOY.md + README.md document: policy stanza, X-Tenant-ID/X-Project-ID/X-Workload-Class headers, hot-reload caveat (Valkey DEL + `docker compose up -d --force-recreate router`), cardinality CI guard"
    - "POL-01 wording in `.planning/REQUIREMENTS.md` is patched per D-03 to reflect the top-level `policies.default.model_allowlist` shape"
    - "Existing smoke test suite passes unchanged with policy defaults (allow-all) — POL Success Criterion 5"
  artifacts:
    - path: "router/models.yaml"
      provides: "Commented policy stanza at top of file; cloud_allowed example inside one model block (still commented)"
      contains: "# policies:"
    - path: "DEPLOY.md"
      provides: "Operator-facing 'Policy primitives (Phase 14)' subsection — covers stanza shape, headers, hot-reload caveat, cardinality CI"
      contains: "policies.default.model_allowlist"
    - path: "README.md"
      provides: "Brief 'Policy' section referencing DEPLOY.md for details"
      contains: "X-Tenant-ID"
    - path: ".planning/REQUIREMENTS.md"
      provides: "POL-01 reworded to reflect top-level policies block (D-03)"
      contains: "policies.default.model_allowlist"
  key_links:
    - from: "router/models.yaml"
      to: "Valkey + router restart sequence"
      via: "Hot-reload caveat — Valkey DEL + force-recreate (project memory project_models_yaml_hot_edit.md)"
      pattern: "valkey-cli DEL"
---

<objective>
Land the operator-visible affordances and final phase regression gate:
1. Commented policy example block in `router/models.yaml` (default behavior unchanged; ergonomic operator UX).
2. DEPLOY.md + README.md sections documenting the policy stanza, scoped-ID headers, hot-reload caveat, and the cardinality CI guard.
3. Patch the POL-01 wording in `.planning/REQUIREMENTS.md` per D-03 (the original "per registry entry" phrasing was a wording bug — the hybrid shape moves the allowlist to top-level).
4. Run the existing smoke test suite end-to-end against the live stack to prove POL Success Criterion 5 (existing smoke passes unchanged with allow-all defaults).

Purpose: Close the operator visibility loop AND prove the "zero-config = unchanged behavior" promise (D-04). This is the regression checkpoint before `/gsd:verify-work` — if any earlier plan accidentally broke an existing smoke assertion, this catches it.

Output: YAML comment block + two docs sections + one REQUIREMENTS edit + smoke gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-05-SUMMARY.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-07-SUMMARY.md

<interfaces>
<!-- models.yaml comment style precedent — top of file -->
backends:
  ollama:
    # base_url: documentation only — NOT used at runtime
    base_url: http://ollama:11434/v1
    concurrency: 2
    queue_max_wait_ms: 30000

<!-- POL-01 current wording in REQUIREMENTS.md (line 21) — TO BE PATCHED per D-03 -->
- [ ] **POL-01**: Operator can declare a `model_allowlist: []` array per registry entry in `models.yaml`...

<!-- POL-01 patched wording (target — D-03) -->
- [ ] **POL-01**: Operator can declare a top-level `policies.default.model_allowlist: []` array in `models.yaml`; empty list (default) = allow-all. Requests for a model outside the allowlist return `403` with structured error `{ code: "model_not_in_allowlist", model }` BEFORE backend resolution (per registry-entry allowlist shape was discarded at discuss time — see CONTEXT.md D-02/D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commented policy stanza in models.yaml + REQUIREMENTS POL-01 wording patch</name>
  <files>router/models.yaml, .planning/REQUIREMENTS.md</files>
  <read_first>
    - router/models.yaml (entire file — capture exact top-of-file structure, comment style, and the cloud-backed model entry name to use as the `policy.cloud_allowed: false` example target)
    - .planning/REQUIREMENTS.md (find POL-01 around line 21; capture the exact existing wording byte-for-byte; ensure adjacent POL-02..06 lines remain unchanged)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"Operator-Visible Affordances → router/models.yaml" (verbatim commented stanza)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md D-01..D-06 (locks the stanza shape) and D-03 (POL-01 wording patch instruction)
    - project memory `project_models_yaml_hot_edit.md` content (hot-reload caveat language)
  </read_first>
  <behavior>
    No behavior block — content patch verified by grep gates.
  </behavior>
  <action>
    (1) `router/models.yaml`: insert a commented policy stanza at the VERY TOP of the file, before the existing `backends:` block. Use `# policies:` etc. style — every line of the example MUST start with `#` so default behavior is unchanged (D-04). Include:
        - Header comment explaining Phase 14 (v0.11.0 — POL-01/POL-02), the AFTER-capability-BEFORE-breaker gate position (D-09), and the allow-all default behavior.
        - Hot-reload caveat block citing project memory `project_models_yaml_hot_edit.md`: editing requires `docker compose exec valkey valkey-cli DEL 'model-registry:*'` followed by `docker compose up -d --force-recreate router`. A bare `docker compose restart router` will NOT pick up the new policies (the cached snapshot lives in Valkey).
        - Example global allowlist commented: `# policies:` / `#   default:` / `#     model_allowlist:` / `#       - chat-local` / `#       - vision-local`.
        - Example per-entry `policy.cloud_allowed: false` commented inside an existing cloud entry's neighborhood (or in a standalone comment block — Claude's discretion).

    (2) `.planning/REQUIREMENTS.md`: PATCH the POL-01 wording per D-03. Replace the existing POL-01 line with the patched text shown in `<interfaces>` above. DO NOT alter POL-02..POL-06. After patching, increment the file's "Last updated" line (bottom of file) with date `2026-05-30` and a note like `POL-01 wording patched per D-03 (top-level policies block)`.

    Commit message: `docs(14): operator policy stanza in models.yaml + POL-01 wording patch (D-03)`. Single commit.
  </action>
  <verify>
    <automated>grep -c "# policies:" router/models.yaml | grep -q "^[1-9]" && grep -c "policies.default.model_allowlist" .planning/REQUIREMENTS.md | grep -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "# policies:" router/models.yaml` returns ≥ 1.
    - `grep -c "# *default:" router/models.yaml` returns ≥ 1.
    - `grep -c "# *model_allowlist:" router/models.yaml` returns ≥ 1.
    - `grep -c "# *cloud_allowed:" router/models.yaml` returns ≥ 1.
    - `grep -c "valkey-cli DEL" router/models.yaml` returns ≥ 1 (hot-reload caveat documented).
    - The commented YAML block is parsed unchanged by Zod (no behavior change — Plan 02's RegistrySchema.parse on the current models.yaml still returns `policies === undefined`). Verify by `cd router && pnpm vitest run --reporter=dot` exits 0.
    - `grep -c "policies.default.model_allowlist" .planning/REQUIREMENTS.md` returns ≥ 1 (POL-01 wording patched).
    - `grep -c "POL-01.*per registry entry" .planning/REQUIREMENTS.md` returns 0 (old wording removed).
    - POL-02..06 line counts unchanged (`grep -cE '^- \\[ \\] \\*\\*POL-0[2-6]\\*\\*' .planning/REQUIREMENTS.md` returns 5).
  </acceptance_criteria>
  <done>Operator can read models.yaml and discover the policy stanza affordance; default behavior unchanged; POL-01 wording reflects the locked hybrid shape.</done>
</task>

<task type="auto">
  <name>Task 2: DEPLOY.md + README.md policy primitives section</name>
  <files>DEPLOY.md, README.md</files>
  <read_first>
    - DEPLOY.md (entire file — capture existing section headings + style; find where operator config sections live; identify the right insertion point)
    - README.md (capture existing structure + style; find the right insertion point — typically a configuration / operator-facing section)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"Operator-Visible Affordances → DEPLOY.md / README.md updates" (key points enumerated)
    - project memory `project_models_yaml_hot_edit.md` (hot-reload caveat wording)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md D-01..D-28 (full set of decisions to mention)
  </read_first>
  <action>
    (1) `DEPLOY.md`: add a section "Policy primitives (Phase 14 — v0.11.0)" covering:
        - Top-level `policies.default.model_allowlist: [...]` — semantics (allow-all default), behavior when violated (403 + envelope shape with code `model_not_in_allowlist`).
        - Per-entry `policy.cloud_allowed: false` — semantics, behavior when triggered (403 + code `cloud_not_allowed`), default `true` per D-05.
        - Gate position invariant — fires AFTER capability check, BEFORE circuit breaker (D-09 / P8-01) — policy violations never count as backend failures.
        - Scoped-ID headers: `X-Tenant-ID`, `X-Project-ID`, `X-Workload-Class` — regex shape (`/^[A-Za-z0-9._:-]{1,128}$/` for IDs; `/^[A-Za-z0-9._-]{1,64}$/` lowercased for workload class), validation behavior (400 vs silent-NULL per D-12/D-16), where they appear (request_log columns + pino structured logs, NOT Prometheus labels per D-25).
        - Hot-reload procedure (cross-link to project memory): `docker compose exec valkey valkey-cli DEL 'model-registry:*'` then `docker compose up -d --force-recreate router`. Bare `restart` will NOT pick up policy changes.
        - Migration 0005 — auto-applies via Drizzle migrator on next boot; rollback procedure documented (drop the three new columns + revert journal idx 5).
        - CI cardinality guard — operators editing `src/metrics/registry.ts` must avoid `*_id` labels; the vitest test in `scripts/__tests__/check-prometheus-cardinality.test.ts` fails the build otherwise.

    (2) `README.md`: add a brief "Policy & multi-tenant context" subsection (1-2 paragraphs):
        - One sentence describing the policy stanza shape (link to DEPLOY.md for details).
        - One sentence describing scoped-ID headers (link to DEPLOY.md).
        - One sentence noting cardinality discipline (no `_id` Prometheus labels).
        - Cross-link to `router/models.yaml` for the commented affordance.

    DO NOT touch other DEPLOY.md / README.md sections. Land both files in one commit.
  </action>
  <verify>
    <automated>grep -q "policies.default.model_allowlist" DEPLOY.md && grep -q "X-Tenant-ID" DEPLOY.md && grep -q "valkey-cli DEL" DEPLOY.md && grep -q "X-Tenant-ID" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "policies.default.model_allowlist" DEPLOY.md` returns ≥ 1.
    - `grep -c "cloud_allowed" DEPLOY.md` returns ≥ 1.
    - `grep -c "X-Tenant-ID" DEPLOY.md` returns ≥ 1; `grep -c "X-Project-ID" DEPLOY.md` returns ≥ 1; `grep -c "X-Workload-Class" DEPLOY.md` returns ≥ 1.
    - `grep -c "valkey-cli DEL" DEPLOY.md` returns ≥ 1 (hot-reload caveat documented).
    - `grep -c "check-prometheus-cardinality" DEPLOY.md` returns ≥ 1 (cardinality guard documented).
    - `grep -c "X-Tenant-ID" README.md` returns ≥ 1.
    - `grep -c "policies" README.md` returns ≥ 1.
    - The phrases used MUST cite specific behaviors (allow-all default; 403 envelope; gate position) — no vague language like "configurable policies" without the specifics. Spot-check: open both files and confirm the new sections are at least 12 lines of substantive content each.
  </acceptance_criteria>
  <done>Operator can read DEPLOY.md and configure policy + headers; README.md surfaces the affordance for newcomers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test gate — existing suite passes unchanged with allow-all defaults</name>
  <files>(verification-only — no source file modifications)</files>
  <what-built>
    Full Phase 14 stack:
    - Migration 0005 applied to live Postgres
    - applyPolicyGate wired into all 5 routes
    - scopedIdsPreHandler stamps tenant/project/workload onto req
    - recordOutcome plumbs scoped IDs into request_log rows
    - Cardinality CI guard live
    - models.yaml + DEPLOY/README updated
    All with default (zero) policy config — allow-all, cloud_allowed=true. Existing smoke test suite MUST pass unchanged.
  </what-built>
  <how-to-verify>
    Run the existing smoke test against the live stack from the host:

    1. Confirm router is up: `docker compose ps router` shows the service is running.
    2. Confirm migration 0005 is applied: `docker compose exec -T postgres psql -U app -d router -c "\\d request_log"` lists tenant_id, project_id, workload_class columns.
    3. Confirm full vitest suite passes: `cd router && pnpm vitest run --reporter=dot` exits 0.
    4. Confirm TypeScript clean: `cd router && pnpm typecheck` exits 0.
    5. Run existing smoke test: `bash bin/smoke-test-router.sh` — every PASS line still PASSES; no new FAIL lines introduced.
    6. (Sanity) Send a manual happy-path request through the public surface to confirm end-to-end:
       `curl -sS -X POST http://localhost:3210/v1/chat/completions -H "Authorization: Bearer $LOCAL_LLMS_TOKEN" -H "X-Tenant-ID: acme" -H "X-Project-ID: agents" -H "X-Workload-Class: dev" -H "Content-Type: application/json" -d '{"model":"chat-local","messages":[{"role":"user","content":"reply with the single word: ok"}]}'` returns a 200 with a normal completion body.
    7. (Sanity) Inspect the row written: `docker compose exec -T postgres psql -U app -d router -c "SELECT tenant_id, project_id, workload_class FROM request_log ORDER BY ts DESC LIMIT 1"` shows `acme | agents | dev`.

    Expected outcome: all 7 checks succeed; no surprises. Failure of any check is a blocker for `/gsd:verify-work`.
  </how-to-verify>
  <action>This is a human-verification checkpoint. The executor runs the 7 verification steps in `<how-to-verify>` IN ORDER against the live local stack and reports back the outcome of each. No code changes are made by this task — its job is to gate phase completion on a clean regression result. The executor must capture: docker compose ps output, the `\d request_log` columns line, vitest reporter summary, typecheck exit code, smoke-test PASS/FAIL counts (before vs after this phase if recorded in 14-05-SUMMARY), the curl response body + status, and the SELECT row from request_log.</action>
  <verify>
    <human-check>All 7 steps in `<how-to-verify>` pass: docker compose ps shows router healthy; psql lists the three new columns; pnpm vitest run exits 0; pnpm typecheck exits 0; bin/smoke-test-router.sh has zero new FAIL lines; the curl request returns 200 with a normal completion body; and the SELECT row from request_log shows `acme | agents | dev`.</human-check>
  </verify>
  <done>User has typed "approved" after personally confirming all 7 verification steps pass against the live local stack.</done>
  <resume-signal>Type "approved" if all 7 checks pass; otherwise describe which check failed and what was observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-facing docs → operator behavior | Misleading docs could cause the hot-reload caveat to be missed → operator thinks edits aren't applying |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09-01 | Information Disclosure (operator confusion) | Docs omit the Valkey-cache hot-reload caveat → operator thinks `restart` is sufficient | mitigate | DEPLOY.md MUST contain `valkey-cli DEL` instruction (grep gate); same caveat repeated in models.yaml comment block |
| T-14-09-02 | Tampering | Doc-drift between docs and actual gate behavior | accept | Phase 14 is a one-shot landing; future changes update docs together with code via GSD workflow |
| T-14-09-03 | Denial of Service (operator misconfig) | Operator removes the cardinality CI guard "to add a tenant_id label for the dashboard" | mitigate | DEPLOY.md explicitly documents that `*_id` labels are forbidden + cites P8-03 cardinality math |
</threat_model>

<verification>
- grep gates per `<acceptance_criteria>` on each task
- vitest full suite: `cd router && pnpm vitest run --reporter=dot`
- typecheck: `cd router && pnpm typecheck`
- smoke: `bash bin/smoke-test-router.sh`
- Live DB column check: `docker compose exec -T postgres psql -U app -d router -c "\\d request_log"`
- Manual end-to-end curl check (Task 3 step 6-7)
</verification>

<success_criteria>
- models.yaml has commented policy affordance + hot-reload caveat
- DEPLOY.md + README.md have substantive policy sections
- POL-01 wording patched in REQUIREMENTS.md per D-03
- Existing smoke test suite passes unchanged (POL SC5)
- Manual end-to-end curl exercises the new headers and the row is populated
</success_criteria>

<output>
Create `.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-09-SUMMARY.md` when done. Include: the smoke-test PASS line count comparison (before/after), the manual curl response status + the `SELECT tenant_id...` row content, and a one-line "POL-01 wording patched per D-03" confirmation.
</output>
