---
phase: 14-policy-primitives-tenant-project-id-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
  - 14-04
files_modified:
  - router/src/routes/v1/chat-completions.ts
  - router/src/routes/v1/messages.ts
  - router/src/routes/v1/embeddings.ts
  - router/src/routes/v1/rerank.ts
  - router/src/routes/v1/responses.ts
  - router/src/routes/__tests__/policy-gate-integration.test.ts
  - router/src/app.ts
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-04
  - POL-05
tags:
  - routes
  - integration
  - migration-apply
must_haves:
  truths:
    - "All 5 routes call `applyPolicyGate(snapshot.policies, entry, body.model)` at the canonical position: AFTER capability gate, BEFORE `opts.breaker.check(entry.backend)` (D-09 / P8-01 BLOCK)"
    - "After a policy 403, the circuit breaker counter is unchanged (POL-05 — verified by integration test with breaker spy)"
    - "No outbound request is emitted to ollama-cloud when CloudNotAllowedError fires (POL-02 — MSW handler asserts zero calls)"
    - "Migration 0005 is applied to live Postgres BEFORE integration tests run (BLOCKING — schema TypeScript passes don't prove DB applied)"
    - "BuildAppOpts gains optional `breaker` seam for test injection without affecting production path"
  artifacts:
    - path: "router/src/routes/v1/chat-completions.ts"
      provides: "applyPolicyGate called between capability gate and breaker.check"
      contains: "applyPolicyGate(snapshot.policies"
    - path: "router/src/routes/v1/messages.ts"
      provides: "Same insertion at the same canonical position"
      contains: "applyPolicyGate(snapshot.policies"
    - path: "router/src/routes/v1/embeddings.ts"
      provides: "Same insertion"
      contains: "applyPolicyGate(snapshot.policies"
    - path: "router/src/routes/v1/rerank.ts"
      provides: "Same insertion"
      contains: "applyPolicyGate(snapshot.policies"
    - path: "router/src/routes/v1/responses.ts"
      provides: "Same insertion"
      contains: "applyPolicyGate(snapshot.policies"
    - path: "router/src/routes/__tests__/policy-gate-integration.test.ts"
      provides: "4 scenarios — allowlist 403 + breaker counter unchanged; cloud-not-allowed 403 + no MSW call; valid 200; absent policies = allow-all"
    - path: "router/src/app.ts"
      provides: "Optional `breaker?: CircuitBreaker` field in BuildAppOpts for test injection"
      contains: "breaker?:"
  key_links:
    - from: "router/src/routes/v1/*.ts (×5)"
      to: "router/src/policy/gate.ts"
      via: "Inline call after capability gate, before breaker.check"
      pattern: "applyPolicyGate\\(snapshot\\.policies"
---

<objective>
Wire `applyPolicyGate()` into all 5 routes at the EXACT same canonical position — right after the existing capability gate, right before `opts.breaker.check(entry.backend)`. Add an optional `breaker` injection seam to `BuildAppOpts` so the integration test can spy on `recordFailure` and prove POL-05 (breaker counter unchanged after a policy 403). Apply migration 0005 to the live Postgres before the integration test runs DB-touching code paths.

Purpose: Materialize POL-01 + POL-02 at the request boundary on every route. The position is non-negotiable (P8-01 BLOCK) — policy violations MUST NOT count as backend failures. Adding the breaker injection seam keeps production code untouched (optional field with existing default) while allowing the test to assert the invariant.

Output: Five route patches (each ~5 lines), one app.ts patch (breaker seam + comment), one integration test with breaker spy + MSW handler, and the `[BLOCKING]` migration apply task that activates the new DB columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-01-SUMMARY.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-02-SUMMARY.md
@.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-04-SUMMARY.md

<interfaces>
<!-- Canonical insertion anchor — chat-completions.ts:196-218 (capability gate) and :324 (breaker.check) -->
<!-- Insert applyPolicyGate(snapshot.policies, entry, body.model) BETWEEN these two -->

From router/src/policy/gate.ts (Plan 04):
  export function applyPolicyGate(policies, entry, requested_model): void;

From router/src/app.ts (existing BuildAppOpts shape — lines 117-121):
  agentIdPreHandler?: preHandlerAsyncHookHandler;
  // <-- new optional `breaker?: CircuitBreaker` field inserts here

From router/src/resilience/circuitBreaker.ts (assumed canonical CircuitBreaker shape):
  interface CircuitBreaker {
    check(backend: string): Promise<{state: 'open'|'closed'|'half-open'}>;
    recordFailure(backend: string): void;
    recordSuccess(backend: string): void;
    reset?(backend: string): void;
  }

<!-- Test seam preference (RESEARCH.md POL-05 pattern) -->
<!-- Add `breaker?: CircuitBreaker` to BuildAppOpts; gate construction as
     `opts.breaker ?? (existing-construction-fallback)` so production untouched -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert applyPolicyGate into all 5 routes + add breaker injection seam to BuildAppOpts</name>
  <files>router/src/routes/v1/chat-completions.ts, router/src/routes/v1/messages.ts, router/src/routes/v1/embeddings.ts, router/src/routes/v1/rerank.ts, router/src/routes/v1/responses.ts, router/src/app.ts</files>
  <read_first>
    - router/src/routes/v1/chat-completions.ts (lines 180–340 — confirm capability-gate block at 196-218 and breaker.check at 324; note exact `snapshot = opts.registry.get()` syntax expected)
    - router/src/routes/v1/messages.ts (find the analogous capability gate + breaker.check block; capture exact line numbers)
    - router/src/routes/v1/embeddings.ts (same — only one capability check applies to embeddings; insertion point still BEFORE breaker.check)
    - router/src/routes/v1/rerank.ts (same)
    - router/src/routes/v1/responses.ts (same)
    - router/src/app.ts (lines 117-121 for BuildAppOpts shape; line 542 area for breaker construction)
    - router/src/policy/gate.ts (signature confirmation from Plan 04)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"Route-side insertion (chat-completions example)" + §"Validation Architecture POL-05" (breaker spy pattern + BuildAppOpts seam)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-PATTERNS.md (lines 506-577 for the 13 grep sites map and the exact insertion anchor)
  </read_first>
  <behavior>
    No behavior block — this task is a structural patch driven by acceptance criteria. Behavior tests live in Task 2 (integration tests) where they can drive a real Fastify app with the breaker spy.
  </behavior>
  <action>
    Wire `applyPolicyGate()` into every model-bound route at the canonical position locked by D-09 (P8-01 BLOCK).

    (1) In each of the 5 route files (`chat-completions.ts`, `messages.ts`, `embeddings.ts`, `rerank.ts`, `responses.ts`):
        - Import `applyPolicyGate` from `'../../policy/gate.js'` (ESM `.js` suffix).
        - Find the insertion anchor: the LAST capability-gate `throw new CapabilityNotSupportedError(...)` (or equivalent) before the `try { ... await opts.breaker.check(entry.backend) ... }` block. In chat-completions.ts this is between the `wantsJson && !entry.capabilities.includes('json_mode')` throw (~line 217) and the `try {` at ~line 318. Embeddings has only one capability gate; rerank similarly; insertion is just BEFORE breaker.check in any case.
        - Insert two lines (plus comment):
          ```
          const snapshot = opts.registry.get();
          applyPolicyGate(snapshot.policies, entry, body.model);
          ```
          Inline comment: `// Phase 14 (v0.11.0 — POL-01/POL-02 — D-09/P8-01 BLOCK): policy gate fires AFTER capability gate, BEFORE the breaker check — policy 403s never mutate the breaker counter.`
        - DO NOT touch any other lines in these route files in this plan. recordOutcome plumbing (tenantId/projectId/workloadClass through safeRecord) belongs to Plan 06's wave because it depends on scopedIdsPreHandler stamping `req.tenantId` etc.

    (2) In `router/src/app.ts`:
        - Add an optional `breaker?: CircuitBreaker` field to the `BuildAppOpts` interface adjacent to the existing `agentIdPreHandler?` (line ~117-121). Add a doc comment: "Test seam — production path uses the breaker constructed from opts.valkey + opts.env. Tests inject a spied wrapper to assert POL-05 (breaker counter unchanged after policy 403)."
        - In the breaker construction site (around line 542), gate the existing constructor: `const breaker = opts.breaker ?? <existing construction expression>`. Production path untouched (no `opts.breaker` passed → fallback to existing construction). DO NOT change the type of `breaker` downstream — keep the symbol the 5 routes already receive.

    (3) DO NOT touch `scopedIdsPreHandler` registration in app.ts (that belongs to Plan 06).
    (4) DO NOT touch `recordOutcome` calls (that belongs to Plan 06 + Plan 07 / DB-row integration).
  </action>
  <verify>
    <automated>cd router && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyPolicyGate(snapshot.policies, entry, body.model)" router/src/routes/v1/chat-completions.ts` outputs `1`.
    - `grep -c "applyPolicyGate(snapshot.policies, entry, body.model)" router/src/routes/v1/messages.ts` outputs `1`.
    - `grep -c "applyPolicyGate(snapshot.policies, entry, body.model)" router/src/routes/v1/embeddings.ts` outputs `1`.
    - `grep -c "applyPolicyGate(snapshot.policies, entry, body.model)" router/src/routes/v1/rerank.ts` outputs `1`.
    - `grep -c "applyPolicyGate(snapshot.policies, entry, body.model)" router/src/routes/v1/responses.ts` outputs `1`.
    - In every patched route, `grep -nE "applyPolicyGate|opts\\.breaker\\.check" router/src/routes/v1/<file>.ts` shows applyPolicyGate appears on a line BEFORE the `opts.breaker.check` line (numerically lower line number).
    - In every patched route, `grep -nE "CapabilityNotSupportedError|applyPolicyGate" router/src/routes/v1/<file>.ts` shows the LAST CapabilityNotSupportedError throw appears on a line BEFORE applyPolicyGate.
    - `grep -c "from '../../policy/gate.js'" router/src/routes/v1/chat-completions.ts` outputs `1` (and similarly for the other 4 routes).
    - `grep -c "breaker?:" router/src/app.ts` returns ≥ 1 (the new optional BuildAppOpts field).
    - `cd router && pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>5 route insertions complete at canonical positions, BuildAppOpts breaker seam landed, TypeScript clean. Behavior tests in Task 2 will exercise the wiring.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration 0005 to live Postgres + verify columns present</name>
  <files>(no source files modified — runtime DB apply only)</files>
  <read_first>
    - router/db/migrations/0005_request_log_scoped_ids.sql (the migration tuple landed in Plan 01)
    - router/db/migrations/meta/_journal.json (confirm idx=5 entry present)
    - router/src/db/schema/request_log.ts (confirm Drizzle schema has the 3 new columns)
    - router/package.json scripts section (locate `drizzle-kit migrate` or equivalent script; check for a `db:migrate` script)
    - DEPLOY.md or README.md (confirm how migrations apply — auto on `docker compose up` vs explicit drizzle-kit run)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-CONTEXT.md D-21..D-24 (migration discipline)
  </read_first>
  <action>
    [BLOCKING] Apply migration 0005 to the live Postgres. Schema TypeScript types passed in Plan 01 — but those types come from the Drizzle schema file, not the live DB. Without this apply, Plan 06/07 integration tests that insert request_log rows will fail at runtime with `column "tenant_id" does not exist`.

    (1) Verify the migration tuple is intact (atomicity guard from project memory `project_drizzle_migration_journal.md`). All three MUST be true BEFORE apply:
        - `test -f router/db/migrations/0005_request_log_scoped_ids.sql`
        - `grep -c '0005_request_log_scoped_ids' router/db/migrations/meta/_journal.json` returns 1
        - `grep -c "tenant_id: text('tenant_id')" router/src/db/schema/request_log.ts` returns 1

    (2) Apply the migration. Preferred command: `cd router && pnpm drizzle-kit migrate` (uses `_journal.json` ordering). Fallback: invoke the in-router migration runner if exposed via a CLI script; or restart the compose `router` service in a way that triggers boot-time migration if that path is wired (verify against existing 0004 apply method recorded in DEPLOY.md / a prior phase SUMMARY). If neither path is available, run the SQL directly via `docker compose exec -T postgres psql -U <user> -d <db> < router/db/migrations/0005_request_log_scoped_ids.sql` AND THEN re-run `pnpm drizzle-kit migrate` so the journal idx=5 entry is registered with the runner.

    (3) Verify columns are live:
        - `docker compose exec -T postgres psql -U <user> -d <db> -c "\\d request_log"` output MUST list `tenant_id text`, `project_id text`, `workload_class text`.

    (4) Record the exact apply command used (which of the three branches above) in the SUMMARY for downstream phase reference.
  </action>
  <verify>
    <automated>docker compose exec -T postgres psql -U app -d router -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='request_log' AND column_name IN ('tenant_id','project_id','workload_class') ORDER BY column_name" | tr '\n' ',' | grep -q "project_id,tenant_id,workload_class,"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 — the three columns are confirmed present in live Postgres `request_log` table.
    - `docker compose exec -T postgres psql -U app -d router -c "\\d request_log"` output (captured for SUMMARY) lists all three columns as nullable text.
    - The exact apply command used is recorded in SUMMARY.
    - If `pnpm drizzle-kit migrate` was used: the command exit code was 0 AND its stdout indicated "0005_request_log_scoped_ids" was applied (vs already-present).
    - No other `request_log` columns were modified or dropped (regression check: `\d request_log` still shows pre-existing columns including `agent_id`, `cost_cents`, `idempotency_key`).
  </acceptance_criteria>
  <done>Live Postgres `request_log` table has tenant_id, project_id, workload_class columns; downstream integration tests can drive INSERTs through `recordOutcome` (Plan 06/07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — policy gate across all 5 routes + breaker spy + MSW cloud-call assertion</name>
  <files>router/src/routes/__tests__/policy-gate-integration.test.ts</files>
  <read_first>
    - router/tests/integration/chat-completions.nonstream.test.ts (MSW handler pattern + buildApp helper usage)
    - router/tests/integration/recordOutcome.test.ts:149-151 (bufferedWriter spy pattern — captures pushed rows)
    - router/tests/resilience/circuitBreaker.test.ts:211-264 (breaker check/recordFailure interaction pattern)
    - router/src/app.ts (buildApp / BuildAppOpts shape — including the new `breaker?` field added in Task 1)
    - router/tests/msw/handlers.ts (existing MSW setup for upstream backends)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-RESEARCH.md §"Validation Architecture" (POL-05 breaker-spy pattern + POL-02 MSW assertion)
    - .planning/phases/14-policy-primitives-tenant-project-id-foundation/14-VALIDATION.md row 14-05-01
  </read_first>
  <behavior>
    - Test 1 (POL-01 — allowlist 403): Build app with registry containing `policies.default.model_allowlist: ['chat-local']` and a model entry `big-cloud` (backend ollama-cloud). POST `/v1/chat/completions` with `model: 'big-cloud'`. Assert: status 403; body.error.code === 'model_not_in_allowlist'; body.error.type === 'policy_violation'.
    - Test 2 (POL-05 — breaker counter unchanged): Same scenario as Test 1, but inject a breaker spy via `opts.breaker = { check: vi.fn().mockResolvedValue({state:'closed'}), recordFailure: vi.fn(), recordSuccess: vi.fn(), reset: vi.fn() }`. After the 403 response, assert `recordFailure` was called 0 times AND `check` was called 0 times (policy gate ran BEFORE the breaker code path).
    - Test 3 (POL-02 — no outbound cloud call): Build app with a model entry `big-cloud` whose `policy.cloud_allowed: false` and an MSW handler at `https://ollama.com/v1/chat/completions` that records every Request hit. POST `/v1/chat/completions` with `model: 'big-cloud'`. Assert: status 403; body.error.code === 'cloud_not_allowed'; the MSW handler captured ZERO requests.
    - Test 4 (allow-all default): Build app with registry that has NO `policies:` section at all. POST `/v1/chat/completions` with `model: 'big-cloud'` (a cloud entry with `policy: undefined`). MSW returns a canned 200 response from cloud. Assert: status 200 (allow-all default preserves existing behavior — smoke regression).
    - Test 5 (POL-01 on Anthropic surface — wire-shape parity): Build same allowlist-violating scenario, but POST to `/v1/messages`. Assert: status 403; body.type === 'error'; body.error.type === 'permission_error'; body.error.message contains the model name.
    - Test 6 (POL-02 on `/v1/embeddings`): Same cloud-not-allowed scenario but POST to `/v1/embeddings`. Assert: 403 + cloud_not_allowed envelope.
    - Test 7 (POL-02 on `/v1/rerank`): Same, POST to `/v1/rerank`. Assert: 403 + cloud_not_allowed envelope.
    - Test 8 (POL-02 on `/v1/responses`): Same, POST to `/v1/responses`. Assert: 403 + cloud_not_allowed envelope.
  </behavior>
  <action>
    Create `router/src/routes/__tests__/policy-gate-integration.test.ts` — vitest integration test that drives `buildApp(opts)` with the policy-extended registry from Plan 02.

    (1) Test helpers (mirror `chat-completions.nonstream.test.ts` structure):
        - `buildPolicyApp({ allowlist, cloudEntries, breakerSpy })` — wraps `buildApp(opts)` with a registry containing the given policy shape. Returns the Fastify instance.
        - MSW server lifecycle via vitest `beforeAll/afterAll` (mirror existing test).

    (2) Encode the 8 behaviors above. Use `app.inject({ method: 'POST', url, headers: {authorization: 'Bearer <token>'}, payload })` for each.

    (3) For Test 2 (breaker spy): construct the spy literal with `vi.fn()` for each of `check`, `recordFailure`, `recordSuccess`, `reset`. Pass via `opts.breaker`. Assert `recordFailure` and `check` were never called after the 403.

    (4) For Test 3 (no outbound cloud call): use `mswServer.use(http.post('https://ollama.com/v1/chat/completions', (info) => { cloudCalls.push(info.request); return HttpResponse.json({}, {status:500}); }))` then assert `cloudCalls.length === 0` after a 403.

    (5) For Tests 5-8 (cross-route parity): drive the same policy violation through each surface (`/v1/messages`, `/v1/embeddings`, `/v1/rerank`, `/v1/responses`); assert each returns the correct wire shape per its surface.

    Use Plan 02's `RegistrySchema.parse(...)` (via `loadRegistryFromString` or equivalent existing helper) to build the test registries — do NOT bypass schema validation in the test fixtures (the schema MUST accept these inputs as part of Plan 02's contract).
  </action>
  <verify>
    <automated>cd router && pnpm vitest run src/routes/__tests__/policy-gate-integration.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && pnpm vitest run src/routes/__tests__/policy-gate-integration.test.ts --reporter=dot` exits 0 with 8 passing assertions.
    - Test 2 assertion `expect(recordFailure).toHaveBeenCalledTimes(0)` passes (POL-05 invariant).
    - Test 3 assertion `expect(cloudCalls.length).toBe(0)` passes (POL-02 — no leaked cloud request).
    - Test 4 returns 200 (allow-all default — regression guard for D-04).
    - All 4 routes /v1/chat/completions, /v1/messages, /v1/embeddings, /v1/rerank, /v1/responses return 403 in their respective scenarios with the correct envelope shape for each wire surface.
    - `cd router && pnpm typecheck` exits 0.
    - The full vitest suite still runs green: `cd router && pnpm vitest run --reporter=dot` exits 0 (regression check).
  </acceptance_criteria>
  <done>Policy gate is wired in all 5 routes at the canonical position, breaker counter invariant is asserted (POL-05), no outbound cloud calls leak on cloud-not-allowed, and allow-all default preserves existing behavior — all proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route handler → applyPolicyGate (trusted) → breaker.check (trusted) | Gate-position invariant — see T-14-03 below |
| Inbound request → /v1/messages on Anthropic surface | Dual-envelope dispatch must produce permission_error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | Gate position regression (P8-01 BLOCK) — moving applyPolicyGate after breaker.check would count policy 403s as backend failures and trip the breaker (DoS) | mitigate | Task 1 inserts at canonical position with inline comment citing D-09/P8-01. Task 3 Test 2 asserts breaker `recordFailure` called 0 times after policy 403 (regression test). |
| T-14-02 | Tampering | Per-request cloud-allowed override | mitigate | Policy lives only in models.yaml (Plan 02 schema). Route Zod schemas remain `.strict()` (unchanged). Test 3 confirms no outbound cloud request fired. |
| T-14-01 | Tampering | Allowlist bypass | mitigate | Test 1 + Test 5 verify allowlist enforcement on both wire surfaces. |
| T-14-MIG-LIVE | Tampering | Migration not applied to live DB → row INSERTs fail at runtime even though TypeScript passes | mitigate | Task 2 is BLOCKING — explicitly applies 0005 to live Postgres AND verifies via `\d request_log`. |
</threat_model>

<verification>
- vitest: `cd router && pnpm vitest run src/routes/__tests__/policy-gate-integration.test.ts --reporter=dot`
- typecheck: `cd router && pnpm typecheck`
- DB schema: `docker compose exec -T postgres psql -U app -d router -c "\\d request_log"` shows new columns
- Grep gates per `<acceptance_criteria>`
</verification>

<success_criteria>
- 5 route insertions at canonical position
- BuildAppOpts gains optional `breaker?` field
- Migration 0005 applied to live Postgres
- 8 integration tests green (POL-01/POL-02/POL-05 + cross-route parity + allow-all regression)
- Breaker counter invariant proven (POL-05)
- No outbound cloud requests on cloud-not-allowed (POL-02)
</success_criteria>

<output>
Create `.planning/phases/14-policy-primitives-tenant-project-id-foundation/14-05-SUMMARY.md` when done. Include: per-route exact line numbers where applyPolicyGate was inserted, the apply command used for migration 0005, the `\d request_log` output snippet, and the 8 integration-test pass list.
</output>
