---
phase: 05-postgres-observability-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yml
  - .env.example
  - router/package.json
  - router/src/config/env.ts
  - router/src/index.ts
  - router/src/app.ts
  - router/drizzle.config.ts                    # NEW
  - router/src/db/index.ts                      # NEW
  - router/src/db/migrate.ts                    # NEW
  - router/src/db/schema/request_log.ts         # NEW
  - router/src/db/schema/usage_daily.ts         # NEW
  - router/src/db/schema/index.ts               # NEW
  - router/src/db/bufferedWriter.ts             # NEW
  - router/db/migrations/0001_init.sql          # NEW (drizzle-kit output)
  - postgres/initdb/01-init.sql                 # NEW
  - router/tests/unit/bufferedWriter.test.ts    # NEW
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, OBS-05]
tags: [postgres, drizzle, buffered-writer, compose, foundation]

must_haves:
  truths:
    - "docker compose up postgres reaches `healthy` state on the data network with NO host port published (D-E1, D-E2, D-E3)"
    - "Two logical databases `router` and `openwebui` exist with the `app` role owning both, created by postgres/initdb/01-init.sql on first init only (D-B3)"
    - "`pgcrypto` extension is installed in the `router` database (D-B8) — `SELECT gen_random_uuid()` succeeds"
    - "Router boots even when postgres is unreachable (D-B5) — `migrate()` warns-and-continues on ECONNREFUSED/ETIMEDOUT/08*; fails loud only on schema-syntax errors (RESEARCH Pitfall 4)"
    - "After router boots against a healthy postgres, `router.request_log` and `router.usage_daily` tables exist with the D-D1 column set + the three baseline btree indexes (`ts DESC`, `(agent_id, ts DESC)`, `status_class`)"
    - "BufferedWriter is constructed at boot and wired into BuildAppOpts, with a `flushing: boolean` re-entrancy lock (RESEARCH Pitfall 1), 1s `setInterval` + 200-row queueMicrotask-deferred flush (D-A2), drop-oldest at cap=10_000 (D-A1), `pg.Pool` shared, and a 3s SIGTERM drain wired into `app.addHook('onClose', ...)` (D-A4)"
    - "The shared `pg.Pool` is created with `max: 8`, `idleTimeoutMillis: 30_000`, and `connectionTimeoutMillis: 2_000` (RESEARCH Pitfall 3 — NOT the default of 0)"
  artifacts:
    - path: "compose.yml"
      provides: "postgres:17-alpine service on data network + pg_isready healthcheck + bind-mounted initdb + bind-mounted postgres-data + router env vars ROUTER_DATABASE_URL / OPENWEBUI_DATABASE_URL + router service joins `data` network + depends_on postgres required:false"
      contains: "postgres:17-alpine"
    - path: "postgres/initdb/01-init.sql"
      provides: "CREATE USER app + CREATE DATABASE router/openwebui + GRANT + CREATE EXTENSION pgcrypto"
      contains: "CREATE EXTENSION IF NOT EXISTS pgcrypto"
    - path: "router/drizzle.config.ts"
      provides: "Drizzle Kit config — dialect: postgresql, schema dir, out dir, dbCredentials from ROUTER_DATABASE_URL"
    - path: "router/src/db/schema/request_log.ts"
      provides: "Drizzle pgTable for request_log per D-D1 column set + indexes"
      contains: "export const requestLog"
    - path: "router/src/db/schema/usage_daily.ts"
      provides: "Drizzle pgTable for usage_daily per CONTEXT specifics §usage_daily suggested shape (lines 288–306) with COALESCE-able non-null agent_id"
      contains: "export const usageDaily"
    - path: "router/src/db/bufferedWriter.ts"
      provides: "makeBufferedWriter({db, droppedCounter, logger, capacity?, flushIntervalMs?, flushAtRows?}) → { push, drain(ms), size } with flushing-lock + drop-oldest + microtask-deferred flush + 3s drain race"
      contains: "flushing"
    - path: "router/src/db/index.ts"
      provides: "makePool(url) → pg.Pool with connectionTimeoutMillis: 2000; makeDb(pool) → NodePgDatabase"
      contains: "connectionTimeoutMillis"
    - path: "router/src/db/migrate.ts"
      provides: "runMigrations(db, log) — wraps drizzle migrate() with selective try/catch (Pitfall 4 — warn on 08*/ECONNREFUSED/ETIMEDOUT/ENOTFOUND, throw on schema-class)"
      contains: "migrate_postgres_unreachable"
    - path: "router/tests/unit/bufferedWriter.test.ts"
      provides: "vitest unit tests with vi.useFakeTimers() — covers 1s flush interval, 200-row microtask flush, drop-oldest+counter, drain timeout race, flush-error rows-stay-in-buffer (D-A7), re-entrancy lock"
    - path: "router/db/migrations/0001_init.sql"
      provides: "drizzle-kit generate output committed to git — CREATE TABLE request_log + CREATE TABLE usage_daily + indexes"
  key_links:
    - from: "router/src/index.ts main()"
      to: "router/src/db/index.ts makePool / router/src/db/migrate.ts runMigrations / router/src/db/bufferedWriter.ts makeBufferedWriter"
      via: "boot wiring before buildApp({...bufferedWriter})"
      pattern: "await runMigrations\\(db, app\\.log\\)"
    - from: "router/src/app.ts onClose hook"
      to: "router/src/db/bufferedWriter.ts drain(3_000)"
      via: "extension of existing onClose hook (currently calls liveness.stop / probeAdapters.clear / semaphoreMap.clear)"
      pattern: "bufferedWriter\\.drain\\(3_?000\\)"
    - from: "compose.yml router service"
      to: "compose.yml postgres service"
      via: "depends_on: postgres: { condition: service_healthy, required: false } AND ROUTER_DATABASE_URL env"
      pattern: "ROUTER_DATABASE_URL=postgresql://app"
---

<objective>
Stand up the Postgres+Drizzle foundation and the unblocked, unit-tested buffered writer. After this plan, `docker compose up postgres router` boots clean, migrations apply at boot, the `request_log` and `usage_daily` tables exist in the `router` database, and the `BufferedWriter` exists in-process — but route call sites that produce rows land in 05-02. This plan is intentionally infrastructure-heavy and route-quiet so wave 2 can pour metrics + recordOutcome + agent-id wiring through `app.ts` and the two route files without contending with file conflicts.

Purpose: every SC in Phase 5 rests on this foundation. SC1 needs the dual-DB compose service. SC2 needs the bufferedWriter design (D-A1..D-A7). SC5 needs the postgres healthcheck. All paths through the request_log column set start from the Drizzle schema declared here.

Output: a runnable postgres service, a router that survives postgres-down at boot, and a bufferedWriter that has been unit-tested against every D-A invariant + RESEARCH Pitfalls 1, 3, 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-postgres-observability-seam/05-CONTEXT.md
@.planning/phases/05-postgres-observability-seam/05-RESEARCH.md
@.planning/phases/05-postgres-observability-seam/05-PATTERNS.md
@.planning/phases/05-postgres-observability-seam/05-VALIDATION.md
@.planning/phases/01-gpu-compose-foundation/01-CONTEXT.md
@.planning/phases/02-mvp-vertical-slice-router-ollama-sse/02-CONTEXT.md
@.planning/phases/03-multi-backend-dispatch-llama-cpp-registry-hardening/03-CONTEXT.md
@CLAUDE.md
@compose.yml
@router/src/app.ts
@router/src/index.ts
@router/src/config/env.ts
@router/src/concurrency/semaphore.ts
@router/src/backends/liveness.ts
@router/package.json
@.env.example

<interfaces>
<!-- Extracted from the existing codebase. Executor must use these contracts verbatim. -->

From router/src/concurrency/semaphore.ts (idempotent release closure pattern — the bufferedWriter `drain` must mirror this):
```typescript
const buildRelease = (): (() => void) => {
  let released = false;
  return () => {
    if (released) return;
    released = true;
    this.inFlight--;
    this.drain();
  };
};
```

From router/src/backends/liveness.ts (in-flight guard + stopped flag + Map factory — the bufferedWriter mirrors this shape but with `flushing: boolean` instead of `inFlight: Set` and `buf: Array` instead of `cache: Map`):
```typescript
const timers = new Map<string, NodeJS.Timeout>();
const cache = new Map<string, ProbeResult>();
const inFlight = new Set<string>();
let stopped = false;
// ...
stop() {
  if (stopped) return;     // idempotent
  stopped = true;
  for (const [, timer] of timers) clearInterval(timer);
  timers.clear();
}
```

From router/src/app.ts (existing BuildAppOpts + onClose hook — extension points):
```typescript
export interface BuildAppOpts {
  registry: RegistryStore;
  bearerToken: string;
  loggerOpts?: FastifyServerOptions['logger'];
  makeAdapter?: AdapterFactory;
  livenessFactory?: ...;
  semaphoreFactory?: ...;
  semaphores?: { get(backend: string): BackendSemaphore };
}

app.addHook('onClose', async () => {
  liveness.stop();
  probeAdapters.clear();
  semaphoreMap.clear();
});
```

From router/src/config/env.ts (the EnvSchema this plan extends — add `ROUTER_DATABASE_URL` as a required string here):
```typescript
const EnvSchema = z.object({
  ROUTER_BEARER_TOKEN: z.string().min(8, ...),
  OLLAMA_URL: z.string().url().default('http://ollama:11434/v1'),
  PORT: z.coerce.number().int().positive().default(3000),
  LOG_LEVEL: z.enum([...]).default('info'),
  NODE_ENV: z.enum([...]).default('production'),
  MODELS_YAML_PATH: z.string().default('/app/models.yaml'),
});
```

From .env.example (the POSTGRES_PASSWORD slot already reserved by Phase 1 — line 35):
```
POSTGRES_PASSWORD=
```

From compose.yml (ollama service block — the structural analog for the new postgres service: pinned image tag, container_name, restart, healthcheck with CMD-SHELL + interval/timeout/start_period/retries, internal-only network membership):
```yaml
ollama:
  image: ollama/ollama:0.23.4
  container_name: ${COMPOSE_PROJECT_NAME:-local-llms}-ollama
  restart: unless-stopped
  # ...
  healthcheck:
    test: ["CMD-SHELL", "ollama list >/dev/null 2>&1 || exit 1"]
    interval: 10s
    timeout: 3s
    start_period: 30s
    retries: 5
```

From compose.yml router service (depends_on with `required: false` pattern — Phase 5 adds postgres entry mirroring this):
```yaml
depends_on:
  ollama:
    condition: service_healthy
    required: false
  llamacpp:
    condition: service_healthy
    required: false
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose postgres service, bootstrap SQL, router env + network wiring</name>
  <files>compose.yml, postgres/initdb/01-init.sql, .env.example</files>
  <read_first>
    - compose.yml (entire file — understand the existing ollama service block at lines 87–161 which is the structural analog per PATTERNS.md §compose.yml; understand router service block at 209–248 — the modifications append to its environment, networks, depends_on; note the `networks:` declaration block at lines 37–48 which already declares `data: internal: true`)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §"Compose / networking placement of Postgres" (D-E1, D-E2, D-E3, D-E4)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Code Examples — Compose snippet" (lines 608–679) — literal compose YAML to copy
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Pitfall 6" (initdb is first-init-only) and §"Pitfall 7" (Alpine postgres uid)
    - .planning/phases/05-postgres-observability-seam/05-PATTERNS.md §"compose.yml" (lines 720–765) — analog reference for healthcheck shape
    - .planning/phases/01-gpu-compose-foundation/01-CONTEXT.md (HOST_DATA_ROOT bind-mount convention)
    - .env.example (full file — confirm POSTGRES_PASSWORD slot at line ~35; add ONLY a comment line above it)
  </read_first>
  <behavior>
    - `docker compose config` succeeds (YAML valid) AND shows the new postgres service joined only to `data` network with no `ports:` entry
    - `docker compose up postgres` reaches `healthy` state within 30s start_period (RESEARCH §Code Examples)
    - `docker compose ps postgres --format '{{.Health}}'` outputs `healthy`
    - `docker compose exec postgres psql -U app -l` lists BOTH `router` AND `openwebui` databases (DATA-01)
    - `docker compose exec postgres psql -U app -d router -c "SELECT extname FROM pg_extension"` lists `pgcrypto` (D-B8)
    - `docker compose exec postgres psql -U app -d router -c "SELECT gen_random_uuid()"` returns a UUID (proves pgcrypto loaded)
    - Router service's `environment:` block now contains `ROUTER_DATABASE_URL=postgresql://app:${POSTGRES_PASSWORD}@postgres:5432/router` AND `OPENWEBUI_DATABASE_URL=...@postgres:5432/openwebui` (D-B6)
    - Router service `networks:` list now contains `data` (in addition to existing `app`, `backend`)
    - Router service `depends_on:` now contains `postgres: { condition: service_healthy, required: false }` (D-E4)
    - `compose.yml` does NOT contain `:latest` for postgres image (CLAUDE.md "What NOT to Use")
    - `grep -c '0.0.0.0:' compose.yml` (postgres section) returns 0 (DATA-01 / D-E1 — no host port published)
  </behavior>
  <action>
    Add the postgres service block to compose.yml AFTER the existing router-dev service block (around line 290). Use image `postgres:17-alpine` (CLAUDE.md pin; NOT `:latest` — failed by INFRA-04 antipattern). Mirror RESEARCH §"Code Examples — Compose snippet" lines 608–629 verbatim with the project's `${COMPOSE_PROJECT_NAME:-local-llms}` naming convention. Set:
    - `container_name: ${COMPOSE_PROJECT_NAME:-local-llms}-postgres`
    - `restart: unless-stopped`
    - `environment: POSTGRES_PASSWORD=${POSTGRES_PASSWORD}` and `POSTGRES_INITDB_ARGS=--encoding=UTF8 --locale=C` (do NOT set `POSTGRES_DB` — the initdb script creates both DBs explicitly per RESEARCH §Code Examples comment)
    - `networks: [data]` (D-E1 — note `data` is already declared `internal: true` at the top of compose.yml — no change needed there)
    - `volumes: ${HOST_DATA_ROOT:-/srv/local-llms}/postgres-data:/var/lib/postgresql/data` (bind mount per D-E3) AND `./postgres/initdb:/docker-entrypoint-initdb.d:ro` (mount the initdb directory read-only per D-B3)
    - `healthcheck:` exactly per D-E2: `test: ["CMD-SHELL", "pg_isready -U app -d router -h 127.0.0.1 || exit 1"]`, `interval: 10s`, `timeout: 3s`, `start_period: 30s`, `retries: 5`
    - NO `ports:` block — internal network only (D-E1)
    - NO `depends_on:` block — postgres has no dependency

    Update the existing `router:` service block (around lines 209–248) by:
    - Appending two env entries to its `environment:` block: `ROUTER_DATABASE_URL=postgresql://app:${POSTGRES_PASSWORD}@postgres:5432/router` and `OPENWEBUI_DATABASE_URL=postgresql://app:${POSTGRES_PASSWORD}@postgres:5432/openwebui` (D-B6)
    - Adding `- data` to the existing `networks:` list (currently `app, backend`)
    - Appending to the existing `depends_on:` block:
      ```
      postgres:
        condition: service_healthy
        required: false
      ```
      Mirror the EXACT same pattern already used for `ollama` and `llamacpp` two entries above (D-E4; `required: false` is Compose ≥ 2.20.2 — already in use here so no version bump needed).

    Apply the SAME router-side env + network + depends_on changes to the `router-dev:` service block (lines 255–289) so dev mode is also Phase-5-ready.

    Create the new file `postgres/initdb/01-init.sql` per CONTEXT D-B3 + D-B8 and RESEARCH §"Code Examples — postgres/initdb/01-init.sql" (lines 681–700). Use the SAFER pattern from the RESEARCH "Note" at line 700: do NOT use `:'postgres_password'` interpolation (psql-only, entrypoint may not honor it). Instead:
    - The official postgres image's entrypoint already creates the default `postgres` superuser from `POSTGRES_PASSWORD`. Our `app` role is a separate non-superuser role — create it inside the SQL script with a literal `WITH PASSWORD '<value>'`, BUT we cannot put the literal in the SQL file because then it's checked into git. SOLUTION: the entrypoint runs `*.sh` and `*.sql` files in `/docker-entrypoint-initdb.d/`. Rename to `postgres/initdb/01-init.sh` ONLY IF you need env expansion. RECOMMENDED CLEAN APPROACH: keep `01-init.sql` as a pure SQL file, and pass `POSTGRES_USER=app` + `POSTGRES_DB=router` as env vars in compose so the entrypoint creates the `app` user from `POSTGRES_PASSWORD` for free, then `01-init.sql` only needs to: (a) create the `openwebui` database with `OWNER app`, (b) `\connect router` and `CREATE EXTENSION IF NOT EXISTS pgcrypto`, (c) `\connect openwebui` and grant on public schema.

    PICK ONE approach and implement it:
    - Approach A (recommended — simpler): set `POSTGRES_USER=app`, `POSTGRES_DB=router` in compose env. Healthcheck stays `pg_isready -U app -d router`. `01-init.sql` is small: `CREATE DATABASE openwebui OWNER app;` + `\connect router` + `CREATE EXTENSION IF NOT EXISTS pgcrypto;` + `\connect openwebui` + `GRANT ALL ON SCHEMA public TO app;`. The `app` user owns both DBs because POSTGRES_USER+POSTGRES_DB created `router` owned by `app`, and we explicitly create `openwebui OWNER app`.
    - Approach B: keep `POSTGRES_USER=postgres` (default), and use a `.sh` wrapper that envsub's the password into a generated SQL file. More moving parts; do NOT use unless Approach A blocks on something.

    Document the choice in a header comment on `01-init.sql`. The header MUST also contain the immutability warning from RESEARCH Pitfall 6 ("This file runs once on first init only. To change schema, use Drizzle migrations — editing this file post-deploy has no effect without `docker compose down -v`").

    In `.env.example`, add a single comment line above the existing `POSTGRES_PASSWORD=` slot (~line 35): `# Phase 5: seeds BOTH router and openwebui databases via postgres/initdb/01-init.sql`. Do NOT add new variables.

    Per RESEARCH Assumption A2 / Pitfall 7: do NOT add a pre-emptive chown step yet. The bind mount path `${HOST_DATA_ROOT}/postgres-data` will be created by Docker on first up; the postgres entrypoint chowns it inside the container. If first-up fails with "permission denied", document the uid resolution in the smoke test slice (Plan 05-04). The CONTEXT specifics §"5s-postgres-pause smoke test" already assumes a working bind mount.
  </action>
  <verify>
    <automated>docker compose config 2>&1 | grep -E '^\s*postgres:' &amp;&amp; docker compose config | grep -cE 'postgres:17-alpine' | grep -q '^1$' &amp;&amp; ! docker compose config | grep -E 'postgres:.*latest' &amp;&amp; grep -c '127.0.0.1' compose.yml | tee /dev/stderr</automated>
  </verify>
  <done>
    `docker compose config` validates; postgres service uses `postgres:17-alpine`, no `:latest`; postgres has no host port published; router service joins `data` network and declares ROUTER_DATABASE_URL + OPENWEBUI_DATABASE_URL env; depends_on postgres with `required: false`. After `docker compose up postgres`, `docker compose ps postgres` shows `healthy` and `docker compose exec postgres psql -U app -l` lists both `router` and `openwebui` databases. The initdb script ran exactly once; subsequent restarts skip it (verified by checking postgres container logs for the `running /docker-entrypoint-initdb.d/01-init.sql` line on first up only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drizzle schema, migrator wrapper, pool factory, generated SQL migration</name>
  <files>router/package.json, router/drizzle.config.ts, router/src/db/schema/request_log.ts, router/src/db/schema/usage_daily.ts, router/src/db/schema/index.ts, router/src/db/index.ts, router/src/db/migrate.ts, router/src/config/env.ts, router/db/migrations/0001_init.sql</files>
  <read_first>
    - router/package.json (alphabetically sorted dependencies block at lines 20–30 — append new entries matching that style)
    - router/src/config/env.ts (existing EnvSchema — extension target; the new ROUTER_DATABASE_URL slot mirrors the existing OLLAMA_URL z.string().url() pattern)
    - router/src/config/registry.ts (lines 15–27, 95–113 — the factory + type-export pattern that schema/index.ts barrel-export mirrors per PATTERNS.md)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §"DB access layer + migrations" (D-B1..D-B8) and §"request_log shape" (D-D1 — authoritative column list)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §"Specifics" lines 288–306 (usage_daily suggested shape)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Architecture Patterns — Pattern 4" (lines 380–397 — makePool shape) and §"Pitfall 4" (lines 528–547 — runMigrations selective try/catch)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Code Examples — Drizzle config + schema + migrator" (lines 702–773 — literal shapes to copy)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Open Questions" Q3 (lines 855–859 — usage_daily NULL-in-PK warning — agent_id must be NOT NULL with a sentinel OR use COALESCE in the UPSERT; this plan picks the sentinel approach: declare `agent_id` NOT NULL with `.default('_no_agent_')` so the composite PK is unambiguous)
    - .planning/phases/05-postgres-observability-seam/05-PATTERNS.md §"router/src/db/schema/request_log.ts" + §"router/src/db/index.ts" + §"router/src/db/migrate.ts"
    - CLAUDE.md §"Supporting Libraries — Router" (drizzle-orm@^0.36 / pg@^8.13 / @types/pg / drizzle-kit@^0.27 pins)
  </read_first>
  <behavior>
    - `cd router && npm install` exits 0 and `package.json` contains `drizzle-orm@^0.36`, `pg@^8.13`, `@types/pg`, `drizzle-kit@^0.27` in the correct dependencies / devDependencies sections; `package-lock.json` is updated and committed.
    - `cd router && npx tsc --noEmit` exits 0 (full type-check, all new code compiles).
    - `cd router && npx drizzle-kit generate` produces `router/db/migrations/0001_init.sql` containing `CREATE TABLE "request_log"`, `CREATE TABLE "usage_daily"`, three `CREATE INDEX` statements per D-D1 indexes, and a `CREATE TABLE "drizzle"."__drizzle_migrations"` line (or equivalent migration-tracking table).
    - The generated SQL is reviewed (file is committed; no surprising `DROP` statements; no unintended schema deltas).
    - Module `router/src/db/schema/index.ts` re-exports `requestLog` and `usageDaily` (barrel pattern per PATTERNS.md `router/src/errors/envelope.ts:5–11` analog).
    - `router/src/db/index.ts` exports `makePool(url: string): Pool` with `max: 8`, `idleTimeoutMillis: 30_000`, `connectionTimeoutMillis: 2_000` (RESEARCH Pitfall 3 — NOT default 0) and `makeDb(pool: Pool): NodePgDatabase` returning a Drizzle handle.
    - `router/src/db/migrate.ts` exports `runMigrations(db, log): Promise<void>` that wraps `drizzle-orm/node-postgres/migrator.migrate(db, { migrationsFolder: './db/migrations' })` in a try/catch matching RESEARCH Pitfall 4 lines 532–546 verbatim: warn-and-continue on `ECONNREFUSED`/`ETIMEDOUT`/`ENOTFOUND` or `code.startsWith('08')`; throw on schema-class errors.
    - `router/src/config/env.ts` EnvSchema now requires `ROUTER_DATABASE_URL: z.string().url()` (no default — must be supplied via compose env).
  </behavior>
  <action>
    Add to `router/package.json`:
    - `dependencies`: `drizzle-orm@^0.36`, `pg@^8.13`, `prom-client@^15` (prom-client gets installed here so 05-02 has zero install steps — only consumes it). Alphabetical insertion per the existing block style.
    - `devDependencies`: `drizzle-kit@^0.27`, `@types/pg@^8.13`. Alphabetical insertion.
    Run `cd router && npm install` and commit the updated `package-lock.json`. Do NOT use `npm install --save` ad-hoc — edit the JSON, then run `npm install` so the lockfile is deterministic. **Per RESEARCH §"Assumptions Log" A1, stay on `^0.36` / `^0.27` per CLAUDE.md (do NOT upgrade to ^0.45 — CLAUDE.md is the contract for this milestone).**

    Create `router/drizzle.config.ts` per RESEARCH lines 703–714 literal. Schema path `./src/db/schema/index.ts`, migrations out `./db/migrations`, `dbCredentials: { url: process.env.ROUTER_DATABASE_URL! }`. `dialect: 'postgresql'`. Do NOT set `verbose` or `strict` (drizzle-kit defaults are correct).

    Create `router/src/db/schema/request_log.ts` per CONTEXT D-D1 verbatim (column-for-column) using `drizzle-orm/pg-core`'s `pgTable`, `uuid`, `timestamp`, `text`, `integer`, `index` imports. Match the RESEARCH §"Code Examples — Drizzle config + schema" block at lines 717–744:
    - `id uuid primaryKey defaultRandom()` (pgcrypto-backed)
    - `ts timestamp(withTimezone:true) notNull defaultNow()`
    - All other columns per D-D1 — pay attention to which are `.notNull()` vs nullable
    - Three btree indexes via the second `pgTable` arg `(t) => ({ idxTsDesc: index('idx_request_log_ts_desc').on(t.ts.desc()), idxAgentTs: index('idx_request_log_agent_ts').on(t.agent_id, t.ts.desc()), idxStatusClass: index('idx_request_log_status_class').on(t.status_class) })`
    - `export type RequestLogInsert = typeof requestLog.$inferInsert;` — this is the type the bufferedWriter consumes.

    Create `router/src/db/schema/usage_daily.ts` per CONTEXT specifics §"usage_daily shape" (lines 288–306) — but per RESEARCH Open Question Q3, change `agent_id: text('agent_id')` (nullable) to `agent_id: text('agent_id').notNull().default('_no_agent_')` so the composite primary key `(day, protocol, backend, model, agent_id)` is unambiguous (Postgres treats NULL as distinct from NULL in unique constraints — would break UPSERT idempotency). Document the sentinel choice in a comment above the column. `day` should be `date('day').notNull()`. Use `primaryKey` from `drizzle-orm/pg-core` for the composite PK.

    Create `router/src/db/schema/index.ts` — pure barrel: `export { requestLog, type RequestLogInsert } from './request_log.js'; export { usageDaily } from './usage_daily.js';`. Use `.js` import extensions per the existing codebase's NodeNext module resolution.

    Create `router/src/db/index.ts`:
    - Import `Pool, type PoolConfig` from `pg`, `drizzle` from `drizzle-orm/node-postgres`, `type NodePgDatabase` from `drizzle-orm/node-postgres`.
    - Export `makePool(url: string): Pool` constructing `new Pool({ connectionString: url, max: 8, idleTimeoutMillis: 30_000, connectionTimeoutMillis: 2_000 })`. **Critical: the `connectionTimeoutMillis: 2_000` is NOT optional — RESEARCH Pitfall 3 says the default 0 hangs forever on Postgres-unreachable, violating D-B5.**
    - Export `makeDb(pool: Pool): NodePgDatabase` → `drizzle(pool)`.
    - Re-export `type NodePgDatabase` for downstream consumers.
    - Do NOT call `pool.connect()` at construction (lazy — D-B5).

    Create `router/src/db/migrate.ts` per RESEARCH §"Code Examples — Migrator + boot wiring" lines 748–772 verbatim. The function signature is `export async function runMigrations(db: NodePgDatabase, log: pino.Logger): Promise<void>`. The selective try/catch is the CORE of D-B5 reconciliation — copy the connection-error code list exactly (`ECONNREFUSED`, `ETIMEDOUT`, `ENOTFOUND`, and the regex `code.startsWith('08')` for Postgres connection-class SQLSTATEs).

    Update `router/src/config/env.ts` EnvSchema by adding ONE field: `ROUTER_DATABASE_URL: z.string().url()` (required, no default — supplied by compose). Add it alphabetically between OLLAMA_URL and PORT (or wherever the alphabetical ordering would place it).

    Run `cd router && npx drizzle-kit generate`. This requires `ROUTER_DATABASE_URL` to be set in the env at generate-time. To avoid requiring a running postgres for `generate`, prefer setting it to a syntactically-valid but unused URL: `ROUTER_DATABASE_URL=postgresql://app:test@127.0.0.1:5432/router npx drizzle-kit generate` (drizzle-kit `generate` does not actually connect — it only reads the schema files; the URL is required by `defineConfig` but never opened). Commit the generated `router/db/migrations/0001_init.sql` AND the `router/db/migrations/meta/` directory drizzle-kit also produces. Verify the SQL contains `CREATE TABLE "request_log"`, `CREATE TABLE "usage_daily"`, and the three `CREATE INDEX` statements.
  </action>
  <verify>
    <automated>cd router &amp;&amp; npm ls drizzle-orm pg prom-client drizzle-kit @types/pg 2>&amp;1 | grep -cE '(drizzle-orm|^├─|^└─)' &amp;&amp; npx tsc --noEmit &amp;&amp; ls db/migrations/0001_*.sql &amp;&amp; grep -lE 'CREATE TABLE "request_log"' db/migrations/0001_*.sql &amp;&amp; grep -lE 'CREATE TABLE "usage_daily"' db/migrations/0001_*.sql &amp;&amp; grep -v '^#' src/db/index.ts | grep -c 'connectionTimeoutMillis: 2_?000'</automated>
  </verify>
  <done>
    All five npm packages installed at the CLAUDE.md-pinned versions; full TypeScript build clean; `router/db/migrations/0001_init.sql` exists with both CREATE TABLE statements and three indexes; `connectionTimeoutMillis: 2_000` confirmed by grep in `src/db/index.ts` (non-comment line). All new files importable via `.js` NodeNext extensions. Barrel `src/db/schema/index.ts` re-exports both tables.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BufferedWriter implementation + unit tests + boot wiring + onClose drain</name>
  <files>router/src/db/bufferedWriter.ts, router/tests/unit/bufferedWriter.test.ts, router/src/index.ts, router/src/app.ts</files>
  <read_first>
    - router/src/concurrency/semaphore.ts (lines 14–60 — the idempotent release closure + `released: boolean` lock; this is the structural analog for the drain idempotency)
    - router/src/backends/liveness.ts (entire file — the setInterval + Map + `inFlight` Set + `stopped` flag + idempotent `stop()` pattern; bufferedWriter mirrors this shape replacing `inFlight: Set<string>` with `flushing: boolean`)
    - router/src/index.ts (entire — current boot sequence to extend; the order matters per D-B5)
    - router/src/app.ts (lines 36–67 — BuildAppOpts current shape; lines 202–206 — existing onClose hook to extend; lines 69–243 — buildApp body to understand where new lifecycle wiring lands)
    - router/tests/unit/semaphore.test.ts (the existing idempotency test patterns — Phase 5 mirrors the numbered-case style)
    - router/tests/unit/liveness.test.ts (lines 43–110 — vi.useFakeTimers boilerplate + interval-scheduler test patterns; bufferedWriter tests mirror this)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §"Buffered async write pipeline" (D-A1..D-A7) — the authoritative behavior contract
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Architecture Patterns — Pattern 1" lines 181–264 — the LITERAL bufferedWriter shape; copy verbatim except where commented as planner-discretion. Pay specific attention to lines 260–264 ("Key invariants") and lines 207, 213, 220, 226 (the four `flushing` / `queueMicrotask` / `buf.unshift` / `timer.unref` invariants)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Pitfall 1" + §"Pitfall 3" + §"Pitfall 5" + §"Pitfall 8" (these are the eight invariants the unit test must encode)
    - .planning/phases/05-postgres-observability-seam/05-PATTERNS.md §"router/src/db/bufferedWriter.ts" + §"router/tests/unit/bufferedWriter.test.ts"
    - .planning/phases/05-postgres-observability-seam/05-VALIDATION.md (DATA-02 row + Wave 0 list)
  </read_first>
  <behavior>
    - `cd router && vitest run tests/unit/bufferedWriter.test.ts` exits 0 with at least 7 named tests covering:
      1. After `push(row)` once and `vi.advanceTimersByTime(1_000)`, the mock `db.insert(...).values(rows)` is called once with a single-element array (1s flush trigger — D-A2).
      2. After `push` × 200 within the same tick, the mock insert is called once with all 200 rows via the queueMicrotask deferred path (200-row trigger — D-A2).
      3. At capacity 10_000, the (10_001)th push drops the oldest row AND increments the injected `droppedCounter.inc()` mock call by exactly one (D-A1, RESEARCH Pitfall 1 warning).
      4. When `db.insert(...).values(rows)` rejects, the rows REMAIN in the buffer (next interval tick is given another mock insert that resolves — assert the SAME rows arrive in the second insert call); the dropped counter is NOT incremented for flush failures (D-A7).
      5. The `flushing: boolean` re-entrancy lock: when `db.insert(...).values(rows)` is made to await a controlled promise that never resolves within the next 5 simulated seconds, advancing timers by 5_000ms produces EXACTLY ONE call to the mock insert (overlapping flushes are NOT spawned — RESEARCH Pitfall 1).
      6. `drain(timeoutMs=3_000)` race: when the in-flight insert never resolves, advancing timers by 3_001ms causes `drain` to resolve AND log a warn with `event: 'log_buffer_shutdown_drop'` and the remaining buffer length (D-A4).
      7. After `drain()` returns, subsequent `push(row)` is a no-op (stopped flag set; mirrors semaphore.ts `released` idempotency).
    - `cd router && npx tsc --noEmit` exits 0.
    - At router boot, the sequence in `src/index.ts main()` is: `loadEnv()` → `makePool(env.ROUTER_DATABASE_URL)` → `makeDb(pool)` → `runMigrations(db, app.log)` → `makeBufferedWriter({ db, droppedCounter: STUB, logger: app.log })` → `buildApp({ ..., bufferedWriter })` → `app.listen(...)`. **Note**: this plan uses a STUB droppedCounter (`{ inc: () => {} }`) — the real prom-client counter is wired in Plan 05-02 when metrics/registry.ts lands. Document the stub with a `// 05-02 will replace this stub with metrics.logBufferDroppedTotal` comment.
    - The existing onClose hook in `app.ts` is EXTENDED (not replaced) — the new line `await opts.bufferedWriter.drain(3_000);` is appended after the existing `semaphoreMap.clear();` line. Mirrors the analog in PATTERNS.md §"router/src/app.ts" lines 561–569.
    - BuildAppOpts in `app.ts` gains a REQUIRED field `bufferedWriter: { push(row: RequestLogInsert): void; drain(ms: number): Promise<void>; size?: number }`. Existing tests that build the app will break — fix them by passing a fake `bufferedWriter` shape (see test fixture below). This is the SAME pattern used for `livenessFactory` / `semaphoreFactory` injection in the existing app.ts.
  </behavior>
  <action>
    Create `router/src/db/bufferedWriter.ts` per RESEARCH §"Pattern 1" lines 187–256 verbatim with these explicit invariants encoded:
    - Default capacity 10_000 (CONTEXT D-A1 + Claude's-discretion paragraph — document the choice in a header comment naming D-A1)
    - Default flushIntervalMs 1_000 (D-A2)
    - Default flushAtRows 200 (D-A2)
    - `flushing: boolean` lock initialized false (RESEARCH Pitfall 1 invariant — NOT in CONTEXT but mandatory per RESEARCH)
    - `stopped: boolean` flag (mirrors liveness.ts + semaphore.ts `released` pattern)
    - `push(row)`: if stopped → return; if buf.length ≥ capacity → `buf.shift()` + `opts.droppedCounter.inc()`; push the row; if buf.length ≥ flushAtRows → `queueMicrotask(() => { void flush(); })` (D-A2 microtask-deferred — NOT `process.nextTick`, NOT synchronous, NOT setImmediate)
    - `flush()`: guard with `if (flushing || buf.length === 0 || stopped) return`; set `flushing = true`; splice up to min(buf.length, 1000) rows into `batch` (cap batch size at 1000 to stay well under Postgres's 65535-parameter limit per RESEARCH Assumption A9); call `await opts.db.insert(requestLog).values(batch)`; on failure, `opts.logger.warn({ event: 'log_buffer_flush_error', err, count: batch.length }, 'flush failed')` AND `buf.unshift(...batch)` (D-A7 — rows STAY in buffer); finally `flushing = false`. Note: when the buffer is at capacity and a flush failure restores rows, the drop-oldest invariant means the OLDEST rows (which were ahead of `batch`) may be lost — this is acceptable per D-A1 (single-host, single-user, manual maintenance acceptable). Document this trade-off in a comment.
    - `setInterval(() => { void flush(); }, flushIntervalMs)` + `timer.unref?.()` so the event loop can exit (RESEARCH Pattern 1 line 226)
    - `drain(timeoutMs = 3_000)`: idempotent via `stopped` flag (mirrors `safeRelease`); `clearInterval(timer)`; `await Promise.race([flush(), new Promise<void>(resolve => setTimeout(resolve, timeoutMs))])`; if `buf.length > 0` after race, log warn `{ event: 'log_buffer_shutdown_drop', buffered_at_shutdown: N }` (D-A4)
    - `get size()` getter for tests + diagnostics
    Export `BufferedWriter = ReturnType<typeof makeBufferedWriter>` so consumers can type-import it without coupling to the factory.

    Create `router/tests/unit/bufferedWriter.test.ts` with the 7 numbered tests above. Mirror the structure of `router/tests/unit/semaphore.test.ts` (numbered `it('1. ...')` cases) + `router/tests/unit/liveness.test.ts` (fake timers boilerplate). Use a lightweight in-memory mock for `db.insert(...).values(...)`:
    ```typescript
    const inserts: Array<{ rows: unknown[]; resolve: () => void; reject: (e: Error) => void }> = [];
    const makeMockDb = () => ({
      insert: () => ({
        values: (rows: unknown[]) => {
          let resolve!: () => void; let reject!: (e: Error) => void;
          const p = new Promise<void>((res, rej) => { resolve = res; reject = rej; });
          inserts.push({ rows, resolve, reject });
          return p;
        },
      }),
    });
    const droppedCounter = { inc: vi.fn() };
    const logger = { warn: vi.fn(), info: vi.fn(), error: vi.fn(), debug: vi.fn() };
    ```
    This pattern matches the fake-injection idiom from `router/tests/integration/readyz.test.ts` (PATTERNS.md §"Fake-injection pattern from readyz.test.ts"). DO NOT use a real `pg.Pool` here — that's the integration test's job (Plan 05-02). Encode each invariant per the behavior block above.

    Update `router/src/app.ts` BuildAppOpts to add the REQUIRED field `bufferedWriter: BufferedWriter` (where `BufferedWriter = { push(row: RequestLogInsert): void; drain(ms: number): Promise<void>; size?: number }`). Import `type { RequestLogInsert } from './db/schema/index.js'`. Extend the existing onClose hook (currently at lines 202–206) by APPENDING `await opts.bufferedWriter.drain(3_000);` as the LAST line inside the async callback. Do NOT remove or reorder the existing three lines (`liveness.stop()`, `probeAdapters.clear()`, `semaphoreMap.clear()`).

    Update `router/src/index.ts main()` to insert the boot sequence BEFORE `await buildApp({...})`:
    ```typescript
    const pool = makePool(env.ROUTER_DATABASE_URL);
    const db = makeDb(pool);
    // app.log is not yet available — use console.* via a minimal pino-shaped shim,
    // OR pre-build the logger from loggerOpts. RESEARCH §"Migrator + boot wiring" uses
    // `app.log` but app doesn't exist yet — the practical pattern is to pass loggerOpts
    // through pino directly: `const bootLog = pino(loggerOpts as pino.LoggerOptions);`
    // then `await runMigrations(db, bootLog)` then later `buildApp({ ..., bufferedWriter })`.
    // ALTERNATIVELY: move `await runMigrations(...)` to immediately AFTER `buildApp` returns
    // but BEFORE `app.listen` — at that point `app.log` is usable. PICK THE SECOND APPROACH:
    // it's cleaner and matches the analog in index.ts:53–60 where post-buildApp wiring lives.
    const app = await buildApp({
      registry,
      bearerToken: env.ROUTER_BEARER_TOKEN,
      loggerOpts,
      bufferedWriter,  // STUB the writer here? No — see below: construct it BEFORE buildApp.
    });
    ```
    The ordering challenge: `bufferedWriter` is a REQUIRED field on BuildAppOpts but needs `db` and a logger; `app.log` is only available after `buildApp`. RESOLUTION: build a temporary pino logger from `loggerOpts` BEFORE `buildApp`, use it for `runMigrations` AND for `makeBufferedWriter`'s logger param; the app.log will be the SAME logger instance (Fastify passes loggerOpts to pino internally, so they share level/redact config). Final boot order:
    ```typescript
    const env = loadEnv();
    const loggerOpts = makeLoggerOptions({ level: env.LOG_LEVEL, isDev: env.NODE_ENV !== 'production' });
    const bootLog = pino(loggerOpts as pino.LoggerOptions);  // standalone instance
    const pool = makePool(env.ROUTER_DATABASE_URL);
    const db = makeDb(pool);
    await runMigrations(db, bootLog);
    const droppedCounterStub = { inc: () => {} };  // TODO 05-02: replace with metrics.logBufferDroppedTotal
    const bufferedWriter = makeBufferedWriter({ db, droppedCounter: droppedCounterStub, logger: bootLog });
    const initialRegistry = loadRegistryFromFile(env.MODELS_YAML_PATH);
    const registry = makeRegistryStore(initialRegistry);
    const app = await buildApp({ registry, bearerToken: env.ROUTER_BEARER_TOKEN, loggerOpts, bufferedWriter });
    // ... existing watcher wiring + closeGracefully + app.listen unchanged ...
    ```
    Import `pino` from `pino` — note: pino is a transitive dep via fastify; if `npm ls pino` fails inside router/, add `pino` as an explicit dep (it's listed in CLAUDE.md as `^9.x`). Verify with `cd router && node -e "console.log(require('pino')().level)"` after install.

    Update `closeGracefully` (lines 39–49) — no changes needed; `app.close()` triggers the onClose hook which now drains the bufferedWriter. Belt-and-suspenders: also explicitly `await pool.end()` AFTER `app.close()` so the pg pool releases its sockets on shutdown.

    Fix any existing test fixtures that build the app: search for `buildApp\(` callers in `router/tests/**` and add the missing `bufferedWriter:` field with a fake `{ push: () => {}, drain: async () => {} }` per PATTERNS.md §"Fake-injection pattern". This is mechanical and identical to the historical fixup for `semaphoreFactory` (per Plan 03-04 SUMMARY). Run `cd router && npm test` to confirm zero regressions BEFORE merging.
  </action>
  <verify>
    <automated>cd router &amp;&amp; npx tsc --noEmit &amp;&amp; vitest run tests/unit/bufferedWriter.test.ts &amp;&amp; npm test 2>&amp;1 | tail -30 | grep -E '(Tests|Test Files).*(passed|failed)'</automated>
  </verify>
  <done>
    `vitest run tests/unit/bufferedWriter.test.ts` reports ≥ 7 passing tests covering all D-A invariants + RESEARCH Pitfalls 1, 3, 5, 8 + Pitfall 4 warn-and-continue path; full `npm test` passes (existing Phase 2/3/4 fixtures updated for new BuildAppOpts field); TypeScript build clean; manual `docker compose up -d postgres router` followed by `docker compose logs router | grep -E 'migrate_ok|migrate_postgres_unreachable'` shows ONE log line (the migrator ran exactly once at boot); `docker compose exec postgres psql -U app -d router -c "\dt"` lists `request_log`, `usage_daily`, and `__drizzle_migrations`; killing the router with SIGTERM produces a clean shutdown log sequence ending in `closing` → onClose hook fires → no `log_buffer_shutdown_drop` warning (buffer was empty).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → postgres | Bind-mounted `postgres-data` and `postgres/initdb` directories; UID mismatch (RESEARCH Pitfall 7) is the only abuse vector and is operator-controlled |
| network: data ↔ router | `data` network is `internal: true` per Phase 1 D-13; postgres has no host port — no external attack surface in Phase 5 |
| router → postgres | Authenticated as `app` user via `ROUTER_DATABASE_URL`; password from `.env` (D-B6); parameterized SQL only via Drizzle `.values()` |
| postgres initdb → app role | Superuser-only operations (`CREATE USER`, `CREATE DATABASE`, `CREATE EXTENSION pgcrypto`) confined to first-init script; `app` role does NOT have CREATE EXTENSION privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Tampering | Drizzle multi-row INSERT in bufferedWriter | mitigate | `.values(batch)` emits parameterized SQL; NO string interpolation; verified by code review (grep for `sql\`` template literals in `db/` — must be zero) |
| T-5-02 | DoS (self-inflicted) | Postgres-down cascading SSE stall | mitigate | D-A1..D-A7 entire design: in-memory bounded buffer with drop-oldest + `connectionTimeoutMillis: 2_000` (Pitfall 3) + `flushing` re-entrancy lock (Pitfall 1) + `migrate_postgres_unreachable` warn-and-continue path (Pitfall 4). SC2 5s-pause smoke test is the regression gate (lands in Plan 05-04) |
| T-5-03 | DoS | Pool exhaustion via concurrent flush + /readyz probe (Pitfall 5) | mitigate | `max: 8` pool size + `flushing` lock means writer holds at most 1 connection; batch cap of 1_000 rows keeps each flush short. `/readyz` postgres probe (lands in Plan 05-04) uses Promise.race + 1s timeout |
| T-5-04 | Privilege Escalation | Bootstrap SQL running as superuser | mitigate | `01-init.sql` runs ONCE on first init only (Pitfall 6) via Postgres entrypoint `docker-entrypoint-initdb.d`. Documented immutability in script header. App role NEVER has CREATE EXTENSION privilege. |
| T-5-05 | Tampering | Bind-mount UID mismatch on `postgres-data` (Pitfall 7) | accept | Single-user host; operator-controlled. If first-up fails, the smoke test slice (Plan 05-04) documents the `docker run --rm postgres:17-alpine id postgres` resolution step. Not exploitable as an attack — only an operational gotcha. |
| T-5-06 | DoS | Drizzle migrator hangs forever on postgres-unreachable | mitigate | `connectionTimeoutMillis: 2_000` (Pitfall 3) bounds the connect attempt; selective try/catch (Pitfall 4) reports as `migrate_postgres_unreachable` and falls through to `app.listen`; `/readyz` returns 503 until postgres recovers (Plan 05-04 adds the probe wiring) |
| T-5-07 | Information Disclosure | `ROUTER_DATABASE_URL` password embedded in env var visible to `docker inspect` | accept | Single-user host; bearer-token rotation procedure lands in Phase 9. `.env` is chmod 600 per Phase 1 ops; `docker inspect` requires shell access which already grants the same privileges. |
</threat_model>

<verification>
## Phase 5 SC Mapping for this plan

This plan delivers the FOUNDATIONS of SC1 + SC2 + SC5; full SC1 + SC2 + SC5 verification happens in subsequent plans.

- **SC1 (postgres:17-alpine + two DBs + request_log + usage_daily tables):** Delivered end-to-end here. Verify:
  ```bash
  docker compose up -d postgres
  docker compose ps postgres --format '{{.Health}}'  # → healthy
  docker compose exec postgres psql -U app -l | grep -cE '^\s*(router|openwebui)\s'  # → 2
  docker compose up -d router
  docker compose logs router | grep -cE 'event":"migrate_(ok|postgres_unreachable)"'  # → 1
  docker compose exec postgres psql -U app -d router -c '\dt' | grep -cE '(request_log|usage_daily|__drizzle_migrations)'  # → 3
  docker compose exec postgres psql -U app -d router -c "SELECT extname FROM pg_extension WHERE extname='pgcrypto'"  # → 1 row
  ```

- **SC2 (buffered async writes never block):** Delivered as the UNIT-test contract — re-entrancy lock, drop-oldest, drain race, flush-error rows-stay (D-A7). The end-to-end SSE-stream-survives-pause-postgres test lands in Plan 05-04. Verify here:
  ```bash
  cd router && vitest run tests/unit/bufferedWriter.test.ts
  # All 7 named tests pass; no warnings about unhandled timers or zombie promises.
  ```

- **SC5 (real healthchecks):** Postgres healthcheck added (`pg_isready`). Existing healthchecks on ollama / llamacpp / router unchanged per D-G1. Verify:
  ```bash
  docker compose ps --format '{{.Name}} {{.Health}}'
  # Every Phase-1..4 service still healthy; postgres now also healthy.
  ```

## Plan-level automated gates

- `cd router && npx tsc --noEmit` exits 0
- `cd router && npm test` passes the unit tests added here AND all existing Phase 2/3/4 tests (no regressions from BuildAppOpts widening)
- `docker compose config` validates with no `:latest` tag for postgres
- Grep gates (filter out comments per planner spec):
  - `grep -v '^\s*#' router/src/db/index.ts | grep -c 'connectionTimeoutMillis: 2_\?000'` ≥ 1
  - `grep -v '^\s*//' router/src/db/bufferedWriter.ts | grep -c 'flushing'` ≥ 3 (declaration + set true + set false)
  - `grep -v '^\s*//' router/src/db/bufferedWriter.ts | grep -c 'queueMicrotask'` ≥ 1
  - `grep -v '^\s*//' router/src/db/migrate.ts | grep -cE 'ECONNREFUSED|ETIMEDOUT|ENOTFOUND'` = 3
  - `grep -c "':latest'" compose.yml` = 0 (verbatim — no `:latest` tag anywhere)
</verification>

<success_criteria>
- [ ] `docker compose up -d postgres` reaches healthy state; both `router` and `openwebui` databases exist; `pgcrypto` extension installed in `router`
- [ ] `docker compose up -d router` succeeds against healthy postgres → migrations applied (single `migrate_ok` log line) → `\dt` shows `request_log`, `usage_daily`, `__drizzle_migrations`
- [ ] `docker compose up -d router` succeeds against MISSING postgres (e.g., `docker compose stop postgres`) → migrator logs `migrate_postgres_unreachable` warn → router still listens on `:3000` and answers `/healthz` 200 (proves D-B5 non-blocking-on-boot via Pitfall 4 reconciliation)
- [ ] `bufferedWriter` unit suite green; all 7 named tests pass
- [ ] Full `cd router && npm test` green (no regressions in Phase 2/3/4 tests; BuildAppOpts widening absorbed by updated fixtures)
- [ ] `cd router && npx tsc --noEmit` exits 0
- [ ] All grep gates in `<verification>` pass
- [ ] SIGTERM the router → onClose hook fires → bufferedWriter drains within 3s → process exits 0 with no `log_buffer_shutdown_drop` warning (empty buffer at the time of shutdown)
- [ ] Threat-model table accepted; T-5-01..T-5-04 + T-5-06 mitigations live in the code shipped by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/05-postgres-observability-seam/05-01-SUMMARY.md` documenting:
- Postgres service + initdb + Drizzle wiring shipped
- BufferedWriter implementation + unit-test coverage (7 numbered cases)
- Boot order: `loadEnv → pool → migrate → bufferedWriter → buildApp → listen`
- D-B5 reconciliation strategy: warn-and-continue on connection-class migrator errors per RESEARCH Pitfall 4
- Open hand-off to Plan 05-02: `droppedCounterStub` in `index.ts` will be replaced with the real `metrics.logBufferDroppedTotal` counter
- Open hand-off to Plan 05-04: `/readyz` postgres probe wiring + smoke-test SC2 pause-pg scenario remain TODO
- Drizzle 0.36 pin held per CLAUDE.md (RESEARCH A1)
- Initdb approach chosen (Approach A: `POSTGRES_USER=app` + `POSTGRES_DB=router` + minimal SQL for openwebui DB + pgcrypto extension)
</output>
