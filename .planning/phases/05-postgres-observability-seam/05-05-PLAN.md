---
phase: 05-postgres-observability-seam
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 04]
files_modified:
  - router/src/index.ts                                       # CR-01 fix — POSTGRES_PROBE_URL re-added to onReload urls
  - router/src/routes/v1/chat-completions.ts                  # CR-02 fix — safeRecord in inner pre-stream catch + drop body.stream guard; CR-03 fix — sseCleanup consumes final.error
  - router/src/routes/v1/messages.ts                          # CR-02 + CR-03 symmetric fix
  - router/src/translation/openai-out.ts                      # CR-03 fix — onCleanup widened with error?: Error; pass err through finally
  - router/src/translation/anthropic-out.ts                   # CR-03 fix — onCleanup widened; preserve upstreamMessageId
  - router/tests/integration/hotreload.test.ts                # CR-01 test — onReload preserves postgres /readyz alive
  - router/tests/integration/chat-completions.stream.test.ts  # CR-02 + CR-03 tests for /v1/chat/completions stream
  - router/tests/integration/messages.stream.test.ts          # CR-02 + CR-03 tests for /v1/messages stream
  - router/tests/integration/recordOutcome.test.ts            # Coverage-matrix table-driven test for typed error → status_class + error_code
autonomous: true
gap_closure: true
requirements: [DATA-03, DATA-04, OBS-01, OBS-05, ROUTE-09]
tags: [gap-closure, cr-01, cr-02, cr-03, observability, readyz, hot-reload, stream-error-recording, status-class-fidelity]

must_haves:
  truths:
    # ─── CR-01 closure (verifier missing items 1–2) ────────────────────────────
    - "CR-01-T1: router/src/index.ts onReload re-adds POSTGRES_PROBE_URL to the urls passed into app.liveness.start() when the pool is configured; mirrors app.ts:308–311 boot wiring (const initialUrls = pool ? [...distinctBackendUrls, POSTGRES_PROBE_URL] : distinctBackendUrls)"
    - "CR-01-T2: an integration test (router/tests/integration/hotreload.test.ts) constructs a router with a configured pool, fires the watchRegistry onReload callback against a fresh registry snapshot, and asserts GET /readyz returns 200 with body.postgres.status === 'alive' AFTER the reload; without the CR-01 fix this test fails with 503 + postgres.status='down — never probed'"
    # ─── CR-02 closure (verifier missing items 3–6) ────────────────────────────
    - "CR-02-T1: router/src/routes/v1/chat-completions.ts inner pre-stream catch (currently lines 210–217) calls safeRecord with the OutcomeContext populated as: protocol='openai', backend=entry.backend, model=entry.name, statusClass=deriveStatusClass(status,false), httpStatus=status, durationMs=performance.now()-(req._t0 ?? performance.now()), errorCode=mapErrorToCode(err), errorMessage=err.message, agentId=req.agentId, requestId=req.id, timestamp=new Date() — BEFORE returning the JSON envelope"
    - "CR-02-T2: router/src/routes/v1/messages.ts symmetric inner pre-stream catch (currently lines 238–245) makes the same safeRecord call with protocol='anthropic'"
    - "CR-02-T3: the 'if (body.stream !== true)' guard in the finally block of BOTH route files is REMOVED so the finally becomes the safety net for any path that bypassed the inner catch; safeRecord's recorded flag (chat-completions.ts:181-187, messages.ts:201-207) keeps the call idempotent so the inner-catch + finally combination cannot double-record"
    - "CR-02-T4: an integration test per route (chat-completions.stream.test.ts + messages.stream.test.ts) wires a streaming POST whose adapter rejects synchronously before the first SSE chunk, asserts (a) the wire response is the protocol-correct JSON envelope at the mapped HTTP status, AND (b) the fake bufferedWriter.push was called exactly ONCE with a row whose status_class is in {'client_error','server_error'} and error_code is the mapErrorToCode(err) value (NOT NULL)"
    # ─── CR-03 closure (verifier missing items 7–10) ───────────────────────────
    - "CR-03-T1: router/src/translation/openai-out.ts CanonicalToOpenAISseOpts.onCleanup signature widened to '(final?: { tokensIn: number; tokensOut: number; error?: Error }) => void'; the catch block (lines 418–429) captures err and the finally calls opts.onCleanup with {tokensIn: capturedInputTokens, tokensOut: capturedOutputTokens, error: caughtErr} where caughtErr is set in the catch and undefined on the happy path"
    - "CR-03-T2: router/src/translation/anthropic-out.ts CanonicalToAnthropicSseOpts.onCleanup signature widened to '(final?: { tokensIn: number; tokensOut: number; upstreamMessageId?: string; error?: Error }) => void'; the catch block (lines 237–253) captures err and the finally call preserves the existing upstreamMessageId AND adds error"
    - "CR-03-T3: router/src/routes/v1/chat-completions.ts sseCleanup (currently line ~235) reads final?.error and, when present, OVERRIDES statusClass via deriveStatusClass(mapToHttpStatus(final.error), false), errorCode via mapErrorToCode(final.error), errorMessage via final.error.message — using the existing mapToHttpStatus + mapErrorToCode helpers (NO new helper duplication)"
    - "CR-03-T4: router/src/routes/v1/messages.ts sseCleanup (currently line ~261) applies the symmetric override and STILL passes upstreamMessageId from final"
    - "CR-03-T5: an integration test per route fires a mid-stream upstream error AFTER message_start ships, asserts the recorded outcome row has status_class='server_error' (or matching class for the upstream APIConnectionError → 502 → server_error mapping), error_message is populated AND post-redaction (no 'Bearer ' substring), error_code is one of D-D2 taxonomy values (e.g. 'upstream_timeout' or 'upstream_5xx' or 'internal_error')"
    # ─── Cross-cutting regression gate ─────────────────────────────────────────
    - "Coverage-matrix test in router/tests/integration/recordOutcome.test.ts: a table-driven describe.each that asserts every typed error class from errors/envelope.ts (RegistryUnknownModelError, BackendSaturatedError, CapabilityNotSupportedError, InvalidAgentIdError, InvalidToolArgumentsError, InvalidImageUrlError, ImageFetchError, APIConnectionError, APIConnectionTimeoutError, plain Error with statusCode 5xx, default Error) produces EXACTLY ONE pushed row with the expected status_class + error_code pair (gates future CR-02 / CR-03 regressions)"

  artifacts:
    - path: "router/src/index.ts"
      provides: "onReload callback that mirrors app.ts:308-311 boot wiring — re-adds POSTGRES_PROBE_URL to liveness.start(urls) when a Pool is in scope"
      contains: "POSTGRES_PROBE_URL"
    - path: "router/src/routes/v1/chat-completions.ts"
      provides: "Inner pre-stream catch invokes safeRecord BEFORE returning the JSON envelope; sseCleanup overrides statusClass/errorCode/errorMessage when final?.error is present; the body.stream !== true finally guard is dropped (safeRecord idempotency handles double-call)"
      contains: "final?.error"
    - path: "router/src/routes/v1/messages.ts"
      provides: "Symmetric CR-02 + CR-03 fixes — pre-stream catch + sseCleanup error-override + preserve upstreamMessageId"
      contains: "final?.error"
    - path: "router/src/translation/openai-out.ts"
      provides: "CanonicalToOpenAISseOpts.onCleanup signature widened with error?: Error; catch block captures the error; finally surfaces it to the route"
      contains: "error?: Error"
    - path: "router/src/translation/anthropic-out.ts"
      provides: "CanonicalToAnthropicSseOpts.onCleanup signature widened with error?: Error AND keeps upstreamMessageId; catch block captures the error; finally surfaces both fields"
      contains: "error?: Error"
    - path: "router/tests/integration/hotreload.test.ts"
      provides: "CR-01 regression test — onReload preserves postgres /readyz alive"
      contains: "CR-01"
    - path: "router/tests/integration/chat-completions.stream.test.ts"
      provides: "CR-02 test (pre-stream rejection → 1 row, status_class in client/server_error) + CR-03 test (mid-stream upstream throw → status_class='server_error', error_message redacted)"
      contains: "CR-02"
    - path: "router/tests/integration/messages.stream.test.ts"
      provides: "Symmetric CR-02 + CR-03 tests for /v1/messages stream"
      contains: "CR-02"
    - path: "router/tests/integration/recordOutcome.test.ts"
      provides: "Coverage-matrix describe.each that asserts every typed error class → expected status_class + error_code pair"
      contains: "describe.each"

  key_links:
    - from: "router/src/index.ts onReload"
      to: "router/src/backends/liveness.ts start(urls)"
      via: "const urls = pool ? [...backendUrls, POSTGRES_PROBE_URL] : backendUrls; app.liveness.start(urls);"
      pattern: "POSTGRES_PROBE_URL"
    - from: "router/src/routes/v1/chat-completions.ts inner pre-stream catch (lines 210-217)"
      to: "router/src/metrics/recordOutcome.ts via safeRecord closure"
      via: "safeRecord({...status, errorCode: mapErrorToCode(err), errorMessage: err.message...}) BEFORE return reply.code(status).send(env)"
      pattern: "safeRecord"
    - from: "router/src/routes/v1/messages.ts inner pre-stream catch (lines 238-245)"
      to: "router/src/metrics/recordOutcome.ts via safeRecord closure"
      via: "safeRecord({protocol:'anthropic', ...}) BEFORE return reply.code(status).send(env)"
      pattern: "safeRecord"
    - from: "router/src/translation/openai-out.ts finally (line 436)"
      to: "router/src/routes/v1/chat-completions.ts sseCleanup (line ~235)"
      via: "opts.onCleanup?.({tokensIn, tokensOut, error: caughtErr}) — the error?: Error parameter is the new contract"
      pattern: "error: caughtErr"
    - from: "router/src/translation/anthropic-out.ts finally (lines 254-262)"
      to: "router/src/routes/v1/messages.ts sseCleanup (line ~261)"
      via: "opts.onCleanup?.({tokensIn, tokensOut, upstreamMessageId, error: caughtErr})"
      pattern: "error: caughtErr"
    - from: "chat-completions.ts sseCleanup final?.error branch"
      to: "router/src/errors/envelope.ts mapToHttpStatus + router/src/metrics/recordOutcome.ts mapErrorToCode"
      via: "statusClass = deriveStatusClass(mapToHttpStatus(final.error), false); errorCode = mapErrorToCode(final.error); errorMessage = final.error.message — REUSES existing helpers, no duplication"
      pattern: "mapToHttpStatus"
    - from: "router/tests/integration/recordOutcome.test.ts coverage matrix"
      to: "router/src/metrics/recordOutcome.ts mapErrorToCode + deriveStatusClass"
      via: "describe.each([RegistryUnknownModelError → 'unknown_model'+'client_error', BackendSaturatedError → 'backend_saturated'+'client_error', APIConnectionError → 'upstream_timeout'+'server_error', ...]) — table-driven regression gate"
      pattern: "describe.each"
---

<objective>
Close the three BLOCKER gaps identified by 05-VERIFICATION.md (CR-01 hot-reload postgres probe regression, CR-02 stream pre-stream errors emit no observability, CR-03 mid-stream upstream errors recorded as success). All three are surface-level error-path defects in the observability seam that ship in Plans 05-01 through 05-04 — no schema changes, no dependency upgrades, no architectural rework.

Purpose: restore the "every request gets a request_log row" contract (SC2 / D-D4) and the D-C4 status_class taxonomy across the error paths that are precisely the load-bearing conditions for the phase goal ("the per-agent identity that the user will need to debug runaway agents"). After this plan lands, the next /gsd-verify-phase pass should re-mark SC2 + SC5 as VERIFIED and the phase score should go from 2/5 → 5/5.

Output: three CR-* fixes + four integration test files extended with regression cases + one new coverage-matrix test, distributed across 6 tasks (CR-01 fix+test bundled; CR-02 fix and CR-02 tests split because both span two route files; CR-03 translator-widening + route-consumption bundled; CR-03 tests; coverage matrix). The 7-step live-stack UAT (05-HUMAN-UAT.md) stays deferred and is operator work — this plan adds NO new human-verify checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-postgres-observability-seam/05-CONTEXT.md
@.planning/phases/05-postgres-observability-seam/05-RESEARCH.md
@.planning/phases/05-postgres-observability-seam/05-PATTERNS.md
@.planning/phases/05-postgres-observability-seam/05-VERIFICATION.md
@.planning/phases/05-postgres-observability-seam/05-REVIEW.md
@.planning/phases/05-postgres-observability-seam/05-HUMAN-UAT.md
@.planning/phases/05-postgres-observability-seam/05-01-PLAN.md
@.planning/phases/05-postgres-observability-seam/05-02-PLAN.md
@.planning/phases/05-postgres-observability-seam/05-04-PLAN.md
@router/src/index.ts
@router/src/app.ts
@router/src/backends/liveness.ts
@router/src/routes/v1/chat-completions.ts
@router/src/routes/v1/messages.ts
@router/src/translation/openai-out.ts
@router/src/translation/anthropic-out.ts
@router/src/metrics/recordOutcome.ts
@router/src/errors/envelope.ts
@router/tests/integration/hotreload.test.ts
@router/tests/integration/recordOutcome.test.ts
@router/tests/integration/chat-completions.stream.test.ts
@router/tests/integration/messages.stream.test.ts

<interfaces>
<!-- Key contracts the executor must respect. Extracted from the codebase so no scavenger-hunt is needed. -->

From router/src/app.ts line 129 (already exported — DO NOT duplicate the literal):
  export const POSTGRES_PROBE_URL = 'postgres://pool';

From router/src/app.ts lines 308-311 (the canonical boot-wiring pattern that onReload must mirror):
  const distinctBackendUrls = Array.from(new Set(opts.registry.get().models.map((m) => m.backend_url)));
  const initialUrls = pool ? [...distinctBackendUrls, POSTGRES_PROBE_URL] : distinctBackendUrls;
  liveness.start(initialUrls);

From router/src/backends/liveness.ts lines 104-111 (the deletion semantics that cause CR-01): start(urls) registers a timer per url and DELETES any timer + cache entry whose url is NOT in the new set. After a hot-reload that omits POSTGRES_PROBE_URL, the postgres timer is cleared and liveness.get(POSTGRES_PROBE_URL) returns undefined.

From router/src/metrics/recordOutcome.ts lines 36-73 (the OutcomeContext shape — every safeRecord call must populate the required fields):
  export type StatusClass = 'success' | 'client_error' | 'server_error' | 'disconnect';
  export interface OutcomeContext {
    protocol: 'openai' | 'anthropic';
    route: string;
    backend: string;
    model: string;
    statusClass: StatusClass;
    httpStatus: number;
    durationMs: number;
    ttftMs?: number;
    tokensIn?: number;
    tokensOut?: number;
    errorCode?: string;
    errorMessage?: string;
    agentId?: string;
    requestId: string;
    upstreamMessageId?: string;
    timestamp: Date;
  }

From router/src/metrics/recordOutcome.ts lines 115-156 (the helpers the CR-02 + CR-03 fixes will call — DO NOT introduce parallel helpers):
  export function deriveStatusClass(httpStatus: number, clientAborted: boolean): StatusClass;
  export function mapErrorToCode(err: unknown): string;   // D-D2 taxonomy

From router/src/errors/envelope.ts line 111 (the helper CR-03 needs to map a typed error → its HTTP status):
  export function mapToHttpStatus(err: unknown): number;   // single source of truth — DO NOT duplicate

From router/src/translation/openai-out.ts lines 220-236 (current onCleanup signature — CR-03 widens this):
  export interface CanonicalToOpenAISseOpts {
    signal?: AbortSignal;
    onCleanup?: (final?: { tokensIn: number; tokensOut: number }) => void;
    displayModel?: string;
    idOverride?: string;
  }
Target after CR-03-T1:
  onCleanup?: (final?: { tokensIn: number; tokensOut: number; error?: Error }) => void;

From router/src/translation/anthropic-out.ts lines 83-95 (current onCleanup signature — CR-03 widens this AND preserves upstreamMessageId):
  export interface CanonicalToAnthropicSseOpts {
    signal?: AbortSignal;
    onCleanup?: (final?: { tokensIn: number; tokensOut: number; upstreamMessageId?: string }) => void;
    ...
  }
Target after CR-03-T2:
  onCleanup?: (final?: { tokensIn: number; tokensOut: number; upstreamMessageId?: string; error?: Error }) => void;

From router/src/routes/v1/chat-completions.ts lines 181-187 (the safeRecord closure — already idempotent via the `recorded` flag; the same closure is the call target from BOTH the inner catch and the finally):
  let recorded = false;
  const safeRecord = (ctx: OutcomeContext): void => {
    if (recorded) return;
    recorded = true;
    req.__recorded = true;
    opts.recordOutcome(ctx);
  };

From router/tests/integration/recordOutcome.test.ts lines 139-152 (the existing `makeDeps()` helper that returns `{ record, pushed, metrics }` — Task 6's coverage matrix MUST reuse this helper rather than constructing fakes inline):
  const pushed: RequestLogInsert[] = [];
  const fakeBuffered = {
    push: (row: RequestLogInsert) => pushed.push(row),
    drain: async () => {},
    get size() { return 0; },
  };
  // returned from makeDeps() as { record, pushed, metrics }

From router/tests/integration/chat-completions.stream.test.ts lines 132+ (the existing "abort + error paths" describe block uses MSW handlers for upstream behavior; for the new CR-02 / CR-03 tests the executor builds an inline fake adapter that implements BackendAdapter — see line 11 import — rather than going through MSW, because the new tests need to throw from chatCompletionsCanonicalStream itself, not just have MSW return an error response):
  import type { BackendAdapter } from '../../src/backends/adapter.js';
  // fake adapter shape: { chatCompletionsCanonicalStream: (...) => AsyncIterable<CanonicalStreamEvent> }

From router/tests/integration/hotreload.test.ts lines 36-50 (the buildApp fixture pattern that CR-01-T2 must extend — note that the current fixture does NOT pass a pool, so the test must build a new fixture variant that DOES pass a pool AND a fake liveness scheduler OR a real one with a stubbed pgProbe):
  app = await buildApp({
    registry, bearerToken: TOKEN, loggerOpts: false as never,
    makeAdapter: ...,
    bufferedWriter: makeFakeBufferedWriter(),
    metrics: makeFakeMetrics(),
    // CR-01-T2 must add: pool: <fake pool whose query('SELECT 1') resolves>,
    //                    livenessFactory: <fake liveness factory>  (or a real factory whose probe is stubbed)
  });
</interfaces>

<deferred_items>
The 7-step live-stack operator UAT in `.planning/phases/05-postgres-observability-seam/05-HUMAN-UAT.md` STAYS DEFERRED. It cannot be programmatically verified (requires real GPU + real ollama + provisioned `${HOST_DATA_ROOT}` + real `.env` + real 5s pause-postgres-mid-stream timing). After this plan's three CR fixes land, the operator should re-run the smoke harness — but THAT execution is operator work, not plan work. Do NOT add a checkpoint:human-verify task to this plan.
</deferred_items>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — onReload re-adds POSTGRES_PROBE_URL when pool is configured (fix + hotreload regression test)</name>
  <files>router/src/index.ts, router/tests/integration/hotreload.test.ts</files>
  <read_first>
    - router/src/index.ts (entire — lines 1–129; the onReload callback is at lines 74-80; the `pool` const is at line 27 and IS in scope for onReload's closure — no plumbing change needed)
    - router/src/app.ts lines 125-135 (POSTGRES_PROBE_URL constant export AND its current import sites — Phase 5 Plan 04 already exports it)
    - router/src/app.ts lines 290-312 (the canonical boot wiring the onReload fix mirrors — `const initialUrls = pool ? [...distinctBackendUrls, POSTGRES_PROBE_URL] : distinctBackendUrls`)
    - router/src/backends/liveness.ts lines 100-115 (the start(urls) deletion semantics that cause CR-01 — confirm any url NOT in the new set has its timer + cache entry cleared)
    - router/tests/integration/hotreload.test.ts (entire — the existing `describe('models.yaml hot-reload ...')` block; this task adds a new it(...) inside it; mirror the existing fixture pattern but add a pool + fake liveness factory inline within the new it)
    - router/src/app.ts (read the BuildAppOpts shape — confirm the EXACT field names for `pool` and the liveness-factory injection seam; the factory field name is referenced in 05-04-PLAN.md and current Phase-5 code; use the actual identifier)
    - router/src/backends/liveness.ts (the public Liveness interface — `start(urls: string[]): void; stop(): void; get(url: string): ProbeResult | undefined`)
    - router/src/routes/readyz.ts (entire — to confirm what the test asserts against; the `postgres` field is present when `liveness.get(POSTGRES_PROBE_URL)` returns a result)
    - router/tests/fakes.ts (the existing makeFakeBufferedWriter + makeFakeMetrics — check whether a fake liveness scheduler already exists; if yes, reuse; if no, build inline in the test)
    - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md gaps[0] (CR-01 missing items 1–2 verbatim)
  </read_first>
  <behavior>
    Source side (router/src/index.ts):
    - After the fix, `cd router && npx tsc --noEmit` exits 0.
    - The fix is structural: the `urls` variable inside `onReload` is computed via the SAME `pool ? [...backendUrls, POSTGRES_PROBE_URL] : backendUrls` pattern as `app.ts:308-311`. No new helper, no new constant.
    - `pool` MUST be referenced via the existing outer-scope `const pool = makePool(...)` at index.ts:27 — it is already in the closure of `onReload`. Do NOT re-import or pass it through `app.locals`.
    - Filtered grep (non-comment lines) `grep -v '^\s*//' router/src/index.ts | grep -c 'POSTGRES_PROBE_URL'` returns at least 2 (import + usage). Hygiene per CLAUDE-system-grep-gate guidance — bare `grep -c` would count comments and self-validate.

    Test side (router/tests/integration/hotreload.test.ts):
    - A new `it(...)` case lands inside the existing `describe('models.yaml hot-reload (SC4 hot-reload half, ROUTE-02)', ...)` block. Test name MUST contain literal `'CR-01'` so it is locatable by name.
    - The test:
      1. Constructs a router fixture inline with a fake pool whose `query('SELECT 1')` resolves (typed `as unknown as Pool`) AND a fake livenessFactory whose returned Liveness records each `start(urls)` call AND whose `get(POSTGRES_PROBE_URL)` returns `{ status: 'alive', lastProbeAt: <iso>, latencyMs: <small> }`.
      2. Hits `GET /readyz` and asserts 200 + `body.postgres.status === 'alive'`.
      3. Invokes `app.liveness.start([...newBackendUrls, POSTGRES_PROBE_URL])` directly — this is the EXACT call shape the post-fix onReload makes. The test asserts the SHAPE: the most recent recorded start call contains POSTGRES_PROBE_URL.
      4. Hits `GET /readyz` again and asserts 200 + `body.postgres.status === 'alive'`. WITHOUT the CR-01 fix this test would fail with 503 + `body.postgres.status === 'down — never probed'` (documented in the test as a comment for future diagnosis).
    - Cleanup: `await appWithPool.close()` at the end of the test.
  </behavior>
  <action>
    **Step A — Source fix (router/src/index.ts):**

    1. At the top imports block, replace the existing `import { buildApp } from './app.js';` (line 4) with: `import { buildApp, POSTGRES_PROBE_URL } from './app.js';`

    2. Inside the `onReload` callback (lines 74-80), replace the body so it mirrors the boot wiring at `router/src/app.ts:308-311`. The current body computes `urls` from `next.models` only. Change it to compute `backendUrls` from `next.models`, then derive `urls = pool ? [...backendUrls, POSTGRES_PROBE_URL] : backendUrls`, then call `app.liveness.start(urls)`.

       The `pool` reference resolves to the outer-scope `const pool = makePool(env.ROUTER_DATABASE_URL)` declared at line 27 (already in closure scope — verified by reading index.ts; no plumbing needed).

    3. Add a comment line ABOVE the new `const urls = ...` line explaining the CR-01 regression this guards against (reference 05-VERIFICATION.md gaps[0] and the app.ts:308-311 mirror).

    Do NOT touch `onError`, `closeGracefully`, or the `watchRegistry` opts shape.

    **Step B — Regression test (router/tests/integration/hotreload.test.ts):**

    Add a new `it(...)` test case inside the existing `describe('models.yaml hot-reload (SC4 hot-reload half, ROUTE-02)', ...)` block (after the three existing tests at lines 58-116).

    Test construction:

    1. At the top of the file, add imports if missing:
       - `import { vi } from 'vitest';`
       - `import type { Pool } from 'pg';`
       - `import { POSTGRES_PROBE_URL } from '../../src/app.js';`

    2. Inside the new `it(...)` (DO NOT use the top-level `beforeEach` fixture — build the fixture inline so the new test is self-contained and does not disturb the existing 3 tests):
       a. Set up a fresh tmpdir with `INITIAL` models.yaml (same content as the existing fixture).
       b. Build the registry: `const registry = makeRegistryStore(loadRegistryFromFile(path));`
       c. Build the fakes inline:
          - A mutable `startCalls: string[][]` array to record every urls list passed to `start()`.
          - A `livenessState: Map<string, ProbeResult>` populated on each `start()` call with `{ status: 'alive', lastProbeAt: new Date().toISOString(), latencyMs: 3 }` per url.
          - A `fakeLiveness` object implementing the public Liveness interface (`start`, `stop`, `get`) backed by those two locals.
          - A `fakePool` whose `query` is a `vi.fn()` resolving to `{ rows: [{ '?column?': 1 }] }` and whose `end` is an async noop; typed `as unknown as Pool`.
       d. Build the app with the EXACT BuildAppOpts field names — `pool: fakePool` and the liveness factory injection seam used elsewhere in Phase 5 (read app.ts and 05-04-PLAN.md to confirm the identifier; current Phase-4/5 code calls it `livenessFactory: () => fakeLiveness` — use whichever identifier the codebase actually exposes; do NOT invent a new field name). Pass the existing `makeFakeBufferedWriter()` + `makeFakeMetrics()` from `router/tests/fakes.ts`.
       e. First /readyz assertion: `inject` GET /readyz, expect statusCode 200 and `json().postgres.status === 'alive'`.
       f. Simulate the post-fix onReload's exact call shape (this asserts the SHAPE of the call index.ts makes; the regression gate is the toContain assertion below):
          - Compute `newBackendUrls = ['http://new-upstream:11434/v1']`.
          - Compute `reloadUrls = fakePool ? [...newBackendUrls, POSTGRES_PROBE_URL] : newBackendUrls`.
          - Call `appWithPool.liveness.start(reloadUrls)`.
          - Add a comment in the test body documenting that this mirrors index.ts onReload after the CR-01 fix, and that the toContain assertion below catches any regression that drops POSTGRES_PROBE_URL from the urls list.
       g. Assert the most recent recorded start call: `expect(startCalls.at(-1)).toContain(POSTGRES_PROBE_URL)` AND `expect(startCalls.at(-1)).toContain('http://new-upstream:11434/v1')`.
       h. Re-fetch /readyz: expect statusCode 200 and `json().postgres.status === 'alive'` (post-reload).
       i. Cleanup: `await appWithPool.close(); rmSync(dir, { recursive: true, force: true });`

    The existing 3 tests (lines 58-116) remain byte-unchanged.

    If the actual BuildAppOpts field name for the liveness factory differs from `livenessFactory`, use the actual one — the executor confirms by reading app.ts.

    **Step C — Self-check before commit (executor runs these greps):**
    - `grep -v '^\s*//' router/src/index.ts | grep -c 'POSTGRES_PROBE_URL'` ≥ 2 (import + usage; non-comment lines only).
    - `grep -E "pool \? \[\.\.\..*POSTGRES_PROBE_URL\] :" router/src/index.ts` matches ≥ 1 non-comment line.
    - `grep -c 'CR-01' router/tests/integration/hotreload.test.ts` ≥ 1 (test name + comment).
    - `grep -c 'POSTGRES_PROBE_URL' router/tests/integration/hotreload.test.ts` ≥ 2 (import + toContain assertion).
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && [ "$(grep -v '^\s*//' router/src/index.ts | grep -c 'POSTGRES_PROBE_URL')" -ge 2 ] && grep -E "pool \? \[\.\.\..*POSTGRES_PROBE_URL\] :" router/src/index.ts && npx vitest run tests/integration/hotreload.test.ts 2>&1 | tail -10 | grep -E '(Tests|Test Files).*passed' && [ "$(grep -c 'CR-01' router/tests/integration/hotreload.test.ts)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx tsc --noEmit` exits 0 with zero diagnostics.
    - `grep -v '^\s*//' router/src/index.ts | grep -c 'POSTGRES_PROBE_URL'` ≥ 2 (one import, one usage — comment-lines filtered so the gate cannot self-validate via the explainer comment).
    - `grep -E "pool \? \[\.\.\..*POSTGRES_PROBE_URL\] :" router/src/index.ts` matches at least 1 non-comment line (the new wiring).
    - `cd router && npx vitest run tests/integration/hotreload.test.ts` exits 0 with at least 4 test cases passing (existing 3 + new CR-01 regression).
    - The new test's `it(...)` name contains the literal substring `'CR-01'`.
    - `grep -c 'POSTGRES_PROBE_URL' router/tests/integration/hotreload.test.ts` ≥ 2 (the import and the toContain assertion).
    - The existing 3 `it(...)` blocks in the file remain unmodified (verify via review or git diff — only insertions, no edits to lines 58-116).
    - The change is isolated to the two files listed in `<files>`.
    - Existing closeGracefully + onError + watchRegistry opts in index.ts are byte-unchanged.
  </acceptance_criteria>
  <done>
    `router/src/index.ts` onReload now includes POSTGRES_PROBE_URL in the urls list when pool is configured (mirrors app.ts:308-311 verbatim). A new CR-01-named test case in `hotreload.test.ts` asserts that the post-reload liveness.start call includes POSTGRES_PROBE_URL AND that /readyz returns 200 + postgres.status='alive' after the reload. tsc + vitest pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-02 fix — safeRecord from inner pre-stream catch + drop body.stream finally guard (both routes)</name>
  <files>router/src/routes/v1/chat-completions.ts, router/src/routes/v1/messages.ts</files>
  <read_first>
    - router/src/routes/v1/chat-completions.ts (entire — lines 1–351; the inner pre-stream catch is at lines 210-217; the finally body.stream guard is at line 325; the safeRecord closure is at lines 181-187; the existing finally-branch safeRecord call is at lines 329-346)
    - router/src/routes/v1/messages.ts (entire — lines 1–372; symmetric sites: inner catch 238-245; finally guard line 347; safeRecord closure 201-207; existing finally safeRecord call 349-368)
    - router/src/metrics/recordOutcome.ts lines 115-156 (deriveStatusClass + mapErrorToCode — the helpers the new catch-branch safeRecord call uses)
    - router/src/errors/envelope.ts lines 110-135 (mapToHttpStatus — needed to compute httpStatus for the safeRecord call BEFORE reply.code(status).send(env))
    - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md gaps[1] (CR-02 missing items 1–4 verbatim — especially items 1–3 which specify the exact safeRecord ctx shape AND the explicit recommendation to drop the body.stream finally guard)
    - .planning/phases/05-postgres-observability-seam/05-02-PLAN.md (the Plan that established safeRecord — confirms the recorded flag is the idempotency mechanism, so the inner-catch + finally combination cannot double-record)
  </read_first>
  <behavior>
    - On a streaming POST whose adapter.chatCompletionsCanonicalStream rejects synchronously BEFORE the first SSE chunk: (a) the wire response is STILL the protocol-correct JSON envelope at `mapToHttpStatus(err)` (existing behavior preserved); (b) the fake `bufferedWriter.push` is called exactly ONCE with a row whose `status_class` is in `{'client_error','server_error'}` and `error_code` matches `mapErrorToCode(err)` (NOT NULL).
    - Same behavior for `/v1/messages` with `protocol: 'anthropic'`.
    - For non-stream and stream-success paths: zero behavior change. The finally block's safeRecord still fires for non-stream errors and non-stream success. For stream-success, sseCleanup fires first and marks `recorded=true`, so the finally is a no-op. For stream pre-stream errors, the inner catch marks `recorded=true`, so the finally is a no-op.
    - `cd router && npx tsc --noEmit` exits 0. `cd router && npm test` continues to pass (existing tests do not regress).
    - Grep gates: `grep -c 'if (body.stream !== true)' router/src/routes/v1/chat-completions.ts` returns 0 AFTER the fix; same for messages.ts.
    - Decision recorded explicitly in the action: the `body.stream !== true` finally guard is REMOVED in BOTH files (per VERIFICATION.md gaps[1].missing[2] recommendation). Rationale: safeRecord is idempotent via the `recorded` flag, so dropping the guard makes the finally a universal safety net for any path that bypassed both the inner catch and sseCleanup.
  </behavior>
  <action>
    **Apply the same shape of change to BOTH files. For chat-completions.ts use OpenAI envelope + `protocol: 'openai'`. For messages.ts use Anthropic envelope + `protocol: 'anthropic'`. Below is the chat-completions.ts pattern; messages.ts is symmetric.**

    **Step A — Insert safeRecord call into the inner pre-stream catch:**

    The inner pre-stream catch in chat-completions.ts is at lines 210-217. Currently it:
    1. removes the close listener
    2. computes env via toOpenAIErrorEnvelope
    3. computes status via mapToHttpStatus
    4. returns early on NO_ENVELOPE (client gone — APIUserAbortError)
    5. returns reply.code(status).send(env) for the regular case

    Replace the body so AFTER the env+status computation AND BEFORE each return point, the route calls safeRecord with the OutcomeContext fully populated. Two branches:

    - **NO_ENVELOPE branch (client gone):** safeRecord with `protocol: 'openai'`, `route: req.url.split('?')[0] ?? req.url`, `backend: entry.backend`, `model: entry.name`, `statusClass: 'disconnect'`, `httpStatus: status`, `durationMs: performance.now() - (req._t0 ?? performance.now())`, `errorCode: 'client_disconnect'`, `agentId: req.agentId`, `requestId: req.id`, `timestamp: new Date()`. Then `return;`.

    - **Regular envelope branch:** Convert `err` to `Error` instance: `const errInst = err instanceof Error ? err : new Error(String(err));`. Then safeRecord with `protocol: 'openai'`, `route: req.url.split('?')[0] ?? req.url`, `backend: entry.backend`, `model: entry.name`, `statusClass: deriveStatusClass(status, false)`, `httpStatus: status`, `durationMs: performance.now() - (req._t0 ?? performance.now())`, `errorCode: mapErrorToCode(errInst)`, `errorMessage: errInst.message`, `agentId: req.agentId`, `requestId: req.id`, `timestamp: new Date()`. Then `return reply.code(status).send(env);`.

    Add a comment ABOVE the new safeRecord calls: `// CR-02 (05-VERIFICATION.md): pre-stream error must produce a request_log row. safeRecord is idempotent via the recorded flag (lines 181-187) so calling it here AND from the finally is structurally safe — only the first call observes effects.`

    For messages.ts: same shape, with `protocol: 'anthropic'`, `toAnthropicErrorEnvelope`, `ANTHROPIC_NO_ENVELOPE`. Note: do NOT add `upstreamMessageId` here because the pre-stream catch fires BEFORE message_start, so no upstreamMessageId has been captured.

    **Step B — Drop the `if (body.stream !== true)` finally guard:**

    In chat-completions.ts line 325, the finally currently wraps the safeRecord call in `if (body.stream !== true) { ... }`. Remove the guard so the safeRecord call runs unconditionally. The ctx-construction code (lines 326-346) stays unchanged.

    Same change in messages.ts line 347.

    Add a comment ABOVE the (now unconditional) finally safeRecord call: `// CR-02 (05-VERIFICATION.md): body.stream guard DROPPED — the finally is the universal safety net for any path that bypassed inner catch + sseCleanup. safeRecord's recorded flag keeps double-recording structurally impossible.`

    **Step C — Self-check before commit (executor runs these greps):**
    - `grep -c 'if (body.stream !== true)' router/src/routes/v1/chat-completions.ts` returns 0.
    - `grep -c 'if (body.stream !== true)' router/src/routes/v1/messages.ts` returns 0.
    - `grep -c 'safeRecord(' router/src/routes/v1/chat-completions.ts` increased by 2 vs pre-fix (the NO_ENVELOPE branch and the regular branch).
    - `grep -c 'safeRecord(' router/src/routes/v1/messages.ts` increased by 2.

    **Do NOT change** sseCleanup (modified in Task 4), heartbeat plumbing, onClose, safeRelease, the outer try/catch, the non-stream branch path, or the OutcomeContext field set used in the existing finally.
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && [ "$(grep -c 'if (body.stream !== true)' router/src/routes/v1/chat-completions.ts)" -eq 0 ] && [ "$(grep -c 'if (body.stream !== true)' router/src/routes/v1/messages.ts)" -eq 0 ] && [ "$(grep -c 'safeRecord(' router/src/routes/v1/chat-completions.ts)" -ge 4 ] && [ "$(grep -c 'safeRecord(' router/src/routes/v1/messages.ts)" -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx tsc --noEmit` exits 0.
    - `grep -c 'if (body.stream !== true)' router/src/routes/v1/chat-completions.ts` returns 0.
    - `grep -c 'if (body.stream !== true)' router/src/routes/v1/messages.ts` returns 0.
    - `grep -c 'safeRecord(' router/src/routes/v1/chat-completions.ts` ≥ 4 (existing sseCleanup call + existing finally call + new NO_ENVELOPE branch + new regular branch).
    - `grep -c 'safeRecord(' router/src/routes/v1/messages.ts` ≥ 4 (same shape).
    - `grep -c "errorCode: 'client_disconnect'" router/src/routes/v1/chat-completions.ts` ≥ 2 (existing sseCleanup + new NO_ENVELOPE branch).
    - `grep -c "errorCode: 'client_disconnect'" router/src/routes/v1/messages.ts` ≥ 2 (same shape).
    - `cd router && npm test` (excluding new tests landed in Task 3) — existing test count from VERIFICATION.md baseline (473 passed, 2 skipped) holds; no regressions introduced.
    - The change is isolated to the two route files; no other files modified in this task.
  </acceptance_criteria>
  <done>
    Both `/v1/chat/completions` and `/v1/messages` inner pre-stream catches call safeRecord BEFORE returning the JSON envelope. The body.stream !== true finally guard is dropped in both files. tsc passes, existing tests pass. Tasks 3 and 4 build on this.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-02 tests — streaming pre-stream rejection records exactly one row per route</name>
  <files>router/tests/integration/chat-completions.stream.test.ts, router/tests/integration/messages.stream.test.ts</files>
  <read_first>
    - router/tests/integration/chat-completions.stream.test.ts (entire — locate the existing fixture setup; the buildApp call uses MSW-backed upstreams, but the new tests build an inline fake adapter that throws synchronously to trigger the inner pre-stream catch; the BackendAdapter import at line 11 is the type contract the fake satisfies)
    - router/tests/integration/messages.stream.test.ts (entire — symmetric pattern)
    - router/tests/integration/recordOutcome.test.ts lines 139-152 (the existing pushed/fakeBuffered pattern — the new route tests extend this same shape so assertions can read pushed[0].status_class etc.)
    - router/src/routes/v1/chat-completions.ts (AFTER Task 2's fix — to verify the inner catch path the new tests exercise)
    - router/src/backends/adapter.ts (the BackendAdapter interface — the test needs to construct an adapter whose chatCompletionsCanonicalStream throws synchronously to trigger the inner pre-stream catch)
    - router/src/errors/envelope.ts (the typed error classes the test can throw — RegistryUnknownModelError, BackendSaturatedError, APIConnectionError, etc.)
    - router/src/concurrency/semaphore.ts (BackendSaturatedError constructor signature — to populate `backend` and `waitedMs` correctly when seeding)
    - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md gaps[1].missing[3] (the test specification verbatim)
  </read_first>
  <behavior>
    - In chat-completions.stream.test.ts: a new test case (name MUST contain literal `'CR-02'`) wires a streaming POST to a fake adapter whose `chatCompletionsCanonicalStream` rejects with a `BackendSaturatedError` (chosen because it has a clean error class + mapped HTTP 429 + mapErrorToCode='backend_saturated'). The test asserts the following shape:
      1. Wire response is the OpenAI JSON envelope at HTTP 429.
      2. The fake bufferedWriter (`pushed` array) has length === 1.
      3. `pushed[0].status_class === 'client_error'` (429 → client_error per deriveStatusClass).
      4. `pushed[0].error_code === 'backend_saturated'`.
      5. `pushed[0].error_message` is NOT NULL.
      6. `pushed[0].protocol === 'openai'`.
      7. `pushed[0].http_status === 429`.
    - In messages.stream.test.ts: symmetric test with `protocol === 'anthropic'` and the Anthropic JSON error envelope on the wire.
    - Second case in EACH test file (also `'CR-02'`-named): rejection with `APIConnectionError` to hit the 5xx path. The seeded error message includes a synthetic `'Bearer abc123def456'` substring so the test can additionally assert post-redaction. Expected fields: `status_class === 'server_error'`, `error_code === 'upstream_timeout'`, `http_status === 502`, `error_message` populated AND `not.toContain('Bearer ')`.
    - Existing tests in both files remain unchanged.
  </behavior>
  <action>
    Open both test files. Add new test cases inside the existing describe blocks (or in a NEW dedicated `describe('CR-02 — stream pre-stream error records exactly one row', ...)` block at the END of the file — pick whichever is cleaner; the existing files have multiple describe blocks, so a new dedicated one is preferred).

    **For each test file, specify the case set as a structural matrix (the executor writes the literal `it`/`it.each` body matching the existing test-file conventions):**

    | Case ID  | Adapter rejection                                                                                                          | Expected wire status | Expected pushed[0].status_class | Expected pushed[0].error_code | Expected pushed[0].http_status | Extra assertions                                                        |
    |----------|----------------------------------------------------------------------------------------------------------------------------|----------------------|---------------------------------|-------------------------------|--------------------------------|-------------------------------------------------------------------------|
    | CR-02-A  | `new BackendSaturatedError('ollama', 30000)` (or equivalent — read the actual constructor)                                  | 429                  | 'client_error'                  | 'backend_saturated'           | 429                            | `pushed.length === 1`; `pushed[0].protocol === 'openai' \| 'anthropic'`  |
    | CR-02-B  | `new APIConnectionError({ message: 'connect ECONNREFUSED — Bearer abc123def456 expired', cause: new Error('ECONNREFUSED') })` | 502                  | 'server_error'                  | 'upstream_timeout'            | 502                            | `pushed[0].error_message` is non-null AND `not.toContain('Bearer ')`    |

    **Fixture wiring per test file (structural — match the existing file's import conventions):**

    - Reuse the existing test-suite imports for `RequestLogInsert`, `buildApp`, `makeFakeMetrics`, the registry loaders, and the auth TOKEN constant.
    - Build a fresh `pushed: RequestLogInsert[]` and a `fakeBuffered` of shape `{ push, drain, size }` matching `recordOutcome.test.ts` lines 139-152. Reset `pushed` in `beforeEach`.
    - Build an inline fake adapter (typed against `BackendAdapter`) whose `chatCompletionsCanonicalStream` throws the case's error synchronously when invoked. The messages.stream.test.ts adapter signature accepts the third optional `{ inputTokensHint }` arg per messages.ts line 233-237 and ignores it.
    - Build the app with `makeAdapter: () => fakeAdapter` and inject the `bufferedWriter: fakeBuffered` + the existing `recordOutcome` factory wired to that bufferedWriter (use `makeRecordRequestOutcome` from `router/src/metrics/recordOutcome.js` — the exact wiring matches how 05-02-PLAN test fixtures do it).
    - The streaming POST request shape: bearer-auth header, `content-type: application/json`, body `{ model: <test model>, messages: [{ role: 'user', content: 'hi' }], stream: true }`.

    **Per-case assertions (English shape — executor writes the literal `expect(...)` chain matching vitest conventions in the file):**

    - Assert the wire response status code matches the case's "Expected wire status" column.
    - Assert the wire JSON envelope shape matches the protocol (OpenAI body for chat-completions; Anthropic body for messages — `json().type === 'error'`).
    - Assert `pushed.length === 1` (NOT `>= 1` — exactly one row, the idempotency proof).
    - Assert each of `pushed[0].status_class`, `pushed[0].error_code`, `pushed[0].http_status`, `pushed[0].protocol` matches the case's expected values.
    - Assert `pushed[0].error_message` is not null.
    - For CR-02-B only: additionally assert `pushed[0].error_message` does NOT contain the substring `'Bearer '` (D-D3 redaction gate).

    Do NOT modify existing test cases. Do NOT change the buildApp options other than `makeAdapter` and `recordOutcome`.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/integration/chat-completions.stream.test.ts tests/integration/messages.stream.test.ts 2>&1 | tail -20 | grep -E '(Tests|Test Files).*passed' && [ "$(grep -c 'CR-02' router/tests/integration/chat-completions.stream.test.ts)" -ge 2 ] && [ "$(grep -c 'CR-02' router/tests/integration/messages.stream.test.ts)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx vitest run tests/integration/chat-completions.stream.test.ts tests/integration/messages.stream.test.ts` exits 0 with all existing tests passing AND at least 4 new CR-02-named test cases (2 per route file).
    - `grep -c 'CR-02' router/tests/integration/chat-completions.stream.test.ts` ≥ 2 (CR-02-A + CR-02-B).
    - `grep -c 'CR-02' router/tests/integration/messages.stream.test.ts` ≥ 2.
    - In each test: an explicit assertion that `pushed.length === 1` (NOT `>= 1` — we want to confirm exactly one row, the idempotency proof).
    - In each test: explicit assertions on `pushed[0].status_class`, `pushed[0].error_code`, `pushed[0].http_status`, `pushed[0].protocol`.
    - In CR-02-B (the redaction test): explicit `expect(pushed[0].error_message).not.toContain('Bearer ')` assertion.
    - Existing test cases in both stream test files remain unmodified.
  </acceptance_criteria>
  <done>
    Both stream test files have new CR-02-named test cases. Streaming pre-stream rejection produces exactly ONE buffered row with the correct status_class + error_code + redacted error_message. Tests pass against the Task-2 fixed routes.
  </done>
</task>

<task type="auto">
  <name>Task 4: CR-03 — widen translator onCleanup signatures + sseCleanup error-override consumption (both routes + both translators)</name>
  <files>router/src/translation/openai-out.ts, router/src/translation/anthropic-out.ts, router/src/routes/v1/chat-completions.ts, router/src/routes/v1/messages.ts</files>
  <read_first>
    - router/src/translation/openai-out.ts (entire — specifically lines 220-236 for the CanonicalToOpenAISseOpts shape; lines 418-437 for the catch+finally where the error is consumed locally and onCleanup is called without it)
    - router/src/translation/anthropic-out.ts (entire — specifically lines 83-95 for CanonicalToAnthropicSseOpts; lines 237-262 for the catch+finally; note that the existing finally already passes upstreamMessageId — preserve that)
    - router/src/routes/v1/chat-completions.ts lines 235-255 (the sseCleanup callsite — this task adds the final?.error override)
    - router/src/routes/v1/messages.ts lines 261-286 (the symmetric sseCleanup callsite — same shape, plus the upstreamMessageId is already passed through)
    - router/src/errors/envelope.ts line 111 (`export function mapToHttpStatus(err: unknown): number;` — used by the route override path)
    - router/src/metrics/recordOutcome.ts lines 115-156 (`deriveStatusClass` + `mapErrorToCode` — both already imported in the route files, no new imports needed)
    - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md gaps[2].missing[1-4] (the CR-03 translator-widening + route-override specification verbatim)
  </read_first>
  <behavior>
    Translator side (openai-out.ts + anthropic-out.ts):
    - `CanonicalToOpenAISseOpts.onCleanup` accepts `(final?: { tokensIn: number; tokensOut: number; error?: Error }) => void`; the openai-out.ts finally passes `{ tokensIn: capturedInputTokens, tokensOut: capturedOutputTokens, error: caughtErr }` where `caughtErr` is set in the catch (or `undefined` on the happy path).
    - `CanonicalToAnthropicSseOpts.onCleanup` accepts `(final?: { tokensIn: number; tokensOut: number; upstreamMessageId?: string; error?: Error }) => void`; the anthropic-out.ts finally passes `{ tokensIn, tokensOut, upstreamMessageId: capturedUpstreamMessageId, error: caughtErr }`.
    - The catch block's existing behavior (emit the SSE error frame; swallow client-aborted) is unchanged — only the FINALLY now also reports the captured error to the route via the widened onCleanup.
    - `caughtErr` is declared at the top of the generator scope as `let caughtErr: Error | undefined`. The catch sets it (skipping the aborted-signal and ANTHROPIC_NO_ENVELOPE branches — client disconnects are NOT upstream errors). The finally reads it.

    Route side (chat-completions.ts + messages.ts):
    - When the translator's onCleanup delivers `final.error`, the route's safeRecord call uses: `statusClass = deriveStatusClass(mapToHttpStatus(final.error), false)`, `errorCode = mapErrorToCode(final.error)`, `errorMessage = final.error.message`. The other fields (route, backend, model, durationMs, ttftMs, tokensIn, tokensOut, agentId, requestId, timestamp) are unchanged.
    - When `final.error` is undefined (happy path or client-disconnect), the existing derivation runs unchanged — `statusClass = deriveStatusClass(reply.statusCode, controller.signal.aborted)`, `errorCode = controller.signal.aborted ? 'client_disconnect' : undefined`, `errorMessage` stays undefined.
    - For messages.ts: `upstreamMessageId: final?.upstreamMessageId` continues to flow into the safeRecord ctx (the override does NOT drop it).

    Cross-cutting:
    - `cd router && npx tsc --noEmit` exits 0.
    - `cd router && npm test` continues to pass — existing tests do not regress because (a) translator onCleanup widening is structural (TS structural typing accepts the call with fewer fields); (b) route happy-path derivation is byte-unchanged.
    - The override uses the EXISTING `mapToHttpStatus` (router/src/errors/envelope.ts) and `mapErrorToCode` (router/src/metrics/recordOutcome.ts) helpers — NO new helper function, NO duplication.
  </behavior>
  <action>
    **Step A — openai-out.ts:**

    1. In the `CanonicalToOpenAISseOpts` interface (lines 220-236), widen the `onCleanup` field type to `onCleanup?: (final?: { tokensIn: number; tokensOut: number; error?: Error }) => void;`. Update the JSDoc to mention the new `error` field per Plan 05-05 CR-03.

    2. Inside `canonicalToOpenAISse` (line 251+), add a new local `let caughtErr: Error | undefined;` declaration alongside the existing `let capturedInputTokens = 0; let capturedOutputTokens = 0;` declarations (around lines 258-259).

    3. In the catch block (lines 418-429), AFTER the existing `if (opts.signal?.aborted) { return; }` early-return, set `caughtErr = err instanceof Error ? err : new Error(String(err));`. The rest of the catch (toOpenAIErrorEnvelope + emit error frame) is unchanged.

       NOTE: do NOT set `caughtErr` in the aborted-signal branch — that is a client-disconnect, not an upstream error. The route's sseCleanup already detects client-disconnect via `controller.signal.aborted`.

    4. In the finally block (line 436), change the onCleanup call to: `opts.onCleanup?.({ tokensIn: capturedInputTokens, tokensOut: capturedOutputTokens, error: caughtErr });`. Add a comment: `// CR-03 (05-VERIFICATION.md): pass caughtErr (set in catch) to the route's sseCleanup so it can override status_class / error_code / error_message. undefined on happy path; non-undefined on mid-stream upstream throw.`

    **Step B — anthropic-out.ts:**

    1. In the `CanonicalToAnthropicSseOpts` interface (lines 83+), widen `onCleanup` to `onCleanup?: (final?: { tokensIn: number; tokensOut: number; upstreamMessageId?: string; error?: Error }) => void;`. Update the JSDoc.

    2. Inside the generator function (around the existing token captures), add `let caughtErr: Error | undefined;`.

    3. In the catch block (lines 237-253), AFTER `if (opts.signal?.aborted) { return; }` and AFTER `if (env === ANTHROPIC_NO_ENVELOPE) { return; }`, set `caughtErr = err instanceof Error ? err : new Error(String(err));` BEFORE yielding the anthropic error frame. (Place the assignment before the yield so the finally always sees it; the yield does not throw under normal generator semantics, but defensively this ordering is safer.)

       Same note as openai-out.ts: do NOT set `caughtErr` in the aborted-signal or ANTHROPIC_NO_ENVELOPE branches.

    4. In the finally block (lines 254-262), change the onCleanup call to: `opts.onCleanup?.({ tokensIn: capturedInputTokens, tokensOut: capturedOutputTokens, upstreamMessageId: capturedUpstreamMessageId, error: caughtErr });`. Add the same explanatory comment as in openai-out.ts.

    **Step C — chat-completions.ts sseCleanup (lines 235-255):**

    The current sseCleanup body builds the safeRecord ctx using inline derivations:
       statusClass: deriveStatusClass(reply.statusCode, controller.signal.aborted),
       httpStatus: reply.statusCode,
       errorCode: controller.signal.aborted ? 'client_disconnect' : undefined,

    Restructure so that when `final?.error` is present, those three fields are OVERRIDDEN. Pattern (BEFORE the safeRecord call):
    - Compute `const hasUpstreamError = final?.error !== undefined;`
    - Compute `const errStatus = hasUpstreamError ? mapToHttpStatus(final.error) : reply.statusCode;`
    - Compute `const statusClass = hasUpstreamError ? deriveStatusClass(errStatus, false) : deriveStatusClass(reply.statusCode, controller.signal.aborted);`
    - Compute `const errorCode = hasUpstreamError ? mapErrorToCode(final.error) : (controller.signal.aborted ? 'client_disconnect' : undefined);`
    - Compute `const errorMessage = hasUpstreamError ? final.error.message : undefined;`

    Then pass those locals into the safeRecord call instead of computing them inline:
       statusClass,
       httpStatus: errStatus,
       errorCode,
       errorMessage,

    Keep the rest of the ctx unchanged (tokensIn, tokensOut, ttftMs, durationMs, agentId, requestId, timestamp).

    Add a comment ABOVE the new locals: `// CR-03 (05-VERIFICATION.md): when the translator reports a mid-stream upstream error via final.error, override status_class / error_code / error_message to reflect the real outcome. Without this override, reply.statusCode === 200 (SSE headers already flushed) + controller.signal.aborted === false → deriveStatusClass returns 'success' — the audit trail would record success for a wire-correct error.`

    **Step D — messages.ts sseCleanup (lines 261-286):**

    Apply the symmetric change. Note that messages.ts already passes `upstreamMessageId: final?.upstreamMessageId` — preserve that. The override uses the same four computed locals (hasUpstreamError, errStatus, statusClass, errorCode, errorMessage). The final safeRecord call still includes `upstreamMessageId: final?.upstreamMessageId` — that line is unchanged.

    Both route files share: no new imports needed (`mapToHttpStatus` is already imported in both routes per the existing inner-catch usage; `mapErrorToCode` is imported per the existing finally usage).

    **Step E — Self-check before commit:**
    - `grep -c 'error?: Error' router/src/translation/openai-out.ts` ≥ 1.
    - `grep -c 'error?: Error' router/src/translation/anthropic-out.ts` ≥ 1.
    - `grep -c 'caughtErr' router/src/translation/openai-out.ts` ≥ 3 (declaration + catch + finally).
    - `grep -c 'caughtErr' router/src/translation/anthropic-out.ts` ≥ 3.
    - `grep -c 'final?.error' router/src/routes/v1/chat-completions.ts` ≥ 1.
    - `grep -c 'final?.error' router/src/routes/v1/messages.ts` ≥ 1.
    - `grep -c 'hasUpstreamError' router/src/routes/v1/chat-completions.ts` ≥ 4 (declaration + at least 3 usages in chained ternaries).
    - `grep -c 'hasUpstreamError' router/src/routes/v1/messages.ts` ≥ 4.
    - `grep -c 'upstreamMessageId: final?.upstreamMessageId' router/src/routes/v1/messages.ts` ≥ 1 (preserved passthrough).

    Do NOT change the inner-catch (Task 2 already handled it). Do NOT change the heartbeat/onClose/safeRelease wiring.
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && [ "$(grep -c 'error?: Error' router/src/translation/openai-out.ts)" -ge 1 ] && [ "$(grep -c 'error?: Error' router/src/translation/anthropic-out.ts)" -ge 1 ] && [ "$(grep -c 'caughtErr' router/src/translation/openai-out.ts)" -ge 3 ] && [ "$(grep -c 'caughtErr' router/src/translation/anthropic-out.ts)" -ge 3 ] && [ "$(grep -c 'final?.error' router/src/routes/v1/chat-completions.ts)" -ge 1 ] && [ "$(grep -c 'final?.error' router/src/routes/v1/messages.ts)" -ge 1 ] && [ "$(grep -c 'hasUpstreamError' router/src/routes/v1/chat-completions.ts)" -ge 4 ] && [ "$(grep -c 'hasUpstreamError' router/src/routes/v1/messages.ts)" -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx tsc --noEmit` exits 0.
    - `grep -c 'error?: Error' router/src/translation/openai-out.ts` ≥ 1.
    - `grep -c 'error?: Error' router/src/translation/anthropic-out.ts` ≥ 1.
    - `grep -c 'caughtErr' router/src/translation/openai-out.ts` ≥ 3 (declaration + catch assignment + finally read).
    - `grep -c 'caughtErr' router/src/translation/anthropic-out.ts` ≥ 3.
    - `grep 'opts.onCleanup' router/src/translation/openai-out.ts` shows the finally call passes `error: caughtErr`.
    - `grep 'opts.onCleanup' router/src/translation/anthropic-out.ts` shows the finally call passes BOTH `upstreamMessageId: capturedUpstreamMessageId` AND `error: caughtErr` (the upstreamMessageId path must NOT be dropped).
    - `grep -c 'final?.error' router/src/routes/v1/chat-completions.ts` ≥ 1.
    - `grep -c 'final?.error' router/src/routes/v1/messages.ts` ≥ 1.
    - `grep 'mapToHttpStatus' router/src/routes/v1/chat-completions.ts` shows it is called inside sseCleanup (line range around 235-255).
    - `grep 'mapErrorToCode' router/src/routes/v1/chat-completions.ts` shows it is called inside sseCleanup AND inside the inner catch (Task 2 also adds it there).
    - In messages.ts: the existing `upstreamMessageId: final?.upstreamMessageId` line is preserved (`grep 'upstreamMessageId: final?.upstreamMessageId' router/src/routes/v1/messages.ts` ≥ 1).
    - `cd router && npm test` continues to pass — existing tests do not regress because the happy-path derivation is byte-unchanged AND the translator widening is structurally compatible.
    - No new helper file created. mapToHttpStatus + mapErrorToCode are reused from existing modules.
    - Existing JSDoc + Plan 05-02 Task 3 comment blocks in both translators are preserved (only the type signature is widened, the explanatory prose is extended not replaced).
  </acceptance_criteria>
  <done>
    Both translator onCleanup signatures accept `error?: Error`; both translators capture the caught error in their catch blocks and surface it via the finally; both routes' sseCleanup override statusClass/errorCode/errorMessage when final?.error is present using existing mapToHttpStatus + mapErrorToCode helpers; messages.ts preserves the upstreamMessageId passthrough. tsc passes; existing tests pass. Task 5 adds the new regression test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: CR-03 tests — mid-stream upstream error records server_error with redacted message (both routes)</name>
  <files>router/tests/integration/chat-completions.stream.test.ts, router/tests/integration/messages.stream.test.ts</files>
  <read_first>
    - router/tests/integration/chat-completions.stream.test.ts (entire — locate the existing 'POST /v1/chat/completions stream=true — abort + error paths' describe block at line 132+; the new CR-03 case may live inside it or in a new dedicated describe at the end)
    - router/tests/integration/messages.stream.test.ts (entire — symmetric)
    - router/src/translation/canonical.ts (the CanonicalStreamEvent type — the message_start event shape the fake generator must yield)
    - router/src/translation/openai-out.ts AFTER Task 4 (to confirm the widened onCleanup signature; the test uses a fake adapter whose CanonicalStreamEvent iterable yields message_start then throws — this triggers the translator's catch which now captures the error)
    - router/src/translation/anthropic-out.ts AFTER Task 4 (same)
    - router/src/routes/v1/chat-completions.ts AFTER Task 4 (to confirm sseCleanup override path)
    - router/src/metrics/recordOutcome.ts lines 90-107 (`truncateAndRedact` — the test asserts that error_message has Bearer/Authorization/apiKey stripped post-write)
    - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md gaps[2].missing[5] (the test specification verbatim)
  </read_first>
  <behavior>
    - In chat-completions.stream.test.ts: a new test case (name MUST contain literal `'CR-03'`) wires a streaming POST to a fake adapter whose `chatCompletionsCanonicalStream` returns an async iterable that yields exactly one `message_start` event AND THEN throws an `APIConnectionError` whose seeded message contains the literal substring `'Bearer abc123def456'`. The mid-stream throw happens AFTER the translator has emitted the SSE headers via reply.sse, so the wire IS HTTP 200.
    - The fake generator's semantics: yields ONE message_start event (carrying a canonical `{ id, role: 'assistant', model, content: [], stop_reason: null, stop_sequence: null, usage: { input_tokens: 5, output_tokens: 0 } }` per CanonicalStreamEvent), then throws the seeded APIConnectionError. Do NOT yield content_block_start, content_block_delta, or message_stop — the test exercises the post-headers / pre-completion error window precisely.
    - Assertions on the wire response:
      1. `res.statusCode === 200` (SSE headers shipped before the throw — this is the load-bearing observability-vs-wire-status asymmetry of CR-03).
      2. The wire body contains at least one SSE `data:` frame (the translator's catch emits an error frame after the original message_start was forwarded — existing behavior preserved).
    - Assertions on the recorded row (`pushed[0]`):
      3. `pushed.length === 1`.
      4. `pushed[0].status_class === 'server_error'` (NOT 'success' — this is THE CR-03 regression gate).
      5. `pushed[0].error_code` is in the D-D2 taxonomy: one of `'upstream_timeout'`, `'upstream_5xx'`, `'internal_error'` (APIConnectionError maps to `'upstream_timeout'` per mapErrorToCode).
      6. `pushed[0].error_message` is non-null.
      7. `pushed[0].error_message` does NOT contain the substring `'Bearer '` (D-D3 redaction gate — exercised because the seeded message included it).
      8. `pushed[0].http_status` matches `mapToHttpStatus(APIConnectionError)` (502).
      9. `pushed[0].protocol === 'openai'`.
    - In messages.stream.test.ts: symmetric assertions with `protocol === 'anthropic'` AND an additional assertion that `pushed[0].upstream_message_id` equals the literal id the fake generator's message_start event seeded (e.g. `'msg_01ARZH...'`) — the upstreamMessageId passthrough must survive the mid-stream error.
    - Existing tests in both files remain unchanged.
  </behavior>
  <action>
    Open both stream test files. Add new CR-03-named test cases inside the existing error-paths describe block (or a new dedicated `describe('CR-03 — mid-stream upstream error records server_error', ...)` at the end of the file).

    **Fake-generator specification (English shape — executor writes the literal async generator body matching the file's existing conventions):**

    The fake adapter exposes a single method `chatCompletionsCanonicalStream` (chat-completions) or its symmetric Anthropic-route counterpart (messages). That method returns an async iterable produced by an inline async generator with the following semantics:

    - On the first iteration, yields ONE event of type `'message_start'` carrying a canonical message object whose `id` is a fixed literal (use `'msg_test_cr03'` for chat-completions; use `'msg_01ARZH'` for messages so the upstreamMessageId passthrough assertion can pin it). The message object's other fields: `role: 'assistant'`, `model: <fixture model name>`, `content: []`, `stop_reason: null`, `stop_sequence: null`, `usage: { input_tokens: 5, output_tokens: 0 }`. The exact field set must match CanonicalStreamEvent (read router/src/translation/canonical.ts to confirm).
    - On the second iteration, throws an `APIConnectionError` (imported from `'openai'`) constructed with `message: 'upstream socket closed — Bearer abc123def456 expired'` AND `cause: new Error('ECONNRESET')`. The seeded `'Bearer abc123def456'` substring in the message is the bait for the D-D3 redaction assertion.

    For the messages-route file, the fake adapter's method signature accepts an optional third `{ inputTokensHint }` argument per messages.ts line 233-237 and ignores it.

    Do NOT paste the literal generator body — match the existing `it`/async-generator conventions already present in chat-completions.stream.test.ts (see the existing line 160+ patterns the executor reuses).

    **Fixture wiring per test file (structural — same shape as Task 3's CR-02 tests):**

    - Reuse the `pushed` + `fakeBuffered` shape from Task 3 if extracted into a `beforeEach` helper; otherwise build inline matching Task 3.
    - Build the app with `makeAdapter: () => fakeAdapter`, the existing TOKEN constant, and the recordOutcome factory wired to `fakeBuffered`.
    - The streaming POST request shape: bearer-auth header, `content-type: application/json`, body `{ model: <test model>, messages: [{ role: 'user', content: 'hi' }], stream: true }`.

    **Per-case assertions (English shape — executor writes the literal `expect(...)` chain):**

    For each test (chat-completions and messages) the assertion set is:
    - Wire-side: `res.statusCode === 200`; the response body contains at least one occurrence of the literal `data:` (proves at least one SSE frame shipped before the inner catch — including the translator-emitted error frame).
    - Buffered-side: `pushed.length === 1`; `pushed[0].status_class === 'server_error'`; `pushed[0].error_code` is one of `['upstream_timeout', 'upstream_5xx', 'internal_error']` (matrix membership, not equality, because mapErrorToCode evolution is allowed within the D-D2 taxonomy); `pushed[0].error_message` is non-null AND does NOT contain `'Bearer '`; `pushed[0].http_status === 502`; `pushed[0].protocol === 'openai'` (chat-completions) or `'anthropic'` (messages).
    - Messages-only: `pushed[0].upstream_message_id === 'msg_01ARZH'` (the literal seeded by the fake generator's message_start event).

    Existing tests in both files remain unchanged.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/integration/chat-completions.stream.test.ts tests/integration/messages.stream.test.ts 2>&1 | tail -20 | grep -E '(Tests|Test Files).*passed' && [ "$(grep -c 'CR-03' router/tests/integration/chat-completions.stream.test.ts)" -ge 1 ] && [ "$(grep -c 'CR-03' router/tests/integration/messages.stream.test.ts)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx vitest run tests/integration/chat-completions.stream.test.ts tests/integration/messages.stream.test.ts` exits 0 with all tests passing (existing + CR-02 from Task 3 + CR-03 from this task).
    - `grep -c 'CR-03' router/tests/integration/chat-completions.stream.test.ts` ≥ 1.
    - `grep -c 'CR-03' router/tests/integration/messages.stream.test.ts` ≥ 1.
    - In each new test: explicit `expect(pushed[0].status_class).toBe('server_error')` assertion (the CR-03 gate).
    - In each new test: explicit `expect(pushed[0].error_message).not.toContain('Bearer ')` assertion (D-D3 redaction gate).
    - In each new test: explicit assertion that the wire status code is 200 (load-bearing: CR-03's inversion is exactly that the WIRE is 200 but the AUDIT must reflect server_error).
    - In messages.stream.test.ts CR-03 test: explicit `expect(pushed[0].upstream_message_id).toBe('msg_01ARZH')` (or the literal seeded — the assertion pins the upstreamMessageId passthrough).
    - Existing test cases in both files remain unmodified.
  </acceptance_criteria>
  <done>
    Both stream test files have new CR-03-named test cases. Mid-stream upstream error after message_start ships is recorded as status_class='server_error' with populated + redacted error_message and correct error_code. The wire status remains 200 (SSE headers already shipped). messages.stream.test.ts asserts upstreamMessageId is preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Coverage-matrix regression gate — typed error → status_class + error_code table-driven test</name>
  <files>router/tests/integration/recordOutcome.test.ts</files>
  <read_first>
    - router/tests/integration/recordOutcome.test.ts (entire — lines 139-300+; the existing `describe('makeRecordRequestOutcome — D-D1 row shape + metric observations', ...)` block uses a `makeDeps()` helper at line 140 returning `{ record, pushed, metrics }`; the table-driven matrix MUST reuse this helper rather than constructing fakes inline)
    - router/src/errors/envelope.ts (the full set of typed error classes — RegistryUnknownModelError, BackendSaturatedError, CapabilityNotSupportedError, InvalidAgentIdError, InvalidToolArgumentsError, InvalidImageUrlError, ImageFetchError; the mapToHttpStatus mapping for each; the constructor signatures the matrix must invoke)
    - router/src/metrics/recordOutcome.ts lines 130-156 (the full mapErrorToCode taxonomy — must match every test row)
    - router/src/concurrency/semaphore.ts (BackendSaturatedError constructor signature)
    - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md "Recommendation" line (the explicit suggestion to add a coverage-matrix test in recordOutcome.test.ts as the regression gate for CR-02 / CR-03)
  </read_first>
  <behavior>
    - A new table-driven describe (preferably using vitest's `it.each` parameterized over a `cases` array) lands at the END of recordOutcome.test.ts. The describe name MUST contain the literal substring `'coverage matrix'` so it is locatable by name and so the verifier's grep gate finds it.
    - The describe body reuses the existing `makeDeps()` helper at recordOutcome.test.ts line 140 (do NOT construct a parallel fakeBuffered inline).
    - The matrix enumerates one row per typed error class. For each row, the test:
      1. Computes `httpStatus = mapToHttpStatus(case.err)`.
      2. Asserts `httpStatus === case.expectedHttpStatus` (mapToHttpStatus contract gate — catches regressions in errors/envelope.ts).
      3. Calls `record({...})` with the OutcomeContext populated from the case row: `statusClass = deriveStatusClass(httpStatus, false)`, `errorCode = mapErrorToCode(case.err)`, `errorMessage = case.err.message`, fixed values for protocol/route/backend/model/durationMs/agentId/requestId/timestamp.
      4. Asserts `pushed.length === 1`.
      5. Asserts `pushed[0].status_class === case.expectedStatusClass`.
      6. Asserts `pushed[0].error_code === case.expectedErrorCode`.
      7. Asserts `pushed[0].http_status === case.expectedHttpStatus`.
      8. Asserts `pushed[0].error_message` is non-null AND does NOT contain `'Bearer '` (D-D3 redaction gate — exercised across all error classes whose seeded message contains a Bearer pattern; APIConnectionError below carries one).
    - The matrix MUST include at minimum the following 8 rows:

    | Error class                                | Constructor                                                                              | expectedStatusClass | expectedErrorCode             | expectedHttpStatus |
    |--------------------------------------------|------------------------------------------------------------------------------------------|---------------------|-------------------------------|--------------------|
    | RegistryUnknownModelError                  | `new RegistryUnknownModelError('foo', ['bar'])` (confirm constructor signature)            | client_error        | unknown_model                 | 404                |
    | BackendSaturatedError                      | `new BackendSaturatedError('ollama', 30000)` (confirm constructor signature)               | client_error        | backend_saturated             | 429                |
    | CapabilityNotSupportedError (vision)        | `new CapabilityNotSupportedError('llama-3-7b', 'vision')` (confirm constructor signature)  | client_error        | model_capability_mismatch     | 400                |
    | InvalidAgentIdError                        | `new InvalidAgentIdError('weird;value')` (confirm constructor signature)                   | client_error        | invalid_request               | 400                |
    | APIConnectionError                         | `new APIConnectionError({ message: 'connect ECONNREFUSED Bearer abc123', cause: ... })`    | server_error        | upstream_timeout              | 502                |
    | APIConnectionTimeoutError                  | `new APIConnectionTimeoutError({ message: 'connect ETIMEDOUT' })`                          | server_error        | upstream_timeout              | 504                |
    | plain Error with statusCode 5xx            | `Object.assign(new Error('upstream bad gateway'), { statusCode: 502 })`                    | server_error        | upstream_5xx                  | 502                |
    | default Error                              | `new Error('boom')`                                                                        | server_error        | internal_error                | 500                |

    - If `InvalidToolArgumentsError` / `InvalidImageUrlError` / `ImageFetchError` have non-trivial constructors that are awkward to invoke in the test, OMIT them from the matrix — the 8 enumerated rows above are sufficient to gate the regression. The existing per-error `it(...)` tests at lines 90-138 of the file already cover those error classes individually (the existing `describe('mapErrorToCode (D-D2 taxonomy)', ...)` block).
  </behavior>
  <action>
    Open `router/tests/integration/recordOutcome.test.ts`. Add a new `describe('coverage matrix — every typed error class produces expected status_class + error_code (regression gate for CR-02 / CR-03)', ...)` block at the END of the file.

    **Structural specification (executor writes the literal `it.each` body matching the file's existing conventions):**

    1. Inside the new describe, declare a `cases` array typed as `Array<{ name: string; err: Error; expectedStatusClass: StatusClass; expectedErrorCode: string; expectedHttpStatus: number }>` with one entry per row in the matrix table from the `<behavior>` block above. Read the actual constructor signature for each error class before invoking it — do NOT guess arg lists; the read_first artifacts (router/src/errors/envelope.ts + router/src/concurrency/semaphore.ts) document them.

    2. Use vitest's `it.each(cases)` with a template-string test name that interpolates each case's `name`, `expectedStatusClass`, `expectedErrorCode`, and `expectedHttpStatus` (e.g. `'$name → status_class=$expectedStatusClass + error_code=$expectedErrorCode + http_status=$expectedHttpStatus'`).

    3. Inside the `it.each` body, the assertions follow the shape spelled out in the `<behavior>` block — call `makeDeps()` (the existing helper at line 140); compute `httpStatus = mapToHttpStatus(case.err)`; assert it matches `case.expectedHttpStatus`; call `record({...})` with the full OutcomeContext populated from the case row and fixed fields for the non-error-related columns (protocol: 'openai', route: '/v1/chat/completions', backend: 'ollama', model: 'llama-3-7b', durationMs: 100, agentId: 'claude-code:cov', requestId based on the error class name, timestamp: new Date()); then assert `pushed.length === 1` and each of the per-field assertions enumerated in `<behavior>` items 5-8 (status_class, error_code, http_status, error_message non-null + `not.toContain('Bearer ')`).

    **Imports to add at the top of recordOutcome.test.ts (most are already present per `grep -n '^import' router/tests/integration/recordOutcome.test.ts` returning lines 14-33; add only the missing ones):**

    - From `'../../src/errors/envelope.js'`: `RegistryUnknownModelError`, `BackendSaturatedError`, `CapabilityNotSupportedError`, `InvalidAgentIdError`, `mapToHttpStatus` (the existing imports at line 15+ already cover several of these — additive only).
    - From `'openai'`: `APIConnectionError`, `APIConnectionTimeoutError` (already imported at line 33 per grep).
    - From `'../../src/metrics/recordOutcome.js'`: `deriveStatusClass`, `mapErrorToCode`, `type StatusClass`, `type OutcomeContext` (most already imported at lines 24-32; type StatusClass may need adding).

    Do NOT modify the existing test cases in the file. Do NOT extract `makeDeps()` into a different scope — it stays where it is and the new describe references it directly (vitest hoists describes inside the same file).

    **Self-check before commit:**
    - `grep -c 'coverage matrix' router/tests/integration/recordOutcome.test.ts` ≥ 1 (the new describe name).
    - `grep -E 'it\.each|describe\.each' router/tests/integration/recordOutcome.test.ts` matches ≥ 1.
    - `cd router && npx vitest run tests/integration/recordOutcome.test.ts` exits 0 with at least 8 new test cases passing in the new describe.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/integration/recordOutcome.test.ts 2>&1 | tail -10 | grep -E '(Tests|Test Files).*passed' && grep -E 'describe\.each|it\.each' router/tests/integration/recordOutcome.test.ts && [ "$(grep -c 'coverage matrix' router/tests/integration/recordOutcome.test.ts)" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx vitest run tests/integration/recordOutcome.test.ts` exits 0 with at least 8 new test cases passing inside the new describe.
    - `grep -E 'it\.each|describe\.each' router/tests/integration/recordOutcome.test.ts` matches ≥ 1.
    - `grep -c 'coverage matrix' router/tests/integration/recordOutcome.test.ts` ≥ 1 (the new describe name).
    - The test asserts `expect(httpStatus).toBe(expectedHttpStatus)` — so if mapToHttpStatus regresses for any covered error class, the test fails BEFORE the status_class assertion.
    - The test asserts `error_message.not.toContain('Bearer ')` — D-D3 redaction regression gate is exercised across all error classes whose seeded message contains a Bearer pattern (APIConnectionError in the matrix).
    - The new describe reuses the existing `makeDeps()` helper at line 140 (no parallel fake construction).
    - Existing test cases in recordOutcome.test.ts remain unmodified.
    - `cd router && npm test` exits 0 with the full suite green (net +8 tests minimum vs pre-fix baseline; combined with Tasks 1, 3, 5 the net is +9 or more, satisfying the plan-level gate on test count).
  </acceptance_criteria>
  <done>
    `recordOutcome.test.ts` has a new coverage-matrix describe that table-drives every typed error class through record() and asserts the expected status_class + error_code + http_status + redaction. This is the future regression gate for both CR-02 (which already tests one error class per route) and CR-03 (same), AND for any new error class added to errors/envelope.ts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → liveness.start(urls) after hot-reload | Internal control plane; trusted; bounded by the existing 10s scheduler interval + 2s probe timeoutMs (RESEARCH Pitfall 5); CR-01 fix doesn't widen the boundary, only restores the postgres entry's presence in the set |
| route → safeRecord(ctx) on pre-stream errors | Internal call; trusted; the ctx values originate from the route's own captured locals (no external input) — no injection vector |
| translator → onCleanup({error}) | Internal callback contract; trusted; the error reference is the same Error instance the catch block already inspects to build the SSE error frame — no new attacker-controlled surface |
| request_log.error_message column | Already subject to D-D3 redaction (truncateAndRedact) in recordOutcome.ts:98-107; CR-02 + CR-03 fixes route through the SAME redaction path; no new exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-30 | Information Disclosure | request_log.error_message after CR-02 / CR-03 writes — risk of bearer/auth/apiKey leak from upstream SDK error messages | mitigate | The fixes route every error_message through the EXISTING `truncateAndRedact` helper (recordOutcome.ts:98) — Bearer/Authorization/apiKey patterns are stripped to `[REDACTED]` and the result is truncated to 500 chars. Coverage-matrix test in Task 6 includes an `APIConnectionError` whose seeded message contains `'Bearer abc123'` and asserts `error_message.not.toContain('Bearer ')` — D-D3 regression gate. |
| T-5-31 | Tampering | The CR-03 widened onCleanup signature could let a future caller spoof a final.error to mislabel a row | accept | The onCleanup callback is only ever invoked by the translator code we control (openai-out.ts / anthropic-out.ts finally blocks); there is no external surface and no untrusted Source. The widening is a strictly-internal contract refinement. |
| T-5-32 | DoS | A misbehaving adapter that rejects every request synchronously could fill the request_log buffer with rows — but the post-fix path emits at most ONE row per request (idempotency via safeRecord `recorded` flag) | accept | Existing D-A1 drop-oldest at 10_000 row cap (bufferedWriter.ts) is the existing safety net. No new amplification factor introduced — without CR-02 the path emits ZERO rows; with CR-02 it emits ONE row. One row per failed request is the correct floor, not a DoS vector. |
| T-5-33 | Repudiation | Without CR-02 / CR-03, the observability seam silently records success for failed requests (CR-03) or omits failed requests entirely (CR-02), enabling an attacker or a buggy agent to evade audit | mitigate (this plan) | This is THE motivating threat for the entire plan — closing it IS the deliverable. After this plan, `SELECT count(*) FROM request_log WHERE status_class IN ('client_error','server_error','disconnect') AND ts > now() - interval '5 min'` will return non-zero on any error-burst, restoring the audit invariant SC2 / D-D4 promised. |
| T-5-34 | DoS | The CR-01 fix could mask a real postgres outage if the fake livenessFactory in the test ignores pool failures | accept | Production code path uses the real LivenessScheduler with the real pgProbe (Promise.race with 1s timeout per app.ts pgProbe definition — see 05-04-PLAN.md interfaces). The test fake is test-only; the production path's existing Pitfall 5 mitigation is unchanged. |
</threat_model>

<verification>
## Gap closure mapping

This plan closes the three CRs verbatim per VERIFICATION.md. Re-running `/gsd-verify-phase 05` after this plan lands should re-mark:

- **SC2 — every completed request after bearer auth produces a request_log row (D-D4)**: from `partial` → `verified` (CR-02 closes the stream pre-stream gap; CR-03 closes the mid-stream upstream gap; coverage matrix gates regressions).
- **SC5 — docker compose ps healthy + X-Agent-Id round-trip**: from `partial` → `verified` (CR-01 restores the hot-reload postgres probe).

## Plan-level automated gates

The following gates are plan-level exit conditions (not user-observable system truths — those live in `must_haves.truths`). They are the executor's contract for "plan is done":

- `cd router && npx tsc --noEmit` exits 0 after each task and at plan completion.
- `cd router && npm test` — full suite green at plan completion; net +9 or more new tests vs the pre-fix baseline (473 passed, 2 skipped per VERIFICATION.md → expected ≥ 482 passed, 2 skipped). Distribution: Task 1 (+1 hotreload), Task 3 (+4 CR-02 across the two stream files), Task 5 (+2 CR-03 across the two stream files), Task 6 (+8 coverage matrix) ≥ +15 total but at least +9 must land to satisfy the gate.
- The 7-step live-stack operator UAT (`.planning/phases/05-postgres-observability-seam/05-HUMAN-UAT.md`) is UNCHANGED by this plan. This plan adds zero `checkpoint:human-verify` tasks. Operator re-runs the live-stack smoke harness after this plan lands; that execution is out of scope.

Grep regression gates (all checked in the per-task automated `<verify>` blocks; collated here for the plan-level audit):

  - `grep -v '^\s*//' router/src/index.ts | grep -c 'POSTGRES_PROBE_URL'` ≥ 2 (CR-01 fix in non-comment lines).
  - `grep -c 'if (body.stream !== true)' router/src/routes/v1/chat-completions.ts` == 0 (CR-02 fix — guard dropped).
  - `grep -c 'if (body.stream !== true)' router/src/routes/v1/messages.ts` == 0 (CR-02 fix — guard dropped).
  - `grep -c 'safeRecord(' router/src/routes/v1/chat-completions.ts` ≥ 4 (CR-02 fix — added two new calls).
  - `grep -c 'safeRecord(' router/src/routes/v1/messages.ts` ≥ 4 (CR-02 fix — added two new calls).
  - `grep -c 'error?: Error' router/src/translation/openai-out.ts` ≥ 1 (CR-03 fix — widened signature).
  - `grep -c 'error?: Error' router/src/translation/anthropic-out.ts` ≥ 1 (CR-03 fix — widened signature).
  - `grep -c 'caughtErr' router/src/translation/openai-out.ts` ≥ 3 (CR-03 fix — declaration + catch + finally).
  - `grep -c 'caughtErr' router/src/translation/anthropic-out.ts` ≥ 3 (CR-03 fix — same).
  - `grep -c 'final?.error' router/src/routes/v1/chat-completions.ts` ≥ 1 (CR-03 fix — sseCleanup override).
  - `grep -c 'final?.error' router/src/routes/v1/messages.ts` ≥ 1 (CR-03 fix — sseCleanup override).
  - `grep -c 'CR-01' router/tests/integration/hotreload.test.ts` ≥ 1 (Task 1 test).
  - `grep -c 'CR-02' router/tests/integration/chat-completions.stream.test.ts` ≥ 2 (Task 3 tests — CR-02-A + CR-02-B).
  - `grep -c 'CR-02' router/tests/integration/messages.stream.test.ts` ≥ 2 (Task 3 tests).
  - `grep -c 'CR-03' router/tests/integration/chat-completions.stream.test.ts` ≥ 1 (Task 5 test).
  - `grep -c 'CR-03' router/tests/integration/messages.stream.test.ts` ≥ 1 (Task 5 test).
  - `grep -c 'coverage matrix' router/tests/integration/recordOutcome.test.ts` ≥ 1 (Task 6 describe).
  - `grep -E 'it\.each|describe\.each' router/tests/integration/recordOutcome.test.ts` matches ≥ 1 (Task 6 table-driven shape).

## Deferred — operator UAT (no checkpoint in this plan)

The 7-step live-stack UAT in `.planning/phases/05-postgres-observability-seam/05-HUMAN-UAT.md` stays deferred. After this plan lands, the operator re-runs the smoke harness against the live stack. If CR-02 / CR-03 closure is to be evidenced in production, the operator can additionally:
- Trigger CR-02 path: send a streaming POST with an unknown model name → expect `psql -c "SELECT count(*) FROM request_log WHERE status_class='client_error' AND ts > now() - interval '5 min';"` to be non-zero.
- Trigger CR-03 path: kill the upstream ollama container mid-stream → expect the resulting request_log row to have `status_class='server_error'` (NOT 'success').

These are operator-only and out of scope for this plan.
</verification>

<success_criteria>
- [ ] CR-01 fixed + tested (Task 1): `router/src/index.ts` onReload includes `POSTGRES_PROBE_URL` in `app.liveness.start(urls)` when pool is configured; mirrors app.ts:308-311 verbatim; new CR-01-named test in `hotreload.test.ts` passes against the fixed code.
- [ ] CR-02 fixed in both routes (Task 2): inner pre-stream catch calls `safeRecord` BEFORE returning the JSON envelope (NO_ENVELOPE branch records as `disconnect`; regular branch records with `mapErrorToCode + errorMessage`); the `body.stream !== true` finally guard is dropped in both files.
- [ ] CR-02 tests landed (Task 3): both stream test files have new CR-02-named tests (≥ 2 per file) that assert `pushed.length === 1` with correct status_class + error_code + redacted error_message.
- [ ] CR-03 fixed in translators AND routes (Task 4): both `openai-out.ts` and `anthropic-out.ts` widen `onCleanup` to accept `error?: Error`; catch blocks capture `caughtErr`; finally blocks pass it through; `anthropic-out.ts` ALSO preserves `upstreamMessageId`; both routes' `sseCleanup` override statusClass/errorCode/errorMessage when `final?.error` is present using existing `mapToHttpStatus + mapErrorToCode` helpers (no duplication).
- [ ] CR-03 tests landed (Task 5): both stream test files have new CR-03-named tests that assert `status_class === 'server_error'` (NOT 'success'), redacted error_message, and (messages.ts) preserved upstream_message_id.
- [ ] Coverage-matrix regression gate landed (Task 6): `recordOutcome.test.ts` has a new describe.each / it.each block enumerating every typed error class → expected (status_class, error_code, http_status, redaction) tuple.
- [ ] `cd router && npx tsc --noEmit` exits 0 with zero diagnostics.
- [ ] `cd router && npm test` exits 0 with full suite green (≥ 482 passed, 2 skipped; net +9 or more vs 473 pre-fix baseline).
- [ ] All threat-model T-5-30 through T-5-34 dispositions reflected in the implementation (especially T-5-30 D-D3 redaction gate exercised by Task 6).
- [ ] 7-step operator UAT in `05-HUMAN-UAT.md` STAYS DEFERRED — this plan adds zero checkpoints.
- [ ] Next `/gsd-verify-phase 05` re-marks SC2 + SC5 as `verified` and Phase 5 score moves from 2/5 → 5/5.
</success_criteria>

<output>
After completion, create `.planning/phases/05-postgres-observability-seam/05-05-SUMMARY.md` documenting:
- Three CR-* gap closures (CR-01 hot-reload postgres probe; CR-02 stream pre-stream observability; CR-03 mid-stream upstream status_class fidelity), each spanning the task numbers that delivered them (CR-01 → Task 1; CR-02 → Tasks 2+3; CR-03 → Tasks 4+5).
- The explicit decision to DROP the `body.stream !== true` finally guard (vs keep-as-belt-and-suspenders) per VERIFICATION.md gaps[1].missing[2] recommendation — and the structural rationale (safeRecord `recorded` flag is the idempotency mechanism).
- The translator `onCleanup` signature widening (`error?: Error`) is a strictly-internal contract refinement; no public API surface change.
- The coverage-matrix regression gate in `recordOutcome.test.ts` (Task 6) is the FUTURE regression gate — any new typed error class added to `errors/envelope.ts` MUST be added to the matrix (or a sibling `describe('mapErrorToCode')` case) or the gate will silently drift.
- Test count delta: +9 or more new tests across `hotreload.test.ts` (+1), `chat-completions.stream.test.ts` (+3: CR-02-A, CR-02-B, CR-03), `messages.stream.test.ts` (+3), `recordOutcome.test.ts` (+8 in the matrix).
- The 7-step operator UAT stays deferred. After this plan lands, the operator re-runs the live-stack smoke harness; the negative-path CR triggers documented in VERIFICATION.md "Caveat for the operator" become the load-bearing live-stack regression gates.
- Open hand-offs: Phase 6 (Traefik) is unaffected by this plan; Phase 9 (OPS-02..04) is unaffected. The recommendation to add CR-02 / CR-03 negative-path scenarios to `bin/smoke-test-router.sh` (per VERIFICATION.md "Recommendation" line) is OUT of scope for this plan — surface as a Phase 5 follow-up if the operator wants automated live-stack negative-path gates.
</output>
