---
phase: 05-postgres-observability-seam
plan: 06
type: execute
wave: 1
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - bin/smoke-test-router.sh
  - router/src/db/bufferedWriter.ts
  - router/tests/unit/bufferedWriter.test.ts
  - .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [DATA-03, DATA-05, OBS-05]
tags: [gap-closure, smoke-script, bufferedwriter-drain, shutdown-flush, verification-housekeeping]

must_haves:
  truths:
    - "bin/smoke-test-router.sh runs to completion against a live --profile ollama stack without Python 3.12 SyntaxError on SC-P4-A/C/E."
    - "SC-P5-E asserts postgres.status from /readyz JSON body, not the overall HTTP code (so it works correctly when llamacpp is permanently down)."
    - "SC-P4-D issues a `skip` (not `fail`) when the vision model is not pulled — matching the existing SKIP idiom."
    - "OBS-05 excludes pg-backup from the unhealthy-count grep (pg-backup is a no-healthcheck sidecar per Plan 03 D-F2)."
    - "bufferedWriter.drain() actually flushes buffered rows on graceful shutdown — the in-process FIFO is drained to postgres before the writer is stopped."
    - "router/tests/unit/bufferedWriter.test.ts has a regression test that fails against the broken drain() and passes against the fixed drain()."
    - "05-VERIFICATION.md status reflects the post-UAT reality (live evidence has been gathered; the deferred bufferedWriter.drain item is closed)."
  artifacts:
    - path: "bin/smoke-test-router.sh"
      provides: "Operator-facing smoke runs clean against a live --profile ollama stack with no Python 3.12 syntax errors, no false-negative OBS-05 failures, and SC-P5-E gated on postgres.status."
    - path: "router/src/db/bufferedWriter.ts"
      provides: "drain() flushes the buffer end-to-end before setting stopped=true; SIGTERM no longer silently drops queued request_log rows."
    - path: "router/tests/unit/bufferedWriter.test.ts"
      provides: "A failing-first regression test (Test 8) proving drain() inserts a non-empty buffer's rows into the (mock) db before resolving."
    - path: ".planning/phases/05-postgres-observability-seam/05-VERIFICATION.md"
      provides: "Updated status frontmatter referencing 05-UAT.md as the live evidence trail; deferred bufferedWriter.drain item closed with a pointer to this plan's commits."
  key_links:
    - from: "router/src/db/bufferedWriter.ts drain()"
      to: "opts.db.insert(requestLog).values(batch)"
      via: "flush({ force: true }) bypasses the stopped-flag early-return during drain (default) or await flush() runs before stopped=true (alternative)"
      pattern: "flush\\(\\{ ?force|await flush\\(.*\\).*stopped = true"
    - from: "router/tests/unit/bufferedWriter.test.ts Test 8 (new)"
      to: "the inserts[] mock array gathered from makeMockDb"
      via: "push() + drain() with the timer paused at 0ms — drain MUST trigger an insert with the buffered rows even though the 1s interval never fired"
      pattern: "drain\\([0-9]+\\).*inserts.*length.*toBe\\(1\\)"
    - from: "bin/smoke-test-router.sh OBS-05 grep -vE"
      to: "the list of legitimately-no-healthcheck services"
      via: "regex extension covering both gpu-preflight (one-shot) and pg-backup (fire-and-forget sidecar, Plan 03 D-F2)"
      pattern: "grep -vE 'healthy\\|gpu-preflight\\|pg-backup'"
---

<objective>
Close the residual gaps surfaced by the live-stack 05-UAT.md execution against Phase 5:

  1. Operator-script polish in bin/smoke-test-router.sh (4 small defects, all pre-existing — exposed by the live run, not introduced by Phase 5 code).
  2. The genuine Phase 5 code defect that 05-VERIFICATION.md flagged as `deferred`: bufferedWriter.drain() does NOT flush — it sets `stopped = true` BEFORE awaiting flush(), and flush() early-returns on `stopped===true`. On SIGTERM, ALL buffered request_log rows are silently dropped. Unit tests at router/tests/unit/bufferedWriter.test.ts encode the broken behavior as passing, so the regression is invisible.
  3. Verification housekeeping — update 05-VERIFICATION.md to reflect that the live UAT has run with `result: pass` on 9/10 tests and the bufferedWriter.drain deferred item has been closed by this plan.

Purpose: Promote Phase 5 cleanly. The smoke script must run clean operator-side so future ops cycles aren't reading false positives, and the SIGTERM data-loss path must be fixed because it breaches the D-A invariant ("buffered writes never drop on graceful shutdown") that SC2 implicitly relies on for shutdown semantics.

Output: One commit per task (six task commits total). Smoke script polish, bufferedWriter fix + failing-first regression test, and the VERIFICATION.md housekeeping update.

Scope guard: This plan does NOT add features, does NOT introduce new dependencies, does NOT change the API surface. It is in-place edits to four files. No researchers needed — every decision is captured in 05-VERIFICATION.md (deferred block + Anti-Patterns table line 168), 05-UAT.md (Gaps section), and 05-CONTEXT.md (D-A1..D-A7 + D-F2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-postgres-observability-seam/05-CONTEXT.md
@.planning/phases/05-postgres-observability-seam/05-VERIFICATION.md
@.planning/phases/05-postgres-observability-seam/05-UAT.md
@.planning/phases/05-postgres-observability-seam/05-05-SUMMARY.md
@router/src/db/bufferedWriter.ts
@router/tests/unit/bufferedWriter.test.ts
@bin/smoke-test-router.sh

<interfaces>
<!-- Key contracts the executor must respect. Extracted from the codebase. -->

From router/src/db/bufferedWriter.ts (current, broken):
```typescript
export interface BufferedWriter {
  push(row: RequestLogInsert): void;
  drain(timeoutMs?: number): Promise<void>;
  readonly size: number;
}

// The bug — drain() body lines 147-167:
async drain(timeoutMs = 3_000): Promise<void> {
  stopped = true;                            // <-- set BEFORE await flush()
  clearInterval(timer);
  await Promise.race([
    flush(),                                 // <-- flush() short-circuits because stopped===true
    new Promise<void>((resolve) => setTimeout(resolve, timeoutMs)),
  ]);
  if (buf.length > 0) {
    opts.logger.warn(
      { event: 'log_buffer_shutdown_drop', buffered_at_shutdown: buf.length },
      'drain timeout — dropping buffered rows',
    );
  }
}

// And flush() at line 98-99 early-returns when stopped===true:
const flush = async (): Promise<void> => {
  if (flushing || buf.length === 0 || stopped) return;
  // ...
};
```

From router/src/db/bufferedWriter.ts (D-A1..D-A7 invariants the fix must preserve):
- D-A1 — Bounded FIFO with drop-oldest on overflow (capacity default 10_000).
- D-A2 — Dual-trigger flush: 1s setInterval OR 200-row queueMicrotask.
- D-A3 — Multi-row INSERT via Drizzle .values(batch), batch cap 1_000.
- D-A4 — drain(3_000) on SIGTERM raced against setTimeout(timeoutMs); warn `log_buffer_shutdown_drop` if buf has leftovers after the race.
- D-A5 — In-process, NOT a worker_thread.
- D-A6 — Single shared FIFO across all backends.
- D-A7 — Flush failure: rows STAY in buffer (buf.unshift(...batch)); droppedCounter NOT incremented.

From bin/smoke-test-router.sh OBS-05 (line 1121, current):
```bash
UNHEALTHY_LINES=$(docker compose ps --format '{{.Name}} {{.Health}}' 2>/dev/null | grep -vE 'healthy|gpu-preflight' || true)
```

From bin/smoke-test-router.sh SC-P5-E (line 1082-1083, current — over-asserts):
```bash
READYZ_BEFORE=$(curl -s -o /dev/null -w '%{http_code}' "${ROUTER_URL}/readyz")
[[ "${READYZ_BEFORE}" == "200" ]] && pass "..." || fail "..."
# Same overall-code check at lines 1108-1115 after the unpause
```

From bin/smoke-test-router.sh SC-P4-A/C/E inline-python (lines 697-708, 773-781, 882-892):
```python
# Backslash-quoted f-string inside `python3 -c '...'` (single-quoted heredoc):
#   f"id does not start with msg_: {d.get(\"id\")}"
# Python 3.12 raises SyntaxError: unexpected character after line continuation character
```

From bin/smoke-test-router.sh SC-P4-D existing SKIP idiom at line 806-807:
```bash
if ! docker compose exec -T ollama ollama list 2>/dev/null | grep -q "${VISION_MODEL}"; then
  skip "SC-P4-D: vision model not pulled; run: docker compose exec ollama ollama pull ${VISION_MODEL}"
```
This pre-check ALREADY exists. The UAT report cites SC-P4-D failing cascade-style; verify whether the failing case is actually the model-not-pulled branch (the operator may have hit a transient 404 from the image URL, in which case the `fail "SC-P4-D: empty vision response"` branch needs the same `skip` treatment when the response indicates "model not found"). See Task 1.4 for the exact assertion.

From bin/smoke-test-router.sh helpers (lines 106-107, 662):
```bash
fail() { echo "[smoke-test-router] FAIL: $*" >&2; FAILURES=$((FAILURES + 1)); }
pass() { echo "[smoke-test-router] PASS: $*"; }
skip() { echo "[smoke-test-router] SKIP: $*"; SKIPS=$((SKIPS + 1)); }
```
</interfaces>
</context>

<tasks>

<!-- ====================================================================== -->
<!-- Gap A — bin/smoke-test-router.sh operator-script polish               -->
<!-- ====================================================================== -->

<task type="auto">
  <name>Task 1: Fix Python 3.12 f-string escapes in SC-P4-A / SC-P4-C / SC-P4-E inline python</name>
  <files>bin/smoke-test-router.sh</files>
  <action>
    Locate the inline `python3 -c '...'` heredocs at SC-P4-A (lines ~697-708), SC-P4-C (lines ~773-781), and SC-P4-E (lines ~882-892). Each uses backslash-quoted f-strings of the form `f"...{d.get(\"id\")}"` inside a single-quoted shell heredoc. Python 3.12's stricter parser rejects the backslash-escaped double quote inside an f-string placeholder with `SyntaxError: unexpected character after line continuation character`.

    Fix approach (whichever is cleanest for each site; pick consistently): use single quotes inside f-string placeholders, OR extract the value to an intermediate variable on the preceding line, OR fall back to `.format()`. Examples:

      # Before (broken):
      print(f"OK id={d[\"id\"][:14]}... in={d[\"usage\"][\"input_tokens\"]} out={d[\"usage\"][\"output_tokens\"]}")

      # After (using single quotes inside f-string placeholders):
      print(f"OK id={d['id'][:14]}... in={d['usage']['input_tokens']} out={d['usage']['output_tokens']}")

    Apply the same conversion to every `{d.get(\"...\")}` and `{d[\"...\"]...}` occurrence in the three SC-P4-A/C/E python blocks. Do NOT touch SC-P4-B or SC-P4-D unless they also contain the broken pattern (grep first: `grep -n 'd\.get(\\"\|d\[\\\"' bin/smoke-test-router.sh`).

    Preserve every assertion's behavior verbatim — this is a syntax fix, not a logic change.
  </action>
  <verify>
    <automated>
      bash -n bin/smoke-test-router.sh &&
      grep -c 'd\.get(\\"' bin/smoke-test-router.sh | grep -E '^0$' &&
      grep -c 'd\[\\"' bin/smoke-test-router.sh | grep -E '^0$' &&
      python3 -c "
import re, sys
src = open('bin/smoke-test-router.sh').read()
# Extract each python3 -c '...' block and try to parse it.
blocks = re.findall(r\"python3 -c '([^']+)'\", src)
errors = []
for i, b in enumerate(blocks):
  try:
    compile(b, f'<block-{i}>', 'exec')
  except SyntaxError as e:
    errors.append((i, str(e)))
if errors:
  for i, msg in errors:
    print(f'block {i}: {msg}', file=sys.stderr)
  sys.exit(1)
print(f'OK — {len(blocks)} python3 -c blocks parse cleanly')
"
    </automated>
  </verify>
  <done>
    `bash -n bin/smoke-test-router.sh` exits 0. Zero remaining `d.get(\"...")` or `d[\"...\"]` patterns in the file. Every `python3 -c '...'` block in the script parses cleanly under the host's Python 3 (3.12 specifically — the live UAT host).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SC-P5-E to gate on body.postgres.status instead of overall /readyz HTTP code</name>
  <files>bin/smoke-test-router.sh</files>
  <action>
    SC-P5-E currently asserts `READYZ_BEFORE == "200"` (line 1083), `READYZ_PAUSED_CODE == "503"` (line 1093), and `READYZ_UNPAUSED_CODE == "200"` (line 1112). In a `--profile ollama` deployment llamacpp is intentionally not running, so /readyz's overall `not_ready` field is true and the HTTP code is permanently 503 regardless of postgres state. The script's middle assertion (line 1099 — "/readyz response includes 'postgres' field") was already correct.

    Rewrite the three overall-code gates to JSON-body gates against `body.postgres.status`. Use the existing helper pattern (curl into a body variable, then parse with python3 -c). The contract for body.postgres.status from router/src/routes/readyz.ts:
      - "alive" when postgres pool.query('SELECT 1') succeeds within the liveness probe window
      - "down" when the probe has failed (or postgres is paused/unreachable)

    Concretely:

      # Replace lines ~1082-1083:
      READYZ_BEFORE_BODY=$(curl -s "${ROUTER_URL}/readyz")
      READYZ_BEFORE_PG=$(echo "${READYZ_BEFORE_BODY}" | python3 -c 'import json,sys; print(json.load(sys.stdin).get("postgres",{}).get("status",""))')
      [[ "${READYZ_BEFORE_PG}" == "alive" ]] \
        && pass "SC-P5-E: postgres.status=alive (healthy stack)" \
        || fail "SC-P5-E: postgres.status=${READYZ_BEFORE_PG} at start (expected alive) — body: ${READYZ_BEFORE_BODY:0:200}"

      # Replace the wait-for-503 loop (~lines 1087-1097) with a wait-for-down loop using the same parser.
      # Replace the wait-for-200 loop (~lines 1107-1115) with a wait-for-alive loop using the same parser.

    Keep the existing "/readyz response includes 'postgres' field" assertion at line 1099-1102 verbatim — it's correct.

    Keep the 25-attempt × 1s sleep window (≈ 2× the 10s scheduler interval from Plan 05-04). Keep the pause/unpause `docker compose pause postgres` / `unpause postgres` commands verbatim.

    Update the PASS/FAIL messages to reference postgres.status, e.g.:
      pass "SC-P5-E: postgres.status -> down within ${attempt}s of postgres pause"
      pass "SC-P5-E: postgres.status -> alive within ${attempt}s of postgres unpause"
  </action>
  <verify>
    <automated>
      bash -n bin/smoke-test-router.sh &&
      grep -c 'READYZ_BEFORE.*== "200"' bin/smoke-test-router.sh | grep -E '^0$' &&
      grep -c 'READYZ_PAUSED_CODE.*== "503"' bin/smoke-test-router.sh | grep -E '^0$' &&
      grep -c 'READYZ_UNPAUSED_CODE.*== "200"' bin/smoke-test-router.sh | grep -E '^0$' &&
      grep -c 'postgres.*status.*alive\|postgres.*status.*down' bin/smoke-test-router.sh | awk '$1 >= 3 {exit 0} {exit 1}'
    </automated>
  </verify>
  <done>
    `bash -n` clean. The three overall-HTTP-code gates are gone from SC-P5-E. At least three new gates against `postgres.status` (initial alive, pause→down, unpause→alive) are present. The "postgres field included" middle assertion at line 1099-1102 is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend OBS-05 grep -vE to exclude pg-backup alongside gpu-preflight</name>
  <files>bin/smoke-test-router.sh</files>
  <action>
    At line ~1121, the OBS-05 grep currently reads:
      UNHEALTHY_LINES=$(docker compose ps --format '{{.Name}} {{.Health}}' 2>/dev/null | grep -vE 'healthy|gpu-preflight' || true)

    Extend the regex to also exclude `pg-backup`. Plan 03 D-F2 / 05-03-SUMMARY documents pg-backup as a fire-and-forget sidecar with no healthcheck by design — same exclusion category as gpu-preflight's one-shot.

    Fix:
      UNHEALTHY_LINES=$(docker compose ps --format '{{.Name}} {{.Health}}' 2>/dev/null | grep -vE 'healthy|gpu-preflight|pg-backup' || true)

    Also update the matching PASS message (line ~1124) from
      pass "OBS-05: all long-running services healthy (gpu-preflight excluded as one-shot)"
    to
      pass "OBS-05: all long-running services healthy (gpu-preflight + pg-backup excluded by design — see Plan 01 D-G1 + Plan 03 D-F2)"

    Do not touch the surrounding `wc -l`/`grep -c .` logic — the regex extension is the entire change.
  </action>
  <verify>
    <automated>
      bash -n bin/smoke-test-router.sh &&
      grep -c "grep -vE 'healthy|gpu-preflight|pg-backup'" bin/smoke-test-router.sh | awk '$1 == 1 {exit 0} {exit 1}' &&
      grep -c "gpu-preflight + pg-backup excluded" bin/smoke-test-router.sh | awk '$1 == 1 {exit 0} {exit 1}'
    </automated>
  </verify>
  <done>
    OBS-05 grep -vE includes all three terms (healthy, gpu-preflight, pg-backup). PASS message references both exclusions and their plan-of-record citations. `bash -n` clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Make SC-P4-D skip gracefully when the response indicates "model not pulled"</name>
  <files>bin/smoke-test-router.sh</files>
  <action>
    SC-P4-D already has a pre-flight `docker compose exec ollama ollama list | grep -q "${VISION_MODEL}"` check at line ~806 that issues a `skip`. The UAT report cites SC-P4-D failing despite this — the failure mode is a downstream cascade where the live curl call to /v1/messages returns an error envelope that the parser then treats as a hard FAIL.

    Add a second skip branch covering the case where the response IS present but indicates "model not found" — typically the router returns a 404 envelope with `error.type == "model_not_found"` or "model not loaded" in the message. Modify the response-validation block at lines ~835-849 so that BEFORE the assertion on `d["content"][0]["text"]`, we detect the model-not-found case and convert it to a skip.

    Concretely, insert a pre-check using the existing python3-c idiom right after the `if [[ -z "${SCP4D_RESP}" ]]` empty-response guard:

      # Pre-check: if the response is an error envelope citing model_not_found / not loaded,
      # treat as skip (operator did not pull the vision model) rather than fail.
      SCP4D_PREFLIGHT=$(SCP4D_RESP="${SCP4D_RESP}" python3 -c '
      import json, os
      try:
        d = json.loads(os.environ["SCP4D_RESP"])
        # Anthropic-style error envelope from the router
        if isinstance(d, dict) and d.get("type") == "error":
          err = d.get("error", {})
          etype = err.get("type", "")
          emsg = (err.get("message") or "").lower()
          if etype == "model_not_found" or "not found" in emsg or "not loaded" in emsg or "pull" in emsg:
            print("SKIP_MODEL_NOT_PULLED")
          else:
            print("OK")
        else:
          print("OK")
      except Exception:
        print("OK")
      ')
      if [[ "${SCP4D_PREFLIGHT}" == "SKIP_MODEL_NOT_PULLED" ]]; then
        skip "SC-P4-D: vision model returned model_not_found from router; run: docker compose exec ollama ollama pull ${VISION_MODEL}"
      else
        # ... existing SCP4D_CHECK validation block unchanged ...
      fi

    Preserve the existing success-path validation verbatim. Do not modify SC-P4-A/B/C/E in this task — Task 1 already handles their syntax fix.

    Note: the existing pre-flight `ollama list` check at line ~806 stays in place — it handles the "model never pulled" branch. This task handles the "ollama list lists it but the router can't load it" / "transient model_not_found at request time" branch.
  </action>
  <verify>
    <automated>
      bash -n bin/smoke-test-router.sh &&
      grep -c 'SKIP_MODEL_NOT_PULLED' bin/smoke-test-router.sh | awk '$1 >= 2 {exit 0} {exit 1}'
    </automated>
  </verify>
  <done>
    `bash -n` clean. SC-P4-D has a second skip branch that fires when the response indicates model_not_found / not loaded, mirroring the existing pre-flight skip pattern. The success-path SCP4D_CHECK assertion remains untouched.
  </done>
</task>

<!-- ====================================================================== -->
<!-- Gap B — bufferedWriter.drain() flush bug (real Phase 5 defect)         -->
<!-- ====================================================================== -->

<task type="auto" tdd="true">
  <name>Task 5: Add failing-first regression test proving drain() flushes a non-empty buffer</name>
  <files>router/tests/unit/bufferedWriter.test.ts</files>
  <behavior>
    - Test 8 (new): `drain() flushes a non-empty buffer end-to-end before resolving (D-A invariant — no silent SIGTERM data loss)`.
    - Setup: makeDeps() + makeBufferedWriter() with default flushIntervalMs=1_000 (DO NOT advance the timer past 0ms — the interval flush MUST NOT fire on its own; we want drain() itself to be the sole trigger).
    - Push 3 rows: `w.push(row(1)); w.push(row(2)); w.push(row(3));`. Verify `inserts.length === 0` (no flush has been triggered yet).
    - Verify `w.size === 3` (buffer holds all three).
    - Call `const drainPromise = w.drain(3_000);` — but DO NOT await yet.
    - Within the same async tick: advance `await vi.advanceTimersByTimeAsync(0);` to flush microtasks. Assert `inserts.length === 1` (drain triggered the flush directly) and `(inserts[0]?.rows ?? []).length === 3`.
    - Resolve the insert: `inserts[0]?.resolve();`.
    - `await drainPromise;` — must resolve cleanly within the 3s budget.
    - Assert `w.size === 0` (all rows were flushed).
    - Assert `logger.warn` was NOT called with `event === 'log_buffer_shutdown_drop'` (because nothing was dropped).
    - This test MUST FAIL against the current (broken) bufferedWriter.ts where drain() sets stopped=true before await flush() — flush() short-circuits, no insert is emitted, the rows stay in buf, the timeout race wins, and the `log_buffer_shutdown_drop` warn fires with buffered_at_shutdown=3.

    Additionally, audit existing Test 7 (lines 313-336): the current Test 7 asserts that push() after drain() is a no-op. That contract MUST be preserved by the Task 6 fix. Add an inline comment at the top of Test 7 noting the stopped-flag invariant: "Test 7 gates the stopped-flag contract — push() after drain() must be a no-op. Test 8 (above) gates the orthogonal drain-flushes-buffer contract. Both must hold simultaneously after the Task 6 fix."

    Run the test suite WITHOUT making any production change yet. Confirm Test 8 fails with a clear diagnostic ("expected inserts.length to be 1, got 0" or equivalent). This is the RED step of red-green-refactor.

    DO NOT modify router/src/db/bufferedWriter.ts in this task. The production fix is Task 6.
  </behavior>
  <action>
    Add a new `it('8. ...', async () => { ... })` block at the bottom of the `describe('makeBufferedWriter', ...)` suite in router/tests/unit/bufferedWriter.test.ts, after the existing Test 7. Follow the existing numbered `it('N. ...')` pattern (mirrors semaphore.test.ts / liveness.test.ts conventions per the file's header comment at lines 1-18).

    Use the existing makeDeps() / makeMockDb() / row() helpers — do NOT duplicate them. The test body should follow the structure in `<behavior>` above precisely:
      1. push 3 rows
      2. assert no inserts yet, size === 3
      3. call drain(3_000) without awaiting
      4. await vi.advanceTimersByTimeAsync(0) to flush microtasks
      5. assert inserts.length === 1 and inserts[0].rows.length === 3
      6. resolve the insert
      7. await drainPromise
      8. assert size === 0 and no shutdown-drop warn

    Add the Test 7 audit comment as described in `<behavior>`.

    Commit with message: `test(05-06): add failing regression for bufferedWriter.drain() silent-drop bug`.

    Run vitest to confirm Test 8 fails (and ONLY Test 8 fails — all 7 existing tests still pass). Capture the diagnostic in the commit message body if useful.
  </action>
  <verify>
    <automated>
      (cd router && npx vitest run tests/unit/bufferedWriter.test.ts --reporter=verbose 2>&1 | tee /tmp/.vitest-task5.log && \
       grep -c "8\. drain() flushes" /tmp/.vitest-task5.log | awk '$1 >= 1 {exit 0} {exit 1}' && \
       grep -E "FAIL|Test Files.*failed|Tests.*failed" /tmp/.vitest-task5.log | head -5)
    </automated>
  </verify>
  <done>
    Test 8 is present in router/tests/unit/bufferedWriter.test.ts. Running vitest shows it FAILS (RED) with a diagnostic about the insert count or buffered_at_shutdown warn. All existing tests 1-7 still PASS. The commit message captures the RED phase. Production code in router/src/db/bufferedWriter.ts is unchanged in this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Fix drain() so flush() runs to completion before stopped=true is set</name>
  <files>router/src/db/bufferedWriter.ts</files>
  <behavior>
    - Test 8 from Task 5 transitions RED -> GREEN.
    - Tests 1-7 remain GREEN (no regression to existing invariants D-A1..D-A7).
    - Specifically: Test 7 (`push() after drain() is a no-op`) still passes — the stopped-flag contract is preserved.
    - Test 6 (drain race with hung flush + leftover row) still passes — the `log_buffer_shutdown_drop` warn still fires for buf.length > 0 after the timeout.
    - The `flushing` re-entrancy lock (Pitfall 1) still prevents overlapping flushes during drain.
    - The drop-oldest counter contract (D-A1) is untouched.
  </behavior>
  <action>
    Fix the drain() bug in router/src/db/bufferedWriter.ts. **Use Option B (force-flag through flush) as the default — it is the minimal-surface-area fix, preserves the original race shape verbatim, has no microtask-spin-loop livelock risk under Test 6's hung-flush fake-timer fixture, and aligns with 05-REVIEW.md's recommendation to separate the push-stopping gate from the flush-stopping gate.**

    **Option B (DEFAULT — recommended; this is the path the executor should take unless they have an explicit reason to deviate):**

    Add an optional `force` parameter to flush() that bypasses the stopped-flag early-return, and pass `force: true` from drain():

      const flush = async (opts?: { force?: boolean }): Promise<void> => {
        const force = opts?.force === true;
        if (flushing || buf.length === 0 || (stopped && !force)) return;
        flushing = true;
        const batch = buf.splice(0, Math.min(buf.length, MAX_BATCH_ROWS));
        try {
          // Multi-row parameterized INSERT via Drizzle (D-A3).
          await opts_outer.db.insert(requestLog).values(batch);  // <-- rename outer opts var if needed
        } catch (err) {
          // ... unchanged ...
        } finally {
          flushing = false;
        }
      };

      // Note: the existing `opts.db.insert(...)` reference inside flush() collides with the new
      // `opts?: { force?: boolean }` parameter name. Resolve by either (a) renaming the parameter
      // to `flushOpts?: { force?: boolean }` so the outer `opts` (the writer config) is still
      // reachable, or (b) destructuring `force` at the top of flush(). Pick (a) — it's the
      // smaller diff and keeps `opts.db`, `opts.logger`, `opts.droppedCounter` references
      // throughout flush() body untouched. Updated signature:
      //
      //   const flush = async (flushOpts?: { force?: boolean }): Promise<void> => {
      //     const force = flushOpts?.force === true;
      //     if (flushing || buf.length === 0 || (stopped && !force)) return;
      //     ...
      //   };

    Drain() body becomes (minimal diff — only the flush() call changes, everything else verbatim):

      async drain(timeoutMs = 3_000): Promise<void> {
        // Idempotent: subsequent push() calls become no-ops; subsequent drain()
        // is allowed (the second call also waits flush + race, which is cheap
        // when buf is already empty).
        stopped = true;
        clearInterval(timer);
        await Promise.race([
          flush({ force: true }),        // <-- force=true bypasses the (stopped && !force) early-return
          new Promise<void>((resolve) => {
            setTimeout(resolve, timeoutMs);
          }),
        ]);
        if (buf.length > 0) {
          opts.logger.warn(
            { event: 'log_buffer_shutdown_drop', buffered_at_shutdown: buf.length },
            'drain timeout — dropping buffered rows',
          );
        }
      }

    This preserves Test 6's race semantics verbatim — the single forced flush call either completes (buf drains) or hangs (the in-flight flush's `flushing` lock holds, force can't override that lock; the setTimeout(timeoutMs) race wins, and any leftover rows in buf produce the `log_buffer_shutdown_drop` warn). Test 7 is unaffected: stopped=true is still set at the top of drain(), and push() still no-ops on stopped===true.

    **Option A (ALTERNATIVE — only choose this if you have an explicit reason to prefer the reorder approach AND you have manually verified Test 6's fake-timer setup won't be starved by a microtask spin loop):**

    Reorder drain() so flush() awaits BEFORE stopped=true is set:

      async drain(timeoutMs = 3_000): Promise<void> {
        clearInterval(timer);
        await Promise.race([
          (async () => {
            while (buf.length > 0) {
              await flush();
              if (buf.length > 0 && flushing) {
                await new Promise<void>((resolve) => queueMicrotask(resolve));
              }
            }
          })(),
          new Promise<void>((resolve) => {
            setTimeout(resolve, timeoutMs);
          }),
        ]);
        stopped = true;
        if (buf.length > 0) {
          opts.logger.warn(/* same */);
        }
      }

    **Known livelock risk in Option A**: Under Test 6's hung-flush fixture (`flushing === true` because the in-flight pg insert never resolves, and `buf.length === 1` because a row was pushed after the in-flight flush started), Option A's while loop enters `await flush()`, flush() short-circuits on the flushing-lock, microtask-yields via `queueMicrotask`, loops, short-circuits again — an unbounded microtask spin. The `setTimeout(3_000)` in the Promise.race is wall-clock-based; under `vi.advanceTimersByTimeAsync(3_001)` the timer fires, BUT only when the event loop yields to timer dispatch. A tight microtask loop CAN starve the timer dispatch in fake-timer mode, causing Test 6 to deadlock until the test's overall timeout fires. Option A is therefore NOT safe without additional changes to either Test 6 or the microtask loop (e.g. replacing `queueMicrotask` with `setTimeout(resolve, 0)` to yield to the timer queue, or aborting the loop after N iterations).

    Default path: use Option B. Do not switch to Option A unless you have read this entire trade-off note and decided to absorb the Test 6 risk.

    Either way, preserve:
      - The header comment block (lines 1-36) — update D-A4 note if the algorithm changed materially (Option B: D-A4 now reads "flush({ force: true }) called from drain() bypasses the stopped early-return so buffered rows are inserted before the race resolves").
      - The `clearInterval(timer)` call must happen during drain (the unref'd interval should be released on shutdown).
      - The `log_buffer_shutdown_drop` warn must still fire when buf.length > 0 after the race resolves.
      - The stopped flag must end up `true` by the time drain() resolves (Test 7 contract).

    With Option B as the default, Test 6 does not need to be touched. Test 8 from Task 5 will transition RED -> GREEN: drain() now calls flush({ force: true }), which sees stopped===true but force===true, so the early-return is bypassed, the 3 buffered rows are spliced into batch, the mock db's insert resolves (when the test calls inserts[0]?.resolve()), and drain returns with buf.length === 0.

    Commit message: `fix(05-06): bufferedWriter.drain() flushes buffer before setting stopped flag`. In the body, cite 05-VERIFICATION.md deferred entry and 05-UAT.md, and indicate which option (A or B) was chosen and why. Default expected choice: Option B (force-flag).
  </action>
  <verify>
    <automated>
      (cd router && npx vitest run tests/unit/bufferedWriter.test.ts 2>&1 | tee /tmp/.vitest-task6.log && \
       grep -E "Tests.*8 passed|Test Files.*1 passed" /tmp/.vitest-task6.log && \
       npx vitest run 2>&1 | tail -20 | grep -E "Test Files.*passed|Tests.*passed" && \
       npx tsc --noEmit)
    </automated>
  </verify>
  <done>
    All 8 tests in tests/unit/bufferedWriter.test.ts pass (RED -> GREEN for Test 8; existing Tests 1-7 still GREEN). Full vitest run is green (488+ tests with the +1 from Test 8 -> 489+). `tsc --noEmit` is clean. The drain() body now calls flush({ force: true }) (Option B — default) or has been reordered (Option A — only if explicitly chosen). The header comment's D-A4 invariant note is consistent with the new algorithm.
  </done>
</task>

<!-- ====================================================================== -->
<!-- Gap C — VERIFICATION.md housekeeping                                   -->
<!-- ====================================================================== -->

<task type="auto">
  <name>Task 7: Update 05-VERIFICATION.md to close the deferred bufferedWriter.drain item and reference 05-UAT.md as live evidence</name>
  <files>.planning/phases/05-postgres-observability-seam/05-VERIFICATION.md</files>
  <action>
    Update the YAML frontmatter at the top of 05-VERIFICATION.md to reflect the post-UAT, post-gap-closure-06 reality:

    1. Change `status: human_needed` to `status: verified` (the canonical post-UAT-resolved literal — the live UAT has been run, 9/10 tests pass per 05-UAT.md, and the deferred bufferedWriter.drain item is now closed by Plan 05-06 Task 6).

    2. Update the `human_verification:` block: keep the `test` / `expected` / `why_human` text, but add a new field `result: passed` referencing 05-UAT.md:
         human_verification:
           - test: "Live-stack 7-step operator UAT (05-HUMAN-UAT.md steps 1-7)"
             expected: "..." (unchanged)
             result: passed
             evidence: "05-UAT.md — 9/10 tests pass; Test 3 (smoke script polish) closed by Plan 05-06 Tasks 1-4; Test 9 (restore drill race) closed inline during UAT (commit 49d8e57)."
             why_human: "..." (unchanged)

    3. Remove the `deferred:` block (lines 21-24 of the current frontmatter) — the bufferedWriter.drain() defect is now closed by Plan 05-06 Tasks 5-6. Replace it with a brief inline citation in the prose section "Required Artifacts" -> `router/src/db/bufferedWriter.ts` row:
         > ✓ VERIFIED on push path AND drain path (drain bug closed in Plan 05-06 Task 6 — see 05-06-SUMMARY.md once written).

       Also update "Anti-Patterns Found" table row at line 168 (the bufferedWriter.drain entry): change severity from `⚠️ Warning (deferred — see frontmatter)` to `✓ FIXED in Plan 05-06 Task 6` and update the Impact column accordingly. Keep the other ⚠️ Warning rows (WR-01..WR-07) verbatim — they remain accepted risks per the original verifier's judgment.

    4. Update the "Gaps Summary" section (lines 203-211). Replace the "One deferred item (not blocking)" paragraph with a one-line note: "All deferred items closed by Plan 05-06 (commits referenced in 05-06-SUMMARY.md)." Update the "Recommendation" paragraph to say: "The phase is verified and ready for promotion. Live UAT is recorded in 05-UAT.md; the post-UAT polish is in Plan 05-06."

    5. Update the bottom-of-file metadata footer:
         _Verified: 2026-05-15T02:20:00Z_
         _Re-verified after Plan 05-06: 2026-05-15T<TIME-OF-EXECUTION>Z_
         _Verifier: Claude (gsd-verifier)_

    Do NOT modify the rest of the file (the Observable Truths / Required Artifacts / Key Link Verification / Data-Flow Trace / Behavioral Spot-Checks tables). Those rows are factually correct as of the codebase axis; this task is purely a status + deferred-item + Anti-Patterns-row update.

    Validate the YAML frontmatter parses cleanly after the edit:
      python3 -c "import yaml,sys; doc = open('.planning/phases/05-postgres-observability-seam/05-VERIFICATION.md').read(); fm = doc.split('---')[1]; yaml.safe_load(fm); print('OK')"

    Commit message: `docs(05-06): close deferred bufferedWriter.drain item + reference live UAT evidence`.
  </action>
  <verify>
    <automated>
      python3 -c "import yaml; doc = open('.planning/phases/05-postgres-observability-seam/05-VERIFICATION.md').read(); fm = doc.split('---')[1]; y = yaml.safe_load(fm); assert y.get('status') == 'verified', f'status={y.get(\"status\")}'; assert 'deferred' not in y, 'deferred block must be removed'; assert 'human_verification' in y; print('OK status=', y['status'])" &&
      grep -c "deferred:" .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md | awk '$1 == 0 {exit 0} {exit 1}' &&
      grep -c "Plan 05-06" .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md | awk '$1 >= 2 {exit 0} {exit 1}'
    </automated>
  </verify>
  <done>
    Frontmatter YAML parses cleanly. `status` is exactly `verified`. The `deferred:` block is removed from frontmatter. `human_verification` block records `result: passed` and `evidence: ...05-UAT.md...`. The Anti-Patterns row for bufferedWriter.drain is marked FIXED with a reference to Plan 05-06 Task 6. The Gaps Summary "deferred item" paragraph is replaced with the closed-by-05-06 note. Bottom footer notes the re-verification timestamp.
  </done>
</task>

</tasks>

<verification>
End-of-plan gate (executor runs after Task 7 commit):

  1. **Smoke-script syntax + python parse** — `bash -n bin/smoke-test-router.sh` exits 0, and every inline `python3 -c '...'` block compiles cleanly under Python 3.12 (Task 1 verify automation).
  2. **OBS-05 exclusion regex coverage** — `grep -c "grep -vE 'healthy|gpu-preflight|pg-backup'" bin/smoke-test-router.sh` returns 1 (Task 3).
  3. **SC-P5-E body-gated assertions** — Three new `postgres.status` gates present; zero remaining overall-HTTP-code gates in the SC-P5-E section (Task 2).
  4. **SC-P4-D second skip branch** — `SKIP_MODEL_NOT_PULLED` token appears ≥ 2 times in the script (Task 4: one in the python parse, one in the bash branch).
  5. **bufferedWriter regression test green** — `cd router && npx vitest run tests/unit/bufferedWriter.test.ts` shows 8 passing tests (Tests 1-7 pre-existing + new Test 8) (Task 6).
  6. **Full vitest suite green** — `cd router && npx vitest run` reports the full suite passes (488 baseline + 1 new = 489 ± any unrelated drift); `cd router && npx tsc --noEmit` zero diagnostics (Task 6).
  7. **VERIFICATION.md frontmatter valid** — `python3 -c "import yaml; yaml.safe_load(open(...).read().split('---')[1])"` returns OK; `status` is exactly `verified`; no `deferred:` block; `human_verification.result` is `passed` (Task 7).
  8. **No accidental drift** — `git diff --stat` shows exactly the four files in `files_modified`. Nothing else.

Live-stack re-run (operator, OUT OF SCOPE for the executor — informational only):
  `bash bin/smoke-test-router.sh` against a fresh `--profile ollama` stack should now exit 0 with zero remaining Phase 4/5 false-negative failures.
</verification>

<success_criteria>
- All 7 tasks committed atomically (one git commit per task, conventional commit prefix).
- bin/smoke-test-router.sh: `bash -n` clean; all inline `python3 -c '...'` blocks parse under Python 3.12; OBS-05 excludes pg-backup; SC-P5-E gates on postgres.status; SC-P4-D skips on model_not_found.
- router/src/db/bufferedWriter.ts: drain() flushes the buffer end-to-end before setting stopped=true (D-A invariant restored).
- router/tests/unit/bufferedWriter.test.ts: 8 tests pass; Test 8 is the new regression gate; Tests 1-7 unmodified (except for the audit comment on Test 7).
- Full router vitest suite green (489 ± unrelated drift); `tsc --noEmit` zero diagnostics.
- .planning/phases/05-postgres-observability-seam/05-VERIFICATION.md frontmatter status updated to `verified`; deferred block removed; live-UAT evidence referenced; Anti-Patterns drain() row marked FIXED.
- Phase 5 is ready for clean promotion; no remaining open items from 05-UAT.md or 05-VERIFICATION.md deferred block.
</success_criteria>

<output>
After completion, create `.planning/phases/05-postgres-observability-seam/05-06-SUMMARY.md` following the template at `@$HOME/.claude/get-shit-done/templates/summary.md`. The summary should:

  - List the four modified files with one-line each describing the change.
  - Cite 05-UAT.md as the live-stack evidence trail.
  - Cite 05-VERIFICATION.md's `deferred:` block (now removed) as the genesis of Task 5-6.
  - Record the option chosen (A or B) for the bufferedWriter.drain fix and a one-line rationale (default expected: Option B — force-flag, no microtask-livelock risk, minimal diff).
  - Capture the final test count (vitest pass/fail line) and any deviations from this plan.
  - Reference the seven commit hashes (one per task) in the "Commits" section.

The summary closes Phase 5 cleanly — the next workflow step is `/gsd-verify-phase 05 --post-uat` (or whatever the orchestrator's standard re-verify entry is) to re-issue the verifier verdict against the updated codebase + VERIFICATION.md.
</output>
