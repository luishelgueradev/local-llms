---
phase: 05-postgres-observability-seam
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - router/src/index.ts
  - router/src/app.ts
  - router/src/auth/bearer.ts
  - router/src/routes/v1/chat-completions.ts
  - router/src/routes/v1/messages.ts
  - router/src/errors/envelope.ts
  - router/src/metrics/registry.ts
  - router/src/metrics/recordOutcome.ts
  - router/src/middleware/agentId.ts
  - router/src/translation/openai-out.ts
  - router/src/translation/anthropic-out.ts
  - router/tests/unit/metricsRegistry.test.ts
  - router/tests/integration/agentIdPreHandler.test.ts
  - router/tests/integration/recordOutcome.test.ts
  - router/tests/integration/auth.test.ts
autonomous: true
requirements: [DATA-03, OBS-01, ROUTE-09]
tags: [metrics, prom-client, recordOutcome, agent-id, prehandler, fastify-hooks]

must_haves:
  truths:
    - "GET /metrics on port 3000 returns Prometheus text/plain;version=0.0.4 WITHOUT a bearer header (D-C5 skip-list extension) and includes the 5 custom metrics + Node default metrics"
    - "A successful POST /v1/chat/completions (stream OR non-stream) increments router_requests_total{protocol='openai',backend=...,model=...,status_class='success'} AND inserts ONE row into request_log with protocol='openai', route='/v1/chat/completions', populated tokens_in/tokens_out/latency_ms/http_status=200, NULL error_code, agent_id populated when X-Agent-Id was sent (D-C3, D-D1, D-D6, ROUTE-09)"
    - "A successful POST /v1/messages (stream OR non-stream) same — but protocol='anthropic', upstream_message_id populated from canonical._upstream_message_id, and ttft_ms populated from heartbeat.msSinceStart on the stream branch"
    - "An error path (RegistryUnknownModelError, CapabilityNotSupportedError, BackendSaturatedError, undici 5xx) writes a request_log row with the matching error_code (D-D2 taxonomy) AND status_class mapped per D-C4 (4xx → client_error, 5xx → server_error, disconnect → disconnect)"
    - "A client-disconnect mid-stream writes a row with status_class='disconnect', partial tokens_out count, error_code='client_disconnect'"
    - "request_log.error_message is truncated to 500 chars AND has Bearer/Authorization/apiKey patterns stripped before write (D-D3, RESEARCH Pitfall 12) — SELECT error_message FROM request_log WHERE error_message ~ '[Bb]earer' returns 0 rows"
    - "X-Agent-Id header round-trip per D-D5: regex ^[A-Za-z0-9._:-]{1,128}$ (ReDoS-safe — anchored + bounded + no nested quantifiers per RESEARCH Pattern 3 line 375), validated in preHandler hook registered AFTER bearer onRequest, attached as req.agentId, decorated into req.log = req.log.child({ agent_id }), surfaced into request_log.agent_id column"
    - "X-Agent-Id absent → req.agentId undefined → request_log.agent_id NULL; X-Agent-Id violating regex (e.g. spaces, 129+ chars) → 400 InvalidAgentIdError → invalid_request_error envelope (code: 'invalid_agent_id') on both wire surfaces"
    - "recordOutcome is idempotent: a safeRecord closure with recorded: boolean flag prevents double-row + double-metric counting if sseCleanup fires twice (RESEARCH Pitfall 8 — mirrors safeRelease pattern from chat-completions.ts:156–162)"
    - "Calling buildApp() twice in succession (vitest fixture) does NOT throw 'metric already registered' — every metric uses a fresh new Registry() and collectDefaultMetrics({ register }) per RESEARCH Pitfall 2 + Pattern 5 line 411"
    - "/healthz, /readyz, /v1/models, /v1/messages/count_tokens, and bearer-auth-failed requests do NOT produce a request_log row (D-D4 SKIP list)"
    - "The bufferedWriter's droppedCounter constructor parameter (was stubbed { inc: () => {} } in Plan 01) is now the real metrics.logBufferDroppedTotal prom-client Counter"
  artifacts:
    - path: "router/src/metrics/registry.ts"
      provides: "makeMetricsRegistry() — fresh Registry + 5 metrics per call (Pitfall 2)"
      contains: "new Registry()"
    - path: "router/src/metrics/recordOutcome.ts"
      provides: "makeRecordRequestOutcome — single helper observes 5 metrics AND enqueues request_log row per D-C6 + D-D6; truncate+redact error_message per D-D3"
      contains: "deps.bufferedWriter.push"
    - path: "router/src/middleware/agentId.ts"
      provides: "agentIdPreHandler — validates X-Agent-Id regex, attaches req.agentId, decorates req.log child, throws InvalidAgentIdError on violation; module augmentation"
      contains: "AGENT_ID_RE"
    - path: "router/src/errors/envelope.ts"
      provides: "InvalidAgentIdError extends Error with readonly code = 'invalid_agent_id'; mapToHttpStatus → 400; both envelopes route to invalid_request_error"
      contains: "InvalidAgentIdError"
  key_links:
    - from: "router/src/routes/v1/chat-completions.ts sseCleanup + non-stream finally"
      to: "router/src/metrics/recordOutcome.ts recordRequestOutcome"
      via: "safeRecord closure (idempotent per RESEARCH Pitfall 8)"
      pattern: "safeRecord\\("
    - from: "router/src/routes/v1/messages.ts sseCleanup + non-stream finally"
      to: "router/src/metrics/recordOutcome.ts recordRequestOutcome"
      via: "safeRecord closure with protocol='anthropic'"
      pattern: "safeRecord\\("
    - from: "router/src/metrics/recordOutcome.ts recordRequestOutcome"
      to: "router/src/db/bufferedWriter.ts push"
      via: "deps.bufferedWriter.push(row) — D-D6 field map populates every D-D1 column"
      pattern: "bufferedWriter\\.push"
    - from: "router/src/app.ts preHandler hook chain"
      to: "router/src/middleware/agentId.ts agentIdPreHandler"
      via: "app.addHook('preHandler', agentIdPreHandler) — AFTER bearer onRequest"
      pattern: "addHook\\(['\"]preHandler['\"]"
    - from: "router/src/auth/bearer.ts PUBLIC_PATHS"
      to: "router/src/app.ts /metrics route handler"
      via: "skip-list extension"
      pattern: "/metrics"
---

<objective>
Land the end-to-end Phase 5 vertical slice: every successful or failed /v1 model request now (a) increments the matching prom-client metric AND (b) enqueues a fully-populated request_log row through the bufferedWriter from Plan 01. The agent-id round-trip works (header → req.agentId → pino child → DB column). /metrics is reachable unauth on 127.0.0.1:3000 and reports the 5 custom metrics plus Node defaults.

Purpose: SC2's "every request has a request_log row" + SC4's "/metrics unauth+only-this-surface" + ROUTE-09's X-Agent-Id round-trip all live in this single plan because they share app.ts, the two route files, and the recordOutcome helper. Splitting them would create unresolvable file conflicts.

Output: an observable router. After this plan, `curl ... && psql 'SELECT * FROM request_log ORDER BY ts DESC LIMIT 1'` returns a row populated from the canonical D-D6 field map; `curl http://127.0.0.1:3000/metrics` returns Prometheus text with all 5 custom metrics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-postgres-observability-seam/05-CONTEXT.md
@.planning/phases/05-postgres-observability-seam/05-RESEARCH.md
@.planning/phases/05-postgres-observability-seam/05-PATTERNS.md
@.planning/phases/05-postgres-observability-seam/05-VALIDATION.md
@.planning/phases/05-postgres-observability-seam/05-01-PLAN.md
@.planning/phases/04-anthropic-surface-v1-messages-tool-calling-vision/04-CONTEXT.md
@.planning/phases/03-multi-backend-dispatch-llama-cpp-registry-hardening/03-CONTEXT.md
@.planning/phases/02-mvp-vertical-slice-router-ollama-sse/02-CONTEXT.md
@CLAUDE.md
@router/src/app.ts
@router/src/index.ts
@router/src/auth/bearer.ts
@router/src/errors/envelope.ts
@router/src/routes/v1/chat-completions.ts
@router/src/routes/v1/messages.ts
@router/src/sse/heartbeat.ts
@router/src/concurrency/semaphore.ts
@router/src/translation/canonical.ts
@router/src/translation/openai-out.ts
@router/tests/integration/auth.test.ts
@router/tests/integration/readyz.test.ts

<interfaces>
<!-- Key contracts the executor must use verbatim. -->

From router/src/auth/bearer.ts (PUBLIC_PATHS extension target — one literal line edit):
- `export const PUBLIC_PATHS: ReadonlySet<string> = new Set(['/healthz', '/readyz']);`
- Phase 5 D-C5 becomes: `new Set(['/healthz', '/readyz', '/metrics'])`

From router/src/errors/envelope.ts (CapabilityNotSupportedError is the structural analog for the new InvalidAgentIdError — class with `readonly code`, branch in `mapToHttpStatus` returning 400, parallel branches in both `toOpenAIErrorEnvelope` and `toAnthropicErrorEnvelope` returning invalid_request_error).

From router/src/concurrency/semaphore.ts (buildRelease closure — safeRecord mirrors this idempotency pattern; `released: boolean` captured, second call is no-op).

From router/src/routes/v1/chat-completions.ts (existing safeRelease + sseCleanup at ~line 201 + non-stream-finally at ~line 260 — the two recordOutcome call sites this plan adds).

From router/src/translation/canonical.ts (CanonicalStreamEvent shape — `message_start.message.usage.input_tokens` is the tokens_in source; `message_delta.usage.output_tokens` is the running tokens_out total; both are already aggregated by canonicalToOpenAISse / canonicalToAnthropicSse — this plan widens the onCleanup callback to expose the final values).

From router/src/sse/heartbeat.ts (HeartbeatHandle exposes `bytesSinceStart` and `msSinceStart` — the ttft_ms source captured at first SSE byte).

From RESEARCH §"Pattern 2 — recordRequestOutcome" lines 270–331 — the LITERAL implementation shape; copy verbatim. OutcomeContext has the 16 fields listed in D-D6.

From RESEARCH §"Pattern 3 — agentIdPreHandler" lines 337–366 — LITERAL preHandler with module augmentation, the regex `/^[A-Za-z0-9._:-]{1,128}$/`, first-value-wins RFC 9110 handling, throw-on-violation InvalidAgentIdError.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Metrics registry + InvalidAgentIdError + agentIdPreHandler + /metrics route + skip-list extension</name>
  <files>router/src/metrics/registry.ts, router/src/middleware/agentId.ts, router/src/errors/envelope.ts, router/src/auth/bearer.ts, router/src/app.ts, router/src/index.ts, router/tests/unit/metricsRegistry.test.ts, router/tests/integration/agentIdPreHandler.test.ts, router/tests/integration/auth.test.ts</files>
  <read_first>
    - router/src/auth/bearer.ts (line 6 — PUBLIC_PATHS literal to extend; lines 17–57 — hook factory shape that agentId.ts mirrors per PATTERNS.md)
    - router/src/app.ts (lines 28–34 — module augmentation block for liveness/semaphores; line 87 — onRequest hook registration; lines 122–214 — Routes section where /metrics GET handler lands; lines 36–67 — BuildAppOpts which gains `metrics`)
    - router/src/errors/envelope.ts (lines 47–59 — CapabilityNotSupportedError class; lines 87–101 — mapToHttpStatus; lines 120–186 — toOpenAIErrorEnvelope; lines 263–310 — toAnthropicErrorEnvelope)
    - router/tests/integration/auth.test.ts (entire — build-and-inject fixture pattern per PATTERNS.md lines 489–501)
    - router/tests/integration/readyz.test.ts (lines 49–63 — fake-injection pattern for buildApp opts)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §"Prometheus metrics surface" (D-C1..D-C7) and §"X-Agent-Id wiring" (D-D5)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Pattern 5" lines 401–467 and §"Pattern 3" lines 336–376
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Pitfall 2" (prom-client double-registration) and §"Pitfall 9" (pino child reassignment grep gate must remain == 1)
    - .planning/phases/05-postgres-observability-seam/05-PATTERNS.md §"router/src/metrics/registry.ts" + §"router/src/middleware/agentId.ts" + §"Shared Pattern E" + §"Shared Pattern F"
  </read_first>
  <behavior>
    - `cd router && vitest run tests/unit/metricsRegistry.test.ts` exits 0 with ≥ 4 tests: (1) returns object with 5 named metrics + register; (2) calling twice does NOT throw 'metric already registered' (RESEARCH Pitfall 2 regression gate); (3) `await reg.register.metrics()` output contains every custom metric NAME and at least one Node default; (4) histogram bucket boundaries match D-C3 / CONTEXT discretion paragraph.
    - `cd router && vitest run tests/integration/agentIdPreHandler.test.ts` exits 0 with ≥ 5 tests: (1) absent → 200 + req.agentId undefined; (2) valid `claude-code:luis` → 200 + req.agentId populated + pino log line contains `"agent_id":"claude-code:luis"`; (3) regex violation (space character) → 400 with `code:'invalid_agent_id'` on BOTH /v1/chat/completions and /v1/messages; (4) duplicate header (array) → first value wins per RFC 9110; (5) 129-char input → 400.
    - `cd router && vitest run tests/integration/auth.test.ts` exits 0 — extended with one new case: GET /metrics WITHOUT Authorization returns 200.
    - Smoke: `curl -i http://127.0.0.1:3000/metrics` returns 200 + Content-Type `text/plain; version=0.0.4; charset=utf-8` + body contains all 5 `# HELP router_*` lines AND at least one `# HELP process_*` or `# HELP nodejs_*` line.
    - `grep -rn 'req\.log = ' router/src/` returns exactly 1 line (Pitfall 9 gate).
  </behavior>
  <action>
    Create `router/src/metrics/registry.ts` per RESEARCH §"Pattern 5" lines 405–449 verbatim. Critical invariants:
    - `new Registry()` per call (NOT prom-client default singleton)
    - `collectDefaultMetrics({ register })` attaches Node defaults to OUR register — without `{ register }`, defaults go to the global singleton and double-register on second buildApp (Pitfall 2)
    - Five custom metrics per D-C3 table with the EXACT names, types, labelNames:
      * `router_requests_total` Counter labels `['protocol', 'backend', 'model', 'status_class']`
      * `router_request_duration_seconds` Histogram labels `['protocol', 'backend', 'model']` buckets `[0.1, 0.5, 1, 2.5, 5, 10, 30, 60, 120, 300]`
      * `router_ttft_seconds` Histogram same labels, buckets `[0.05, 0.1, 0.25, 0.5, 1, 2.5, 5, 10]`
      * `router_tokens_total` Counter labels `['protocol', 'backend', 'model', 'direction']` (RESEARCH Open Q5 — two observations per request)
      * `router_log_buffer_dropped_total` Counter NO labels (D-C3 row)
    - Every metric passes `registers: [register]` explicitly — Pitfall 2 invariant
    - Help strings from D-C3 verbatim
    - Forbidden labels (agent_id, request_id, raw status_code, error_message) MUST NOT appear in any labelNames — these live in request_log columns where unbounded cardinality is fine (T-5-11)
    - Export `MetricsRegistry = ReturnType<typeof makeMetricsRegistry>`

    Add `InvalidAgentIdError` to `router/src/errors/envelope.ts` mirroring `CapabilityNotSupportedError`:
    - `class InvalidAgentIdError extends Error` with `readonly code = 'invalid_agent_id'` and a constructor accepting `suppliedValue: string`; message references the regex constraint; truncates the suppliedValue to 32 chars in the message for safety
    - Add `if (err instanceof InvalidAgentIdError) return 400;` to `mapToHttpStatus`
    - Add parallel branches in `toOpenAIErrorEnvelope` AND `toAnthropicErrorEnvelope`: `type: 'invalid_request_error'`, `code: 'invalid_agent_id'`, `message: err.message`, `param: 'X-Agent-Id'`. Copy the existing CapabilityNotSupportedError branches as the structural template.

    Create `router/src/middleware/agentId.ts` per RESEARCH Pattern 3 lines 336–366:
    - Module augmentation: `declare module 'fastify' { interface FastifyRequest { agentId?: string; _t0?: number; } }`
    - `const AGENT_ID_RE = /^[A-Za-z0-9._:-]{1,128}$/` — anchored + bounded + no nested quantifiers — ReDoS-safe (T-5-10)
    - Hook function `export async function agentIdPreHandler(req, _reply)` — `req._t0 = performance.now()` ALWAYS first (post-bearer latency source); absent header → return early; first-value-wins on array header; regex test → throw `new InvalidAgentIdError(value)` on violation; on success `req.agentId = value` + `req.log = req.log.child({ agent_id: value })`
    - This is THE ONLY site that reassigns `req.log` — Pitfall 9 grep gate

    Update `router/src/auth/bearer.ts` line 6:
    - Extend PUBLIC_PATHS set to include `'/metrics'`
    - Add a multi-line comment ABOVE the line: `// TODO Phase 6: Traefik MUST add a middleware returning 404 for external /metrics requests (CONTEXT D-C5, RESEARCH Pitfall 11). Currently safe because router binds 127.0.0.1:3000 (compose.yml line 224). Phase 6 removes that binding — DO NOT FORGET TO ADD THE PATH BLACKLIST.`

    Update `router/src/app.ts`:
    - Add REQUIRED field `metrics: MetricsRegistry` to BuildAppOpts (alongside `bufferedWriter` from Plan 01)
    - Optional `agentIdPreHandler?: preHandlerAsyncHookHandler` for test injection
    - Import `makeMetricsRegistry`, `MetricsRegistry`, `agentIdPreHandler as defaultAgentIdPreHandler`
    - After `app.addHook('onRequest', makeBearerHook(opts.bearerToken));`, ADD: `app.addHook('preHandler', opts.agentIdPreHandler ?? defaultAgentIdPreHandler);`. Fastify v5 onRequest precedes preHandler (verified RESEARCH Pattern 3 line 369 — bearer gates first, agent-id is post-auth)
    - In Routes section AFTER `registerReadyz(...)` and BEFORE `registerChatCompletionsRoute(...)`, ADD:
      `app.get('/metrics', async (_req, reply) => { void reply.type(opts.metrics.register.contentType); return opts.metrics.register.metrics(); });`
    - Do NOT add compress middleware on /metrics (forbidden globally since Phase 2)

    Wire `router/src/index.ts` to construct and pass `metrics`. Place `const metrics = makeMetricsRegistry();` BEFORE `const bufferedWriter = makeBufferedWriter(...)` and use `droppedCounter: metrics.logBufferDroppedTotal` (replacing the Plan 01 stub). Pass `metrics` into `buildApp({ ..., bufferedWriter, metrics })`. Remove the `droppedCounterStub` line and its TODO comment.

    Create `router/tests/unit/metricsRegistry.test.ts` with the 4 named tests. The double-call test is the CRITICAL regression gate. Use plain vitest; no buildApp needed for the unit tier.

    Create `router/tests/integration/agentIdPreHandler.test.ts` per PATTERNS.md. Build the app via the existing fixture passing fake `bufferedWriter = { push: vi.fn(), drain: async () => {} }` and fake `metrics = makeMetricsRegistry()` (real registry; cheap). Inject requests with `app.inject({...})`. The "valid X-Agent-Id propagates to pino child" test captures log output via `loggerOpts` `stream:{ write }` redirect (Fastify v5 supports passing a custom write stream through loggerOpts) OR uses `vi.spyOn(process.stdout, 'write')`. For the invalid-header tests, send to `/v1/chat/completions` and assert against the OpenAI envelope shape; send the same to `/v1/messages` and assert against the Anthropic envelope shape. Mock `makeAdapter` per existing chat-completions integration test patterns so the route doesn't hit a real Ollama.

    Extend `router/tests/integration/auth.test.ts` with ONE new case: `it('GET /metrics unauth returns 200 (D-C5 skip-list)', ...)` mirroring existing `/healthz`/`/readyz` cases. Assert statusCode === 200 AND `res.headers['content-type']` starts with `text/plain`.

    Update ALL existing buildApp call sites in `router/tests/**` to add the new REQUIRED `metrics: makeMetricsRegistry()` field. This is mechanical — grep + add. The pattern is identical to the Plan 01 fixup for `bufferedWriter`.
  </action>
  <verify>
    <automated>cd router &amp;&amp; npx tsc --noEmit &amp;&amp; vitest run tests/unit/metricsRegistry.test.ts tests/integration/agentIdPreHandler.test.ts tests/integration/auth.test.ts &amp;&amp; (grep -rn 'req\.log = ' src/ | wc -l | grep -q '^1$')</automated>
  </verify>
  <done>
    All 3 test files pass; `req.log = ` reassignment grep gate returns exactly 1 (Pitfall 9); `/metrics` literal present in bearer.ts skip-list; manual smoke `curl -i http://127.0.0.1:3000/metrics` returns 200 + correct Content-Type + 5 custom metrics + Node defaults visible; sending `X-Agent-Id: claude-code:luis` produces a pino log line containing `"agent_id":"claude-code:luis"`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recordOutcome helper + truncateAndRedact + deriveStatusClass + mapErrorToCode + integration test</name>
  <files>router/src/metrics/recordOutcome.ts, router/tests/integration/recordOutcome.test.ts</files>
  <read_first>
    - router/src/metrics/registry.ts (created in Task 1 — the metrics handle shape this consumes)
    - router/src/db/schema/request_log.ts (created in Plan 01 — the RequestLogInsert type this populates per D-D6)
    - router/src/db/bufferedWriter.ts (created in Plan 01 — the .push(row) signature this calls)
    - router/src/errors/envelope.ts (entire — every typed error class with `.code` field — the source of request_log.error_code per D-D2 taxonomy)
    - router/src/translation/canonical.ts (lines 211–226 — CanonicalStreamEvent; lines 177–189 — CanonicalResponse with .usage)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §"request_log shape + X-Agent-Id wiring" (D-D1..D-D7)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Pattern 2" lines 270–331 (LITERAL implementation) and §"Pitfall 12" lines 599–603 (truncateAndRedact)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Open Questions" Q4 (BackendSaturatedError = client_error + error_code='backend_saturated' — keep simpler split)
    - .planning/phases/05-postgres-observability-seam/05-PATTERNS.md §"router/src/metrics/recordOutcome.ts" + §"router/tests/integration/recordOutcome.test.ts"
    - router/tests/integration/auth.test.ts (build-and-inject fixture pattern)
    - .planning/phases/05-postgres-observability-seam/05-VALIDATION.md DATA-03 row (the column-shape assertion gate)
  </read_first>
  <behavior>
    - `cd router && vitest run tests/integration/recordOutcome.test.ts` exits 0 with ≥ 6 tests:
      1. Successful non-stream /v1/chat/completions → ONE bufferedWriter.push call with row matching D-D1 shape: protocol='openai', route='/v1/chat/completions', backend + model from registry resolve, status_class='success', http_status=200, populated tokens_in/tokens_out/latency_ms, NULL ttft_ms (non-stream), NULL error_code/error_message, request_id from req.id, upstream_message_id NULL
      2. Successful stream /v1/chat/completions → ONE row with ttft_ms populated from heartbeat.msSinceStart (> 0) AND tokens_out > 0 from message_delta.usage aggregation
      3. Successful /v1/messages non-stream → protocol='anthropic', upstream_message_id matches canonical id pattern 'msg_*'
      4. RegistryUnknownModelError path: unknown model → 404 → ONE row with status_class='client_error', http_status=404, error_code='unknown_model'
      5. truncateAndRedact: `truncateAndRedact('error: Bearer abc123def456 expired')` → result does NOT contain 'abc123def456' AND does NOT match the case-insensitive regex `/[Bb]earer\s+\S+/` (D-D3 + Pitfall 12); `truncateAndRedact('apiKey="secret-12345"')` → result does NOT contain 'secret-12345'; a 1000-char message → ≤ 503 chars (`maxLen + '...'.length`)
      6. Skip list (D-D4): requests to /healthz, /readyz, /metrics, /v1/models, /v1/messages/count_tokens do NOT call bufferedWriter.push (zero captured)
  </behavior>
  <action>
    Create `router/src/metrics/recordOutcome.ts` per RESEARCH §"Pattern 2" lines 270–331 verbatim. The exported surface:
    - `interface OutcomeContext` with 16 fields per D-D6 (protocol, route, backend, model, statusClass, httpStatus, durationMs, ttftMs?, tokensIn?, tokensOut?, errorCode?, errorMessage?, agentId?, requestId, upstreamMessageId?, timestamp)
    - `makeRecordRequestOutcome(deps: { metrics: MetricsRegistry; bufferedWriter: { push: (row: RequestLogInsert) => void } })` returns the per-request helper
    - Inside: derive `labels = { protocol, backend, model }`; call `requestsTotal.inc({ ...labels, status_class })`; call `requestDurationSeconds.observe(labels, durationMs/1000)`; conditional observe on ttftSeconds; conditional inc on tokensTotal for input AND output directions; then `bufferedWriter.push(row)` populating EVERY D-D1 column from ctx per D-D6 field map

    Internal helper `truncateAndRedact(msg: string, maxLen = 500): string` per D-D3 + Pitfall 12:
    - Three precompiled module-level RegExps:
      * `BEARER_RE = /\bBearer\s+\S+/gi`
      * `AUTH_RE = /\bAuthorization\s*:\s*\S+/gi`
      * `APIKEY_RE = /(?:apiKey|api_key|api-key)\s*['"]?\s*[:=]\s*['"]?\S+/gi`
    - Apply all three replaces with the literal replacement string `'[REDACTED]'` (no capture-group preservation — the must_haves truth requires "SELECT error_message ... ~ '[Bb]earer'" returns 0 rows, which means the literal token `Bearer` must NOT remain in the output). Then truncate to maxLen with `'...'` suffix if over.
    - Export so tests can directly assert against literals
    - Apply INSIDE recordRequestOutcome before the bufferedWriter.push call

    Export `deriveStatusClass(httpStatus: number, clientAborted: boolean): StatusClass` per D-C4:
    - `clientAborted === true` → `'disconnect'` (regardless of httpStatus)
    - 200–299 → `'success'`
    - 400–499 → `'client_error'`
    - 500+ → `'server_error'`
    - default → `'server_error'` (defensive)

    Export `mapErrorToCode(err: unknown): string` per D-D2 taxonomy:
    - `RegistryUnknownModelError` → `'unknown_model'`
    - `BackendSaturatedError` → `'backend_saturated'`
    - `CapabilityNotSupportedError` → `'model_capability_mismatch'`
    - `InvalidAgentIdError | InvalidToolArgumentsError | InvalidImageUrlError | ImageFetchError | ZodError | hasZodFastifySchemaValidationErrors(err)` → `'invalid_request'`
    - `APIConnectionError | APIConnectionTimeoutError` → `'upstream_timeout'`
    - any other Error with `(err as {statusCode?: number}).statusCode >= 500 && < 600` → `'upstream_5xx'`
    - default → `'internal_error'`
    Client-disconnect is NOT routed through this helper — callers pass `errorCode: 'client_disconnect'` directly when they detect controller.signal.aborted.

    Per RESEARCH Open Question Q4: BackendSaturatedError → status_class='client_error' (4xx group per D-C4) AND error_code='backend_saturated' (D-D2). The taxonomy split is what differentiates saturation from genuine client errors when querying. Document in a top-of-file comment.

    Create `router/tests/integration/recordOutcome.test.ts` per PATTERNS.md. Use buildApp fixture from auth.test.ts:
    - Inject fake bufferedWriter: `const pushedRows: RequestLogInsert[] = []; const fakeBuffered = { push: (row) => pushedRows.push(row), drain: async () => {}, size: 0 };`
    - Real `metrics = makeMetricsRegistry()` (lightweight) — assert against `metrics.register.getSingleMetric('router_requests_total')` for the metric-side coverage
    - Use the existing fake `makeAdapter` from `router/tests/integration/chat-completions.nonstream.test.ts` (copy inline) returning deterministic canonical results with `usage.input_tokens=10, output_tokens=20`
    - For unknown_model: send `{"model":"no-such-model"}` and assert `pushedRows[0].error_code === 'unknown_model'`, `status_class === 'client_error'`, `http_status === 404`
    - For truncateAndRedact: import the helper directly, assert against literals (no buildApp needed)
    - For skip list: send to each skipped route and assert `pushedRows.length === 0`

    `recordOutcome` is invoked from BOTH route files via safeRecord closure wired in Task 3. Task 2 only ships the helper + tests.
  </action>
  <verify>
    <automated>cd router &amp;&amp; npx tsc --noEmit &amp;&amp; vitest run tests/integration/recordOutcome.test.ts &amp;&amp; (grep -v '^\s*//' src/metrics/recordOutcome.ts | grep -cE 'BEARER_RE|AUTH_RE|APIKEY_RE' | grep -q '^3$')</automated>
  </verify>
  <done>
    recordOutcome.test.ts reports ≥ 6 passing tests covering D-D1 row shape, error_code taxonomy (D-D2), truncateAndRedact (D-D3), skip-list (D-D4); `truncateAndRedact('Bearer abc123')` returns a string that contains neither 'abc123' nor the literal `Bearer ` token; status_class derivation matches D-C4 exactly; mapErrorToCode covers every typed error class in envelope.ts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire recordOutcome into both route files with safeRecord idempotency + widen onCleanup signature to expose token totals</name>
  <files>router/src/routes/v1/chat-completions.ts, router/src/routes/v1/messages.ts, router/src/translation/openai-out.ts, router/src/translation/anthropic-out.ts, router/src/app.ts</files>
  <read_first>
    - router/src/routes/v1/chat-completions.ts (entire — 268 lines; especially lines 156–205 safeRelease + sseCleanup, lines 244–266 non-stream + catch + finally)
    - router/src/routes/v1/messages.ts (entire — 296 lines; lines 200–270 stream branch sseCleanup, lines 273–293 non-stream + catch + finally)
    - router/src/translation/openai-out.ts (entire — note onCleanup signature at line ~222 and the call site at line ~422; capturedInputTokens / capturedOutputTokens at lines 278 + 362)
    - router/src/translation/anthropic-out.ts (find the parallel onCleanup signature and call site)
    - router/src/metrics/recordOutcome.ts (created in Task 2)
    - router/src/concurrency/semaphore.ts (lines 14–60 — released closure pattern that safeRecord mirrors per Pitfall 8)
    - router/src/sse/heartbeat.ts (msSinceStart and bytesSinceStart properties)
    - .planning/phases/05-postgres-observability-seam/05-CONTEXT.md §D-D7 (token-counting for stream — iterator's message_delta is already aggregated by the translators; route's sseCleanup reads the aggregated value)
    - .planning/phases/05-postgres-observability-seam/05-RESEARCH.md §"Pitfall 8" (safeRecord MUST be idempotent — sseCleanup may fire twice)
    - .planning/phases/05-postgres-observability-seam/05-PATTERNS.md §"router/src/routes/v1/chat-completions.ts + messages.ts" lines 635–675
    - router/src/app.ts (lines 36–67 BuildAppOpts + lines 222–238 register*Route call sites — they receive recordOutcome as a new opt)
  </read_first>
  <behavior>
    - End-to-end against running postgres+ollama: `curl -N -H "Authorization: Bearer $TOKEN" -H "X-Agent-Id: claude-code:luis" -d '{"model":"<m>","messages":[...],"stream":true}' http://127.0.0.1:3000/v1/chat/completions` completes; within ~2s of stream end, `psql -c "SELECT protocol,route,backend,model,status_class,http_status,tokens_in,tokens_out,ttft_ms,agent_id,latency_ms FROM request_log ORDER BY ts DESC LIMIT 1"` returns a row matching the request (protocol=openai, route=/v1/chat/completions, backend=ollama, status_class=success, http_status=200, agent_id='claude-code:luis', ttft_ms > 0, tokens_in + tokens_out > 0)
    - Same against /v1/messages produces protocol='anthropic', upstream_message_id LIKE 'msg_%'
    - Killing curl mid-stream produces status_class='disconnect', error_code='client_disconnect', tokens_out > 0 (partial)
    - Vision request to non-vision model → status_class='client_error', error_code='model_capability_mismatch', http_status=400
    - Unknown model → status_class='client_error', error_code='unknown_model', http_status=404
    - Existing Phase 2/3/4 integration suite remains green
    - `grep -rn 'safeRecord' router/src/routes/v1/` returns ≥ 2 lines (one per route — closures are per-request)
  </behavior>
  <action>
    First widen the translator onCleanup signature so route sseCleanup can read final token totals without re-aggregating.

    In `router/src/translation/openai-out.ts`:
    - Find the `onCleanup?: () => void` declaration in the streaming helper opts (around line 222)
    - Widen to `onCleanup?: (final?: { tokensIn: number; tokensOut: number }) => void` — the parameter is OPTIONAL so existing callers with `() => void` still type-check
    - Find the `opts.onCleanup?.()` call (around line 422) and pass `{ tokensIn: capturedInputTokens, tokensOut: capturedOutputTokens }` — the two variables are already captured at lines 278 and 362
    - Note: `capturedInputTokens` may be 0 if message_start was not received (pre-stream error path); `capturedOutputTokens` is the LAST message_delta.usage.output_tokens value before stream end

    Mirror the change in `router/src/translation/anthropic-out.ts`'s canonicalToAnthropicSse helper. Find its onCleanup callback and pass the same `{ tokensIn, tokensOut }`. The aggregation logic should already exist (Plan 04-03 wired the message_delta.usage flow).

    Update `router/src/app.ts`:
    - Import `makeRecordRequestOutcome` from `./metrics/recordOutcome.js`
    - After `app.decorate('semaphores', semaphores);` (line ~197), construct: `const recordOutcome = makeRecordRequestOutcome({ metrics: opts.metrics, bufferedWriter: opts.bufferedWriter });`
    - Pass `recordOutcome` into BOTH `registerChatCompletionsRoute({..., recordOutcome })` and `registerMessagesRoute({..., recordOutcome })` calls
    - Do NOT pass into `registerCountTokensRoute` or `registerModelsRoute` (D-D4 skip list)
    - Extend `RegisterChatCompletionsOpts` and `RegisterMessagesOpts` with `recordOutcome: (ctx: OutcomeContext) => void`

    Update `router/src/routes/v1/chat-completions.ts`:
    1. Add `recordOutcome: (ctx: OutcomeContext) => void` to `RegisterChatCompletionsOpts`
    2. INSIDE the route handler closure, after `entry = opts.registry.resolve(body.model)` and `canonical = openAIRequestToCanonical(...)` are bound (around line ~85), declare the idempotent wrapper mirroring the existing `safeRelease` shape:
       ```
       let recorded = false;
       const safeRecord = (ctx: OutcomeContext): void => {
         if (recorded) return;   // Pitfall 8 — sseCleanup may fire twice
         recorded = true;
         opts.recordOutcome(ctx);
       };
       ```
    3. Capture `let caughtErr: Error | undefined;` AND `let canonicalResult: CanonicalResponse | undefined;` BEFORE the try block — used in the finally branch to know whether to record success or failure
    4. In the catch (around line 247–259), preserve the existing throw chain and set `caughtErr = err as Error;` as the FIRST line
    5. In the catch path's `entry` is already bound — but for the unknown_model case where resolve() throws BEFORE the controller setup, the error is caught by the centralized handler (app.setErrorHandler) BEFORE reaching this catch. To cover that path, wrap the entire route handler body in an OUTER try/catch that records and re-throws — OR — let app.setErrorHandler do the recording. PICK THE CLEANER APPROACH: route handlers do their own recording in finally; for pre-resolve errors (unknown_model thrown by resolve()), the row is missed in Phase 5. Document this as a known coverage gap and add a TODO for Phase 7.

       BETTER ALTERNATIVE: capture the error in app.setErrorHandler too. In `app.ts` setErrorHandler (line 100), AFTER the existing `req.log.warn(...)` line, call `recordOutcome({ ... })` IF `req.url` matches a recorded route (/v1/chat/completions or /v1/messages). The setErrorHandler has access to err, req, reply, and the bound `recordOutcome` via closure. This gives complete coverage including pre-resolve errors. PICK THIS APPROACH.

       Implement: in `app.ts` setErrorHandler, after the existing error log, ADD:
       ```
       const isRecorded = url.startsWith('/v1/chat/completions') || url.startsWith('/v1/messages');
       const isCountTokens = url.startsWith('/v1/messages/count_tokens');
       if (isRecorded && !isCountTokens && !req.__recorded) {
         recordOutcome({
           protocol: isAnthropicRoute ? 'anthropic' : 'openai',
           route: url.split('?')[0],
           backend: 'unknown',  // pre-resolve path — no entry bound
           model: 'unknown',
           statusClass: deriveStatusClass(status, false),
           httpStatus: status,
           durationMs: performance.now() - (req._t0 ?? performance.now()),
           errorCode: mapErrorToCode(err),
           errorMessage: err.message ?? String(err),
           agentId: req.agentId,
           requestId: req.id,
           timestamp: new Date(),
         });
       }
       ```
       Where `req.__recorded` is an optional FastifyRequest decorator that the route's safeRecord SETS to true. Augment FastifyRequest in `agentId.ts` to include `__recorded?: boolean`.

    6. Wire safeRecord into sseCleanup (replacing the existing arrow):
       ```
       const sseCleanup = (final?: { tokensIn: number; tokensOut: number }): void => {
         heartbeat.stop();
         req.raw.socket?.off('close', onClose);
         safeRelease();
         req.__recorded = true;
         safeRecord({
           protocol: 'openai',
           route: req.url.split('?')[0],
           backend: entry.backend,
           model: entry.name,
           statusClass: deriveStatusClass(reply.statusCode, controller.signal.aborted),
           httpStatus: reply.statusCode,
           durationMs: performance.now() - (req._t0 ?? performance.now()),
           ttftMs: heartbeat.msSinceStart,
           tokensIn: final?.tokensIn,
           tokensOut: final?.tokensOut,
           errorCode: controller.signal.aborted ? 'client_disconnect' : undefined,
           agentId: req.agentId,
           requestId: req.id,
           timestamp: new Date(),
         });
       };
       ```
       Pass `onCleanup: sseCleanup` to `canonicalToOpenAISse(upstream, { signal, onCleanup: sseCleanup, displayModel })` — the widened signature accepts the optional final-tokens parameter.

    7. Wire safeRecord into non-stream finally — the non-stream branch sets `canonicalResult = await adapter.chatCompletionsCanonical(...)` so the finally can read tokens. Append to the existing finally:
       ```
       if (!body.stream) {
         req.__recorded = true;
         safeRecord({
           protocol: 'openai',
           route: req.url.split('?')[0],
           backend: entry.backend,
           model: entry.name,
           statusClass: caughtErr
             ? deriveStatusClass(mapToHttpStatus(caughtErr), false)
             : deriveStatusClass(reply.statusCode, false),
           httpStatus: caughtErr ? mapToHttpStatus(caughtErr) : reply.statusCode,
           durationMs: performance.now() - (req._t0 ?? performance.now()),
           tokensIn: caughtErr ? undefined : canonicalResult?.usage.input_tokens,
           tokensOut: caughtErr ? undefined : canonicalResult?.usage.output_tokens,
           errorCode: caughtErr ? mapErrorToCode(caughtErr) : undefined,
           errorMessage: caughtErr?.message,
           agentId: req.agentId,
           requestId: req.id,
           timestamp: new Date(),
         });
       }
       ```

    8. Mirror all of 1–7 in `router/src/routes/v1/messages.ts`. Differences:
       - `protocol: 'anthropic'`
       - `upstreamMessageId: canonical._upstream_message_id` (Plan 04-03 builder produces this) for stream branch; `upstreamMessageId: canonicalResult.id` for non-stream (CanonicalResponse.id is 'msg_<ulid>' per canonical.ts line 178)
       - onCleanup callback uses the widened canonicalToAnthropicSse signature
       - All other field-source rules identical

    9. Existing integration tests in `router/tests/integration/chat-completions.*.test.ts` and `messages.*.test.ts` need the new `recordOutcome` opt — add `recordOutcome: () => {}` (no-op) to each `registerChatCompletionsRoute / registerMessagesRoute` call. Mechanical.

    10. Add `__recorded?: boolean` to the FastifyRequest module augmentation in `router/src/middleware/agentId.ts` (keep all module augmentation in one place per the existing convention in `router/src/app.ts:28–34`).

    11. Grep gate: after wiring, `grep -rn 'safeRecord' router/src/routes/v1/` returns ≥ 2 (one per route file). `grep -rn 'recordOutcome' router/src/` returns ≥ 4 (the helper, the buildApp construction, the chat-completions opt, the messages opt, the setErrorHandler call — likely 5).
  </action>
  <verify>
    <automated>cd router &amp;&amp; npx tsc --noEmit &amp;&amp; npm test 2>&amp;1 | tail -20 | grep -E '(Test Files|Tests).*passed' &amp;&amp; (grep -rn 'safeRecord' src/routes/v1/ | grep -vc 'count-tokens\|models' | xargs test 2 -le)</automated>
  </verify>
  <done>
    Full router test suite green; safeRecord declared in both chat-completions.ts and messages.ts; manual end-to-end smoke against running stack returns request_log rows for: success non-stream, success stream, client-disconnect mid-stream, unknown_model, capability mismatch; /metrics shows nonzero `router_requests_total{status_class="success"}` after a successful request; SKIP routes do NOT add rows (verified via row-count diff before/after curling each skip-listed route).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1/chat/completions, /v1/messages | Bearer-authenticated; X-Agent-Id is operator-controlled identifier (regex-validated) |
| route handler → recordOutcome → bufferedWriter | In-process; trust is full but idempotency-required |
| recordOutcome → request_log.error_message | Trust degraded — upstream SDK errors may embed bearer tokens; D-D3 redaction is mitigation |
| /metrics → external scraper | Phase 5: loopback-only via 127.0.0.1:3000; Phase 6 must firewall — TODO carrier comment in bearer.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-08 | Information Disclosure | request_log.error_message may contain bearer tokens from upstream SDK errors | mitigate | `truncateAndRedact(msg)` applies BEARER_RE / AUTH_RE / APIKEY_RE pre-write per D-D3 + Pitfall 12; integration test asserts redaction |
| T-5-09 | Information Disclosure | /metrics externally reachable post-Phase 6 (operational telemetry as reconnaissance) | accept | TODO comment in bearer.ts explicitly flags Phase 6 follow-up per Pitfall 11; currently safe via 127.0.0.1:3000 binding (compose.yml line 224) |
| T-5-10 | DoS | X-Agent-Id ReDoS via malicious header | mitigate | Regex `/^[A-Za-z0-9._:-]{1,128}$/` anchored + bounded + no nested quantifiers (RESEARCH Pattern 3 line 375) |
| T-5-11 | DoS / Resource exhaustion | prom-client cardinality blow-up via unbounded agent_id/request_id labels | mitigate | D-C3 forbids high-cardinality labels; grep gate ensures labelNames arrays do NOT contain agent_id/request_id/http_status/error_message |
| T-5-12 | Information Disclosure | agent_id field visible in pino log lines | accept | D-D5: agent_id is a PUBLIC identifier by design; operators are responsible for not embedding secrets |
| T-5-13 | Tampering | Double-row insertion via sseCleanup firing twice (Pitfall 8) | mitigate | safeRecord closure with `recorded: boolean` flag (per-request closure capture) mirrors safeRelease |
| T-5-14 | Information Disclosure | prom-client collectDefaultMetrics exposes process metrics | accept | Pitfall 10 — only process telemetry, no request bodies; operational risk addressed by T-5-09 |
</threat_model>

<verification>
## Phase 5 SC Mapping for this plan

This plan delivers SC2 (every request has a request_log row) + SC4 (/metrics unauth on loopback) + ROUTE-09 (X-Agent-Id round-trip). SC1 dual-DB tables were created in Plan 01; this plan populates them.

- **SC1 (request_log populated):** Send a request with X-Agent-Id, then `SELECT protocol,route,backend,model,status_class,http_status,tokens_in,tokens_out,agent_id FROM request_log ORDER BY ts DESC LIMIT 1` returns the expected row.

- **SC2 (every-request-has-row + ttft_ms for stream):** Stream variant produces non-NULL ttft_ms. Pause-pg-5s end-to-end test lands in Plan 04.

- **SC4 (/metrics surface):** `curl -i http://127.0.0.1:3000/metrics` returns 200 + correct Content-Type + 5 `# HELP router_*` lines. `curl -s -o /dev/null -w '%{http_code}' .../v1/chat/completions -d '{}'` returns 401 (proves bearer-gated everywhere else).

- **ROUTE-09 (X-Agent-Id round-trip):** Three branches verified per D-D5 (absent → NULL, valid → populated, invalid → 400).

## Plan-level automated gates

- `cd router && npx tsc --noEmit` exits 0
- `cd router && npm test` — all tests pass; no regressions in Phase 2/3/4 suites
- `cd router && vitest run tests/unit/metricsRegistry.test.ts tests/integration/agentIdPreHandler.test.ts tests/integration/recordOutcome.test.ts tests/integration/auth.test.ts` — all green
- Grep gates:
  - `grep -rn 'req\.log = ' router/src/ | wc -l` == 1 (Pitfall 9)
  - `grep -rn 'safeRecord' router/src/routes/v1/ | grep -cE 'chat-completions|messages'` ≥ 2
  - `grep -v '^\s*//' router/src/auth/bearer.ts | grep -c '/metrics'` ≥ 1
  - `grep -v '^\s*//' router/src/metrics/recordOutcome.ts | grep -cE 'BEARER_RE|AUTH_RE|APIKEY_RE'` == 3
  - `grep -v '^\s*//' router/src/metrics/registry.ts | grep -c 'new Registry()'` == 1 (single factory call — Pitfall 2)
  - `grep 'labelNames' router/src/metrics/registry.ts | grep -cE 'agent_id|request_id|error_message|http_status'` == 0 (T-5-11)
</verification>

<success_criteria>
- [ ] /metrics returns 200 unauth on 127.0.0.1:3000 with 5 named custom metrics + Node defaults
- [ ] A successful /v1/chat/completions or /v1/messages request produces ONE request_log row matching D-D6 field map
- [ ] Error paths produce rows with correct error_code (D-D2) AND status_class (D-C4)
- [ ] Client-disconnect produces status_class='disconnect', error_code='client_disconnect'
- [ ] X-Agent-Id absent → NULL; valid → populated everywhere; regex violation → 400 on both wire surfaces
- [ ] All 3 new tests green; full `npm test` green
- [ ] All grep gates pass (Pitfall 9 == 1; Pitfall 2 single new Registry(); cardinality gate == 0)
- [ ] T-5-08..T-5-14 threat-model mitigations live in code
- [ ] Manual `bin/smoke-test-router.sh` Phase 2/3/4 sections still green (transparent to existing happy paths)
</success_criteria>

<output>
After completion, create `.planning/phases/05-postgres-observability-seam/05-02-SUMMARY.md` documenting:
- prom-client Registry pattern (fresh per buildApp — Pitfall 2 regression gate)
- recordOutcome single-helper pattern (D-C6) — label/column drift impossible by construction
- agentIdPreHandler hook ordering (onRequest:bearer → preHandler:agentId; verified Fastify v5)
- truncateAndRedact regex set (D-D3 + Pitfall 12)
- safeRecord idempotency (mirrors safeRelease — Pitfall 8)
- setErrorHandler also records — covers pre-resolve errors (unknown_model thrown before route body runs)
- onCleanup signature widening in openai-out.ts + anthropic-out.ts (now passes final {tokensIn,tokensOut})
- Plan 01 droppedCounter stub replaced with real metrics.logBufferDroppedTotal
- TODO comment in bearer.ts for Phase 6 (Pitfall 11)
- Open hand-off to Plan 03 (backups) and Plan 04 (usage_daily + /readyz + smoke + SC2 pause-pg scenario)
</output>
