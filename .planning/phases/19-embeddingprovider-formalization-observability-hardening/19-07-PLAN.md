---
phase: 19-embeddingprovider-formalization-observability-hardening
plan: 07
type: execute
wave: 4
depends_on: [19-03, 19-04, 19-05, 19-06]
files_modified:
  - DEPLOY.md
  - README.md
  - router/tests/integration/migrations/0007-hook-log.test.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [OBSV-03, OBSV-04]
must_haves:
  truths:
    - "DEPLOY.md ships new section §'EmbeddingProvider (Phase 19 — v0.11.0)' between Phase 18 section and 'Backups + retención' (D-21)"
    - "README.md ships new section §'EmbeddingProvider (v0.11.0)' between MCP Client + Hooks and Operacion (D-21)"
    - "README.md 'Estado del proyecto' table v0.11.0 row flipped to ✅ shipped with completion date (D-21)"
    - "tests/integration/migrations/0007-hook-log.test.ts EXTENDED with Phase 19 OBSV-04 re-verify describe block — NEVER creates a new file (D-22)"
    - "STATE.md milestone metadata updated: status=completed, completed_phases=6, percent=100, last_updated, last_activity (mark v0.11.0 SHIPPED)"
    - "ROADMAP.md Phase 19 row marked complete with completion date; v0.11.0 milestone marked ✅ shipped"
    - "REQUIREMENTS.md EMBP-01 + EMBP-02 + OBSV-01..04 status flipped to Complete with phase attribution"
    - "OBSV-04 is RE-VERIFICATION only — NO new migration files created (D-22)"
  artifacts:
    - path: "DEPLOY.md"
      provides: "EmbeddingProvider operator section"
      contains: "## EmbeddingProvider (Phase 19 — v0.11.0)"
    - path: "README.md"
      provides: "EmbeddingProvider consumer section + v0.11.0 SHIPPED banner"
      contains: "## EmbeddingProvider (v0.11.0)"
      contains: "v0.11.0.*✅"
    - path: "router/tests/integration/migrations/0007-hook-log.test.ts"
      provides: "OBSV-04 re-verify describe block appended"
      contains: "OBSV-04"
    - path: ".planning/STATE.md"
      provides: "v0.11.0 SHIPPED metadata"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 19 row + v0.11.0 milestone marked complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "EMBP-01/02 + OBSV-01..04 status flips"
  key_links:
    - from: "README.md Estado del proyecto"
      to: "v0.11.0 SHIPPED banner row"
      via: "table row flip"
      pattern: "v0\\.11\\.0.*✅"
    - from: "router/tests/integration/migrations/0007-hook-log.test.ts"
      to: "Phase 19 OBSV-04 describe block"
      via: "sibling describe append"
      pattern: "Migration 0007: re-verified by Phase 19"
---

<objective>
Final phase wrap-up: close OBSV-03 (docs) + OBSV-04 (PG-gated re-verify) + flip the v0.11.0 milestone metadata to SHIPPED.

Three coordinated changes, ONE commit per task (so the operator can `git revert` any sub-step cleanly):
1. **DEPLOY.md + README.md** — new EmbeddingProvider sections + README's "Estado del proyecto" v0.11.0 row flipped to ✅.
2. **Extend `tests/integration/migrations/0007-hook-log.test.ts`** with a Phase 19 OBSV-04 re-verify describe block (D-22 — NEVER create a new migration file; this is verification, not migration).
3. **STATE.md + ROADMAP.md + REQUIREMENTS.md** — milestone metadata flip + Phase 19 row complete + EMBP/OBSV requirement statuses flipped.

Purpose: Close OBSV-03 + OBSV-04 and ship the v0.11.0 milestone. After this plan lands, all 48 v0.11.0 requirements are Complete; the router is "Retrieval-Ready Infrastructure" with five provider interfaces, MCP host + client, streaming Responses, policy primitives, and observability hardening — without a single line of retrieval, memory, or business logic shipped.

Output: 6 files modified across 3 atomic task commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md
@DEPLOY.md
@README.md
@router/tests/integration/migrations/0007-hook-log.test.ts

<interfaces>
From PATTERNS.md §"DEPLOY.md (MODIFIED)" — new section structure (insert BETWEEN Phase 18 §"MCP Client + Pre-Completion Hooks (Phase 18 — v0.11.0)" and "Backups + retención"):

```markdown
## EmbeddingProvider (Phase 19 — v0.11.0)

> **Strategic frame (binding):** "Retrieval Interfaces, not Retrieval Logic" —
> the router exposes an `EmbeddingProvider` interface so downstream
> RetrieverProvider implementations can fetch vectors without HTTP round-trips,
> but the router itself ships **zero retrieval logic**. Frame-01 BLOCK:
> the production implementation is a factory returning an object literal,
> NOT a class — the router carries embeddings-shaped logic only because
> `/v1/embeddings` must work; nothing more.

### Interface

```typescript
export interface EmbeddingProvider {
  embed(
    input: string | string[],
    opts: { model: string; dimensions?: number; user?: string },
  ): Promise<{
    embeddings: number[][];
    model: string;
    usage: { prompt_tokens: number; total_tokens: number };
  }>;
}
```

### Consuming the provider from a custom pre-completion hook

```typescript
// Inside a caller-supplied RetrieverProvider implementation:
async retrieve(request: RetrieverRequest): Promise<RetrieverResponse> {
  const provider = request.fastify.embeddingProvider;  // decorated by buildApp
  const { embeddings: [queryVec] } = await provider.embed(request.query, {
    model: 'embed-local',
  });
  // … your vector-store similarity search using queryVec …
}
```

### Wire-shape invariant (P7-01 BLOCK)

`/v1/embeddings` wire shape is **byte-identical** to pre-Phase-19 (Phase 12 baseline).
Verified by:
- `router/tests/unit/grep-gates/embeddings-untouched.test.ts` SHA-256 baseline (rotated atomically with the route refactor in Plan 19-03).
- `router/tests/routes/embeddings.test.ts` 30+ regression cases.
- Smoke gates: Phase 7 EMBED-01 + Phase 12 EMB-H01..06.

### Observability surface

| Metric | Labels | Owned by | Notes |
|--------|--------|----------|-------|
| `router_embeddings_cache_total` | `result` ∈ {hit, miss, bypass} | provider (hit/miss); route (bypass for base64) | hit/miss/bypass; bypass-on-base64 stays at the route (Risk #2 Option A) |
| `router_embeddings_batch_size` | (histogram) | route | EMB-H03 — inbound batch size; route owns wire-shape metric (D-07) |
| `router_embeddings_dims_total` | `model`, `dims` | provider | per-served-vector increment; cardinality bounded by `models.yaml` × distinct dims observed |

### Verification matrix

| Requirement | Verified by |
|-------------|-------------|
| EMBP-01 (interface + decorator) | `router/tests/providers/embedding-provider.test.ts` (vitest unit + expectTypeOf) |
| EMBP-02 (route delegates; wire identical) | `tests/routes/embeddings.test.ts` regression suite + `tests/unit/grep-gates/embeddings-untouched.test.ts` SHA gate + smoke Phase 7 + Phase 12 gates |
```

From PATTERNS.md §"README.md (MODIFIED)" — new section + status banner flip:

1. INSERT new section §"EmbeddingProvider (v0.11.0)" between §"MCP Client + Hooks (v0.11.0)" and §"Operacion":
```markdown
## EmbeddingProvider (v0.11.0)

> **Strategic frame (binding):** "Retrieval Interfaces, not Retrieval Logic."
> Frame-01 BLOCK: zero retriever implementations ship in `router/src/`.

The router exposes an `EmbeddingProvider` interface so downstream
RetrieverProvider implementations consume vectors without an HTTP
round-trip through `/v1/embeddings`:

```typescript
export interface EmbeddingProvider {
  embed(
    input: string | string[],
    opts: { model: string; dimensions?: number; user?: string },
  ): Promise<{ embeddings: number[][]; model: string; usage: ... }>;
}
```

From a Fastify hook: `const provider = fastify.embeddingProvider;` (decorated by `buildApp`).

**`/v1/embeddings` wire shape is byte-identical to pre-Phase-19** (P7-01 BLOCK SHA-256 baseline).

See [`DEPLOY.md` §"EmbeddingProvider (Phase 19 — v0.11.0)"](./DEPLOY.md#embeddingprovider-phase-19--v0110) for the full operator reference.
```

2. FLIP the `Estado del proyecto` v0.11.0 row to ✅:
```markdown
| **v0.11.0** Retrieval-Ready Infrastructure | ✅ shipped <today YYYY-MM-DD> | 48 reqs / 6 phases · Policy primitives · MCP host + client · Streaming Responses · Sessions/Context · Pre-completion hooks · EmbeddingProvider + observability hardening |
```

From CONTEXT D-22 + RESEARCH §8 + PATTERNS.md §"router/tests/integration/migrations/0007-hook-log.test.ts (EXTENDED)" — sibling describe block to APPEND at the bottom of the existing test file (do NOT create a new file):

```typescript
describeMaybe('Migration 0007: re-verified by Phase 19 (OBSV-04)', () => {
  let pool: Pool;
  beforeAll(async () => { pool = new Pool({ connectionString: PG_URL! }); });
  afterAll(async () => { await pool.end(); });

  it('Phase 19 OBSV-04: hook_log column still present + still JSONB + still nullable', async () => {
    const r = await pool.query(
      `SELECT data_type, is_nullable
         FROM information_schema.columns
        WHERE table_schema = 'public'
          AND table_name = 'request_log'
          AND column_name = 'hook_log'`,
    );
    expect(r.rowCount).toBe(1);
    expect(r.rows[0].data_type).toBe('jsonb');
    expect(r.rows[0].is_nullable).toBe('YES');
  });
});
```

From REQUIREMENTS.md current state — EMBP/OBSV rows are all `Pending`. Flip to `Complete` per phase attribution:
- `EMBP-01 | Phase 19 | Complete`
- `EMBP-02 | Phase 19 | Complete`
- `OBSV-01 | Phase 19 | Complete`
- `OBSV-02 | Phase 19 | Complete`
- `OBSV-03 | Phase 19 | Complete`
- `OBSV-04 | Phase 19 | Complete (re-verifies Phase 18 Plan 18-02 migration 0007)`

Coverage line updated: `v0.11.0 requirements: 48 total | Complete: 48 (...everything... + EMBP × 2 + OBSV × 4) | Pending: 0`.

From STATE.md current state — milestone status is already `completed` (was set per memory `project_gapclosure_state_regression.md` regression-avoidance protocol). Verify the metadata reads:
- `milestone: v0.11.0`
- `milestone_name: Retrieval-Ready Infrastructure`
- `status: completed`
- `last_updated: <today ISO>`
- `last_activity: <today YYYY-MM-DD>`
- `progress.total_phases: 6`
- `progress.completed_phases: 6`
- `progress.total_plans: <40+7 = 47>`
- `progress.completed_plans: <47>`
- `progress.percent: 100`

If any of these mismatch, fix as part of this task (memory cite: gap-closure regression protocol).

From ROADMAP.md — flip Phase 19 row to `[x] **Phase 19: ...** — completed YYYY-MM-DD` + update progress table row + flip v0.11.0 milestone to ✅ shipped.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEPLOY.md + README.md new EmbeddingProvider sections + v0.11.0 SHIPPED banner flip</name>
  <read_first>
    - DEPLOY.md (FULL FILE — locate Phase 18 §"MCP Client + Pre-Completion Hooks (Phase 18 — v0.11.0)" closing point and "Backups + retención" opening; insert Phase 19 section in between)
    - README.md (FULL FILE — locate §"MCP Client + Hooks (v0.11.0)" closing point and §"Operacion" opening; insert EmbeddingProvider section in between; also locate "Estado del proyecto" table around line 37 and flip the v0.11.0 row to ✅)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md decision D-21 (full doc consolidation matrix)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md §"DEPLOY.md (MODIFIED)" + §"README.md (MODIFIED)" (full structure with strategic frame block-quote)
    - Phase 17 + Phase 18 sibling sections in DEPLOY.md (e.g., §"Sessions + ContextProvider (Phase 17 — v0.11.0)", §"MCP Client + Pre-Completion Hooks (Phase 18 — v0.11.0)") — match voice and structure
    - Phase 17 + Phase 18 sibling sections in README.md (e.g., §"Sessions / X-Session-ID (v0.11.0)", §"MCP Client + Hooks (v0.11.0)") — match voice and brevity
  </read_first>
  <files>DEPLOY.md, README.md</files>
  <action>
Edit `DEPLOY.md`:
1. Locate the closing of §"MCP Client + Pre-Completion Hooks (Phase 18 — v0.11.0)" (typically a horizontal rule `---` before the next section).
2. INSERT the new §"EmbeddingProvider (Phase 19 — v0.11.0)" section per `<interfaces>` block exactly: strategic-frame block-quote, Interface signature, consuming-from-hook code example, P7-01 wire-shape invariant subsection, Observability surface table (3 rows: cache_total, batch_size, dims_total), Verification matrix (EMBP-01 + EMBP-02 rows).
3. Add a closing `---` horizontal rule before the next section (probably "Backups + retención" — verify).

Edit `README.md`:
1. Locate the closing of §"MCP Client + Hooks (v0.11.0)".
2. INSERT the new §"EmbeddingProvider (v0.11.0)" section per `<interfaces>` block: strategic-frame block-quote, brief interface intro, code snippet, P7-01 invariant note, DEPLOY.md cross-link.
3. Locate the "Estado del proyecto" table (around line 37). FLIP the v0.11.0 row from "TBD" / "candidatos: ..." / "—" to:
```
| **v0.11.0** Retrieval-Ready Infrastructure | ✅ shipped <YYYY-MM-DD> | 48 reqs / 6 phases · Policy primitives · MCP host + client · Streaming Responses · Sessions/Context · Pre-completion hooks · EmbeddingProvider + observability hardening |
```

Use today's date (2026-06-01) per the convention from v0.9.0 / v0.10.0 rows in the same table.

Verify the surrounding Phase 14/15/17/18 DEPLOY sections are UNCHANGED byte-for-byte. The Phase 19 addition is purely additive within DEPLOY.md.

Conventional commit: `docs(19-07): OBSV-03 — DEPLOY + README EmbeddingProvider sections + v0.11.0 SHIPPED banner`.
  </action>
  <verify>
    <automated>grep -c "## EmbeddingProvider (Phase 19" DEPLOY.md && grep -c "## EmbeddingProvider (v0.11.0)" README.md && grep -cE "v0\.11\.0.*✅" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## EmbeddingProvider (Phase 19 — v0.11.0)" DEPLOY.md` returns `1`.
    - `grep -c "## EmbeddingProvider (v0.11.0)" README.md` returns `1`.
    - `grep -E "v0\.11\.0.*✅ shipped" README.md` returns at least 1 line (Estado del proyecto v0.11.0 row).
    - `grep -c "Retrieval Interfaces, not Retrieval Logic" DEPLOY.md` returns at least `1` (strategic frame citation in new section).
    - `grep -c "Retrieval Interfaces, not Retrieval Logic" README.md` returns at least `1` (strategic frame in new README section).
    - `grep -c "Frame-01 BLOCK" DEPLOY.md` returns at least `1` (invariant cited).
    - `grep -c "P7-01" DEPLOY.md` returns at least `1` (wire-shape invariant cited).
    - The DEPLOY.md new section includes the Observability surface table — `grep -c "router_embeddings_cache_total" DEPLOY.md` increases by 1 from prior baseline (it was likely cited in Phase 12 sections too — net delta should be at least 1).
    - The verification matrix table in DEPLOY.md includes both EMBP-01 and EMBP-02 row entries.
    - Phase 14/15/17/18 existing DEPLOY sections UNCHANGED — `git diff HEAD~1..HEAD -- DEPLOY.md | grep '^-' | grep -v 'TBD\|candidatos\|v0.11.0 *|' | wc -l` returns 0 (no removed lines outside the v0.11.0 banner flip in README).
    - Phase 14/15/17/18 existing README sections UNCHANGED — `git diff HEAD~1..HEAD -- README.md` only shows added lines + a single edited line (the Estado del proyecto v0.11.0 row).
    - Commit scope is `(19-07)` and message is `docs(19-07): ...`.
  </acceptance_criteria>
  <done>
    OBSV-03 closed. Operators have a one-stop EmbeddingProvider reference in DEPLOY.md; consumers see the v0.11.0 SHIPPED row in README.md plus a quick-reference EmbeddingProvider section.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/integration/migrations/0007-hook-log.test.ts with Phase 19 OBSV-04 re-verify describe block</name>
  <behavior>
    - Existing describe block `'Migration 0007: request_log.hook_log JSONB column'` and its 7 cases UNCHANGED byte-for-byte.
    - New sibling describe block `'Migration 0007: re-verified by Phase 19 (OBSV-04)'` appended at the bottom of the file.
    - New describe runs under the same `describeMaybe` env gate (`PG_TESTS=1 + POSTGRES_URL`).
    - New describe contains exactly 1 `it()` case asserting hook_log column still exists in live Postgres with `data_type === 'jsonb'` and `is_nullable === 'YES'`.
    - When run with `PG_TESTS=1`, all 8 cases (7 existing + 1 new) pass; without `PG_TESTS`, both describes skip cleanly.
    - NO new migration file created (D-22). NO journal entry modified.
  </behavior>
  <read_first>
    - router/tests/integration/migrations/0007-hook-log.test.ts (FULL FILE — primary target; you must understand the existing `describeMaybe` setup at lines 26-44 + the existing 7 cases so the new describe mirrors style)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md decision D-22
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md §8 (full re-verify pattern verbatim)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md §"router/tests/integration/migrations/0007-hook-log.test.ts (EXTENDED)" (sibling describe pattern)
  </read_first>
  <files>router/tests/integration/migrations/0007-hook-log.test.ts</files>
  <action>
Append to `router/tests/integration/migrations/0007-hook-log.test.ts` (do NOT modify existing content):

```typescript
// ── Phase 19 OBSV-04 re-verification ─────────────────────────────────
// D-22: NO new migration in Phase 19. This describe block re-verifies
// the Plan 18-02 migration 0007 still holds in the live Postgres.
// If the column is missing or its type drifted, OBSV-04 fails — the
// safety-net REQ text ("if not already added in Phase 18, migration
// 0007 adds it here") is satisfied by structural verification, not
// by a redundant migration.
describeMaybe('Migration 0007: re-verified by Phase 19 (OBSV-04)', () => {
  let pool: Pool;
  beforeAll(async () => {
    pool = new Pool({ connectionString: PG_URL! });
  });
  afterAll(async () => {
    await pool.end();
  });

  it('Phase 19 OBSV-04: hook_log column still present + still JSONB + still nullable', async () => {
    const r = await pool.query(
      `SELECT data_type, is_nullable
         FROM information_schema.columns
        WHERE table_schema = 'public'
          AND table_name = 'request_log'
          AND column_name = 'hook_log'`,
    );
    expect(r.rowCount).toBe(1);
    expect(r.rows[0].data_type).toBe('jsonb');
    expect(r.rows[0].is_nullable).toBe('YES');
  });
});
```

Place this AFTER the existing describe's closing brace + a blank line. No other modifications to the file.

Verify the existing describe block + 7 cases byte-for-byte unchanged (git diff should show ONLY added lines).

Run with `PG_TESTS=1`:
```bash
docker compose -f docker-compose.yml exec -e PG_TESTS=1 \
  -e POSTGRES_URL=postgres://router:router@postgres:5432/router \
  router npx vitest run tests/integration/migrations/0007-hook-log.test.ts
```
OR (without docker exec, if router is local): `PG_TESTS=1 POSTGRES_URL=... npx vitest run tests/integration/migrations/0007-hook-log.test.ts`. Both describes should report 8 pass; 0 failed.

If `PG_TESTS=1` is not set in your environment, the test cleanly SKIPs (both describes) — that's the expected behavior; the smoke gate Plan 19-06 doesn't depend on this test running locally.

Conventional commit: `test(19-07): OBSV-04 re-verify — extend 0007-hook-log.test.ts with Phase 19 describe block`.

NO new migration file. NO journal entry modification. NO drizzle schema change. This is verification-only per D-22.
  </action>
  <verify>
    <automated>grep -c "Migration 0007: re-verified by Phase 19" router/tests/integration/migrations/0007-hook-log.test.ts && ls router/db/migrations/ | grep -c 0007_</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Migration 0007: re-verified by Phase 19 (OBSV-04)" router/tests/integration/migrations/0007-hook-log.test.ts` returns `1` (new describe added).
    - `grep -c "Phase 19 OBSV-04" router/tests/integration/migrations/0007-hook-log.test.ts` returns at least `1`.
    - `grep -c "Migration 0007: request_log.hook_log JSONB column" router/tests/integration/migrations/0007-hook-log.test.ts` returns `1` (existing describe untouched).
    - `ls router/db/migrations/ | grep -c '^0007_'` returns `1` (only the existing migration file — Phase 19 added NONE).
    - `cat router/db/migrations/meta/_journal.json | grep -c '"idx": 7'` returns `1` (Phase 18's entry unchanged; no new entry).
    - `git diff HEAD~1..HEAD -- router/tests/integration/migrations/0007-hook-log.test.ts | grep -c '^-' | grep -v '^---'` returns lines only for whitespace fixes (ideally 0 — purely additive).
    - When `PG_TESTS=1 POSTGRES_URL=...` is set: `npx vitest run tests/integration/migrations/0007-hook-log.test.ts` reports 8 passed (7 prior + 1 new), 0 failed.
    - Without `PG_TESTS`: both describes report SKIP cleanly (no error).
    - Commit scope is `(19-07)` and message is `test(19-07): ...`.
  </acceptance_criteria>
  <done>
    OBSV-04 closed by re-verification (D-22 honored — no new migration). Live PG column shape proven persistent across Phase 18 → Phase 19.
  </done>
</task>

<task type="auto">
  <name>Task 3: Milestone wrap-up — STATE.md + ROADMAP.md + REQUIREMENTS.md metadata flip</name>
  <read_first>
    - .planning/STATE.md (FULL FILE — frontmatter + Last Updated narrative; verify milestone metadata + write Phase 19 SHIPPED narrative at the top per Phase 17/18 precedent)
    - .planning/ROADMAP.md (FULL FILE — locate Phase 19 row + the progress table + the v0.11.0 milestone bullet at line ~10; flip all three to ✅ / complete)
    - .planning/REQUIREMENTS.md (search for EMBP/OBSV rows; full file context for the coverage line + per-REQ status table)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md decision D-21 + D-23 (Plan 7 wrap-up)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md §"Planner Guidance" Plan 19-07
    - User memory `project_gapclosure_state_regression.md` — STATE.md milestone metadata regression-avoidance protocol: verify status=completed, percent=100, completed_phases match
    - Phase 17 Plan 17-07 SUMMARY + Phase 18 Plan 18-08 SUMMARY — voice and structure of the SHIPPED narrative at the top of STATE.md
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Edit `.planning/STATE.md`:
1. Update frontmatter (top of file):
   - `last_updated: "<today ISO>"`
   - `last_activity: <today YYYY-MM-DD>`
   - `progress.total_phases: 6`
   - `progress.completed_phases: 6`
   - `progress.total_plans: 47` (previous 40 from Phases 14-18 + 7 from Phase 19)
   - `progress.completed_plans: 47`
   - `progress.percent: 100`
   - `status: completed` (already set per gap-closure protocol; verify)
2. Update "Last Updated:" narrative at the top of the body to a new Phase 19 SHIPPED block (Phase 17/18 style):
   - Start: `**Last Updated:** <today YYYY-MM-DD> — **Phase 19 COMPLETE. Plan 19-07 SHIPPED — milestone wrap-up: EmbeddingProvider sections in DEPLOY + README + v0.11.0 SHIPPED banner + OBSV-04 re-verify describe block + STATE/ROADMAP/REQUIREMENTS flip.**`
   - Cite each task's commit short SHA (you generate these as you commit).
   - List the 6 REQs closed: EMBP-01, EMBP-02, OBSV-01..04.
   - Cite the v0.11.0 milestone status: 6/6 phases SHIPPED + 48/48 requirements complete.
   - Add a `(Previous: ...)` block prepended to the existing Phase 18 last-updated narrative.

Edit `.planning/ROADMAP.md`:
1. Locate `### Phase 19: EmbeddingProvider Formalization + Observability Hardening` section.
2. Find the Plans bullet list. Replace each `- [ ] 18-XX-PLAN.md` (which seems to be a copy-paste error in the source — should reference Phase 19 plans) entries with:
   ```
   - [x] 19-01-PLAN.md — Wave 0 scaffold (4 files: 2 test placeholders + tests/fakes.ts ext + types/fastify.d.ts) [EMBP-01, OBSV-02] — SHIPPED <YYYY-MM-DD>
   - [x] 19-02-PLAN.md — EmbeddingProvider interface + makeOpenAIEmbeddingProvider factory (Frame-01) [EMBP-01] — SHIPPED <YYYY-MM-DD>
   - [x] 19-03-PLAN.md — /v1/embeddings route refactor + P7-01 SHA-256 baseline atomic rotation [EMBP-02] — SHIPPED <YYYY-MM-DD>
   - [x] 19-04-PLAN.md — Composition-root wire-up + BuildAppOpts widening + app.decorate('embeddingProvider') [EMBP-01] — SHIPPED <YYYY-MM-DD>
   - [x] 19-05-PLAN.md — checkCardinalityLive parser + dual-mode CLI + CI integration test [OBSV-02] — SHIPPED <YYYY-MM-DD>
   - [x] 19-06-PLAN.md — Smoke Phase 19 section: OBSV-02-LIVE + RESS-WITH-TOOLS gates [OBSV-01, OBSV-02] — SHIPPED <YYYY-MM-DD>
   - [x] 19-07-PLAN.md — OBSV-03 docs + OBSV-04 re-verify + milestone wrap-up [OBSV-03, OBSV-04] — SHIPPED <YYYY-MM-DD>
   ```
3. Update the "Plans" count: `**Plans:** 7/7 plans complete`.
4. Update the top-of-file milestone status line: change `🚧 **v0.11.0 Retrieval-Ready Infrastructure** ...` to `✅ **v0.11.0 Retrieval-Ready Infrastructure** — ... shipped <YYYY-MM-DD>`.
5. Update the Phases checklist line for Phase 19: change `- [ ] **Phase 19: ...**` to `- [x] **Phase 19: EmbeddingProvider Formalization + Observability Hardening** ✅ <YYYY-MM-DD> — All new v0.11.0 surfaces covered by smoke + Prometheus metrics; cardinality CI guard enforced live; README/DEPLOY reflect full v0.11.0 surface.`
6. Update the Progress table row for Phase 19: `| 19. EmbeddingProvider Formalization + Observability Hardening | 7/7 | Complete | <YYYY-MM-DD> |`.

Edit `.planning/REQUIREMENTS.md`:
1. Find the EMBP/OBSV requirement checkbox section (currently `- [ ] **EMBP-01**: ...` etc.). Change to `- [x] **EMBP-XX**: ...` for all 6 IDs.
2. Find the per-REQ status table (currently `| EMBP-01 | Phase 19 | Pending |` × 6). Flip:
   ```
   | EMBP-01 | Phase 19 | Complete |
   | EMBP-02 | Phase 19 | Complete |
   | OBSV-01 | Phase 19 | Complete |
   | OBSV-02 | Phase 19 | Complete |
   | OBSV-03 | Phase 19 | Complete |
   | OBSV-04 | Phase 19 | Complete (re-verifies Phase 18 Plan 18-02 migration 0007) |
   ```
3. Update the "Coverage" summary line: `v0.11.0 requirements: 48 total | Complete: 48 (POL × 6 + MCPS × 6 + RESS × 5 + SESS × 6 + CTXP × 4 + SUMP × 3 + MCPC × 6 + RETR × 6 + EMBP × 2 + OBSV × 4) | Pending: 0`.

Verify Phase 14-18 requirement status rows UNCHANGED — `git diff HEAD~1..HEAD -- .planning/REQUIREMENTS.md | grep '^-' | head -20` shows only the EMBP/OBSV pending → complete flips.

Conventional commit: `docs(19-07): v0.11.0 SHIPPED — STATE + ROADMAP + REQUIREMENTS metadata flip`.
  </action>
  <verify>
    <automated>grep -c "status: completed" .planning/STATE.md && grep -c "✅.*Phase 19" .planning/ROADMAP.md && grep -c "EMBP-01 | Phase 19 | Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter: `progress.completed_phases: 6`, `progress.percent: 100`, `status: completed`.
    - STATE.md narrative top of body cites Phase 19 SHIPPED + lists all 6 closed REQs (EMBP-01, EMBP-02, OBSV-01..04).
    - ROADMAP.md Phase 19 plans list shows 7 `[x]` entries with SHIPPED dates.
    - ROADMAP.md `**Plans:** 7/7 plans complete` is present for Phase 19.
    - ROADMAP.md "Phases" checklist Phase 19 row is `[x]` with date.
    - ROADMAP.md Progress table Phase 19 row is `Complete` with date.
    - ROADMAP.md milestone status line shows `✅ **v0.11.0 Retrieval-Ready Infrastructure** ... shipped`.
    - REQUIREMENTS.md all 6 EMBP/OBSV checkbox rows are `- [x]`.
    - REQUIREMENTS.md per-REQ status table has all 6 EMBP/OBSV rows showing `Complete`.
    - REQUIREMENTS.md coverage line shows `Complete: 48` and `Pending: 0`.
    - `git log --oneline | head -3` shows 3 commits scoped to `(19-07)` (one per task) — `docs`, `test`, `docs`.
    - Phase 14-18 metadata UNCHANGED — `git diff HEAD~3..HEAD -- .planning/REQUIREMENTS.md | grep '^-' | wc -l` shows minus-lines ONLY for the EMBP/OBSV flips (≤ 6 minus lines + the coverage line).
    - Commit scope is `(19-07)` for all 3 task commits.
  </acceptance_criteria>
  <done>
    v0.11.0 milestone SHIPPED. All 48 requirements complete. Phase 19 closed. STATE / ROADMAP / REQUIREMENTS in sync. After this lands, the v0.11.0 milestone audit document can be drafted (per the v0.9.0/v0.10.0 precedent) — that's outside this plan's scope but unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied env vars for OBSV-04 PG-gated test | PG_TESTS=1 + POSTGRES_URL — test skips cleanly when absent |
| README/DEPLOY docs → consumers | Manual-review boundary; markdown rendering is the only attack surface (none meaningful) |
| Metadata files (STATE, ROADMAP, REQUIREMENTS) → planning system | Internal trust; consistency invariant enforced by the gsd-sdk validators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-STATE | Repudiation | STATE.md milestone metadata drifts from reality | mitigate | gap-closure regression protocol (`project_gapclosure_state_regression.md`): verify percent=100, completed_phases=6 after every edit; `gsd-sdk query state.load` confirms post-write. |
| T-19-07-MIG | Tampering | OBSV-04 re-verify accidentally creates a new migration | mitigate | Acceptance criteria explicitly grep for migration file count + journal idx=7 unchanged. The describe block adds ONLY a test case; no SQL file, no schema change, no journal entry. |
| T-19-07-DOC | Tampering | Phase 14-18 doc sections accidentally modified | mitigate | Acceptance criteria require `git diff` minus-line counts to be near-zero outside the explicit Phase 19 additions. |
| T-19-07-FR1 | Tampering | Frame-01 BLOCK | mitigate | Docs explicitly cite Frame-01 BLOCK invariant + zero retriever implementations + factory-of-object-literal pattern. README + DEPLOY strategic frame quotes re-anchor the invariant. |
| T-19-07-SC | Tampering | npm/pip/cargo installs | mitigate | This plan modifies docs + metadata + one test file. ZERO new packages. Acceptance: `git diff router/package.json router/package-lock.json` empty. |
</threat_model>

<verification>
- Full vitest sweep: `cd router && npm test` passes; no regressions (modulo +1 in PG-gated tests when PG_TESTS=1).
- `cd router && npx tsc --noEmit` exits 0.
- `bash -n bin/smoke-test-router.sh` exits 0 (Plan 19-06 already verified, but re-check).
- All grep counts in the 3 task acceptance_criteria match.
- Phase 14-18 metadata + docs UNCHANGED (regression net).
- STATE.md frontmatter percent=100, completed_phases=6, status=completed.
- `gsd-sdk query state.load 2>&1 | grep -E "percent|status"` confirms metadata is consistent (regression-avoidance protocol).
- `git diff router/package.json router/package-lock.json` empty.
</verification>

<success_criteria>
- 6 files modified across 3 atomic task commits.
- DEPLOY + README EmbeddingProvider sections shipped.
- README v0.11.0 SHIPPED row banner flipped.
- OBSV-04 re-verify describe block appended (NO new migration).
- STATE / ROADMAP / REQUIREMENTS metadata in sync, percent=100.
- v0.11.0 milestone CLOSED.
- 3 commits scoped to `(19-07)`.
</success_criteria>

<output>
Create `.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-07-SUMMARY.md` when done. Cite:
- Each of the 3 task commit short SHAs
- The exact line/section locations where DEPLOY + README new sections were inserted
- The exact lines in STATE/ROADMAP/REQUIREMENTS that changed (frontmatter percent, plan list, per-REQ status table)
- Confirmation `ls router/db/migrations/` has the same file count as pre-Plan-19-07 (no new migration files)
- Confirmation `gsd-sdk query state.load` shows percent=100
- v0.11.0 closure summary: 6/6 phases + 48/48 requirements + 5 provider interfaces shipped (SessionStore, ContextProvider, RetrieverProvider, EmbeddingProvider, SummaryProvider) + MCP host + MCP client + streaming Responses + policy primitives + observability hardening + ZERO retrieval/memory/business logic in production code
- Any inline deviations (Rule 1/2/3)
</output>
