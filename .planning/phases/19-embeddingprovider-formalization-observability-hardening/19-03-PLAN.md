---
phase: 19-embeddingprovider-formalization-observability-hardening
plan: 03
type: execute
wave: 2
depends_on: [19-02]
files_modified:
  - router/src/routes/v1/embeddings.ts
  - router/tests/unit/grep-gates/embeddings-untouched-baseline.json
autonomous: true
requirements: [EMBP-02]
must_haves:
  truths:
    - "/v1/embeddings route delegates upstream call + cache + dims to req.server.embeddingProvider.embed(...) (D-09 + D-10)"
    - "Wire shape byte-identical: OpenAI list { object: 'list', data: [{ object: 'embedding', index, embedding }], model, usage } unchanged"
    - "X-Cost-Cents header still emitted; req.computedCostCents stamped BEFORE reply.send() (Fastify v5 onSend timing — project_fastify_onsend_timing.md)"
    - "router_embeddings_batch_size histogram still observed by ROUTE (D-07 — wire-shape metric stays in wire layer)"
    - "router_embeddings_cache_total{result:'bypass'} still incremented BY ROUTE for base64 requests (Risk #2 Option A)"
    - "Existing tests/routes/embeddings.test.ts ~30 cases all pass post-refactor (wire-shape regression net)"
    - "P7-01 SHA-256 baseline rotated atomically with route diff in ONE commit (D-24 — never split across commits)"
    - "Route's post-loop dims block removed (D-03 option (a)) — provider is single source of truth for dims enforcement"
  artifacts:
    - path: "router/src/routes/v1/embeddings.ts"
      provides: "Thin route handler delegating to EmbeddingProvider"
      contains: "req.server.embeddingProvider.embed"
    - path: "router/tests/unit/grep-gates/embeddings-untouched-baseline.json"
      provides: "Updated SHA-256 baseline pinning the post-refactor file shape"
  key_links:
    - from: "router/src/routes/v1/embeddings.ts"
      to: "router/src/providers/embedding-provider.ts:EmbeddingProvider"
      via: "req.server.embeddingProvider.embed"
      pattern: "req\\.server\\.embeddingProvider\\.embed"
---

<objective>
Refactor `/v1/embeddings` to delegate the upstream-call + cache + dims-enforcement triad to `req.server.embeddingProvider.embed(...)` while preserving the wire shape byte-identical (P7-01 BLOCK). Atomically rotate the P7-01 SHA-256 baseline in the SAME commit as the route diff (D-24 — never split).

Purpose: Close EMBP-02. The route becomes a thin wrapper: applyPreflight → idempotency leader/follower → batch-size observe → base64-bypass-increment (route owns this — Risk #2 Option A) → `provider.embed(...)` → re-encode base64 if requested → wrap as OpenAI list → stamp cost → return.

Output: 1 modified source file (`embeddings.ts` shrunk by ~250 LOC — cache loop + dims block + adapter call REMOVED), 1 updated baseline JSON. ONE atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md
@router/src/routes/v1/embeddings.ts
@router/src/providers/embedding-provider.ts
@router/tests/unit/grep-gates/embeddings-untouched.test.ts
@router/tests/unit/grep-gates/embeddings-untouched-baseline.json
@router/tests/routes/embeddings.test.ts
@router/src/cost/computeCostCents.ts

<interfaces>
From PATTERNS.md §"router/src/routes/v1/embeddings.ts (MODIFIED — route refactor)" — what STAYS in the route:

1. **`applyPreflight`** untouched (lines 162-180 of current file).
2. **Idempotency leader/follower gate** untouched.
3. **Capability check** untouched (`'embeddings' in entry.capabilities`).
4. **`router_embeddings_batch_size.observe(inputs.length)`** STAYS at the route, BEFORE provider call (D-07).
5. **Base64 bypass counter** STAYS at the route — before `provider.embed(...)` call:
   ```typescript
   if (body.encoding_format === 'base64' && opts.cache !== undefined) {
     for (let i = 0; i < inputs.length; i++) {
       opts.metrics?.embeddingsCacheTotal.inc({ result: 'bypass' });
     }
   }
   ```
6. **`computeCostCents(usage, entry)`** + `reply.header('X-Cost-Cents', ...)` + `req.computedCostCents` stamp BEFORE `reply.send` (Fastify v5 onSend timing — fires sync inside `.send()`; user memory `project_fastify_onsend_timing.md`).
7. **Base64 re-encode on egress**: when `body.encoding_format === 'base64'`, route encodes provider's `number[]` via existing `encodeBase64` util.
8. **OpenAI list re-wrap**: `{ object: 'list', data: providerResult.embeddings.map((vec, i) => ({ object: 'embedding', index: i, embedding: ... })), model: providerResult.model, usage: providerResult.usage }`.
9. **`recordOutcome` + `bufferedWriter.push`** outer finally untouched.

What MOVES OUT of the route:
- Per-input cache lookup loop (lines 270-318) — now inside provider.
- Adapter construction + `adapter.embeddings(...)` call (lines 320-400) — now inside provider.
- Post-loop dims block + `embeddingsDimsTotal.inc(..., inputs.length)` (lines 399-429) — now inside provider per-served-vector. **Route's dims block DELETED entirely** (D-03 option (a)).
- `makeEmbeddingsCache(...)` construction in route-opts wiring (lives in Plan 19-04's app.ts changes).

From CONTEXT D-10 + Plan 19-04: route reads provider from `req.server.embeddingProvider` (Fastify decorator) by default, with `opts.embeddingProvider` as test-injection fallback (option-bag pattern). Plan 19-04 wires the decorator; this plan codes the route to consume it.

From RESEARCH §5 — P7-01 SHA-256 baseline rotation procedure:
1. Apply route refactor.
2. `NEW_SHA=$(shasum -a 256 router/src/routes/v1/embeddings.ts | awk '{print $1}')`.
3. Update `router/tests/unit/grep-gates/embeddings-untouched-baseline.json`: `sha256` ← NEW_SHA, `captured_at` ← today, `phase` ← `"19"`, `plan` ← `"19-03"`, `rationale` ← per RESEARCH §5 template.
4. Commit BOTH files in ONE git commit.

Current baseline SHA: `b53c6ba1298b8b78b65f75d951e778bd031994fdcd65d14e659f8f3dd666e970`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor /v1/embeddings to delegate to provider + rotate P7-01 baseline (ATOMIC commit)</name>
  <behavior>
    - All ~30 existing cases in `tests/routes/embeddings.test.ts` pass UNCHANGED post-refactor (wire-shape regression net).
    - Wrong-dim adapter still triggers `EmbeddingsDimsMismatchError` → 500 + `pushed[0].status_class === 'server_error'` (existing test at lines 691-740). The throw site moves DEEPER (provider's loop) but envelope mapping is identical.
    - `embeddingsBatchSize.observe(inputs.length)` fires once per request (route-side; D-07).
    - `embeddingsCacheTotal.inc({result:'bypass'})` fires `inputs.length` times when `encoding_format === 'base64'` AND cache present (Risk #2 Option A — verified against the existing base64 test cases).
    - `embeddingsCacheTotal.inc({result:'hit'|'miss'})` fires from inside the provider (existing cache-hit-miss tests still green).
    - `embeddingsDimsTotal.inc(...)` fires from inside the provider per served vector (count equivalent to pre-refactor `inc(..., inputs.length)` — counter sums are invariant).
    - `X-Cost-Cents` header still on response; `request_log.cost_cents` row matches pre-refactor (smoke EMB-H gates verify).
    - SHA-256 of `router/src/routes/v1/embeddings.ts` matches NEW baseline JSON value; `tests/unit/grep-gates/embeddings-untouched.test.ts` passes with the new SHA.
    - Atomic commit invariant: ONE commit contains both `router/src/routes/v1/embeddings.ts` AND `router/tests/unit/grep-gates/embeddings-untouched-baseline.json` diffs.
  </behavior>
  <read_first>
    - router/src/routes/v1/embeddings.ts (FULL FILE — 552 LOC — the route being refactored; you MUST understand every section before removing/restructuring)
    - router/src/providers/embedding-provider.ts (shipped by Plan 19-02 — verify the exact return shape your route consumes)
    - router/tests/routes/embeddings.test.ts (FULL FILE — ~30 test cases; you MUST keep ALL passing — wire-shape regression net)
    - router/tests/unit/grep-gates/embeddings-untouched.test.ts (read in full — understand the gate logic at lines 47-65; the gate reads `embeddings-untouched-baseline.json` and asserts SHA matches; your baseline JSON edit is what makes the gate pass post-refactor)
    - router/tests/unit/grep-gates/embeddings-untouched-baseline.json (current values; rotate sha256/captured_at/phase/plan/rationale fields)
    - router/src/cost/computeCostCents.ts (cost computation untouched; review signature so route still calls correctly)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md decisions D-01..D-10 + D-24
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md §4 (route's post-loop dims check is fully redundant) + §5 (SHA-256 baseline rotation procedure verbatim) + Risk #2 + Risk #3
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md §"router/src/routes/v1/embeddings.ts (MODIFIED — route refactor)" + §"Wire-Shape Preservation (project_fastify_onsend_timing memory)"
    - User memory `project_fastify_onsend_timing.md` — req.computedCostCents MUST be stamped BEFORE reply.send(); NOT in finally
  </read_first>
  <files>router/src/routes/v1/embeddings.ts, router/tests/unit/grep-gates/embeddings-untouched-baseline.json</files>
  <action>
Refactor `router/src/routes/v1/embeddings.ts`:

KEEP (do not modify substantively):
- Top-of-file imports for Fastify, zod, schema (EmbeddingsRequestSchema), `applyPreflight`, `BreakerOpenError`, error envelope helpers, `computeCostCents`, `recordOutcome`, `bufferedWriter` types, `encodeBase64` util.
- `EmbeddingsRequestSchema` zod schema declaration.
- `RegisterEmbeddingsOpts` type — drop `cache?: EmbeddingsCache` field (cache is owned by provider now; D-06), add `embeddingProvider?: EmbeddingProvider` field (D-10 option-bag fallback when decorator absent).
- `registerEmbeddingsRoute(app, opts)` function shell + route registration.
- `applyPreflight` call + breaker-open `Retry-After` handling.
- Capability check (`'embeddings' in entry.capabilities`).
- Idempotency leader/follower gate (multiplexer.acquire / follower replay) — untouched.
- `router_embeddings_batch_size.observe(inputs.length)` (D-07 — stays).
- Base64 bypass increment (Risk #2 Option A):
  ```
  if (body.encoding_format === 'base64' && opts.cache !== undefined) {
    for (let i = 0; i < inputs.length; i++) {
      opts.metrics?.embeddingsCacheTotal.inc({ result: 'bypass' });
    }
  }
  ```
  NOTE: with `opts.cache` removed, the predicate becomes `body.encoding_format === 'base64' && opts.metrics?.embeddingsCacheTotal !== undefined` OR keep the cache-presence semantic via the provider's existence (`opts.embeddingProvider !== undefined OR req.server.embeddingProvider !== undefined`). Pick whichever preserves the EMB-H03 metric semantic byte-identical (verified by existing test cases — if a test asserts bypass count = N for cache-enabled+base64, that exact count must match post-refactor).
- `computeCostCents(...)` + `reply.header('X-Cost-Cents', ...)` + `req.computedCostCents` stamp BEFORE `reply.send()`.
- `recordOutcome` outer finally.

DELETE (entirely):
- Per-input cache lookup loop (~lines 270-318) — provider owns this.
- `makeAdapter` call + `adapter.embeddings(...)` invocation + signal handling — provider owns this.
- Post-loop dims check + `embeddingsDimsTotal.inc(..., inputs.length)` (~lines 399-429) — provider owns this per-vector (D-03 option (a)).
- Local base64-decode helper at the adapter response site (provider does this).

INSERT (the new core):
```
const provider = opts.embeddingProvider ?? (req.server as { embeddingProvider?: EmbeddingProvider }).embeddingProvider;
if (!provider) {
  // Should never happen in production — composition root always provides one.
  throw new Error('embeddings: EmbeddingProvider not injected via opts or fastify.embeddingProvider decorator');
}

const providerResult = await provider.embed(inputs, {
  model: body.model,
  dimensions: body.dimensions,
  user: body.user,
});

// D-02: re-encode to base64 if client asked
const data = providerResult.embeddings.map((vec, i) => ({
  object: 'embedding' as const,
  index: i,
  embedding: body.encoding_format === 'base64' ? encodeBase64(vec) : vec,
}));

const result = {
  object: 'list' as const,
  data,
  model: providerResult.model,
  usage: providerResult.usage,
};
```

Order of operations preserved: applyPreflight → idempotency → capability gate → batch_size observe → base64-bypass increment → provider.embed → base64 re-encode → OpenAI list re-wrap → cost stamp BEFORE send → reply.send → recordOutcome (finally).

Then ROTATE the P7-01 baseline:
1. Compute `NEW_SHA = $(shasum -a 256 router/src/routes/v1/embeddings.ts | awk '{print $1}')`.
2. Edit `router/tests/unit/grep-gates/embeddings-untouched-baseline.json`:
```
{
  "file": "router/src/routes/v1/embeddings.ts",
  "sha256": "<NEW_SHA>",
  "captured_at": "<today YYYY-MM-DD>",
  "phase": "19",
  "plan": "19-03",
  "rationale": "P7-01 BLOCK — baseline rotated as part of EMBP-02 route refactor (delegates to fastify.embeddingProvider per CONTEXT D-09 / D-10). Wire shape verified byte-identical by full vitest regression on router/tests/routes/embeddings.test.ts + smoke Phase 7 EMBED-01 + Phase 12 EMB-H01..06 gates."
}
```
3. Verify `cd router && npx vitest run tests/unit/grep-gates/embeddings-untouched.test.ts` passes.
4. ONE atomic commit containing BOTH file diffs.

Conventional commit: `refactor(19-03): /v1/embeddings delegates to EmbeddingProvider (P7-01 baseline rotated atomically)`.

Hard rule (D-24): if the commit lands with only ONE of the two files, the bisect is broken — Plan 19-03 FAILS and must be reverted + redone as a single commit.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/routes/embeddings.test.ts tests/unit/grep-gates/embeddings-untouched.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd router && npx vitest run tests/routes/embeddings.test.ts` reports ALL ~30 existing cases pass; 0 failed; 0 skipped (skip count baseline matches pre-refactor).
    - `cd router && npx vitest run tests/unit/grep-gates/embeddings-untouched.test.ts` reports 1 pass.
    - `shasum -a 256 router/src/routes/v1/embeddings.ts | awk '{print $1}'` matches `.sha256` field in `router/tests/unit/grep-gates/embeddings-untouched-baseline.json` (post-rotation).
    - `cd router && npx tsc --noEmit` exits 0 (production code clean).
    - `grep -c 'req\.server\.embeddingProvider\|opts\.embeddingProvider' router/src/routes/v1/embeddings.ts` returns at least `2` (the new fallback chain).
    - `grep -c 'adapter\.embeddings\b' router/src/routes/v1/embeddings.ts` returns `0` (adapter call moved out).
    - `grep -c 'embeddingsCacheKey' router/src/routes/v1/embeddings.ts` returns `0` (cache loop moved out).
    - `grep -c 'EmbeddingsDimsMismatchError' router/src/routes/v1/embeddings.ts` returns `0` (dims throw moved out — D-03 option (a)).
    - `grep -c "embeddingsDimsTotal" router/src/routes/v1/embeddings.ts` returns `0` (dims metric increment moved into provider).
    - `grep -c "embeddingsBatchSize.observe" router/src/routes/v1/embeddings.ts` returns at least `1` (D-07 — stays in route).
    - `grep -c "result: 'bypass'" router/src/routes/v1/embeddings.ts` returns at least `1` (Risk #2 Option A — route still owns base64 bypass).
    - `grep -c "req.computedCostCents" router/src/routes/v1/embeddings.ts` returns at least `1` (Fastify v5 onSend timing — stamp before send).
    - The git log shows ONE commit (`refactor(19-03): ...`) containing BOTH `router/src/routes/v1/embeddings.ts` AND `router/tests/unit/grep-gates/embeddings-untouched-baseline.json` in its diff (`git show --stat HEAD | grep -E 'embeddings\.ts|embeddings-untouched-baseline\.json' | wc -l` returns `2`).
    - `cd router && npx vitest run tests/unit/grep-gates/no-default-retriever.test.ts` passes (Frame-01 BLOCK green).
    - `git diff router/package.json router/package-lock.json` empty.
    - Commit scope is `(19-03)`.
  </acceptance_criteria>
  <done>
    `/v1/embeddings` is now a thin route delegating to the provider. P7-01 wire shape preserved byte-identical (regression suite confirms). P7-01 SHA-256 baseline atomically rotated. Provider is the single source of truth for cache + dims. Plan 19-04 (composition root) and Plan 19-06 (smoke) can proceed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /v1/embeddings | Untrusted JSON body; `EmbeddingsRequestSchema` (zod) at route boundary unchanged |
| Route → provider | In-process trusted boundary; provider returns dims-enforced `number[][]` |
| Route → onSend hook | Fastify v5 onSend fires synchronously inside `reply.send()` — `req.computedCostCents` MUST be stamped BEFORE send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-WS | Tampering | `/v1/embeddings` wire shape (P7-01 BLOCK) | mitigate | 4-layer defense: (1) SHA-256 baseline rotated atomically with route diff (same commit — D-24); (2) `tests/routes/embeddings.test.ts` ~30 cases verify wire-shape regression; (3) smoke Phase 7 EMBED-01 + Phase 12 EMB-H01..06 still pass; (4) byte-identical OpenAI list shape verified by re-wrap logic. |
| T-19-03-BCK | Spoofing/Privilege escalation | Capability gate bypass | mitigate | Capability check (`'embeddings' in entry.capabilities`) STAYS at the route — untouched. applyPreflight STAYS upstream of provider call (V4 ASVS). |
| T-19-03-COST | Repudiation | Cost recording goes wrong | mitigate | `computeCostCents(...)` STAYS in route; `req.computedCostCents` stamped BEFORE `reply.send()` (Fastify v5 onSend timing — verified by acceptance criterion grep); outer finally records to `request_log.cost_cents`. |
| T-19-03-VAL | Input validation | Untrusted body | accept | `EmbeddingsRequestSchema` (zod) at route boundary unchanged — provider receives validated inputs. ASVS V5. |
| T-19-03-FR1 | Tampering | Frame-01 BLOCK | mitigate | Route refactor introduces ZERO new classes. Acceptance: `grep -rE 'class \w+EmbeddingProvider' router/src/` returns 0. |
| T-19-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO new packages; supply chain unchanged. Acceptance: `git diff router/package.json router/package-lock.json` empty. |
</threat_model>

<verification>
- `cd router && npx vitest run tests/routes/embeddings.test.ts tests/unit/grep-gates/embeddings-untouched.test.ts tests/providers/embedding-provider.test.ts` returns full green (no regressions).
- `cd router && npm test` full sweep passes — no count regressions from pre-Plan 19-03 baseline (modulo +0 net since refactor preserves wire shape).
- `cd router && npx tsc --noEmit` exits 0.
- `git show --stat HEAD` reveals BOTH `router/src/routes/v1/embeddings.ts` AND `router/tests/unit/grep-gates/embeddings-untouched-baseline.json` in the SAME commit.
- P7-01 baseline JSON post-rotation has `phase: "19"` + `plan: "19-03"` + a new SHA matching the file.
- Provider's per-served-vector `embeddingsDimsTotal` increments add up to the pre-refactor route-side `inc(..., inputs.length)` — counter totals invariant.
- `grep -rE 'class \w+RetrieverProvider' router/src/` empty (Phase 18 Frame-01 BLOCK still green).
- `grep -rE 'class \w+EmbeddingProvider' router/src/` empty (Phase 19 Frame-01 BLOCK still green).
- `git diff router/package.json router/package-lock.json` empty.
</verification>

<success_criteria>
- Route delegates to provider; ~250 LOC removed from route.
- P7-01 baseline rotated in the SAME commit as the route diff (D-24).
- All 30+ `tests/routes/embeddings.test.ts` cases still pass.
- Wire shape byte-identical (verified by smoke gates in subsequent plan and by the regression suite here).
- One atomic commit scoped to `(19-03)`.
</success_criteria>

<output>
Create `.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-03-SUMMARY.md` when done. Must cite:
- OLD baseline SHA → NEW baseline SHA
- LOC removed from route + LOC added
- Confirmation atomic commit constraint honored (git show --stat output)
- Confirmation `tests/routes/embeddings.test.ts` count delta is zero (no test cases added/removed in this plan)
- Any inline deviations (Rule 1/2/3) — none expected; route refactor is mechanical
</output>
