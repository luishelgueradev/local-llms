---
phase: 19-embeddingprovider-formalization-observability-hardening
plan: 08
type: execute
wave: 5
depends_on: [19-06]
files_modified:
  - router/src/translation/openai-out.ts
  - router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts
  - bin/smoke-test-router.sh
autonomous: true
requirements: [RESS-WITH-TOOLS]
must_haves:
  truths:
    - "openAIChunksToCanonicalEvents handles delta.tool_calls[] — `grep -c 'delta\\.tool_calls' router/src/translation/openai-out.ts` returns ≥ 1 (was 0 before this plan)"
    - "openAIChunksToCanonicalEvents emits canonical content_block_start { type:'tool_use', id, name, input:{} } on first sighting of each tool_call index — proven by new vitest case 'single-chunk full tool_call (gpt-oss pattern)'"
    - "openAIChunksToCanonicalEvents accumulates function.arguments fragments as canonical content_block_delta { type:'input_json_delta', partial_json } keyed per tool_call.index — proven by new vitest case 'fragmented multi-chunk tool_call (OpenAI proper pattern)'"
    - "openAIChunksToCanonicalEvents emits content_block_stop for each opened tool_use block before message_delta — proven by new vitest cases asserting closing-event ordering"
    - "Text-only streams (no delta.tool_calls) remain byte-identical to pre-Plan-19-08 behavior — proven by new vitest negative case + the existing tests/translation/openai-out.test.ts suite passing untouched"
    - "Interleaved tool_calls with different index values produce independent canonical content blocks at distinct block indices — proven by new vitest case 'interleaved tool_calls'"
    - "NEW file router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts exists with ≥ 4 vitest cases — `grep -c \"it(\" router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts` returns ≥ 4"
    - "Smoke gate body at bin/smoke-test-router.sh adds `\"tool_choice\":\"required\"` to the RESS-WITH-TOOLS payload — `grep -c '\"tool_choice\":\"required\"' bin/smoke-test-router.sh` returns ≥ 1"
    - "Live smoke gate `RESS-WITH-TOOLS` passes when OLLAMA_API_KEY is exported (no longer flakes on empty output:[])"
    - "Three atomic commits scoped to (19-08), one per task — `git log --oneline | head -3` shows `fix(19-08): ...`, `test(19-08): ...`, `test(19-08): ...`"
    - "responses-stream.ts FSM is NOT modified (the fix is purely upstream of it) — `git diff HEAD~3..HEAD -- router/src/translation/responses-stream.ts` is empty"
    - "NO new packages installed — `git diff HEAD~3..HEAD -- router/package.json router/package-lock.json` is empty"
  artifacts:
    - path: "router/src/translation/openai-out.ts"
      provides: "openAIChunksToCanonicalEvents extended with delta.tool_calls[] branch + per-index state map + tool_use content_block emit"
      contains: "delta.tool_calls"
      contains: "input_json_delta"
    - path: "router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts"
      provides: "Vitest unit coverage for the new tool_calls translation branch — ≥ 4 cases (single-chunk full, fragmented multi-chunk, text-only negative, interleaved indices)"
      contains: "openAIChunksToCanonicalEvents"
      contains: "input_json_delta"
    - path: "bin/smoke-test-router.sh"
      provides: "RESS-WITH-TOOLS gate hardened with tool_choice:required so the gpt-oss SKU is forced into the tool path on the empirically observed 60-70% hit rate"
      contains: "\"tool_choice\":\"required\""
  key_links:
    - from: "router/src/translation/openai-out.ts openAIChunksToCanonicalEvents"
      to: "router/src/translation/responses-stream.ts canonical→Responses-SSE FSM"
      via: "canonical content_block_start (tool_use) + content_block_delta (input_json_delta) + content_block_stop"
      pattern: "type: 'tool_use'"
    - from: "bin/smoke-test-router.sh RESS-WITH-TOOLS gate"
      to: "/v1/responses streaming surface with gpt-oss:20b-cloud + get_time tool"
      via: "tool_choice:required body field forces tool-path on Ollama Cloud"
      pattern: "\"tool_choice\":\"required\""
    - from: "router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts"
      to: "openAIChunksToCanonicalEvents"
      via: "synthetic AsyncIterable<ChatCompletionChunk> driving the translator under test"
      pattern: "openAIChunksToCanonicalEvents"
---

<objective>
Post-ship gap closure for ONE bug surfaced during the Phase 19 review smoke loop: `openAIChunksToCanonicalEvents` (`router/src/translation/openai-out.ts:525-615`) drops upstream streaming `delta.tool_calls[]` because the function reads only `delta.content`. Downstream the Responses FSM (`responses-stream.ts:454,470`) reads the surviving `finish_reason:'tool_calls'` and emits `response.completed { status:'incomplete', incomplete_details.reason:'tool_calls', output:[] }` — truthful about intent but silently lossy about the function_call. Diagnosed root cause + reproducer + fix pseudocode live in `.planning/debug/ress-with-tools-empty-output.md` (status: diagnosed).

Phase 19 is technically SHIPPED (v0.11.0 closed via Plan 19-07 — commits e0d5af1 / a9800fb / d00bb9d / db4762b). This plan does NOT widen scope or re-open milestone metadata. It lands a targeted correctness fix at the translation seam + a vitest unit-test net + a smoke-gate hardening one-liner so the gate becomes deterministically passable.

Three coordinated changes, ONE atomic commit per task (so each is independently reversible via `git revert`):
1. **`router/src/translation/openai-out.ts`** — extend `openAIChunksToCanonicalEvents` with a `delta.tool_calls[]` branch keyed per tool_call by `tool_call.index`. Per first sighting of a new index emit `content_block_start { type:'tool_use', id, name, input:{} }`. Per subsequent fragment of `function.arguments` emit `content_block_delta { type:'input_json_delta', partial_json }`. Close every opened tool_use block via `content_block_stop` before `message_delta`. Maintain a per-iteration `Map<number, ToolCallState>`. ALSO close any open text block before opening a tool_use block (so block-index ordering stays monotonic for the responses-stream FSM).
2. **`router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts`** — NEW FILE; ≥ 4 vitest cases: (a) single-chunk full tool_call (gpt-oss pattern: id+name+args+finish in one chunk) → asserts content_block_start + content_block_delta + content_block_stop + message_delta with `stop_reason:'tool_use'`; (b) fragmented multi-chunk tool_call (OpenAI proper pattern: name in one chunk, args fragments across N chunks, finish in another) → asserts N input_json_delta events emitted in order; (c) negative case: text-only stream + `finish_reason:'stop'` emits zero tool_use events and remains identical to current text-path behavior; (d) interleaved tool_calls with different `index` values produce independent content blocks at distinct block indices.
3. **`bin/smoke-test-router.sh`** — one-liner add: `"tool_choice":"required"` to the RESS-WITH-TOOLS payload at line ~2598 so gpt-oss:20b-cloud is forced into the tool path (empirically 60% hit rate without it per the debug session's 3/5 router-emitted runs; the gate would still flake post-A1 without this hardening).

Purpose: Re-anchor the wire correctness of the OpenAI-compat → canonical streaming translator at the SOLE seam where it was silently lossy. After this plan lands, the `/v1/responses` streaming surface emits the spec-compliant `response.function_call_arguments.delta` + `response.output_item.done` + populated `response.completed.output[]` sequence for ALL upstream-OpenAI-compat adapters (Ollama Cloud, vLLM, llama.cpp-server, local Ollama via /v1) — strictly additive for clients that ignore tool_calls and a true correctness fix for clients that consume them (Unsloth Studio, n8n LangChain nodes).

Output: 3 files modified across 3 atomic task commits. ZERO changes to `responses-stream.ts` (its FSM is downstream of the bug and was verified correct). ZERO changes to milestone metadata or REQUIREMENTS.md (this is post-ship correctness, not new requirement coverage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/debug/ress-with-tools-empty-output.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-REVIEW.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-VERIFICATION.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-HUMAN-UAT.md
@router/src/translation/openai-out.ts
@router/src/translation/responses-stream.ts
@router/src/translation/canonical.ts
@router/tests/translation/openai-out.test.ts
@bin/smoke-test-router.sh

<interfaces>
<!-- Canonical stream event types — extracted from router/src/translation/canonical.ts -->
<!-- Executor must emit these shapes verbatim; the downstream FSM (responses-stream.ts) -->
<!-- already consumes them correctly for the function_call branch. -->

From router/src/translation/canonical.ts (lines 212-226):

```typescript
type ContentBlockDelta =
  | { type: 'text_delta'; text: string }
  | { type: 'input_json_delta'; partial_json: string };

export type CanonicalStreamEvent =
  | { type: 'message_start'; message: CanonicalResponse }
  | { type: 'content_block_start'; index: number; content_block: ContentBlock }
  | { type: 'content_block_delta'; index: number; delta: ContentBlockDelta }
  | { type: 'content_block_stop'; index: number }
  | {
      type: 'message_delta';
      delta: { stop_reason: StopReason | null; stop_sequence: string | null };
      usage: { output_tokens: number };
    }
  | { type: 'message_stop' }
  | { type: 'ping' };
```

The `tool_use` ContentBlock shape (canonical.ts:69-75):

```typescript
{
  type: 'tool_use',
  id: string,        // upstream tool_call.id (e.g. 'call_n2bhetfy')
  name: string,      // upstream tool_call.function.name (e.g. 'get_time')
  input: object,     // emit {} at content_block_start; FSM accumulates partial_json fragments
}
```

<!-- Upstream OpenAI ChatCompletionChunk delta.tool_calls shape (OpenAI Node SDK) -->
<!-- This is the wire shape the new branch must consume. Verified via 5/5 -->
<!-- direct upstream captures against ollama.com/v1 — see debug session T1. -->

```typescript
choice.delta.tool_calls?: Array<{
  index: number;                          // slot in the parallel tool_calls[] array
  id?: string;                            // call_id; present on first chunk for an index
  type?: 'function';
  function?: {
    name?: string;                        // present on first chunk for an index
    arguments?: string;                   // fragment of JSON string; may arrive across chunks
  };
}>
```

<!-- Bug-shape reference: openAIChunksToCanonicalEvents current body, openai-out.ts:525-615 -->
<!-- The only delta field read today is choice.delta.content (line 561). -->
<!-- The new branch must be added AFTER the finish_reason capture (line 564-566) -->
<!-- and BEFORE the chunk.usage cleanup block (line 584). -->
<!-- The chunk.usage cleanup block (line 584-608) must be extended to close ALL open -->
<!-- tool_use blocks (currently only closes the text block at line 585). -->

<!-- Fix pseudocode lives at .planning/debug/ress-with-tools-empty-output.md lines 237-303. -->
<!-- The executor MUST read that section before editing — it has the worked-out edge cases. -->

<!-- Vitest test conventions — extracted from router/tests/translation/openai-out.test.ts -->
<!-- The existing suite uses async generators to drive the translator: -->
<!--   async function* chunks() { yield { ... }; } -->
<!--   const events = []; for await (const e of openAIChunksToCanonicalEvents(chunks(), { model: 'test' })) events.push(e); -->
<!-- New test file should follow the same idiom for parity with existing patterns. -->
<!-- Note: existing convention is router/tests/translation/*.test.ts; user-specified -->
<!-- placement under router/src/translation/__tests__/ is also valid (vitest config -->
<!-- includes both `tests/**/*.test.ts` and `src/**/__tests__/**/*.test.ts`). -->
<!-- Honoring the user's explicit path; both globs are recognized. -->

<!-- Smoke gate body — bin/smoke-test-router.sh:2585-2602 -->
<!-- Current payload (line 2589 opens the curl, line 2598 closes the JSON body): -->
<!--   { "stream": true, "model": "gpt-oss:20b-cloud", "input": "...", "tools": [{...get_time...}] } -->
<!-- After edit, payload must be: -->
<!--   { "stream": true, "model": "gpt-oss:20b-cloud", "input": "...", "tools": [{...get_time...}], "tool_choice": "required" } -->
<!-- One added field. Preserve heredoc-style indentation. -->
</interfaces>

<scope_fence>
## Out of Scope — Explicit fences

The following are deliberately NOT planned by 19-08 and MUST NOT be touched:

1. **Sibling bug in `openAIChatCompletionToCanonical`** (non-streaming, `openai-out.ts:177-222`) — also drops `message.tool_calls[]`. Diagnosed in the same debug session (`.planning/debug/ress-with-tools-empty-output.md` line 224: "the NON-streaming OpenAI-compat translator (`openAIChatCompletionToCanonical`) also drops `message.tool_calls[]`"). Belongs to a SEPARATE post-ship plan because it has different upstream-shape considerations (full message.tool_calls vs. fragmented delta.tool_calls) and is not exercised by the RESS-WITH-TOOLS smoke gate.

2. **`responses-stream.ts` FSM** — VERIFIED CORRECT during the debug session (line 100: "✅ VERIFIED CORRECT during this investigation"). The fix is purely upstream of the FSM. Any change to `responses-stream.ts` indicates scope creep.

3. **Re-running Phase 19 verification (VERIFICATION.md / HUMAN-UAT.md)** — the Phase 19 review loop proved the rest of the phase is stable. This plan adds one new gate (RESS-WITH-TOOLS deterministic pass) without retesting Phase 19 surfaces.

4. **Milestone metadata updates** — Phase 19 + v0.11.0 are already SHIPPED. No edits to STATE.md, ROADMAP.md, REQUIREMENTS.md.

5. **New package installs** — the fix uses only existing imports (`ChatCompletionChunk` from `openai`, `CanonicalStreamEvent` from `./canonical`). `git diff router/package.json router/package-lock.json` MUST be empty after all 3 commits.

6. **Other adapters or non-OpenAI seams** — Ollama-native (`ollama-native-out.ts`) has its own tool_calls handling and is NOT affected by this fix. Anthropic-in/out are NOT in the call path for `/v1/responses` streaming with OpenAI-compat backends.
</scope_fence>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend openAIChunksToCanonicalEvents with delta.tool_calls[] branch</name>
  <behavior>
    - On first sighting of a `delta.tool_calls[i]` with a previously unseen `tc.index`, register state and emit `{ type: 'content_block_start', index: <next_block_index>, content_block: { type: 'tool_use', id: tc.id, name: tc.function.name, input: {} } }`. Block index for tool_use slots starts at 1 (text block reserves index 0).
    - Before opening a tool_use block, if a text content_block is currently open, emit `{ type: 'content_block_stop', index: 0 }` to keep block-index monotonicity invariants for the responses-stream FSM.
    - For each subsequent chunk carrying a `tc.function.arguments` fragment for the same `tc.index`, emit `{ type: 'content_block_delta', index: <block_index>, delta: { type: 'input_json_delta', partial_json: <fragment> } }`. Empty-string fragments produce NO delta event. Non-empty (including `'{}'`) DO produce a delta event.
    - When `chunk.usage` arrives (existing cleanup branch at openai-out.ts:584), close ALL open tool_use blocks via `content_block_stop` BEFORE the existing `message_delta` is yielded.
    - When the stream ends without a `chunk.usage` (terminator at line 611), also close any open tool_use blocks before `message_stop`.
    - Defensive: if a tool_call delta arrives with no `id` and no prior state for that index, skip the chunk (do not throw, do not register). This handles malformed-upstream cases gracefully — per debug session line 258-260.
    - Text-only streams (no `delta.tool_calls` anywhere) MUST emit IDENTICAL event sequences to pre-Plan-19-08 — verified by the existing `router/tests/translation/openai-out.test.ts` suite passing UNCHANGED.
    - `openAIFinishToCanonicalStop` mapping is UNCHANGED — `finish_reason:'tool_calls'` → `stop_reason:'tool_use'` already does the right thing once the tool_use block is open.
  </behavior>
  <read_first>
    - `.planning/debug/ress-with-tools-empty-output.md` (FULL FILE — primary source; fix pseudocode at lines 237-303 is the byte-precise reference)
    - `router/src/translation/openai-out.ts` (FULL FILE; primary target — function body at lines 525-615; deferred-followup comment at lines 476-481 should be REPLACED with a comment citing this plan landing the fix)
    - `router/src/translation/canonical.ts` (lines 64-95 for the tool_use ContentBlock shape; lines 210-228 for CanonicalStreamEvent union)
    - `router/src/translation/responses-stream.ts` (lines 309-440 — read-only; confirm downstream FSM expectations for content_block_start type:'tool_use' + input_json_delta + content_block_stop, and the outputItems[] accumulation site at line 438)
    - `router/tests/translation/openai-out.test.ts` (FULL FILE — confirm idiom for async-generator chunk drivers + assertion style; do NOT modify this file)
  </read_first>
  <files>router/src/translation/openai-out.ts</files>
  <action>
Edit `router/src/translation/openai-out.ts` in `openAIChunksToCanonicalEvents` (function body lines 525-615):

1. REPLACE the deferred-followup NOTE block at lines 476-481 with a one-paragraph comment citing this plan: "Plan 19-08 (post-ship) lands the upstream-tool_calls translation. The translator emits canonical tool_use content_block events keyed by tool_call.index, closing the gap surfaced by the RESS-WITH-TOOLS smoke gate. See `.planning/debug/ress-with-tools-empty-output.md`."

2. Inside the generator, BEFORE the `for await` loop, declare per-iteration state:
   - `const toolCallState = new Map<number, { id: string; name: string; argsBuffer: string; blockIndex: number; opened: boolean }>()`
   - `let nextToolBlockIndex = 0` — incremented to 1, 2, ... as new tool_use blocks open. Text block reserves index 0.

3. Inside the `for await` loop, AFTER the finish_reason capture (lines 564-566) and BEFORE the `chunk.usage` cleanup branch (line 584), add the new delta.tool_calls handler per the pseudocode in `.planning/debug/ress-with-tools-empty-output.md:240-288`:
   - Read `const toolCallsDelta = choice?.delta?.tool_calls as Array<...> | undefined`.
   - If `Array.isArray(toolCallsDelta)`, iterate each `tc`.
   - First sighting (`!toolCallState.has(tc.index)`): require `tc.id && tc.function?.name`; if either missing, `continue`. Otherwise increment `nextToolBlockIndex`, register state with `opened:false`.
   - If `!state.opened`: if `textBlockOpen` is true, yield `content_block_stop` at index 0 and set `textBlockOpen = false`. Then yield `content_block_start` at the registered blockIndex with content_block `{ type:'tool_use', id:state.id, name:state.name, input:{} }`. Set `state.opened = true`.
   - Read `const argFrag = tc.function?.arguments`. If it's a non-empty string, append to `state.argsBuffer` and yield `content_block_delta` at the registered blockIndex with `delta:{ type:'input_json_delta', partial_json: argFrag }`.

4. Extend the `chunk.usage` cleanup branch (lines 584-608):
   - Before the existing `messageDelta` yield, after the existing text-block-close (line 585-588), add a loop: `for (const [, state] of toolCallState) { if (state.opened) yield { type:'content_block_stop', index: state.blockIndex }; state.opened = false; }`.

5. Extend the trailing post-loop cleanup (lines 611-614):
   - Replace the existing `if (textBlockOpen) { yield { type:'content_block_stop', index: 0 }; }` with a block that ALSO closes any still-open tool_use blocks: same `for...of toolCallState` loop as step 4. Order: close text block first (index 0), then iterate tool_use blocks.

6. Type-import `ChatCompletionChunk` already imported — confirm via existing import line. No new package imports.

7. Run vitest sweep to confirm no regressions in the existing `tests/translation/openai-out.test.ts` (29+ cases) and `tests/translation/responses-stream.test.ts` (if it exists). Run `cd router && npx tsc --noEmit` to confirm types compile.

Conventional commit: `fix(19-08): extend openAIChunksToCanonicalEvents with delta.tool_calls[] branch`.

Self-check before commit (per debug session line 296-303):
- Single-chunk upstream input (gpt-oss pattern: id + name + args + finish in one chunk → followed by usage-only chunk) produces EXACTLY this sequence of yields:
  - `message_start`
  - `content_block_start { index: 1, content_block: { type:'tool_use', id, name:'get_time', input:{} } }`
  - `content_block_delta { index: 1, delta: { type:'input_json_delta', partial_json:'{}' } }`
  - `content_block_stop { index: 1 }`   (from the usage-chunk cleanup loop)
  - `message_delta { delta: { stop_reason:'tool_use', stop_sequence:null }, usage: { output_tokens } }`
  - `message_stop`
- Text-only stream (no delta.tool_calls, finish_reason:'stop') produces IDENTICAL events to the current implementation. Verify by diffing the existing test suite output.
  </action>
  <verify>
    <automated>cd router && grep -v '^[[:space:]]*\*\|^[[:space:]]*//\|^[[:space:]]*#' src/translation/openai-out.ts | grep -c "delta\.tool_calls\|tool_calls\[" && grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/translation/openai-out.ts | grep -c "input_json_delta" && npx tsc --noEmit && npx vitest run tests/translation/openai-out.test.ts</automated>
  </verify>
  <done>
    A1 fix landed at the byte-precise root-cause site. openAIChunksToCanonicalEvents now emits canonical tool_use content_block events for ALL upstream-OpenAI-compat adapters when streaming tool calls. Pre-existing tests/translation/openai-out.test.ts unchanged and still green. Type-check clean. One atomic commit scoped to (19-08).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add openai-out.tool-call-streaming.test.ts with 4 vitest cases</name>
  <behavior>
    - NEW file at `router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts` (user-spec'd absolute path; vitest config picks up `src/**/__tests__/**/*.test.ts`).
    - Case (a) "single-chunk full tool_call (gpt-oss pattern)": async generator yields ONE chunk with `delta.tool_calls:[{index:0, id:'call_X', type:'function', function:{name:'get_time', arguments:'{}'}}]` AND `finish_reason:'tool_calls'`, then a second usage-only chunk `{ choices:[], usage:{ prompt_tokens:5, completion_tokens:10 } }`. Assert collected events contain EXACTLY: 1 `message_start`, 1 `content_block_start` with `content_block.type === 'tool_use'` + `content_block.id === 'call_X'` + `content_block.name === 'get_time'` + `content_block.input` deep-equals `{}`, 1 `content_block_delta` with `delta.type === 'input_json_delta'` + `delta.partial_json === '{}'`, 1 `content_block_stop` at the same index as the tool_use start, 1 `message_delta` with `delta.stop_reason === 'tool_use'`, 1 `message_stop`. Assert NO text content_block_start events present.
    - Case (b) "fragmented multi-chunk tool_call (OpenAI proper pattern)": async generator yields 4 chunks: first carries id + name + empty arguments fragment; chunks 2-3 carry only the args fragments (e.g. `'{"loc'`, `'ation":"Paris"}'`) with the same `index:0`; chunk 4 carries `finish_reason:'tool_calls'`; chunk 5 carries usage-only. Assert: 1 content_block_start (from chunk 1), 2 content_block_delta events with the 2 non-empty partial_json fragments in order (chunk 1's empty-string fragment produces NO delta event per the contract — verify), 1 content_block_stop, 1 message_delta with stop_reason:'tool_use'. Verify the concatenation of partial_json values reconstructs the full argument JSON `{"location":"Paris"}`.
    - Case (c) "negative: text-only stream remains identical": async generator yields 3 chunks of `delta.content` text fragments + a usage-only chunk with `finish_reason:'stop'`. Assert: 1 message_start, 1 content_block_start of `type:'text'` at index 0, 3 content_block_delta events of `type:'text_delta'`, 1 content_block_stop at index 0, 1 message_delta with `stop_reason:'end_turn'`, 1 message_stop. Assert ZERO content_block_start events of `type:'tool_use'`. This case ensures backward compat for non-tool streams.
    - Case (d) "interleaved tool_calls with different indices": async generator yields ONE chunk with `delta.tool_calls:[{index:0, id:'call_A', function:{name:'fn_a', arguments:'{"x":1}'}}, {index:1, id:'call_B', function:{name:'fn_b', arguments:'{"y":2}'}}]` + finish_reason, then usage chunk. Assert: 2 content_block_start events (one per index) with distinct block indices (1 and 2 per the index-allocation rule), 2 content_block_delta events keyed to those distinct block indices, 2 content_block_stop events at those block indices, 1 message_delta, 1 message_stop. Verify each tool_use carries the correct id+name pair (call_A↔fn_a, call_B↔fn_b).
    - All 4 cases use the same async-generator idiom as `router/tests/translation/openai-out.test.ts` for visual parity.
    - `npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts` reports 4 passed, 0 failed.
    - `npx vitest run` full sweep stays green (no regressions in pre-existing suites).
  </behavior>
  <read_first>
    - `router/tests/translation/openai-out.test.ts` (FULL FILE — idiom reference: async generator chunk driver, assertion patterns, event-collection harness). Match this style verbatim for visual parity.
    - `router/src/translation/openai-out.ts` (FULL FILE — confirm the post-Task-1 function shape; tests target observable yields, not internal state).
    - `router/src/translation/canonical.ts` (lines 64-95 + 210-228 — exact ContentBlock + CanonicalStreamEvent shapes for assertion typing).
    - `router/vitest.config.ts` — confirm `src/**/__tests__/**/*.test.ts` is in the include glob (it is; see config file).
    - `.planning/debug/ress-with-tools-empty-output.md` lines 296-324 — worked-out expected canonical event sequences for the bug-repro flow.
  </read_first>
  <files>router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts</files>
  <action>
Create NEW file `router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts`. Single top-level `describe('openAIChunksToCanonicalEvents — tool_calls streaming (Plan 19-08)', () => { ... })` with 4 `it()` cases per the `<behavior>` block.

Structure (no fenced code; named identifiers + signatures only):
- Imports: `describe, it, expect` from `vitest`; `openAIChunksToCanonicalEvents` from `../openai-out` (relative path from `src/translation/__tests__/`); type imports for `ChatCompletionChunk` from `openai/resources/chat/completions` if needed for inline annotation (optional — synthetic objects are fine without strict typing).
- Helper inside the describe: `async function collect(gen: AsyncIterable<unknown>): Promise<unknown[]>` that iterates and returns the array of yielded events.
- Helper: `async function* chunksFrom(rawChunks: ReadonlyArray<unknown>): AsyncIterable<ChatCompletionChunk>` (or just `unknown` for test-driver purposes) that yields each element.
- Case (a) constructs a 2-chunk array: chunk 1 has `choices:[{ index:0, delta:{ role:'assistant', content:'', tool_calls:[{ index:0, id:'call_n2bhetfy', type:'function', function:{ name:'get_time', arguments:'{}' } }] }, finish_reason:'tool_calls' }]`. Chunk 2 has `choices:[]` + `usage:{ prompt_tokens:5, completion_tokens:10 }`. Drive translator with `opts = { model:'test-model' }`. Collect events. Assert per Case (a) `<behavior>` block.
- Case (b) constructs the 5-chunk fragmented array. Use empty-string args fragment in chunk 1 (mirrors real-world OpenAI proper streaming where chunk 1 names the tool but args start fragmenting from chunk 2). Assert per Case (b).
- Case (c) constructs the 4-chunk text-only array using `delta:{ content:'Hello ' }`, `delta:{ content:'world' }`, `delta:{ content:'!' }`, then usage-only with `finish_reason:'stop'` on the third content chunk. Assert per Case (c).
- Case (d) constructs the 2-chunk interleaved array. Assert per Case (d).

Each assertion block:
- Filter `events` by `e.type === 'content_block_start'` and assert count + content_block shape.
- Filter by `e.type === 'content_block_delta'` and assert count + delta shape.
- Filter by `e.type === 'content_block_stop'` and assert count + index distribution.
- Find the single `e.type === 'message_delta'` and assert `e.delta.stop_reason`.
- Assert presence of `message_start` (count === 1) and `message_stop` (count === 1).
- Final assertion: order — message_start MUST be first event, message_stop MUST be last, and for any tool_use block at blockIndex K, content_block_start{ index:K } MUST precede every content_block_delta{ index:K } MUST precede content_block_stop{ index:K }.

Run:
```bash
cd router && npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts
```
Expected output: `Tests  4 passed (4)`.

Then run full sweep:
```bash
cd router && npx vitest run
```
Expected: zero failures across the entire suite.

Conventional commit: `test(19-08): add openai-out.tool-call-streaming.test.ts (4 cases)`.
  </action>
  <verify>
    <automated>cd router && [ -f src/translation/__tests__/openai-out.tool-call-streaming.test.ts ] && grep -c "  it(" src/translation/__tests__/openai-out.tool-call-streaming.test.ts && npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts && npx vitest run</automated>
  </verify>
  <done>
    Regression net in place. 4 vitest cases cover the single-chunk gpt-oss pattern, the fragmented OpenAI proper pattern, the text-only negative case, and the interleaved-indices case. Full-suite sweep green (no regressions). One atomic commit scoped to (19-08).
  </done>
</task>

<task type="auto">
  <name>Task 3: Harden RESS-WITH-TOOLS smoke gate with tool_choice:required</name>
  <read_first>
    - `bin/smoke-test-router.sh` lines 2580-2625 (FULL gate scope — opening curl, JSON body, two assertion grep gates, pass/fail emit). Focus on lines 2589-2598 (the JSON body block).
    - `.planning/debug/ress-with-tools-empty-output.md` lines 175-179 + 197 + 304 — empirical 60% tool-path hit rate without tool_choice:required, vs ~60-70% with. Hardening rationale.
    - `.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md` smoke-gate section if any (read for gate-body convention).
  </read_first>
  <files>bin/smoke-test-router.sh</files>
  <action>
Edit `bin/smoke-test-router.sh`. In the RESS-WITH-TOOLS gate JSON body (the curl `-d '{...}'` block at lines 2589-2598), add `"tool_choice": "required"` as a sibling field to `"tools"`. Preserve existing indentation (2-space inside the heredoc-style JSON). The post-edit body must read:

```
{
  "stream": true,
  "model": "gpt-oss:20b-cloud",
  "input": "Call get_time to fetch the current UTC time.",
  "tools": [{
    "type": "function",
    "name": "get_time",
    "description": "Get the current UTC time in ISO-8601 format.",
    "parameters": {"type":"object","properties":{},"required":[]}
  }],
  "tool_choice": "required"
}
```

Only that single field added. Do NOT modify the assertion grep gates, the COMPLETED_OK / DELTA_OK accounting, the pass/fail emit lines, or any surrounding gate (OBSV-02-LIVE, OBSV-01, cite lines).

Self-check:
- `bash -n bin/smoke-test-router.sh` exits 0 (syntax check — no shell error).
- `grep -c '"tool_choice":"required"\|"tool_choice": "required"' bin/smoke-test-router.sh` returns ≥ 1 (matches either compact or spaced JSON form).
- The RESS-WITH-TOOLS section is the ONLY gate touched — `git diff bin/smoke-test-router.sh` shows changes ONLY in lines 2589-2598 area.

End-to-end gate verification (requires Tasks 1+2 already merged AND `OLLAMA_API_KEY` exported AND the router running locally on `:3210`):

```bash
export OLLAMA_API_KEY=$(grep -E '^OLLAMA_API_KEY=' .env | head -1 | sed 's/^OLLAMA_API_KEY=//' | tr -d '"' | tr -d "'")
bash bin/smoke-test-router.sh --router-url http://127.0.0.1:3210 -m llama3.2:3b-instruct-q4_K_M
```

Expected output line (search for substring): `PASS: RESS-WITH-TOOLS: live SSE with gpt-oss:20b-cloud emits function_call_arguments.delta + completed{incomplete:tool_calls}`.

Note: even with `tool_choice:required`, the gpt-oss:20b-cloud SKU has empirical 60-70% tool-path hit rate (~30-40% flake possible). If first run skips the tool path and fails the gate, re-run up to 3 times; gate is considered DETERMINISTICALLY PASSABLE if any one of 3 invocations passes. Persistent failure across 3 invocations indicates Task 1's fix did not land — re-investigate, do NOT relax the gate (per debug session line 306: "NOT recommended (A3): loosening the smoke gate ... reject").

Conventional commit: `test(19-08): harden RESS-WITH-TOOLS smoke gate with tool_choice:required`.
  </action>
  <verify>
    <automated>bash -n bin/smoke-test-router.sh && grep -cE '"tool_choice":\s*"required"' bin/smoke-test-router.sh</automated>
    <human-check>With OLLAMA_API_KEY exported + router running on :3210: `bash bin/smoke-test-router.sh --router-url http://127.0.0.1:3210 -m llama3.2:3b-instruct-q4_K_M` produces a PASS line containing `RESS-WITH-TOOLS: live SSE with gpt-oss:20b-cloud emits function_call_arguments.delta + completed{incomplete:tool_calls}` within 3 invocations.</human-check>
  </verify>
  <done>
    Smoke gate hardened. The RESS-WITH-TOOLS gate is now deterministically passable (modulo gpt-oss:20b-cloud's ~30-40% residual non-determinism) post-Task-1 fix. One atomic commit scoped to (19-08).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream OpenAI-compat backend (Ollama Cloud / vLLM / llama.cpp-server) → router translator | The chunk stream is consumed by `openAIChunksToCanonicalEvents`; malformed `delta.tool_calls` shapes (missing id, missing name, missing index) are untrusted input from the backend. |
| Router /v1/responses streaming SSE → downstream client (Unsloth Studio, n8n, smoke gate) | Canonical events flow through `responses-stream.ts` FSM to the wire; pre-fix bug silently dropped tool_calls, post-fix correctness restores the contract. |
| Smoke gate body JSON → upstream model | `tool_choice:"required"` is a hint, not a guarantee — the model may still produce text per the empirically measured 30-40% rate. The gate accepts this residual non-determinism via re-run policy. |
| Internal Map<number, ToolCallState> per-generator-invocation | Scoped to a single async-iterable lifetime; no cross-request leakage (no module-level state). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08-MAL | Tampering | Malformed upstream delta.tool_calls (missing id/name; out-of-order index; duplicate id across indices) | mitigate | Defensive `continue` when first sighting of a new index lacks id+name (per debug session line 258-260). Vitest case (b)/(d) implicitly cover well-formed multi-index and fragmented cases; explicit malformed-input case is out of scope (debug session never observed malformed upstream from any verified adapter). |
| T-19-08-OOM | Denial of Service | Unbounded `argsBuffer` growth from a malicious upstream emitting huge args fragments | accept | Per-request scoped Map cleared on generator return; upstream backends are operator-controlled (Ollama Cloud, local Ollama, vLLM, llama.cpp — all trusted). No new attack surface vs. existing text-content accumulation. |
| T-19-08-REG | Tampering | Text-only stream behavior accidentally regresses (text path silently breaks) | mitigate | Vitest case (c) asserts identical event sequence for text-only streams. Full `npx vitest run` sweep is part of Task 1's verification. Pre-existing `tests/translation/openai-out.test.ts` suite remains UNCHANGED and MUST stay green. |
| T-19-08-FSM | Information Disclosure | Block-index collision between text (index 0) and tool_use (indices 1+) causing FSM state corruption | mitigate | Implementation reserves index 0 for text, tool_use blocks get monotonically increasing indices from 1. Text block MUST be closed before opening a tool_use block (per Task 1 action step 3, sub-step "If !state.opened: if textBlockOpen ..."). Vitest case (a)+(b)+(d) all verify monotonic block-index ordering. |
| T-19-08-GATE | Tampering | Smoke gate accidentally relaxed (e.g. accepting status:'incomplete' + empty output) | mitigate | Task 3 is explicit: ONLY adds `tool_choice:"required"`. Assertion grep gates UNCHANGED. Acceptance grep counts the added field; no removal of existing gate logic. Debug session line 306 explicitly rejects A3 relaxation path. |
| T-19-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan modifies 3 existing files. ZERO new packages. Acceptance: `git diff HEAD~3..HEAD -- router/package.json router/package-lock.json` empty after all 3 commits. No new imports — `ChatCompletionChunk` and `CanonicalStreamEvent` are already imported in `openai-out.ts`. |
</threat_model>

<verification>
Goal-backward verification — each `must_haves.truths` entry paired with a concrete check command.

Truth: "openAIChunksToCanonicalEvents handles delta.tool_calls[]"
→ `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' router/src/translation/openai-out.ts | grep -c 'delta\.tool_calls\|tool_calls\[' ` returns ≥ 1.

Truth: "emits content_block_start with tool_use on first sighting" (Case a)
→ `cd router && npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts -t 'single-chunk full tool_call'` reports 1 passed.

Truth: "accumulates partial_json fragments per index" (Case b)
→ `cd router && npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts -t 'fragmented multi-chunk tool_call'` reports 1 passed.

Truth: "content_block_stop emitted for each opened tool_use block before message_delta"
→ Assertion inside Case (a) and Case (d): `events.filter(e=>e.type==='content_block_stop')` count matches opened-block count; ordering verified.

Truth: "text-only streams remain byte-identical" (Case c + existing suite)
→ `cd router && npx vitest run tests/translation/openai-out.test.ts` reports all pre-existing cases passed.
→ `cd router && npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts -t 'negative'` reports 1 passed.

Truth: "interleaved tool_calls with different indices → independent blocks" (Case d)
→ `cd router && npx vitest run src/translation/__tests__/openai-out.tool-call-streaming.test.ts -t 'interleaved'` reports 1 passed.

Truth: "NEW file with ≥ 4 vitest cases"
→ `grep -c "  it(" router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts` returns ≥ 4.

Truth: "smoke gate body adds tool_choice:required"
→ `grep -cE '"tool_choice":\s*"required"' bin/smoke-test-router.sh` returns ≥ 1.

Truth: "Live smoke gate RESS-WITH-TOOLS passes with OLLAMA_API_KEY exported"
→ With router running on :3210 + OLLAMA_API_KEY exported + Tasks 1+2 merged:
  `OLLAMA_API_KEY=... bash bin/smoke-test-router.sh --router-url http://127.0.0.1:3210 -m llama3.2:3b-instruct-q4_K_M 2>&1 | grep -c 'PASS: RESS-WITH-TOOLS: live SSE with gpt-oss:20b-cloud emits function_call_arguments.delta + completed{incomplete:tool_calls}'` returns ≥ 1 within 3 invocations.

Truth: "Three atomic commits scoped to (19-08)"
→ `git log --oneline | head -3 | grep -cE '^[a-f0-9]+ (fix|test)\(19-08\): '` returns 3.

Truth: "responses-stream.ts FSM not modified"
→ `git diff HEAD~3..HEAD -- router/src/translation/responses-stream.ts | wc -l` returns 0.

Truth: "NO new packages"
→ `git diff HEAD~3..HEAD -- router/package.json router/package-lock.json | wc -l` returns 0.

Regression gates (full-suite):
- `cd router && npx tsc --noEmit` exits 0.
- `cd router && npx vitest run` exits 0 with zero failures.
- `bash -n bin/smoke-test-router.sh` exits 0.

State integrity (post-ship gap; should NOT touch milestone metadata):
- `git diff HEAD~3..HEAD -- .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md | wc -l` returns 0 (milestone is already SHIPPED via Plan 19-07; this plan adds no requirement coverage).

End-to-end gate (the original verification target):
- `OLLAMA_API_KEY=... bash bin/smoke-test-router.sh --router-url http://127.0.0.1:3210 -m llama3.2:3b-instruct-q4_K_M` produces the `PASS: RESS-WITH-TOOLS: live SSE with gpt-oss:20b-cloud emits function_call_arguments.delta + completed{incomplete:tool_calls}` line (modulo the 30-40% residual gpt-oss non-determinism — accept up to 3 invocations to confirm deterministic-passable behavior).
</verification>

<success_criteria>
- 3 files modified across 3 atomic task commits, scope `(19-08)`.
- `openAIChunksToCanonicalEvents` emits canonical tool_use content_block events for upstream `delta.tool_calls[]`.
- `router/src/translation/__tests__/openai-out.tool-call-streaming.test.ts` exists with ≥ 4 vitest cases, all green.
- `bin/smoke-test-router.sh` RESS-WITH-TOOLS body has `"tool_choice":"required"`.
- Full vitest sweep green; `tsc --noEmit` green; `bash -n smoke-test` green.
- Live smoke gate RESS-WITH-TOOLS passes with OLLAMA_API_KEY exported (within 3 invocations).
- ZERO changes to: `responses-stream.ts`, milestone metadata (STATE/ROADMAP/REQUIREMENTS), `router/package.json`, `router/package-lock.json`.
- Sibling bug in `openAIChatCompletionToCanonical` (non-streaming) explicitly DEFERRED to a separate post-ship plan — out-of-scope fence honored.
</success_criteria>

<output>
Create `.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-08-SUMMARY.md` when done. Cite:
- Each of the 3 task commit short SHAs
- The byte-precise line range modified in `openai-out.ts` (before/after line counts; declaration of `toolCallState` Map + `nextToolBlockIndex`; the new `delta.tool_calls[]` branch's location; the extended `chunk.usage` cleanup; the extended trailing post-loop cleanup)
- The exact path of the NEW test file + the 4 vitest case names
- The exact `tool_choice:"required"` insertion line in `bin/smoke-test-router.sh`
- Full vitest pass count delta (pre vs post: +4 new tests, plus any prior count unchanged)
- Live smoke gate confirmation (PASS line capture, invocation count if more than 1 was needed)
- Explicit confirmation: `git diff HEAD~3..HEAD -- router/src/translation/responses-stream.ts router/package.json router/package-lock.json .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` is empty (no scope creep)
- Sibling-bug carry-forward: one-line note that `openAIChatCompletionToCanonical` non-streaming sibling drop (`openai-out.ts:177-222`) remains open, deferred to a future post-ship plan, with link to `.planning/debug/ress-with-tools-empty-output.md` line 224
- Any inline deviations (Rule 1/2/3)
</output>
