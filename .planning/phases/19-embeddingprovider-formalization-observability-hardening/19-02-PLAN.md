---
phase: 19-embeddingprovider-formalization-observability-hardening
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - router/src/providers/embedding-provider.ts
  - router/tests/providers/embedding-provider.test.ts
autonomous: true
requirements: [EMBP-01]
must_haves:
  truths:
    - "EmbeddingProvider interface is exported from router/src/providers/embedding-provider.ts with the locked D-01..D-05 signature"
    - "D-04: embed(input, opts: { model, dimensions?, user? }) — provider runs registry.resolve(model) + capability check (`embeddings` in entry.capabilities) + makeAdapter(entry) INTERNALLY; route still runs applyPreflight(body.model, { registry, breaker }) UPSTREAM of the provider call so policy + breaker fire once per wire request"
    - "makeOpenAIEmbeddingProvider is exported as a factory returning an object literal (Frame-01 — NOT a class)"
    - "Cache lookup logic + dims enforcement live INSIDE the provider (D-06 + D-03 option (a))"
    - "Provider always works in float (D-02 — never accepts/passes encoding_format='base64')"
    - "D-08: provider returns usage = { prompt_tokens, total_tokens } aggregated across cache-miss sub-batch only (cache hits contribute 0); route owns X-Cost-Cents header + request_log.cost_cents stamping via computeCostCents(usage, entry)"
    - "D-12: provider conformance test in router/tests/providers/embedding-provider.test.ts uses makeFakeEmbeddingProvider({ dims = 1024 }) from tests/fakes.ts for fake-path assertions; upstream-behavior cases use existing MSW fixtures"
    - "Provider throws RegistryUnknownModelError + CapabilityNotSupportedError + EmbeddingsDimsMismatchError — NO new error types"
    - "EMB-H04 fail-open semantics preserved in provider cache reads: warn log + fall through, NO metric increment"
    - "Wave-0 conformance test placeholders flipped to real assertions (4 it.todo → 4 it()) covering expectTypeOf + runtime shape + string-vs-array input"
    - "Frame-01 grep gate (`class \\w+EmbeddingProvider` against router/src/) returns 0"
    - "P7-01 baseline (SHA-256 of router/src/routes/v1/embeddings.ts) UNCHANGED — route untouched in this plan"
  artifacts:
    - path: "router/src/providers/embedding-provider.ts"
      provides: "EmbeddingProvider interface + MakeOpenAIEmbeddingProviderOpts type + makeOpenAIEmbeddingProvider factory"
      contains: "export interface EmbeddingProvider"
      contains: "export function makeOpenAIEmbeddingProvider"
    - path: "router/tests/providers/embedding-provider.test.ts"
      provides: "Flipped conformance test (5 real it() — 1 sentinel becomes real + 4 it.todo → it())"
  key_links:
    - from: "router/src/providers/embedding-provider.ts:makeOpenAIEmbeddingProvider"
      to: "router/src/embeddings/cache.ts:embeddingsCacheKey"
      via: "import"
      pattern: "import.*embeddingsCacheKey"
    - from: "router/src/providers/embedding-provider.ts:makeOpenAIEmbeddingProvider"
      to: "router/src/errors/envelope.ts:EmbeddingsDimsMismatchError"
      via: "import + throw"
      pattern: "throw new EmbeddingsDimsMismatchError"
---

<objective>
Ship the production `EmbeddingProvider` interface + `makeOpenAIEmbeddingProvider` factory at `router/src/providers/embedding-provider.ts`. Move per-input Valkey cache logic, dims enforcement, and base64-decode into the provider (D-03, D-06). Provider returns the normalized `{ embeddings: number[][], model, usage }` shape (D-01) — the `/v1/embeddings` route refactor lives in Plan 19-03. Flip the Wave-0 conformance test from `it.todo` to real `it()` assertions.

Purpose: Establish the single-source-of-truth seam for embeddings (per RetrieverProvider/SessionStore pattern). Future caller-supplied RetrieverProvider implementations can consume `fastify.embeddingProvider.embed(query, { model })` without HTTP round-tripping. Wire shape preservation is enforced by Plan 19-03 + the SHA-256 baseline rotation that lives there.

Output: 1 new source file (~280 LOC estimated — interface, opts type, factory body, cache loop, dims gate); 1 flipped test file (5 real `it()` cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md
@router/src/providers/retriever-provider.ts
@router/src/providers/session-store.ts
@router/src/embeddings/cache.ts
@router/src/routes/v1/embeddings.ts
@router/src/backends/adapter.ts
@router/src/errors/envelope.ts
@router/tests/providers/session-store.interface.test.ts

<interfaces>
From CONTEXT D-01..D-05 (locked) — full EmbeddingProvider interface:

```typescript
export interface EmbeddingProvider {
  embed(
    input: string | string[],
    opts: { model: string; dimensions?: number; user?: string },
  ): Promise<{
    embeddings: number[][];
    model: string;
    usage: { prompt_tokens: number; total_tokens: number };
  }>;
}
```

From CONTEXT "Specific Ideas" lines 302-362 + PATTERNS.md §"router/src/providers/embedding-provider.ts" — factory opts:

```typescript
export interface MakeOpenAIEmbeddingProviderOpts {
  registry: RegistryStore;
  makeAdapter: AdapterFactory;
  valkey?: Redis;
  env?: { ROUTER_EMBED_CACHE_TTL_SEC: number };
  metrics: {
    embeddingsCacheTotal: { inc(labels: { result: 'hit' | 'miss' | 'bypass' }): void };
    embeddingsDimsTotal: { inc(labels: { model: string; dims: string }, value?: number): void };
  };
  log: Logger;
}
```

From `router/src/routes/v1/embeddings.ts` (lines 270-318) — cache lookup loop that MOVES into provider verbatim (preserves EMB-H04 fail-open: try/catch around `cache.get`; warn + fall through + NO metric increment).

From `router/src/routes/v1/embeddings.ts` (lines 399-429) — dims gate that MOVES into provider per D-03 option (a). Provider's loop throws `EmbeddingsDimsMismatchError(entry.name, entry.dims, vec.length)` BEFORE returning, increments `embeddingsDimsTotal{model, dims}` per served vector.

From `router/src/backends/adapter.ts` lines 94-108 — `BackendAdapter.embeddings(input, model, signal?, opts?)` is the upstream call. Provider always passes `encoding_format: 'float'` (D-02) so base64 decode never happens inside provider; if upstream returns base64 anyway (some adapters do), decode at the boundary using existing util.

From `router/src/errors/envelope.ts` — reuse `RegistryUnknownModelError`, `CapabilityNotSupportedError`, `EmbeddingsDimsMismatchError`, `BreakerOpenError`. NO new error classes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship router/src/providers/embedding-provider.ts (interface + factory) + flip conformance tests</name>
  <behavior>
    - EmbeddingProvider interface matches D-01..D-05 signature byte-for-byte.
    - makeOpenAIEmbeddingProvider returns an object literal (not a class); `embed()` resolves model, gates capability, runs per-input Valkey lookup, calls upstream for misses, enforces dims, returns normalized shape.
    - Cache hit: `embeddingsCacheTotal.inc({result:'hit'})`; miss: `inc({result:'miss'})` then upstream call. Bypass increment is NOT made by the provider (route owns bypass for base64 path — Risk #2 Option A).
    - On cache get/set error: warn-log + fall through to upstream (EMB-H04 fail-open); NO metric increment.
    - Provider always passes `encoding_format: 'float'` to upstream (D-02).
    - For each vector returned by upstream: if `entry.dims !== undefined && vec.length !== entry.dims` → throw `EmbeddingsDimsMismatchError(entry.name, entry.dims, vec.length)`; else increment `embeddingsDimsTotal.inc({model: entry.name, dims: String(vec.length)})` per-vector.
    - Returns `{ embeddings: slots.filter(v => v) as number[][], model: entry.name, usage: aggregated }`.
    - String input becomes a 1-element array internally; output `embeddings.length === 1`.
    - Array input preserves order; cache-hit slots interleave with upstream-fetched slots correctly.
    - Conformance test (4 it.todo → 4 it()): expectTypeOf signature, runtime shape (1 input), string-vs-array union (2 cases), fake provider with `shouldThrow` propagates error.
    - Runtime sentinel `await import('../../src/providers/embedding-provider.js')` now PASSES (no longer Cannot find module).
  </behavior>
  <read_first>
    - router/src/providers/retriever-provider.ts (full file — Frame-01 header tone + JSDoc style; interface-only file analog)
    - router/src/providers/session-store.ts (full file — interface + companion types collocation analog; how `Turn`, `AppendTurnResult`, `LoadHistoryOpts` are exported alongside the interface)
    - router/src/routes/v1/embeddings.ts lines 162-180 (applyPreflight pattern), lines 270-318 (cache lookup loop — MOVE into provider), lines 320-400 (adapter call + base64 decode), lines 399-429 (dims enforcement — MOVE into provider). DO NOT EDIT this file in this plan — Plan 19-03 owns the route refactor.
    - router/src/embeddings/cache.ts (full file — `embeddingsCacheKey`, `CachedVector`, `EmbeddingsCache`, `makeEmbeddingsCache`)
    - router/src/backends/adapter.ts lines 94-108 (`BackendAdapter.embeddings` signature — upstream call shape)
    - router/src/errors/envelope.ts (search for `EmbeddingsDimsMismatchError`, `CapabilityNotSupportedError`, `RegistryUnknownModelError`, `BreakerOpenError` — confirm all 4 are exported)
    - router/src/config/registry.ts (search for `RegistryStore` / `resolve` method — provider calls `opts.registry.get().resolve(model)`)
    - router/src/backends/factory.ts (search for `AdapterFactory` type — exact type to import for `opts.makeAdapter`)
    - router/tests/providers/session-store.interface.test.ts (full file — conformance test analog; mirror imports + describe + expectTypeOf + runtime assertions)
    - router/tests/providers/embedding-provider.test.ts (Wave-0 scaffold from Plan 19-01 — file being flipped)
    - router/tests/fakes.ts (verify `makeFakeEmbeddingProvider` from Plan 19-01 is exported; tests use the fake)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md decisions D-01..D-08 + D-12 + D-20
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md §"Answered Unknowns" #3 + #4 + Risk #2 + Risk #3 (Risk #2 confirms route owns base64-bypass; provider only emits hit/miss)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md §"router/src/providers/embedding-provider.ts" + §"Frame-01" + §"Conformance Test"
  </read_first>
  <files>router/src/providers/embedding-provider.ts, router/tests/providers/embedding-provider.test.ts</files>
  <action>
Create `router/src/providers/embedding-provider.ts` as a single file containing:
1. JSDoc header citing Phase 19 + v0.11.0 + EMBP-01 + Frame-01 BLOCK + strategic frame ("Retrieval Interfaces, not Retrieval Logic"). Tone matches `retriever-provider.ts` lines 1-16.
2. Imports: `Redis` from `ioredis`, `Logger` type from `pino` (or wherever the existing `bootLog` type comes from — check `index.ts:68`), `RegistryStore` from `'../config/registry.js'`, `AdapterFactory` from `'../backends/factory.js'`, `embeddingsCacheKey` + `CachedVector` from `'../embeddings/cache.js'`, all 4 error classes from `'../errors/envelope.js'` (`RegistryUnknownModelError`, `CapabilityNotSupportedError`, `EmbeddingsDimsMismatchError`, `BreakerOpenError`).
3. Export `interface EmbeddingProvider` exactly matching CONTEXT D-01..D-05 signature (see `<interfaces>` block above).
4. Export `interface MakeOpenAIEmbeddingProviderOpts` matching CONTEXT "Specific Ideas" + PATTERNS.md.
5. Export `function makeOpenAIEmbeddingProvider(opts: MakeOpenAIEmbeddingProviderOpts): EmbeddingProvider` returning an object literal `{ async embed(input, callOpts) { ... } }`. Body:
   - Coerce input to `inputs: string[]` (Array.isArray check).
   - Call `opts.registry.get().resolve(callOpts.model)` — throws `RegistryUnknownModelError` on miss.
   - Check `entry.capabilities.includes('embeddings')` — throw `CapabilityNotSupportedError(entry.name, 'embeddings')` if absent.
   - Build `adapter = opts.makeAdapter(entry)`.
   - Allocate `slots: Array<number[] | null> = new Array(inputs.length).fill(null)` and `missIndices: number[] = []`.
   - Per-input cache loop (only when `opts.valkey` is present): compute `embeddingsCacheKey({ backend: entry.backend, backend_model: entry.backend_model, encoding_format: 'float', dimensions: callOpts.dimensions, input: inputs[i] })`; `try { const cached = await cache.get(key); if (cached) { slots[i] = cached; inc({result:'hit'}); } else { missIndices.push(i); inc({result:'miss'}); } } catch (err) { log.warn({ err, key }, 'embeddings cache: get failed; falling through to upstream (fail-open)'); missIndices.push(i); }` — NO metric increment on the catch branch (EMB-H04).
   - When `opts.valkey` is absent: every index becomes a miss; `missIndices = [0..inputs.length-1]`. NO cache metric increment.
   - If `missIndices.length > 0`: call `adapter.embeddings(missIndices.map(i => inputs[i]), entry.backend_model, undefined, { encoding_format: 'float', dimensions: callOpts.dimensions, user: callOpts.user })`. Adapter returns `{ data: [{ index, embedding }], usage }`. For each `item`: `const vec = Array.isArray(item.embedding) ? item.embedding : decodeBase64ToFloat32Array(item.embedding)` (use existing util from `routes/v1/embeddings.ts` — extract to `embeddings/cache.ts` or local helper if not already exported); if `entry.dims !== undefined && vec.length !== entry.dims` throw `EmbeddingsDimsMismatchError(entry.name, entry.dims, vec.length)`; assign `slots[missIndices[item.index]!] = vec`; increment `embeddingsDimsTotal.inc({model: entry.name, dims: String(vec.length)})` ONCE per served vector; populate cache via `cache.set(key, vec)` wrapped in try/catch with warn-log on failure (EMB-H04).
   - Aggregate usage: `{ prompt_tokens: upstream.usage?.prompt_tokens ?? 0, total_tokens: upstream.usage?.total_tokens ?? 0 }` (cache-only hits contribute 0). Sum across upstream sub-batches if any (single call here, but write the addition for clarity).
   - Return `{ embeddings: slots.map(v => v ?? []) as number[][], model: entry.name, usage }`. (At this point all slots are filled — the `?? []` is defensive; assertion `slots.every(v => v !== null)` could fail post-loop only on logic bugs.)
6. Strictly NO `class` keyword anywhere in the file. Factory returns object literal per Frame-01.
7. NO new error classes (D-05 lock).

Then flip `router/tests/providers/embedding-provider.test.ts`:
- The runtime sentinel `await import('../../src/providers/embedding-provider.js')` now resolves — keep it but expand to `expect(typeof mod.makeOpenAIEmbeddingProvider).toBe('function')`.
- Replace the 4 `it.todo` with 4 real `it()`:
  - `it('EmbeddingProvider.embed signature', () => { expectTypeOf<EmbeddingProvider['embed']>().toEqualTypeOf<(input: string | string[], opts: { model: string; dimensions?: number; user?: string }) => Promise<{ embeddings: number[][]; model: string; usage: { prompt_tokens: number; total_tokens: number } }>>(); });`
  - `it('returns { embeddings: number[][], model, usage } on array input', async () => { const provider = makeFakeEmbeddingProvider({ dims: 1024 }); const r = await provider.embed(['hello'], { model: 'embed-local' }); expect(r.embeddings).toHaveLength(1); expect(r.embeddings[0]).toHaveLength(1024); expect(typeof r.model).toBe('string'); expect(typeof r.usage.prompt_tokens).toBe('number'); expect(typeof r.usage.total_tokens).toBe('number'); });`
  - `it('handles string input (matches wire schema union)', async () => { const provider = makeFakeEmbeddingProvider({ dims: 1024 }); const r = await provider.embed('hello', { model: 'embed-local' }); expect(r.embeddings).toHaveLength(1); });`
  - `it('handles batch array input preserving order', async () => { const provider = makeFakeEmbeddingProvider({ dims: 1024 }); const r = await provider.embed(['a', 'b', 'c'], { model: 'embed-local' }); expect(r.embeddings).toHaveLength(3); expect(r.embeddings.every(v => v.length === 1024)).toBe(true); });`

Final test file ends with the sentinel re-asserting `makeOpenAIEmbeddingProvider` is exported. NO it.todo remaining.

Conventional commit: `feat(19-02): EmbeddingProvider interface + makeOpenAIEmbeddingProvider factory (Frame-01 — no class)`.
  </action>
  <verify>
    <automated>cd router && npx vitest run tests/providers/embedding-provider.test.ts && grep -rE 'class \w+EmbeddingProvider' src/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `router/src/providers/embedding-provider.ts` exists; `grep -c 'export interface EmbeddingProvider' router/src/providers/embedding-provider.ts` returns `1`; `grep -c 'export function makeOpenAIEmbeddingProvider' router/src/providers/embedding-provider.ts` returns `1`.
    - `grep -E 'class \w+EmbeddingProvider' router/src/providers/embedding-provider.ts` returns empty (Frame-01 BLOCK preserved by code review).
    - `grep -rE 'class \w+EmbeddingProvider' router/src/` returns empty across the whole tree.
    - `grep -c 'embeddings_cache_total\|embeddingsCacheTotal' router/src/providers/embedding-provider.ts` returns at least `2` (hit + miss; bypass stays in route per Risk #2 Option A).
    - `grep -c 'embeddingsDimsTotal' router/src/providers/embedding-provider.ts` returns at least `1`.
    - `grep -c 'EmbeddingsDimsMismatchError' router/src/providers/embedding-provider.ts` returns at least `1` (throw site).
    - `grep -c 'CapabilityNotSupportedError' router/src/providers/embedding-provider.ts` returns at least `1`.
    - `grep -E "encoding_format:\s*'base64'" router/src/providers/embedding-provider.ts` returns empty (provider never asks for base64 — D-02).
    - `grep -c "encoding_format: 'float'" router/src/providers/embedding-provider.ts` returns at least `1` (provider always asks for float — D-02).
    - `cd router && npx vitest run tests/providers/embedding-provider.test.ts` reports 5 passed (1 sentinel + 4 cases), 0 failed, 0 todo (`expect(r.numTodo).toBe(0)` qualitatively).
    - `cd router && npx tsc --noEmit 2>&1 | grep -E "embedding-provider" | wc -l` returns `0` (production module compiles clean).
    - `shasum -a 256 router/src/routes/v1/embeddings.ts | awk '{print $1}'` equals `b53c6ba1298b8b78b65f75d951e778bd031994fdcd65d14e659f8f3dd666e970` (P7-01 baseline UNCHANGED — route untouched in this plan).
    - `cd router && npx vitest run tests/unit/grep-gates/embeddings-untouched.test.ts` passes (P7-01 baseline test green).
    - `cd router && npx vitest run tests/unit/grep-gates/no-default-retriever.test.ts` passes (Frame-01 BLOCK green).
    - `git diff --stat HEAD~1..HEAD -- router/src/routes/ router/src/app.ts router/src/index.ts` empty (no route / app / index modifications in this plan).
    - `git diff router/package.json router/package-lock.json` empty (zero new packages).
    - Commit scope is `(19-02)`.
  </acceptance_criteria>
  <done>
    EmbeddingProvider interface + production factory available for consumption by Plans 19-03 (route refactor) and 19-04 (composition root + decorator). Wave-0 RED signal for `embedding-provider.js` is now GREEN; downstream `tsc --noEmit` errors caused by `tests/fakes.ts` and `src/types/fastify.d.ts` resolve. Conformance test fully real. Frame-01 BLOCK ongoing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider → upstream adapter | Provider calls `adapter.embeddings(...)`; adapter response (especially `.embedding` field) may be base64 (server-side decision) — provider always decodes to `number[]` |
| Provider → Valkey | Cache read/write — failures are non-fatal (EMB-H04 fail-open); provider must NOT propagate cache errors as upstream errors |
| Provider → caller (route or future RetrieverProvider) | Returns `number[][]` with dims-enforced; downstream cannot poison vector stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-DIMS | Tampering | Wrong-dim vector poisoning of downstream vector stores | mitigate | D-03 option (a): `EmbeddingsDimsMismatchError` thrown INSIDE provider for any upstream vector whose length ≠ `entry.dims`. Verified by `tests/routes/embeddings.test.ts` 691-740 (existing) — flipping a wrong-dim adapter still surfaces 500 via the centralized error envelope after the refactor (Plan 19-03 wires route to provider). |
| T-19-02-CACHE | DoS / Availability | Valkey cache outage causes embeddings outage | mitigate | EMB-H04 fail-open: cache get/set wrapped in try/catch; warn log + fall through to upstream; NO metric increment. Verified by existing `tests/routes/embeddings.test.ts` cache fail-open cases. |
| T-19-02-FR1 | Tampering | Frame-01 BLOCK | mitigate | Factory returns object literal — no `class` keyword in file. Acceptance criterion `grep -c 'class \w+EmbeddingProvider' router/src/` returns 0. |
| T-19-02-LBL | DoS via metric blowup | `embeddingsDimsTotal{model, dims}` cardinality | mitigate | Labels `model` (bounded by `models.yaml` declarations — ~10) + `dims` (bounded by physical model architectures — handful of integer values). NO `_id` label. Verified by OBSV-02 cardinality gate (Plan 19-05 ships the live parser; Plan 19-06 wires the smoke gate). |
| T-19-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO new packages; supply chain unchanged. Acceptance: `git diff router/package.json router/package-lock.json` empty. |
</threat_model>

<verification>
- `cd router && npx vitest run tests/providers/embedding-provider.test.ts` returns 5 pass / 0 fail.
- `cd router && npx tsc --noEmit` exits 0 across `router/src/` and `router/tests/` (Wave-0 errors from Plan 19-01 now resolved).
- `grep -rE 'class \w+EmbeddingProvider' router/src/` empty.
- `grep -rE 'class \w+RetrieverProvider' router/src/` empty (Phase 18 Frame-01 BLOCK still green).
- P7-01 baseline SHA-256 of `router/src/routes/v1/embeddings.ts` UNCHANGED (route untouched).
- `cd router && npx vitest run tests/routes/embeddings.test.ts` passes unchanged (route still consumes its own adapter/cache code; refactor lands in Plan 19-03).
- `git diff router/package.json router/package-lock.json` empty.
</verification>

<success_criteria>
- 1 new source file (`embedding-provider.ts`) + 1 flipped test file.
- 5 vitest cases pass on `embedding-provider.test.ts` (1 sentinel + 4 flipped).
- Factory returns object literal (Frame-01); zero `class` keyword in new file.
- Existing test suites still green — no regressions.
- One atomic commit scoped to `(19-02)`.
</success_criteria>

<output>
Create `.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-02-SUMMARY.md` when done. Cite:
- File size and exported symbols
- Wave-0 it.todo flipped count (4 → real)
- P7-01 baseline status (UNCHANGED)
- Frame-01 gate status (GREEN)
- Any deviations from the plan inline (Rule 1/2/3)
</output>
