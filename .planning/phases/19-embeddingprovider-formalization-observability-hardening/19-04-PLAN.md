---
phase: 19-embeddingprovider-formalization-observability-hardening
plan: 04
type: execute
wave: 2
depends_on: [19-02]
files_modified:
  - router/src/index.ts
  - router/src/app.ts
autonomous: true
requirements: [EMBP-01]
must_haves:
  truths:
    - "Composition root (router/src/index.ts) constructs the production EmbeddingProvider via makeOpenAIEmbeddingProvider({...}) (D-09)"
    - "BuildAppOpts widened with embeddingProvider?: EmbeddingProvider (D-10) — optional for backward test compatibility"
    - "buildApp registers app.decorate('embeddingProvider', opts.embeddingProvider) when present (D-10)"
    - "Route opts thread receives embeddingProvider via opts.embeddingProvider; old cache field removed from RegisterEmbeddingsOpts (cache moved into provider per D-06)"
    - "Existing buildApp test fixtures continue to compile without modification (option-bag pattern preserves backward compat)"
    - "makeAdapterWithCloudKey is reachable at composition root via thin closure or factory export (Risk #1 resolution)"
    - "Frame-01 BLOCK preserved — composition root constructs an OBJECT LITERAL provider (no class instantiation)"
  artifacts:
    - path: "router/src/index.ts"
      provides: "Production composition of EmbeddingProvider + threading through buildApp opts"
      contains: "makeOpenAIEmbeddingProvider"
      contains: "embeddingProvider,"
    - path: "router/src/app.ts"
      provides: "BuildAppOpts.embeddingProvider widening + app.decorate('embeddingProvider', ...) registration"
      contains: "embeddingProvider?: EmbeddingProvider"
      contains: "app.decorate('embeddingProvider'"
  key_links:
    - from: "router/src/index.ts:embeddingProvider construction"
      to: "router/src/providers/embedding-provider.ts:makeOpenAIEmbeddingProvider"
      via: "factory call"
      pattern: "makeOpenAIEmbeddingProvider\\("
    - from: "router/src/app.ts:buildApp"
      to: "fastify.embeddingProvider (decorator)"
      via: "app.decorate"
      pattern: "app\\.decorate\\('embeddingProvider'"
---

<objective>
Wire the production `EmbeddingProvider` into the composition root (`router/src/index.ts`) and the bootstrap (`router/src/app.ts`). Widen `BuildAppOpts` with optional `embeddingProvider?: EmbeddingProvider`; register `app.decorate('embeddingProvider', opts.embeddingProvider)` when present; thread the provider through `registerEmbeddingsRoute` opts; remove the `embeddingsCache` local from `buildApp` (cache lives inside the provider now per D-06).

Purpose: Close EMBP-01's "exposed via `fastify.decorate('embeddingProvider', ...)`" success criterion. Establishes the seam any future RetrieverProvider implementation reads via `fastify.embeddingProvider.embed(query, { model })`.

Output: 2 modified source files. One atomic commit.

Note: This plan is PARALLEL to Plan 19-03 (route refactor) — both depend only on Plan 19-02 (provider impl) and touch DIFFERENT files. Plan 19-03 touches the route; this plan touches the composition root + buildApp. Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md
@.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md
@router/src/index.ts
@router/src/app.ts
@router/src/providers/embedding-provider.ts
@router/src/backends/factory.ts
@router/src/types/fastify.d.ts

<interfaces>
From PATTERNS.md §"router/src/index.ts (MODIFIED — composition root)" — exact insertion site (between line ~220 Phase 18 bootLog.info and line ~222 `const app = await buildApp({`):

```typescript
// Phase 19 (v0.11.0 — EMBP-01): production-wired EmbeddingProvider.
// Frame-01 BLOCK: factory returns an object literal, not a class.
const embeddingProvider = makeOpenAIEmbeddingProvider({
  registry,
  makeAdapter: makeAdapterWithCloudKey,
  valkey,
  env: { ROUTER_EMBED_CACHE_TTL_SEC: env.ROUTER_EMBED_CACHE_TTL_SEC },
  metrics: {
    embeddingsCacheTotal: metrics.embeddingsCacheTotal,
    embeddingsDimsTotal: metrics.embeddingsDimsTotal,
  },
  log: bootLog,
});
bootLog.info({ defaultModel: 'embed-local' }, 'Phase 19 EmbeddingProvider initialized');
```

Then add `embeddingProvider,` to the `buildApp({ ... })` call argument.

From PATTERNS.md §"router/src/app.ts (MODIFIED)" — BuildAppOpts widening (insert AFTER `preCompletionHooks?` field, with JSDoc per D-10 / Phase 17 / Phase 18 tone):

```typescript
/**
 * Phase 19 (v0.11.0 — EMBP-01): optional EmbeddingProvider. When provided,
 * buildApp calls app.decorate('embeddingProvider', opts.embeddingProvider)
 * so future RetrieverProvider implementations can read fastify.embeddingProvider
 * without HTTP round-tripping. When undefined (test fixtures), the
 * /v1/embeddings route falls back to opts.embeddingProvider injected
 * directly via RegisterEmbeddingsOpts. Production composition (index.ts)
 * always passes a real provider.
 */
embeddingProvider?: EmbeddingProvider;
```

Decorator registration (immediately after the Phase 18 decorators block — search for the existing `app.decorate('mcpClientRegistry', ...)` or similar; if no Phase 18 decorate calls in app.ts, insert just before the route registrations):

```typescript
if (opts.embeddingProvider) {
  app.decorate('embeddingProvider', opts.embeddingProvider);
}
```

Route opts threading (lines 1048-1066 of app.ts current — the `registerEmbeddingsRoute(app, { ... })` call site):
- ADD `embeddingProvider: opts.embeddingProvider,` to the opts object.
- REMOVE the existing `cache: embeddingsCache,` line (D-06 — cache moves into provider).
- DELETE the `makeEmbeddingsCache(...)` block above it (lines ~1033-1043) — local `embeddingsCache` variable disappears.

From RESEARCH §7 + Risk #1 — `makeAdapterWithCloudKey` is currently constructed INSIDE `buildApp`. Plan-time read `router/src/backends/factory.ts` to determine the cleanest exposure:
- If `factory.ts` already exports a function that accepts `cloudApiKey` as a parameter, build a thin closure at composition root: `const makeAdapterWithCloudKey = (entry: ResolvedEntry) => makeAdapter(entry, env.OLLAMA_API_KEY ?? '')`.
- If not, refactor `factory.ts` minimally to expose the closure factory.
- This closure is then passed to BOTH the existing `buildApp` adapter-fallback chain AND the new `embeddingProvider` construction.

From CONTEXT D-11 + Plan 19-01 — `router/src/types/fastify.d.ts` already exists (shipped Wave 0). Verify it correctly augments `FastifyInstance.embeddingProvider: EmbeddingProvider` so `app.decorate('embeddingProvider', ...)` type-checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire EmbeddingProvider into index.ts composition root + widen BuildAppOpts in app.ts + register decorator + thread route opts</name>
  <behavior>
    - `npx tsc --noEmit` exits 0 — type augmentation from Plan 19-01 + provider from Plan 19-02 + this plan's widening all compose.
    - `npm test` full sweep passes — no regressions in any existing buildApp-using test (option-bag backward compat preserved by `opts.embeddingProvider?: EmbeddingProvider`).
    - At runtime in production (index.ts → buildApp), `fastify.embeddingProvider` is defined and conforms to the EmbeddingProvider interface.
    - At runtime in test fixtures (buildApp called without `embeddingProvider`), no decorator is registered; route falls back to its `opts.embeddingProvider` (test injects a fake) or throws the route-internal "not injected" error from Plan 19-03 (which is fine — tests that exercise embeddings pass their own provider explicitly).
    - Cache instantiation is removed from `buildApp` (the `makeEmbeddingsCache(...)` block + the `embeddingsCache` local + the `cache:` route-opts line are gone).
    - `embeddingProvider` is added to the `buildApp({ ... })` call argument in `index.ts`.
    - `makeAdapterWithCloudKey` is reachable at composition root (via closure or factory export — pick whichever fits `backends/factory.ts` shape).
    - bootLog confirms "Phase 19 EmbeddingProvider initialized".
  </behavior>
  <read_first>
    - router/src/index.ts (FULL FILE — composition root; need to know exact line for insertion between Phase 18 bootLog.info and buildApp call)
    - router/src/app.ts lines 1-100 (BuildAppOpts interface block + existing field ordering — Phase 17 sessionStore? at lines 142-154, Phase 18 mcpClientRegistry + preCompletionHooks at lines 306-322; new embeddingProvider? field inserts AFTER preCompletionHooks?)
    - router/src/app.ts lines 1020-1090 (current registerEmbeddingsRoute call + makeEmbeddingsCache block — both edited/deleted)
    - router/src/app.ts lines 60-90 (existing inline `declare module 'fastify'` augmentations + decorate calls — pattern reference for new app.decorate('embeddingProvider', ...))
    - router/src/backends/factory.ts (FULL FILE — determine how `makeAdapter` / `makeAdapterWithCloudKey` is exposed; Risk #1 resolution)
    - router/src/providers/embedding-provider.ts (Plan 19-02 — confirm exact factory signature)
    - router/src/types/fastify.d.ts (Plan 19-01 — confirm augmentation exists and references the correct path)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-CONTEXT.md decisions D-09 + D-10 + D-11
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-RESEARCH.md §7 (composition-root threading verbatim) + Risk #1 (makeAdapterWithCloudKey resolution)
    - .planning/phases/19-embeddingprovider-formalization-observability-hardening/19-PATTERNS.md §"router/src/index.ts (MODIFIED)" + §"router/src/app.ts (MODIFIED)"
  </read_first>
  <files>router/src/index.ts, router/src/app.ts</files>
  <action>
Edit `router/src/index.ts`:

1. Add import at top: `import { makeOpenAIEmbeddingProvider } from './providers/embedding-provider.js';` (or extend existing providers barrel if one exists).
2. RESOLVE Risk #1: read `router/src/backends/factory.ts` and determine the cleanest path to make `makeAdapterWithCloudKey` reachable at composition root:
   - If `factory.ts` exports `makeAdapter(entry, cloudApiKey?)`: at composition root, `const makeAdapterWithCloudKey = (entry: ResolvedEntry) => makeAdapter(entry, env.OLLAMA_API_KEY ?? '');` (place near line 159 where registry is constructed).
   - If `factory.ts` already exports `makeAdapterFactory(cloudApiKey)`: `const makeAdapterWithCloudKey = makeAdapterFactory(env.OLLAMA_API_KEY ?? '');`.
   - Document the chosen path in the SUMMARY (Rule 3 deviation if you had to refactor `factory.ts`).
3. Insert the EmbeddingProvider construction block per `<interfaces>` above, between the existing Phase 18 bootLog.info (line ~220) and the `const app = await buildApp({` call (line ~222).
4. Add `embeddingProvider,` to the `buildApp({ ... })` argument object (mirror Phase 17/18 ordering — after `preCompletionHooks,`).
5. If the existing buildApp call already passes `cloudApiKey: env.OLLAMA_API_KEY ?? ''` and `buildApp` internally re-constructs `makeAdapterWithCloudKey`, this remains untouched — the composition-root closure is a parallel binding solely for the provider. If your refactor unified the two, that's fine — verify no buildApp tests break.

Edit `router/src/app.ts`:

1. Add import at top: `import type { EmbeddingProvider } from './providers/embedding-provider.js';`.
2. Inside `BuildAppOpts` interface, AFTER the existing `preCompletionHooks?` field (~line 306-322), insert the new field per `<interfaces>` block with the JSDoc above.
3. Inside `buildApp(opts: BuildAppOpts) { ... }`, immediately after the existing decorators block (find by `grep -n "app.decorate" router/src/app.ts`), insert:
   ```
   if (opts.embeddingProvider) {
     app.decorate('embeddingProvider', opts.embeddingProvider);
   }
   ```
4. Find the `registerEmbeddingsRoute(app, { ... })` call (line ~1048-1066). EDIT the opts object:
   - REMOVE the `cache: embeddingsCache,` line (D-06).
   - ADD `embeddingProvider: opts.embeddingProvider,` (route falls back to fastify decorator when this is undefined — handled by Plan 19-03's route code).
5. DELETE the `makeEmbeddingsCache(...)` block above (~lines 1033-1043) including the `const embeddingsCache = makeEmbeddingsCache({...})` local — cache lives in the provider now.
6. If `RegisterEmbeddingsOpts` (in `router/src/routes/v1/embeddings.ts`) still declares a `cache?: EmbeddingsCache` field, that's Plan 19-03's territory — verify it was removed; if not, file a Rule-1 deviation in SUMMARY and add an `// XXX(19-04): RegisterEmbeddingsOpts.cache must be removed by Plan 19-03` comment instead of editing the route file (this plan's `files_modified` excludes routes/).

Conventional commit: `feat(19-04): wire EmbeddingProvider through composition root + buildApp decorator`.

DO NOT touch `router/src/routes/v1/embeddings.ts` in this plan — that's Plan 19-03's exclusive territory. The two plans run in PARALLEL (Wave 2) and have ZERO file overlap.
  </action>
  <verify>
    <automated>cd router && npx tsc --noEmit && npx vitest run tests/providers/embedding-provider.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'makeOpenAIEmbeddingProvider' router/src/index.ts` returns at least `1` (factory call site).
    - `grep -c "Phase 19 EmbeddingProvider initialized" router/src/index.ts` returns at least `1` (bootLog cite).
    - `grep -c "embeddingProvider," router/src/index.ts` returns at least `1` (buildApp argument).
    - `grep -c "embeddingProvider?: EmbeddingProvider" router/src/app.ts` returns at least `1` (BuildAppOpts widening).
    - `grep -c "app.decorate('embeddingProvider'" router/src/app.ts` returns `1` (single decorator registration site).
    - `grep -c "makeEmbeddingsCache" router/src/app.ts` returns `0` (cache construction REMOVED from app.ts — provider owns it now).
    - `grep -c "cache: embeddingsCache" router/src/app.ts` returns `0` (route opts no longer wires cache).
    - `grep -c "embeddingProvider: opts.embeddingProvider" router/src/app.ts` returns at least `1` (route opts threading).
    - `cd router && npx tsc --noEmit` exits 0.
    - `cd router && npm test` full sweep — failures count UNCHANGED from pre-Plan-19-04 baseline (no regressions; option-bag backward compat preserved).
    - `cd router && npx vitest run tests/providers/embedding-provider.test.ts` passes (Plan 19-02 conformance still green).
    - `cd router && npx vitest run tests/unit/grep-gates/no-default-retriever.test.ts` passes (Frame-01 BLOCK green).
    - `cd router && npx vitest run tests/unit/grep-gates/embeddings-untouched.test.ts` passes (P7-01 baseline — depends on whether Plan 19-03 has landed; if 19-03 already merged, the rotated baseline; if not, the unchanged baseline; either way GREEN).
    - `git diff --stat HEAD~1..HEAD -- router/src/routes/` empty (this plan does NOT touch routes/ — that's Plan 19-03's territory).
    - `git diff router/package.json router/package-lock.json` empty.
    - Commit scope is `(19-04)`.
  </acceptance_criteria>
  <done>
    Production composition wires the EmbeddingProvider; `fastify.embeddingProvider` is a real, decorated, dims-enforcing, cache-backed provider in production. BuildAppOpts widened with optional field preserves test backward compat. Cache instantiation no longer happens in `buildApp` (moved into provider). When Plan 19-03 also lands (Wave 2), the route consumes the decorator and the EmbeddingProvider seam is complete end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composition root → buildApp opts | In-process trusted construction; production always passes a real provider |
| buildApp → Fastify decorator | Type-safe via `router/src/types/fastify.d.ts` augmentation (Plan 19-01) |
| Route → fastify.embeddingProvider | Read-only access via decorator; route doesn't construct or mutate the provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-WS | Tampering | `/v1/embeddings` wire shape (P7-01 BLOCK) | accept | This plan does NOT touch the route; P7-01 SHA baseline status depends on Plan 19-03 timing. Either green-with-old-baseline OR green-with-new-baseline. |
| T-19-04-DI | Spoofing | Production app boots without a real provider | mitigate | Composition root explicitly constructs + passes `embeddingProvider` to buildApp; bootLog.info confirms initialization. Plan 19-03's route fallback chain (`opts.embeddingProvider ?? fastify.embeddingProvider ?? throw`) prevents silent missing-provider in production. |
| T-19-04-FR1 | Tampering | Frame-01 BLOCK (no class instantiation) | mitigate | Composition root uses `makeOpenAIEmbeddingProvider({...})` factory call returning object literal — no `new ClassName(...)` syntax. Acceptance: `grep -c 'new \w*EmbeddingProvider' router/src/index.ts` returns `0`. |
| T-19-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO new packages; supply chain unchanged. Acceptance: `git diff router/package.json router/package-lock.json` empty. |
</threat_model>

<verification>
- `cd router && npx tsc --noEmit` exits 0 across the whole tree.
- `cd router && npm test` passes — failure count UNCHANGED vs pre-Plan-19-04 baseline.
- Production composition exposes `fastify.embeddingProvider` (verified at type level by tsc; at runtime by Plan 19-06's smoke gate hitting `/v1/embeddings`).
- BuildAppOpts is backward compatible — existing test fixtures that omit `embeddingProvider` still build apps.
- `grep -c 'makeEmbeddingsCache' router/src/app.ts` returns 0 (cache instantiation moved into provider).
- `grep -rE 'class \w+EmbeddingProvider' router/src/` empty (Frame-01 BLOCK green).
- `git diff router/package.json router/package-lock.json` empty.
</verification>

<success_criteria>
- 2 files modified.
- BuildAppOpts widened, decorator registered, route opts threaded, cache moved into provider.
- `fastify.embeddingProvider` is a real, type-augmented decorator in production.
- Zero regressions in existing test suite.
- One atomic commit scoped to `(19-04)`.
</success_criteria>

<output>
Create `.planning/phases/19-embeddingprovider-formalization-observability-hardening/19-04-SUMMARY.md` when done. Cite:
- Chosen Risk #1 resolution (closure vs factory export) and rationale
- BuildAppOpts widening line number
- `app.decorate` registration site line number
- `makeEmbeddingsCache` block removal (line range deleted)
- Full vitest sweep pass count delta vs pre-plan baseline
- Any inline deviations (Rule 1/2/3)
</output>
