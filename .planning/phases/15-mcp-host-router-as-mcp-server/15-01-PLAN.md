---
phase: 15
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - router/package.json
  - router/package-lock.json
  - router/src/config/env.ts
  - router/.env.example
autonomous: false
requirements: [MCPS-01]
user_setup: []
tags: [mcp, dependencies, env-config]

must_haves:
  truths:
    - "@modelcontextprotocol/sdk@^1.29.0 is listed under dependencies in router/package.json after a slopcheck-style legitimacy verification"
    - "router/src/config/env.ts EnvSchema parses MCP_ENABLED (boolean, default true), MCP_SESSION_TTL_SEC (number, default 3600), MCP_GC_INTERVAL_MS (number, default 1_800_000)"
    - "router/.env.example documents the three new env vars under a Phase 15 (MCP host) comment block"
  artifacts:
    - path: "router/package.json"
      provides: "Production dep entry @modelcontextprotocol/sdk@^1.29.0"
      contains: "@modelcontextprotocol/sdk"
    - path: "router/src/config/env.ts"
      provides: "Zod EnvSchema entries for MCP_ENABLED, MCP_SESSION_TTL_SEC, MCP_GC_INTERVAL_MS"
      contains: "MCP_ENABLED"
    - path: "router/.env.example"
      provides: "Operator-facing documentation for the three new env vars"
      contains: "MCP_SESSION_TTL_SEC"
  key_links:
    - from: "router/package.json"
      to: "router/node_modules/@modelcontextprotocol/sdk"
      via: "npm install"
      pattern: "@modelcontextprotocol/sdk"
    - from: "router/src/config/env.ts"
      to: "process.env"
      via: "loadEnv() at boot"
      pattern: "MCP_ENABLED"
---

<objective>
Land the single new npm dependency for Phase 15 (`@modelcontextprotocol/sdk@^1.29.0`) and extend `EnvSchema` + `.env.example` with the three new MCP host env vars (D-15: `MCP_ENABLED`, `MCP_SESSION_TTL_SEC`, `MCP_GC_INTERVAL_MS`). This plan is a foundation for every later wave: every subsequent plan imports from `@modelcontextprotocol/sdk/server/{mcp,streamableHttp}.js` and reads the three new env values.

Purpose: Defense-in-depth package legitimacy (RESEARCH §Package Legitimacy Audit) demands a `checkpoint:human-verify` BEFORE `npm install` because slopcheck was not invokable during research. The env-var widening is a no-impact addition (all defaults preserve current behavior — `MCP_ENABLED=true` means the plugin registers, but later plans only register the route in Wave 3+).
Output: SDK installed with verified provenance; env loader accepts and validates three new keys with defaults; `.env.example` documents them; codebase compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md

<interfaces>
<!-- Existing EnvSchema lines 53-63 of router/src/config/env.ts use the z.coerce pattern. -->
<!-- The three new keys append to the schema in the same style. -->

From router/src/config/env.ts:
```typescript
const EnvSchema = z.object({
  ROUTER_BEARER_TOKEN: z.string().min(8, ...),
  // ...existing fields...
  CIRCUIT_FAILURE_THRESHOLD: z.coerce.number().int().min(1).default(5),
  CIRCUIT_WINDOW_MS: z.coerce.number().int().min(1_000).default(30_000),
  CIRCUIT_COOLDOWN_MS: z.coerce.number().int().min(1_000).default(60_000),
  // ...
});
export type Env = z.infer<typeof EnvSchema>;
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for @modelcontextprotocol/sdk@^1.29.0</name>
  <read_first>
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md §Package Legitimacy Audit
    - .planning/research/PITFALLS.md (T-15-SC supply-chain threat row)
  </read_first>
  <what-built>
    Pre-install verification of `@modelcontextprotocol/sdk@1.29.0`. Research flagged this package as `[ASSUMED]` because slopcheck was not invokable in the researcher context. Per package_legitimacy_protocol, ASSUMED packages MUST pass a blocking-human checkpoint before `npm install`.
  </what-built>
  <how-to-verify>
    Run these commands and confirm each output matches expectation. Paste the outputs into the resume signal so the next task can proceed.

    ```bash
    # 1. Repo URL must be github.com/modelcontextprotocol/typescript-sdk
    npm view @modelcontextprotocol/sdk@1.29.0 repository.url
    # Expected: git+https://github.com/modelcontextprotocol/typescript-sdk.git

    # 2. Maintainers should include the official org user
    npm view @modelcontextprotocol/sdk@1.29.0 maintainers

    # 3. postinstall MUST be empty / undefined
    npm view @modelcontextprotocol/sdk@1.29.0 scripts.postinstall
    # Expected: (empty)

    # 4. Capture integrity hash for later verification
    npm view @modelcontextprotocol/sdk@1.29.0 dist.integrity
    # Expected: sha512-... (capture for cross-check after install)

    # 5. (Optional, if slopcheck available)
    command -v slopcheck && slopcheck install @modelcontextprotocol/sdk --json || echo "slopcheck not installed — falling back to manual checks above"
    ```

    Also visually confirm `https://npmjs.com/package/@modelcontextprotocol/sdk` shows: official `@modelcontextprotocol` scope, link to `github.com/modelcontextprotocol/typescript-sdk` repo, weekly download count consistent with an official-org package.
  </how-to-verify>
  <resume-signal>Type "approved — sdk legitimacy verified" once the four `npm view` checks return expected values, OR describe any anomaly (repo URL mismatch, surprise postinstall, suspicious maintainer).</resume-signal>
  <files>(none — this is a pre-install verification checkpoint; no files are modified during this task)</files>
  <action>Operator runs the npm view commands listed in &lt;how-to-verify&gt; above to confirm package provenance against the official @modelcontextprotocol GitHub organization. No code is written by this task; it gates Task 2 (npm install) on a human-confirmed result. If any check returns an unexpected value, do NOT type "approved" — instead describe the anomaly so the orchestrator can halt the phase pending investigation.</action>
  <verify>
    <human-check>Operator confirms via resume-signal that all four npm-view checks returned expected values (repo URL, maintainers, empty postinstall, integrity hash captured).</human-check>
  </verify>
  <done>Operator has approved the package legitimacy gate via the resume signal; integrity hash captured for cross-check in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Install @modelcontextprotocol/sdk@^1.29.0 into router workspace</name>
  <read_first>
    - router/package.json (verify current dependencies section structure)
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md §Standard Stack (peer-dep zod compat)
  </read_first>
  <action>
    Add `@modelcontextprotocol/sdk@^1.29.0` to `router/package.json` `dependencies` (alphabetically sorted with existing entries). Run `pnpm install` from inside the `router/` directory so the existing pnpm-lock workflow generates the lockfile entry (the repo uses pnpm — confirmed via `pnpm-lock.yaml` and the smoke script using `pnpm vitest`). After install, run `pnpm typecheck` to confirm zero TS errors (the new dependency is not yet imported anywhere so this is a baseline check). Do NOT touch `router/package-lock.json` directly — pnpm regenerates lockfiles. If pnpm is not used after all (verify via `ls router/pnpm-lock.yaml` first), fall back to `npm install`.

    Verify post-install: `ls router/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js` must exist (the path the plugin will import from). Also run `node -e "console.log(require('@modelcontextprotocol/sdk/package.json').version)"` from `router/` and confirm `1.29.0` (or any `1.29.x`).
  </action>
  <verify>
    <automated>cd router && ls node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js && ls node_modules/@modelcontextprotocol/sdk/dist/esm/server/streamableHttp.js && ls node_modules/@modelcontextprotocol/sdk/dist/esm/types.js && grep -q '"@modelcontextprotocol/sdk"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `router/package.json` `dependencies` contains `"@modelcontextprotocol/sdk": "^1.29.0"`
    - `router/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js` exists
    - `router/node_modules/@modelcontextprotocol/sdk/dist/esm/server/streamableHttp.js` exists
    - `router/node_modules/@modelcontextprotocol/sdk/dist/esm/types.js` exists
    - `cd router && pnpm typecheck` (or `npm run typecheck`) returns 0 errors
    - The integrity hash of the installed tarball matches the value captured in Task 1
  </acceptance_criteria>
  <done>SDK installed with verified provenance; no transitive dependency issues; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend EnvSchema with MCP_ENABLED, MCP_SESSION_TTL_SEC, MCP_GC_INTERVAL_MS</name>
  <read_first>
    - router/src/config/env.ts (entire file — short, ~78 lines)
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md §D-15
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md §`router/src/config/env.ts` (modified)
  </read_first>
  <action>
    Append three new fields to `EnvSchema` in `router/src/config/env.ts`, placed AFTER `ROUTER_EMBED_CACHE_TTL_SEC` (line ~70) and BEFORE the closing `})` of `z.object({ ... })`. Use the exact `z.coerce` pattern established by `CIRCUIT_FAILURE_THRESHOLD` / `CIRCUIT_WINDOW_MS` / `CIRCUIT_COOLDOWN_MS`. Include a preceding comment block referencing Phase 15 + MCPS-01..06 + CONTEXT D-15:

    - `MCP_ENABLED: z.coerce.boolean().default(true)` — boolean; when `false`, downstream plans must skip plugin registration so `/mcp` returns 404 (verified in Plan 12).
    - `MCP_SESSION_TTL_SEC: z.coerce.number().int().positive().default(3600)` — seconds; `positive()` rejects `0`/negative.
    - `MCP_GC_INTERVAL_MS: z.coerce.number().int().positive().default(1_800_000)` — milliseconds; 30 min default.

    The `Env` type (`z.infer<typeof EnvSchema>`) auto-widens; do not touch the type alias.

    Add a brief unit test at `router/tests/unit/config/env.test.ts` (create file if absent) that loads `loadEnv({ ROUTER_BEARER_TOKEN: 'x'.repeat(16), ROUTER_DATABASE_URL: 'postgres://x:y@h/d', ROUTER_VALKEY_PASSWORD: 'x'.repeat(16) })` and asserts the three new defaults (`true`, `3600`, `1_800_000`); a second case overrides via env strings (`'false'`, `'120'`, `'60000'`) and asserts coerced values (`false`, `120`, `60000`). The test framework is vitest@^4.1.6 (already installed). If `router/tests/unit/config/env.test.ts` already exists, append the new cases to its existing `describe` block instead of duplicating boilerplate.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/config/env.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MCP_ENABLED:' router/src/config/env.ts` equals 1
    - `grep -c 'MCP_SESSION_TTL_SEC:' router/src/config/env.ts` equals 1
    - `grep -c 'MCP_GC_INTERVAL_MS:' router/src/config/env.ts` equals 1
    - All three use `z.coerce` (no raw `z.string()`)
    - Vitest unit test passes both the default-value case and the override case
    - `pnpm typecheck` clean
  </acceptance_criteria>
  <done>EnvSchema widened; defaults preserve current behavior; coerce ladder accepts `"true"`/`"false"`/`"1"`/`"0"` for the boolean and stringified ints for the numerics.</done>
</task>

<task type="auto">
  <name>Task 4: Document the three new env vars in router/.env.example</name>
  <read_first>
    - router/.env.example (read once to find a good insertion point — likely near the Phase 14 / CIRCUIT_* block)
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md §D-15 (per D-15 — env-var lifecycle matches plugin lifecycle)
  </read_first>
  <action>
    Append a new commented block to `router/.env.example` (after the `CIRCUIT_*` block or at file end) documenting the three new keys. Each line: a comment explaining the field + the env line itself commented out with the default value. Exactly:

    ```
    # ===== Phase 15 (v0.11.0 — MCPS-01..06 / D-15): MCP host plugin =====
    # MCP_ENABLED=false skips /mcp registration entirely (returns 404).
    # MCP_ENABLED=true

    # Idle MCP session TTL (seconds). GC closes any session whose last activity
    # is older than this. Default 3600 (1h).
    # MCP_SESSION_TTL_SEC=3600

    # MCP session GC sweep cadence (milliseconds). Default 1_800_000 (30 min).
    # MCP_GC_INTERVAL_MS=1800000
    ```

    Do not include actual values uncommented (the defaults already kick in when env var is absent — uncommenting would be misleading). Match the existing comment style in `.env.example` (verify by reading the file first).
  </action>
  <verify>
    <automated>grep -c 'MCP_ENABLED' router/.env.example && grep -c 'MCP_SESSION_TTL_SEC' router/.env.example && grep -c 'MCP_GC_INTERVAL_MS' router/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - All three env var names appear in `router/.env.example`
    - All three lines are commented out (start with `# `)
    - A `Phase 15` header comment precedes the block
  </acceptance_criteria>
  <done>Operators have discoverable, documented knobs for MCP host configuration before any plugin code lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → router/node_modules | New external code crosses this boundary; supply-chain attack vector |
| operator env → router process | New config values flow from env to runtime via Zod parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-SC | Tampering | npm install of @modelcontextprotocol/sdk | mitigate | Task 1 blocking-human checkpoint verifies repo URL, maintainers, postinstall hook, integrity hash against npm registry metadata BEFORE Task 2 runs `pnpm install` |
| T-15-01-EN | Information Disclosure | EnvSchema accepting non-default values | accept | Defaults preserve current behavior (`MCP_ENABLED=true`); negative/zero values rejected by `z.coerce.number().int().positive()`; no secrets touched (no MCP-specific token; reuses ROUTER_BEARER_TOKEN) |
</threat_model>

<verification>
- `cd router && pnpm typecheck` → 0 errors
- `cd router && pnpm vitest run tests/unit/config/env.test.ts` → all cases pass
- `grep '@modelcontextprotocol/sdk' router/package.json` → 1 occurrence under dependencies
- `ls router/node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js` → file exists
</verification>

<success_criteria>
1. Package legitimacy gate passed by human verification with documented npm-registry metadata match
2. `@modelcontextprotocol/sdk@^1.29.0` listed in `router/package.json` `dependencies` and resolvable in `node_modules`
3. `EnvSchema` parses MCP_ENABLED / MCP_SESSION_TTL_SEC / MCP_GC_INTERVAL_MS with documented defaults
4. `router/.env.example` documents the three new keys with a Phase 15 header
5. All existing tests continue to pass; new unit test for env defaults passes
6. `pnpm typecheck` clean
</success_criteria>

<output>
Create `.planning/phases/15-mcp-host-router-as-mcp-server/15-01-SUMMARY.md` documenting:
- Slopcheck verification outcomes (repo URL, maintainers, integrity hash)
- Installed SDK version + transitive dep summary (note that the SDK pulls ~15 transitive deps but the router only imports from `/server/mcp.js`, `/server/streamableHttp.js`, `/types.js`)
- Env defaults confirmed via vitest
</output>
