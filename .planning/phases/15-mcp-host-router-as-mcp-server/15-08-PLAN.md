---
phase: 15
plan: 8
type: execute
wave: 4
depends_on: ["15-02", "15-04", "15-05"]
files_modified:
  - router/src/mcp/host/tools/create-embedding.ts
  - router/tests/unit/mcp/host/tools/create-embedding.test.ts
autonomous: true
requirements: [MCPS-03, MCPS-04]
user_setup: []
tags: [mcp, tool-handler, embeddings]

must_haves:
  truths:
    - "registerCreateEmbeddingTool registers a tool named 'create_embedding' with inputSchema = z.toJSONSchema(EmbeddingsRequestSchema) (D-01)"
    - "Success returns { content:[{type:'text', text:'embedded N inputs, dims=D, model=M'}], structuredContent: <full OpenAI embeddings response with .data and .usage> } per D-03"
    - "Embedding vector payload rides entirely in structuredContent — content stays a one-line stamp (D-03 explicit)"
    - "Errors → isError:true via toOpenAIErrorEnvelope (D-04)"
    - "extra.signal → adapter signal (D-14)"
    - "One bufferedWriter row per call with protocol='mcp'; metrics counter increments"
  artifacts:
    - path: "router/src/mcp/host/tools/create-embedding.ts"
      provides: "registerCreateEmbeddingTool"
      exports: ["registerCreateEmbeddingTool"]
      min_lines: 90
    - path: "router/tests/unit/mcp/host/tools/create-embedding.test.ts"
      provides: "Unit-test matrix"
      min_lines: 100
  key_links:
    - from: "router/src/mcp/host/tools/create-embedding.ts"
      to: "router/src/routes/v1/embeddings.ts"
      via: "import { EmbeddingsRequestSchema }"
      pattern: "EmbeddingsRequestSchema"
---

<objective>
Register the `create_embedding` MCP tool. The dual-shape D-03 stamp is `"embedded N inputs, dims=D, model=M"` — explicitly NOT the vector payload (which would bloat `content` for downstream LLM tools that aggregate it). The full embeddings response (data array with vectors + usage) rides in `structuredContent` so agents can read the floats.

Purpose: Land the embedding tool — required by retrieval-bound agents that want to compute query/document vectors via a single MCP interface.
Output: `create_embedding` tool registered; tools/list now returns 3 tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md
@router/src/routes/v1/embeddings.ts
@router/src/dispatch/preflight.ts
@router/src/mcp/host/plugin.ts
@router/src/mcp/host/tools/chat-completion.ts

<interfaces>
From router/src/routes/v1/embeddings.ts:85-97:
```typescript
export const EmbeddingsRequestSchema = z.object({
  model: z.string(),
  input: z.union([z.string(), z.array(z.string())]),
  // ...
}).passthrough();
export type EmbeddingsRequest = z.infer<typeof EmbeddingsRequestSchema>;
```

From router/src/backends/adapter.ts:
```typescript
embeddings(args: { model: string, input: string[], ... }, signal: AbortSignal): Promise<EmbeddingsResponse>;
// Returns { data: [{ embedding: number[], index: number }], model, usage: { prompt_tokens, total_tokens } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit-test matrix for registerCreateEmbeddingTool (RED)</name>
  <files>router/tests/unit/mcp/host/tools/create-embedding.test.ts</files>
  <read_first>
    - router/tests/unit/mcp/host/tools/chat-completion.test.ts (Plan 06 — scaffold)
    - router/src/routes/v1/embeddings.ts (verify schema fields)
  </read_first>
  <behavior>
    - Test 1 (D-01): inputSchema deep-equals z.toJSONSchema(EmbeddingsRequestSchema)
    - Test 2 (D-03 stamp): success result content[0].text matches /embedded \d+ inputs, dims=\d+, model=.+/ regex; structuredContent contains the full embeddings.data array with vectors
    - Test 3 (D-04): policy violation → isError:true
    - Test 4 (D-14): extra.signal abort → adapter saw signal.aborted=true
    - Test 5 (D-05/D-06): bufferedWriter row with protocol='mcp'
    - Test 6 (D-07): metrics counter increment {tool: 'create_embedding'}
    - Test 7 (input shape passthrough): args with `input: ['a','b','c']` reaches adapter intact (FakeAdapter.embeddings.mock.calls[0][0].input deep-equals ['a','b','c'])
  </behavior>
  <action>
    Create the test file mirroring Plan 06's structure. FakeAdapter exposes `embeddings: vi.fn().mockResolvedValue({ data: [{ embedding: [0.1, 0.2, ..., 0.<DIM>], index: 0 }, { embedding: [...], index: 1 }, ...], model: 'embed-local', usage: { prompt_tokens: 5, total_tokens: 5 } })`. For Test 2, use a fake embeddings response with N=3 inputs and DIM=8 — assert the stamp string equals `'embedded 3 inputs, dims=8, model=embed-local'` exactly.

    Run `pnpm vitest run tests/unit/mcp/host/tools/create-embedding.test.ts` — all 7 RED.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/mcp/host/tools/create-embedding.test.ts 2>&1 | grep -E "Cannot find module|FAIL" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 it() blocks, all RED
  </acceptance_criteria>
  <done>RED state established for create_embedding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registerCreateEmbeddingTool (GREEN) + register in buildServerForRequest</name>
  <files>router/src/mcp/host/tools/create-embedding.ts</files>
  <read_first>
    - router/src/mcp/host/tools/chat-completion.ts (Plan 06 — scaffold)
    - router/src/routes/v1/embeddings.ts (full file — short, ~250 lines max)
  </read_first>
  <action>
    Create `router/src/mcp/host/tools/create-embedding.ts`. Single export: `registerCreateEmbeddingTool(server, deps, capturedReq)`. Differences from chat-completion.ts:

    1. Imports `EmbeddingsRequestSchema` from `../../../routes/v1/embeddings.js`.
    2. Tool name: `create_embedding`. Description: "OpenAI-compatible embeddings. Returns the full vector payload in structuredContent; the text content carries a one-line stamp. No MCP streaming."
    3. inputSchema: `z.toJSONSchema(EmbeddingsRequestSchema)`.
    4. Handler does NOT call `openAIRequestToCanonical` — embeddings have their own adapter method (`adapter.embeddings(args, signal)`). After `applyPreflight`, call `adapter.embeddings({ model: entry.backend_model, input: Array.isArray(args.input) ? args.input : [args.input], encoding_format: args.encoding_format, dimensions: args.dimensions }, controller.signal)`. The response is already in the OpenAI embeddings shape (verify by reading the adapter type).
    5. Build the D-03 stamp: `const n = resp.data.length; const dims = resp.data[0]?.embedding?.length ?? 0; const stamp = \`embedded ${n} inputs, dims=${dims}, model=${resp.model}\`;`. Return `{ content: [{ type: 'text' as const, text: stamp }], structuredContent: resp }`.
    6. Error/finally blocks identical to chat-completion (D-04, D-05, D-06, D-07, D-14). For tokens_in/out, use `resp.usage.prompt_tokens` / `resp.usage.total_tokens` (or whatever the embeddings response exposes — verify).

    After GREEN, wire the tool inside Plan 10's `buildServerForRequest` `buildServerForRequest` — replace the create_embedding TODO marker with `registerCreateEmbeddingTool(server, opts, capturedReq);`.

    Run unit tests + integration test (`tests/integration/mcp-host.integration.test.ts` — tools/list now returns 3 tools).
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/mcp/host/tools/create-embedding.test.ts tests/integration/mcp-host.integration.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `router/src/mcp/host/tools/create-embedding.ts` exists with `registerCreateEmbeddingTool` export
    - `grep -c "z.toJSONSchema(EmbeddingsRequestSchema)" router/src/mcp/host/tools/create-embedding.ts` equals 1
    - `grep -c "embedded " router/src/mcp/host/tools/create-embedding.ts` is 1 or more (D-03 stamp string)
    - `router/src/mcp/host/plugin.ts` calls `registerCreateEmbeddingTool` in buildServerForRequest
    - 7/7 unit tests pass; integration test shows 3 tools in tools/list
    - `pnpm typecheck` clean
  </acceptance_criteria>
  <done>create_embedding tool live; vector payload rides in structuredContent; stamp text matches D-03 format.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-PAYLOAD | Information Disclosure | embedding vectors in content[].text bloat MCP response | mitigate | D-03 stamp keeps content text minimal; vectors only in structuredContent (Test 2 asserts this) |
| T-15-08-DRIFT | Tampering | embedding schema drift | mitigate | z.toJSONSchema(EmbeddingsRequestSchema) source of truth |
| T-15-08-ABORT | Denial of Service | upstream cancel | mitigate | D-14 AbortController |
</threat_model>

<verification>
- `pnpm vitest run tests/unit/mcp/host/tools/create-embedding.test.ts` → 7/7 pass
- `pnpm vitest run tests/integration/mcp-host.integration.test.ts` → tools/list returns 3 tools
- `pnpm typecheck` → 0 errors
</verification>

<success_criteria>
1. create_embedding tool registered with passthrough Zod schema
2. Success returns D-03 stamp + full embeddings.data in structuredContent
3. Errors return isError:true (D-04)
4. D-05/D-06/D-07 row + metrics; D-14 abort propagation
5. tools/list shows 3 tools
</success_criteria>

<output>
Create `.planning/phases/15-mcp-host-router-as-mcp-server/15-08-SUMMARY.md` noting the exact stamp format and confirming vector payload exclusion from content.
</output>
