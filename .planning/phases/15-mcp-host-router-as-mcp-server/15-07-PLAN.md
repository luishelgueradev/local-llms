---
phase: 15
plan: 7
type: execute
wave: 4
depends_on: ["15-02", "15-04", "15-05"]
files_modified:
  - router/src/mcp/host/tools/create-response.ts
  - router/tests/unit/mcp/host/tools/create-response.test.ts
autonomous: true
requirements: [MCPS-03, MCPS-04]
user_setup: []
tags: [mcp, tool-handler, responses-api]

must_haves:
  truths:
    - "registerCreateResponseTool registers a tool named 'create_response' on McpServer with inputSchema = z.toJSONSchema(ResponsesRequestSchema) (D-01)"
    - "Successful tool call returns dual-shape { content: [{ type:'text', text: <extracted output text> }], structuredContent: <full /v1/responses response> } per D-02/D-03"
    - "stream:true is silently coerced to false inside the tool handler (D-12); description documents this"
    - "Errors are returned as { isError: true } envelopes via toOpenAIErrorEnvelope (D-04 / MCPS-04)"
    - "extra.signal → AbortController → adapter signal (D-14)"
    - "One bufferedWriter row per tool call with protocol='mcp', route='/mcp', scoped IDs from capturedReq (D-05/D-06)"
    - "router_mcp_tool_calls_total{tool='create_response'} increments per call"
  artifacts:
    - path: "router/src/mcp/host/tools/create-response.ts"
      provides: "registerCreateResponseTool"
      exports: ["registerCreateResponseTool"]
      min_lines: 100
    - path: "router/tests/unit/mcp/host/tools/create-response.test.ts"
      provides: "Unit-test matrix mirroring 15-06 chat-completion"
      min_lines: 100
  key_links:
    - from: "router/src/mcp/host/tools/create-response.ts"
      to: "router/src/routes/v1/responses.ts"
      via: "import { ResponsesRequestSchema }"
      pattern: "ResponsesRequestSchema"
---

<objective>
Register the `create_response` MCP tool on the McpServer built by `buildServerForRequest`. Wraps the existing `/v1/responses` capability per the same D-01..D-14 pattern established by Plan 06 (chat_completion). The text content stamp (D-03) is the assistant text extracted from the canonical Responses API `output` field.

Purpose: Expose the /v1/responses surface (v0.10.0 Phase 13) to MCP clients; n8n's "Message to a Model" node currently 404s against `/v1/responses` (memory note `project_n8n_integration.md`) but Phase 13 added the minimal non-streaming /v1/responses — exposing it as an MCP tool provides another consumer path.
Output: `create_response` tool registered; canonical shape preserved; dual-shape result tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md
@router/src/routes/v1/responses.ts
@router/src/dispatch/preflight.ts
@router/src/mcp/host/plugin.ts
@router/src/mcp/host/tools/chat-completion.ts

<interfaces>
From router/src/routes/v1/responses.ts:94-105:
```typescript
export const ResponsesRequestSchema = z.object({...}).passthrough();
export type ResponsesRequest = z.infer<typeof ResponsesRequestSchema>;
```

The non-streaming /v1/responses handler internally calls the same chatCompletionsCanonical pipeline (Phase 13 implemented it as a translation layer over the OpenAI chat-completions surface). The MCP tool handler follows the same approach: build a canonical request, call the adapter, then translate the response into Responses API shape via the existing helper (read `router/src/translation/responses-out.ts` or whatever the existing helper is named — verify via `grep -rn 'canonicalToResponses\|responsesOut' router/src/translation/`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit-test matrix for registerCreateResponseTool (RED)</name>
  <files>router/tests/unit/mcp/host/tools/create-response.test.ts</files>
  <read_first>
    - router/tests/unit/mcp/host/tools/chat-completion.test.ts (from 15-06 — mirror the 7-test structure)
    - router/src/routes/v1/responses.ts (verify the schema and the inner pipeline)
  </read_first>
  <behavior>
    - Test 1 (D-01): inputSchema deep-equals z.toJSONSchema(ResponsesRequestSchema)
    - Test 2 (D-02/D-03): success → content.text = assistant text extracted from canonical Responses `output` (joined text from `output[i].content[*].text` where `type === 'output_text'`), structuredContent = full /v1/responses-shape body
    - Test 3 (D-04): policy violation → isError:true with `code: 'model_not_in_allowlist'` (or whichever error fires)
    - Test 4 (D-12): stream:true → adapter sees stream:false in canonical
    - Test 5 (D-14): abort propagation via extra.signal
    - Test 6 (D-05/D-06): bufferedWriter row push with protocol='mcp'
    - Test 7 (D-07): metrics counter increment {tool: 'create_response', status_class: ...}
  </behavior>
  <action>
    Create `router/tests/unit/mcp/host/tools/create-response.test.ts` mirroring 15-06's test file structure exactly. Replace `chat_completion` with `create_response` and `ChatCompletionRequestSchema` with `ResponsesRequestSchema`. The FakeAdapter response shape is the same canonical (CanonicalResponse) but the result extraction path goes through `canonicalToResponses` (or the equivalent — verify the actual export from `router/src/translation/` before writing). For the assistant-text extraction in Test 2, write a small inline helper in the test that joins text from the canonical's output array — this is what the production handler will also do.

    Use the same fakes (FakeMcpServer, FakeAdapter, FakeRegistry, FakeBreaker, FakeBufferedWriter, FakeMetrics, fakeReq) as 15-06. Copy-paste is acceptable — keeping the tests independent (no shared helper module) means each tool's tests can evolve without touching siblings.

    Run `pnpm vitest run tests/unit/mcp/host/tools/create-response.test.ts` — all 7 must fail RED.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/mcp/host/tools/create-response.test.ts 2>&1 | grep -E "Cannot find module|FAIL" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 it() blocks present, all RED (module not found)
  </acceptance_criteria>
  <done>RED state for create_response tool established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registerCreateResponseTool (GREEN) + register in buildServerForRequest</name>
  <files>router/src/mcp/host/tools/create-response.ts</files>
  <read_first>
    - router/src/mcp/host/tools/chat-completion.ts (from 15-06 — copy as scaffold)
    - router/src/routes/v1/responses.ts (the existing /v1/responses handler — understand its translation flow)
    - router/src/translation/ (find the canonical → Responses API translator: `grep -rn 'canonicalToResponses\|responses-out' router/src/`)
  </read_first>
  <action>
    Create `router/src/mcp/host/tools/create-response.ts`. Single export: `registerCreateResponseTool(server, deps, capturedReq)`. Structure mirrors `chat-completion.ts` (Plan 06 output) with these differences:

    1. Imports: `ResponsesRequestSchema` from `../../../routes/v1/responses.js`. The canonical→responses translator from `../../../translation/` (verify exact name via grep). The same applyPreflight, openAIRequestToCanonical (Responses translates through the OpenAI surface internally in Phase 13), toOpenAIErrorEnvelope, deriveStatusClass, etc.
    2. Tool name: `create_response`. Description must explicitly state: "Streaming via MCP is NOT supported in v0.11.0 — set stream:false or omit. Use HTTP POST /v1/responses for the future streaming surface (Phase 16)."
    3. inputSchema: `z.toJSONSchema(ResponsesRequestSchema)`.
    4. Handler body: identical pipeline (applyPreflight → coerce stream:false → call adapter.chatCompletionsCanonical → translate canonical to Responses-shape via the dedicated helper). The text stamp (D-03) is the assistant text extracted from `responsesResp.output` walking entries with `type === 'output_text'` and joining their `.text` fields.
    5. Error / finally blocks identical to chat-completion (D-04, D-05, D-06, D-07, D-14).

    After GREEN: open `router/src/mcp/host/plugin.ts` and replace the Wave-4 TODO marker for `create_response` with `registerCreateResponseTool(server, opts, capturedReq);` inside `buildServerForRequest`. Keep the TODOs for the remaining 3 tools.

    Run the unit-test matrix from Task 1 — all 7 must pass GREEN.

    Also run the integration test from 15-05/15-06 (`tests/integration/mcp-host.integration.test.ts`) — `tools/list` should now return 2 tools (`chat_completion`, `create_response`).
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/mcp/host/tools/create-response.test.ts tests/integration/mcp-host.integration.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `router/src/mcp/host/tools/create-response.ts` exists with `registerCreateResponseTool` export
    - `grep -c "z.toJSONSchema(ResponsesRequestSchema)" router/src/mcp/host/tools/create-response.ts` equals 1
    - `grep -c "stream: false" router/src/mcp/host/tools/create-response.ts` is 1 or more (D-12)
    - `router/src/mcp/host/plugin.ts` calls `registerCreateResponseTool` in `buildServerForRequest`
    - All 7 unit tests pass; integration test sees 2 tools in tools/list
    - `pnpm typecheck` clean
  </acceptance_criteria>
  <done>create_response tool live; D-01..D-14 invariants verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-DRIFT | Tampering | Responses schema drift | mitigate | z.toJSONSchema(ResponsesRequestSchema) single source of truth |
| T-15-07-STREAM | Denial of Service | stream:true buffering | mitigate | D-12 coercion (verified by Test 4); description explicitly documents non-support |
| T-15-07-POL | Elevation of Privilege | policy bypass via MCP | mitigate | applyPreflight shared helper |
| T-15-07-ABORT | Denial of Service | upstream continues post-disconnect | mitigate | D-14 AbortController; verified by Test 5 |
</threat_model>

<verification>
- `pnpm vitest run tests/unit/mcp/host/tools/create-response.test.ts` → 7/7 pass
- `pnpm vitest run tests/integration/mcp-host.integration.test.ts` → tools/list returns 2 tools
- `pnpm typecheck` → 0 errors
</verification>

<success_criteria>
1. create_response tool registered with inputSchema = z.toJSONSchema(ResponsesRequestSchema)
2. Success returns dual-shape result with assistant text in content[0].text and full Responses API body in structuredContent
3. Error returns isError:true with structured envelope (D-04 / MCPS-04)
4. stream:true silently coerced (D-12)
5. D-05/D-06 row written; D-07 metric incremented
6. Integration tools/list shows 2 tools
</success_criteria>

<output>
Create `.planning/phases/15-mcp-host-router-as-mcp-server/15-07-SUMMARY.md` noting the canonical→Responses helper name + the extraction logic for the text stamp.
</output>
