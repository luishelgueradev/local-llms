---
phase: 15
plan: 9
type: execute
wave: 4
depends_on: ["15-02", "15-04", "15-05"]
files_modified:
  - router/src/mcp/host/tools/rerank.ts
  - router/tests/unit/mcp/host/tools/rerank.test.ts
autonomous: true
requirements: [MCPS-03, MCPS-04]
user_setup: []
tags: [mcp, tool-handler, rerank]

must_haves:
  truths:
    - "registerRerankTool registers a tool named 'rerank' with inputSchema = z.toJSONSchema(RerankRequestSchema) (D-01)"
    - "Success returns { content:[{type:'text', text:'reranked N docs vs query, model=M'}], structuredContent: <full rerank response with results array> } per D-03"
    - "Errors → isError:true via toOpenAIErrorEnvelope (D-04)"
    - "extra.signal → adapter signal (D-14)"
    - "D-05/D-06/D-07 row + counter"
  artifacts:
    - path: "router/src/mcp/host/tools/rerank.ts"
      provides: "registerRerankTool"
      exports: ["registerRerankTool"]
      min_lines: 90
    - path: "router/tests/unit/mcp/host/tools/rerank.test.ts"
      provides: "Unit-test matrix"
      min_lines: 100
  key_links:
    - from: "router/src/mcp/host/tools/rerank.ts"
      to: "router/src/routes/v1/rerank.ts"
      via: "import { RerankRequestSchema }"
      pattern: "RerankRequestSchema"
---

<objective>
Register the `rerank` MCP tool wrapping the existing `POST /v1/rerank` capability (v0.10.0 Phase 11). The D-03 stamp is `"reranked N docs vs query, model=M"` — short summary; full results array (per-doc scores + indices) rides in structuredContent.

Purpose: Expose reranker — needed by retrieval-bound agents that want to score candidate documents after an initial retrieval. Establishes the third independent capability surface (after chat + embeddings).
Output: `rerank` tool registered; tools/list now returns 4 tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md
@router/src/routes/v1/rerank.ts
@router/src/dispatch/preflight.ts
@router/src/mcp/host/plugin.ts
@router/src/mcp/host/tools/create-embedding.ts

<interfaces>
From router/src/routes/v1/rerank.ts:58-68:
```typescript
export const RerankRequestSchema = z.object({
  model: z.string(),
  query: z.string(),
  documents: z.array(z.string()),
  top_n: z.number().optional(),
}).passthrough();
export type RerankRequest = z.infer<typeof RerankRequestSchema>;
```

From router/src/backends/adapter.ts:
```typescript
rerank(query: string, documents: string[], model: string, signal: AbortSignal): Promise<RerankResponse>;
// Returns { model, results: [{ index, relevance_score }], usage: { total_tokens } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit-test matrix for registerRerankTool (RED)</name>
  <files>router/tests/unit/mcp/host/tools/rerank.test.ts</files>
  <read_first>
    - router/tests/unit/mcp/host/tools/create-embedding.test.ts (Plan 08 — scaffold)
    - router/src/routes/v1/rerank.ts (verify schema fields + adapter signature)
    - router/tests/integration/rerank.test.ts (existing FakeAdapter for rerank shape — copy the inline FakeAdapter literal)
  </read_first>
  <behavior>
    - Test 1 (D-01): inputSchema deep-equals z.toJSONSchema(RerankRequestSchema)
    - Test 2 (D-03 stamp): success result content[0].text matches `'reranked N docs vs query, model=M'`; structuredContent.results has N entries with index + relevance_score
    - Test 3 (D-04): policy violation → isError:true
    - Test 4 (D-14): abort propagation
    - Test 5 (D-05/D-06): bufferedWriter row push
    - Test 6 (D-07): metrics counter increment {tool: 'rerank'}
    - Test 7 (passthrough): args.documents reaches adapter.rerank(..., documents, ...) intact
  </behavior>
  <action>
    Create test file mirroring 15-08's structure. FakeAdapter.rerank returns `{ model: 'rerank-local', results: [{ index: 0, relevance_score: 0.9 }, { index: 1, relevance_score: 0.1 }], usage: { total_tokens: 50 } }`. For Test 2, with documents=['a','b'] the expected stamp is `'reranked 2 docs vs query, model=rerank-local'`.

    Run `pnpm vitest run tests/unit/mcp/host/tools/rerank.test.ts` — all 7 RED.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/mcp/host/tools/rerank.test.ts 2>&1 | grep -E "Cannot find module|FAIL" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 it() blocks, all RED
  </acceptance_criteria>
  <done>RED state for rerank tool established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registerRerankTool (GREEN) + register in buildServerForRequest</name>
  <files>router/src/mcp/host/tools/rerank.ts</files>
  <read_first>
    - router/src/mcp/host/tools/create-embedding.ts (Plan 08 — scaffold)
    - router/src/routes/v1/rerank.ts (lines 100-200 — the canonical pipeline use)
  </read_first>
  <action>
    Create `router/src/mcp/host/tools/rerank.ts`. Single export: `registerRerankTool(server, deps, capturedReq)`. Differences from create-embedding.ts:

    1. Imports `RerankRequestSchema` from `../../../routes/v1/rerank.js`.
    2. Tool name: `rerank`. Description: "Rerank candidate documents against a query. Returns per-document relevance_score in structuredContent.results; content carries a one-line stamp."
    3. inputSchema: `z.toJSONSchema(RerankRequestSchema)`.
    4. Handler calls `adapter.rerank(args.query, args.documents, entry.backend_model, controller.signal)` (verify the exact signature in `adapter.ts` — some adapters might pass args as an object).
    5. Build the D-03 stamp: `const stamp = \`reranked ${args.documents.length} docs vs query, model=${resp.model}\`;`. Result shape: `{ content: [{ type: 'text', text: stamp }], structuredContent: resp }`.
    6. Error/finally blocks identical to other tools. For tokens_in/out: rerank uses `resp.usage.total_tokens` only (no input/output split — verify). Store it as tokens_in if the row builder requires it, or leave both null.

    Register the tool in `plugin.ts` `buildServerForRequest` (replace TODO marker).

    Run unit tests + integration test (tools/list returns 4 tools).
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/mcp/host/tools/rerank.test.ts tests/integration/mcp-host.integration.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `router/src/mcp/host/tools/rerank.ts` exists with `registerRerankTool` export
    - `grep -c "z.toJSONSchema(RerankRequestSchema)" router/src/mcp/host/tools/rerank.ts` equals 1
    - `grep -c "reranked " router/src/mcp/host/tools/rerank.ts` is 1 or more (D-03 stamp)
    - `router/src/mcp/host/plugin.ts` calls `registerRerankTool` in buildServerForRequest
    - 7/7 unit tests pass; tools/list returns 4 tools
    - `pnpm typecheck` clean
  </acceptance_criteria>
  <done>rerank tool live; D-03 stamp + structuredContent shape verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09-DRIFT | Tampering | rerank schema drift | mitigate | z.toJSONSchema source of truth |
| T-15-09-ABORT | Denial of Service | upstream rerank continues post-disconnect | mitigate | D-14 AbortController |
| T-15-09-POL | Elevation of Privilege | policy bypass | mitigate | applyPreflight shared |
</threat_model>

<verification>
- `pnpm vitest run tests/unit/mcp/host/tools/rerank.test.ts` → 7/7 pass
- `pnpm vitest run tests/integration/mcp-host.integration.test.ts` → tools/list returns 4 tools
- `pnpm typecheck` → 0 errors
</verification>

<success_criteria>
1. rerank tool registered with passthrough schema
2. Success returns D-03 stamp + full results array in structuredContent
3. Errors return isError:true (D-04)
4. D-05/D-06/D-07 + D-14 wired
5. tools/list shows 4 tools (chat_completion, create_response, create_embedding, rerank)
</success_criteria>

<output>
Create `.planning/phases/15-mcp-host-router-as-mcp-server/15-09-SUMMARY.md` documenting the rerank-specific stamp format.
</output>
