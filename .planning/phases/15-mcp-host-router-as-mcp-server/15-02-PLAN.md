---
phase: 15
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - router/src/dispatch/preflight.ts
  - router/tests/unit/dispatch/preflight.test.ts
autonomous: true
requirements: [MCPS-01]
user_setup: []
tags: [mcp, preflight, refactor-prep]

must_haves:
  truths:
    - "applyPreflight(model, opts) is exported from router/src/dispatch/preflight.ts and runs registry.resolve → applyPolicyGate → breaker.check in that fixed order"
    - "applyPreflight returns { entry, breakerState } per Option A (sentinel return) so HTTP callers preserve their Retry-After stamp before throwing"
    - "applyPreflight throws RegistryUnknownModelError on unknown model, AllowlistViolationError on allowlist violation, CloudNotAllowedError on cloud_not_allowed entries — preserving Phase 14 envelope mapping verbatim"
    - "Unit test matrix covers happy path + each error branch + breaker state=closed/half-open/open"
  artifacts:
    - path: "router/src/dispatch/preflight.ts"
      provides: "applyPreflight helper consolidating resolve + gate + breaker"
      exports: ["applyPreflight", "ApplyPreflightOpts", "ApplyPreflightResult"]
      min_lines: 40
    - path: "router/tests/unit/dispatch/preflight.test.ts"
      provides: "Unit-test matrix proving D-09 preflight semantics"
      min_lines: 80
  key_links:
    - from: "router/src/dispatch/preflight.ts"
      to: "router/src/policy/gate.ts"
      via: "import { applyPolicyGate }"
      pattern: "applyPolicyGate"
    - from: "router/src/dispatch/preflight.ts"
      to: "router/src/config/registry.ts"
      via: "RegistryStore type + .resolve()"
      pattern: "registry.resolve"
    - from: "router/src/dispatch/preflight.ts"
      to: "router/src/resilience/circuitBreaker.ts"
      via: "CircuitBreaker.check"
      pattern: "breaker.check"
---

<objective>
Create the shared `applyPreflight(model, opts)` helper per D-09. The helper consolidates the `registry.resolve` → `applyPolicyGate` → `breaker.check` triplet that is currently duplicated identically across all 5 HTTP routes (`chat-completions.ts:161/225/331`, `messages.ts:197/228/285`, `embeddings.ts:236/240`, `rerank.ts:140/142`, `responses.ts:369/371`). The helper will also be called from every MCP tool handler in Wave 4, ensuring policy + breaker semantics are identical across protocols.

Purpose: Single source of truth for the preflight pipeline; Phase 14 invariant (gate-before-breaker, breaker counter unchanged by 403) is preserved across both HTTP and MCP surfaces.
Output: New module `router/src/dispatch/preflight.ts` + unit-test matrix that proves resolve/gate/breaker ordering and the Option A sentinel return shape (so Plan 03 can refactor HTTP routes without behavior drift).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md
@.planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md
@router/src/policy/gate.ts
@router/src/config/registry.ts

<interfaces>
<!-- The trio being consolidated. Extracted verbatim from chat-completions.ts (lines 161, 225, 331-338). -->

From router/src/policy/gate.ts:
```typescript
export function applyPolicyGate(
  policies: Registry['policies'],
  entry: ModelEntry,
  requested_model: string,
): void;
// Throws AllowlistViolationError or CloudNotAllowedError on violation; void on success.
```

From router/src/config/registry.ts:
```typescript
export interface RegistryStore {
  get(): Registry;
  resolve(name: string): ModelEntry;  // throws RegistryUnknownModelError when not found
  getCreatedAtSec(): number;
}
```

From router/src/resilience/circuitBreaker.ts (already used by all 5 routes):
```typescript
export interface CircuitBreaker {
  check(backend: string): Promise<{ state: 'closed' | 'half-open' | 'open' }>;
  recordSuccess(backend: string): void;
  recordFailure(backend: string): void;
}
```

The trio's existing site (router/src/routes/v1/chat-completions.ts:161/225/331-338):
```typescript
const entry = opts.registry.resolve(body.model);            // line 161
// ...capability gate / cloud-cap guard (stay in route)...
applyPolicyGate(opts.registry.get().policies, entry, body.model);  // line 225
// ...
const breakerResult = await opts.breaker.check(entry.backend);     // line 331
if (breakerResult.state === 'open') {
  void reply.header('Retry-After', String(opts.breakerCooldownSec));
  throw new BreakerOpenError(entry.backend, opts.breakerCooldownSec);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit-test matrix for applyPreflight (RED)</name>
  <files>router/tests/unit/dispatch/preflight.test.ts</files>
  <read_first>
    - router/src/policy/__tests__/gate.test.ts (if it exists — read for vitest style + fake-registry/fake-breaker pattern)
    - router/src/config/registry.ts (verify RegistryStore shape — especially how `resolve` throws RegistryUnknownModelError)
    - router/src/errors/envelope.ts lines 29-36, 138-148, 299-320 (RegistryUnknownModelError, BreakerOpenError, AllowlistViolationError, CloudNotAllowedError signatures)
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-CONTEXT.md §D-09
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md §Pattern 5 (applyPreflight Helper)
  </read_first>
  <behavior>
    - Test 1: happy path — registry resolves entry, no allowlist violation, breaker state='closed' → returns `{ entry, breakerState: 'closed' }`; no exception
    - Test 2: registry.resolve throws RegistryUnknownModelError → applyPreflight propagates the same error class (does NOT wrap)
    - Test 3: applyPolicyGate throws AllowlistViolationError (allowlist non-empty, model not in list) → applyPreflight propagates; breaker.check is NEVER called (assert via spy: `checkSpy.toHaveBeenCalledTimes(0)`)
    - Test 4: applyPolicyGate throws CloudNotAllowedError (entry.policy.cloud_allowed=false, backend='ollama-cloud') → applyPreflight propagates; breaker.check NEVER called
    - Test 5: breaker.check returns state='half-open' → applyPreflight returns `{ entry, breakerState: 'half-open' }` without throwing (D-09 sentinel pattern; caller decides how to react)
    - Test 6: breaker.check returns state='open' → applyPreflight returns `{ entry, breakerState: 'open' }` WITHOUT throwing (Option A from RESEARCH §Pattern 5 — caller throws BreakerOpenError with its own cooldownSec context)
    - Test 7: ordering invariant — when applyPolicyGate throws, registry.resolve was called exactly once and breaker.check was never called (proves gate-before-breaker invariant from Phase 14 POL-05)
  </behavior>
  <action>
    Create `router/tests/unit/dispatch/preflight.test.ts` with the 7 vitest test cases above. Use vi.fn() for fake RegistryStore.resolve, fake registry.get() (returning a Registry with a single ModelEntry + a policies block), fake CircuitBreaker.check. Import the types from `router/src/config/registry.ts` and `router/src/resilience/circuitBreaker.ts`. The helper file `router/src/dispatch/preflight.ts` does NOT exist yet — tests will RED first, then Task 2 implements them GREEN.

    Use `expect().rejects.toThrow(RegistryUnknownModelError)` (vitest async matcher) — the helper is async because `breaker.check` is async. Tests 3/4 also use `.rejects.toThrow(AllowlistViolationError)` / `.rejects.toThrow(CloudNotAllowedError)`.

    Fake registry shape: `{ get: () => ({ policies: { default: { model_allowlist: [...] } }, models: [...] }), resolve: vi.fn().mockReturnValue(entry) | mockImplementation(() => { throw new RegistryUnknownModelError('x', ['a']); }) }`. ModelEntry minimum: `{ name: 'chat-local', backend: 'ollama', backend_model: 'qwen2.5:7b', capabilities: {...}, policy: { cloud_allowed: true } }`.

    Run `pnpm vitest run tests/unit/dispatch/preflight.test.ts` — all 7 tests MUST fail with "Cannot find module" or similar because the implementation does not exist.
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/dispatch/preflight.test.ts 2>&1 | grep -E "Cannot find module|FAIL" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `router/tests/unit/dispatch/preflight.test.ts` exists
    - File defines exactly 7 `it(...)` cases matching the behavior list above
    - All 7 tests FAIL (RED state) — the failure reason is "Cannot find module '../../src/dispatch/preflight'" or equivalent
    - File commits its imports from `../../src/dispatch/preflight.js` (note the `.js` extension per the project's ESM convention)
  </acceptance_criteria>
  <done>RED state confirmed; matrix covers all D-09 invariants (gate-before-breaker, sentinel return for open state, error class fidelity).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applyPreflight (GREEN)</name>
  <files>router/src/dispatch/preflight.ts</files>
  <read_first>
    - router/src/policy/gate.ts (entire file — short, ~45 lines)
    - router/src/config/registry.ts lines 200-225 (resolve + RegistryUnknownModelError re-export)
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-RESEARCH.md §Pattern 5
    - .planning/phases/15-mcp-host-router-as-mcp-server/15-PATTERNS.md §preflight.ts
  </read_first>
  <action>
    Create `router/src/dispatch/preflight.ts` with the exact signature from RESEARCH §Pattern 5 (Option A sentinel return):

    Module header comment: reference Phase 15 + MCPS-01..06 + CONTEXT.md D-09. State explicitly that the helper preserves the Phase 14 gate-before-breaker invariant (POL-05) and that breakerState is returned as a sentinel rather than thrown so HTTP callers can stamp `Retry-After` before raising `BreakerOpenError`.

    Exports:
    - `interface ApplyPreflightOpts { registry: RegistryStore; breaker: CircuitBreaker; }`
    - `interface ApplyPreflightResult { entry: ModelEntry; breakerState: 'closed' | 'half-open' | 'open'; }`
    - `async function applyPreflight(requested_model: string, opts: ApplyPreflightOpts): Promise<ApplyPreflightResult>`

    Implementation (must match the unit-test matrix from Task 1):
    1. `const snapshot = opts.registry.get();`
    2. `const entry = opts.registry.resolve(requested_model);` — let `RegistryUnknownModelError` propagate
    3. `applyPolicyGate(snapshot.policies, entry, requested_model);` — let `AllowlistViolationError` / `CloudNotAllowedError` propagate
    4. `const breakerResult = await opts.breaker.check(entry.backend);`
    5. `return { entry, breakerState: breakerResult.state };` — DO NOT throw on state='open'; return the sentinel and let the caller decide (HTTP route stamps Retry-After then throws; MCP tool throws without header).

    Type imports use type-only syntax: `import type { RegistryStore, ModelEntry } from '../config/registry.js';` and `import type { CircuitBreaker } from '../resilience/circuitBreaker.js';`. `applyPolicyGate` is a runtime import: `import { applyPolicyGate } from '../policy/gate.js';`.

    Run `pnpm vitest run tests/unit/dispatch/preflight.test.ts` — all 7 tests must now PASS (GREEN).
  </action>
  <verify>
    <automated>cd router && pnpm vitest run tests/unit/dispatch/preflight.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `router/src/dispatch/preflight.ts` exists with the exact exports listed above
    - All 7 unit tests pass (GREEN)
    - `grep -c 'opts.registry.resolve' router/src/dispatch/preflight.ts` equals 1
    - `grep -c 'applyPolicyGate' router/src/dispatch/preflight.ts` equals 1
    - `grep -c 'opts.breaker.check' router/src/dispatch/preflight.ts` equals 1
    - The function does NOT throw on `state === 'open'` (verified by Test 6 passing without `expect().rejects`)
    - `pnpm typecheck` clean
    - No imports from anything in `router/src/mcp/` (helper is protocol-agnostic)
  </acceptance_criteria>
  <done>Helper implemented; all 7 unit tests green; protocol-agnostic (used by both HTTP routes and MCP tools).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → applyPreflight | requested_model is opaque string input; resolve/gate validate it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-EOP | Elevation of Privilege | preflight order regression | mitigate | Test 7 asserts gate-before-breaker ordering invariant; Plan 03 cannot proceed until this test is green |
| T-15-02-INFO | Information Disclosure | error class fidelity | mitigate | Tests 2/3/4 verify the original error classes propagate (no error wrapping that would change HTTP status mapping) |
</threat_model>

<verification>
- `pnpm vitest run tests/unit/dispatch/preflight.test.ts` → 7/7 pass
- `pnpm typecheck` → 0 errors
- `grep -rn 'applyPreflight' router/src/` → at least 1 hit (the implementation); after Plan 03 this number grows
</verification>

<success_criteria>
1. `applyPreflight` helper module exists with the exact contract from RESEARCH §Pattern 5
2. Unit-test matrix (7 cases) covers happy path + 3 error branches + 3 breaker states
3. Gate-before-breaker invariant verified by Test 7 (mirrors Phase 14 POL-05 assertion)
4. Helper is protocol-agnostic (no MCP- or HTTP-specific imports)
5. Plan 03 (HTTP route refactor) and Wave 4 plans (MCP tool handlers) can safely import this helper without further investigation
</success_criteria>

<output>
Create `.planning/phases/15-mcp-host-router-as-mcp-server/15-02-SUMMARY.md` documenting:
- The chosen return shape (Option A sentinel) and why (preserves HTTP byte-identical Retry-After behavior)
- The 7-test matrix with each case's purpose
- A note that BreakerOpenError is NOT thrown by the helper — callers throw it themselves with their own cooldown context
</output>
